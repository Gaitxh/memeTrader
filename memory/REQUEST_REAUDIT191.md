# 191 continuity, 2026-09-16

Newest user instruction is a restart of the full request audit, not acceptance of earlier narrow work. Read the latest 125-line attachment as 94 distinct retrospective case labels. No future data as entry features; manual review only, no scheduler or automatic parameter promotion. One production writer, Paper only, Live locked, current 1000U-per-arm funding period preserved.

Casebook `data/research/goal_cases190/cases_20260916T090250Z.{json,md}`: 70 current-period canonical matches, 24 absent, 44 evaluated without admission, 4 admitted without position, 22 with positions. This is not a historical acquisition coverage percentage because the current database begins 2026-09-12. Two case trajectories show same-pool low-to-tradable liquidity crossing within about 90s while price fell. This is the causal motivation, not a learned winner label.

New `depth191_first_tradable_v1` appended Paper arm in `depth_cross191.py`, Store registration and Runtime passive-frame signal delivery. Uses existing shared Dex/Gecko observations, at most 256 in-memory pool states, no new HTTP/DB scan. First low below current original-pool floor, subsequent same-pool >=max(2000U,2x floor) within300s and pool age<=600s; fresh as-of, positive price, >=2 buys, >=1 sell and buys>=sells. It routes through shared safety and next observed fill. Uniform 20U/8-slot policy overrides any smaller registered size, so this arm explicitly registers 20U/8. Never claim small 1U exposure. Four new tests pass, including full cohort next-quote BUY. Broader legacy `test_cohort_store.py` has two pre-existing failures (5U assertion vs uniform20 and incomplete Runtime mock). Do not change the user-authorized uniform size to satisfy them.

At 10:01Z added `depth191_first_tradable_fast5_v1` as same-frozen-signal 5-minute max-hold challenger to the original 30-minute arm; all other entry, safety, stop, slippage and sizing terms remain equal. Five focused tests pass including two positions on the exact same source fill after a next-observed quote. Second arm activated separately at snapshot915648, original remains914128. Subsequent snapshot915722, health running, Paper/Live lock unchanged, zero natural fast5 positions. Compare only future jointly eligible source BUYs, not all historical positions or projected cross-arm counts.

At 09:42Z a two-hour `deep_cycle_report.py` child was actually running although the user requires manual research initiation. `runtime.py` now defaults `optimization151.review_enabled` and `supervise_enabled` to false when omitted; essential market/held/exit loops are untouched.

Deployment acceptance 09:44-09:45Z: restarted Paper child under existing supervisor, unchanged funding-20260906-v002-final-1000, Paper/Live lock intact. New arm addition ID370 activated at snapshot914128, later snapshot914190 and evaluation frontier advancing, 0 new-arm positions in short window. /health, /api/live, /api/performance all 200 with direct no-proxy localhost calls. No scheduled research child after restart. Passive queue 5 batches processed/0 dropped, p95 wait ~2.51s; no held targets, so no comparable held latency claim. Natural trading/economics pending.

Update-history visibility defect: Web used the last100 JSON array elements, hiding 191/190/189 reports written at the front. `ChainWebData.update_history` now sorts by recorded_at and original index descending, takes100. Two focused tests pass. Web reloaded; real `/api/updates` now returns 191,190,189 first. Health still running, original funding and Paper/Live lock unchanged.

Full itemized evidence, scope and open economic questions: `docs/PROJECT_CONTEXT/GOLD_CASE_REAUDIT_AND_DEPTH191_20260916.md`. Must verify deployment separately before claiming it; then wait for natural independent token outcomes. Pending: 24 missing addresses' historical source availability, 44 unadmitted cohort counterfactuals, first-tradable depth arm's trigger/PNL, robust exit comparisons, and same-load acquisition/held latency. No blanket claim all requests or profitability solved.
