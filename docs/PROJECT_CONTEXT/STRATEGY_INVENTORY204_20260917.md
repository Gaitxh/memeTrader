# V6 strategy inventory and natural Paper check, 2026-09-17

The manually run, read-only `scripts/audit_strategy_inventory204.py` generated `memory/STRATEGY_INVENTORY_20260917.json` from the configured Paper database. Its focused test passed. The artifact records all 13 V6 registrations (1,398 base-policy **version rows**, not independent strategies), plus each of the current funding period's 502 effective policies with registered/effective behavior hash, activation frontier, admission mode, lifecycle, decision counts, latest account snapshot, position count and distinct position tokens. Database registration timestamps of older versions cluster at the 2026-09-12 import; they are **not** asserted to be original deployment times. Historical versions' economic outcomes remain separated rather than added to current funding results.

Current effective-policy inventory: 502 arm IDs, 395 distinct behavior hashes and 22 duplicate-hash groups. Lifecycle counts: 81 ACTIVE_FORWARD, 183 PAUSED_NEW_ENTRY, 85 FAILED_ACCOUNT_DEPLETED, 81 FAILED_FORWARD_EXPECTANCY, 61 RETIRED_DUPLICATE, 7 DOMINATED_IN_PAIRED_TEST and 4 RETIRED_UNREACHABLE_CONTRACT. Eighteen active arms have no position yet; 50 active arms have negative realized PnL. Neither arm count nor contract-hash count is an independent economic sample. These lifecycle labels record existing governance, not a fresh claim that each retirement was correctly caused by strategy alpha rather than engineering or market conditions. A per-family, per-frontier keep/revise/retire judgment remains open.

The depleted `alpha149_nonbsc_flow_v1` is an especially clear current opportunity-cost example: 542 closed/written-off positions on 542 token IDs, realized -999.59U, 23 recorded writeoffs, latest cash 0.4146U. Its 19 cash-rejected arm decisions in the 16:48-17:05 UTC cohort window are therefore account-level, not a shared cash gate. The registered `1U` name is historical; the user-authorized effective Paper contract uniformly uses 20U per ordinary order. Do not inject money or erase this evidence to create more trades. Other related flow arms also lost heavily (`alpha149_deep_pool_flow_v1` -789.98U, `alpha149_mid_band_flow_v1` -985.69U); naive reactivation is not a defensible fix.

There is a real right tail, but no proven repeatable alpha from these aggregates. `trajectory144_trend_runner_v1`: 222 terminal tokens, +539.80U realized, median terminal -2.696U, +133.36U after removing its single best result, and 26 writeoffs. `trajectory187_solana_runner_v1`: 46 terminal tokens, +272.94U realized, median -1.947U, +183.14U without its best result, and 3 writeoffs. These arms may be worth preserving and comparing, but their overlapping source BUYs and fat tails require token-clustered paired analysis, drawdown and full-cost accounting before promotion.

The newly deployed `trajectory198_solana_confirmed_stop_v1` now has three closed, distinct-token same-source-BUY pairs against 187: -2.655U, -5.134U and -4.239U. All three terminal reasons and PnL equal the parent. Two hard-stop positions persisted a `fresh_stop198` first-mark state, so the distinct-frame confirmation mechanism was reached; the later confirmed/catastrophic stop or common exit produced no realized differentiation yet. `depth199_first_floor_cross_v1` still has no admitted decision or position. `activity200_old_pool_tempo_fast5_v1` has one admitted decision, but cohort 36063 was rejected before BUY by explicit safety findings (`dangerous_balance_mutable_authority`, `dangerous_default_account_state_upgradable`, `dangerous_freezable`, `dangerous_mintable`, `dangerous_transfer_hook_upgradable`). No admission or safety gate was loosened to make it trade.

**Disposition now:** retain 144/187 as observational controls, keep 198-200 as separate small Paper trials with no profitability claim, and flag the depleted/losing flow family for reasoned revision or retirement after a family-level same-source comparison. Do not promote a policy on positive realized total alone, retire a zero-trade new arm before its first eligible safe opportunity, or use retrospective hot-token labels as live features. Next analysis must quantify each candidate's net distribution, cohort overlap, safe signal-to-fill conversion and winner-capture versus writeoff tail at its own activation frontier. The inventory is reproducible via the manual script, does not run in the trading loop and adds no API calls.
