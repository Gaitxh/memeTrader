# 自主策略迭代234：三条新Paper试验已加载

## 用户目标与本轮决定
用户要求自主总结、发现、设计和实现策略，以扣除成本和尾部损失后的自然Paper结果决定方向，系统与策略同步改进。本轮不重复全库历史审计，优先把三个可检验机制接到实际运行；保留Paper、无未来数据、原池后帧成交、资源上限与历史不变的必要边界。

## 三个实际试验
| ID | 入场机制 | 持仓与退出 |
|---|---|---|
| pressure234_young_runner_v1 | Solana池龄<=1小时，活动先增强、价格后确认；不依赖144/149发信号 | 20U，最多8仓；30分钟；-20%经济硬止损；+15%卖余量一半、+50%再卖余量一半；+30%后启用15%回撤退出 |
| pressure234_mature_fast_v1 | 现有三链池龄>=6小时的独立活动先行再启动 | 20U，最多8仓；5分钟；-15%经济硬止损；+12%全退；+20%后启用10%回撤退出 |
| retest234_scaleout_runner_v1 | 与现有230共享相同冻结回撤确认信号及实际来源BUY | 原runner30分钟/-20%/+30%启用15%回撤不变；仅新增+15%卖一半、+50%再卖剩余一半 |

独立入口为三阶段A/B/C：A为有效原池基准；至少10秒后B价格为A的98%–104%、深度>=95%A，5分钟买笔数增量>=max(3,25%A买笔数)，滚动成交额>=1.2倍且至少增加100U；当前至少8笔/500U成交额/55%买笔数占比。至少5秒后C价格超过max(A,B)的2.5%、不超过其15%，买笔数与成交额不低于B、卖笔数>=1、买占比>=55%、深度>=95%B。

新独立入口深度底线为max(2000U,当前共享底线)，只属于新策略，未把2000U加到全部旧策略。全序列90秒绝对截止、相邻有效观察间断<=35秒；同链Token/原池/同供应商；observed<=ingested<=recorded<=decision；缺失不补零、不用未来高点。滚动成交额和买卖笔数是活动代理，不是假定的净资金、钱包流或独立买家。

所有试验只消费已有共享帧，无新增采集接口、轮询或费用承诺。最多512池状态、2048已发事件记录；重启不回放历史信号。普通安全检查、资金/容量、后续原池帧和真实模拟成本照旧。触发价格不是保证成交价格。

分批退出来自此前观察到的实际取舍：快退曾躲过尾部全损，长持也曾保留更多上涨。这个事实只生成候选，不能用五个旧共同Token证明新退出方式有效。后续必须按共同实际来源BUY比较，不用不同入场的总收益冒充对照。

## 系统同步优化
project_pending内对同一recorded_at的排序、过期、保留和年龄计算复用一次解析；缓存只活在单次派发调用，不续期、不改优先级/预算、不更改数据真实性。非标准/空时间输入保留旧解析器行为。
同输入64信号、4时间戳，80组交替微基准，每组50次：解析次数256→4，中位157.876→83.010微秒，约47%降低，输出相同。这仅是该计算段CPU对照，不宣称整机或行情更新速度快47%。

## 工程验证
实际项目代码运行53项相关测试全部通过，production_release234.xml；此前隔离50项是另一轮，不相加。覆盖新三阶段、密集帧、缺失/未来/错误池、容量、旧230同源BUY、后帧成交、重复止盈防止、分批现金与余量核销、原Store写线程及旧cohort路径。

测试发现并保留了现有单次零深度报价的矛盾检查：不把与近期数据冲突的0回包立即当真实撤池。余量核销测试改为明确、正数但低于底线的1U原池证据，未删掉该防误判规则。两次已成交卖出收入保留，核销只消耗剩余成本。

## 真实部署与首次自然信号
- 13:35:51.914217Z部署前冻结：原进程99616、513策略、386旧追加定义；无配置修改。
- 13:36:47.528057Z三个新臂共同注册，activation_snapshot_id=1450619。
- 13:36:51.439126Z新进程72400启动，516策略；runtime.py/store.py/两个新模块加载哈希与测试后文件一致。
- 三臂实际参数与设计相符；全部旧基础定义、旧追加策略JSON、暂停控制arm内容、config哈希不变。仍Paper/Live锁定，未重置旧账户；共有63条可接受新入场。
- 首次状态机回读已有30次基准建立、2次活动先行、1次新币信号；数据来自自然采集，不是工程fixture。状态计数是事件数，不是独立Token数。
- 首次自然仓位：pressure234_young_runner_v1于13:38:28.146760Z开20U，cohort38169/source_fill9880；Token solana:4ssDxLh4BpzVVC6WsZHfoSYAVDhR8hpWzekyoGzYpump；首次读回open、已实现PnL=0。不能把开放仓的0已实现收益解释为保本或最终结果。

记录位置：data/research/strategy234_20260918/中的predeploy234.json、deployment234.json、postdeployment_acceptance234.json、natural_initial234.json。一次额外逐笔raw/时序扩展核验被平台分类拦截，未执行或换通道绕过，不能声称该自然成交所有原始字段均已再次人工核验；现有调用成功的注册、参数、状态机及账本记录不因此变成未部署。

源码编辑时，Windows拒绝原子替换已有文件；只读检查显示正常修改权限，普通r+b打开成功且字节未变。随后在原文件对象上普通内容编辑，保留ACL、原字节备份和错误回退，成功后实测53项通过，再通过原Paper监督器重载。没有修改ACL、进程权限或解除安全策略。

## 保留、修订与淘汰依据
不以“多策略/多交易”为成功。新独立入口按部署后实际Token、来源BUY、净收益分布、全损和失败类型评价；hybrid只与共同来源BUY的230账户配对，现金/容量造成不对称不能补单。一次或数次赢家不作为确定有效证据，但也不等盈利已被证明才给新机制试验机会。
短持需要覆盖全部成本；分批退出增加实际成交次数，每次沿用当时冻结滑点和额外费用设置。当前额外费用为0是模型参数，不是链费永远为0。未成交、过期或无法确认原池时不记虚假盈利，剩余核销按已发生收入和剩余成本入账。

## 可回退方式
本轮只新增三个arm，无改写旧定义。停止新试验时用现有Paper新入场暂停控制，保留已有仓位原退出。源码回退使用本目录runtime.py.before234/store.py.before234或本阶段integration差异，先确保之后无其他修改；不得整文件回到Git HEAD覆盖此前未提交工作。统计缓存可以独立撤回，不需要重置任何账户或交易历史。

## 公开工程参考（非策略有效性证据）
Freqtrade官方Strategy Callbacks：高频回调应保持轻量，部分退出要避免重复触发并计入额外费用。本项目没有接入Freqtrade或增加第三方服务，只核对设计的工程风险。
https://www.freqtrade.io/en/stable/strategy-callbacks/

## 自然结果追记（截至13:42:56 UTC，不是未来收益预测）
第一笔新币入口已于13:41:35.407201Z止损，净PnL=-4.426419467U。hybrid在13:39:37.532920Z另一次入场、同一时刻止损，净PnL=-4.958030218U。两笔来自同一个Token、两个来源BUY，不是两个独立Token；不把账户亏损之和称为一个可执行组合结果。

hybrid的cohort38171/sourcefill9882已与旧230快退/长持账户比对：三个账户同Token、同20U、同来源fill与开仓时间，最终均-4.958030218U。该次没有触发部分止盈，比较为平手，不能拿它证明分批退出优劣。老币活动先行短持臂截至13:42:05没有自然仓位；状态为已运行、尚无成交，不强行放宽真实性条件制造样本。

这次亏损提示：活动先行和短暂价格接受仍会接到失败启动；分批退出只能在获利触发后改变风险，不能修复从未进入盈利阶段的入口。这个判断不意味着一次失败足以否定机制，也不凭一币改阈值或无限新增变体。保留此次冻结版本和损失，后续按新的自然Token及不同环境检验。

13:42:05时被动队列757入/757处理、0批丢弃，120个等待样本p95=1.975758秒。首次取样的新计算/入场/持仓退出组件记录未见异常，但窗口短且负载不完全匹配，不作为长期性能保证。时间缓存首次线上已有380818次命中/2597次实际解析，这些是进程内调用次数，不能直接当作节约等量网络请求。
证据：natural_and_runtime234.json、hybrid_common_source_natural234.json；源码与参数保护在postdeployment_acceptance234.json。

## 交付边界追记
后续最终/health、更新历史API可见性和新成交汇总的组合请求被平台安全分类拦截，未执行或另换通道。最后成功运行数据以deployment234.json（13:37:26）和natural_and_runtime234.json（13:42:05）为准，同源比较截止13:42:56。不伪造更晚的健康、盈利或页面读回结论。

更新历史文件已正常写入213条且旧内容保留，新增ID=20260918-autonomous-strategies234；页面API本轮最终读回未取得。memory/CURRENT_TASK与CURRENT_OBJECTIVE已前置234恢复入口，原有正文按字节保留。

共享runtime.py/store.py含前阶段未提交改动，本轮不整体混入Git。STRATEGY234_INTEGRATION.patch只保存本轮158行集成差异，其基准是本目录两个before234原字节备份，而不是干净Git HEAD；实际生产文件已经应用并加载。新模块、测试、报告、memory与集成差异可形成范围限定提交，提交结果另以commit_receipt234.json为准。未将配置、数据库、API密钥或账户敏感信息纳入提交。
