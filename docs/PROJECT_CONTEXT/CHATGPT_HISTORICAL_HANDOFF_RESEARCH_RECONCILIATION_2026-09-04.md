# Historical GoT_MEME Handoff Research Reconciliation with V5

Date: 2026-09-04
Status: `SELECTIVE STRUCTURAL REVIEW / CURRENT WORKSPACE REMAINS AUTHORITATIVE`

## 1. Scope and authority

The July GoT_MEME handoff is split across a CORE archive plus ORIGINALS/EXTRACTED volumes. A file-level and decision/limitation-line index was run across the CORE and all available EXTRACTED volumes for research related to:

- DexScreener/technical indicators;
- immutable/forward data factories;
- objective feasibility/no-strategy diagnosis;
- wallet behavior/activity/event decoding;
- causal flow/multi-asset cash flow/historical schema;
- semantic-oracle/external-gold/failed-transaction handling;
- liquidity/rug/exit/execution/momentum.

This is a selective reconciliation, not a claim that every historical bundle byte, fixture and statistic has been independently revalidated in this turn. Current `E:\memeTrader` code, r6 forward SQLite, immutable registrations, current objective/ledger and current protocol documentation take precedence. Historical findings become candidates only when they fit a current strict-forward hypothesis.

## 2. Historical asset classes and disposition

### 2.1 Forward/immutable data-factory work

**Disposition: RETAIN AS DESIGN FOUNDATION.**

Reusable principles:

- source/observed/ingested/recorded/available clocks must be explicit;
- immutable cohort/attempt/result denominators;
- fixed-horizon outcomes and missing/error terminals;
- historical replay separated from live forward enrollment;
- future labels never enter prior features;
- capture failures and empty windows are data.

V5 mapping:

- Strategy registry/frontiers;
- MarketFrame availability and late-event semantics;
- OrderIntent/Plan/Attempt/Fill lineage;
- Reawakening baseline/crossing;
- propensity/selection logging;
- chronological model train/validation/sealed test.

No old registration point or historical winner is imported into v5.

### 2.2 Wallet-behavior corrected bundles

**Disposition: RETAIN RAW DECODER/FEATURE IDEAS; REJECT STATIC WALLET LABEL TRANSFER.**

Potentially reusable feature families:

- creator/deployer prior local launch behavior;
- transaction-role-aware buy/sell/cash-flow reconstruction;
- multi-asset cash flow and funding/exit paths;
- participant breadth, concentration and repeat behavior;
- cluster edges with exact evidence and availability time;
- failed-transaction and partial-observation treatment.

Required v5 corrections:

- address is not a human/entity;
- unique wallets are paired with effective economic breadth and dust/concentration controls;
- local history is a left-censored lower bound unless a registered source proves broader history;
- “smart/scammer” outcome labels cannot be learned from this token’s future and backfilled;
- cluster revisions discovered later affect only future decisions;
- raw addresses are not sent to general Agents/public Web by default.

V5 mapping:

- `CHATGPT_V5_CREATOR_WALLET_AND_PARTICIPANT_FEATURE_SPEC_2026-09-04.md`;
- PumpSwap MarketFrame flow/breadth/concentration;
- portfolio cluster exposure;
- sellability/dead-risk models after forward maturity.

### 2.3 DexScreener technical-indicator work

**Disposition: RETAIN AS COARSE FEATURE/CONTROL LIBRARY; REJECT AS EXECUTION TRUTH OR DIRECT THRESHOLD SOURCE.**

Potentially reusable concepts:

- price/volume/transaction-count momentum;
- buy/sell imbalance;
- acceleration/deceleration;
- volatility/drawdown;
- liquidity level and change;
- cross-window trajectories.

Current limitations:

- provider aggregation and update time are coarser than local-top/withdrawal dynamics;
- pair selection may differ from actual Jupiter route/holding surface;
- missing/stale provider data can look like flat price/zero activity;
- a provider mark is not an amount-specific sellable price;
- indicators selected from old winners or historical complete paths can leak future/survivor information.

V5 mapping:

- keep provider 5m features as transparent controls/low-frequency comparisons;
- add 1/3/5/15/60s PumpSwap transaction/account-derived MarketFrames;
- use fresh full-position Jupiter minimum recovery for economic state;
- report provider/account/route divergence;
- no old technical-indicator threshold is activated without a new forward registration.

### 2.4 Objective feasibility and no-strategy diagnosis

**Disposition: RETAIN NEGATIVE EVIDENCE AND HUMILITY; DO NOT USE AS A PERMANENT IMPOSSIBILITY CLAIM.**

Historical inability to establish a robust strategy is evidence against optimistic backtest narratives. It supports:

- no profitability claim from a few winners;
- all failed/no-strategy/negative results remain visible;
- total PNL paired with median/tail/remove-best robustness;
- capital-feasible Paper separated from unlimited Shadow;
- explicit stop/revise lifecycle;
- avoid endlessly modifying a failed strategy on the same data.

V5 response is not to deny the objective; it changes the experimental structure so broader entry, fast exit, PeakGuard and Agent treatment can be tested independently under real execution semantics.

### 2.5 Causal-flow and historical-schema work

**Disposition: RETAIN LINEAGE/ROLE/AMOUNT SEMANTICS; INTEGRATE WITH CURRENT EXECUTION KERNEL.**

V5 requires:

- exact transaction/account/event role and direction;
- signed quote/base flow rather than price-only inference;
- slot/signature/instruction lineage;
- explicit ordering quality when same-slot order is unavailable;
- multi-asset cost/proceeds/fees separated;
- source gaps and failed transactions preserved;
- immutable decision-linked aggregates rather than mutable current wallet history.

These ideas map into `MarketFrame`, execution cost completeness and position-event reconciliation. The old schema is not copied wholesale; current r6 tables and the smallest v5 lifecycle drive implementation.

### 2.6 Semantic oracle, external gold and failed-transaction research

**Disposition: RETAIN FAILURE/PROVENANCE DISCIPLINE; REJECT EXTERNAL LABEL AS PRODUCTION TRUTH.**

Reusable principles:

- external semantic labels need source/time/provenance and cannot silently become decision facts;
- inaccessible/ambiguous/conflicting sources are explicit terminals;
- failed transactions/no-route/simulation reverts are outcomes, not discarded noise;
- independent-origin support is distinct from repeated transport/repost;
- semantic research is explanatory/advisory until a registered treatment exists.

V5 mapping:

- shared post-buy dual-role Agent case;
- structured evidence roles and available-at clocks;
- execution error taxonomy;
- Agent missing/late/error in intention-to-treat;
- no Agent numeric price/position/execution authority.

## 3. Historical directions explicitly not revived

V5 does not automatically revive:

- strategies previously failed and repeatedly patched on the same historical data;
- wallet-address memorization or current “smart money” labels;
- Dex path appreciation as executable PNL;
- technical-indicator thresholds chosen from later winners/ATH;
- semantic-oracle/KOL/project metadata as independent truth;
- failed/no-route cases removed from denominators;
- historical full-universe enrichment that blocks current launch/exit latency;
- raw high-frequency data replication per strategy;
- unlimited-capital aggregate performance.

A historical idea can re-enter only as a named current hypothesis with a new activation frontier, exact current data source and falsifiable forward test.

## 4. High-value historical assets to reuse after Gates A-C

### Priority 1 — decoder fixtures and transaction-role semantics

After the v5 registry/execution kernel:

- inspect wallet-activity/event-decoder fixtures for Pump/PumpSwap transaction role/amount validation;
- compare with the current official IDL/version;
- import only fixture cases or deterministic parsing knowledge that remain valid;
- record any changed program layout as a new decoder version.

### Priority 2 — multi-asset cash-flow accounting

Use prior cash-flow work to challenge:

- quote/base direction;
- token taxes/fees;
- partial exits;
- network/priority fees;
- transfer amounts versus quoted amounts;
- future EVM allowance/gas/L1 fee accounting.

Do not replace the current `OrderIntent -> Plan -> Attempt -> Fill -> PositionEvent` authority.

### Priority 3 — indicator definitions

Compare historical Dex indicator formulas with the new MarketFrame spec. Reuse formulas only when:

- inputs are available at evaluation time;
- missing/stale semantics are explicit;
- execution outcome label is current amount-specific recovery;
- thresholds are re-preregistered and tested forward.

### Priority 4 — forward data manifests/quality tests

Reuse historical quality checks for:

- duplicate/late/out-of-order events;
- missing clocks;
- immutable append-only constraints;
- fixed-horizon maturity;
- data/strategy version linkage;
- interrupted attempt recovery.

## 5. What this changes in the active DAG

No change to immediate Codex scope. Gates A-C remain:

1. current premise verification;
2. atomic v4 entry-stop + v5 registry/readiness;
3. minimal shared execution-observation/virtual-Paper lifecycle.

Historical decoder/indicator/wallet assets enter only during Gate F and subsequent feature/model work. They must not delay or enlarge the first registry/execution release.

## 6. Evidence and audit boundary

The local selective index/manifest was generated from the uploaded handoff archives under `/mnt/data/gxh_handoff_review/` for this review session. It is not the project’s execution authority and need not be copied wholesale into `E:\memeTrader`. The durable project artifact is this reconciliation and the current v5 specifications.

## 7. Conclusion

The historical handoff substantially supports the **data/causality/failure-handling architecture** of v5, but it does not supply a ready profitable strategy. Its best contribution is to reduce reinvention:

- reuse corrected transaction/wallet decoding ideas;
- reuse strict-forward data discipline;
- retain negative/no-strategy findings;
- treat Dex indicators as candidate features;
- keep failed execution and semantic ambiguity in the denominator.

The current system must still earn its strategy claims from new v5 forward cohorts, real amount-specific execution observations and terminal outcomes.
