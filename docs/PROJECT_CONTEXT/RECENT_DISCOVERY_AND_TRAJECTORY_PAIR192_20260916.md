# Recent discovery conversion and paired exits, 2026-09-16

All measurements below are read-only and use the current `chain-meme-trader/funding-20260906-v002-final-1000` period. They are bounded diagnostics, not a historical backfill or a claim of live fillability.

## Discovery to first frame

For discovery rounds started `2026-09-16T11:15:00Z` through `11:25:00Z`, join `token_discovery_rounds.id` to `token_discovery_exposures.round_id` with `first_local_discovery=1`, then exact `token_detail_hydration.token_id` and indexed `token_snapshots.token_id`. The measured cohort is 233 distinct tokens: 116 PumpPortal/Solana, 61 native-launch across supported chains, and 56 Gecko new-pool discoveries. At the 11:29 read, all 233 had at least one hydration attempt, 1 remained pending, and 126/233 had a subsequently recorded snapshot. Among those 126, discovery-to-first-snapshot median was about 8.06 seconds and p90 about 109.90 seconds. These are **any** snapshots, not 126 buy-eligible fresh original-pool quotes; the denominator is first-local token IDs, not round/list items. The last few minutes of the cohort had limited maturation time.

The hydration status distribution was 133 `no_pair`, 99 `hydrated`, and 1 `pending` in the read's per-chain grouping (the statuses are current provider-specific task outcomes, not immutable first-attempt results). Some Robinhood `no_pair` tokens nevertheless had GeckoTerminal snapshots with numeric original-pool liquidity and V6/observer evaluations, e.g. `robinhood:0x4bef8c3b969bbd6a67d920f05229dcba3edfec79` had a 4020.55U Gecko pool frame at 11:16:58Z and V6 evaluation on snapshot 925669. Therefore `no_pair` from Dex hydration cannot be read as all-provider pool absence or a universal strategy rejection. Current queue had only 11 pending Solana tasks near 11:29; no sustained fresh queue backlog was demonstrated. Do not weaken the 1000U floor or pretend an unknown original pool is sellable.

The active Dex poll surfaces returned 6855 list items across 136 completed rounds in a bounded post-11:15 read, but first-local discovery there was zero; lists are repeated identities and possible old-token reawakening clues, not 6855 new coins. The source episode requeue is already capped at two per minute. A later comparison should report unique tokens, valid original-pool quote coverage and 429/timeouts by equal load window; raw API quota utilization alone is not the target.

## Same-entry 144 exit comparison

`scripts/paired_arm_ab.py trajectory144_early_activity_fast_v1 trajectory144_trend_runner_v1 --pair-on cohort --hours 48 --json` at this read had 108 settled same-cohort independent-token pairs. Each side staked 2160U; fast realized -95.51U, runner +167.79U. Mean fast-minus-runner difference -2.4379U/token, token-clustered 90% interval [-5.1905, -0.0242], with leave-one-token-out sign surviving. Full current-period window (`--hours 240`) had 198 settled token pairs, fast -125.58U versus runner +532.64U, difference -3.3243U/token and 90% interval [-7.2472, -0.2332]. The 48-hour window is nested within the full-period sample, **not** an independent replication. Both arms share the same entry, so this isolates their exit contract better than unpaired strategy rankings.

The risk picture is not one-sided: over the 198 current-period settlements, fast had 14 original-pool writeoffs versus runner's 24, and runner's bottom-decile trade PnL was -20U versus fast's roughly -19.50U. Many pairs had identical exits (166/198); the mean gap comes from rare paths that diverged, including large runner winners and some extra runner full losses. The runner's strongest full-period improvement is sensitive to right-tail opportunities but its mean advantage survives removing the single largest winner. This supports continued **paired Paper observation**, not an automatic rewrite of 144 fast/runner or a global 5-to-120-minute hold rule. The newer 187/190 same-entry exit variants remain below independent-token maturity for a confident promotion decision. No lifecycle controls, positions, API budget or running strategy rules changed from this review.
