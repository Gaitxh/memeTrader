# Retrospective case discovery and creation clocks, 2026-09-17

The user-supplied Appendix A contains 125 ordered lines and 94 unique address keys. The source order/duplicates remain in `memory/USER_EXECUTION_REQUEST_20260916.md`; the read-only `scripts/audit_goal_cases156.py` normalized those keys only for this study. Its 2026-09-16 17:38:45 UTC frozen output is `data/research/goal_cases205/cases_20260916T173845Z.json` and `.md`. All 94 keys completed; 70 had current-DB canonical matches, 24 did not. No network lookup, trade write, ranking backfill or strategy entry rule was added. Nine focused audit tests passed.

The updated per-case timeline now separates: `tokens.created_at` as a database token-record clock; first stored discovery exposure and first `first_local_discovery` marker with observed/recorded times, provider, surface, round and no-pair fields; first causally valid launch fact with source-observed/ingested/recorded clocks; first locally seen provider-reported pair creation with its later availability time; and first valid recorded candidate-pool snapshot. This is a stored-evidence timeline, **not** proof that the candidate pool was the final original execution pool or that a supplier-reported earlier event was known when it occurred.

Across the 70 matched tokens: all 70 have at least one valid stored discovery exposure and a provider-reported pair creation first seen in a snapshot; 47 have a first-local-discovery marker; only 19 have a valid three-clock launch fact, leaving 51 without that source clock. Sixty-eight have a valid candidate-pool snapshot in the bounded historical sample; two do not. No new discovery/launch query failed or truncated. For 35 tokens the first stored exposure preceded the first valid pool snapshot, giving a measurable local-recorded gap: median 1,151.29 seconds, p90 9,000.61 seconds, 19 above 600 seconds. For 33 tokens the first valid market snapshot predates the first persisted exposure; their difference is **not** treated as negative collection latency. The 35 positive intervals include 13 long-gap PumpPortal-first tokens and one native-launch-first case; they may reflect curve-stage/nonexistent pool as well as collection delay. The audit does not determine that cause or infer a missed executable trade from delay alone.

Matching the **first valid quoted pool itself**, not a different earlier pool for the same token, its provider-reported creation time is after the first exposure in 15 of the 19 >10-minute cases. The other four report that same pool already existed at first exposure. One of those four (`solana:3nqHijNUExsnjNBb15WJsJ2xisyMVGN6FK4aUgZk1Rwj`) had 27 Dex profile/updates identity exposures before the first hydrated market snapshot, none carrying a snapshot; the current collector does enqueue identities, but this older 2026-09-12 sequence does not prove the present scheduler still delays 34 minutes. The other three need their period-specific attempts and liquidity checked. Among all 19, 10 first valid snapshots were more than 10 minutes after their pool's reported creation; pool creation alone still does not establish that an executable quote or sufficient liquidity existed then.

This closes one evidence gap in requirement 04, not the complete sample/control study. Next, inspect the four already-created-pool cases and contemporary ordinary tokens for point-in-time executable liquidity, attempts, queue age and later-frame timing; establish exact original-pool identity and safe executable followup before calling a case a missed opportunity. Reuse contemporaneous ordinary/failed controls, whose current artifact has only 13 independent selected tokens with covered paired 30-minute indicative outcomes; those marks are not BUY/SELL fills. The generated report and research artifact are historical assessment only and must never feed a forward decision or allow-list.
