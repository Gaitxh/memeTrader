# 端到端核查、交易历史修复与已知故障处置

## 整笔作废已应用，公共报价修复仍待完成（2026-09-06 16:04—16:07）

用户确认按“无论盈利或亏损，就当该笔未发生”处理已证实异常，替代此前仅补本金/展示标记的口径。当前账期 `chain-meme-trader/funding-20260905-fixed-1000` 内573个生命周期/62策略/1155条腿正式作废：170个同帧原池流动性与线性SELL回款异常、372个已确认低于1U入场、31个已有确认采集故障损失补款。两笔先前已有效排除，因此不重复撤销其净现金流。全部573笔此前均已终结，本批未取消237个仍存正常开放仓。

现金中性：撤销整笔BUY/SELL/WRITEOFF净现金流、对应fill更正及旧补款。净现金调整 -2971.388866549485U（大部分为撤回错误盈利）；原403笔/8058.807662700746U补款原始记录保留，但因对应生命周期均作废，不再重复进入有效现金。此为账户重述，不是新交易或新增盈利。不能把余额恢复理解为将所有账户重置1000U。

本地一致性备份：`data/backups/before-position-void-20260906T073326Z.sqlite3`，11,405,979,648 bytes，源/备份均2,784,663页。原当前账期43,941条trade逐行SHA256前后一致：`c4b32ece32287612dbce2ec70b0006bb628e72c8c0faea9b47782a29178ce362`。注册定义及activation逐行相同，无重置/回填/改策略规则。作废表保存原position/mark、所有交易、相关credit/correction；执行面标ineligible，禁止再执行其退出。旧自动污染重算不能撤回人工作废，也不自动将所有后续交易连带作废。

详细逐笔归因：`data/tmp/confirmed-lifecycle-void-manifest-20260906.json`；逐策略资金回执：`data/tmp/confirmed-lifecycle-void-receipt-20260906.json`。这些含运行数据，仅本地保留，不推Git。注意position.source_buy_trade_id是旧来源标识，不能直接当当前trade.id；本次严格按当前实际BUY的version/arm/cohort/token关联。

验证：5个新回归覆盖盈利/亏损/部分卖出/open/旧补款/旧更正、原子失败、重复幂等、独立账户、公开快照和full summary、分页及归档。3个相关历史/compact回归通过。旧curve断言假设删除异常时间段，已改为全时段保留UNKNOWN缺口而非伪造0；有效点仍须为真实更正金额。JS语法通过。实际184个最新账户快照逐一与有效ledger现金对平，237open保留。8790已仅重启Web（supervisor24824，16:06:36），实测vault_hazard_v1/DG2g正式历史0条、作废归档2条，均VOIDED。

仍存问题：撤回错误盈利后3个账户存在资金缺口：broad_cost_coverage_scaleout_v1=-97.58666454580202U；dex-successor-049-5ce40de1d93304fb=-88.09275659030709U；dex-successor-120-f98e117baa206954=-18.526622549819876U。须追踪原错误收益的后续资金依赖；未擅自新补钱或删除所有后续交易。未知核销证据案例未被本批冒认故障。旧污染期间权益曲线仍只能保留缺口，不能重造历史可执行mark。

下一阶段：优先修复BUY/SELL/估值数量与池深度真实经济路径，再恢复Paper，防止继续制造线性虚假收益。Paper仍停止，/health的runtime_status=stale真实反映此状态；Web在线不等于采集运行。随后接通184策略前信号outcomes漏斗、追踪缺失输入、做有效结果复盘及新增独立策略。

低频只读研究文件：`data/tmp/memetrader_184_arm_funnel_20260906.json`（pre-void，禁止按其经济结果评价优劣）。分类16零成交、46仅1–3BUY、66现金受限、56活跃现金可用。停机前30m5997次评估/845币/19BUY，入口未整体中断；16零成交中10主要缺合格输入/出处、6条件或成熟度未过。现有Web只计entry_decisions，遗漏信号前outcomes，须展示这些真实原因；不以放宽硬门替代修数据。

## 历史证据待核查进入Web结果解释（2026-09-06）

具体差异已闭合到代码：既有57笔报告不是全都未处理，其中1笔grade=ALREADY_CREDITED；其余56笔涉及24策略。Web启动只读取该有限只读报告一次，按账期+arm+cohort+Token匹配，不新增数据库查询/表/后台轮询。只附加研究标记，当前策略列表/详情及分页历史明确显示待核查；受影响策略的胜率/expectancy/profit factor/尾部等研究评价暂不可用、退出优劣榜，账户现金/持仓/数量/原始记账PNL/曲线/回撤不变。

不把疑点写作UNRESOLVED fill，不改原始positions/trades，不新增credit；不把已补款那一笔再次称未决。此为Web解释边界，原始Store账本统计仍保留，离线研究也必须参照同份证据报告，不能据此声称历史经济真相已恢复。正式显示与API回归覆盖compact/full和完整历史，3实例通过；JS语法通过。生产只读新边界实际184策略、56标记、24受影响arm，待本组Web部署及页面确认。Paper无需重启。

## 最新部署回执（2026-09-06 13:29）

用户明确重新授权后，同一正常shell重启路径成功：05:24:39Z supervisor31180启动原run_paper.ps1，child43160于05:24:42Z载入e68b42f；未绕过控制、未重复初始化，Web41760保留。RO核验旧activated_at=2026-09-05T07:37:08.842373Z、snapshot829428、57追加策略不变，最新自然交易已到417255。

公共精确池补源自然成功于Solana `taoC6…BrBY` 的 `8j5pX…mqki` 原池，历史行2154023，observed05:25:23.901Z、provider geckoterminal、sequence168；05:26:11后续attempt发生但该观察时间/序号未假更新。当前mark/history不保存raw缓存头，因此ETag/Age生产证明仍不可由这些表给出，缓存代语义以定向测试为证。

BSC cohort14149此前05:07:43Z已按原池新鲜liquidity<1核销，早于本次重启；最新CG原池liq0，不能将它不再补采说成新路径失败，也不证明这是工程亏损。05:25:25启动短窗主intervalP952.264秒(n27)，held fetch1.736秒/apply.090秒(n65)，补源3轮intervalP9510.710秒，计时采集失败0。无稳定SLA或新策略收益承诺。

## 连续缺池计时与公共精确池补源（2026-09-06 13:20，代码已测待部署）

部署状态更新13:22：e68b42f已推送。重启调用在执行前被安全policy拒绝；Paper12948与Web41760仍12:52启动，/health running且原funding保留。未采用其他路径绕过拦截，未初始化/清仓/补历史。待人工或执行环境完成正常重启后，核验自然公共原池mark及新连续性逻辑，再比较时延。05:21:35Z仍为旧进程窗口：120样本主判断interval P952.231秒、held fetch .957秒、apply .100秒、entry .035秒，补源12.688秒；各计时失败0，不宣称新代码提速。

明确的新公共缺陷：pool/token mark failure 以前只更新failure_kind，旧first_missing/misses仍存；下一missing恢复结构性reason后沿用旧窗口，可能把网络未知期当连续缺池。现在失败中断episode，MISSING转UNKNOWN、清计数/起点；新missing重新计时超过60秒。保留已见价格、last_success及新鲜原池liquidity<1的既定终局，不改旧策略阈值。pool中断后重计时、fresh dust及旧token-level断言3个定向测试通过。

公共Gecko精确池补源复用现有gap队列，置于Dex覆盖缺口后、Demo预算前。单池用pool-detail，多池用multi；只用同chain/token/original pool，held主源恢复时丢弃晚到fallback。公共同host请求起点至少2.1秒，复用发现器同一HttpClient；429不原地重试并尊重Retry-After，404仅该批延后。公共源失败仍可走已有Demo预算，不增加热持仓路径等待。ETag/Date同代及本地缓存保留原observedAt和值，不把重复缓存当新SELL帧；HTTP Age/Cache-Control及receipt另存。normalizer保留base/quote比值，正确支持原池反向报价。两个market API/Runtime文件33实例通过（含缓存不增加sample/历史、不跨源重复、HTTP失败后Demo恢复、原源抢先恢复、host预算）。单次线上multi探测429未重试；先前单池200只说明能力，部署后自然补源尚待验收。

57历史missing补款归因复核没有新增确证金额：全局error825虽在cohort12378的missing窗口中，却没有token/pool/chunk身份，不能证明该仓请求超时；BUY412307的20U不直接补，此前子审阅中“可执行补款候选”的过强归因撤回。BUY412305已有20U独立补款，不重复。57笔WRITEOFF共1125U，position最终realized合计-1120.438238634U（含此前部分SELL）；原始账本保留。

纠正此前报告表达：57笔的未决等级目前只在证据报告，不是全部生产质量排除。31笔/-620U仍在11个eligible arm，另26笔/-500.438238634U的13个arm因其他工程异常quarantine；账户原PNL均保留。不能声称57笔已从全部正式指标剔除。缺历史原池原始响应不能擅自改UNRESOLVED fill或抹去已记亏损，本轮研究结论须明确这些样本不确定性。

## 范围与边界

当前用户要求：多源互补、采集到结果整链路准确稳定、单策略完整可读交易历史，以及有池误核销个案。生产资金期 `chain-meme-trader/funding-20260905-fixed-1000` 保留；184 策略、旧合同、开放仓和原始交易不重置。本轮不是策略研究或自动复盘。以下测试样本不属于自然前向表现。

对照文档记录的 `46fdbe6`（2026-09-06 00:14:29+08）至已部署 `4420950`：未发现旧124的直接 policy/family/threshold 替换；但 hydration/pregrad 排队、原池补源和新实验池轮转已经改变候选覆盖及延迟。因此不能以“参数未改”声称整条链路逐笔等同昨晚，也未做事后重放来伪造同样结果。

## 已确认并修复

1. 金额分配快照副本在取最近80帧之后才过滤，可能挤走真实行情；横截面16帧还直接包含副本。改为 SQL LIMIT 前排除。实际 Store 回归证明80真帧顺序及原18 pattern outcomes 不变，不调整阈值。原124一般入场排除 observer 数据，不把该缺陷泛称旧124全被改写。
2. 持仓与精确池 Dex 请求遇429时，HTTP 层原地重试最多等待15秒，占用同host排队。现在把这些请求交回运行时退避，普通低频发现原行为保留。
3. 补源请求返回时主路可能已恢复。返回后和落地前重验缺口，避免旧响应覆盖新行情或继续消耗CG额度。主源忙/退避只跳过Dex，CoinGecko仍按原独立配额和每池60秒限制补缺；测试拦截并修正了曾一度把CG也暂停的未部署回归。不进行跨源价格重复一致性验证。
4. CoinGecko EVM resource ID 大小写导致拒绝合法资源，现按0x地址不区分大小写，Solana仍区分。
5. Jupiter 通用拒绝缺少分类。单次官方quote请求成功，不能据此证明历史119次失败的根因；解析器现在报告协议不匹配的安全字段名，不输出密钥或原始响应。
6. 交易历史后端近期窗口加前端30条截断，仓位拼接因而可能有SELL无BUY。新增独立50条分页接口，固定 through_id，上/下页可遍历当前账期全部记录；可按本次持仓过滤，展示完整日期、Token、操作、净现金流、已实现PNL、污染/纠正及独立补款，保留原始账本展开项。历史按需读取，不加入后台轮询，不扩大实时全量载荷。
7. 新历史接口实库预检发现部分原始 PNL=NULL，已修为保留未知而不造0，并加入同一回归。未将失败接口部署到生产。
8. 本轮用户具体案例证实：原池 exact HTTP200空也只是供应商覆盖缺口，不是死亡证据。补源最终缺报价现在记pool_failure，更新尝试/失败类型，不增加缺池次数、不核销；明确新鲜同原池liq<1的dust原规则保留。EVM补源字典键统一canonical，合法checksum地址不再被漏查。失败不能更新“恢复”last_item心跳。
9. 已有Gecko new_pools真实收到的新鲜行情此前不进入held mark。现在每network poll只读取一次持仓目标映射，同Token/同原池/时间合格才复用pool_visible并判断退出，换池拒绝；不新增HTTP、不扩大频率，不声称新池列表替代持续持仓采集。

## 核查证据

- RO SQLite 聚合当前账期20,635仓位，约2秒：BUY成本与stake差异0，trade realized与position realized差异0，终局成本未分配/数量不为0共0，open剩余数量/成本比例异常0，每arm/cohort BUY次数非1共0。不是逐仓N+1。
- 已有86条accounting contamination和4条UNRESOLVED成交纠正单列；未将其评价为自然策略亏损。open持仓累计已实现+28.068891U来自部分卖出，不能误作浮盈。
- 最近500笔BUY/SELL（00:10:49–03:06:12 UTC）：251 BUY/126个去重entry fill、249 SELL；同原池身份、下一采样序列、trigger→post→fill/trade→recorded时间，以及BUY=stake/(price×1.04)、SELL=剩余真实数量比例×postprice×0.96均匹配。
- 最近50 WRITEOFF：43 dust帧同池且0≤liquidity<1，源龄最大0.00833秒；7 missing封存3次、60.975332秒结构性缺失。后者只能证明本地记录自洽，不能独立证明远端当时真的没池。
- 真实分配副本：最近300 cohort仅找到1个，后25窗口最大62行，没有达到80截断门，不能据此断言已造成旧信号变更或损失。
- 分页回归103条、跨arm隔离、翻页期间新记录不重漏、尾页最老BUY、cohort过滤、补款按实际BUY ID、污染/未决/NULL语义通过。静态前向合同回归及JS语法通过。
- 新历史接口实库最大记录arm首50/2490条约491ms（一次冷读取），不把单次结果当P95或持续SLA。

## 性能基线与处置备案

03:10:07 UTC部署前最近120样本：held fetch P50/P95=1.619/21.720秒；held apply/exit=.024/.104秒；held周期=2.948/16.489秒；主策略周期=1.043/11.240秒。这个时段明显有长尾，不能称全链路秒级。snapshot写入P95=3.715秒、entry batchP95=7.638秒仍需结合部署后数据判断。Web显示5秒、隐藏30秒、Token详情10秒与后台独立。

| 已知故障场景 | 处置与留证 | 不允许的处理 |
| --- | --- | --- |
| HTTP超时、429、额度耗尽 | source health/error记录、主源退避、独立有额度源补缺；保留原采样时间 | 当作撤池或把价格置0 |
| 原池从批量token入口遗漏 | 按持仓原池ID精确检索；保存可见/缺失/失败区别 | 用另一池最新价替代、把批量遗漏算死亡 |
| 多源响应乱序/主路先恢复 | 补源落地前确认缺口仍存在，按真实时间判断 | 用较旧响应覆盖刚恢复结果 |
| 新鲜原池价值<1美元 | 按既定dust规则核销剩余仓位，留原池帧 | 仅凭UNKNOWN的默认0核销 |
| 历史成交证据缺失/污染 | 标待核查或排除，保留原交易与纠正记录 | 猜过去价格、补造收益或伪造0 |
| 工程实际损失可证实 | 按实际BUY、损失、已有补款核对后幂等追加资本事件 | 补虚构最高点利润、重复赔付、把补款算PNL |
| 新增代码拖慢关键路径 | 比较相同口径和实际负载，先局部修复；不能迅速修复则仅回退对应新增代码 | 重置账户/删除自然历史来掩盖问题 |

回退以本轮前 `59475ad` 为代码基线、`4420950` 为旧生产实现参考。只回退本轮具体改动并受控重启Paper/Web，数据库、策略注册和旧资金期不得随之回退或初始化。仓位继续原退出流程，Live保持锁定。配置及密钥不进Git。

## 未决与交付

### 后续阶段：全部策略历史入口与公共计算链路

最新范围是全部184策略，不是137/138两例。登记/历史/资金隔离及入场退出分支覆盖分别核对；已完成的20,635仓位聚合和500成交检查不重复运行，也不据此声称所有上游原始信息必然正确。

- 历史：新增按需账期目录，包含实际孤立旧账期及零交易追加策略；不合并跨期PNL。每期每策略稳定分页，支持完整Token地址查询。实库25账期416,943条历史仍存在；用户记忆中的Robinhood Token实际属于137和138，各BUY+2SELL，136无该记录。当前/旧期选择和Token过滤定向回归通过，实际浏览器验证待部署。
- 特征：同原池限制移到80帧LIMIT前，防止兄弟池挤占时间窗；保留旧策略条件和阈值。真实Store兄弟池窗口回归通过。已有生产样本存在多个池，但尚未证明某笔历史损失由此造成，不猜补偿金额。
- 性能：账户最新快照、受污染仓位/成交三处SQL改用既有索引seek，未改变frontier或补款/收益口径。只读等值比较中，184账户最新读取约.966→.0038秒，89仓位.179→.0064秒；这是局部SQL而非全链路SLA。真实Store回归验证新成交、无变化抑制与同frontier新增资本补款仍正确更新。
- 调度：入场批处理在完成一个原子批次后检查.8秒本轮预算，及时让出退出执行；原8批上限保留，未处理候选下轮继续，不额外拒绝。预算不是强行中断一个慢批次。Runtime定向测试通过，部署后核验实际时延。
- 独立性：实库cohort14160一个arm无现金不阻断其他arm；cohort13957共享BUY但一arm已退出、另一仍持仓。不同下单规模分组执行。新增策略登记边界正在确定性验证；先前965133样本不满足broad_launch年龄，归因已撤回，不能作为已证实误拒或补偿证据。

以上是代码/定向测试结果，尚未替代本阶段部署和全部策略分支覆盖结果。既有403笔资本补款8058.807662700746U不重复，57笔历史missing归因缺口仍显式保留。

#### 全部184路由与多源适配核查结果

部署回执：8bc7455于04:42:49Z受控部署，原184/资金期激活07:37:08.842373Z/frontier829428保持；自然交易继续到417006。9个相关测试实例整合通过。实际Chrome已验证策略137/138粘贴混合大小写Token地址各返回完整BUY+2SELL；138本期100条可50+50翻页，返回首页记录一致；25账期可切换，v22历史312423条中的选中arm4328条可分页，无JS错误。补款仍403笔8058.807662700746U，未重复。

真实部署后账户快照P95约.546秒（5样本），此前约3.94秒；但入场批次仍P959.16秒，不能称实时问题已解决。继续定位到入场源查询每tick从activation重新扫描已处理历史：同生产只读无候选LIMIT4仍8.093秒。修复以主串行入场流程真正消费frontier为下界，排除异步pattern_observation，保留NOT EXISTS。全仓仅两个evaluation writer，所有主入场异常/过期行亦顺序落拒绝记录；不使用异步较大ID跳过低ID普通帧。修后同只读空查询.101ms，frontier查询.281ms；两个实际Store测试覆盖追加arm/旧arm隔离、异步高ID、过期行和新鲜行正常消费。该第二段性能修复待受控部署测全链路。

另真实传输失败为多源ConnectTimeout，不是本轮Dex429；held计时包括共享最长30秒backoff及同步阻塞，不能当成纯HTTP响应。改善公共SQL后再看余下长尾，不伪报固定1秒数据年龄。

84c20b6已04:52:45Z受控部署并推送。04:55:51Z最近120样本：主判断间隔P50/P95=1.009/1.175秒、duration P95=.295秒；入场批次P95=.034秒；持仓fetch P95=1.003秒、apply/exit=.056秒，累计fetch失败0。21个Solana持仓Token原池年龄.751秒；1个BSC原池年龄49.985秒。SQLite约11.24GB、WAL97.72MB、盘余53.59GB。短窗样本不能证明长期免费源SLA；不将UI刷新等同后台频率。

剩余BSC慢池已逐项定位：用户原例Token `0x1a5f9d77ca46646cd4937fd8d093f460b66f4444`，当前cohort14149/broad_principal_lock_runner_v1剩余20U，原池 `0xae76531f57d2e1cf9e42bb7514720d745377f54e2979534a6e852e7d1cf95be7`。CoinGecko04:55:10Z确有新鲜price.00117636465334758、liq980.573；Dex对该64hex原池仍coverage gap，另一池的成功不能替代它。补源轮询约10秒、同池CG最短60秒，因此年龄增长不是主循环再次卡死。配额当时可用（day188/month7708），VISIBLE、misses0，不当死亡池。该低频覆盖限制单独报告，不能拿Solana秒级数字覆盖它，也不因现在仍持仓认定自然20U亏损。

继续核查免费补源能力：官方[公共API说明](https://apiguide.geckoterminal.com/faq)给出30请求/分钟，[接口记录](https://apiguide.geckoterminal.com/changelogs)列出pool/multi能力。一笔真实只读公共pool-detail请求接受此64hexID并200（1.39秒），同原池price.00117636465334758、liq979.9563；响应明确max-age30、s-maxage60。当前公共collector无exact方法，不能声称已接线，也不能仅把轮询缩到10秒就声称市场信息加速。下一步可最小接公共同池补源以减Demo额度依赖，但须尊重实际缓存/额度和原池判别，不新增多源重复核对。此探测未写交易/行情账本，不影响历史收益。

当前184唯一ID全部有可达处理分支：90普通/缺省、14dex_visible、24exact_entry_family归主admission（128）；56 isolated由20 pattern和36capital处理，16种capital exit overlay有明确evaluator。主family分布broad36/reawakening32/shadow14/flow32/dex继承14。未发现unknown handler，但分支存在不等于所有自然输入均已证明正确。

确证并已局部修复的多源问题：生产snapshot1005378的Gecko原文h1 volume30849.6037588876、buys462/sells336实际存在，normalize却仅保留m5，致prior55等输入丢失；现保留h1，缺失保持None。Gecko发现入口以前没有调用Dex已有的本地上下文触发器，现先记录discovery建立forward cohort后再按相同candidate scope触发，不增网络调用、不放宽clean-onchain。原observer复制把Gecko标成Dex，现保留provider与观察时戳。真实Runtime/Store以及Dex/Gecko参数化来源测试通过。该生产帧还存在现金不足，不能将输入缺陷直接称漏买损失。

追加arm边界真实Store fixture已证明旧cohort误阻部署后首次新鲜eligible帧；修复限定runtime addition自身frontier，只投影新arm，旧arm不重复，第三帧新arm也不重复，旧帧无回填。无现网实际损失金额证明，不据此补款。

用户补充的 BSC `0x1a5f9d77ca46646cd4937fd8d093f460b66f4444` 已确证公共误判：Gecko 64-hex 原池ID不在Dex精确查询覆盖内，备用源当时未形成同池成功mark，Dex HTTP200空被当结构性缺池。cohort13136的25臂与13137的1臂于23:42:53Z BUY、23:43:08Z first missing、23:44:09Z核销，实际BUY415583–415608各20U，共520U，无已有补款。00:04:18Z CG找回两原池（后一池liq952.51U，前一池liq仍未知）。03:14:49Z单次Dex exact合并请求仍200/0 pairs。这些证据证明来源覆盖不足，不证明核销时每臂本来可以卖回20U。证据校准：月累计249减当前日9，仅说明此前240次；没有核销当刻的逐池quota记录，不能断言23:44已耗尽额度。先前“额度当时耗尽”的中间归因撤回。

需要补偿的是由不成立的缺池判断强制记入的520U终局损失，作为独立资本纠错，不声称市场可回收520U、不补反事实利润；该币更早正常SELL的4.6483U自然亏损不补。交易表实际BUY主键415583–415608才是credit键，position.source_buy_trade_id=13038/13039不是这些实际BUY键。已于03:32:45Z执行幂等补款，回执见末节，不再重复执行。

主动范围核查：当前WRITEOFF3044=2961dust+83missing。除上述26笔外还有57笔/29 token-pool-cohort，需按原始来源/故障时间区分证实与suspect，而不是统一按后来有池改写历史。已有377笔补款合计7538.807662700746U，本报告前未新增。

其余57笔已逐笔留证 `MISSING_WRITEOFF_EVIDENCE_2026-09-06.json`，核销记账损失1125U（含分批卖出后仅剩5U的一仓），已有补款20U，不能再补。3组11笔核销后同原池有自然有效价格/流动性，合计220U，仍为suspect：cohort9647的BUY381392/381393/381396/381397/381399；cohort10398的BUY390267–390271；cohort12258的BUY411751。没有核销时原始响应与逐池额度证据，不自动推算赔付或恢复历史仓位。其余46笔也不默认视为无错；明确保留证据缺口，不纳入策略优劣判断。

部署、Git回执与改后真实性能见末节。免费源持续可用与全历史逐笔因果一致性不作无证据保证。

本轮整合验证：`tests/test_market_api.py tests/test_market_api_runtime.py tests/test_chain_meme_pool_identity.py` 30项通过；两个Store历史窗口回归、完整分页/NULL回归、静态前向合同、补款实际净损失与幂等回归通过。JS语法及diff检查通过；未运行全项目测试或重放生产策略。

### 03:32:45 UTC 部署与补款回执

`9c1e796` 已推送当前Git分支并受控重启Paper/8790。补款26笔/25策略/520U，记录时间03:32:45.278090Z，pending=0；累计403笔/8058.807662700746U。同token更早的自然SELL亏损未补。原资金期激活时间07:37:08.842373Z、frontier829428、184策略、Live锁定均保留。新交易继续到416527（03:33:37Z）。历史API返回该仓完整BUY+WRITEOFF，BUY显示独立20U补款而原PNL仍-20U。

03:36:35短窗口：held fetch最近120样本P50/P95=1.210/6.334秒；apply/exit=.017/.059秒；held周期63样本P50/P95=2.369/7.881秒；主策略周期120样本=1.013/3.900秒；28个持仓币均有价，当时最大年龄1.808秒。改前27币与此时28币规模接近，外部市场/网络并非受控条件，不能全归因代码，也不能称长期SLA。

另自主发现仍需修复的Web瓶颈：`/api/live`首次11.507秒、随后10.707秒，而`/api/performance`=.036秒，不仅是冷启动。正在限量剖析重计算路径；不以刷新按钮更快掩盖它。SQLite WAL从此前381.8MB到此时25.4MB，数据库约11.12GB，剩余约54GB，无空间耗尽证据。

### Web账户快照索引读取修复

定位到每次状态读取对约54万条资金期快照执行MAX/GROUP BY。改为登记策略集合的一条SQL内逐arm反向索引查找；保持ledger frontier非空和纠正时间边界。单纯相关LIMIT仍被SQLite选中时间索引并排序，实测5.312秒；明确复用现有latest索引后同184个账户查询约0.004秒，无新表/索引/后台任务。代码已完成，Web部署后的HTTP耗时另行记录，不能用单条SQL时间冒充整个API时间。

真实Store回归覆盖旧时间快照排除、较新NULL ledger不压过有效快照、其他资金期隔离、新arm无快照fallback；原profit factor/完整曲线最大回撤回归通过。Web展示现金继续来自有效账本，不拿孤立快照覆盖。Paper本阶段不再重启。

`0eabfb4`已推送；03:52:21Z仅重启8790，Paper39240启动时间仍03:32:47Z。HTTP实测4.126秒及03:53:57Z的3.076秒（前值10.707秒），轻量performance=.024秒，184/原资金期/Live锁定不变。此为两个真实请求，不是P95或全部页面操作SLA。最近自然交易416816，03:49:33Z。

必须同时保留较差的后续窗口：03:53 held fetch120样本P50=.651/P95=15.189秒，apply P95=.207秒，主循环interval P95=12.031秒，进程累计held fetch失败2。主采集22币全部有价、当时年龄6.110秒，UI24个去重开放币为不同scope。03:36较好窗口不能代表长期表现，外部获取与运行调度长尾仍需观察，不宣称全链路恒定低延迟。已确认错误与补款已落地，其余历史疑点/限额与来源覆盖边界保持公开，不以“全面核查”承诺绝无错误。
