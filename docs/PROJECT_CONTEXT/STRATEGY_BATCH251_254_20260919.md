# 策略251–254：环境保护、去相关路由、BSC生存确认与参与广度

## 目标与边界

用户在系统、策略、数据和结果复盘后要求直接落实四项新策略。账户耗尽策略明确排除：本阶段不恢复、不调参、
不新增其替代臂。所有新增策略为独立Paper账户、普通20U、有效并发上限16、Live关闭；不修改父策略、旧仓、旧交易、
资金期、原池安全、后帧成交或历史结果，也不增加行情请求。

## 新策略

1. `revision251_unpaired_trend_regime_guard_v1`：继承249的无配对信号和487退出，使用仍运行的
   `trajectory144_trend_runner_v1` 在决策前6小时的同链终态作为环境证据。最近不超过20个不同Token至少10个、净PnL>0、
   核销比例<=15%才准入，避免249自身暂停后无法识别恢复。
2. `single_token_diversified_router_v1`：不是复制已有5U优先级路由。只有当前同一Token同时出现至少两个预先声明的独立
   证据族时，才建立一个20U组合仓位；同Token终身一次，最多16仓。原研究臂继续运行，便于和重复暴露影子组合比较。
3. `bsc_post_signal_survival_v1`：继承trajectory144 trend退出。BSC父信号后等待30–60秒独立帧，要求价格、流动性及活动
   不恶化且买入占比>=58%，之后仍需正常下一帧成交。只复用现有轨迹帧。
4. `liquidity_lead_breadth_confirmed_v1`：先满足原三帧流动性领先且价格受控规则，再要求新鲜金额流中的有效参与广度>=2、
   top1名义占比<=50%、净quote流>0。缺失保持WAIT，不将稀疏金额流冒充安全。

## 验证与部署

- 新策略、249和250相关13项测试全部通过。
- 扩展共享边界回归共69项：65项通过；4项失败为本阶段前已存在的不匹配：3项旧测试替身不接受运行代码已有的
  `reserved_movers`参数，1项仍硬编码cohort策略数12而当前基础定义已为32。本阶段未修改该接口或策略清单，不把它们
  算成新策略失败，也没有顺手改动无关代码。
- 新增模块及接线通过`py_compile`和`git diff --check`。
- 2026-09-19T08:24:37.941525Z在快照1766889、评价1758277后同一前沿追加四臂；行为哈希分别为
  `292c588d2a236e01`、`c89d0edf6aa91ff2`、`ee31643e7306a43f`、`f32499f612d738d1`。
- 受控重载后Paper PID43664；manifest为531臂，四个新模块哈希存在。原资金期
  `chain-meme-trader/funding-20260906-v002-final-1000`、Paper-only、Live locked，开放仓继续按原合同退出。

## 首个自然读回

部署后前292条评价中：251和253分别有68条可审计结果，当前主要等待独立轨迹帧/真实机会；252有68条，均因没有同时出现
两个独立证据族而WAIT；254有63条，主要等待三帧父信号，另有池龄、活动或输入无效拒绝。四臂均0决策、0仓位、0自然
收益。它证明策略已加载并在真实前向数据上评价，不证明盈利，也不应据此立即放宽条件。

后续只按自然的 `signal → admitted → next-frame BUY → terminal` 继续评价，重点比较核销、硬止损、独立Token收益、
去掉最大赢家后的结果及同源父臂差异。
