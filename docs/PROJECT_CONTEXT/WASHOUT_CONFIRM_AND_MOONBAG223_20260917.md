# Manual Paper strategy iteration 223, 2026-09-17

## Scope and factual boundary

This is one implement-and-observe slice of the user's 12-section request, not completion of that request. The full text and ordered sample lists remain in `memory/USER_EXECUTION_REQUEST_20260916.md` and `memory/USER_ADDITIONAL_SAMPLES_20260917.txt`; no supplied address is a trading allowlist. The current database and funding period are unchanged: `data/memetrader_forward.sqlite3`, `chain-meme-trader/funding-20260906-v002-final-1000`. Default Paper costs remain 4% adverse buy and 4% adverse sell slippage, no extra fill fee, 20U ordinary entry, and a fresh 1000U original-pool floor. Live remains locked. The observations below are local Paper records, not realizable-profit guarantees.

## Existing strategy decision

At the manual pre-change read, `trajectory169_trend_runner_moonbag_v1` and its fresh same-entry control `trajectory169_trend_runner_control_v1` had **148 fully terminal exact same-token/cohort/source-BUY pairs**. Moonbag net was -96.1178U; control net was +100.6335U; paired moonbag-minus-control was -196.7513U. Moonbag was better in 3 pairs, worse in 13, and tied in 132. This compares only the overlapping fills, not the parent's larger unmatched trade population, and is strong evidence against this specific moonbag exit contract at this frontier, not against all partial-profit ideas.

Manual `scripts/converge_moonbag223.py --apply` stored `FAILED_SAME_SOURCE_EXIT_VARIANT` for moonbag new entries in the existing read-time account-control KV, at `2026-09-17T02:53:39.791079Z`. Receipt `moonbag223:convergence:2026-09-17T02:53:39.791079Z` contains the previous control for deliberate rollback. No fill, trade, account balance, historical policy or open-position exit was changed. The script requires at least 100 exact-source terminal pairs, 100 independent tokens and delta <= -100U; it is manual, idempotent and Paper-only.

## New mechanism, not a copy of the old entry

`alpha222_washout_half_reclaim_v1` observes the existing `alpha149_washout_reclaim_hold_v1` signal without changing it. At the original trigger it freezes the prior observed low L, the current price P and the then-known high H = P / (1 + observed drawdown). It waits for a *later* fresh frame of the same token, original pool and provider, within 180 seconds, with price >= (L + H) / 2, nondecreasing original-pool liquidity and at least one reported sell in the five-minute window. The threshold must be above P, so this is a genuinely later entry rather than a duplicate. Missing clocks, stale or future frames, different pool/provider, missing sell count, or nonrecovering price do not create a signal. Source event time, receipt time and later confirmation time are stored separately; the trigger is never retrospectively re-timed. The ordinary safety, next-observed source BUY and costs remain mandatory.

The new Paper arm uses 20U, up to eight simultaneous positions, one lifetime entry per token, the 212 two-fresh-original-pool-mark pre-uptick-anchor failure exit, existing hard stop/trailing and 120-minute maximum hold. It uses only already collected frames, O(1) work per frame and a 1024-entry bounded tracker; it makes no API request. The hypothesis is that a delayed structural reclaim reduces false first upticks enough to offset late entry. The control is the existing 212 same-trigger arm; because entry timestamps differ, compare independent token/cohort outcomes and coverage, **not** fictitious same-fill exit attribution. A later second wave needs a new episode, not a replay of this one-time trigger.

New code: `src/memetrader/washout_confirm222.py`; append/routing in `store.py` and `runtime.py`. Four 222 tests plus the moonbag-control test passed; the related 212/220 suite passed too, **13 passed** overall. A Store integration fixture confirms no position at the confirmation frame and a Paper position only after the next observed frame, with the frozen exit anchor. These tests prove engineering behavior, not alpha.

## Deployed frontier and natural result

The exact Paper worker was replaced under the existing `scripts/run_paper.ps1` supervisor. Worker PID 108040 loaded the arm at `2026-09-17T02:54:45.669768Z`, activation snapshot 1038193. At the ~02:55:25Z API read, `/health` and `/api/live` were running, strategy count 509, `paper_only=true`, `live_locked=true`, original funding period unchanged; snapshot frontier was already 1038236. At ~02:56Z, held Solana quote age was about 10.7s with zero listed held retrieval gaps, but this is a tiny **unmatched-load** window, not a latency improvement claim. A stricter trigger-frame as-of check was then added and the same 13 relevant tests passed again; the supervisor loaded final child PID 110780 at ~02:59:38Z. The ~02:59:37Z API read still reported running, 509 strategies, Paper-only and Live locked. There were two open positions in one held token across all strategies at the earlier read. The 222 arm had no post-activation natural terminal result at this cutoff. It is deployed for natural forward observation, **not proven profitable**.

## Pending full-task acceptance

Continue the current `memory/ACTIVE_EXECUTION_SCOPE_20260917.md` checklist: per-chain/provider discovery-to-valid-original-pool clocks, cohort safety/next-frame terminal decomposition, all 111 distinct submitted research keys with contemporaneous ordinary/failed controls where data exist, current and retired strategy family disposition, matched-load latency/held age, and costed independent-token forward comparisons. No automatic strategy deployment or scheduled review was added. If 222 remains unfilled, diagnose each distinct terminal without weakening identity, pool floor, sellability or as-of truth. If its natural net expectancy or right-tail capture is inferior, pause its *new entries* using an auditable control; keep its old exits and history. A source rollback would remove only the 222 registration/routing and reload Paper, not reset the funding period.
