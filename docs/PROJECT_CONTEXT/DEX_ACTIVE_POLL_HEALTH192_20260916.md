# Dex discovery health source correction, 2026-09-16

Observed failure: `/api/live?view=summary` exposed four `dexscreener:*:stream` records whose last successful heartbeat was 2026-09-15 16:09–16:10 UTC, suggesting stale discovery. Current `runtime.py` defines `dex_discovery_stream_loop` but does not schedule it; the active `poll_dexscreener_discovery_once` path instead records `dexscreener:{surface}` heartbeats. The six active poll sources had success around 2026-09-16 11:21–11:22 UTC. An indexed five-minute-plus discovery-round sample after 11:15 UTC showed 136 completed Dex poll rounds returning 6855 source-list items; these are repeated/identity observations, **not** 6855 newly discovered tokens. In the same bounded sample Gecko new pools first-discovered 74, native launch 62 and PumpPortal stream 116. Thus the stale display did not establish a runtime Dex poll outage.

Changed the three existing Web health queries to show the six current Dex poll sources rather than four retired stream heartbeat rows; source labels now say “轮询”. The old stream/round history remains in SQLite and no new API, polling task, scheduler or strategy gate was added. Focused Web source-health test and JS syntax check passed. The Web process reload and API readback are recorded in the accompanying update-history entry. Trading-engine hot path and Paper execution were not changed.

Open questions: repeated Dex surface items may be useful as old-token rediscovery clues but are not fresh unique-token coverage; the existing `requeue_dormant_source_episode` caps such requeues at two per minute. Compare first-valid-quote coverage and per-source unique discoveries over equal load windows before changing scheduling. The 1000U known-pool floor and unknown-liquidity wait remain unchanged.
