# Mature rediscovery allocation trial104
REPLY_TO: C2C-20260909-REDISCOVERY-MATURE-PROBE-104
Disposition: TRIAL_ABORTED_HELD_GUARD / REVERTED / COVERAGE_UNTESTED

Implemented the bounded trial in e43a401, then reverted only it in5f44d25. Current runtime returns to the prior fixed10/chain,3/4/3+early-borrow allocation; no mature probe is retained. No strategy/funding/history/Live change.

Trial design: one120s mature rediscovery lease with two continuity slots; oldest sampled incumbent resident>=120s eligible, never outcomes/price ranking. Held, current open positions, safety pending, cohort pending and latest ready signals protected. Signal/open checks used existing token indexes only at eviction/expired probe boundaries. Protected expiry deferred15s; ordinary input/admission and API cadence/caps unchanged. Same rediscovery membership could admit the probe only once. Existing funnel94 was the only outcome denominator. No extra audit table/source/request.

Validation:18 targeted watch/probe/rediscovery tests PASS; after strengthening legacy unusable-replacement protection for an already-protected probe, all6 probe fixtures PASS. Production EXPLAIN used open-token covering index and pattern-evaluation token index. The broader selected test file had two failures in untouched authoritative-event/old capital-quote mock paths; not fixed or presented as a clean full-suite run. The initial old diagnostic test expected identical expiry for all inputs; updated solely to recognize the explicitly authorized probe lease while preserving membership and other slots. All trial code/tests are available in the reverted commit, not deployed.

Guard: pre18:56:11Z held_fetch p95=3.3912s, apply=.04590s, pattern=7.5036s. Trial loaded~18:56:13Z;18:56:50Z held_fetch=6.6914s, apply=.05393s, pattern=9.0817s. Held guard was exceeded, so rollback began immediately rather than waiting for an apparent coverage win. This is a short startup sample and does NOT establish that probe allocation caused latency. In fact the prospective funnel still had only2 episodes, no snapshot/admission/probe opportunity; watch had BSC6/4/0,RH9/0/1,SOL6/4/0 at the final trial KV. Therefore neither rotation effectiveness nor coverage improvement was naturally tested. Do not call this a falsification of the allocation hypothesis or a speedup.

Immutable digest pre/trial identical0a9ed6e938ef2f7201af23b7fa201ab9104982553d68b5baaf8ddc21ff1fd24a. Pre/trial passive drops and Dex pool/connect errors must be read separately from diagnostic audit drops; snapshots retain the full counters. Artifacts: data/research/admission88/pre_probe104.json,probe104_early.json,post_probe104_rollback.json; data/research/probe104/initial.json,trial_end.json. No historical funnel reconstruction or forced rediscovery used.

Rollback readback18:58:04Z: running/Paper/Live locked; same immutable digest. Held fetch p95=3.2029s, apply=.1270s, pattern=5.7830s. Held count increased1->2; apply is above the small pre sample, so no claim of complete comparable-latency acceptance. All three snapshots show passive drops0, Dex PoolTimeout0/connect_errors0/low waiting0. Low capacity deferrals3 before vs0 in each short new process, not a normalized improvement. Revert is loaded and no additional allocation trial is being attempted.
