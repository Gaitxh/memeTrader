# Recent universe71 — bounded extraction blocker

REPLY_TO: C2C-20260909-RECENT-GOLDDOG-UNIVERSE-71
Disposition: INCOMPLETE_DENOMINATOR_RESOURCE_BOUND — research NOT completed

Existing second-frame strict-v2 uses first v6 cohort, not all-market first-valid original pool; matched47 is case-selected. Neither can be substituted as the requested denominator. Preregistered RECENT_UNIVERSE_PREREG_71.md freezes definitions/calendar/windows/costs and stops incomplete scans from generating precision/Alpha claims.

Read-only r6 cutoff2026-09-09T04:55:14.116134Z, snapshot frontier2194123. Existing snapshot index is(token_id,observed_at); there is no verified first-valid pool projection. Earliest recorded same-exact-pool anchors require validating earlier rows, not assuming first row after a date is first-ever. No production index/schema changes authorized as incidental research.

Actual bounded scan stopped at250001 qualifying(price>0,liquidity>=1000) rows,36.315645s,43050 provisional pool groups;24542 rejected for missing/invalid clocks or pool identity. This is the predeclared local resource limit, NOT SQLite corruption or proof full research is impossible. The probe counting all qualifying rows also exceeded a5s deadline. Data quality rejection count is bounded-prefix only. No complete date denominator exists from this run, and first-ever identities could change after reading remaining rows. Do not treat retained groups as an all-market sample or classify unseen futures as losers.

No outcome feature correlations, tail rates, matched-control precision or strategy capture conclusions emitted. Sep7 thresholds not learned;Sep8 holdout not evaluated;Sep9 not labeled. No strategy registration/reset/backfill/Live/runtime writes. Scripts/report history preserved. Runtime data path unchanged; read I/O is nonzero, no negligible-overhead claim.

Concrete remaining requirement:complete, causally validated normalized snapshot projection using chunked reads and disk-backed research storage (bounded per-chunk memory), then first pool/entry/future labels and first-BUY joins. That is not implemented by this stopped in-memory attempt. A larger in-memory cap on the live trading host was deliberately not silently used. The user task remains unfinished; this RESULT is a resource/coverage checkpoint, not a negative Alpha finding. Resume from source records with same frozen definitions/cutoff; no need to repeat old selected-case studies.

Artifacts data/research/universe71/scan.json; scripts/research_universe71.py; prereg file. Script emits complete_scan=false and exits before outcome construction on cap/time. No tests/deploy claimed; actual safe-stop exercised. Reports only committed.
