# 2026-09-06 实施交付逐项表

> 本表按当前目标、批准的 `NEXT_STRATEGY_PROPOSAL_2026-09-06.md`、本轮实现记录整理。“已注册/已接线/Shadow”只表示工程或观察链路状态，不表示已完成自然结果、经济验证或全部部署。当前共享原池门槛为 1000 USD，四项 Paper 设置已具备 Web 配置入口但待部署生效；零交易只表示当前观测范围内无已写入 BUY/SELL，不等于无机会或损失。最终统一初始化尚未执行。

## 总体边界

- 当前正式账期、旧历史、开放仓和旧策略保留；历史退款/VOID、旧账户重置不在本轮执行。
- 当前共享原池门槛为新鲜流动性 `<1000`；缺失、陈旧、请求失败不等于无池，不据此自动核销。
- Live 与自动复盘保持关闭；Paper 的自然 BUY/SELL 结果必须按真实部署后 frontier 计，不用测试造数或事后价格补交易。
- 新资金期的“所有策略 1000U、全策略重启”已获授权，但必须等本表工作完成后由 root 最终执行；本报告生成时未初始化。

## S01–S10 执行表

| 项目 | 代码/测试 | 部署与运行事实 | 自然结果/成熟度 | 外部阻碍或边界 |
|---|---|---|---|---|
| S01 L0 延续失败退出 | 已实现并有纯逻辑/集成验证 | 已随第一阶段增量接入，使用独立前向观察 | 已有部署后自然记录；短样本，不证明 alpha | 共享行情和原池年龄仍受上游覆盖影响 |
| S02 L0 盈利保存 | 已实现并有纯逻辑/集成验证 | 已随第一阶段增量接入 | 有真实部署后 BUY/SELL，但样本仍早期 | 不把单笔结果或 sibling 账户当独立样本 |
| S03 5U→15U确认 | Store/Runtime 接线及纯状态测试通过 | 已正式接入；正式 20/5/5U 风险基准，第二腿 15U 为 Shadow | Shadow/观察不等于正式分腿盈利；尚无充分自然成熟度 | 低于共享门槛不建 Shadow；不能把 1000U 组合账户写成已完成 |
| S04 深度/热度 leader | 模块与接线测试通过 | 已接入 2 臂，复用有界批次 | 仅有限 observed-set/短 episode；无充分经济结论 | 不全市场扫描，不事后补 leader |
| S05 钱包退出 | 钱包纯状态、接线测试通过 | 已接入候选/对照；仅 Solana PumpSwap 可观测名单 | 尚无完整钱包账本或人物 PnL；自然样本需继续积累 | 最多 20 个观察地址，不称 smart money/真人 |
| S06 钱包确认入场 | 钱包纯状态、接线测试通过 | 已接入候选/对照；晚到钱包正向参与后下一帧 | 等待成本与错失右尾尚无充分样本 | 封榜后命中不能自证；换池/跨链保持隔离 |
| S07 成熟币新接受 | 纯逻辑/Store 集成测试通过 | 已接入 1 臂，复用事件/安静后再活跃输入 | 尚无充分自然闭仓结果 | 两次事件确认后下一帧 5U；不要求旧策略曾买过 |
| S08 机会环境标签 | `opportunity_regime.py` 纯测试及 Store→outcome→标签验证通过 | phase 1 只标签，不交易，低优先级接入 | 未形成可用于改策略的成熟经济结论 | UNKNOWN 计覆盖分母；不得作为当前策略硬门 |
| S09 局部相对抗跌 | 模块/集成验证通过 | 已接入候选/对照，使用有界市场批次 | 当前仅局部 3–4 成员 observed-set，不能称全链环境结果 | 不新增全家族行情请求，不把短样本当 alpha |
| S10 深度主导权交接 | 模块/集成验证通过 | 已接入一次交接实验 | 必须等原 leader 实际关闭且另一成员自然连续观察；尚未成熟 | 不回填历史赢家，不覆盖旧策略 |

## C/D 扩展与基础修复

| 项目 | 状态 |
|---|---|
| D18 Four.meme 原生发现 | `four_meme_observer.py` decoder 与定向测试完成；固定官方 TokenCreate/LiquidityAdded topic，拒绝错误 topic/伪造 pool。37ea6a0 已完成 Runtime 接线，尚未部署。 |
| D19 Pons V1/V2 | Pons V1 observer 与测试完成；V1 官方事件/工厂身份、链 4663、bounded logs。V2 官方路径受地域访问限制，不能冒称已覆盖；37ea6a0 已完成 Runtime 接线，尚未部署。 |
| D10 权威事件扩源 | OKX、Kraken、KuCoin 三个官方上市源；Coinbase Status 仅 incident observation，不生成上市 event。collector 与 6 项测试通过，Runtime 轮换已接线，尚未部署。 |
| 策略版本 UI | `app.js` 已支持 `strategy_display_index` + `strategy_revision` 显示 `策略 XXX-V001`，历史 selector/drawer 不猜 revision；介绍页展示 reason/changes/time/source period，并明确无盈利证明。 |
| 核销 SELL 语义 | 已修复为发出显式 SELL 指令，同时保留 `paper_side=WRITEOFF`；Paper 账本只核销，不伪造 SELL/fill；Live 不启用。 |
| Jupiter 路由恢复 | 已排除不兼容固定滑点的 JupiterZ，保留身份/金额/4% 最低回款校验；后续成功已观察到，但上游 429/额度问题仍须单独监督。 |
| 低交易/信号后漏斗 | Web 已展示逐策略/分链放行、拒绝及 top 原因，现金不足、槽位、流动性未知、后帧等待与输入缺失区分；不把无记录当 0。 |
| 原池时效/覆盖 | 已区分本地调度耗时与上游缓存/覆盖缺口；BSC/RH 部分原池仍可能年龄数百秒，不能仅凭 health 或主循环 P95 声称全源实时。 |

## C01–C12 对应表

| 项目 | 当前处置 |
|---|---|
| C01 可复制钱包证据/前向信誉 | 数据层/观察名单接线；最多 20–50 地址，尚无完整钱包账本或人物 PnL。 |
| C02 钱包确认入场 | 复用 S06，代码/状态验证通过；等待成本与错失右尾待自然样本。 |
| C03 观察钱包派发退出 | 复用 S05，区别于 creator/early-buyer/issuance-holder；待自然样本。 |
| C04 多钱包共识延续 | 后置组合候选；等独立钱包样本，不强制本轮建设。 |
| C05 Actor→Chain 搜索优先级 | 低频观察/预算建议；不改变交易门，不强制建设。 |
| C06 KOL×钱包×市场确认 | 依赖新鲜事件与钱包输入，后置候选；未形成可交易策略。 |
| C07 KOL 来源效用分配 | 后置研究统计；需延迟、CA 绑定和成熟结果，未按赢家排榜。 |
| C08 实际互动邻接发现 | 有界临时观察方向；不抓全社交图、不自动交易，后置。 |
| C09 创建者×流×钱包 | 后置复合候选；UNKNOWN 不推断质量，creator 旧策略保留。 |
| C10 成熟币催化/持续持有 | 复用 S07 基础；分钟级新币不套长持叙事，观察中。 |
| C11 钱包参与下的市场状态 | 等前向样本成熟后研究，不现在拟合分数。 |
| C12 Fast-stop 独立再收复 | 已实现并继续观察，不重复注册新策略。 |

## D01–D23 对应表

| 项目 | 对应处置与状态 |
|---|---|
| D01 L0 延续失败退出 | S01；已接入并有测试，持续自然观察。 |
| D02 L0 盈利保存 | S02；已接入并有测试，持续自然观察。 |
| D03 5U→15U确认 | S03 phase 1；第二 BUY/正式分腿后置，Shadow 不等于完成。 |
| D04 Copyability Wallet 层 | 复用 C01；不重复建设。 |
| D05 钱包确认入场 | 复用 C02/S06；已接入，待样本。 |
| D06 Smart Distribution 退出 | 复用 C03/S05；已接入，待样本。 |
| D07 Clone 流动性 leader | S04；有限 observed-set，持续观察。 |
| D08 Clone 非 leader 延期/缩量 | S04 后续对照，后置，不改 Broad。 |
| D09 成熟币新资本 episode | S07；已接入，待自然成熟。 |
| D10 权威事件多源确认 | OKX+Kraken+KuCoin 三个上市源；Coinbase status 仅观察；collector/Runtime 轮换已接线，6 项测试通过，尚未部署。 |
| D11 KOL-lite | 复用 C05/C06，低频后置观察，不恢复重型 Runtime。 |
| D12 Interaction adjacency | 复用 C08，后置，不重复实现。 |
| D13 Fast-stop reclaim | 既有策略继续，不改阈值。 |
| D14 早失败→再收复组合 | 等 S01 与既有 reclaim 各自样本后再新 ID，后置。 |
| D15 PREGRAD 晚到 create 路由 | 长期占槽未证实，暂不修改。 |
| D16 Migration no-chase | 新 outcome 标记 60/120 秒局部轨迹，非新增参数族。 |
| D17 钱包派发×利润保存 | 等 S02/S05 独立样本后再组合，后置。 |
| D18 BSC Four.meme 原生发现 | Observer/decoder、测试、Runtime 接线完成；尚未部署。 |
| D19 RH Pons V1/V2 场所 | V1 Observer/测试/Runtime 接线完成；V2 受外部访问阻碍，整体尚未部署。 |
| D20 EVM route-lite | 可选 Shadow，非主线；不改变普通 Paper。 |
| D21 Chain-only outcomes | P0-B；已实现只恢复新严格前向 outcome，尚无成熟经济结果。 |
| D22 迟滞机会市场状态 | S08 phase 1 只标签不交易，测试通过；不作为硬门。 |
| D23 有限资金机会排序 | 先复用现有 ranker，预测/强制换仓后置。 |

## 结果与部署判定

- 已证明：若干模块可编译、定向测试通过、独立 namespace/账期隔离和前向接线可运行；Paper 在旧账期继续产生自然评估/交易。
- 未证明：S01–S10 任一策略已达到成熟样本、稳定 alpha、全市场覆盖或可迁移盈利；Shadow、注册、代码 PASS、短窗 BUY/SELL 均不等价于经济完成。
- Four/Pons V1、OKX+Kraken+KuCoin上市源及独立Coinbase状态观察已于14:23Z生产重启加载。Pons V2受外部访问限制，Four公共RPC仍有getLogs限制；接线不等于每轮均取得事件。
- WRITEOFF→显式SELL已实现、3项定向测试通过并于14:23Z加载。Paper账本仍记录核销而非假SELL，Live仍关闭。
- 修订对象122个全部已有实际V002规则变化，另84个未选策略未被改写。新增63个的实施如下表；原矩阵的REVIEW_ONLY等分组保留为选择时记录，不是最终处置。逐编号由矩阵display_index映射到策略XXX-V002。
- 四设置API/UI、Store成交/账本、Runtime、普通/pattern/精确报价与Shadow全部接通、测试通过并于14:23Z加载；/api/paper-settings已核实默认4/4/0/1000、无需再次重启。已补Shadow漏扣卖出费、0%响应缺字段被误接受、Web买卖滑点不同时的错误文案。保存不直接重启或初始化。
- 新增63个的Store实际BUY/SELL与后帧测试16项通过，中央规则11项和资金期6项通过。最终新资金期仍待本轮发布；不能将已编码或短窗BUY/SELL称为盈利验证。

### 用户追加63个的实际处置

| 槽位范围 | 实际V002变化 | 验证/边界 |
|---|---|---|
| 32个旧reawakening兄弟槽位 | 改用现有主通道成熟flow-burst再加速：池龄1–6小时，5分钟8笔/500U及既有三倍加速；各旧退出保留 | 原32臂只有一个共同cohort，非32样本；main实际入场、年龄/池门和下一帧测试通过，无新API |
| 8个balanced-harvest | 保留Broad入场及原退出，增加15分钟零活跃/买方占比低于45%的退出 | FLOW/INACTIVITY都实际触发并后帧成交；不是用污染收益调参 |
| 17个事件/表面/资金流/排名等稀疏输入槽位 | 替换为7类明确标注的同池L0实验：早期成交质量、单币质量门、持续性、流动性领先、压缩突破、静默复苏、年轻池承接 | 不把聚合成交冒称钱包、官方事件或真实资金；既有观察批次最近最多8帧，7机制实际BUY/SELL通过 |
| 125/128 | 125较早回收本金后保留runner；128由多档退出改为成本后整仓兑现 | 各自原入场不变，旧污染样本不作为经济结论；实际部分/全仓与费用通过 |
| 190/191 | 190取消狭窄60/120秒检查点，改滚动衰退；两臂均10分钟未回本退出、30分钟资本占用上限 | 同入场保留；未知/陈旧不能伪成交，后帧和独立账户通过 |
| 192/193 | 净25%时分别半仓/全仓兑现，60分钟上限；192保留原盈利后衰退退出 | 是分批/整仓行为差异，不用同名覆盖旧期历史 |

所有122个在新资金期显示V002，详情展示旧版本、变更时间、问题、改动、依据及来源账期。修复详情入场展示优先取旧source_entry_family的问题，当前规则与历史来源分别显示。

### 本轮验证边界

- 已通过：费用与原始数量守恒、固定费用每次成交一次、0%/缺字段、exact不二次滑点、WRITEOFF不扣假成交费；新旧账期独立、历史定义保持、新期1000U起点、配对下一帧入场、126主通道超过3币仍覆盖；原池身份、低于/等于门槛与unknown区分；配置保存/生效区分、完整曲线/MDD、分链漏斗、低频源轮换。
- 独立账本算例：投入20U另付1U，2U价格加10%买滑点，数量9.090909；1.95U卖价扣5%及1U费用，回款15.840909，PNL=-5.159091，持仓与Trade一致。
- GUI控制工具因本机kernel assets路径错误无法初始化；未声称实际点击验收通过，未绕过限制。API/静态语法已验，生产页面返回与新设置接口仍需部署后核对。
- 新策略是可检验假设和已接通实验，不是盈利保证；批准的phase2/可选项、资源限制仍按以上逐项表保留，不用名字注册冒称全部完成。
- 经济与账本边界：writeoff 现在可发 SELL 指令但不代表成交；路由恢复只说明协议兼容性改善，不证明历史交易或策略收益；任何金额损失须有逐笔证据，禁止推测。

## Root 最终动作（本表之外）

1. 补齐/确认 Four、Pons、D10 的生产调度接线与 source health 证据。
2. 汇总每个 S/C/D 的自然 forward age、BUY/SELL/closed/open、输入/门控/资金原因；不把未成熟项结算为失败。
3. 冻结新期 effective definitions、revision metadata、旧仓退出隔离规则后，执行一次统一 1000U 初始化并重启；保留旧账期历史和旧 open positions，不跨期合并现金或曲线。
