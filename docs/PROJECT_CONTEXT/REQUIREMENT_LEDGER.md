# memeTrader 长期需求台账

最后核验：2026-09-03（Asia/Shanghai）
本轮实现前基线：`main` / `121d9192e898bb8f88cf84a69cd4fceceb9aafcd` / Paper / `live.enabled=false`；发布后 SHA 以 Git 为准。

本文件是长期任务的逐项台账，不是某次发布的完成声明。运行事实仍以 `AGENTS.md`、当前代码、忽略的本机 `config.json`、当前 SQLite、实时 API、进程与端口检查为准。`CONTINUOUS` 表示工程链路存在但研究或运行验证必须持续，不能改成 `DONE`。

状态：`DONE`、`PARTIALLY_DONE`、`NOT_STARTED`、`IN_PROGRESS`、`CONTINUOUS`、`BLOCKED`、`INVALIDATED`、`NEEDS_REVALIDATION`、`SUPERSEDED`。

| requirement_id | 来源消息或文档 | 真实需求 | 当前状态 | 完成证据 | 未完成部分 | 持续任务 | 被后续要求取代 | 风险或阻塞 | 下一步行动 | 最后核验 |
|---|---|---|---|---|---|---|---|---|---|---|
| CORE-001 | 长期总 Prompt §2–5 | 单机、单进程、SQLite 的前向事件→Token→决策→Paper 系统 | CONTINUOUS | `Runtime`、`Store`、`Strategy`、47 张业务表；当前 r6 持续产生 observation/event/token/decision | 持续检查漏斗损失、延迟、zero-yield、误报和漏检 | 是 | 否 | 长期运行漂移与来源失效 | 每轮以真实数据库增量复核 | 2026-08-31 16:35 |
| SAFE-001 | AGENTS.md；长期总 Prompt §21 | Mainnet Live 永久锁定，网页/Telegram 不可解锁 | CONTINUOUS | 当前 `mode=paper`、`live.enabled=false`；Web 返回 locked/available=false | Mainnet Broker 尚不存在，且不是当前发布目标 | 是 | 否 | 私钥暴露、误广播 | 每轮测试 Live 锁与敏感字段隔离 | 2026-08-31 16:35 |
| OPS-001 | 长期总 Prompt §4、§21 | Paper Runtime 单实例、SQLite 单 writer、端口与任务可审计 | CONTINUOUS | Windows `memeTrader Paper Bot` Running/IgnoreNew；8765/8787/8788 均为单 listener；SQLite lock 被活动进程持有 | 需持续防止重复实例 | 是 | 否 | 重复实例、僵尸进程 | 每轮复核监听、任务和活动锁 | 2026-08-31 18:00 |
| OPS-002 | 长期总 Prompt §22 | 北京时间 00:00/08:00/16:00 复盘，不创建重复自动任务 | DONE | 既有 `memetrader` heartbeat 已从 09:00 更新为 `BYHOUR=0,8,16` | 无 | 否 | 旧 09:00 已 SUPERSEDED | 时区解释错误 | 后续只更新同一 automation | 2026-08-31 16:35 |
| OPS-003 | 用户要求项目存储不得落到 C 系统盘 | SQLite、日志、上下文、Web 状态、测试产物和临时 Agent 工作区统一保存在 E 盘项目目录 | CONTINUOUS | 既有持久数据均使用项目相对路径；`load_config()` 现统一将当前进程及子进程 `TEMP/TMP` 路由到 `<project>\data\tmp`，replay 也显式使用该目录；pytest `basetemp` 已是项目内 `.pytest-tmp/current` | Codex、浏览器和 Windows 自身应用缓存不受 memeTrader 控制 | 是 | 否 | 后续新增库重新直接调用系统临时目录 | 新增存储路径时必须验证解析后仍在项目目录；发布测试显式在 E 盘执行 | 2026-09-01 14:20 |
| MEM-001 | 用户“项目内保留上下文”；长期总 Prompt §3 | 持久化完整需求台账和最新断点 | IN_PROGRESS | 本文件建立；`START_HERE` 与日期快照已存在 | 每轮需同步状态、证据、下一步与新 supersession | 是 | 否 | 上下文压缩、旧报告被当成现状 | 每轮巡检更新本表和最新快照 | 2026-08-31 16:35 |
| GOV-001 | 用户反复要求全面理解、持续推进且不得避重就轻；2026-09-02 冻结 | 后续每轮先处理 Source/Token→Event↔Token→Decision→Paper→前向学习中最高影响的真实断点，不得用容易完成的 UI、文档、审计或重复验证替代核心推进 | CONTINUOUS | 根目录 `AGENTS.md -> Frozen execution contract` 已成为最高权威执行契约；`START_HERE` 明确继承 | 每轮仍需用当前 SQLite、Runtime 与真实增量确定最高影响断点；不能把某次修复宣布为长期目标完成 | 是 | 否 | 上下文压缩后重新转向表面工作；为追求非空结果放宽门槛；把报告数量当进展 | 每个实施周期先写明要改变的真实断点，完成最小实现和定向验证；若外部阻塞则记录合法替代路径 | 2026-09-02 |
| WEB-001 | 用户网站/双语/动态要求；长期总 Prompt §20 | 深色中英控制台、动态刷新、响应式、真实数据 | CONTINUOUS | Overview/Events/Tokens/Decisions/Paper/Wallet/Agents/Sources/Audit/Settings 已实现；低成本 polling；8787/8788 与鉴权公网入口运行。大库 Web health 已拆为热路径可读/schema/WAL 与 doctor 全库 integrity，Audit 约 50s→5.1s。Sources 组合查询实测约 16.3s，前端现只把该页读取上限由 9s 调整为 30s；真实浏览器中英文均已同步显示准入账本且 console 0 error | 固定公网域名仍需用户自有 Tunnel；Sources 查询仍应持续观察而非把 30s 当性能治愈 | 是 | 否 | 旧页面缓存、临时公网域名变化、组合审计继续随库增长变慢 | 持续做真实浏览器中英/响应式/错误日志 QA，并监测重查询延迟 | 2026-09-01 19:12 |
| WEB-002 | 用户事件来源超链接要求 | 展示全部来源、角色、权威/热度排序与时间线 | CONTINUOUS | Event detail 有 F/C/I/P 分组、链接、透明排序、事实/局部传播/纠正三分栏、来源版本链与 `event-claim-relation/v1` 目标关系图；`observation-provenance/v1` 展示 Origin→Transport→Local capture | `agent-fact-verification/v1` 已上线但自然核验样本尚待积累；旧 Observation 不回填 provenance/关系/核验 | 是 | 否 | RSS/Agent/域名推断被误算独立来源、删除被误当撤回、关系被误读为事实证明 | 累积自然 provenance/revision/relation；独立性只显示已证明不同原始条目下界 | 2026-08-31 21:28 |
| SRC-001 | 长期总 Prompt §10、§14 | 平台/来源/人物/主题按前向增量价值学习，保留探索 | CONTINUOUS | source learning、trend lane、watch exposure、至少 40% exploration 已实现 | 样本未成熟，不能改仓位/退出 | 是 | 否 | 幸存者偏差、过拟合 | 累积自然 exposure、失败和 fixed-horizon 结果 | 2026-08-31 16:35 |
| SRC-002 | 长期总 Prompt §5 | source-poll exposure 保存完成、空结果、重复、过滤和错误 | CONTINUOUS | `source_poll_attempts` 与 Sources 面板已实现 | 持续评估各入口覆盖和稳定性 | 是 | 否 | 为非空结果删分母 | 保留所有真实 poll 终态 | 2026-08-31 16:35 |
| TOKEN-001 | 用户 Dex/OKX/新币来源要求；长期总 Prompt §5、§7 | 多入口新 Token/池发现与 Event↔Token 证据链 | CONTINUOUS | PumpPortal、GeckoTerminal、DexScreener discovery/hydration、token discovery exposure 已实现 | OKX Premium/签名接口未接；不逆向绕过 | 是 | 否 | 推广榜单、同名币、来源竞争 | 继续比较本机首次发现与后续漏斗 | 2026-08-31 16:35 |
| TOKEN-002 | 长期总 Prompt §7 | canonical Token 竞争与 CA 置信度 | CONTINUOUS | match/candidate/canonical margin、WAIT/REJECT/CANDIDATE 已实现 | image similarity、dominant contract/buyer breadth仍不完整 | 是 | 否 | 同名币和未来赢家回填 | 只用决策时可得证据扩充特征 | 2026-08-31 16:35 |
| TOKEN-003 | 用户提醒 Pump 每天有一批而非极少数高涨幅 Token | 证明完整新币总体的机会召回率和当前 Paper 交易稀疏度是否合理 | CONTINUOUS | `token-discovery-quote-attempt/v2` 已拆开实际晚到与非成功重试窗口越界。eligible→lookup 采用注册后 180m 持久欠账、最老 pending 优先和 `2 pending + 至少 1 current` 公平边界；每轮最多 10 个 probe、3 个 lookup、45 秒频率不变。`token-event-lookup-name-screen/v1` 进一步对活动 pending 复用原有确定性名称门，把不可检索名称写成终态而非伪装成等待；不调用 Agent/报价/新闻源，固定 `decision_eligible=0 / affects=none` | 最终公平版从 00:35:34Z 起的独立窗口中，新 eligible cohort 8/8 完成 lookup，lookup 共 9（5 pending/4 current），自然轮次出现 2 pending+1 current；170 个 reverse quote 全部 success，9/9 poll 与 result 终态，0 error，`decision_eligible=0`。此前 raw pending=12 全为既有名称门不可检索项，促成名称终态层；上线首批浏览器实证为 screened 15、searchable 5、rejected 10。按正确谓词（仅 no_pair/error/interrupted）复核，v2 `retry_window_exhausted=0`；早先把成功行也计入的 37 是错误口径。长期机会召回和可成交性仍未成熟 | 是 | 否 | 把 raw pending 当可执行工作；把成功行误算 retry-window exhaustion；把链路缺失称为风控拒绝；盲目提高 timeout/并发；回填赢家；把 identity/context 送入 evaluator | 持续观察 searchable pending 年龄、当前候选延迟、no-pair 与 Context 终态；保持有限槽位和证据门。下一 P0 是同 route、同时间的真实可卖出/成本证据，而不是用队列清空或事后涨幅冒充盈利能力 | 2026-09-01 09:08 |
| TOKEN-003-EXEC | 用户要求模拟交易可无未来函数并尽量接近真实执行；长期总 Prompt P0-A | 用固定目标时点和当时安全证据验证“路径涨幅”能否形成可解释的 Paper 执行模型 | CONTINUOUS | `token-universe-fixed-target-execution/v1` 在 activation cohort 21987 前向注册；baseline→15/60/240m 固定目标，不用 canonical peak；EVM 达到 $14k 流动性门后采集 GoPlus/Honeypot 双端税率、honeypot、sellability，再用冻结 $35、每侧 4% 不利滑点和实际配置费率建模。SQLite registration/result 不可更新删除；Web 中英显示完整终态，固定 `decision_eligible=0 / affects=none` | 首次发布时分母为 0，必须等待自然固定时点结果；当前不是实际 router quote，未建模链费/priority fee/MEV/部分成交，Solana 明确 unsupported | 是 | 否 | 把 DexScreener 快照或安全 API 冒充成交报价；把未知安全字段当零；使用事后峰值；用模型 PNL 宣称真实利润 | 先积累自然 EVM 双端安全样本；下一独立版本优先验证 Jupiter V2 keyless quote-only 的 Solana BUY/SELL route、price impact、minimum output 和 fee 字段，仍不得签名或广播 | 2026-09-01 09:34 |
| TOKEN-003-ONCHAIN | 用户提醒热点/叙事可能领先市场，同时大量 Token 会先出现链上动量且交易仍稀少 | 严格衡量“本机尚未观察到合格 Event/Context 时链上先动”的 trigger-time 结果，避免用 discovery 锚或后来赢家解释 | CONTINUOUS | `onchain-only-shadow/v1` 只纳入注册后每 Token 首个合格 onchain trigger，冻结精确 transition/snapshot、触发前三时间和本机 Event/Context 缺失状态；15m 主终点、60/240m 次终点，缺报价/无 pair/时间异常/后来语境均留 ITT。BSC 仅用双端时效安全与冻结成本标 `modeled_only`，Solana 仅 market path；Audit 中英展示，表不可更新删除，`decision_eligible=0 / affects=none` | 旧 749 个 trigger 不回填；部署后自然分母尚未形成。必须达到 30 个主终态 Token、15 个独立日期及正/非正各 5 个后才有描述性成熟度，仍不自动进入策略 | 是 | 否 | 把本机未观察误称全球无信息；用 discovery→target 冒充 trigger 回报；删除后来发现 Context 的样本；把 modeled-only 或 raw return 称成交/利润 | 等待自然 trigger-anchored 样本，按链、安全来源与 later-context 分层；Solana 真实执行结论须另有 trigger-anchored Jupiter 双腿时效证据 | 2026-09-01 15:10 |
| TOKEN-003-ONCHAIN-JUPITER | TOKEN-003-ONCHAIN 的 Solana route 缺口；用户持续追问交易稀疏是否存在系统阻隔 | 用 trigger 时刻而非 discovery/后来快照锚定 Solana 双边真实聚合器只读路由，区分捕捉延迟与策略门槛 | CONTINUOUS | `onchain-only-shadow-jupiter-quote/v1` 在 activation shadow cohort=90 前向注册且不回填；baseline=`trigger_recorded_at`，target=trigger+15/60/240m，queue≤30s、total≤45s，固定 `$35`、400 bps ExactIn。attempt 先于 provider 落库，SELL 使用 BUY minimum output，回报使用 SELL minimum output；5 秒独立小队列与 30 秒 universe 队列经单一互斥入口运行，固定 `decision_eligible=0 / affects=none` | 初始 6 个 cohort 因旧全量查询阻塞而 queue-expired；实测旧查询 21.49s、gap 44.80s。新增精确索引和每轮 12 个 gap 上限后降至 0.084s/0.046s。17:03 截面 eligible 21、有效 baseline 15；首个 15m 双腿有效往返的最低输出下界为 -83.18%，target queue/total=1.75s/2.75s。链路已验证，样本及日期成熟度仍不足 | 是 | 否 | 删除过期样本；回填注册前 winner；把 quote 当 fill/利润；为非空结果放宽已注册时间门；保存 wallet/taker/transaction/requestId | 继续等待 15/60/240m target；至少 30 个有效往返、15 个独立触发日且正/非正各 5 个前不接入 Strategy、Paper 或 Live | 2026-09-01 17:04 |
| TOKEN-003-JUPITER | 长期总 Prompt P0-A；TOKEN-003-EXEC 的 Solana 缺口 | 用真实聚合器只读路由验证 Solana 固定金额 BUY、固定目标 SELL 与双边最低输出 | CONTINUOUS | 原始 `token-universe-jupiter-quote/v1` 完整保留。新增 `token-universe-jupiter-quote-validity/v1` 在 activation cohort/raw result=`25941/374` 后严格前向注册：baseline anchor=`source_recorded_at`，target anchor=冻结 `target_at`，queue≤30s、total≤45s；过期前不请求，响应后过期仅保留 raw，只有双腿 time-valid+quoted 才计算回报。Runtime 每 tick 最多仍只发 3 个真实请求；它会在最多 10,000 个到期项中寻找新鲜任务，但每 tick 最多只同步终结 12 个陈旧缺口，避免 `not_requested` 占满 provider 槽位或阻塞单进程。不可变表、Web 双语白名单、`decision_eligible=0 / affects=none` | bounded raw audit（result≤239）证明延迟主要来自本机队列：baseline queue p50≈1399s，15m target p50≈624s，60m target p50≈156s；旧 v1 的时效未知。部署后先观察到过期任务长期占满 `limit=3`，导致实际 provider request=0。修复并受控重启后，同一自然前向账本从 9 个 valid quoted 增至 18；12:45 截面 validity=1,911，其中 baseline queue expired=992、source missing=98、valid quoted=18，target baseline-not-valid=794、queue expired=9、valid round-trip=0。调度恢复已证明，可执行回报仍未成熟 | 是 | 否 | 把旧 raw quote 当同时间证据；target 用后来 source time 重启时钟；把 quote 当 fill；保存 transaction/requestId；回填旧赢家；把报价下界称为利润 | 继续观察 18 个 valid baseline 的 15/60/240 target 到期结果与新增 valid coverage；成熟前不接 Strategy/Paper/Live，也不因 0 round-trip 放宽已预注册时效门 | 2026-09-01 12:45 |
| TOKEN-003-P0-B | 长期总 Prompt P0-B；完整 Token 漏斗的 no-decision attribution | 将注册后 `no_decision` 按 hydration、trigger、admission、Agent result、lookup/relation、candidate/evaluator 等实际断点追加归因，并区分 raw 与可执行质量 | CONTINUOUS | `missed-opportunity-no-decision-attribution/v1` 保留 immutable registration/result、完整终态、三时间上限和 `decision_eligible=0 / affects=none`。只读 `quality_view` 既连接 `quality.assessed_at<=attribution.classified_at` 的不可变质量行，也分开连接同一 outcome、归因冻结前已存在的 Solana Jupiter 时效有效往返和 BSC 固定目标 `modeled_executable`；Web/API 中英展示 raw、路径、估算与固定时点执行分母，不改写归因 | 发布验收动态截面 raw attribution=524；固定时点执行已知=19、非负=15、≥25%=12，其中 Solana Jupiter 5 个且 1 个≥25%，BSC 模型执行 14 个且 11 个≥25%。这比旧的 confirmed executable known=0 更接近可执行性，但 BSC 仍是模型、Solana 仍是报价下界，均不是成交或利润；交易稀少仍主要发生在事件证据与候选准入漏斗 | 是 | 否 | 后来 evidence 倒灌、把路径峰值或模型收益当成交、identity/context-only 或 Agent 自报升级成决策证据 | 继续积累时间有效 Jupiter 与 EVM 安全样本；保持 Strategy/Paper 门不变，按断点比较覆盖改善的前向召回与误报 | 2026-09-01 13:40 |
| TOKEN-004 | 用户“新 Meme 币详情中的官网/社交链接可供 Agent 溯源” | 把 provider 已附带的项目元数据作为调查入口，同时严格区分项目自述、推广与独立事实证据 | CONTINUOUS | Dex/Gecko 元数据继续规范化为 `token_source_links`。PumpPortal create 现从公开 metadata URI 做 SSRF/DNS/redirect/size 受限补全，保存 website/X/Instagram/Reddit/TikTok 与 manual-only Telegram，并精确关联原始 discovery exposure；链接以 HTTP 完成时点记录，不再回填为请求开始。精确命中已启用高影响账号的 social post 只提升为 priority 2 `high_impact_account_metadata_lead` 调查线索，仍为 `provider_metadata_unverified / decision_eligible=false / endorsement_inferred=false`；只有本机精确浏览器原帖才是 priority 3。 | 冻结到 10:14:58Z 的真实分母为 Pump create 141，metadata attempt 139、成功终态 132、links found 103（73.05%），discovery→result 中位 0.966s/p95 6.247s；旧实现 167 条 link 与 exposure 关联为 0。修复后稳定窗口 87 个 create exposure、86 次 attempt；52 个 links-found 全部精确关联 exposure（84 links），52/52 result 保存 source-link FK，link/result 时间差 0–4ms。自然出现 F1、Reuters、Binance 三个 priority-2 trigger；Reuters/Binance 两次 Agent 均为 no-context，Reuters 找到候选事件但独立正文核验仍为 0，未生成可决策证据。 | 是 | 否 | `tokens.source/raw_json` 是 last-writer 状态，不能当发现历史；项目自填账号/帖子可能借名人或机构身份推广；普通 metadata 大量 zero-yield，冷却后没有持久重试 | 完整分母只用 exposure/cohort；继续比较高影响精确帖子、普通 metadata、on-chain trigger 的独立来源 yield 与成本。未成熟前不增加生产并发、不放宽 Decision/Strategy/Paper 门 | 2026-09-01 18:35 |
| TOKEN-005 | TOKEN-004 的冷却后观察缺口；用户允许按证据增加常驻 Agent | 冷却或错误跳过不能静默消失；先建立严格前向分母，再判断是否需要主动重试或增加生产 Agent | CONTINUOUS | `token-context-deferred-admission/v1` 从 admission/trigger=`4201/127750` 后严格前向注册。每个 cohort 精确冻结 admission、trigger transition、可选 source-link 指纹及首次到达时间；观察区间固定为 `[projected_next_eligible_at, deferred_at+180m)`。重复跳过保持 pending，只有同 Token/触发/链接/Event/Decision 的自然准入或到期才追加终态；三表不可更新删除，固定 `decision_eligible=0 / affects=none`。Runtime 只复用本地 shadow follow-up 结算，不主动派发 Agent、不改配额/冷却/策略 | 注册前只读窗口有 93 次 global-cooldown skip（普通 metadata 90、on-chain 2、高影响 1），普通 metadata 中 78 次没有后来 attempt、仅 1 次后来准入，证明沉默分母缺失但不能证明重试会有收益。`2026-09-01T11:12:48Z` 自然截面为 69 cohort，69 exact-trigger lineage、62 source-linked，全部 pending；首批 180m 终态尚未到期。同期 Context 输出仍以 `no_context` 为主，独立来源/verifier yield 为 0；生产任务最大排队约 0.405s，因此未增加超过 2 个生产 Agent | 是 | 否 | 把观察账本误称主动队列或机会恢复；用 later source 回填；重复 skip 提前终结；用项目自填链接推断背书；只统计进入 admission 的触发而忽略更上游 preselection starvation | 等待自然 later-admitted/expired 终态并按 trigger/source 类型比较 yield/cost；只有持续排队或高价值漏过证据成立时才预注册主动重试/并发 challenger | 2026-09-01 19:12 |
| TOKEN-006 | 长期总 Prompt P0-C；反向报价 timeout 与队列背压 | 区分一次供应商批次故障和受影响 Token 数，并在短暂传输故障时跨 lane 有界退避，避免立即重复撞击同一主机 | CONTINUOUS | 既有逐 Token immutable attempt 继续保留；Store/API/Web 新增 `request_rounds`、`request_error_rounds` 与每失败批次受影响 Token 数，不再把一个 30-Token 批次错误叙述为 30 次独立网络故障。Runtime 的 hydration、reverse probe、universe/on-chain follow-up 现共用一个 Dex quote gate；`httpx.TransportError` 后按 2–30s 指数退避并加确定性 0–20% 抖动，成功即清零。低优先级 reverse probe 在 gate 忙或退避时让路；没有增加 timeout、连接并发、批大小或第二 Runtime | 24h 动态截面为 74,868 Token attempts、5,282 provider batches、1,335 affected-token errors、94 failed batches，错误放大比 14.202。新部署前最后一簇从 11:14:38Z 到 11:15:20Z：11 个 ConnectError 批次影响 142 Token，3 秒后首个批次成功；证明是短时主机/网络故障被多 lane 立即重复撞击，而非 142 个独立故障。新 Runtime 从 11:36:23Z 到 11:39:30Z 自然产生 422 Token attempts/33 batches、262 success、151 no-pair、0 error；窗口过短，不能宣称长期治愈 | 是 | 否 | 退避使固定时点任务排队变长；24h 历史仍包含旧调度；request round 在 mixed role 分组中可重复展示；短窗口 0 error 被误当长期可靠性 | 按独立自然故障簇比较 failed batches、受影响 Token、deadline miss 与恢复时间；若固定时点被低价值 lane 持续挤占，再预注册显式优先队列，而不是提高 timeout/并发 | 2026-09-01 19:39 |
| TOKEN-007 | TOKEN-005 暂缓项；用户强调高影响人物/热点信息可早于价格且允许按证据增加 Agent | 区分 eligible Trigger 是否真正进入 Admission，并优先消除高影响触发在单轮 `max()` 选择中被静默丢弃的问题 | CONTINUOUS | 不新增表或回填：Store/API/Web 只读连接 `token_universe_funnel_transitions` 与 `token_context_admission_attempts`，以延期观察注册点 admission/trigger=`4201/127750` 为前向边界，按 Token/trigger/source-link/Event/Decision 去重 episode，显示 transition、repeat、exact lineage、进入 Admission、超过 5m 未进入和 admitted/skipped。Runtime hydration 每轮顺序处理最多 4 个 priority≥2 高影响直接触发并保留 1 个普通 metadata；全部仍经过既有全局/单 Token 冷却、有限配额和最多 2 个生产 Agent | 部署前动态截面 823 episode 中 232 进入 Admission、591 未进入；高影响 metadata 19/11、缺失 8。部署后浏览器自然增长到高影响 21/13，新增两个 episode 均进入 Admission；这只证明路径开始覆盖，不能证明 Agent 内容 yield 或交易收益。同期普通 metadata 仍有 554 个超过 5m 未进入，on-chain 仍有 49 个，表明 persistent scheduling 仍未解决 | 是 | 否 | eligible trigger 被误称 Agent call/信号；普通 metadata 洪泛；增加准入尝试被误称增加模型并发；把部署后两个样本过度外推 | 等待延期 180m 终态并比较各 trigger 的独立来源/verifier yield、成本与漏过；若 on-chain/普通上游缺口有可解释价值，再预注册有界持久队列 challenger，不先增加 Agent 数或放宽证据门 | 2026-09-01 20:00 |
| TOKEN-008 | TOKEN-007 的链上准入缺口；用户反复提醒每日存在大量先涨 Token | 让纯链上动量触发以受控方式进入 Token Context 准入层，同时避免 metadata 洪泛垄断有限调查窗口 | CONTINUOUS | v1 先为每个 Dex hydration 周期选择最高动量、再按最新快照排序的 1 个 `onchain_momentum`。其连续 9 次选择全部被既有全局冷却跳过后，`token-context-onchain-admission-challenger/v2-fair-lanes` 从 admission/trigger=`4926/161767` 严格前向注册：浏览器精确原帖与新鲜高热事件关系始终第一；未核验高影响 metadata lead 与 on-chain 在同时存在时按周期轮换先后；普通 metadata 最后。v1 账本原样保留。选择路径/版本写入现有 Admission transition；全局/单 Token 冷却、调用/Token 上限、结构校验、每轮 on-chain≤1 和最多 2 个生产 Agent 全部不变。全局冷却现于准入后、首次 await 前预留，Agent 失败时释放，避免同一窗口被两个 Context 同时穿透；`decision_eligible=0 / affects=none` | v2 前延期边界后的普通 metadata 37 admitted/36 result 全为 `no_context`，高影响 metadata 6/6 全为 `no_context`，on-chain 35 次 Admission 尝试却 0 admitted。no-decision 质量覆盖中 on-chain unadmitted 有 123 个归因、17 个 confirmed-executable-known、14 个≥25%，只用于说明公平性研究值得前向验证，不证明调度会盈利。v2 已自然选择 11 次、准入 2 次，BSC 与 Solana 两次结果均为 `no_context`。并发可见性修复部署后的 admission `5088@14:01:50Z` 与 `5106@14:06:36Z` 相隔 286.65 秒，第二次准入后 30 秒到达的普通 metadata 明确记录 `global_cooldown_active`，未再出现同窗双准入 | 是 | 否 | 把链上动量或历史 outcome 当买入信号；让未核验 metadata 假冒精确人物原帖；轮换被误称增加频率/并发；冷却只在 Agent 完成后可见；为非空结果放宽来源、时间或结构门 | 继续观察更多相邻 admitted 的自然间隔、v2 selected/admitted/skipped/result、独立来源/verifier yield 与成本；样本和事实产出不足时保持 1/轮，不增加 Agent 数、不接 Strategy/Paper/Live | 2026-09-01 22:08 |
| INFO-001 | 用户“信息可能先于价格”；长期总 Prompt §6 | 独立 information-first 前向 cohort | CONTINUOUS | `information-first-shadow/v1`，缺基线也入分母，15/60/240 追加终态 | 样本尚不成熟 | 是 | 否 | 把低活动误写成未定价 | 继续积累注册后 cohort | 2026-08-31 16:35 |
| INFO-002 | 长期总 Prompt §6 | 严格 Information Lead Gap | CONTINUOUS | `information-first-ilg/v1`；同 provider/chain/DEX/pair；Store 生成 `recorded_at` | 需更多注册后 crossing/missing 样本 | 是 | 否 | 后来快照/跨池回填 | 保持固定 240m+30m 终态 | 2026-08-31 16:35 |
| INFO-003 | 长期总 Prompt §6 | 10s/30s/1m/5m mention velocity、加速度、跨平台扩散 | PARTIAL | `event-attention-trajectory/v1` 保留本机到达率下界；`observation-provenance/v1` 从上线后显式拆分 Origin/Transport/Local capture，并给出已证明不同原始条目下界。旧事件不回填，覆盖不足为 null | 全平台分母、稳定 actor 分母、reply/quote/repost 修订和完整跨社区扩散仍不可用 | 是 | 否 | 45s 轮询不足以解析 10s/30s；前向 proven origin 样本仍需自然积累 | 先积累注册后路径与轨迹，再做 collector coverage；不得把 transport/域名/singleton 当 origin independence | 2026-08-31 19:30 |
| INFO-004 | 用户“热点先于价格”与重点人物低注意力提醒；三路 ChatGPT 独立复核及 active implementation review | 在价格动量前研究重点人物/机构信息能否及时形成 exact Token addressability 与可路由性，先证明可寻址，再谈收益 | IN_PROGRESS | v1 的唯一自然 no-seed cohort原样保留。base v2 因读取可变 `events.attention` 在 0 cohort 时废弃。base v3 从 Observation `6499` 后只接受 exact immutable attention point。route v1 在 0 attempt 时停用。`kol-token-addressability-route/v2-compatible-deadline-edf` 于 `2026-09-02T06:23:40Z`、activation cohort `1` 注册并绑定 live v3 hash；按 route version 隔离，强制 canonical CA/完整 lineage。自然链随后暴露 hydration FIFO 延迟：859 个 due item、两个重点帖 Token 前有436/491项。现仅在 hydration 调度层优先配置重点账号的 exact social-post Token，普通 FIFO仍保留 | 部署前两 Token 等待14–15分钟且 attempts=0；部署后首轮均 hydrated 并产生 exact browser Observation/source-link lineage 的 `high_impact_account_post` trigger。一个 deferred retry 成功 admitted，Luna/low 88,380 tokens 后为 `no_context`，另一项继续等待全局冷却；Decision/Paper均为0。两项调度测试与真实前向路径通过，Paper/Live边界未变；尚不能判断经济价值 | 是 | 原始低注意力收益探针被 addressability-first 取代；base v2 与 route v1 保留 immutable registration 但不再运行 | 旧赢家回填；修改冻结 registration；读取可变 Event attention；provider 时间冒充本地可见；非 canonical CA；pair/queue/response 混因；multi-hop 冒充 same-pair；跨版本去重/混计；普通 hydration 永久饥饿；污染交易状态 | 继续等待第二项自然 deferred retry、route/confirmation 终态与更多日期分母；成熟前不接 Strategy/Paper/Live、不因 `no_context` 放宽证据门 | 2026-09-02 17:00 |
| INFO-005 | 用户反复指出重点人物/热点可能先于价格；2026-09-02 重点 X 精确帖子→Token 分叉前向审计 | 研究 provider 已给出的精确帖子链接能否在浏览器正文尚未到达前形成“帖子→当时已知 CA 集合”的不可变歧义轨迹；不得把后来赢家倒灌成信号，也不得把 identity/promotion 当背书 | IN_PROGRESS | 初算 `130/548` 被独立审查发现存在 URL query 分组、组内最早时间门及可变 `first_observed_at` 污染。固定 cutoff `2026-09-02T07:32Z`，按 Snowflake status ID、逐 Token 时间门及不可变 exposure-link `recorded_at` 后，严格设计证据为 103 个 episode、529 membership、528 个不同 Token；37/36/30 个 episode 分别为 fanout 1/2–4/至少5，最大82，约78.8%集中于 Solana 与 Elon。最终 180m 集合在 T0/30/60/120/300s 已完整比例仅 35.9%/53.4%/64.1%/68.0%/73.8%；529 个 membership 中仅 3 个在链接到达时有完整价格和流动性 | 全部 membership 为 `provider_metadata / identity`，只证明项目方引用帖子，不能证明人物背书或 canonical。3 个 v3 自然 cohort 在各自信号时 provider-linked Token 也均为0，因此新研究是独立 Token-first estimand，不得改写 v3。三路独立复核均为 `REVISE` 并同意仅先运行 Phase A。`provider-post-ambiguity-shadow/v1` 已于 exposure-link `451913` 后激活；首次部署验证中最小 admission link 为 `451914`，旧数据未回填，当前 episode/membership/checkpoint 均为0，Strategy/Decision/Paper/Live 零影响 | 是 | 否 | URL 变体拆 episode；可变时钟；事后补齐集合；按最大涨幅挑 winner；membership 当独立样本；把项目方 metadata 误称人物背书；对82个 clone 全报价；用当前 Dex 快照回填信号时状态 | 按 T0/30/60/120/300s/180m 不可变 checkpoint 等待真实前向 episode；只评估 episode 级集合完整性、fanout 与 abstention 假设。经济版本仍后置，不发网络请求，不修改 v3/route v2/Strategy/Paper/Live | 2026-09-02 16:32 |
| CHAIN-001 | 长期总 Prompt §8 | unique buyers、new holders、buyer breadth、集中度和 cluster | CONTINUOUS | `solana-holder-breadth-shadow/v1` 从部署注册点后确定性抽样 0.2% 的新 Solana universe cohort，在 0/15/60/240m 低频读取官方公共 RPC；只保存正余额 token-account owner 聚合数、Top1/Top10 供应占比、低于 1bp 尘埃比例、覆盖/延迟/响应大小，不保存地址，固定 `decision_eligible=0 / affects=none` | owner 字节数不是 unique human/buyer/smart money；池、托管、尘埃空投与女巫地址可显著放大。小样本探测 3 个新 Pump Token 得到约 938–3,014 个 owner，低于 1bp 比例约 43.56%–93.0%，证明数据可得但不能直接作交易特征 | 是 | 否 | 公共 RPC 限频与非生产 SLA；全量 program accounts 成本；把 holder 数错称独立买家；winner 回填；保存地址或做身份画像 | 等待不可回填自然样本，先比较错误率、响应成本、尘埃/集中度稳定性与后续固定时点结果；未经预注册成熟门不得接入 Strategy/Paper，unique buyer/cluster 仍需合规索引器或可验证交易级数据 | 2026-09-01 20:25 |
| FACT-001 | 长期总 Prompt §9 | 分离事实真实性、传播真实性、纠错/删除/反转 | PARTIALLY_DONE | `event-claim-assessment/v1`、`source-item-revision/v1`、`observation-provenance/v1` 与 `event-claim-relation/v1` 均已前向注册；关系与 revision 同事务追加。新增 `ObservationRevisionHandoff` 将本次 revision 与新 relation IDs 原子交给 Runtime，即使复用旧 Observation 也不会在早退中丢失；既有 tuple API 完全兼容，固定 `affects=none` | 独立内容 verifier 已工程上线；仍缺自然 verifier 样本、自然 correction/retraction handoff 样本和固定时点市场反转结果，工程实现不等于事实研究成熟 | 是 | 否 | Agent 自报被误当事实、关系动作被误当独立证明、转载被误算交叉确认 | 累积自然 handoff/relation/verifier 样本；下一步研究纠正/撤回后的固定时点市场反转 | 2026-09-01 06:56 |
| AGENT-001 | 长期总 Prompt §19 | 不同任务使用合适模型/推理强度，并发最多 2，完整 Token 记账 | CONTINUOUS | Trend/Source 为 Spark-low→Luna-low；Context 为 Luna-low→Terra/Sol-medium；Web 分模型/强度/Token 展示；新 Trend/Context 输出统一 identity/context-only，不再直接成为 F/C 决策证据 | 独立事实核验已上线；冲突驱动的受控质量升级尚未实现 | 是 | 否 | 空结果被误判失败、Agent 自报进入策略、重复升级烧额度 | 积累独立核验自然样本；所有核验结果继续 context-only | 2026-08-31 18:00 |
| AGENT-002 | 用户预算补充；长期总 Prompt §19 | 本机预算不过度阻塞，但保留有限门、冷却和退避 | CONTINUOUS | 当前上限 Trend 96/50M、Source 12/10M、Context 384/50M；并发 2。v2 自然窗口揭示 Token Context 全局冷却只在 Agent 完成后才可见，曾在 13:29:48/13:30:05、13:34:39/13:35:00、13:43:12/13:43:36 形成同一 4 分钟窗口双准入。现把冷却预留前移到准入后的首次 await 之前，Agent 失败则释放；按每 4 分钟最多一次的理论日上限 360，将本机有限调用上限从 256 调整为 384，50M Token 上限不变 | 需持续检查真实用量与 zero-yield cost；双准入修复后自然窗口尚待验证，不得因额度充足取消 trigger/cooldown | 是 | “取消预算”被有限大上限替代 | 无限制循环、重复调用与低产出成本；预留未在失败时释放 | 验证相邻 admitted 的起始间隔与失败恢复；只有真实 limiter 偏低时才提高，不移除触发门/冷却/退避 | 2026-09-01 21:50 |
| AGENT-003 | 用户“重点信息或买入后是否提高关注频率”提醒 | 对高价值事件、Token 或持仓进行有限、证据触发的复查，而非每项新建常驻 Agent | IN_PROGRESS | `agent-shadow-review-trigger/v2` 已于 2026-09-01 07:38 本机前向注册（activation relation 807、cohort 18595），只接收新 publisher correction/retraction。relation 与 immutable input 同一事务；结果显式区分 shadow_triggered、coverage_gap、ineligible。v2 要求 target Observation 只对应一个 Event；多个 Event 不再静默选最小 ID，而终结为 `ambiguous_target_event`。Event→Token 歧义、无 cohort、时间排除及 unresolved 均保留；Paper 持仓只由触发时以前不可变 BUY trade/净数量判定。DB 强制 dispatch_count=0、decision_eligible=0、affects=none；当前自然 input/dispatch 均为 0 | 当前只覆盖 correction/retraction；尚未覆盖高影响原帖、跨平台跃迁、canonical/migration/liquidity 独立触发。活动库历史上 target 多 Event 自然案例为 0，本轮测试仅证明 fail-closed 工程语义；无证据支持真实派发复查 Agent | 是 | 否 | 买入后确认偏差、重复 Agent 搜索、自我强化、静默任取 Event、无 cohort 静默漏记和额度浪费 | 前向积累每类至少 30 个终态、20 个事件/Token、10 天；PumpPortal migration 若扩展，必须明确标为 provider observation 并独立版本化，仍保持 dispatch=0 | 2026-09-01 07:40 |
| AGENT-004 | 用户补充 `XiaoDuoYa/codex-with-chatgpt`，并于 2026-09-02 至 2026-09-03 修订协同规则 | 重大架构、策略和实验方案在确有实质不确定性时默认由一条最高可用强度的 ChatGPT Lead 复核；结论冲突、信息不足或影响特别重大时才增加独立会话。低级路由、自述不一致或明显降质的回答废弃并新建会话重试 | CONTINUOUS | 历史三路复核及 Lead 系统研究均已保存；Codex 已核验其中可执行部分并保持唯一 writer。当前权威规则已固化在 `AGENTS.md` 与 `CURRENT_OBJECTIVE_AND_PLAN.md`，普通局部实现不再机械多审 | ChatGPT 协同不能取代本地代码、当前 SQLite、前向证据和运行结果；历史建议只进研究候选，不自动进入 Strategy | 是 | 旧“关键方案一律至少三审”已被用户后续“至少一个即可”取代 | 低级路由；重复泛泛意见；把模型自述当事实；协同流程挤占收益主线 | 只在 material gate、非局部失败或证据推翻假设时同步；Codex 最终核验、实现和测试 | 2026-09-03 |
| COLLAB-001 | 2026-09-02 当前 GXH ChatGPT 聊天 + 指定 Codex thread/history 的重复用户要求 | 建立不会随 chat/compaction 丢失的 ChatGPT↔Codex 协作控制层：两边都把用户意图视为互补权威；详细研究/反证/方案进入 E 盘 Common Space，实时消息只作 doorbell；Codex 仍唯一 active checkout writer，Lead ChatGPT 负责目标守护、研究、推理和纠偏；按任务复杂度治理开发子 Agent 成本，通用工具问题 open-source/official-first，避免无收益 review/audit/test；Lead chat 满时从 E 盘 rollover 继承且只能有一个有效 coordinator | IN_PROGRESS | 已完整按时间恢复目标 Codex thread 的 139 条 userMessage 并脱敏写入 `CHATGPT_RECOVERED_USER_REQUIREMENTS_2026-09-02.md`；本聊天新增要求写入 `CHATGPT_CURRENT_CONVERSATION_REQUIREMENTS_2026-09-02.md`；`COMMON_SPACE/`、Lead state/journal/rollover、execution-efficiency policy 已建立。目标 Codex thread 的 C 盘 ChatGPT-project mirror 被实读确认 `This project has no custom instructions`，而 E 盘 `AGENTS.md` 才含冻结契约，解释了 compaction 后跑偏风险。全局 `~/.codex/hooks.json` 仅作为薄 bootstrap 指向 E 盘 `codex_project_context_guard.cmd`，guard 只对 memeTrader/GXH mirror cwd 在 `UserPromptSubmit` 与 `SessionStart(resume/compact)` 注入 compact E 状态；sync pointer 已 `attention_required=true` 并指向不可变 ChatGPT→Codex correction alert/mailbox。一个 read-only ephemeral probe 无 hook 配置/信任错误，但约消耗 19k input tokens，故不再重复 | 当前 active Desktop 是否热加载新 hook 尚待自然下一 prompt/resume/compact 观察；本 ChatGPT tool surface 未暴露 Desktop 内部已存在的 `codex_app.send_message_to_thread`，且目标 Codex thread 当前 writer-locked，因此未用第二 CLI/app-server 抢同一 thread；Codex 尚未对本轮 alert 写 ACK/CODEX Common-Space note | 是 | 否 | 协作基础设施反客为主；两个 Lead 或两个 Codex writer；C mirror 被同步覆盖；过时 sync pointer；hook 反复注入造成上下文成本；把 Common Space 当第二套计划；为验证协作再烧大量 Agent/token | 下一稳定 checkpoint 由 Codex读取 `attention_required/open_groups` 与 Common Space alert，按 `ACK_IMPLEMENTED/ACK_DEFERRED/REJECT/SUPERSEDED` 只处理新增约束并写自己的 side note；自然观察 hook 是否生效。完成最小闭环后停止优化协作系统，立即回到 exact-post→source-fact→per-Token binding 的当前 P0 | 2026-09-02 22:18 |
| PAPER-001 | 用户 Paper 与成本要求；长期总 Prompt §18 | 前向 Paper、滑点/费率、账户曲线、分批止盈和 runner | CONTINUOUS | append-only cash/equity；4% 配置滑点、60bps、PumpSwap 125bps；两笔历史 Paper fill | 当前只有一个闭环；历史 fill 仍记录旧 2% 滑点 | 是 | 否 | 把 Paper 当真实利润、费用遗漏 | 等待新 cohort，按当时配置审计 | 2026-08-31 16:35 |
| PAPER-002 | 用户长期学习；长期总 Prompt §14、§18 | 热度/人物/社区只能先做 shadow，成熟后预注册策略 challenger；纯链上同入场可比较固定与动态退出 | IN_PROGRESS | `onchain-paper-exit-challenger/v2-fair-start` 已严格前向运行，复用纯链上策略的新入场，仅比较止损、移动止盈、分批止盈、流动性退出与 240 分钟上限；卖出只接受 Jupiter 特定数量最低输出，no-route 不伪装成交 | 当前自然成对样本很少；综合策略的叙事/人物/社区 challenger 仍未达到成熟门 | 是 | 否 | 后验调参、样本太少、把第三种退出方法误读为第三个选币账户 | 保持规则冻结，积累同入场成对结果；不按单笔赢家改阈值 | 2026-09-03 |
| PAPER-003 | 用户 2026-09-03 明确提出“纯链上买入后再分析热点/舆论/社区/新闻，好的可长持”，并要求所有策略计滑点和费用 | 建立策略 3：精确复制策略 2 新 BUY，只在买后用当时已观察叙事决定是否延长 runner；三账本前向、公平、可审计 | IN_PROGRESS | 产品与因果边界冻结为“三个模拟账户、两种入场逻辑”。策略 3 只接受激活后的 `source_buy_trade_id` exact pair；`onchain-paper-narrative-context/v1-forward-only` 已在 Strategy 2 BUY `133` 注册，旧 8 仓位全部排除不回填。新仓位只用完整前向时序快照触发，Seed 中断可恢复；买后结果强制 assessment-only，不能进入主 Event/Decision、仓位或退出。Portfolio 显示 eligible/seed/gap/dispatch/admission/assessment 分母 | 注册后尚无自然 Strategy 2 新 BUY，因此有效买后叙事 cohort 仍为 0；runner 未启用；Base/BSC/Robinhood 的 amount-specific route 和分链成本仍未完成 | 是 | 否 | 把策略 3 变成第三个选币器；复制旧赢家；后来新闻回填；DEX mark 冒充卖出；正面叙事覆盖不可交易状态；Seed 后崩溃永久漏调；单笔赢家改规则 | 等待下一笔自然 exact pair，核对一次 Agent 准入和 assessment；按预注册成熟门比较 2/3 的扣费 PNL、回撤、route failure 和尾部，成熟前不启用 runner | 2026-09-03 |
| PAPER-004 | 用户指出流动性归零与早跑收益、要求所有策略使用真实成本和可卖语义 | 区分系统错误、当前无路线、报价不经济与真实可执行卖出；不可卖时不生成虚假 PNL | CONTINUOUS | Jupiter Swap V2 官方 400 `Failed to get quotes` 已从通用错误改为 `no_route`；修复后首个自然结果 id `410` 正确落库，未生成 SELL。策略 3 从首个 4/4 核验截面自然增长到浏览器 QA 时的 7/7 exact paired，始终 0 mismatch、0 backfill，runner 仍禁用 | 历史 400 错误不追改；no-route 恢复频率、240m write-off 与早期可执行退出收益仍需自然前向样本 | 是 | 否 | 把无路线当系统故障；把 DEX mark 当可卖收益；为早跑结论剔除后来归零的亏损样本 | 保留每次指定数量报价终态，按 trigger→first economic quote 延迟、no-route 持续时间、扣费退出与最终核销比较策略 2/3 | 2026-09-03 |
| MISS-001 | 长期总 Prompt §17 | 系统化漏检、误报、迟发现、错映射账本 | CONTINUOUS | r5/r6 与粗 `missed-opportunity-audit/v1` 保留；细粒度图先修复 BSC discovery→hydration 和 reverse 单报价旁路。zero-yield 已在原图前向拆为 no-results、outside-time-window、identity-mismatch、mixed，并拒绝超过 5 分钟的未来发布时间。继续审计 19 个旧 `reverse_news_matched` 后发现 accepted=85，而实际新 Observation 仅 55（8 confirmation、47 identity/stale），Slate、ARROW、ROCK、Google Gemini 等同名新闻被误记为可决策数量；现已把 name-only 命中固定为 identity/audit-context-only，并分开 matched、decision-eligible、context-only 与 distinct-publisher 计数。部署后首个自然 `CHAD` 样本取回 72、名称命中 6、eligible 0、context-only 6，6 条均为 identity，且无后续 Decision/Paper | 新语义只对部署后 lookup 生效，旧 `no_matching_recent_news`、`reverse_news_matched`、粗 `no_decision` 与修复前 cohort 均不追改；需要继续积累真正项目语境/独立原始来源样本 | 是 | 否 | 用注册前赢家倒推、把缺记录称为拒绝、把 token-only 后续行为错归因、名称同词新闻升级为确认、发布域名冒充独立来源、未来发布时间进入 Event | 保持单一前向图，观察 identity-match 与真正独立证据的分母；不因名称命中直接进入策略，下一步再决定查询覆盖或 Context 触发改进 | 2026-09-01 06:16 |
| AUDIT-002 | 长期总 Prompt §16–17；identity/promotion 边界 | 召回统计必须区分全部匹配、可决策证据、仅身份上下文和旧未分类记录 | CONTINUOUS | Funnel/API/Web 现分别显示 reverse lookup all/eligible/identity-context/legacy-unclassified 与 Event↔Token all/eligible/context-only；机会召回只计记录时点已到达的 feature/confirmation。发布后 API：25 个 lookup found 中 eligible 0、identity-only 5、legacy 20；71 条 relation 中 eligible 8、context-only 63 | 旧记录不回填，因此 20 个旧 found 永久保持 unclassified；需继续积累新 explicit eligibility 样本 | 是 | 否 | identity/promotion 虚高信息捕获率；把旧 `accepted_count` 猜成可决策证据 | 只用新明确字段计算 evidence recall；持续对比 all-role 与 eligible 分母 | 2026-09-01 06:45 |
| RESEARCH-20260903-P0 | 2026-09-03 系统级研究与用户明确交接 | 按 deadline correctness→immutable launch facts→market surface→liquidity survival→execution realism 的顺序，把系统升级为严格前向 Meme 风险与机会学习系统 | IN_PROGRESS | 两份权威研究/交接文件已完整读取；P0-A 已实现最多4个 target 的有界并发、15秒/provider deadline cap、terminal-before-late-result，3项定向测试通过 | P0-B/C/D/E 与 P1/P2 尚未完成；自然修复后 sampler 样本仍需成熟 | 是 | 否 | 新研究污染生产 gate、旧赢家回填、LP drop 被误称撤池、增加 Agent 掩盖调度问题 | 受控部署 P0-A 后观察自然 target；随后实现 immutable launch facts 与 market-surface classifier，固定 affects=none | 2026-09-03 |
| RESEARCH-20260903-P0D | 2026-09-03 Lead research implementation handoff | 冻结新币首发事实、区分具体 market surface，并以前向同一 pair 固定时点学习流动性生存与失败模式 | CONTINUOUS | P0-B launch facts 与 P0-C market-surface v2 已上线；P0-D v1 partial 与 v2 contaminated 原样冻结，当前 v3 从 snapshot 623852 严格激活。2026-09-03 当前只读截面已有 384 cohorts、1,085 attempts、1,200 outcomes；调度/索引工程门已通过 | 仍需按日期、surface、missing/error、流动性崩塌与可执行卖出状态评估经济成熟度；Pump canonical 仍需真实 IDL/RPC pair-account proof | 是 | 否 | 版本交叉污染、bonding curve 混入 AMM estimand、Dex liquidity drop 被误称 LP withdrawal | 保持 v3 前向积累，不再扩建同类基础设施；主工程转入 P0-E amount-specific execution | 2026-09-03 |
| RESEARCH-20260903-P0E | 2026-09-03 系统研究、当前 Runtime 代码与用户对虚假 PNL/真实成本的反复要求 | 让主综合 Paper 在实际策略金额上取得同一时点 BUY→SELL 的 amount-specific route、最低输出、链费和失败语义；不得用 Dex 参考价冒充可实现成交 | IN_PROGRESS | `event-route-execution-challenger/v1-entry-preflight` 已在独立 registration/attempt 账本前向注册：固定最大仓位 probe 仍只作容量门；最终 policy size 以 USDC raw amount 冻结后重新请求 BUY，并用 BUY minimum token output 做即时 SELL preflight。保存双腿 raw amount、请求/完成时钟、route-only 成本、费用完整性以及 no-route/error/protocol-invalid/interrupted 终态；固定 `decision_eligible=0 / affects=none / no_fill_research_only`，旧主 Paper 与 Live 不变 | 当前 r6 仍无自然 route-backed Decision；quote-only 返回的网络费 lamports/同时点 SOL-USD 转换尚不能支持严格链费，故有效报价也会明确标 `cost_unknown` 而不成交。实际 remaining-raw SELL、部分退出、账户/仓位 ledger 与主 Paper 晋级仍未完成 | 是 | 否 | 直接移除 WAIT 会把容量探针误当真实成交；重复计入滑点/费用；不同金额复用 route；没有 SELL route 却显示浮盈；把 entry preflight 当成交 | 先收集注册后的最终金额 paired 终态并补齐链费；随后实现独立 remaining-raw position/SELL challenger。达到跨日配对成熟门前不替换主 Paper，不进入 Live | 2026-09-03 |
| CHAIN-ROBINHOOD-001 | 用户多次补充 Robinhood Chain；2026-09-03 官方 Robinhood/0x 文档与当前 r6 | 将 Robinhood Chain 作为独立 EVM L2 研究链，覆盖新池、Meme/RWA 资产分类、动态 ETH gas/L1 data fee、安全、amount-specific route 与失败语义 | IN_PROGRESS | GeckoTerminal 官方 network id=`robinhood` 已验证；当前 24h 本机发现约 9,942 个 Robinhood Token。官方确认 chain id 4663、Arbitrum L2、ETH gas；0x 已支持 4663 及多类 DEX/launch/bonding-curve 来源。Robinhood 官方 `/rhj/assets` 当前返回 194 个 Stock Token 合约；本机 Robinhood Token 总表与其重叠 103 个，证明资产分类是实际而非理论需求 | 当前只做 discovery/hydration `research_only`，不派 Agent、不写 Decision/Paper；尚未建立官方 Stock Token/RWA append-only 排除表、0x API key/route、L2+L1 fee、安全与成熟门 | 是 | 否 | 把 Stock Token/RWA 当 Meme；把 Solana/BSC 费率复制到 Robinhood；使用当前聚合页面回填历史；把 route support 等同于可盈利 | 先以前向官方资产 registry 建立 `stock_token/rwa_excluded` 分类；随后仅对剩余 Meme-like cohort 做 0x amount-specific Shadow 和链费观测，达到成熟门后再讨论候选链 | 2026-09-03 |
| X-001 | 用户 X 名单；长期总 Prompt §11 | 候选账号目录，不继承人工 S/A 评级，持续核验 handle/职责/价值 | CONTINUOUS | 107 条 v3 目录；CoinbaseAssets 已由 CoinbaseMarkets 取代；critical 只影响轮换 | 新追加的 BNO/娱乐/crypto 账号仍待逐一核验 | 是 | 人工评级已 INVALIDATED | 冒充、改名、推广利益冲突 | 分批核验并以 exposure/zero-yield 评价 | 2026-08-31 16:35 |
| TG-001 | 最新长期总 Prompt §1、§12 | 用户允许项目内 Telegram 配置和只读 allowlist | SUPERSEDED | 最新授权已写入本台账与 Web policy | 授权不等于平台许可 | 否 | 旧“因用户选择永久禁止”已被取代 | 容易误读为可绕过平台条款 | 与 TG-002 分开判断 | 2026-08-31 16:35 |
| TG-002 | Telegram 官方 Content Licensing/API 条款；长期总 Prompt 的客观判断要求 | 自动抓取、聚合并送入 AI 前必须满足平台条款 | BLOCKED | 2026-08-31 官方条款明确禁止面向 AI/ML 的 scraping/indexing/aggregation/deployment；Web 返回 `blocked_by_platform_terms` | 未取得 Telegram 及所有相关用户的明确、持续、限定同意 | 是 | 否 | 账号/API 封禁、版权与隐私 | 保持自动正文采集和 Agent 摄取关闭；定期复核官方条款 | 2026-08-31 16:35 |
| TG-003 | 长期总 Prompt §12.1–12.2 | Telegram 候选、身份、角色、替代机器源目录 | IN_PROGRESS | `SOCIAL_SOURCE_CATALOG.json` 已扩充为 13 个 Telegram 候选；未核验/非官方/transport 明确标注 | 仍需逐一核验当前 profile/运营方/活跃性 | 是 | 否 | 把搬运当官方、一源多算 | 从官网双向核验；优先使用外部原文/RSS | 2026-08-31 16:35 |
| TG-004 | 长期总 Prompt §12.3–12.9 | MTProto collector、message revision/tombstone/exposure/provenance/learning | BLOCKED | 当前 r6 无 Telegram 表/Observation/poll；Web 明确显示 0 messages/0 exposure | 全部工程链路未上线 | 是 | 否 | 与 TG-002 平台条款冲突 | 只有合规条件改变后再按严格 allowlist 设计评审 | 2026-08-31 16:35 |
| TG-005 | 用户“Telegram 失败时作备用” | 人工发现后回到官网/RSS/X/链上原始证据 | CONTINUOUS | Dex Token Telegram 链接仅保存为 manual identity/promotion；通用 Agent/Bridge 拒绝 t.me；`telegram-manual-external-origin-handoff/v1` 可在本机提交站外原始 URL 并重新抓取 | 需持续衡量这些链接是否带来可核验外部原文 | 是 | 否 | 项目方自报和转发污染 | 保留成功、失败、重复和 zero-yield 分母；只生成 context | 2026-08-31 20:20 |
| TG-006 | 用户“让 Telegram 机器人转发到本地”及“曲线实现” | 在不摄取 Telegram 正文的前提下建立可运行交接 | CONTINUOUS | append-only attempts/results、SSRF/重定向/大小/类型检查、本机 POST、公网 403、Sources 双语表单与 Audit 证据差距已实现 | 尚未创建 Telegram Bot 凭据或自动转发；当前由用户主动提交站外 URL | 是 | 否 | 自动机器人仍受平台条款及凭据创建确认约束 | 先积累真实手工交接分母；只有合规条件改变才评审自动化 | 2026-08-31 20:20 |
| DEVNET-001 | 用户真实交易测试讨论 | Devnet 可核验签名；Mainnet 仍锁定 | BLOCKED | 隔离 Devnet Wallet 工程存在 | 测试钱包无 Devnet SOL，尚无公开可核验 signature | 否 | “只接私钥即可无缝实盘”已 INVALIDATED | 用户曾暴露的私钥不可使用 | 仅用户自行充值 Devnet SOL 后做最小签名验证 | 2026-08-31 16:35 |
| PUBLIC-001 | 用户公开 URL 要求 | 受保护公网只读控制台，不暴露 loopback/secret | PARTIALLY_DONE | Quick Tunnel 与 8788 token protection 正常；当前公网 URL 未鉴权 GET 返回 401，本机 8788 单 listener | URL 为临时域名，不是固定自有域名 | 是 | 否 | 临时域名变化、口令泄露 | 固定域名仍需用户自有 Cloudflare Tunnel；继续验证未鉴权拒绝 | 2026-09-01 15:23 |

## 2026-09-03 当前周期追加处置

| ID | 来源 | 真实需求 | 状态 | 已完成证据 | 仍缺 | 严格前向 | 主要禁止项 | 下一步 |
|---|---|---|---|---|---|---|---|---|
| CHAIN-EVM-002 | 用户要求 Sol/Base/BSC/Robinhood 多链、分链手续费、真实可卖语义 | 在不复制 Solana 成本模型的前提下，为 BSC/Base/Robinhood 建立指定金额双向 route 与完整费用成熟路径 | IN_PROGRESS | `onchain-only-shadow-evm-uniswap-v3-quote/v1-forward-only` 已在 cohort `2112` 后注册；固定区块、direct/mixed-fee 两跳、BUY→SELL、双边 4% 不利滑点和错误分型已实现；真实 BSC `$35` pool-math quote 成功；生产三表已建立且 0 策略污染 | 自然 post-registration EVM cohort、gas/L1 fee、Router simulation、transfer tax/blacklist、Base/Robinhood live quote 与跨日经济样本 | 是 | 把 Quoter 当可执行交易；把 minimum recovered 当 PNL；回填旧 cohort；在费用不全时启用 Paper | 等待首个自然 EVM cohort；再补完整交易成本研究层 |
| UI-PORTFOLIO-003 | 用户反复指出三个现金难懂、UI 很乱、PNL 必须真实 | 三策略易懂、研究/账户分层、未知估值不显示假 PNL | PARTIALLY_DONE | 首屏现在先显示三策略 Tab；公平说明、策略 1 route 与 EVM research 默认折叠；非 Solana 固定显示 `PAPER DISABLED · RESEARCH ONLY`；浏览器实机 QA 显示 Runtime/SQLite 动态脉冲 | 策略 3 买后研究区仍可在获得自然 assessment 后继续压缩；全站视觉深化后置 | 是 | 把研究数据混入账户；把 null 显示为 0；为了美观伪造曲线 | 用自然数据继续验证布局，只有误导/不可操作时再改 UI |
| PAPER-003-UPDATE | 策略 3 后续自然进展 | 同入场买后叙事增强持有，保持公平和严格因果 | IN_PROGRESS | 当前浏览器截面已显示 15 个 exact pair、0 mismatch；7 个 context-eligible 中 7 个已 seed，5 个 coverage gap、0 assessment/0 admitted、runner=0；早期 8 个注册前仓位仍不回填 | 首个真实 Agent assessment、独立来源成熟度、策略 2/3 成对退出与扣费结果 | 是 | 用后来新闻改入场；无 assessment 就启用 runner；用单笔上涨改规则 | 继续自然采样，不因 sparse assessment 增 Agent 或放宽证据门 |
| PAPER-003-CONTEXT-V2 | PAPER-003 的自然覆盖缺口；用户要求持续学习但禁止未来数据 | 避免 60 秒首次检查过早永久漏掉买后调查，同时不把后来快照倒灌为入场信息 | IN_PROGRESS | v1 不可变终态为 11 seed（7 gap/4 triggered）；v2 从 BUY `155` 后前向注册，优先买后刷新快照，否则仅用入场前已完整可用的 exact cohort trigger snapshot；开放仓位延期任务按 exact transition 去重并优先于 bulk metadata；Portfolio 区分当前 snapshot basis 与旧版冻结记录 | v2 首个自然 seed/admission/assessment、独立来源产出、策略 2/3 成对扣费退出与跨日成熟样本 | 是 | 改写 v1；用入场后信息改变入场；仓位关闭后继续烧 Agent；未成熟启用 runner | 等待自然 v2 exact pair；按 snapshot basis、准入原因和真实退出结果比较，不增并发、不降门 |
| RESEARCH-20260903-P1B | 用户要求研究开发者/创建者重复发币、早期行为与流动性死亡，并持续优化 | 以 PumpPortal 本机已收创建消息建立严格前向的启动者历史 Shadow；不得冒充 RPC 验证或完整实体画像 | IN_PROGRESS | `creator-launch-risk-shadow/v1-local-history-lower-bound` 于 launch fact 10533 后注册；截至 02:03:37Z 有 68/68 个合格自然 cohort、0 个注册前 cohort；Token API/UI 已显示，4 项定向验证通过 | 等待 240m 与 60m 流动性结果成熟；补 RPC 创建者/首批买家实体证据前不形成硬门或分数 | 是 | 地址可轮换、历史左删失、provider 字段并非链上证明 | 先比较重复发币频率与严格前向结果，不按少量赢家/失败者改策略 |

## 当前真实运行快照

- 核验时刻：2026-09-02 12:39（Asia/Shanghai）。当前 r6 为 SQLite WAL，Web health=`ok`；本轮未删除、清空、回填或改写历史证据。
- 主 Paper：cash=`995.681236 USD`、realized PNL=`-4.318764 USD`、0 个开放仓、8 个 fill（4 BUY / 4 SELL）；这只是模拟执行结果，不是 Mainnet 利润。全库有 2,642 个 Decision、6,409 个 Observation、4,643 个 Event、142,648 个 Token。
- 最近 24 小时：51,867 个新 Token、1,864 个新 Event、1 CANDIDATE / 336 REJECT / 735 WAIT；主 Paper 1 BUY + 1 SELL。Token Context 369 次 admitted，结果 333 `no_context`、29 `insufficient_reachable_sources`、1 `cross_source_supported_context_only`、1 `agent_error`；新增 1,092 条 Event↔Token relation 全部 `decision_eligible=0 / affects=none`。主要断点仍是及时独立证据到 exact canonical Token 的转化，不是 Token 供给或预算。
- Agent 当日核验截面：Trend `9` calls / 459,189 tokens；Source `0`；Context `59` calls / 4,647,427 tokens；并发上限 2。当前无预算阻塞，Context 的主要结果仍是 `no_context`。
- 最新证据断点：浏览器桥真实记录使用 `source=x:<handle>` 而非测试形态 `browser:*`，查询已兼容 `raw_json.browser`。自然样本 `A6Ew…pump` 又证明新发现链接可能指向 2019 年旧 Elon 帖；现已在 Agent 准入与优先队列前用 X Snowflake 做确定性时间过滤，并合并 Truth Social 同帖 URL 变体。普通 X/KOL 轮换持续有新正文入库；后续 `20260901T2000Z:x:@bubblemaps` 已以 priority 4 / critical 留下 completed、accessible 心跳，证明扩展采用了新轮换配置，因此 Reload 不再是当前阻塞。尚未完成的是 priority request→新鲜正文→独立 confirmation→精确 Token 绑定→Decision/Paper 的自然前向闭环。
- Runtime：Paper 计划任务保持 `Running / IgnoreNew`；8765/8787/8788 各一个 loopback listener，Web/SQLite 正常，Live=`false / locked`。浏览器桥一度约 2.1 秒返回 200，暴露 Web 的 0.5 秒探测过严；探测容限已调整为 3 秒并部署。重载后又出现一次直接 `/health` 超过 15 秒的瞬时阻塞，随后自然恢复，当前 Web health 已重新显示 `bridge_reachable=true`。因此现状是“已恢复但存在事件循环延迟样本”，后续按真实复发记录处理，不能把 listener 存在等同于持续可用。ChatGPT 高智能协同入口已统一切换为 `@笔记本mcp20260902`，本次关键方案 3/3 独立复核已完成。

这些数字是本次核验快照，会继续变化；后续不得直接复制为“当前状态”。
