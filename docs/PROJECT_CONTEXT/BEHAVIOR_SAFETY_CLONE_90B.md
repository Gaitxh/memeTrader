# 90-ADDENDUM-B — behavior safety and clone consensus

REPLY_TO: C2C-20260909-STRATEGY-SYSTEM-SAFETY-90-ADDENDUM-B

Common Paper BUY guard now reads at most2 indexed existing vault_frame evidence
rows for exact token/pool. Reuses capital_context confirmed observer/provenance,
observed<=recorded<=decision and15s freshness. Protocol-confirmed KNOWN effective
quote reserve<=0 hard rejects; real reserve zero alone is not rejection. Existing
contract sell/rug/identity vetoes remain. SYNTHETIC_SUPPORT_PATTERN and
UNWIND_HAZARD_PRECURSOR_RECOVERY_UNKNOWN are WAIT_HAZARD, not hard scam findings.
Cached security permission cannot bypass a fresh local hazard. Pending work stays
bounded and resumes on later evidence; no permanent token blacklist. No extra
request while the local hazard persists. Missing/expired behavior is not proof of
normal behavior. No new concentration/wash numeric threshold or LP-lock rule.
Audit assessments split hard_veto,soft_hazard,behavior_sources; ledger statuses
REJECT_BEHAVIOR/WAIT_HAZARD support separate unique-cohort counts. Existing contract
vetoes also carry hard_veto. Source clocks are never refreshed by the safety worker.

clone_consensus_leader_v2 reuses5--25 frozen same-chain/lifecycle/symbol candidates,
existing600s discovery span and30s freshness. Requires unique liquidity and volume
leaders to equal the exact token/pool, then strictly later fresh original-pool
confirmation. Freeze must be after activation; no hindsight winner replacement.
A5U/max4 proposal is registered in KV as observer_only/decision_eligible=false,
affects=none. It is NOT appended as a funded policy. Runtime removes its signal
before Paper routing and writes idempotent episode-keyed Shadow evidence with
local safety assessment, never execution authority. Any future Paper promotion
still requires the common BUY safety gate; no funded duplicate control now.

Supply cutoff09:29:49Z: current bounded state has0 clone episodes/0 overlap episodes,
5 resilience episodes and200 identities. Historical formal clone source has4
unique episodes onSep8, liquidity4positions/volume2; this does not prove overlap.
Current capacity is not expanded and no threshold relaxed. INSUFFICIENT_NATURAL_EPISODES.

Tests:12 cohort selector cases PASS;4 behavior tests PASS, including cached-pass
bypass prevention and hazard recovery; existing14 safety cases PASS. One Store
Shadow registration/activation/dedup/no-funded-arm case PASS. Initial fixture
failures were fixed (missing active epoch; future observed time). No production
clock relaxation. No new scheduler,requests or watch capacity.

Natural runtime acceptance pending below; no Alpha or empirical false-positive
rate claim. Old admission84 audit remains discontinuous and outside this patch.
