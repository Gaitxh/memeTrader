# 项目状态快照 — 2026-08-31

本快照响应用户对整段聊天的再次全量复核要求。完整逐项状态仍见 `REQUIREMENTS_ACCEPTANCE_2026-08-30.md`；本文件记录其后的真实增量、首笔 Paper 交易审计和下一阶段边界。运行数字会继续变化，以当前 API、配置和 SQLite 为准。

## 1. 目标没有被后续补充覆盖

长期目标仍是：在个人电脑上前向采集新闻、社交、名人原帖、新 Token/池和链上状态，学习哪些来源、平台、人物与主题更值得优先观察，通过可审计的 WAIT/CANDIDATE、Paper 成交和固定时点随访改善样本外、扣除成本后的风险调整表现。

已实现的网站、采集器、Agent、Dex 链接溯源、双向证据链、Paper、双语动态 UI、公开鉴权入口、Settings、审计和项目记忆继续保留。新增需求默认追加，只有明确取消或与安全不变量冲突时才可跳过。

## 2. 状态分类

### 已实现并保持

- Overview、Events、Tokens、Decisions、Paper、Wallet、Agents、Sources、Audit、Settings 的真实 SQLite/Runtime 控制台；中英文、响应式和低成本自动刷新。
- RSS/Google News/Mastodon、PumpPortal、GeckoTerminal、DexScreener 和安全服务；Dex 附带链接严格区分 identity/promotion。
- Trend Scout、Source Discovery、Token Context 三类受预算 Agent，任务级模型/推理路由和按模型/强度 token 账本；物理并发不超过 2。
- `feature/confirmation/identity/promotion`、未来数据拒绝、r5 错误案例隔离、r6 stale 证据审计、WAIT 中性语义。
- Paper 账户曲线、固定不利滑点、双边手续费、已知 Token 税、执行尝试、新鲜报价门、持仓监督和历史成交；所有金额明确为模拟。
- Paper 成本基线已进一步分层：每侧 4% 只是不利执行压力；通用场地费每笔 60 bps，明确识别 PumpSwap 时用 125 bps 保守上限；滑点、场地费、Token 税和未建模链/priority fee 不再用一个模糊百分比表达。当前保持一次入场与最多四层剩余仓位止盈，紧急退出仍全仓卖出。
- 公开 URL 使用 loopback origin 与鉴权隧道；API 不返回 secret；网页不能开启 Live。
- `source-poll-exposure/v1` 为 RSS、Bluesky、Mastodon 和 Token 反向新闻的每次真实请求前向记录完成、零新增、重复、过滤、仅上下文和错误；Sources 中英文展示脱敏分母，统计不自动改变调度或交易。
- `token-discovery-exposure/v1` 为 GeckoTerminal 新池、DexScreener 各 discovery surface/hydration，以及 PumpPortal create/migration 固定流窗口记录真实轮次、空窗口、错误、重复、本机首次发现、链接、快照与 no-pair；只对本机首次发现 cohort 描述后续 CANDIDATE/Paper 漏斗，历史不回填且不自动改变策略。

### 部分实现或等待真实前向样本

- X 等平台的登录状态不等于实际采集。当前关键断点仍是专用 Chrome/Edge 扩展对精确账号页产生真实 heartbeat、账号暴露和原帖 Observation；Codex 侧栏页面不会自动进入该桥。
- 选择性学习、主题通道、账号暴露、Shadow 与 Token Context 随访机制已实现，但样本量、日期和亏损多样性都未达到成熟门，不能声称已学出最优来源、人物或主题。
- Token Context 旧调用没有当前 assessment 记录；首笔 Paper token 也没有 assessment。当前 0 follow-up 是 0 assessment 的必然结果，并非 finalizer 漏跑。新 `token-context-admission/v1` 已开始前向持久化准入、预算和冷却原因；旧调用仍不回填。
- 发布前 Shadow 表中的既有 cohort 仍保留原版本，不做迁移或回填。当前源码是 `v3-strategy-labels` / `admission/v2-all-actions`；只有发布后首次真实 WAIT、REJECT、CANDIDATE 才获得新标签和独立入库时间证明，旧候选继续如实计为历史/未受监测。
- Agent 已按任务复杂度分层，进程/额度/结构无效会回退；基于事实冲突或证据复杂度的通用质量升级仍未实现。
- Token/池发现面的前向轮次与暴露分母已经统一到独立账本；上线前的历史轮次不会回填，首次样本仍需常驻进程发布后真实积累，不能把空样本解释成无价值，也不能把后续入选解释成来源导致收益。

### 外部人工步骤

- 精确社交平台采集需要用户在专用浏览器加载扩展、配置本机 Bridge Token、完成必要登录并保持公开账号页打开；MFA、验证码、CAPTCHA、手机号和条款确认必须由用户处理。
- Devnet 钱包仍需少量测试 SOL 才能取得可公开核验的 signature。没有 signature 不能声称真链测试成功。
- 临时 Cloudflare Quick Tunnel 不是固定域名；固定 URL 需要用户自有 Tunnel/Access。

### 未实现且当前禁止

- Mainnet Broker、私钥签名、广播/确认、路由费与链费、失败重试、防重复、余额对账、紧急停机和小额真实成交验收均未完成。`live.enabled=false`，网页永远不可切换；用户曾粘贴的私钥视为暴露，不能使用或保存。
- Telegram 仍是人工目录和发现备用，不自动抓取、索引正文、转发到 Agent 或作为独立交易证据。
- 不自动注册、关注、点赞、发帖、私信、读取密码/Cookie/Session/验证码或规避平台限制。

## 3. 首笔 r6 Paper 交易的真实审计

- Event 393：NASA 新空间望远镜事件；Token 为 Solana `Roman Telescope (ROMAN)`。
- 2026-08-30 17:48:51Z 形成唯一 CANDIDATE，score 81.06、match 61.18、canonical margin 32.11、外部来源 6、流动性约 111,484 美元、5m 量约 18,335 美元。
- BUY 使用当时重新取得的 DexScreener 报价，执行价包含 2% 不利滑点和 60 bps 手续费；约 11.45 美元 Paper gross。
- 约五分钟后流动性快照突降至 0.11 美元，触发 `liquidity_emergency` SELL；卖出同样使用新鲜报价、2% 不利滑点和 60 bps 手续费。
- 完全平仓后费后 Paper PNL 为约 +0.308706 美元。它是真实前向模拟闭环，不是 Mainnet 利润，也不是足够的策略表现样本。
- 没有发现未来数据、推广榜单或普通词误匹配污染；但一个样本不能证明 NASA、France24、Guardian、RTHK、The Verge 或 Google News 有预测价值。

## 4. 本轮已证实并修复的学习语义

审计确认旧闭仓归因会在平仓时按 `event_id` 重新扫描开仓前来源。它虽然有三时间戳门，但交易记录没有 `decision_id/cohort_id`，同一事件出现多个 Token 或多轮决策时无法证明某个来源确实属于该笔成交；卖出税也没有从来源学习的净回报中扣除。

从 `paper-source-attribution/v2-decision-cohort` 起：

- 最终 CANDIDATE 的 `decision_id` 与 `create_shadow_event_cohort()` 返回的 admitted `cohort_id` 会同时写入 position、BUY 和每次 SELL。
- 前向 event-action cohort 现覆盖首次 WAIT、REJECT、CANDIDATE，并冻结事件/来源/人物/链上质量/拒绝原因的粗粒度研究标签；所有后续快照必须同时满足 `observed_at` 与本机 `ingested_at` 时间门。
- Paper execution attempt 已链接 `decision_id/cohort_id`，可区分成功、执行拒绝和旧版未链接尝试。
- 完全平仓只读取该 cohort 在决策时冻结的最早合格 `feature/confirmation`，并写入同一 decision/cohort 的 `source_utility_outcomes`。
- 买入成本包括 gross+fee；卖出净额包括 gross-fee-known_tax，滑点由真实模拟成交价和数量体现。
- 每个完全平仓 round 都写 `paper_source_attribution_attempts`；缺 decision/cohort、主键不匹配、无合格来源或现金流无效均明确记录 skipped 原因。
- 原来 1 个事件时间窗闭仓结果迁移后标为 `legacy-event-window/v1`，保留审计但不进入学习，不推测或回填 decision/cohort。
- Sources 诊断分别展示 feature、confirmation、identity、promotion；只有 observed/ingested/published 都不晚于事件首个最终 CANDIDATE 的 feature/confirmation 才计入决策时合格率与候选前关联。

这修复的是归因可审计性，不是因果识别。新账本只能描述“被选中、成交并平仓”的条件样本，不能声称某个来源、人物、平台或题材导致收益；也不改变证据、候选、风险、仓位、退出或 Live。

## 5. 下一阶段按证据推进

1. 等待第一个新 CANDIDATE Paper round 完全平仓，验证真实数据库中的 `decision → cohort → fills → attributed close`；旧 ROMAN 交易保持 legacy，不回填。
2. 已实现 Token Context append-only admitted/skipped、预算/冷却原因账本；继续等待预算重置后的真实准入，绝不把 skip 当 `no_context`。
3. 已实现 Shadow cohort created/skipped 原因、历史/未监测分离与新 CANDIDATE 覆盖率；继续等待第一个真实新 CANDIDATE cohort，不回填旧 CANDIDATE。
4. 让专用浏览器桥产生第一个精确 X 账号暴露和原帖 Observation，完成账号学习链最前端。
5. 观察 PumpPortal、GeckoTerminal 和 DexScreener 新前向账本的真实空轮次、错误、首次发现与后续候选漏斗；不回填旧轮次，也不据未成熟统计自动调频。
6. 继续真实 Paper/Shadow/Token Context 前向运行；样本成熟前不放宽门槛、不自动改策略。
7. Devnet 有测试 SOL 时只做一次最小人工签名验证；Mainnet 继续锁定。

## 6. 本轮发布验证

- 全量 Python：167 tests passed。
- `compileall`、前端与浏览器扩展 JavaScript 语法、详情请求竞态测试、`git diff --check`：通过。
- `doctor --online`：SQLite、Live lock、Bridge、Codex、DexScreener、GeckoTerminal、安全服务和必需 RSS 全部通过，0 warning / 0 error。
- 活动 r6 迁移后，Sources API 显示精确闭仓 outcome 0、旧事件时间窗 outcome 1、精确归因尝试 0、成熟标签 0；证明旧 ROMAN 交易被保留但没有伪造成新 decision/cohort 样本。
- 验收时新 Token Context 准入账本已有 8 条真实前向检查：包含 `no_eligible_trigger` 与 `daily_token_reserve_exceeded`，0 条伪造 Agent 结果；Shadow 准入账本尚无新 decision，唯一旧 CANDIDATE 明确显示为历史/未受监测。
- 本机 Web、鉴权 origin 与 Bridge 继续只监听 `127.0.0.1`；公开入口未鉴权 401、正确鉴权 200。
- 13 个本机 API 响应约 606 KB 做了当前配置秘密值扫描，0 命中；Live 仍为 `enabled=false / locked=true / available=false`。
- 浏览器实测 Sources 的精确 cohort 学习说明、F/C/I/P 角色分栏、中文/英文和 390×844 响应式；390px 下 body/document 宽 375、表格容器 350/1653px，页面无整体横向溢出，宽表只在自己的容器内横向滚动；控制台 0 error。

### `source-poll-exposure/v1` 续轮验收

- 全量 Python：170 tests passed；`compileall`、Web/扩展 JavaScript 语法和 `git diff --check` 通过。
- `doctor --online` 再次返回 0 warning / 0 error；计划任务仍为 Running/IgnoreNew，强制结束旧 Runtime 子进程后监督器自动拉起新 PID，8765/8787/8788 各只有一个 listener。
- 首次验收快照已有 98 次真实请求：97 completed、1 running、0 error、96 个 completed zero-new；累计抓取 1694 条，前向新入库 1 个 Observation/1 个事件。Le Monde 轮次提供这条新增，其余已见内容如实记为零新增；数字会继续变化。
- Local Web、Bridge、公开隧道分别验证 200；公开隧道未认证 401、正确认证 200。API 与页面只返回脱敏 `source_key`，不返回原始查询或带参数 URL；Live 仍为 false。
- 浏览器实测中文/英文、动态刷新和 390×844 响应式；390px 下 body/document 宽 375、表格容器 350/1103px，页面无整体横向溢出，宽表仅在自身容器滚动；控制台 0 error。

### 全量需求复核、Token 发现面与成本模型续轮

- 全量 Python 176 tests passed；`compileall`、Web/扩展 JavaScript、详情请求状态测试、`git diff --check` 与 `doctor --online` 通过，在线诊断 0 warning / 0 error。
- 验收快照为 1,512 observations、985 events、32,050 tokens、472 decisions、2 笔 Paper 成交、0 开放仓位；Paper 现金/权益约 1,000.308706 美元。它仍只是 1 个历史闭环，不是策略表现结论。
- `token-discovery-exposure/v1` 已有 165 个真实轮次：156 completed、5 interrupted、0 error；3,942 个返回项、2,635 个 Token 暴露、460 个本机首次发现。首次 cohort 尚无后来 CANDIDATE/Paper 买入，不能据此评价入口收益。
- `source-poll-exposure/v1` 验收时为 903 attempts、881 completed、13 error、9 running；累计 15,947 fetched、44 个新 Observation、31 个新事件。运行中请求和后续数字会继续变化。
- Agent 有限上限已提高并实际生效：Trend Scout 96 calls/50M tokens、Source Discovery 12 calls/10M tokens、Token Context 96 calls/50M tokens；并发仍为 2，触发门、全局/单 Token 冷却和退避保留。日常巡检会复核 call/token 余量和拒绝原因，必要时继续提高有限上限。
- Paper 新基线已生效：每侧 4% 不利执行压力、通用 60 bps 场地费、明确 PumpSwap 时 125 bps 上限；Overview API 与中英文页面显示一致，Live 仍为 locked。
- Windows 计划任务为 Running/IgnoreNew；8765/8787/8788 各一个 loopback listener，Bridge/Local Web/鉴权 origin 正常；公开 origin 未认证 401、正确认证 200。
- 浏览器实测 Overview 成本说明、Sources Token 发现面、中文/英文、21 秒动态刷新和 390×844 响应式；390px 下 body/document 宽 375，表格容器 350/1366px，页面无整体横向溢出，宽表只在自身容器滚动。
- 学习有效性审计进一步收紧：Shadow 人物/平台/题材统计只使用每个事件时间最早的 cohort，后续 WAIT/REJECT/CANDIDATE 仍保留在动作账本但不构成独立样本；Token Context 重复调查同一 Token 只贡献一个成熟度样本；`watch-attention/v3-experiment-gated` 禁止以平台总体结果替代具体账号，实体缺失或冲突时保持基线。
- Overview 新增真实“前向学习状态”：显示当前版本独立事件、W/R/C、固定时点观察/缺失、独立 Token、精确关联 Paper 入场/平仓以及 Phase 2 数据门。“正在收集”不能误报为“已经学会”。
- 当前运行库远未达到 Phase 2：当前版本还没有足够的 REJECT/CANDIDATE/关联平仓，固定时点缺失率也未达门槛。预注册 challenger 继续不启用；首个实验候选应优先比较保守提前止盈 + 固定 runner，而不是利用人物或热度放大仓位。
- 全量聊天需求仍不是“全部完成”：X 精确账号 Bridge 暴露为 0、Phase 2 预注册 challenger 尚未实现、Devnet 无 signature、固定公网域名未配置、通用事实冲突升级未完成、Mainnet 与 Telegram 自动摄取保持禁止或锁定。长期目标继续进行。

### 选择性注意力实验发布前增量

- 新增 `attention-experiment/v1`：在一对同平台、同优先级、非 critical 的普通账号间随机分配一个观察槽位；每组固定 60 个 assignment，2:2 平衡区块在 Agent 调用前持久化。
- 主分析采用 intention-to-observe。Agent 错误、进程中断、零产出、跨组事件碰撞和 60 分钟 missing 不会从分母消失；只有 run/platform/handle/entity 精确匹配的原帖观察能够链接。
- 固定 assignment 与市场结果全部终结前不能显示 Stage 1 通过；通过仍要求独立时间顺序 holdout。账号/主题历史相关性倍率固定 `1.00×`，实验结果不自动改调度，也不进入证据、决策、风控、仓位、退出或 Live。
- 增加 Trend Scout 调用与 Runtime 入库之间的完成握手：进程重启、Agent 错误和入库中断均有明确终态，不能把未入库结果误显示为“完成但零产出”。
- `max_concurrent_agents` 在配置加载层和 Web 层统一限制为 1–2，符合个人电脑单实例运行边界。

### 选择性注意力实验部署验收（06:38Z）

- 全量 Python 182 tests passed；`compileall`、前端 JavaScript 语法、`git diff --check` 与 `doctor --online` 通过，在线诊断 0 warning / 0 error。
- 单一 Windows Paper 计划任务保持 Running；8765/8787/8788 各一个 loopback listener。SQLite `quick_check=ok`、WAL，Runtime、浏览器桥、本机 Web 和受保护公网 origin 正常，Live 仍为 `enabled=false / locked=true / available=false`。
- 已前向注册并激活 `watch-x-openai-vs-anthropic-20260831-v1`：X `@OpenAI` 对 X `@AnthropicAI`，每组固定 60 个 assignment；验收时两组均为 0/60、Stage 1 collecting、实际倍率 `1.00×`、自动提升关闭、独立 holdout 必需。
- 验收快照为 1,998 observations、1,311 events、47,281 tokens、740 decisions、2 笔 Paper 成交、0 开放仓位；Paper 现金/权益仍约 1,000.308706 美元。当前版本 Shadow 为 6 cohort/5 独立事件，Token Context 为 93 个独立 Token，成熟标签仍为 0，Phase 2 不可启动。
- 当日 Agent 配额没有新的 daily call/token 阻塞，现有有限大预算足够；本轮未继续盲目提高。API 11 个端点约 1.85 MB 响应中未命中任何当前配置秘密值；只出现“不接受密码/secret 不可导出”等布尔策略字段。
- 浏览器实测 Sources 的中英文切换、预注册实验说明、Paper/Live 状态与动态数据；桌面和 390×844 均无页面级横向溢出，宽表只在自身容器滚动，控制台 0 error。公网入口未鉴权 401、正确鉴权 200。
- “热点信息可能先于价格”保留为已实现的并行调查入口：精确高影响力原帖或新鲜高热事件关系可在低 momentum 时触发 Token Context，但只触发调查，不绕过时间、身份、独立来源、CA、安全、流动性和既有决策门。

### 注意力实验首个真实前向样本（06:44Z）

- 在确认没有 memeTrader Agent 子进程占用槽位后，执行一次受控 `Trend Scout --force` 验收；它不是第二个 Runtime，也不改变 Paper 或交易策略。
- 平衡区块首次随机分配到 challenger：X `@OpenAI`。实际观察清单共 12 个账号，只包含实验侧 `@OpenAI`，不包含对照侧 `@AnthropicAI`；4 个 critical 槽位保持固定，5/12 个槽位为普通轮换探索，满足至少 40% 探索约束。
- Spark/low 调用成功并记录 36,984 tokens，返回 0 个合格事件。实验账本如实保存 `assigned=1 / completed=1 / zero-yield=1 / agent-error=0`，Trend run 与 observation ingestion 均为 completed；没有事件，因此没有需要伪造或等待的 60 分钟市场结果。
- Agent 内部调用 ID 与 Trend lane run ID 保持不同用途；assignment、账号 exposure 与入库终态均使用同一个 Trend lane run ID 精确连接。实际学习倍率仍为 `1.00×`，Stage 1 继续 collecting。

### Information-first 独立前向 cohort 发布

- `information-first-shadow/v1` 已实现并部署。Runtime 只在最终决策持久化后冻结 `event + token` 首个样本，信息可用时间取合格 lead 的观察/入库时间与关系可用时间中的最晚者；精确 CA 与语义/Agent 映射使用不同关系时间边界。
- 信号时点缺少合规价格基线仍保留 denominator cohort，标为 `baseline_missing_at_signal_available`，绝不用后来快照补填；15/60/240 分钟结果为追加式 observed/missing 终态。
- 市值、5 分钟成交量和成交笔数仅形成 `low_observed_market_activity / observed_market_activity / insufficient_market_data` 描述层。“低活动”不表示“未定价”。unique buyers、image similarity、holder clusters 仍明确不可用。
- Sources API 与中英文 Web 已新增独立面板，`affects=none`；它不改变 Agent 调度、证据角色、候选、安全门、仓位、Paper 或 Live。部署时尚无上线后最终决策样本，页面如实显示 `not_observed / 0 cohorts`，历史未回填。
- 全套 186 个 Python 测试、编译、前端语法和差异检查通过；`doctor --online` 为 0 warning / 0 error。Paper Runtime、8787 本机 Web、8788 受保护 Web 均为单 listener，SQLite quick check 正常，Live 继续 locked。
- 浏览器实测空状态中文/英文、Live 锁、桌面与 390×844 响应式；移动端没有页面级横向溢出，宽表只在自身容器滚动，控制台 0 error。
