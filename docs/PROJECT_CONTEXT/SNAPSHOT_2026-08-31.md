# 项目状态快照 — 2026-08-31

本快照响应用户对整段聊天的再次全量复核要求。完整逐项状态仍见 `REQUIREMENTS_ACCEPTANCE_2026-08-30.md`；本文件记录其后的真实增量、首笔 Paper 交易审计和下一阶段边界。运行数字会继续变化，以当前 API、配置和 SQLite 为准。

## 1. 目标没有被后续补充覆盖

长期目标仍是：在个人电脑上前向采集新闻、社交、名人原帖、新 Token/池和链上状态，学习哪些来源、平台、人物与主题更值得优先观察，通过可审计的 WAIT/CANDIDATE、Paper 成交和固定时点随访改善样本外、扣除成本后的风险调整表现。

已实现的网站、采集器、Agent、Dex 链接溯源、双向证据链、Paper、双语动态 UI、公开鉴权入口、Settings、审计和项目记忆继续保留。新增需求默认追加，只有明确取消或与安全不变量冲突时才可跳过。

## 2. 状态分类

### 已实现并保持

- Overview、Events、Tokens、Decisions、Paper、Wallet、Agents、Sources、Audit、Settings 的真实 SQLite/Runtime 控制台；中英文、响应式和低成本自动刷新。
- RSS/Google News/Mastodon、PumpPortal、GeckoTerminal、DexScreener 和安全服务；Dex 附带链接严格区分 identity/promotion。
- Trend Scout、Source Discovery、Token Context 三类受预算 Agent，任务级模型/推理路由和按模型/强度 token 账本；物理并发不超过 2。
- `feature/confirmation/identity/promotion`、未来数据拒绝、r5 错误案例隔离、r6 stale 证据审计、WAIT 中性语义。
- Paper 账户曲线、固定不利滑点、双边手续费、已知 Token 税、执行尝试、新鲜报价门、持仓监督和历史成交；所有金额明确为模拟。
- 公开 URL 使用 loopback origin 与鉴权隧道；API 不返回 secret；网页不能开启 Live。

### 部分实现或等待真实前向样本

- X 等平台的登录状态不等于实际采集。当前关键断点仍是专用 Chrome/Edge 扩展对精确账号页产生真实 heartbeat、账号暴露和原帖 Observation；Codex 侧栏页面不会自动进入该桥。
- 选择性学习、主题通道、账号暴露、Shadow 与 Token Context 随访机制已实现，但样本量、日期和亏损多样性都未达到成熟门，不能声称已学出最优来源、人物或主题。
- Token Context 旧调用没有当前 assessment 记录；首笔 Paper token 也没有 assessment。当前 0 follow-up 是 0 assessment 的必然结果，并非 finalizer 漏跑。新 `token-context-admission/v1` 已开始前向持久化准入、预算和冷却原因；旧调用仍不回填。
- Shadow 表中现有 5 个 cohort 全是旧 `v1` WAIT；唯一真实 CANDIDATE 没有 cohort。当前源码已是 `v2-event-action`，新 `shadow-event-admission/v1` 会把它如实计为历史/未受监测候选，绝不补造原因或 cohort；新版本第一个真实 CANDIDATE cohort 仍需前向运行产生。
- Agent 已按任务复杂度分层，进程/额度/结构无效会回退；基于事实冲突或证据复杂度的通用质量升级仍未实现。

### 外部人工步骤

- 精确社交平台采集需要用户在专用浏览器加载扩展、配置本机 Bridge Token、完成必要登录并保持公开账号页打开；MFA、验证码、CAPTCHA、手机号和条款确认必须由用户处理。
- Devnet 钱包仍需少量测试 SOL 才能取得可公开核验的 signature。没有 signature 不能声称真链测试成功。
- 临时 Cloudflare Quick Tunnel 不是固定域名；固定 URL 需要用户自有 Tunnel/Access。

### 未实现且当前禁止

- Mainnet Broker、私钥签名、广播/确认、路由费与链费、失败重试、防重复、余额对账、紧急停机和小额真实成交验收均未完成。`live.enabled=false`，网页永远不可切换；用户曾粘贴的私钥视为暴露，不能使用或保存。
- Telegram 仍是人工目录和发现备用，不自动抓取、索引正文、转发到 Agent 或作为独立交易证据。
- 不自动注册、关注、点赞、发帖、私信、读取密码/Cookie/Session/验证码或规避平台限制。

## 3. 首笔 r6 Paper 交易的真实审计

- Event 393：NASA 新空间望远镜事件；Token 为 Solana `Roman Telescope (ROMAN)`。
- 2026-08-30 17:48:51Z 形成唯一 CANDIDATE，score 81.06、match 61.18、canonical margin 32.11、外部来源 6、流动性约 111,484 美元、5m 量约 18,335 美元。
- BUY 使用当时重新取得的 DexScreener 报价，执行价包含 2% 不利滑点和 60 bps 手续费；约 11.45 美元 Paper gross。
- 约五分钟后流动性快照突降至 0.11 美元，触发 `liquidity_emergency` SELL；卖出同样使用新鲜报价、2% 不利滑点和 60 bps 手续费。
- 完全平仓后费后 Paper PNL 为约 +0.308706 美元。它是真实前向模拟闭环，不是 Mainnet 利润，也不是足够的策略表现样本。
- 没有发现未来数据、推广榜单或普通词误匹配污染；但一个样本不能证明 NASA、France24、Guardian、RTHK、The Verge 或 Google News 有预测价值。

## 4. 本轮已证实并修复的学习语义

审计确认旧闭仓归因会在平仓时按 `event_id` 重新扫描开仓前来源。它虽然有三时间戳门，但交易记录没有 `decision_id/cohort_id`，同一事件出现多个 Token 或多轮决策时无法证明某个来源确实属于该笔成交；卖出税也没有从来源学习的净回报中扣除。

从 `paper-source-attribution/v2-decision-cohort` 起：

- 最终 CANDIDATE 的 `decision_id` 与 `create_shadow_event_cohort()` 返回的 admitted `cohort_id` 会同时写入 position、BUY 和每次 SELL。
- 完全平仓只读取该 cohort 在决策时冻结的最早合格 `feature/confirmation`，并写入同一 decision/cohort 的 `source_utility_outcomes`。
- 买入成本包括 gross+fee；卖出净额包括 gross-fee-known_tax，滑点由真实模拟成交价和数量体现。
- 每个完全平仓 round 都写 `paper_source_attribution_attempts`；缺 decision/cohort、主键不匹配、无合格来源或现金流无效均明确记录 skipped 原因。
- 原来 1 个事件时间窗闭仓结果迁移后标为 `legacy-event-window/v1`，保留审计但不进入学习，不推测或回填 decision/cohort。
- Sources 诊断分别展示 feature、confirmation、identity、promotion；只有 observed/ingested/published 都不晚于事件首个最终 CANDIDATE 的 feature/confirmation 才计入决策时合格率与候选前关联。

这修复的是归因可审计性，不是因果识别。新账本只能描述“被选中、成交并平仓”的条件样本，不能声称某个来源、人物、平台或题材导致收益；也不改变证据、候选、风险、仓位、退出或 Live。

## 5. 下一阶段按证据推进

1. 等待第一个新 CANDIDATE Paper round 完全平仓，验证真实数据库中的 `decision → cohort → fills → attributed close`；旧 ROMAN 交易保持 legacy，不回填。
2. 已实现 Token Context append-only admitted/skipped、预算/冷却原因账本；继续等待预算重置后的真实准入，绝不把 skip 当 `no_context`。
3. 已实现 Shadow cohort created/skipped 原因、历史/未监测分离与新 CANDIDATE 覆盖率；继续等待第一个真实新 CANDIDATE cohort，不回填旧 CANDIDATE。
4. 让专用浏览器桥产生第一个精确 X 账号暴露和原帖 Observation，完成账号学习链最前端。
5. 继续真实 Paper/Shadow/Token Context 前向运行；样本成熟前不放宽门槛、不自动改策略。
6. Devnet 有测试 SOL 时只做一次最小人工签名验证；Mainnet 继续锁定。

## 6. 本轮发布验证

- 全量 Python：167 tests passed。
- `compileall`、前端与浏览器扩展 JavaScript 语法、详情请求竞态测试、`git diff --check`：通过。
- `doctor --online`：SQLite、Live lock、Bridge、Codex、DexScreener、GeckoTerminal、安全服务和必需 RSS 全部通过，0 warning / 0 error。
- 活动 r6 迁移后，Sources API 显示精确闭仓 outcome 0、旧事件时间窗 outcome 1、精确归因尝试 0、成熟标签 0；证明旧 ROMAN 交易被保留但没有伪造成新 decision/cohort 样本。
- 验收时新 Token Context 准入账本已有 8 条真实前向检查：包含 `no_eligible_trigger` 与 `daily_token_reserve_exceeded`，0 条伪造 Agent 结果；Shadow 准入账本尚无新 decision，唯一旧 CANDIDATE 明确显示为历史/未受监测。
- 本机 Web、鉴权 origin 与 Bridge 继续只监听 `127.0.0.1`；公开入口未鉴权 401、正确鉴权 200。
- 13 个本机 API 响应约 606 KB 做了当前配置秘密值扫描，0 命中；Live 仍为 `enabled=false / locked=true / available=false`。
- 浏览器实测 Sources 的精确 cohort 学习说明、F/C/I/P 角色分栏、中文/英文和 390×844 响应式；390px 下 body/document 宽 375、表格容器 350/1653px，页面无整体横向溢出，宽表只在自己的容器内横向滚动；控制台 0 error。
