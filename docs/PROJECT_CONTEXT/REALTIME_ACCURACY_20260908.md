# 后台优先：稳定性、准确性、实时性优化

用户连续要求：在允许范围越快越好；检查计算、特征、判别、结果指标；修复原池行情过期；资源优先后台。此前资金期与策略退休决定保持。

## 已改变的实际问题

1. 新币补全的15秒任务遇持仓忙直接返回，实际数轮空转。改为等待持仓通道空闲后继续，保留共享退避及每轮30目标上限，不额外扩大默认180目标/90秒规模。
2. 存在待执行BUY时，资金预留查询可能从整个历史决策版本驱动JOIN。新增 `(definition_version,shadow_cohort_id,status,arm_id)` 索引并从pending intents CROSS JOIN精确查cohort，不改参与账户/现金预留公式。
3. 形态候选查询每轮按token分组历史evaluation且做JSON过滤。新增与过滤匹配的partial索引，保留最新记录、候选排序、数量及原池规则。生产只读同函数单次耗时从10.5276秒降为1.1355秒，各返回30目标；不同采样时间，不是严格同负载因果基准。
4. 原池A已取得有效Dex行情，却等待同批B的Gecko/Demo fallback结束再写入，可能过期。现每个provider成功后立即更新已成功池并检查退出，未覆盖项继续补查；单轮去重成功写入，不重新标记旧数据为fresh。
5. 前台初始 `/api/live` 和策略合同表会并发重复计算同份cold compact state。改为先实时状态、后合同表复用cache。隐藏页面暂停实时轮询，切回立即刷新；可见页面仍5秒。性能API及系统页说明同步。

## 准确性复核

特征审查核实pattern从raw.pairs按锚定pair取数，cohort以(token,pair)隔离状态/信号，无当前可复现跨池混帧错误。没有据“original_pool”名称错误地施加“只许Gecko首池”门槛。

对strategy230/resource_profit_structure_candidate_v1独立读取72笔交易、36终结仓、824全期快照：cash977.0844463281786，PNL−22.91555367182138，expectancy−0.636543157550594，PF0.5428980812743203，胜率30.5555556%，DD26.14161774133629均匹配API；逐仓回款−成本−PNL最大差0。范围是一个完整终结账户，不声称核验所有历史/全部开放仓。

## 验证与部署

16个不同定向Python用例通过：补全等待1、flat2、资金预留1、原池路由及fallback10、新early-apply1、Web诊断1。新增预留测试读取生产方法内的真实SQL，覆盖pending/filled/SELL、admitted/rejected、跨version；EXPLAIN确认cohort精确索引。JS语法、既有策略展示测试及隐藏页零fetch/零重排计时测试通过。

索引与等待先于2026-09-07T16:35:47Z重启Paper；首次建索引引入启动等待，约16:36:44恢复进度。最终含原池/前台补充改动于16:43:49Z按原launcher重启Paper及8790 Web，未碰8787/8788。注册、追加合同、activation摘要保持一致，Live锁定，无初始化。

16:44:36Z短窗：health running；补全实际间隔p50=15.0038秒；flat实际5.0578秒（此前p50约11.17秒）；主策略1.0116秒；持仓1.1577秒/p95=2.0161秒。新增BUY1、SELL11，仅证明自然运行接线，不证明盈利或长期速度稳定。

## 原池过期的剩余边界

实际案例RH `0x537eb9003797323550a9c730dd4f02167d538e78` 原池 `0x91d0a52786cf75286f93181a55a187f2e5a172c7` 有主源coverage gap；SOL `FsmotAayZVCuYEGrErmaLMmcEXpFXUrtehvz7LTGpump` 原池 `GX9GtAQrrWezCMGMVyaLEgSXGSbdav77vhok9U6FL36W` 及 `FjY1upxoYdt5r4mtcuDH8zutkmcoRy9gcBbgiQSmpump` 原池 `F2SEgEryBRKvYcBB28dka64BcAFsrfQq9a8cBRp5TUbg` 主源缺流动性。UI与后端pool marks身份和15秒过期口径一致，不是错误join。

最终采样RH最老30.0456秒、SOL最老19.5324秒，仍有coverage/缺字段。已修复本地“成功结果被其他请求拖延”，尚未消除所有源端缺失、缓存代际及额度约束。保留告警、真实观察时间和缺失状态；不以改阈值、换池代替原池、旧liq配新价或伪造fresh消除提示。

原始短窗与性能工件在忽略目录data/research/speed_next_baseline.json、flat_query_before_index.json、flat_query_after_index.json、realtime_accuracy_after.json。没有新增Agent后台任务、定时研究或自动策略扩张。

## 用户要求回顾与追加核验

已覆盖本轮连续要求：频率/实际速度、计算与判别、特征准确性、结果指标、原池过期、后台资源优先。交易量曲线与启动下降诊断仍保留在data/research/trade_volume_20260908/；没有以提高交易笔数作为工程验收条件。旧新策略扩张和初始化授权不因此恢复。

追加源端实测（约16:48Z，使用现有HttpClient与适配器）：RH原池0x91d0...精确Dex查询返回空字典；上述两个SOL原池Dex精确查询有价格但liquidity=None。Gecko同批返回两池流动性2303.9309与2378.7658 USD，响应Cache-Control明确`max-age=30, public, must-revalidate, s-maxage=60`，本机收件16:48:17.766496Z。这直接证实15秒有效期与当前补源缓存存在空窗，非本地提示写错。

针对两SOL例，SQLite最新原始pair为dexId=pumpfun、quote mint=So11111111111111111111111111111111111111112。已有PumpSwap vault观察器不能直接当成这两个池的完整实时USD行情源；现有WSOL参考还受30秒内复用及共享请求预算约束。未新增未经核验的储备定价adapter、拼接旧流动性、绕过缓存代际或自动购买API。

追加计算/消费者复核：entry_batch短窗p50/p95约0.0111/0.0171秒，净现金流已每批一次，未冒险加TTL资金缓存；cohort和sealed风险模型仍有有效策略消费者，旧ranker被替代后已早返回。未为省一个小字典或两次小查询新增缓存层，未删除有用的前向观察。

剩余未解决项明确为：部分原池目前免费源没有持续、完整、15秒内的价格与流动性。解决它需要满足当前身份/时间/估值合同的实时数据来源；加快本地循环不能单独补出不存在的数据。现有补查继续运行，失败与过期如实保留，不能宣称所有问题已清零。

## 第二轮：缩短补全与判别等待（2026-09-08）

用户继续要求允许范围内尽可能快。本轮基线：补全间隔中位15.007秒、耗时4.651秒；策略间隔1.011秒；持仓刷新1.191秒、退出应用耗时0.0178秒。到期补全选择SQL只读单次0.073秒，未新增字段或索引。持仓已按链并发、成功批次即退出，未重复建设请求通道。

本轮改动：补全拆为每5秒最多10个目标，仍最多180目标/90秒；新行情落库后唤醒策略判断，最多每0.2秒启动一次，没有新数据时维持1秒回退。唤醒合并且沿用原工作时间预算、下一观察执行与账户约束，不新增外部请求。分拆补全可能增加小批HTTP请求次数，但仍服从共享250毫秒主机节流、429冷却和持仓优先。

修复两处真实重复工作：pattern observer及narrative路径调用共享报价后，不再第二次把同批行情加入容量8的passive-cohort队列。保留历史非chain-only模式原路径。已有watch item本来就原地更新报价，本轮未将其误判为“等待下一轮才计算”的问题。

9个定向Python用例通过，验证补全分批/预算/持仓与退避、唤醒合并和空闲频率、两个分析路径单次入队及新帧计算、持仓报价复用、周期任务互不阻塞。JS语法通过。Paper于2026-09-07T17:31:27.9533391Z按原脚本局部重启；旧账户、资金期、策略及历史保持。

部署后短窗（2026-09-07T17:33:38.434142+00:00）：

| 环节 | 部署前中位 | 部署后中位 | 说明 |
|---|---:|---:|---|
| 补全启动间隔 | 15.007秒 | 5.010秒 | 后窗24轮，每轮最多10个目标 |
| 补全每轮耗时 | 4.651秒 | 2.898秒 | 每轮工作量已缩小，不是相同批量的吞吐倍数 |
| 策略判断间隔 | 1.011秒 | 0.307秒 | 有行情唤醒；后窗P95约1.006秒 |
| 持仓刷新间隔 | 1.191秒 | 1.308秒 | P95为1.954→2.036秒，持仓Token为11→15，非等负载 |
| 持仓应用/退出耗时 | 0.0178秒 | 0.0195秒 | 时间预算、资金与下一观察约束保持 |

两窗口负载和市场对象不同，不能归因全部差异或保证长期速度。cohort整体耗时未证实下降（后窗中位0.813秒、P95约4.488秒），本轮可确认的是同一调用路径从重复入队变为单次；并未声称整体分析翻倍提速。SOL仍有1个缺失、2个失败计数，最大完整观察年龄约43.8秒；BSC和Robinhood该快照无失败。免费源覆盖空窗仍未消除。

health运行正常，注册/V6激活/资金激活三类表摘要和行数一致；部署后25 BUY、19 SELL继续自然产生，Live锁定。详见忽略目录 `data/research/speed_cycle2_before_20260908.json`、`speed_cycle2_after_20260908.json`。未获得全体新币端到端等待分位数：额外只读历史排序查询达到2秒预算后中止，未加报告专用索引或继续全表扫描。


## 第三轮：发现覆盖、队列吞吐与迁移交接（2026-09-08）

用户追加要求核查“每分钟发现很多，却只持有9币”，并实现更多、更全、更快的发现与处理。

### 9币的真实含义与当下流失

截图55仓/9币是有效开放仓位数与去重Token数；同一Token可由多个独立策略持有。不是累计成交币数，也不是右侧再活跃曲线的买入计数。最近10分钟的真实成交表尾部核验（截至17:59:44Z，当前资金期、最新5000 id范围）有32 BUY、8个Token、21个策略arm，以及8 SELL。不能用order_intents子路径的0记录推出全系统没有BUY。

最新1000条评估尾样本为984 rejected、16 admitted，属于评估记录而非独立机会分母。其中pattern/cohort观察帧占大多数；150条为候选所属entry家族的现金/配对资格阻断，80条原池流动性未知、29条低于门槛、5条as-of行情无效。现金原因不能泛化为120个账户都不足，也可能包含features.paired_rejections。代码按当前期、候选家族、各臂独立余额和前向激活时点判断；未发现其它账户亏损否决有资金独立策略的缺陷。最新评估792929使用snapshot1597019，入库17:58:48.726032Z→评估17:58:48.746892Z，约0.021秒；此样本不支持主策略消费积压。

UI修正：顶部“近5分钟X个”来自最新展示列表，并非完整发现总数，现明确写“最新列表中5分钟内X条（非发现总数）”；持仓写“X仓/Y币”并说明多策略可持同币。删除过时的“缺行情一分钟全损”说明，按现合同显示新鲜原池流动性低于有效门槛才核销，缺失/过期不核销。未改历史成交或资金规则。

### 已实现的五项改动

1. Dex发现列表先过滤已启用链和非空地址，再应用原数量上限。无关链/无地址条目不再占用本地名额；仍受每次响应及原限额约束。
2. 新增官方token_profiles/latest、community_takeovers/latest WebSocket，复用已有两路的连接、去重、收件时间、来源曝光、补全和退避逻辑。REST回退保留；四路资料流均已运行。身份/推广线索不直接触发BUY。
3. 原生发行observer已经把区块frontier前移，但Runtime只入库前8个事件。现在处理该observer最多200个已返回事件，每个事件让出事件循环并等待持仓空闲。RPC数量/30秒调度/区块窗口不变；不声称覆盖全链或超出200个事件的响应。
4. passive cohort从10秒消费最多8个自然批次改为2秒最多8批，缓冲8→16；实际三链输入约2.4批/秒，原消费上限0.8批/秒不足。各批仍分开、FIFO、保留收件及观察时间；每轮8身份/.25秒投影预算、30秒时效、KV60秒保存不变。没有增加行情请求或合并帧制造共现。
5. 修复旧mint收到新migration却排在持续新币流后：实际25cSWn…Cpump的新migration fact121244于16:24:21.511039Z落库，重新排队后逾93分钟仍无新attempt，且不在最多3币的pregrad watch中。现在在既有选择器有界读取最新500条launch事实，对30秒内收到、尚未首次尝试的migration占用原最多3个优先名额。首次尝试后失去该优先级，重放不重置正常退避；默认FIFO及普通旧币requeue仍保持新币优先。没有无差别改成按最后重排时间排序。

### 实测、验证与边界

两条新官方流的单次接口探测：token_profiles REST得到28个启用链身份、WS87个，多59个；CTO WS相比该次REST多75个身份。这是接口返回覆盖差异，不是59/75个新发行或系统首次发现的币。部署到18:00:13Z，两条新增流都产生真实完成曝光，但first_local计数均0；它们的初始身份当时已被其它路径发现。不能据此宣称新增流已经贡献交易alpha或保证发现量增长。官方文档未给固定推送频率/连接额度：[DexScreener WebSockets](https://docs.dexscreener.com/api/websockets)。

15个不同定向Python用例通过，覆盖四流格式/角色、过滤后上限、既有去重和429、200原生事件、16批FIFO溢出、周期调度、无watch旧mint的新migration单次优先、普通FIFO/新币优先/社交优先。JS语法通过。生产只读运行实际补全选择函数，10目标用时0.0587秒，未新增数据库索引。

首批于17:56:42Z部署；迁移优先于18:03:20Z部署；最终含同连接锁保护的代码于18:04:57Z按原Paper脚本生效。8790 Web于首批同步，静态说明即时更新。保留原资金期与所有注册、激活摘要，Live锁定，没有初始化、新策略或定时Agent。

首批部署后截至18:00:13Z：cohort实际间隔中位10.013→2.012秒；策略0.274→0.302秒；持仓1.269→1.333秒，P95为2.143→2.203秒；持仓币11→13，非相同负载。退出应用中位约0.0207秒。补全仍约5.020秒一轮；本轮没有证明外部网络请求本身加速。新增13 BUY/5币、2 SELL/1币仅证明自然接线。17:59:57与18:04:33两个保存点的累计cohort溢出均56546批/460830帧，后段记录未增加；窗口中有局部重启，不能推论以后零丢弃。

迁移优先首次部署后的3个自然样本：新migration落库到补全结果约3.37、4.84、5.31秒，两个hydrated、一个no_pair。发现更快并不保证上游已经收录。SOL原池仍有源失败记录；缺池/缺字段/上游缓存及额度边界仍保留，不放宽原池15秒时效、流动性门槛、现金或配对合同来制造更多交易。

本轮原始工件位于data/research/coverage_cycle3_*.json、coverage_stream_probe_20260908.json、migration_priority_*.json、coverage_final_*.json（均不提交）。


最终验收截至18:07:34Z：health/API正常；四路推送均有真实成功收件；注册25条、V6激活20条、资金激活1条的摘要与本轮修改前完全相同。最后重启后有7 BUY/2币、9 SELL/3币。当前有效73仓/12币，Live锁定；cohort间隔中位2.013秒（71轮）、补全5.010秒（30轮）、策略0.272秒、持仓1.351秒（P95 2.027）、退出应用0.0205秒。各受影响周期无异常计数。8790实际/app.js返回新说明；首次误查/static/app.js得到404，已按index.html中的真实路径核验，没有将错误探测计为PASS。未进行浏览器视觉回归。详细快照为data/research/coverage_final_acceptance_20260908.json。
