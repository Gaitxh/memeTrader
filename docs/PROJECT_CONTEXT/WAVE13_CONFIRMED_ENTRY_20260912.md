# 2026-09-12 全面审查第 6 轮：交易量前提被证伪 + 存活带样本外验证 + 确认式入场（wave 13）

## 1. 「交易量与开仓数量明显偏少」在本轮被数据证伪

| 指标（最近 60 分钟） | 数值 |
|---|---|
| 开仓笔数 | **1,004** |
| 准入决策 | 2,017 |
| 有实时账户快照的臂 | 366 条，其中现金 < 2U 的仅 **2 条** |
| 现金中位（各臂最新快照） | 943.50U（p25 122.06U，最大 1,634.96U） |
| 持仓分布 | 43 条臂持仓（24 条正好 2 仓上限、18 条 1 仓、1 条 3 仓） |
| 已评估代币（60 分钟） | 46,994 个（占发现数 95.3%） |

→ **资金与容量都不是约束**：绝大多数臂闲置且现金充足，系统每小时开约 1,000 笔仓。
配合第 1 轮修掉的两个真实缺陷（家族 depth 取值使 43 条标定向量仅 1 条可触发、信号循环嵌套使 trajectory144/145 每周期只处理一个 identity），可以确认："交易少"是**修复前**的现象，当前瓶颈已不在数据流或资金，而在**策略选择性与退出经济学**。

### 拒绝原因分解（30 万行有界样本）
| 占比 | 原因 |
|---|---|
| 34.3% | `pattern_observation`（纯观察行，不是入场尝试） |
| 22.3% | `no_active_matching_entry_policy`（无匹配的入场策略＝设计上的选择性） |
| 21.0% | `cohort_observation`（纯观察行） |
| **13.1%** | `entry_pool_liquidity_unknown`（池深未知＝数据覆盖缺口） |
| 5.4% | `entry_pool_liquidity_below_configured_floor`（低于 1000U 底线＝设计） |
| 3.9% | `invalid_exact_asof_market_snapshot`（快照时序不合法） |
| 0.0% | 冷却/已入组（23 行） |

即：约 **55% 是观察行、28% 是设计性拒绝**，真正与数据质量相关的是 13.1% + 3.9% ≈ 17%（池深未知/快照时序）。这两类**不允许用零或别的池替代**（项目安全规则），因此属于需要监控的覆盖缺口，而不是可以"修"的逻辑缺陷。

## 2. 存活带假设的样本外验证（把 41,254 笔已平仓按时间对半切）

| FDV/池深 | 前半段写销率 | 后半段写销率 | 前半段均值 | 后半段均值 |
|---|---|---|---|---|
| <1 | **50.7%** | **40.4%** | −4.746 | −4.314 |
| 1–2 | 15.4% | 11.0% | −2.132 | −0.738 |
| 2–5 | 4.1% | 7.2% | −2.657 | −1.780 |
| **5–20** | **1.3%** | **2.0%** | −1.358 | −1.362 |
| >20 | 5.2% | 3.2% | −1.946 | **−0.358** |

→ 分带排序与量级**在两半段一致**，说明 wave 10 的设计依据不是单期偶然。
→ **但必须诚实指出**：即使在最好的分带里，**单笔均值仍为负**（最好仅 −0.358U）。存活带能"少死"，不能单独创造正期望——剩余缺口在退出与入场时机。

## 3. 前向行为验证：23–25 秒止损（用户观察的秒级证据）

对新增臂与其对照臂的实盘退出计时：

| 臂 | 首次硬止损距入场 |
|---|---|
| `alpha149_df_price_up_liquidity_up_v1`（单帧入场·原退出，对照） | **23 秒** |
| 同上（第二次） | **25 秒** |
| `alpha149_merged_multi_setup_fast_v1`（快出对照） | **25 秒** |
| `alpha149_survive_noise_confirm_v1`（宽限 60s + 连续 2 帧） | **188 秒**（未在其 60 秒宽限内触发 ✓） |
| `alpha149_survive_noise_wide_v1`（宽限 180s + 连续 2 帧） | 0 次止损（1 次追踪、1 次写销） |

家族层面：**37 次硬止损中 28 次（76%）发生在入场后 60 秒内**。
→ 这既是"被正常振幅洗出"的秒级证据，也证明**宽限/确认机制在实盘按设计生效**（未发现任何在声明的宽限期内触发的止损）。

## 4. 新增策略（wave 13，1 条全新 ID）

| arm_id | 入场 | 退出 |
|---|---|---|
| `alpha149_confirmed_survivable_v1` | **新机制 `confirmed_survivable`** = 存活带 **且** 本池已连续两个观测帧上行（成交因此**推迟一帧**，先确认再买入） | 实测最强短兑现合同（−20% / 追踪 30→15 / 30 分钟） |

设计依据就是上面 23–25 秒的证据：既然单帧入场买在回撤里、由噪音付费，那就**把确认放在买入之前**，而不是买入之后再容忍。这是对"买"侧的系统性探索，与 wave 8/9 的"卖"侧容忍形成互补对照。

- 注册：append-only，`policy_additions` 238 → **239**，账本/资金周期/激活前沿不变；21:01:33Z 加载。
- 单测 30 项 alpha149 + 2 项 whipsaw 全 PASS（新增：确认臂必须"存活带 + 连续两步"同时成立——只有一次上行、或只有两步但不在存活带内，都不得触发）。

## 5. 诚实边界与下一轮（7/256）

1. 拒绝原因样本取自 id 区间（30 万行），占比可代表近期但不能外推到全部历史。
2. `entry_pool_liquidity_unknown` 13.1% 是数据覆盖缺口；按安全规则不得用零或替代池修补，只能作为监控项。
3. 存活带最好分带单笔仍为负 → 下一步重点应放在**退出与入场时机**（wave 8–13 的 A/B 数据仍在积累，当前每组 1–10 笔）。
4. wave 13 的自然样本尚未产生，不作结论。
