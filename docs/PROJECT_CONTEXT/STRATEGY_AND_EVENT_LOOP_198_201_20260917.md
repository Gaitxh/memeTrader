# Paper strategy and event-loop continuation, 2026-09-17

## Scope and baseline

This continuation answers the user's request to implement strategy work alongside the shared data-path repair. It is Paper-only and manually reviewed; no Live order, schedule, funding reset, historical fill, address allow-list or new market API was introduced. The current source baseline is commits `cbe260d`, `314f1e8`, and `9809e00`; earlier strategy results remain in their original policy versions and funding period.

The current database has 125 user-supplied address lines, 94 distinct retrospective research keys in `data/research/goal_cases190/cases_20260916T090250Z.json`. The prior bounded case review matched 70 to current canonical records and left 24 without a defensible match. Of the matched cases, 44 had evaluation but no admission, four admission but no position, and 22 positions. These counts do not establish that all cases were tradable winners. The contemporary matched-control file covers only 13 selected tokens and 25 controls across 25 links; this is too sparse and selected to calibrate a profitable entry rule. Concrete case examples remain in `GOLD_CASE_REAUDIT_AND_DEPTH191_20260916.md` and `COHORT_TERMINALS_AND_STRATEGY_TAXONOMY194_20260916.md`.

The post-197 real funnel showed a natural Solana source BUY on 2026-09-16 14:51:12 UTC and a cost-accounted SELL at 15:06:56 UTC, gross proceeds 54.7581U on a 20U stake. Thus "zero ordinary order intents" was not "zero Paper trades": market projections live in `chain_meme_trader_trades`, not only `chain_meme_trader_v6_entry_fills`. Still, the frozen Dex trajectory cohort had 44,261 pending dispatches versus 40,013 expirations in the sampled record, and only two first-dip signals. Future studies must count source fills, distinct tokens and strategy-account positions separately. The next exact-pool frame remains the dominant observed conversion gap; nontrading cannot simply be labeled a market absence or an overstrict safety gate.

## Shared-path corrections

1. A live `py-spy` stack on Paper PID97276 located a synchronous research-only fixed-target outcome finalizer inside the event loop. Its old query scanned and sorted the growing outcome set; the pre-change five-sample component duration was p50 18.219s and p95 31.944s against a 30s interval. Commit `cbe260d` bounds the current tail and rotating older-ID sweep, uses primary-key ranges and preserves append-only outcomes. The two focused tests passed. On the first post-change process, the bounded CTE read measured about 0.35ms for tail and 0.68ms for sweep as isolated read-only query plans. The full component later reported 2.297s in one sample and 4.188s in a different single-sample process; these are not a comparable-load p95 or proof that all latency is solved.
2. A later live stack on PID94068 found synchronous `_record_creator_launch_risk_cohort_locked` work on every Pump create. `EXPLAIN QUERY PLAN` showed a primary-key range scan over up to 107,727 launch facts for the creator prior-history aggregate, and the all-history minimum also lacked a definition/time index. Commit `9809e00` adds covering `token_launch_facts_creator_asof_idx` and `token_launch_facts_definition_recorded_idx`; the first index now appears in the current DB plan and a no-match read averaged about 0.005ms across 100 local read-only calls. This is a narrow indexed-query measurement, not a whole event-loop latency claim. The focused index test and forward-immutability test passed on rerun; the latter failed once in a rapid-clock fixture and passed in an isolated reproduction, so timestamp-tie flakiness remains a test concern.
3. Dex timing still shows a material gap: on the first strategy deployment process, a three-sample frozen-followup window had two failures and p95 total 9.875s, while observer work took up to about 27.6s in a tiny sample. The event-loop fixes do not prove timely next-frame conversion. Held, followup and background lanes must continue to be compared under natural comparable load. No stale or wrong-pool mark has been promoted into a fill to hide this problem.

## Implemented strategy trials

Commit `314f1e8` adds three independent, post-frontier Paper arms and one correction to the existing depth tracker. All use already shared Dex/Gecko observations, unchanged global original-pool floor, shared safety and independent next-observed execution. Focused existing/new tests passed: 18 tests for old-pool tempo, depth crossing and fresh-stop routing; an additional next-quote depth entry check passed. These are engineering checks, not profitable forward outcomes.

| Arm | Distinct hypothesis | Existing control | Cost and failure boundary |
| --- | --- | --- | --- |
| `trajectory198_solana_confirmed_stop_v1` | Same Solana runner entry and existing trailing/horizon. Ordinary -20% economic stop requires two distinct fresh original-pool observations; fresh <=-35% loss triggers immediately. State persists through restart. | `trajectory187_solana_runner_v1` | No extra API; a delayed second frame can enlarge a loss. Liquidity/writeoff and later-frame settlement remain unchanged. |
| `depth199_first_floor_cross_v1` | A young original pool first observed below the configured execution floor is considered on its first same-provider, same-pool move to that floor, with positive price, at least two buys and a real sell. It tests an earlier tradable-depth entry, not a lower shared floor. | `depth191_first_tradable_v1` (requires >=2x floor, minimum 2000U) | 20U Paper stake, max eight positions, 30m hold. At the 1000U floor this has materially less depth and may lose the full stake. Fresh safety and next-frame fill remain required. |
| `activity200_old_pool_tempo_fast5_v1` | Exactly the existing old-pool five-minute activity-acceleration signal, but a 5m maximum hold rather than 30m. | `activity193_old_pool_tempo_v1` | 20U Paper stake, inherited risk and floor. Eight-percent modeled round-trip adverse slippage can dominate a modest short move. It cannot trade until the common next-frame bottleneck improves. |

The depth tracker previously deleted its low-pool state after a buy-only crossing quote despite rejecting the signal. It now retains that state while advancing the observation clock, so a later same-provider quote with a real sell can qualify; cross-provider jumps, absent fields, stale and future frames still fail. The comparison is not an assertion that all such crossings are profitable.

Independent strategy analysis rejected a blanket 90-second grace: existing confirmed-stop/steady exact-fill pairs covered 230 tokens and differed in 25 exits; the +45.27U paired total turns to -5.65U after removing its single best token. A Solana runner/fast exact-fill comparison covered 43 tokens and favored the runner by +252.22U, +39.40U after removing the top three, but remains historical observational evidence with selection and regime risk. Existing first-mark trailing versus armed runner covered 36 pairs with zero difference. No result is promoted as a verified general alpha.

## Activation and forward status

After `314f1e8`, the existing Paper supervisor loaded PID94068 at 16:38:40 UTC, with 502 registered arms, the same `chain-meme-trader/funding-20260906-v002-final-1000` period, a 1000U original-pool floor, 4% buy and 4% sell adverse slippage, 0U extra fee and Live locked. The three new policy additions all have activation snapshot 959127, recorded between 16:38:36 and 16:38:37 UTC. Commit `9809e00` was then loaded by the same supervisor in PID103272; `/health`, `/api/live?view=summary` and `/api/performance` returned HTTP 200, 502 policies remained and loaded source SHA-256 matched disk. No current/new arm had a natural position or trade in the initial readback. This is deployed and ready to observe, not a profitable result.

Manual falsifiers: require future same-source BUY pairs and cost-inclusive terminal outcomes; retain open/unvalued positions and full writeoffs. For the confirmed stop, compare changed independent-token exits and worst-decile loss against its parent, not total duplicate account count. For the floor crossing, measure same-provider eligible signals, next-frame fill rate, writeoffs and net outcome versus the deeper-entry arm; pause if thin-depth tails dominate. For the fast old-pool arm, compare paired 5m/30m net returns only after both actually enter and exit; no-fill is a data-path result, not a zero-return strategy trial. Review after roughly 20-30 changed independent-token pairs or a meaningful manual window, not by schedule. No automated deployment, parameter adjustment or retirement is enabled.

Next execution priority is to recover on-time exact-pool followup without extra requests or weakening point-in-time execution checks, then perform a comparable-load timing readback and real source BUY -> strategy position -> exit reconciliation. Rollback is by the scoped commits and new-entry pause; keep old accounts, positions, history and funding period intact.
