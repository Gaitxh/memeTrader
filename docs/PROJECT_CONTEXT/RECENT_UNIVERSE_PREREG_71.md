#71 preregistered bounded universe
As-of cutoff frozen at run start. Source=all local snapshot IDs<=frontier; no v6/cohort/watch selection. Stream positive price/liquidity>=1000 rows with valid observed<=ingested<=recorded<=cutoff, exact pool identity. Unit unique token+pool, first retained valid row ordered recorded/id; original token pool determined earliest availability across pools. Anchor dates Sep7 exploration,Sep8 holdout;Sep9 descriptor only. Later other pools separate successor cohorts, not original-pool outcomes. Entry first valid same-pool observation strictly after anchor recorded; no maximum gap invented, report delay. Future15/60/360min from entry availability; observed strictly after entry recorded, valid3 clocks/exact pool/floor. MFE observed thresholds100/400/900%;loss observed<=-50 separate overlapping label. Missing horizon sample within [h,h+120s] => censored endpoint; missing path=>UNKNOWN; observed maxima are lower bounds, not achievable exits. Features onlyanchor and entry: age-normalized5m activity (trades/min(age,300) if positiveknownage),buy count share,volume/liquidity,entrypricecontinuation,liquiditygrowth,same-source. Real buy-vs-sell dollar imbalance/wallet/native state UNKNOWN unless independently sourced. Broad bins training Sep7 medians fixed thereafter; no threshold grid. Candidate conjunction train high age-normalized activity and nonnegative short price/liquidity growth; holdout must enrich tail without equally enriched observedcollapse and adequate endpoint coverage. No registration if coverage/tail concentration inadequate. Match exact date/chain/provider,age bands<180/<900/<3600/older,liquidity.5x–2x,rank age/logliquidity/time distance before labels. Read budget180s/250000 qualifying rows; if exceeded, STOP incomplete universe, no precision/Alpha claims. No DB writes/new polling.
