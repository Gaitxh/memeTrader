# Failed impulse → cooling hold experiment
REPLY_TO: C2C-20260909-FAILED-IMPULSE-COOLING-103

Naming correction received after deployment: C2C-20260909-FAILED-IMPULSE-COOLING-103-NAME specifies `failed_impulse_cooling_reentry_v1`. This is the requested name of the SAME experiment already registered as `failed_impulse_cooling_v1` (addition292), not authorization for a second funded copy. Readback after correction: zero positions/entry decisions/claims, but registration and activation already exist. No supported arm-rename/alias migration was found. Preserve the frozen registered ID and use this explicit documentary mapping; the requested ID is not a runtime alias. Do not claim an actual registry rename or register a duplicate to repair spelling.

Implementation: 67f5189. New arm failed_impulse_cooling_v1, 5U/max4, copies the current effective resource_cooling_hold_candidate_v1 entry thresholds, exits and cost contract. No new market request or numeric tuning. Original cooling and core stay unchanged. The added entry condition is a latest same-token/exact-original-pool core position which is terminal and cumulatively negative, with its final SELL/WRITEOFF created at/after close and recorded strictly before this cooling decision. Missing recording evidence is ineligible; closed_at alone never suffices. A newer open/winning core prevents selecting an older loss.

The cooling signal is frozen and enters only on a strictly later valid original-pool frame through common safety. Its opportunity key includes token, exact pool, core definition/cohort and closing trade receipt; existing persistent cohort claim consumes it once, including asynchronous safety waits/rejection. A different core episode has a different key. This candidate uses core-close episodes instead of the parent's lifetime first-common-per-pool enrollment scope, while preserving the underlying cooling signal. It neither changes other reawakening strategies nor imposes a global token blacklist.

Seven new fixtures plus three existing common-claim tests passed: loss/writeoff receipt-before-signal, win/open/future/equal-time receipt and other pool, no same-frame fill, strict next-frame fill shared with original cooling, one claim despite additional frames, registration idempotency and unchanged original registration. Existing common-claim tests cover restart/race/reject. Query is signal-only and uses the existing token-history index across registered versions plus cohort PK; bounded production lookup measured 11.37ms (single observation, not p95). No schema/index addition.

The supplied three historical samples (+11.8281U in aggregate) are hypothesis-generating only, not independently validated profitability, not an allowlist and not a backfill. Natural sample and parent common-opportunity economics must be reported separately from implementation success. No promotion based on these three examples.

Deployment: Paper loaded around 2026-09-09T18:49:01Z, addition292; readback18:49:36Z has one registration/zero positions. All25 prior registration rows and291 prior policy-addition rows match predeployment exactly. Overall seven-table digest changes legitimately for the new prospective addition; this is not an unchanged-global-digest claim. Existing funding period/Live lock preserved. Held fetch p95 pre2.870s/post1.909s, apply35.62/35.64ms, pattern6.878/1.490s; startup and different sample windows are not sustained performance proof. Audit drops0. No historical trading writes or resets. Artifacts data/research/cooling103/{before_contracts,acceptance}.json and data/research/admission88/{pre_cooling103,post_cooling103}.json. Natural signal/fill/terminal acceptance remains pending, no forced opportunity.
