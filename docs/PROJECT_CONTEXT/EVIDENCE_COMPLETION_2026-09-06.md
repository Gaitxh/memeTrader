# 双篇独立方向与实际证据接线交付

事实截点：2026-09-05T21:27:19Z。两份来源及逐项状态：`TWO_INDEPENDENT_DISCUSSIONS_2026-09-06.md`。下文代码完成不代表自然盈利已验证。

## 已运行的前阶段

fc3c0f3已推送并在20:53:49Z受控重启，原资金期、旧策略与开放仓保留；180个策略。Direct LP实际5U两腿原池预检和时长竞争风险两项新臂在20:53:56Z、frontier917071部署。共享Recovery Shadow证据7893（20:55:11Z）、9066（21:13:28Z）包含完整真实报价，均quote-only，未被当作成交或现金。

duration模型固定部署cutoff20:53:56.231943Z；256个去重自然样本，15右删失、166普通亏损、34盈利、41核销。模型id beaa33402f1d5c63bf1b9894553953953a493354590738420e2ae9b85f2dd01a。失联删失保守情景独立展示；当前分组获利终局概率低而等待，不是已经证明新策略有效，也不是应该放宽底线。

查询性能缺陷的实际修复：同币历史买入覆盖索引，三次查询.138/.039/.017ms，部署前另三个币456/94/88ms；不同币、非配对性能试验，不能相除宣称统一倍数。20:55窗口：主判断61个间隔P95 2.73s/工作1.65s；held26个间隔4.77s；held_fetch104个P95 3.95s、apply .05s；pattern_token_compute120个P95 .04s。Web节拍与后台分开。

## 本阶段四个独立5U实验

| 新策略 | 真实输入与行为差异 | 验证与覆盖边界 |
| --- | --- | --- |
| official_event_actual_flow_v1 | 精确CA的第一方事件后，完整金额流窗口才可确认；时间、原池、真实净流、参与广度都满足，再下一市场帧买入 | 真实Store事件→资金→信号→5U BUY测试；原authoritative_event_shock不变；仅当前已支持官方源，不宣称互联网全部事件 |
| migration_amount_rate_absorption_v1 | 实际迁移事实/原池，迁移后价格flush/reclaim；相邻两窗真实SELL raw/秒下降、净流转正、广度恢复 | 6项纯测试+真实Store事实/4价格帧/两金额窗/15秒后5U BUY；修复同秒ISO精度合法事实被拒绝，专项通过；不把卖出笔数当金额速率 |
| early_observed_buyer_distribution_v1 | 部署后第一完整BUY窗口封存最多32个实际买家，不再扩充；其中买家真实派发和深度/广度恶化连续两窗触发下一原池帧退出 | 5项纯测试+真实Store封存→BUY→派发→pending→SELL；是first-observed群，不冒充mint初始分配，隐藏历史无证据 |
| common_funding_adjusted_breadth_5u_v1 | 复用已抓getTransaction中真实System/SPL资金转账，明确共同来源的买家金额聚类后计算广度/集中度 | 纯函数、collector及真实Store下一帧5U测试；零额外RPC、最多128条当前两窗边；只表示可观察转账关系，不表示同一控制人或完整资金图谱 |

新4独立账户1000U、单笔5U，双侧4%沿用，未修改父合同；普通旧策略规模保持。新策略只从部署frontier开始，不能补以前事件。注册重复幂等、旧父记录不变测试包含在实际Store路径中。

## 基础/UI修复

- 原池未在Token批量响应中出现：`DEX_SOURCE_COVERAGE_GAP`；确实出现而字段无效才`ENTRY_POOL_INVALID`。没有用别的池替代原持仓，也不把未知流动性当0。
- 共享Vault分支已有新帧却提前continue，漏了恢复心跳；现沿用10秒心跳上报，不增加RPC、不改退出逻辑。最小运行测试通过。
- 普通重复曝光改“再次发现”；尚无snapshot的币不显示“有行情”。真正再次活跃API在21:17Z最近60分钟为Sol1/BSC1/RH7。20秒图表刷新、120个有界10秒耗时桶，未扩大后台扫描。

## 原池待价与补款边界

WOFI cohort12462原池J8nq…XpS7曾被RH反复missing饿死，公平补采修复后原池20:56起重新更新。该旧arm最大持有时间实际240分钟，不是15分钟；21:02时约83分钟，没有退出意图属于实际旧规则。原池与另一个高价PumpSwap池绝不能混价。原池liquidity未知、成功原池价与批量漏报区分保留，不强制编造成交。

已核实BUY412305误核销损失补20U（20:16:31.231352Z），累计工程资本补款7538.807662700746U，source hash a29dd3e6fea26d2f3debe6dc5b2f333975ea6f117a27209fcc51e21958a45212。原交易和-20U PNL不改。cohort12154/12378/12462未有足够证据证明新增可补实际损失，不能用后来最高价计算假想利润。保留待核记录，禁止重复补款。

## 实际资源边界

免费精确交易观察最多6池、每轮最多2池，每池最多10条签名，5秒外层期限；热池截断不称完整，后续新完整窗才能交易。20:56–21:02的48个参与样本中22截断、18seed、7完整、1丢弃，仅1个完整双窗。全量早期holder、隐藏控制簇、未覆盖交易场所、完整付费逐笔流未凭空恢复。普通Sol/BSC/Robinhood市场策略照常运行，依赖精确PumpSwap输入的方向不能假称跨链全覆盖。

后台资源优先；增量证据解析复用现有RPC和窗口，等待不是放宽数据/身份/时序底线。新臂经济效果需要继续积累自然样本；不为名字数量或短时盈利作有效性结论。自动复盘不恢复、Live不启用。

## 待本阶段部署后的回执

7b66aa5已提交推送，21:31:37Z重启，四臂实际激活21:31:40.298067Z–21:31:40.371770Z/frontier927746。8790实际184、原资金期保留，stderr0。共享Vault21:32:29Z已恢复真实item心跳，旧Vault四个传输错误自动更新为fixed；没有把重复Jupiter错误抹掉。

## 额外确定故障：原池base/quote方向不同导致QQQ缺价

当前期cohort12728，Robinhood QQQ原池0xb04513d07d3483682421b776debca1aaf240dd410c7d61bfeb63881a19f2627a。不是legacy排除或池子消失。入场snapshot903699的原始GT证据为QQQ/BIRK，QQQ base USD720.220599993854；当前Dex同一池返回BIRK/QQQ，priceUsd为BIRK，原解析只取base而拒绝目标QQQ。

21:37的精确请求HTTP200、同池且liq9791.64；后一次仅为确认priceNative语义的精确响应为base USD.00001486/base-in-quote .00000002090，按同池两值换算quote USD约711.005。保留原响应，不与其他池或其他API互验，不把base价格拿作QQQ。quote方向买卖次数反向、base市值和项目资料不挪到quote；只有明确两端identity和正有限比率才支持。参考字段：[DexScreener官方API](https://docs.dexscreener.com/api/reference)。新增定向测试覆盖32byte pool ID、大小写、同池换算、次数、市值清空和缺比率拒绝。

另修首次pool_failure只有UPDATE导致零行、失败次数假0：首次建立UNKNOWN记录，last_success=NULL、missing计数0；重复失败不改变旧价/成功时间，不是撤池或0估值。原池补采成功现在回写来源恢复心跳。两项最小回归通过；待这项局部修复部署。不改入场历史，也没有发现当时入场价格身份错误。

当前Jupiter WSOL参考仍间歇出现协议不匹配拒绝；一次独立只读请求HTTP200、400bps、正输出、无transaction、真实route有效，只能说明该次恢复，不能宣称错误永久消除。系统继续保留这种拒绝而不放宽成交证据。[Jupiter官方order接口](https://developers.jup.ag/docs/api-reference/swap/order)允许无taker的quote-only，未签名/发送任何交易。

## 最终运行与交付回执（截至21:53Z）

4420950已推送并于21:45:35Z部署。184策略、原账期、四项21:31:40Z/frontier927746均保留。QQQ原池21:46:04.644052Z真实price711.0770801429302、liq9491.87，首次成功seq1；现已有原池估值。其规则为流动性<3000退出/最长240分钟，当前约105分钟且无价格止损，因此未生成退出意图不是故障。实际BUY413876净成本20U，数量.026701220752271368；按711.077×数量×.96=18.227159U，未实现约-1.772841U，不能把旧allocated_cost字段0当零成本，也不强制赔偿假想卖出收益。

API21:47:13.784Z：184策略、282开放仓/42去重币；primary held40币无缺价（其余为历史执行未决等统计边界，不混为40=42）。Sol数据年龄P50/P95 .809s、max12.492s；RH P50 1.682s/P95 2.596s。held_fetch最近120 P50/P95 1.275/2.280s，apply/exit .0165/.0492s；主判断76样本工作.254/1.233s、实际周期1.010/1.556s。与部署前120样本held P95 2.415s比较未见明显退化，窗口/负载不同，不能当正式恒定性能保证。

重启后有自然3 BUY/4 SELL，旧臂照常前向。新4已有128条金额流/7币、42条surface/7币、12个迁移事实、2份封存买家群（各1人）。3377次包含这四臂的评估中尚无ready或BUY；实际窗口不完整或无对应事件/迁移/群证据时等待，未制造交易。新策略需继续自然观察，尚未验证盈利。

错误监督：Vault四个旧传输告警随真实恢复心跳自动fixed；原池ConnectTimeout亦随成功恢复。手动仅依据同源后续成功，更新58/60/61/62（一次capital timeout、三链各一次429）。截至21:53共64 fixed、4 new；4个未决为历史两类metadata host错误和两组Jupiter协议拒绝，未称永久解决。最近一次旧Vault订阅拒绝重现后8秒恢复，不掩盖发生记录。

377笔独立资本补款合计7538.807662700746U，最新确认20U仍为BUY412305，没有重复赔付。QQQ/WOFI目前没有触发其规则且未闭仓，不能把已恢复估值后的浮亏或其他池后来上涨算作确证工程损失。旧cohort12154/12378等证据不足事项保留，原成交/PNL不改。

实际Chrome无头浏览器1440px总览验收：脚本错误0、documentWidth=1440无横向溢出，检索图与发现图同一y=522.75并排；1张耗时图、2张发现/再次活跃图。05:49最近窗口再次活跃Sol3/BSC0/RH8，BSC0是该窗值，不伪造非零。曲线26个真实后端桶，最近均值1.12s。截图仅在忽略目录data/tmp/overview-qa-w3H6O4/overview.png，不提交浏览器资料。既有CUA不可用后使用独立临时Chrome，不接触用户浏览器profile。

工作集：后台97MiB、Web135.6MiB；DB9709.8MiB、WAL364.1MiB，E盘空闲50.9GiB。未删除自然证据，未增加逐币RPC、未扩大队列/缓存、未恢复自动复盘；无Live。

本轮可运行机制、必要回归、部署、低优先级UI和状态项已交付。支持范围之外的原始持有人分配、隐藏协同控制、全池高频完整逐笔数据没有假称补齐；现有观察/额度及经济样本限制见上文。后续来源扩展与自然收益观察不能改写本轮已封存决策或强行补赢家。
