# Washout reclaim contemporary control, 2026-09-17

The first natural `alpha212_washout_reclaim_anchor_v1` position opened at `2026-09-16T18:41:05.831118Z` on `solana:7BaYAZBL5tbA9ZZFgeKxyHR9goE4ftbcSnvJrdNJpump`, cohort 36159. Its frozen pre-uptick anchor (observed 18:40:38.220792Z, signal observed 18:40:40.238898Z) was persisted on the open position. This proves an actual forward signal passed the regular BUY path; it does **not** prove the new exit improves returns. The source `alpha149_washout_reclaim_hold_v1` account was already `FAILED_ACCOUNT_DEPLETED` and paused, with 162 historical positions and -981.9172U realized at its older frontier. It could not supply a contemporary same-opportunity control. The initial 212 position must remain an unpaired observation.

To make the experiment interpretable without refunding or reactivating that depleted account, `alpha212_washout_reclaim_hold_control_v1` was appended at a **new** frontier. It aliases the same frozen 149 signal, has a separate 1000U Paper account and the parent 120-minute exit contract unchanged; the trial arm alone has the confirmed pre-uptick anchor-failure exit. Both retain current shared safety, cash, pool and next-observed fill rules and ordinary 20U effective entry size. The two arm IDs are a specific causal contrast, not two independent discoveries or evidence of alpha.

Four focused 212 tests now additionally verify two registered arms, distinct decision keys, and a same-cohort/same-source-entry-fill position pair when both are eligible. Six adjacent 198/211 tests also pass (10 total). Existing 212 activation, signal and position history were not changed. Control activation: `2026-09-16T18:45:11.857557Z`; worker PID100836 manifest 18:45:15.636522Z, 506 policies, matching `runtime.py`, `store.py`, `washout_reclaim212.py` source hashes. `/health` and `/api/live` reported running in the same funding period, Paper true and Live locked. At immediate readback the control had no natural position; the initial 212 position was still open. Only opportunities after **both** activation times may enter a paired analysis.

Next manual review must match by token, original pool, source decision key, source entry fill and activation frontier; compare realized net exit, open/unvalued state, writeoffs, maximum adverse excursion and later recovery. If one arm is rejected for safety/cash/cap, report the selection difference instead of inventing a paired zero-return trade. Neither profitability nor a reliable reduction in washout frequency has yet been established.

## Later natural readback (after the activation checks)

The first, unpaired 212 position closed at `2026-09-16T18:46:24.932028Z` for **+12.6213U net realized**, reason `confirmed_pre_reclaim_anchor_loss212:dex_mark_paper_fill`. This is one post-deployment natural result, not an estimate of expected return or a paired win. At the same readback, all seven distinct-token 209 positions had closed for **-41.2301U net realized** in aggregate. This supersedes earlier 209 partial-window totals only; the earlier cutoffs remain valid historical observations. Neither result justifies retrospectively changing an entry or refunding a loss.
