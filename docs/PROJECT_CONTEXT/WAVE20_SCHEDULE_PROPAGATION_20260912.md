# 2026-09-12 全面审查第 14 轮：把胜出的止损时点合同推广到其他入场族（wave 20）

## 1. 本轮起点：退出矩阵的分歧维持

| 合同 | 已平仓 | 合计 | 止损 | 写销 |
|---|---|---|---|---|
| 名义 −20% | 4 | −0.106U | 1 | 0 |
| 预补偿 −13.6% | 4 | −0.106U | 1 | 0 |
| 不用价格止损 | 4 | −0.106U | 1 | 0 |
| **仅前 5 分钟止损** | 6 | **+1.528U** | 1 | 0 |
| **随时间收紧** | 6 | **+1.528U** | 1 | 0 |

两种**时点**方案仍优于三种**深度**方案。样本仍小，但这是目前唯一持续存在的方向性差异，且与"止损越晚越贵"（越深随持仓时间翻倍）的实测一致。

## 2. 安全带供应的追查（未结案，但排除了一层）

本轮用 154,381 帧做"池龄 × FDV/深度"联合分布，结论**修正了我上一轮的表述**：

| 池龄 | <1 | 1–2 | 2–5 | 5–20 | >20 |
|---|---|---|---|---|---|
| 0–5m | 35.0% | **37.8%** | 21.1% | 1.6% | 4.5% |
| 5–15m | 40.4% | 21.4% | 20.8% | 5.2% | 12.2% |
| 15–30m | 32.3% | 16.1% | 23.9% | 9.3% | 18.4% |
| **30–180m** | 26.7% | **28.6%** | 12.4% | **10.9%** | **21.4%** |
| 3–6h | 44.8% | 31.3% | 12.8% | 3.7% | 7.4% |

→ 在 `survivable_core` 的池龄窗口内，**FDV/深度 1–20 占 51.9%**（1–2 28.6% + 2–5 12.4% + 5–20 10.9%），>20 仅 21.4%。也就是说"安全带区间在市场里并不稀缺"。引擎自身的 `fdv_liquidity` 分布（p10 1.16 / p50 7.11 / p90 48.45）也覆盖 1–20 约 60%。

**因此 wave 19 的"区间本身稀缺"解释也不完整**：真正造成 `survivable_core`（5 次）与 `survivable_band_deep`（75 次）15 倍差距的原因仍未定位，且两者的代码差异只有 FDV 区间与深度下限。已列为下一轮的**首要排查项**（用主通道帧回放逐条件计数，定位是哪一条条件在真实帧流上把核心带筛掉）。

## 3. 新增策略（wave 20，2 条全新 ID）

既然时点合同是当前唯一有证据的胜出方向，就把它推广到**其他入场族**，检验它是否只对合并入场有效：

| arm_id | 入场 | 退出 | 投注额 |
|---|---|---|---|
| `alpha149_time_decay_age_rate_v1` | `age_rate_acceleration`（当前供应最高的新机制，275/1030 帧） | **随时间收紧**（30% → 每分钟 0.6 点 → 下限 12%）、追踪 30/15、60 分钟 | 2U |
| `alpha149_early_stop_deep_v1` | `survivable_band_deep`（供应是安全带族 15 倍） | **仅前 5 分钟价格止损**，其后交给追踪/时限/流动性规则、60 分钟 | 1U |

两条臂都设 `hard_stop_return=-.90`（价格止损完全交给各自的时点方案），与 wave 15/16 的矩阵保持同一套合同定义。

- 注册：append-only，`policy_additions` 253 → **255**，账本/资金周期/激活前沿不变；22:33:04Z 加载。
- 单测 **44 项 alpha149 + 2 项 whipsaw 全 PASS**（新增：两条臂只改止损时点、追踪与持有期一致、父臂合同不变、两种时点方案沿用已注册的退出 kind）。

## 4. 其他状态
- 深池带无止损臂（断崖档 + 无止损）3 仓在持；宽带/安全带×加速/中段带各 1 仓在持。
- 修复后队列：**347 笔已平仓、0 写销、+12.59U**（窗口约 2 小时，跨越一次市场急转与一次回暖）。
- 错误 #265/#266 保持冻结；Live 锁定、资金周期/激活数/账本未变。

## 5. 诚实边界与下一轮（15/256）

1. wave 20 的两条臂是对**小样本差异**（6 笔 vs 4 笔）的推广，存在追逐噪音的风险；文档化其依据，后续以同入场 A/B 继续检验，不以单次结果调整。
2. 安全带供应之谜未解：已排除"深度下限"（wave 19）与"区间稀缺"（本轮），下一轮必须用**主通道帧回放逐条件计数**定位。
3. 下一轮：① 安全带供应的逐条件排查；② 矩阵与 wave 20 臂的样本累积；③ 若时点方案持续占优，考虑将其作为**新增族的标准合同**（仍以新臂形式）。
