# 基础审查修复与验收（2026-09-07）

本阶段用户授权“开始修复和验收”。修复代码 `03ace85`。不新增策略、不修改持久化策略规则、不重置账户、不改写历史交易/收益、不补款、不启用 Live、不恢复自动复盘。工程修复会改变原先错误的运行行为，不能宣称前后执行完全相同。

## 已完成修复

| 已证实问题 | 实际修复 | 定向证据与边界 |
|---|---|---|
| 老 observer BUY 只保证后帧晚于旧行情；SELL 的晚于实际触发观察条件仅对部分策略启用 | 所有这些入口统一要求新观察严格晚于实际决策/触发；缓存/迟到帧不覆盖待确认信号 | 三链真实 Store BUY/SELL；旧规则 JSON 不变，迟到帧仍等待，下一合格帧才成交 |
| DS TTL缓存重新解析会生成新的 observed_at | HttpClient缓存保留首次响应观察时间；search/quote/batch传递该时间 | 12项缓存路径测试；新请求可形成新观察，本地重放不能伪装刷新；不能推断上游实际市场时间 |
| 策略131/166在成交量NULL时TypeError | 缺量帧不满足需成交量的候选条件，控制组不误阻断 | 两策略实际Store缺输入WAIT、后续完整帧正常买入；未伪造volume=0 |
| 策略192回本锁利接线未传实际回款 | 以累计已实现回款是否覆盖stake派生cost_covered | 实际分批SELL不足/超过本金两例；不把浮盈或partial次数当回本 |
| 老L0退出原池EVM大小写不一致 | 只在适配层按链规范原池身份；SOL区分大小写不变 | BSC校验和地址在capital与revision两条退出路径实际触发/成交 |
| 策略190–193 MAIN配对声明未落实 | 决策和成交时均检查配对参与者资格/资金 | 两组×两时点资金竞争测试；非配对策略不受影响，孤立观察实验保留自己的候选/对照条件 |
| 互补队列队首所有源冷却仍占用本轮，后方可用源等待 | 选池前排除本轮无可用provider的项 | 三项离线真实Runtime测试；全冷却不请求、不刷attempt/deadline，其他可用链可推进；不增加配额 |
| performance持仓年龄用收件时间代替观察时间 | 年龄改取所需原池最旧observed_at，另列receipt_age_max_seconds | 观察12秒/收件2秒夹具分开；新旧age口径不能直接当性能改善 |

配对成交失败沿用既有 `skipped_cash_unavailable_at_fill` 枚举，可由另一配对账户缺现金导致；并非记录该行的账户一定余额不足。入场特征记录 `paired_group_not_all_eligible`。未扩展数据库表或迁移枚举。

## 测试

共164项相关测试通过，分开执行、未重复成功组：时序/旧cohort6，缓存12，策略接线9，路由与既有market API/runtime41，共享绩效/资金/修订/执行边界95，观察年龄1。另 `git diff --check` 通过。

此前审查10项旧测试失败中：2处报价夹具写错原池、7项激活fixture只冻结store而未冻结models时钟、1项runtime测试未冻结时间。修正夹具，不放宽生产身份/时序或删除断言。测试数据库全部在E盘，不对生产构造Store来做只读诊断。

## 部署边界

2026-09-07T05:14:03.4322224Z按现有Paper/Web脚本重启，原监督及其子进程经现场解析后停止。新Paper监督49848、Web监督47980（仅当时PID）。无初始化参数。

部署前只读边界：当前资金期 `chain-meme-trader/funding-20260906-v002-final-1000`；activation=2026-09-06T15:29:28.508111Z/frontier1130786；注册原文摘要fe1e47fbc4872998a1fb2b6cbe263950a36ba065fe1860b9493460ae19275388；97条addition完整行摘要2e73d30906f7c8698c73011edd2431013f75c482923ed448daecb84b628de635；最大交易477064、快照1474947。

工程修复的前后样本须分开解释，旧异常成交保留为历史证据，不能拿修复后规则重新解释成过去已经严格前向。

## 真实运行验收

05:15:07Z `/health` 为running；`/api/live`返回224策略，心跳年龄0.882秒、Paper-only、Live locked，双侧400bps/额外0U/原池1000U不变。样例旧账户余额15.460953U而非1000U，历史闭仓/核销仍存在；全量注册及97条addition原文SHA、资金activation逐项相同。05:15:59Z之前最大交易477064→477096、快照1474947→1475309，说明未停留于启动或旧缓存。四个新进程日志中两份stderr为0字节，但这不代表来源不再失败。

性能对照（旧进程最近120样本，对比重启后约2分钟；样本长度及持仓组成不同，不作因果提速结论）：

| 指标 | 部署前P95秒 | 部署后P95秒 |
|---|---:|---:|
| 主判断实际间隔 | 1.121 | 1.170 |
| 持仓行情实际间隔 | 1.842 | 2.076 |
| Pattern实际间隔 | 15.394 | 15.136 |
| 持仓fetch耗时 | 2.285 | 1.371 |
| 持仓apply/exit耗时 | 0.043 | 0.042 |
| 互补任务耗时 | 2.243 | 0.941 |

改动后未观察到明显本地计算回退；并不意味着1秒设定已经全部达标。SOL当时22个必需持仓Token的观察年龄最大35.459秒，RH1个7.390秒，来源失败状态仍存在。观察年龄新口径不与旧收件年龄直接比较。前后紧凑原件见 `REPAIR_DEPLOYMENT_BOUNDARY.json`。

更新历史的 `/api/updates` 已实际返回本轮条目；验收为API层，不冒称已人工逐页点击所有界面。自然交易独立只读抽验另附 `NATURAL_REPAIR_ACCEPTANCE.md`；任何本轮通过均不抹去旧异常trade473261/473262。

独立自然抽验固定32笔477065–477096（05:14:45.227–05:15:18.482Z）：30 BUY/2 SELL/0核销。30 BUY均为pattern路径，来自5个不同信号、7个cohort；新观察晚于实际信号决策13.047–27.555秒。两次SELL的观察分别晚于pending记录2.134975/.956906秒。一个完整闭仓的6.362811358净回款−5成本=1.362811358，与交易及仓位PnL一致；另一SELL不冒充本窗口新完整闭仓。普通MAIN和核销本窗没有自然样本，只有对应定向测试/既有审查，明确未完成本窗自然验证。

错误尾部包含部署后14次事件：没有本轮NULL/TypeError观察崩溃，但有case48观察补抓TimeoutError（05:16:32Z），以及HTTP429/RPC错误。源码该非持仓补抓为3秒预算，捕获超时后继续处理已有targets，不是本次新增或未捕获崩溃；具体上游超时根因不能仅凭异常名称确定。本次不提高超时、不抢占持仓通道、不把所有错误案件改为已解决。

## 不能据此宣称已解决的事项

免费源缺流动性、原池缺覆盖、429及Demo本地日预算仍可能阻止形成合格新行情。本次解决的是本地时间戳/队列错误，不能保证上游覆盖率。普通Paper仍按用户认可的原池市场价、双侧4%及共享1000美元门槛模拟，不加入深度/冲击估算，不保证对应数量能在链上卖出。接线正确不等于策略盈利已验证，短窗也不能逐个证明224策略全部自然触发通过。
