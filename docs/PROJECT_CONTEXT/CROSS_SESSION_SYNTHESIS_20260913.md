# 跨会话综合：旧设备历史结论 × 本设备当前状态 × 对当前工作的修正

时间：2026-09-13（本设备 `H:\OpenTrader\memeTrader_2`）
来源：用户上传的旧设备 session 包 `session-e4b95c0e`（cwd `E:\memeTrader`，58 MB 主 session
+ 15 个子 session），已解压到 `data/research/oldsession/`，摘要见该目录 `digest/`。

**这份文档存在的理由**：旧 session 与我这次 session 执行的是**同一个 goal**
（`goal-6e9a0a98…`，目标文本逐字相同），旧 session 在旧设备上积累了**大得多的证据基础**
（39 GB 库、24h 内 3,286 个仓位）而本设备的新资金期只有 ~450 个仓位。我此前有几处结论
**建立在过小的样本上，或与旧 session 已测得的结论冲突**。下面逐条记录，并明确区分
「已确认」「已修正」「待你决定」。

---

## 一、必须立即修正的三处（我此前的说法有误）

### 1. 那 237 个被暂停的策略**不是缺陷，是你自己的决定**
`docs/PROJECT_CONTEXT/ATTACHMENT_EXIT_CONTROL_20260913.md` 记载：你提供了**442 行策略
导出**，其中 **237 个明确标注「暂停新入场，保留退出」**。该控制已于
`2026-09-12T17:32:05.617030Z` 写入 `chain-meme-account-convergence/v1:<period>`，成为
当前 Paper 资金期的**真实运行控制**而非界面文字。来源文件 SHA256
`19db13ddc8a5add1f1b5e261c9e9348401d5b3e46e757e6026a5ea1b366b08cb`，可复现脚本
`scripts/apply_attachment_exit_controls_20260913.py`（默认只预览，须显式 `--apply`）。

明细：61 重复/被替代退役、162 负收益实验停止、10 证据不足暂停、4 数据受阻暂停，
**205 保留继续入场**。

**撤回**：我在 ROUND120_RECORD 中把「110 个运行时新增臂被误停」写成工程缺陷。该控制是
按你导出的 442 行逐条应用的，且脚本会先固定校验「442 条来源 / 237 条退出标记 / 显示编号
唯一映射」才允许写入。**在能逐条证明某个臂在你导出里是「保留入场」却被暂停之前，不应
把它称为误停。** 你的答复「先别动，仅继续观察」与这份文档完全一致，我未做任何改动。

**同时修正我的框架**：`no_active_matching_entry_policy` 占 32% **不是通道坏了**。「可准入
臂全部暂停 + 未暂停臂全是观察模式」正是你导出的必然结果——你保留的 205 个臂走的是
observer/cohort 通道，**它们才是真正在交易的那条路**（本资金期 450 个仓位由此产生）。

### 2. 我的 EXIT150 臂**不是 1U，是 20U**
同目录 `CONTINUOUS_OPTIMIZATION_ROUNDS2_3_20260913.md` 第 38 行明确：
> 「注册值为 1U/最多 2 仓，但当前全局 Paper 统一合同会显示和执行为 **20U/最多 8 仓**；
> 这是系统既有统一层，不更改旧策略。」

这同时解释了我观察到的 `max_concurrent_positions` 登记为 2 而非模块声明的 4。
**所以 EXIT150 三个臂每个实际是 20U、每臂最多 8 仓，不是我以为的 1U×4 有界假设。**
在 8 臂同池上限下，单池最坏敞口是 8 臂 × 20U = 160U。我上一轮报告中「1U 有界」的表述
**不准确**，以此处为准。

### 3. 同池多臂上限：旧 session 两次选择「仅影子、不执行」
- 第 79 轮：用户再次选择「same-token concurrency cap = **shadow only, do NOT enforce**」。
- 压缩摘要 2：「shadow evidence is strong (a 10-arm cap would have blocked 952/1,271
  positions = 74.9%, realized −335.4U); **awaiting explicit user authorisation** to enforce
  (would change existing arms' behaviour)」。
- 压缩摘要 3：「shadow evidence now 1,080/1,332 positions (81.1%) worth −603.35U — still
  **not authorised** for enforcement」。

**我在第 1–2 轮把它执行了**（每池 8 臂 / 每次结算 3 臂）。这既触及你「严禁修改现有策略」
的约束（它改变了现有臂的行为），也与旧 session 记录中你两次的选择相反。当时的判断依据是
我自己的诊断，我没有意识到存在这条历史决定。**这是我需要向你说明并请你裁定的第一件事。**

方向性旁证（不构成授权）：本设备 `scripts/supervise_metrics.py` 刚测出 10 臂影子上限
会拦掉 **339/450 个仓位（75.33%），这些仓位实际结果是 −3,329.95U**。也就是说拦住它们
在这段窗口上是省钱的。但省钱 ≠ 你有授权，且这与「不改现有策略」冲突。

---

## 二、旧 session 已测得、而我此前不知道或低估的关键事实

### 2.1 亏损结构：**摩擦占 58%，不是退出**
旧 session 24h 分解（3,286 仓位 / 9,632U 本金）：总 −1,392.7U（−14.46%），其中
**摩擦 802.7U = 58%**，毛损失仅 −590U = 42%。**只有 37.2% 的仓位曾经超过
`entry × (1 + 摩擦)`**，而摩擦 ≈ 8.33%（4% 买 + 4% 卖）。

→ **含义**：主因是「多数仓位根本没走够覆盖 8.33% 往返成本」，而不是退出规则不好。
这直接重排优先级：**入场质量与摩擦管理 > 退出重构**。

### 2.2 退出层「不是亏损来源」——4 个独立检验一致
- 配对退出合同对照：**28 组里 27 组亏损**；唯一正的是 `alpha149_plateau_stall_exit_v1`
  （+7.75U/57 笔，且去掉单笔 +17.98U 后为 −10.23U）。
- 独立退出复盘：硬止损 −34.97% ROI / 1.5% 胜率；追踪 +29.37% / 86.2% 胜率；
  **20 个变体组中 19 组为负**；**72 小时内没有任何一档止盈平过仓**。
- 固定持有期反事实：持更久更差（30 分钟 −0.065，60 分钟 −0.074）。
- **「早兑现止盈阶梯」的期望值 = `stake × f × (0.44 − 0.558)`，对所有 f > 0 均为负。**

→ **这直接质疑我的 EXIT150**。我的实测（本资金期 350 仓位：峰值经济收益 p50 +22.7%、
p90 +34.8%、最大 +55.0%，+20% 达成率 51.0%）与旧 session 的 37.2% 并不矛盾（样本与口径
不同），但**旧 session 用大得多的样本得出「早止盈阶梯期望为负」**。因此 EXIT150 应当被视为
**一个尚未被证伪、也远未被证实的对照实验**，绝不能当成「修复」。它已按你的要求以纯新增臂
形式登记在前沿 15928，代价可控。

### 2.3 池死亡风险的分层（token 级，已复现）
| 维度 | 死亡率 |
|---|---|
| BSC | **40.4%**（19/47） |
| Solana | 3.4%（3/88） |
| Robinhood | 0.0%（0/9） |
| 入场深度 ≥100k | **3.8%**（1/26） |
| 20k–100k | 20.3% |
| 5k–20k | **32.1%** |
| 基准 | 15.3% |

→ 与本设备我测到的「4 个 BSC 池（深度 41k–78k）造成 89.9% 亏损」**同向**。旧 session 还测得
**24h 内 216 个写销全部是 BSC**（BSC 写销率 31.5%，占净亏损 74%；Solana 0/1,599、
Robinhood 0/184）。**且只有 ≥100k 入场深度档写销为 0 且 PF>1——但该档在实盘不可达。**

### 2.4 金狗画像（39 个案例地址 vs 132 个同周 BSC 对照）
最大倍数 p50 **20.99x vs 1.01x**；≥2x **81.1% vs 10.6%**；≥5x **73.0% vs 3.0%**；
首帧深度 p50 ≈ **19,424U**；本地帧数 p50 **74 vs 6**。
→ **机会在右尾，设计应是「高召回 + 小仓位 + 宽追踪」。**

### 2.5 覆盖率的根因链（已因果确认）
token 级信号率长期在 **0.38%–0.57%**。旧 session 的因果链：
(a) 帧准入 `provider.startswith('dexscreener')` 切掉了 43.5% 的 token → 已由 wave-38 宽面修复；
(b) 入场门要求 `windows['30']` → 已由 `alpha149_broad` 路由 + `requires_distinct_wide_frame` 修复；
(c) 采集预算 → 已批并花掉 +13.7%；
(d) **保留名额落在拒绝路径上**：第 101 轮测得 24 次中 **0 次重叠**，所以 `skip_bucket_full`
几乎不触发（~6 次准入/小时，设计目标 32/小时）。**第 104 轮的 `reserved_candidates()`
辅助函数已就绪且 5 个测试通过，但尚未接线**——这是旧 session 留下的明确待办。

### 2.6 观测密度是入场的前提条件
- **密集观测的 ready 池中，「同一池 60 秒内两次 cohort 评估」满足率 78.3%**（92 个间隔、
  15 个池）；**稀疏池仅 4.0%**（75 个间隔、140 个池）。
- **稀疏 ready 池：累计 161+ 个，0 次转化。**
- 模式观察窗 30 槽已饱和（3 链 × {early 3, growth 4, mature 3}），`watched≈32–37`。

### 2.7 两个会直接毁掉统计的陷阱（旧 session 已写成硬纪律）
- **时间窗陷阱**：库内时间戳是 `2026-09-12T01:06:42.450166Z`，而 `datetime('now')` 返回
  空格分隔。文本比较 `col >= datetime('now','-N minute')` 会把截止日的**整日**行都算进来，
  实测把窗口**放大 2.1–5.5 倍**（旧 session 因此一度把「近 60 分钟」算成整日，约 24 倍）。
  **必须用 `julianday(col) >= julianday('now','-N minute')`。**
  *我在本轮 `probe2.py` 里犯过完全相同的错*，当时只当作笔误，现在确认这是已记录的陷阱。
- **镜像陷阱**：`strategy-observer:X` 会在**同一** `observed_at` 复制 `X`，快照行中
  **64.3%** 是这种评估副本（一次抓取 → 2–3 行、价格相同）。**必须按
  `(token_id, observed_at)` 去重，否则所有计数翻倍。**
  这与我观察到的「轨迹引擎收到相隔 2.15 秒、数值完全相同的"两帧"」是同一现象。

### 2.8 查询纪律（旧 session 五条，血泪换来的）
1. 针对具体新臂**用精确 `arm_id` 列表，绝不用 `LIKE` 通配**——旧 session 曾因
   `LIKE 'alpha149_%flow%'` 误匹配旧臂，在同一问题上**连续得出 5 个错误结论**。
2. 判定「有样本」要按**每臂全时**或覆盖所有开仓批次的时间窗，并先看该臂最新 `opened_at`。
3. 任何「0 样本 / 停滞 / 缺陷」结论**必须用第二个独立测量确认后才能发布**。
4. 转化率/胜率只能在**固定年龄队列**内比较。
5. 池死亡结论必须同时给 **token 级和仓位级**分母——一个死池被约 40 个臂持有。

### 2.9 已有的「监督-复盘-优化闭环」——**已经建好，我此前在重复造**
`scripts/supervise_metrics.py`（漏斗/覆盖/经济性/容量/稳定性 + 9 个复盘触发器，写
`data/reports/supervise/`）、`scripts/trade_context_ledger.py`（每笔交易上下文：发现来源、
入场机制、MFE、退出原因、持有分钟、`washed_out` 标记）、`scripts/paired_arm_ab.py`
（同 cohort 配对 A/B + 按 token 聚类的 bootstrap 置信区间 + leave-one-token-out，
`MIN_SETTLED_PER_SIDE=20`，不足则打印 `NOT READY`）。

**这些正是你 §2 要求的机制，且已在本设备可用**（我本轮已成功运行 `supervise_metrics.py`）。

---

## 三、本设备当前权威状态（`supervise_metrics.py --hours 2`，2026-09-12T20:24Z）

```
discovery              3,523.7 新 token/小时
collection             8,877.6 快照行/小时，其中 7,294.4 深度>=1000U
evaluations 采样        17,427  →  admitted 592
positions opened       102 个 / 73 臂 / 8 个 token
token 链               2,641 已评估 → 10 有信号 (0.38%) → 8 已交易 (12.75 仓位/token)
write-offs             177 (-3540.0U)   open 52 仓位 / 4 token / 1040U
arm cash               p10 940U  p50 1000U   低于 20U 的臂: 0
shadow 10 臂上限        会拦 339/450 仓位 (75.33%)，这些仓位实际 -3329.95U
未解决错误用例          15（窗口内新增 10）
```

复盘触发器（warn/error）：`stop_family_loss_dominant`（硬止损 114 笔 −502.44U，均值
−4.407U）、`write_offs_present`、**`token_signal_rate_low`（仅 0.38%）**、
`token_crowding`（8 个 token 上 102 个仓位，最拥挤 36 臂）、
`single_token_concurrent_exposure`（`solana:JQWYyzQ9…` 同时持 32 臂 / 640U），
以及 8 个活跃错误用例（`chain-meme-market-marks` TimeoutError ×63 / HTTPStatusError ×2、
`chain-meme-pattern-observer` TimeoutError ×60、`pumpportal:metadata` 429、
`native-launch:pons_v1/v2` 超时、`native-paper-held` ValueError ×9、`capital-quote`
QUOTE_TIMEOUT ×9）。

**结论**：你最初的问题——「发现大量 token，但绝大多数未进入策略判断与开仓」——**被精确
证实且定位**：token 级信号率 **0.38%**，即 2,641 个被评估 token 里只有 **10 个**触达任何臂。
这与旧 session 的 0.57% 基线一致，说明它是**长期结构性**的，不是本设备新引入的。

---

## 四、据此重排的优先级（替换我之前基于 87 分钟样本的排序）

| # | 动作 | 依据 |
|---|---|---|
| **P0-1** | **接线已就绪的 `reserved_candidates()`**（第 104 轮遗留，5 测试已过） | 观测密度是入场前提：密集池满足率 78.3% vs 稀疏池 4.0%，稀疏池累计 161+ 且 0 转化；保留名额当前落在拒绝路径上，24 次 0 重叠 |
| **P0-2** | **摩擦治理**：只有 37.2% 的仓位越过 `entry×(1+8.33%)` | 摩擦占 24h 亏损 **58%**；这是唯一能系统性改变符号的杠杆 |
| **P0-3** | **按链/深度分层入场**：BSC 40.4% 死亡率、深度 5k–20k 32.1%，而 ≥100k 仅 3.8%（但实盘不可达） | 已复现的 token 级风险分层；本设备 4 个 BSC 池造成 89.9% 亏损 |
| **P0-4** | 让 EXIT150 与 `alpha149_confirmed_stop_steady_v2` 积累**独立前向终局样本**，用 `paired_arm_ab.py` 判读 | 旧 session 的「早止盈期望为负」需要被前向检验，而不是被我的小样本推翻 |
| **P1-1** | 用 `supervise_metrics.py` + `trade_context_ledger.py` 作为**常设**闭环，按**独立 cohort**（而非同 token 多臂）计样本 | 你的 §2 要求；同 token 多臂会虚增样本量 |
| **P1-2** | 索引 `(definition_version, evaluated_at, id)`；观测型评估不再写 67KB `feature_json` | 30–358ms → ms；约 235MB 与约 50% 写压力 |

**明确的非目标**：不再把「退出重构」当作首要修复。旧 session 四个独立检验一致指向
「退出层不是亏损来源」。

---

## 五、诚实边界

- 旧 session 的 24h 数字来自旧设备 39GB 库（3,286 仓位）；本设备新资金期只有约 450 仓位。
  两者**不可直接相减或混合**，本文只做方向性对照。
- 「摩擦 58%」是旧 session 的分解口径，未在本设备复算。
- 金狗画像是**历史案例对照**，不是可交易的前向信号；不得据此回填或调参。
- EXIT150 的档位选择是**样本内**的，只能用前沿 15928 之后的数据评判。
- `data/research/oldsession/` 下的 session 摘要是**证据**，不是任务队列；其中未完成的
  待办只有 `reserved_candidates()` 接线一条被我确认为仍然有效。
