# 现金拒绝与核销损失复核157

## 结论

两个补充案例的1083次 `entry_cash_below_order_size` 拒绝，可以用当时可获得的Paper现金流水复现；不是把43个独立账户误当作共享1000U资金池，也不是因4%滑点多扣了买入本金。更重要的是，这些账户主要被已实现Paper亏损消耗，而非被未平仓占住资金。

这不说明整体交易链路已经健康、也不证明其余过滤合理：安全未知过期、历史跟踪缺口、当前采集超时仍是不同问题。不能用增资/重置使亏损账户重新开仓来冒充修复。

## 截止时间、方法与证据

2026-09-13 20:13:42 UTC只读事务；源案例 `cases_20260913T200146Z.json`，两个Token为Robinhood `0x1cdb289befdfac8af945a288bcdccc382cb34d32` / `0xbea92c5bffd98972d7b83b8a7e7f6d0209751e18`。决定发生在17:45:37–18:19:35 UTC。

- 1083次拒绝分布于60个唯一cohort、43个策略、2个Token；742与341分别来自两个Token，不能称为1083个独立机会。
- 当前资金期注册1000U/策略、普通20U/笔，无自由名义本金激活或资金期恢复变更；唯一执行配置自18:26:45 UTC（前一天）生效，额外费用0。滑点影响买到的数量/价格，不额外增加20U本金扣款。
- credits、voids、contaminations、contamination resolutions、market fill corrections在该资金期均为0。脚本遇到任何非零修正或资金/费用变化就停止，不能把原始现金合计冒充有效账本。
- 对每次拒绝重读不可变决定主键；仅纳入 `created_at` 与 `recorded_at` 均不晚于该决定的本策略交易。复算 `1000 + Σ net_cash_flow_usd`，1083/1083均低于20U（容差1e-9），无复算分歧候选。没有拿今天现金解释过去，也没有计入未实现估值。
- 原始SQL事务前沿未存入这些历史决定；时间戳复算与结果一致，不是逐指令状态的完美重放。若有分歧应调查时间前沿，不自动改写决定。

每个策略取这两个案例中第一次现金拒绝（43账户，**高度相关的策略对照，不是43个独立投资实验**）：现金0.41–19.58U，平均11.16U；Paper已实现损益约−999.59至−880.42U，平均−976.75U；按现金与已实现损益推得未回收成本平均12.09U、最高100U。以上不是全系统账户表现或真实账户亏损。

## 为什么现金几乎耗尽

选取现金最低的 `alpha149_merged_multi_setup_v1` 作定向核查，不声称它是随机或代表性样本：

- 90个BUY，累计投入1800U；退出回款800.409747U；初始1000U后剩余0.409747U。
- 51个不同Token发生WRITEOFF，每个损失20U，累计−1020U。
- 其余39个SELL累计净损益约+20.409747U，因此该账户总已实现约−999.590253U。现金耗尽主要来自核销尾部损失，不是只由手续费或止损洗出解释。

核对全部51个核销，保存的终止证据都满足：同原池（EVM大小写规范化）、有数值流动性且低于当时1000U门槛、观察≤入库≤核销，观察至核销0.021–0.908秒，流动性最高486.68U。没有在这51条中发现“缺值当零”、错池或超时直接核销。例：cohort15入场池流动性41689U，核销证据0U；cohort1119由9770.73U至1.58U；cohort7461由16671.62U至0.03U。

证据边界：这是保存的原池判断证据，不是独立重放供应商原始HTTP报文或链上储备；旧行情历史已不全，但不可变mark内仍有terminal_dust_pool证据。低于1000U按现行Paper约定核销，不等于真实链上已经卖出，也不一定意味着代币价格归零。未把今天价格回填历史、未取消任何核销。

## 实施与验证

新增离线 `scripts/audit_cash_cases157.py` 及3项定向测试。脚本默认15秒查询预算、每策略最多5000条交易（超过则停止）；索引查询、只读事务、无网络请求、无生产写入。输出使用独占创建，不能覆盖旧工件。

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_cash_cases157.py -q
.\.venv\Scripts\python.exe scripts/audit_cash_cases157.py --casebook data/research/goal_cases156/cases_20260913T200146Z.json --output data/research/goal_cases156/cash_cases157_final.json
```

3项测试通过；生产只读审计完成，原始结果留在上述JSON。不要用同名路径重跑覆盖。新增脚本不在运行路径，无服务部署/重启需求；回滚仅撤销157脚本/测试/文档。未更改策略、现金、历史或Live。

## 后续优先级与未完成项

1. 155传输/健康修复仍等待获准的启动路径或操作者重载。04:06 UTC+8回执确认旧PID40164，快照424683仍推进，但持仓请求进程累计534批/243失败。禁止绕过此前宿主重启拒绝；必须有真实新manifest和负载验证后才能标为恢复。
2. 策略经济侧应优先控制低流动性核销尾部，而非一味延长持仓或为亏损臂重新注资。可复用现有流动性变化/历史路径特征，在独立Paper版本与已有基线比较；本阶段没有足够跨时间独立验证去宣布某一过滤阈值更优，也没有重复添加相似策略。
3. 43账户现金不足不是阈值计算补丁的证据。其参与者路径与安全未知问题、其他用户Token的低频跟踪仍分别保留。当前工程目标不标完成；已有周期复盘保持运行，收益结论继续标注待自然样本。
