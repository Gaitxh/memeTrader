# 重新评估241：237实际上线、全策略复核与实验配对依赖

## 本轮范围与时间口径
用户要求基于新增运行数据重新全面推进，不重置Paper账户、不触碰Live、不以策略数量代替结果。当前工作目录D:/OpenTrader/memeTrader_2，使用Remote Desktop Commander实际执行。没有调用子agent，也没有创建定时研究或后台自动改策略任务。

基线读于2026-09-19T03:12:03.316428Z，交易结果使用随后冻结的positions_frozen241.json。当前资金期chain-meme-trader/funding-20260906-v002-final-1000；旧进程11396/520策略/67未暂停。数据共42,224个账户仓位、2,207个Token；36,467 closed、5,753 written_off、4 open。账户仓位不是独立机会。

上一份240结果截止02:53:25Z为42,207仓位/2,202Token。本次基线只比那个截止新增17个账户仓位和净增5个不同Token；更长的过夜积累已含在旧结果内，不能重复算成本轮新增证据。当前冻结和更早资料分开，不把全部历史重新标成今日发现。

## 1. 237已完成生产加载，不再停在测试阶段
先核对正式runtime.py、store.py、relative_reawakening237.py与application240.json的三项测试源码哈希，全部相同。复用上一轮58项通过的真实Store/旧235回归，不重复运行同一批测试充数。

核验11396→项目venv18368→run_paper监督器4296后，只停止这一Paper worker一次，由原监督器重载；Web和监督器不动。新进程30376于03:13:17.672989Z启动。237于03:13:09.227086Z注册，snapshot1690480，加载定义总数521。

03:14:11.392049Z部署回读确认：只新增revision237_relative_reawakening_v3；旧追加记录逐条相同，具体策略控制、配置保持，登记源码全部匹配，/health运行。资金期和已有仓位退出没有重置。部署证据deployment237_241.json。

该修订沿用旧绝对静默策略的退出组合、20U/最多8仓，只将入口改为已有正活动基线之上的相对增长，且遵守原池/时钟/后帧规则。没有新增行情接口或独立轮询。

03:23:27.868887Z的仓位读回：237尚无自然仓位。03:28:20Z读取最近5000评价行，237实际有825次判断、覆盖60个Token：等待基线319、历史数量不足116、换源或混合身份47、池龄不足185、正基线不足51、价格尚未恢复54、基线未压缩1、观察断档52。这些是判断次数，不能当825个机会。

这证明它已接入实际评价，而不是证明它应该产生交易或具备正期望。没有人为注入生产信号或回填成交来缩短等待。

## 2. 当前520条定义的重新整理，不抹去退役策略
全量冻结结果分别生成all_strategy_review241.json/.md和修正口径后的all_strategy_review241_corrected.json/.md。包含453个保留暂停的定义，14个无终局、36个样本不足、6个分链方向混合、9个已观察负收益待机制复核、1个观察正收益但未证明期望、1个正收益依赖最大赢家。

该目录逐账户保留规则、源码定位、激活前沿、分链损益、终局/开放数量、全损/剩余核销及最大赢家敏感性。它是完整机械目录和部分深入机制复核，不冒充逐行审计所有历史源码或重做归档账本。更早的13版本和归档账本结果保留既有证据，不重计为本轮成果。

本轮真实观察到的例子：
| 策略 | 终局Token | 模型净盈亏U | 说明 |
|---|---:|---:|---|
| trajectory169_trend_runner_control_v1 | 148 | +100.633474 | 41胜107负、profit factor1.1200；去掉最好Token仅+9.876322U |
| trajectory187_solana_runner_v1 | 256 | +15.83 | 去掉最好Token约-73.97U，右尾依赖明显 |
| trajectory169_trend_runner_moonbag_v1 | 148 | -96.12 | 当前已暂停；不能仅凭相同样本数当成逐笔配对 |
| trajectory144_trend_runner_v1 | 586 | -204.81 | 与169的起始时间/准入总体不同，不能直接做因果相减 |

这些不是统一资金组合，不能相加作为系统账户损益。当前仍存在累计正收益，不能说“所有原策略均已失效”；但也不能据少数赢家宣布稳定快进快出或未来持续盈利。

## 3. 识别实验配对依赖，并拒绝重复开账户
源码中isolated observer的paired_entry_group要求组内所有预期成员都具备准入资格。169 control仍声明paired_entry_size=2，而其moonbag同伴已暂停。它虽未自身暂停，却有静态配对阻断；这是既有实验约束，不应与普通策略筛选或数据故障混淆。

本轮实现了独立后继候选并在隔离Store完成5项测试：原配对因同伴暂停而明确返回paired_group_not_all_eligible，新候选和144在后续有效帧共享同一BUY；新旧账户与停止状态保留，原止损仍需后帧卖出，现金核对正确。

但继续检查发现：删除配对约束以后，候选与现有144的交易规则只剩entry_family和entry_filter.direction标签不同；信号源、入场字段、退出、容量和成本相同。新旧账户历史资格仍可能不同，但没有新的独立交易机制。因此最终拒绝把它新增上线，避免重新开一个相同风险账户再当作研究增量。

候选源码已从生产src移至证据目录independent_runner241.REJECTED_DUPLICATE.py，接线只留在isolated副本，未修改生产runtime/store，未注册该候选。测试和independent_behavior_comparison241.json保留，后者是静态合同对照，不是被拦截的历史同源收益查询。

最初隔离运行的Windows双换行问题及测试遗漏activated_at、方向标签断言错误均有失败记录；最终5项通过，不把失败/重跑数量累加。实际历史漏单数的追加查询被拦截，不能凭这个复现声称具体少买了多少币。

## 4. 修正已有全策略复核工具的可入场口径
scripts/review_policy_lifecycle233.py增加纯内存entry_availability，按同一观察通道的配对成员、暂停及forward_enabled辨认结构性依赖。不增加SQL、网络请求，不参与交易风控。

旧entry_capable字段为兼容仍保留“未暂停数量”，新增明示定义与structural_entry_upper_bound。对本轮冻结520条定义，67未暂停中有1个不完整配对，因此结构上的上界为66，而不是保证66条实际可买。信号、资金、原池和执行条件仍可能继续过滤。

同时修正离线full_loss_writeoffs：没有realized_proceeds_usd或其值未知时，不再默认为0并列为全损。该字段缺失只影响报告分类，不改任何账本。本轮真实冻结仓位修正前后所有金额指标逐项相同。

最终availability_tests241.xml为原8项加本轮5项共13项通过。此前被拒绝的历史配对查询未换作别的工具重做；上述是已读合同与冻结数据的静态复核，不能提供真实漏单次数。

## 5. 另一项候选未完成，不写成上线
曾提出retest241实际成交前重验原种子折价和深度的独立候选，以处理“信号时有折价，安全等待后已涨回去”的问题。只写入32行策略声明，后续函数追加被工具层拦截。因此该项没有实现完整、更没有测试/注册/自然结果。已移出生产目录，保存为retest_entry241.PARTIAL.txt。

该问题与169配对约束是不同机制；两者不能互相替代验收。237的部署也不能被当作这个候选完成。

## 6. 系统与资源
当前已部署采样仍为239的五字段变化签名及原15/30/60秒退避；持仓、待执行任务保留优先级，预算和并发未变。基线3个持仓Token的读取显示原池报价年龄上界约0.668秒；仅是那个读取点，不是全天SLA。原累计队列字段不以简单相减推断损失；累计计数的守恒关系未单独验证，不能直接相减；直接dropped字段另列。

本轮没有为了新策略增加接口、队列、付费数据、逐策略SQL或采样层。237只使用现有80帧窗口。配对报告修正只处理已读冻结结果。最终性能/仓位/控制组合读回被工具层拦截，所以没有最新performance_after241.json/final_runtime241.json，不宣称整体性能改善或最终全链路验收通过。

最后成功部署回执为03:14:11UTC；最后健康和237仓位读回03:23:27UTC；最新237实际评价证据03:28:20UTC。分别使用对应截止，不用旧结果冒充更晚的状态。

## 7. 十二节验收映射与真实边界
| 原任务 | 本轮推进 | 未达范围 |
|---|---|---|
| 01 基线与清单 | 新进程/源码/策略/控制和42224仓位冻结 | 不把旧研究截止当今日数据 |
| 02 全链路少交易原因 | 237实际825次判断、169配对阻断真实Store复现 | 全期各来源/队列因果分解和实际漏单数未重做 |
| 03 样本复盘 | 沿用239补17键/12本地身份及完整HOP记录 | 未恢复的身份和早期可见性仍未知，不补造 |
| 04 特征研究 | 237相对活动合同实际接入，基线/缺失/年龄原因可见 | 尚无其自然BUY或特征正增量证据 |
| 05 新策略落地 | 237正常注册/加载521清单 | 成交前折价复核候选受限未完成 |
| 06 现有/退役策略 | 当前520逐项目录含453暂停；配对依赖和重复行为深入复核 | 不能把机械目录当所有历史版本的完整因果审计 |
| 07 发现采集 | 保留已部署239修复，未增加资源负担 | 当前仍未获得全来源长期频率与公平性验收 |
| 08 性能 | 报告计算与交易路径隔离；阻止重复账户扩张 | 最终性能读取受限，不能判全局提速 |
| 09 严格前向 | 237真实注册前沿、旧账保留；复用相同代码58项工程验收 | 不保证未来盈利或真实链上成交 |
| 10 实际实施 | 一项生产部署；一项报告修正；5项候选验证后拒绝重复 | 新候选不因测试通过自动部署 |
| 11 知识与历史 | 本报告、独立memory和证据目录 | 更新历史/提交以成功回执为准 |
| 12 全面复核 | 逐项区分运行、代码、测试、结果与未知 | 不声称十二节全部完成或盈利GO |

## 证据文件索引
均位于data/research/reassessment241_20260919/，旧源码/参数与受限尝试保留，不含密钥。
- baseline241.json、positions_frozen241.json、effective_policies241.json：本轮输入与前沿。
- deployment237_241.json：真正上线237、旧定义控制不变及源码校验。
- natural237_checkpoint241.json、relative237_reachability241.json：不同时间截点的自然状态。
- all_strategy_review241_corrected.json/.md：完整520定义的修正口径复核。
- independent_behavior_comparison241.json、independent_third241.xml、isolated/tests/test_independent_runner241.py：重复机制结论及真实Store复现。
- availability_tests241.xml、review_policy_lifecycle233.before241.py：13项报告工具回归与回退原件。
- retest_entry241.PARTIAL.txt：被拦截的未完成候选，未进入生产包。

当前模型仍是原池前后帧Paper，使用既定成本而非宣称每笔实际链上能成交。已实现净收益与未知可退出性保持区分；独立策略账户结果不可相加为一个真实组合。没有任何新Live授权或账户重置。

本轮数据截至相应回执；报告本身不是更晚时点的运行证明。后续若要解决剩余项，需要取得相应自然样本、恢复被拒绝操作的受支持执行能力，或找到确实可用的新证据，不能以反复改计划、重命名候选或重跑通过测试替代。

## 发布与恢复追记
03:35:32UTC更新历史文件成功合并，219条，旧条目完整保留。03:36:13UTC通过/api/updates实际读回20260919-reassessment241-deploy237-pair-review。根CURRENT_OBJECTIVE_AND_PLAN.md的原子替换被Windows WinError5拒绝，未更新，staged文件保留；不以成功写出独立memory冒充根索引成功。

恢复请直接读取memory/REASSESSMENT241_20260919.md或系统更新历史的241入口。此前240里“237尚未加载”的状态已被deployment237_241.json取代。

Git为有限范围交付：新237模块、它的两份测试、报告工具修正及测试、本轮报告/记忆和已验收的237局部接线patch。未整体暂存runtime.py/store.py中大量前期工作。该提交不是整个工作区的clean-checkout发布，运行代码以已读manifest及application240哈希为准；局部patch应用对象必须核对，不应重复施加到已含237的文件。

具体提交结果见证据目录commit_receipt241.json；没有成功回执不声明提交完成。本轮没有向外发布或推送配置、数据库、密钥、原始研究账本或被拒绝的候选片段。
