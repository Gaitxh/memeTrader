# Snapshot — 2026-09-01

## 结论

本快照只记录代码边界和当前本机 r6 的自然前向事实。所有研究层均不改变 Strategy、Decision、Position、Paper execution 或 Live；它们固定 `decision_eligible=false`，并按层使用 `affects=none` 或明确的审计/报价研究标记。没有使用 future data、历史回填或注册前赢家。

## P0-A：Token 可执行性证据

`token-universe-fixed-target-execution/v1` 已在 activation cohort 21987 后注册，固定 baseline→15/60/240m 目标，不使用事后峰值。r6 只读结果为 1,718 条：`unsupported_chain=1,587`、`insufficient_liquidity=66`、`baseline_missing=41`、`safety_unknown=20`、`modeled_executable=4`。4 条模型可执行均为 BSC 15m；这是冻结 `$35`、双边 4% 不利滑点和配置费率后的 Paper 模型，不是成交、收益或 router quote。链费、priority fee、MEV 和部分成交仍未建模；Solana 在此层仍是 unsupported。

`token-universe-jupiter-quote/v1` 是独立的 Solana 只读 route overlay。它只请求 Jupiter V2 `/order` 的 mint/amount/slippage 字段，不发送 taker/payer/receiver，拒绝非空 transaction，永不签名、广播或调用 execute/build/submit。当前 r6 有 62 条结果：baseline BUY quoted 38、15m target SELL quoted 17、target SELL error 7；尚无成熟的 60/240m target 分母。SELL 使用 baseline 的 `otherAmountThreshold`，因此 `round_trip_min_return` 仍只是最低输出报价下界，不是 fill、Paper 成交或利润。

质量覆盖层仍把 `same_route_return`、估算成本和“确认可执行”分开：57,291 条质量结果中 `net_executable_return_after_costs` 非空为 0。未知安全字段不会按零处理。P0-A 的工程链路已覆盖，P0-A 的自然可执行性结论尚未成熟。

## P0-B：no-decision attribution v1

代码已实现并部署 `missed-opportunity-no-decision-attribution/v1`：追加式 registration/result 表绑定注册后的 `missed-opportunity-audit/v1`，按实际观察到的 hydration、trigger、admission、Agent result、lookup/relation、candidate/evaluator 断点记录终态，并强制不可更新/删除、`decision_eligible=0 / affects=none`。缺少节点只表示未观察或未调用，不等于拒绝。

活动 r6 已在 `2026-09-01T03:07:59Z` 冻结 activation cohort/audit=`24661/63063`。核验时 cohort 已到 24765，但还没有新的成熟 missed audit，故 attribution 仍为 0 / `registered_waiting`；这是等待前向目标时点，不是通过、拒绝或无漏检。定向测试证明注册前 cohort 不回填，且 observation、ingestion、recording 任一时间晚于目标时点都不能作为归因。不得把 identity/context-only、zero-yield 或 Agent 自报升级为决策证据。

## Paper 交易稀疏度

当前仍只有 6 笔 fill，即 3 次 BUY 和 3 次 SELL，0 开放仓位；全库 Decision 为 WAIT 1,489、REJECT 27、CANDIDATE 3。完整 Token 漏斗已注册 15,000+ cohort，但当前质量有效潜在机会在 15/60/240 分钟分别为 157/155/76，其中进入 candidate evaluator 的只有 4/8/6，形成 CANDIDATE 与 Paper BUY 均为 0。Agent 预算不是当前阻塞：当日 Token Context 39/256 calls、约 2.77M/50M tokens；更明显的覆盖断点是大量 `no_eligible_trigger`、Context `global_cooldown_active`、Agent `no_context/insufficient_reachable_sources`、reverse lookup zero-yield，以及候选 `match_score_below_minimum`。这些是漏斗相关证据，尚不能单独证明某个阈值应放宽；P0-B 新账本用于在同一潜在机会 cohort 和固定目标时点上建立因果可归因分母。

## 下一最小增量

保持 Paper、Live lock、策略和报价边界不变：先完成 P0-B v1 的受控前向注册并观察自然终态；同时继续积累 Jupiter BUY→固定目标 SELL 的完整 horizon/route 分母和 EVM 双端安全样本。成熟前不宣称长期治愈、可成交利润或真实收益。
