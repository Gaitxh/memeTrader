# Snapshot — 2026-09-01

## 结论

本快照只记录代码边界和当前本机 r6 的自然前向事实。所有研究层均不改变 Strategy、Decision、Position、Paper execution 或 Live；它们固定 `decision_eligible=false`，并按层使用 `affects=none` 或明确的审计/报价研究标记。没有使用 future data、历史回填或注册前赢家。

## P0-A：Token 可执行性证据

`token-universe-fixed-target-execution/v1` 已在 activation cohort 21987 后注册，固定 baseline→15/60/240m 目标，不使用事后峰值。r6 只读结果为 1,718 条：`unsupported_chain=1,587`、`insufficient_liquidity=66`、`baseline_missing=41`、`safety_unknown=20`、`modeled_executable=4`。4 条模型可执行均为 BSC 15m；这是冻结 `$35`、双边 4% 不利滑点和配置费率后的 Paper 模型，不是成交、收益或 router quote。链费、priority fee、MEV 和部分成交仍未建模；Solana 在此层仍是 unsupported。

`token-universe-jupiter-quote/v1` 是独立的 Solana 原始只读 route 账本。它只请求 Jupiter V2 `/order` 的 mint/amount/slippage 字段，不发送 taker/payer/receiver，拒绝非空 transaction，永不签名、广播或调用 execute/build/submit。只读审计在固定边界 `result.id<=239` 证明历史延迟主要来自本机队列而不是 Jupiter 响应：baseline queue p50≈1,399 秒，15m target p50≈624 秒，60m target p50≈156 秒；baseline/15m 的 total delay 绝大多数超过 60 秒。因此旧 v1 的 `round_trip_min_return` 只能作为 raw diagnostic，不能继续解释为同一固定时点的可执行证据。

本轮新增 `token-universe-jupiter-quote-validity/v1`，于 `2026-09-01T03:54:51Z` 冻结 activation cohort/raw quote result=`25941/374`。旧 v1 表与结果不修改、不删除、不回填。新层的 baseline anchor 是 baseline 快照 `source_recorded_at`，target anchor 是预先冻结的 outcome `target_at`；请求 queue 上限 30 秒、anchor→completed 上限 45 秒。请求前过期不会访问 Jupiter；响应后才过期仍保留白名单 raw quote，但不会生成往返回报。只有 baseline 与 target 均 time-valid 且 quoted，才计算覆盖层 `round_trip_min_return`。首个自然截面有 101 个 validity 终态：valid quoted 9、queue expired 36、source missing 56、valid round-trip 0。

随后定位到一个真实调度饥饿点：每个 tick 的 `limit=3` 同时限制了陈旧缺口结算和真实 provider 请求，三个已经过期且不会访问 Jupiter 的任务仍会占满本轮槽位。Runtime 现会在最多 10,000 个到期项中寻找新鲜任务，但每 tick 最多只同步终结 12 个陈旧缺口；真实 Jupiter 请求仍严格不超过 3 个、单主机间隔仍至少 2.1 秒。这个分离既避免 provider 饥饿，也避免一次写入大量缺口长期阻塞单进程。受控重启后的自然 valid quoted 已从 9 增至 18；12:45 截面共有 1,911 个 validity 终态：baseline queue expired 992、source missing 98、valid quoted 18，target baseline-not-valid 794、queue expired 9、valid round-trip 0。这个结果证明新鲜 provider 采样恢复，不证明往返回报或交易机会。它不写 Decision、Position、Trade，固定 `decision_eligible=0 / affects=none`。

质量覆盖层仍把 `same_route_return`、估算成本和“确认可执行”分开：57,291 条质量结果中 `net_executable_return_after_costs` 非空为 0。未知安全字段不会按零处理。P0-A 的工程链路已覆盖，P0-A 的自然可执行性结论尚未成熟。

## P0-B：no-decision attribution v1

代码已实现并部署 `missed-opportunity-no-decision-attribution/v1`：追加式 registration/result 表绑定注册后的 `missed-opportunity-audit/v1`，按实际观察到的 hydration、trigger、admission、Agent result、lookup/relation、candidate/evaluator 断点记录终态，并强制不可更新/删除、`decision_eligible=0 / affects=none`。缺少节点只表示未观察或未调用，不等于拒绝。

活动 r6 已在 `2026-09-01T03:07:59Z` 冻结 activation cohort/audit=`24661/63063`。新增只读 `quality_view` 只连接 `quality.assessed_at<=attribution.classified_at` 的不可变行，不用后来质量结果重写归因。13:10 截面已有 374 个 raw attribution，冻结时 quality available=374、missing=0；fixed-target raw≥25%=241、same-route path≥25%=356、canonical liquid path≥25%=90、estimated after costs≥25%=73，但 confirmed executable known=0、confirmed executable≥25%=0。最大断点为 `no_eligible_trigger=237`，其次是 lookup `no_results_returned=78`、`all_results_outside_time_window=34`，match score below minimum=15。这个分层证明 raw potential miss 与已确认可交易机会之间仍有完整证据缺口。注册前 cohort 不回填，且 observation、ingestion、recording 任一时间晚于目标时点都不能作为归因；identity/context-only、zero-yield 或 Agent 自报不能升级成决策证据。

## Paper 交易稀疏度

当前仍只有 6 笔 fill，即 3 次 BUY 和 3 次 SELL，0 开放仓位；本轮部署前全库 Decision 为 WAIT 1,513、REJECT 27、CANDIDATE 3。交易稀疏度目前不能判断为“正常”，也没有证据支持直接放宽风险门。已证明的断点包括大量 `no_eligible_trigger`、Context `global_cooldown_active`、Agent `no_context/insufficient_reachable_sources`、reverse lookup zero-yield、候选 `match_score_below_minimum`，以及本轮隔离出的 Jupiter stale queue；Agent 日预算不是当前主要阻塞。P0-B 新账本用于在同一潜在机会 cohort 和固定目标时点上建立因果归因分母，P0-A validity 层则先保证“是否错过机会”的路由回报不是由迟到报价伪造。

## On-chain-first 独立前向 Shadow

只读复审识别出 749 个历史首次合格 on-chain momentum trigger，其中 748 个在触发前本机没有 Event relation 或 Context assessment，但只有 113 个后来形成 Context outcome；这说明既有 Context outcome 分母遗漏了未准入、失败和无调查结果的链上先动样本。既有 fixed-target 又以 discovery 而不是 trigger 为锚，一些 BSC 样本在 trigger 前已经上涨约 50%–132%，不能用于解释 trigger 后回报。上述历史行只用于证明工程缺口，不进入新研究分母。

新增 `onchain-only-shadow/v1`，注册时冻结最大 funnel transition ID，注册前 749 个 trigger 全部不回填。每个 Token 只接受注册后第一个合格 `onchain_momentum` transition，必须绑定精确 trigger snapshot，且截至 trigger 本机合格 Event relation 与 Context assessment 均为 0。未来/逆序或延迟快照仍入 cohort，但标为无效基线且不会发目标报价；有效 cohort 从 trigger 严格起算 15/60/240 分钟，目标无 pair、错误、缺失、晚到及 later-context 均保留。

Runtime 复用现有 30 秒 token-universe follow-up，不新增长驻 Agent、线程或交易入口。每个 cohort/horizon 只允许一次 Dex quote attempt；EVM 仅在动量与冻结流动性门已满足时随同一快照前向采集 GoPlus 与 Honeypot，来源冲突单独标记。BSC 的 `modeled_only` 仍只是 Dex spot、同 route、安全时效与冻结 `$35`/双边 4% 滑点/费率模型；Solana 只报告 raw market path 并明确 execution unsupported，直至另有 trigger-anchored Jupiter 双腿证据。Audit API 与中英文面板只返回白名单汇总和 Token 链接，不暴露 raw provider body 或 secret。该层固定 `decision_eligible=false / affects=none`，成熟门为 15m 主终态至少 30 Token、15 个独立日期、正/非正各至少 5 个；未成熟前不改变 Strategy、Paper、Agent 频率、仓位或退出。

部署时浏览器 QA 发现 1.46 GB r6 上每次 Web `database_health()` 同步执行 `PRAGMA quick_check(1)` 约耗时 42 秒，导致页面停在首次同步并短时挤压 Runtime。Web 热路径现只检查数据库可读、预期 schema 与 WAL，返回 `quick_check=doctor_only`；完整 integrity check 仍由发布/巡检的 `doctor` 执行。Audit 组合查询实测由约 50 秒降至约 5.1 秒；修复后中英文 Audit、动态 Overview 与 console 0 error 均在实际浏览器验证。

新 Runtime 于 `2026-09-01T07:02:21Z` 以 activation transition `101486` 正式注册。15:23 的自然截面已有 27 个 cohort（Solana 18、BSC 9），27 个基线均有效；15m 主终态 3 个，其中正回报 1、非正 2、≥25% 0、later-context 1。执行证据为 Solana `unsupported_chain=2`、BSC `insufficient_liquidity=1`，没有 modeled-only。该非空结果来自部署后的真实 trigger 与后续报价，不是测试夹具或历史回填；只有 1 个独立日期，研究明确未成熟。

发布验证共收集 240 个 Python 测试并全量通过；`compileall`、Web/扩展 JavaScript 语法、前端详情请求状态与 `git diff --check` 通过。在线 doctor 为 0 warning / 0 error，完整 SQLite integrity=`ok`；8765/8787/8788 各一个 loopback listener，计划任务 `Running / IgnoreNew`，8788 与当前 Quick Tunnel 未鉴权均返回 401。模式保持 Paper，Live 为 locked/unavailable。

## 下一最小增量

### Trigger-anchored Solana Jupiter 覆盖与调度阻塞修复（2026-09-01 16:50）

`onchain-only-shadow-jupiter-quote/v1` 已从 activation shadow cohort `90` 严格前向注册。它不回填旧 trigger：baseline 精确锚定 `trigger_recorded_at`，15/60/240 分钟 target 精确锚定冻结目标；固定 `$35 USDC`、400 bps ExactIn，BUY minimum output 成为 SELL input，SELL minimum output 才用于报价下界回报。attempt 在 provider 调用前追加，三表不可更新/删除；无 wallet/taker/transaction/requestId，不签名、不广播，不写 Decision、Position 或 Trade。

首次真实部署暴露出运行阻隔：1.5 GB r6 上旧 universe due 查询约 21.49 秒，缺口终结约 44.80 秒；把它们放进 5 秒循环后阻塞事件循环，6 个注册后自然 baseline 在 143.7–146.6 秒才被处理并按预注册规则保留为 `queue_delay_expired`。没有删除、修改或伪装这些失败样本。新增两个精确 lookup 索引，并将缺口终结限制为每轮最多 12 条后，同一真实库实测 due=0.084 秒、gap=0.046 秒。trigger lane 现在每 5 秒检查小队列，旧 universe lane 继续随 30 秒 follow-up 运行；两者经同一互斥入口，避免同时访问 Jupiter。

受控恢复后，16:49 动态截面已有 eligible cohort 9、attempt 3、baseline valid quoted 3、旧阻塞导致的 baseline queue-expired 6、valid round-trip 0；三个新鲜 baseline 总延迟为 3.59–6.85 秒。8765 三次健康请求均为 200，约 0.002 秒；计划任务为 `Running / IgnoreNew`。这证明捕捉链路恢复，也证明交易稀疏曾包含真实调度阻隔；它仍不证明策略应放宽或已经可盈利。首批 15/60/240 分钟目标尚未全部到期，成熟门保持不变。

发布验证收集并全量通过 243 个 Python 测试；`compileall`、Web 与扩展 JavaScript 语法及 `git diff --check` 通过。在线 doctor 对活动 r6 的完整 SQLite integrity=`ok`，全部必需来源可达，0 warning / 0 error。实际浏览器验证中英文切换、动态 Overview、Paper/Live 锁和新增 Audit 面板；16:54 面板已自然增长到 eligible 12、attempt/valid baseline 6，页面明确显示 quote-only 下界、样本未成熟与 `affects=none`。

17:03 第一个有效 baseline 的 15 分钟目标自然到期并完成真实 Jupiter 只读 SELL 报价：target queue=1.75 秒、anchor→completed=2.75 秒，最低输出往返回报下界为 -83.18%。动态截面随即增长到 eligible 21、attempt 16、有效 baseline 15、有效双腿往返 1。该负结果完整留在分母，证明目标腿与最低输出链路工作；它仍是 quote-only 下界，不是成交、Paper 损益或真实亏损，也不能据单样本调整策略。

保持 Paper、Live lock 和 Strategy 门不变。P0-B quality 分层与 on-chain-first trigger-time 分母已经落地；下一步等待自然 Shadow 触发，并继续观察 Jupiter valid baseline→15/60/240 target。调度恢复和 raw/estimated 分母只解决覆盖与解释，不构成降低阈值或提高仓位的依据。成熟前不宣称交易稀疏合理、长期治愈、可成交利润或真实收益。

## Pump launch metadata 前向入口（2026-09-01 18:28）

用户连续提供的强势 Token 样本证明需要改善完整新币总体的早期调查入口，而不是逐个赢家事后解释。只读冻结在 `2026-09-01T10:14:58.981917Z`：从 exposure/cohort 重建的 PumpPortal create 分母为 141 Token；139 个有 launch-metadata attempt，132 个在冻结时成功解析，103 个找到链接，覆盖 73.05%。discovery→result 中位 0.966 秒、p95 6.247 秒、最大 9.072 秒。`tokens.source` 同时只剩 53 个 Pump family 值，是 Dex/Gecko 后续 upsert 的 last-writer 状态，不能替代 discovery exposure 分母。

Runtime 现用一个有界 metadata worker 获取 Pump create payload 中的公开 URI，复用现有 public-document SSRF、DNS、redirect、MIME 和 128 KiB 大小门。website、X、Instagram、Reddit、TikTok 保存为 identity seed；Telegram 继续 manual-only。所有项目自填链接固定 `decision_eligible=false`，不得推断名人背书。精确匹配已启用高影响账号的 social post 只成为 priority-2 `high_impact_account_metadata_lead`；它仍是未核验调查线索，只有本机精确收到的账号原帖才能升级为 priority 3。

首版自然审计发现 167 条 launch link 与原 Pump exposure 的关联为 0，Web funnel 会把 103 个 links-found cohort 错报为 0；`first_observed_at` 也被提前写成 HTTP 请求开始。修复后 metadata enqueue 发生在 exposure 持久化之后，worker 保存 fingerprint→exposure 关系，并以 HTTP 完成时点写入 link。稳定窗口已有 87 个 create exposure、86 次 attempt；52 个 `metadata_links_found` 全部关联 exposure（84 links），52/52 result transition 保存有效 `source_link_id`，link 可用时间与 result 相差 0–4ms。自然触发了 F1、Reuters、Binance 三个精确帖子 priority-2 lead；Reuters 与 Binance 两次 Agent 调查均为 no-context，Reuters 找到事件候选但独立正文核验仍为 0，因此没有生成 Event relation、Candidate 或 Paper。这证明新分支正在活动 r6 运行，也证明项目附带的知名账号 URL 不能自动成为背书或交易证据。

并行只读审计显示最多 2 个生产 Agent 并不是当前瓶颈：已派发任务的最大排队约 0.405 秒。冻结窗口中 metadata 与 on-chain Context Agent 均为 `no_context`，没有独立来源或 verifier 产出；冷却后的候选当前没有持久延期重试。故本轮不增加生产常驻 Agent、不放宽冷却、证据门、Candidate 或 Paper。下一研究问题是比较精确高影响帖子与普通 metadata 的独立来源 yield/cost，并为 cooldown 后的 exposure 建立不影响交易的前向分母，而不是让每个项目链接都调用更多 Agent。

## Token Context 冷却后延期观察（2026-09-01 19:12）

注册前只读诊断从 `2026-09-01T10:26:12Z` 起找到 93 个 `global_cooldown_active` admission：普通 metadata 90、on-chain 2、高影响 metadata 1。普通 metadata 中 78 个没有任何后来 attempt，只有 1 个后来准入。这证明“跳过后沉默”的观察分母真实存在；它不证明主动重试能发现信息或改善交易，因为同一窗口的 Context 自然结果仍为 `no_context`，独立来源与 verifier 产出为 0。

新增 `token-context-deferred-admission/v1`，在 `2026-09-01T10:59:16.847306Z` 以前向 activation admission/trigger=`4201/127750` 注册。该层只纳入注册后已经到达 admission 且因冷却或错误延期的尝试，冻结 admission、trigger transition、可选 source-link fingerprint/first-observed/link kind/provider/platform/verification。观察区间为 `[projected_next_eligible_at, deferred_at+180m)`；重复 skip 不会提前成为终态，只有同 Token、触发、来源链接、Event 与 Decision 的自然 later-admission，或严格到期，才追加 `later_admitted` / `expired_without_admission`。三类表均不可更新/删除，固定 `decision_eligible=0 / affects=none`。它不主动重试、不派发 Agent、不改 quota/cooldown，也不覆盖从未进入 admission 的上游 eligible trigger。

同时修复了一个真实时间顺序错误：旧 `search_token_context` 在 trigger resolution 之前捕获 admission `now`，可能形成 `trigger recorded_at > admission evaluated_at`。现在 disabled 分支保留请求开始时点，正常分支只在 trigger resolution 完成后获取 admission/quota 时点。测试覆盖精确 trigger/source lineage、早到 admission 排除、重复 skip 保持 pending、匹配自然准入、严格到期、不可变约束与无 Decision 副作用。

`2026-09-01T11:12:48Z` 的自然 r6 截面为 69 个延期 cohort：69 个 exact-trigger lineage、62 个 source-linked、69 pending、0 later-admitted、0 expired-without-admission。首批 180 分钟终态尚未成熟，因此不能宣称补回机会或证明应建立优先队列。生产 Agent 仍保持最多 2 个：实测最大排队约 0.405 秒，当前瓶颈是可验证上下文产出而非并发。用户已授权按需要增加常驻 Agent；只有后续出现持续排队或高价值触发的可解释漏过证据时才预注册并发/主动重试 challenger。

Web `/api/sources` 在当前大库上实测约 16.3 秒，原前端 9 秒上限导致实际页面长期停在“等待首次同步”。该页读取上限已局部调整为 30 秒；浏览器强制刷新后，中英文均显示“准入透明账本 / Admission transparency ledger”“延期观察 / deferred observation”和 `affects:none`，console 0 error。这个改动只恢复观测界面，不改变 Runtime、采集、Agent 或交易逻辑。

全量 Python 测试、`compileall`、Web JavaScript syntax、旧 schema migration smoke test 与在线 `doctor --online` 均通过；doctor 为 0 warning / 0 error、SQLite integrity=`ok`。受控重启后计划任务保持 `Running / IgnoreNew`，8765/8787/8788 均为单 listener，8788 未鉴权返回 401。Paper 保持运行，Live 继续 locked/unavailable；未 commit、未 push。

## P0-C：DexScreener 批次故障与共享退避（2026-09-01 19:39）

当前 24 小时逐 Token 账本在实现前显示约 73k–75k attempts 和 1,335 errors，但它的研究单位是“批次中的一个 Token”，不是独立 HTTP 请求。补充 request-round 分母后，`2026-09-01T11:38Z` 动态截面为 74,868 Token attempts、5,282 provider batch rounds、1,335 个受影响 Token、94 个失败批次，即每失败批次平均影响 14.202 个 Token。Web 现在同时显示两种分母，避免把一个 30-Token 批次错误误报成 30 次独立网络故障。

按时段复核：09:00–10:00Z 有 4,329 Token attempts、66 失败批次、962 个受影响 Token，其中 952 个 Token 行为 `PoolTimeout`，p95 约 48.08 秒；10:00Z 到延期账本注册前为 6,774 attempts、14 失败批次、197 个 `PoolTimeout` Token 行，p95 已降至约 5.86 秒。新 Runtime 部署前最后一簇从 11:14:38Z 到 11:15:20Z，11 个 `ConnectError` 批次影响 142 个 Token；首个成功批次在 11:15:23Z 开始。错误在 reverse probe、universe baseline、15/60/240m 与 on-chain follow-up 间连续出现，说明短时网络/主机故障被共用主机的多个 lane 依次立即撞击；它不是 142 次独立故障，也没有证据支持直接增加 timeout 或并发。

Runtime 现让 hydration、reverse-context probe、full-universe 与 on-chain shadow 的 Dex batch quote 共用一个请求 gate。出现 `httpx.TransportError` 后，下一 lane 在同一 gate 内执行 2–30 秒有限指数退避，并加入确定性 0–20% 抖动；供应商成功一次即清零。低优先级 reverse probe 在 gate 已忙或正在退避时直接让路，不写一个伪请求 attempt，保留既有候选等待下一自然周期。每 Token/role 的既有 120–900 秒有限重试仍保留。没有提高 12 秒 HTTP timeout、30-Token batch、连接并发或 Runtime/Agent 数量。

新 Paper Runtime PID `43688` 于 `2026-09-01T11:36:23.567670Z` 受控启动。到 `11:39:30Z` 的首个自然窗口已有 422 Token attempts、33 provider batches、262 success、151 no-pair、0 error、0 failed batch，覆盖 reverse probe、universe mixed/baseline/60m。该窗口只证明新 gate 没有阻塞正常采集，不证明长期消除了传输故障；需要等待下一独立故障簇比较 failed batches、deadline miss、受影响 Token 和恢复时间。

新增共享退避并发测试验证：第一个等待 lane 收到真实 `ConnectError`，第二个已经排队的 lane 必须等待共享抖动退避后才调用，成功后 streak/backoff 清零。全量 Python 测试最终 100% 通过；`compileall`、Web JavaScript syntax 与在线 doctor 通过，doctor 0 warning / 0 error、SQLite integrity=`ok`。实际浏览器中英文均显示“供应商失败批次 / 批次”“受影响 Token”，Sources 正常同步且 console 0 error。8765/8787/8788 各一个 listener，任务 `Running / IgnoreNew`，8788 未鉴权为 401，Paper/Live lock 不变；未 commit、未 push。

## P0-B：Trigger → Admission 上游覆盖（2026-09-01 20:00）

延期观察只覆盖已经到达 Admission 的尝试，无法回答 eligible Trigger 是否在更早的单轮候选选择中被丢弃。复用其前向注册点 admission/trigger=`4201/127750` 后，只读连接既有 immutable funnel transition 与 admission attempt，按 `(Token, trigger kind, source-link, Event, Decision)` 去重。部署前动态截面共有 880 次 eligible transition、823 个 episode、57 次重复；232 个 episode 进入过 Admission，591 个没有，其中 532 个已超过 5 分钟。分类型为普通 metadata `739/205`、on-chain momentum `65/16`、高影响 metadata `19/11`（episode / 进入 Admission）。这证明存在 Admission 之前的真实覆盖缺口，不能再把所有无 Agent 结果都归因于额度或冷却。

Store/API/Web 现显示 transition、episode、repeat、exact trigger lineage、Admission 覆盖率、超过 5 分钟未进入、admitted/skipped；固定 `forward_read_only_existing_ledgers / decision_eligible=false / affects=none`。eligible Trigger 只表示调查入口，不等于 Agent 已调用，更不是 Candidate、Paper 或交易信号。页面不回填注册前历史，也没有新建第二套事实表。

根因之一是 Dex hydration 把同轮所有直接语境触发放入内存后只执行一次 `max()`。Runtime 现在顺序处理最多 4 个 priority≥2 的高影响直接触发，并保留 1 个普通 metadata；同 Token 去重。每一次仍进入既有全局/单 Token 冷却、错误退避、有限调用/Token 配额和最多 2 个生产 Agent，因此增加的是可解释 Admission 尝试覆盖，不是无限模型派发。部署后浏览器自然截面从高影响 `19/11` 增长到 `21/13`，新增两个 episode 均进入 Admission；该短窗口只证明调度路径开始生效，不证明 Context yield、候选质量或交易结果改善。

仍未解决的断点保持可见：后续动态页已有普通 metadata 554 个、on-chain 49 个超过 5 分钟未进入 Admission；首批延期 cohort 的 180 分钟终态尚未到期。当前生产 Agent 最大历史排队约 0.405 秒，故没有增加常驻 Agent、调用预算、冷却频率或 Strategy/Paper 门。下一步应先等待 later-admitted/expired 和独立 verifier yield，再决定是否为 on-chain 或普通 metadata 预注册有界持久调度 challenger。

两个定向路径测试与 Web 后端测试通过，完整 Python suite 100% 通过；`compileall`、Web JavaScript syntax、`git diff --check` 与在线 doctor 通过，doctor 0 warning / 0 error、SQLite integrity=`ok`。新 Runtime PID `30316` 受控启动；8765/8787/8788 各一个 listener，计划任务 `Running / IgnoreNew`，8788 未鉴权返回 401。实际浏览器中英文均显示新上游覆盖表与“不是交易信号”边界。Paper 保持运行，Live 为 `false / locked`；未 commit、未 push。

## CHAIN-001：Solana 持有人宽度可用性 Shadow（2026-09-01 20:25）

交易次数不能冒充独立买家，最终持有人数也不能回填早期决策。对 3 个新 Pump Token 的只读公共 RPC 小样本探测显示，`getProgramAccounts` 可在约 0.62–1.22 秒返回经 mint 过滤的 Token 账户切片，聚合得到约 938–3,014 个正余额 owner、Top1/Top10 供应占比与低于 1bp 的尘埃比例；但其中一个 5 分钟成交量仅约 173 美元的 Token 仍有约 3,014 个 owner，且尘埃 owner 占比约 93%。这证明数据可得，也直接反证“owner 数=独立买家/社区热度/聪明钱”。`getTokenLargestAccounts` 同期触发 429，因此没有采用它作为生产依赖。

新增 `solana-holder-breadth-shadow/v1`：从 Store 注册点后，只对完整新币总体中的 Solana cohort 做 `SHA256(version+token_id)` 固定 2/1000 抽样；在 0/15/60/240 分钟每 5 分钟最多处理一个到期项。Runtime 通过官方公共 RPC 确认 mint program、读取总供应和带 mint filter 的账户切片，只在内存中按 owner 字节合并正余额；SQLite/API/Web 仅保存账户数、owner 数、余额覆盖、Top1/Top10、低于 1bp 数/比例、slot、延迟和响应字节，不保存任何 owner 地址。错误、429、不支持 program 与不可用结果均不可变保留，不回填注册前 Token。

该层是链上数据成本/污染研究，不是 holder alpha，更不是 unique buyer、new holder、cluster 或 smart-money 能力。固定 `decision_eligible=false / affects=none`，不写 Decision/Position/Trade，不扩大 Agent 数、不改变 Strategy/Paper/Live。Audit API 与中英文页面新增白名单汇总；当前先等待部署后的自然确定性抽样，比较响应错误率、尘埃/集中度稳定性和固定时点结果，再决定是否值得引入合规索引器与独立预注册研究。

完整 Python suite、`compileall`、Web JavaScript syntax 与 `git diff --check` 通过。受控部署在 `2026-09-01T12:33:11Z` 冻结 activation universe cohort `37499`；到 `12:39:57Z` 自然样本仍为 0 cohort / 0 result，如实显示 registered/waiting，不制造测试数据。浏览器中英文区块、地址不保存/决策不合格边界与 Live lock 均可见，console 0 error。在线 doctor 为 0 warning / 0 error、SQLite integrity=`ok`；计划任务 `Running / IgnoreNew`，Runtime 单进程树，8765/8787/8788 各一个 listener，8788 未鉴权为 401。Agent 仍最多 2 个；当日 Context 为 163/256 calls、约 11.63M/50M tokens，当前不是配额或生产并发阻塞。未 commit、未 push。

## TOKEN-008：链上动量 Context 有界准入挑战者（2026-09-01 20:55）

`Trigger → Admission` 的更新截面共有 1,545 个 episode：普通 metadata 1,401、高影响 metadata 24、on-chain momentum 120。on-chain 只有 27 个 episode 到达 Admission，0 个 admitted，93 个超过 5 分钟没有 attempt；同期 Token Context 配额仍有充足余量、生产 Agent 历史最大排队约 0.405 秒。因此这里是 hydration 内存候选选择造成的覆盖缺口，不是 Agent 并发或日预算阻塞。

新增 `token-context-onchain-admission-challenger/v1`。每个 Dex hydration 周期把所有纯链上合格触发按动量、再按快照时间排序，只选择 1 个进入原有 `_investigate_token_context`；priority≥2 高影响最多 4 个和普通 metadata 1 个的现有选择不变。同 Token 继续去重，所有调用继续经过原有全局/单 Token 冷却、错误退避、有限调用/Token 配额、结构校验和最多两个生产 Agent。选择路径与 challenger 版本写入现有 `context_admission` transition，新增不可变 registration 只冻结上线后的 admission/trigger ID，不回填旧 trigger。

Sources 中英界面只读显示注册后 on-chain trigger/Token、challenger 选择、admitted/skipped 和 Agent 终态；固定 `decision_eligible=false / affects=none`。该变更只回答提高受控调查覆盖后发生了什么，不把链上动量、Agent 调查或 `no_context` 解释为买入/卖出信号，不改变 Strategy、Paper、仓位或 Live 锁。后续以版本精确标记的自然样本比较独立来源/verifier yield 与成本；证据不足时保持 1/轮，不以用户额度充足为由增加生产并发。

受控部署在 `2026-09-01T13:06:24Z` 冻结 activation admission/trigger=`4809/153954`。到约 `13:10Z` 已自然出现 18 个注册后 on-chain trigger / 18 个 Token、2 次 challenger 选择；两次均真实到达 Admission 并因既有 `global_cooldown_active` 跳过，0 admitted、0 Agent result。该结果证明选择路径已运行，也如实证明当前尚未增加模型调用或有效语境，不能宣称召回改善。全套 Python 测试 100% 通过，`compileall`、Web JavaScript syntax、`git diff --check` 与 `doctor --online` 通过；doctor 0 warning / 0 error、SQLite integrity=`ok`。实际浏览器中英文均显示 challenger、非信号和 `affects:none`，console error=0。计划任务 `Running / IgnoreNew`，8765/8787/8788 各单 listener，8788 未鉴权返回 401；Paper/Live lock 不变，未 commit、未 push。

## TOKEN-008 v2：Hydration 调查通道公平排序（2026-09-01 21:32）

继续观察 v1 后，链上 challenger 连续 9 次被选择、9 次全部 `global_cooldown_active`，0 admitted。每个 4 分钟冷却窗口打开时，先到的是 `provider_metadata_unverified` 的高影响账号 metadata lead；部署前的精确账本显示，延期注册点后的普通 metadata 为 481 次 attempt / 37 admitted / 36 result、36/36 `no_context`，高影响 metadata 为 37/6/6、6/6 `no_context`，on-chain 为 35 次 attempt、0 admitted。完整 Token 漏斗同期为 29,747 cohort、3,264 个 eligible context trigger、216 个 context admission、214 个 Agent result、209 个 `no_context`、0 个 context-found，最终只有 6 个 decision-eligible Event relation、4 WAIT、2 REJECT、0 CANDIDATE、0 前向 Paper BUY attempt。交易稀疏的当前主断点是可决策证据与 Context 产出，不是 Paper 执行器故障。

Outcome-quality 只读覆盖进一步显示，`eligible_trigger_unadmitted/onchain_momentum` 有 123 个 no-decision attribution，其中 17 个在归因冻结时已有 confirmed-executable evidence、14 个固定执行回报≥25%；普通 metadata unadmitted 为 74 个且 confirmed-executable-known=0。该对比受事后 outcome 条件选择影响，只能支持“给 on-chain 一个前向公平样本值得验证”，不能支持买入、放宽证据门或策略收益声明。

因此新增 `token-context-onchain-admission-challenger/v2-fair-lanes`，在 `2026-09-01T13:29:32Z` 冻结 activation admission/trigger=`4926/161767`。v1 registration/attempt 全部保留。v2 仍每 hydration 周期最多选择一个最高动量 on-chain；浏览器精确原帖和新鲜高热事件关系永远第一，未核验高影响 metadata lead 与 on-chain 在两者同时存在时按持久 cursor 轮换先后，普通 metadata 最后。没有独立冷却、额外 Agent、并发、额度、频率或结构例外；`decision_eligible=false / affects=none`，Strategy/Paper/Live 不变。

定向测试覆盖精确原帖不降级、最高动量单选、on-chain-first 公平轮次、v2 immutable registration/API/UI；完整 Python suite、`compileall`、Web JavaScript syntax 和 `git diff --check` 通过。实际浏览器中英文均显示 v2 轮换边界、非信号和 `affects:none`，console error=0。

首个自然可用冷却窗口在 `13:34:39Z` 由 `onchain_first` lane 准入 BSC Token `0xdbc0…7777`，momentum=82.86；Admission→queued→dispatch 精确链完整，24.4 秒后由 Luna/low 返回 `no_context`，使用 61,801 tokens，0 独立来源、0 人物关联、0 项目 metadata seed、fact verifier 未运行。该结果证明公平排序确实解除“35 次 attempt / 0 admitted”的调度饥饿，也证明第一个样本没有语境产出；不把它称为机会、信号或收益。后续继续比较 yield 与成本，当前不增加 1/轮上限、不调整模型或冷却。

## Token Context 全局冷却并发修复（2026-09-01 22:05）

v2 上线后的自然数据证明全局 4 分钟冷却存在并发可见性缺陷：`CONTEXT_RUN_KEY` 虽保存的是调查开始时点，却只在 Agent 完成后写入。在 Agent 运行的约 20–60 秒内，第二个 hydration task 仍会读取旧值并通过冷却。活动 r6 已出现三组同窗双准入：`13:29:48/13:30:05Z`、`13:34:39/13:35:00Z`、`13:43:12/13:43:36Z`。这不是“更多独立 Agent 发现了更多信息”；这些结果仍全部 `no_context`，反而使有限日调用更快耗尽，并让 fair-lane 的“一次窗口选择”无法被干净评价。

Runtime 现在在 admission 与 quota 成功后、首次 `await` 之前预留全局冷却；Agent 真正失败时仅在预留值仍属于本次调用时释放，再由既有 error retry 接管。成功路径不再延迟写入相同起始值。定向测试覆盖顺序调用、真实 `asyncio.gather` 并发调用和失败后恢复；并发场景只允许一个 Agent 运行，另一个必须形成可审计 `global_cooldown_active` admission。

受控重启后的前两个自然 admission 为 `5088@14:01:50Z` 与 `5106@14:06:36Z`，起始间隔 286.65 秒，超过冻结的 4 分钟冷却。第一个窗口中后续 14 个 eligible trigger 均记录 `global_cooldown_active`；第二次准入后 30.06 秒到达的普通 metadata 也被明确跳过，没有再出现同窗双准入。两次 Agent 结果仍都是 `no_context`、0 verified domain，因此修复只证明调度/记账正确，不证明语境产出改善。仍需跨更多窗口验证长期稳定性。最多两个生产 Agent 的上限保留，供 Trend/Source/Context 等不同任务并行；它不再允许两个 Token Context 穿透同一个全局窗口。

当日 Token Context 当前为 189 calls / 13.58M known tokens。按修复后的每 4 分钟最多一次，理论日上限为 360；为避免本机有限 limiter 在仍有高质量触发时提前阻塞，`context_search_daily_limit` 从 256 有限提高到 384，50M Token 上限、单 Token 冷却、错误退避、触发门、结构校验和最多 2 个生产 Agent 均不变。Web 与 Runtime 重载后 `/api/agents` 已显示 `189/384` 和 `13.58M/50M`。

完整 Python suite 100% 通过，`compileall`、`git diff --check` 与在线 doctor 通过；doctor `ok=true`、0 warning / 0 error、SQLite integrity=`ok`。计划任务为 `Running / IgnoreNew`，8765/8787/8788 各一个 loopback listener，公网未鉴权返回 401。模式保持 Paper，Live 为 `false / locked / unavailable`；未 commit、未 push。

## 链上 Paper 探索与 X/KOL 真实闭环（2026-09-02 00:25）

`onchain-paper-exploration/v1` 在 `2026-09-01T15:01:50.457669Z` 以前向 Jupiter result `794` 注册，不回填旧报价。首个自然窗口产生 11 次 BUY、8 次 15 分钟 SELL、3 个开放仓；8 次平仓为 5 赢 3 输，合计已实现 `-$38.58931`，独立账户现金 `$856.38069`。所有入场与已退出报价请求均为 400 bps，主 Paper 仍为 0 开放仓、6 笔历史交易，Live 继续锁定。极小额退出曾把低于 `$0.01` 的回收额现金流截为 0；现已改为完整承担估算网络费并允许负净现金流，定向测试通过后部署。这里的结果是模拟报价下界，不是真实利润，也不用于放宽主策略。

X 扩展的真实采集曾把状态链接作者解析为空，形成 `source=x:x.com / priority=0 / entity_id=null`，使账号暴露无法关联到 Observation。状态链接解析已修复并有定向回归测试；部署后自然记录已正确形成 `x:elonmusk`、`x:cz_binance`、`x:binance` 等精确作者、实体与 exposure→observation→event 链。用户把扩展补采窗口设为 240 分钟，用于覆盖普通账号约一小时以上的轮换间隔；Runtime 仍按事件 `max_source_age_minutes=30` 把超过 30 分钟才首次看到的帖子降为 `identity`，因此补采旧帖不会伪装成新鲜 feature。Lookonchain 被提升为第四个 critical 账号，约每 4 分钟访问；Bubblemaps 与 ZachXBT 保持普通核验/风险来源。Lookonchain 已有 2 条精确原帖关联事件，但因首次观察时已超过 30 分钟而正确保持 context-only。

随后出现的 `x:elonmusk` observation `4935` → event `2892` → decision `2126` 不能作为有效 Event→Token 链证据。复核发现被匹配的 Robinhood Token 元数据是上游异常拼接结果（`name` 34,090 字符、`symbol` 9,575 字符），其巨大词表制造了虚假的 `match=80`。历史 observation/event/decision 保留为不可改写的误报证据；新版本在 DexScreener 解析入口拒绝超长元数据，并在候选匹配入口再次排除已存畸形 Token。该案例只证明 X 作者归因与事件入库已运行，不证明 Token 关联正确。

同轮修复了两个已观测效率断点。事件评估不再在阈值、冷却与 freshness 判断前用 attention top-8 截断，而是先按最新观测取有界候选池，只让真正进入 evaluator 的事件占用每轮 8 个名额；部署后新决策 `2139/2140` 均为无关联 Token 的真实 `WAIT`，未再被上述畸形 Token 污染。Token Context 最近 40 个 admitted 样本中，21 个无 metadata seed 的纯 onchain trigger 与 19 个仅 provider-attached X seed 的 trigger 共耗 2,982,217 tokens，但充分独立来源为 0；当前本机以前向开关将“无 seed onchain”和“无本地精确 browser observation 的未验证 X metadata”记录为 exposure-only，不扣 Agent quota。精确浏览器帖子、非 X 网站 seed 与新鲜事件关系仍正常进入 Agent 调查；该开关不改变 Strategy、证据门、Paper 或 Live。

到 `2026-09-01T16:34:28Z`，链上探索累计 12 个仓位、21 笔交易（12 BUY / 9 SELL），9 个已平仓、3 个开放，现金 `$853.545771`、已实现 `-$41.424229`；新增的 cohort `696` 以 15 分钟有效报价亏损 `-$2.834919` 后释放名额，随后 cohort `714` 自然开仓。cohort `630` 的 15 分钟报价错误后确实进入 60 分钟 fallback，但请求恰逢本轮部署切换，被不可变记录为 `interrupted_after_request/request_evidence_missing`，仍等待 240 分钟；成功 60/240 退出和 240 分钟 writeoff 尚无成熟样本。dust fee 修复已经由新 Runtime 加载，但部署后尚未再次自然出现 `gross < $0.01` 的退出，因此只由定向测试证明，不能声称已有运行态 dust 样本。Token Context 同期为 `221/384` calls、约 `15.94M/50M` known tokens，没有调用或 Token 限额阻塞；最近结果继续多为 `no_context`，当前不提高并发或预算。

## 信息确认到 Token 绑定分层与事件聚类修复（2026-09-02 02:40）

24 小时漏斗显示 Token Context 已有大量触发和调用，但绝大多数停在 `no_context`，最终没有 CANDIDATE；主断点是新鲜、独立、可决策的 Event→Token 证据，不是 Paper 执行器或 Agent 日预算。当前实现不再把所有 Token Context 结果永久锁为 identity：只有第二阶段 fact verifier 得到至少两个 `distinct_origin` 支持域、事实状态/置信度达标且来源发布时间不超过 30 分钟，才把新鲜来源提升为事件 confirmation。事件真实性与 Token 身份分开；只有至少两个新鲜独立来源正文实际包含同一 CA，且 Strategy 再次检查 role、proof、distinct-origin 数、正文地址和独立 evidence origin，才允许 `reverse_token_id`、exact quote 和 96 分路径。普通 confirmation、项目元数据、shared wire、转载、同名币和旧闻不能获得该捷径。8 项针对性测试通过；后续 ChatGPT 高智能协同统一使用 `@笔记本mcp20260902`，历史复核结论保留但不再沿用旧连接名。

部署后的第一个 Token Context 前向样本是 `Logomaxxing`，由 Solana 官方帖的 provider metadata lead 触发，结果为 `no_context`、0 独立来源；它证明新路径正常运行，但不证明 yield 改善。同期 3 个新 Decision 均为 WAIT、0 Trade，其中 cbHYPE/cbZEC 事件出现了新的真实污染：同一 `x:CoinbaseMarkets` 页面轮换采集时，把无关的“Brent crude oil 接近 95 美元”帖子聚进 Token 上线事件，虚增 attention/source 并污染 canonical 比较。根因是聚类 alias 把账号显示名当成事件语义。EventEngine 现在从新 Observation 的相似度词集合中剔除 author、source handle、持久实体和社交标题前缀；相同账号的 cbHYPE 合约续帖仍能合并，无关原油帖子必须新建事件。4 项聚类回归测试通过。历史事件与 Decision 不改写，修复只影响后续前向入库。

最终 Runtime 已受控加载，保持一个 Paper worker；SQLite WAL、数据库活动、浏览器桥与 8787 控制台正常，Live 仍为 `false / locked`。当前继续收集 confirmation、exact binding、WAIT/CANDIDATE、15/60/240 分钟随访和扣费 Paper 结果；不因短窗口仍为 0 Trade 就放宽 canonical margin、证据门或安全门。未 commit、未 push。

## 重点账号精确原帖优先队列（2026-09-02 03:15）

24 小时漏斗的主断点继续是 Token/热点到及时、独立、可决策证据的转化率，而不是新币数量。真实案例中，Token 元数据在本地发现时已附带 WhiteHouse 精确状态链接，但 107 个普通账号共享主页轮换使浏览器约一小时后才捕获正文。Web 现在从最近 15 分钟本地 `token_source_links.first_observed_at` 中挑出已启用重点账号的精确 `social_post`，最多 12 条；扩展用独立后台标签每 30 秒访问。浏览器正文进入 `high_impact_account_post` 调查种子，但请求与触发均保持 `decision_eligible=false`，不能替代两条新鲜独立来源、精确 CA 绑定、canonical margin 或安全门。

实现同时修复已验证的运行问题：已禁用账号不会进入队列；不再先截断前 100 条普通链接后才匹配重点账号；过期 15 分钟的请求即使 Web 同步失败也不会继续轮换；X/Twitter 和路径大小写按同一帖子去重。`token_source_links_social_first_idx` 已在 r6 创建，当前 115k 链接上的查询计划由全表扫描改为索引范围搜索，实测约 `165ms → 0.04ms`。

四项定向测试、失败项修正后的完整 Python suite、`compileall` 和 `doctor --online` 均通过；在线 doctor 0 warning / 0 error。Runtime 受控从 PID 21104 切换为 23596，计划任务仍为 `Running / IgnoreNew`；8787/8788 各一个监听，Web/SQLite 正常，Paper=true，Live=`false / locked`。Web 当前可见 5 条真实优先请求。Chrome 安全策略阻止自动打开 `chrome://extensions`，因此本地 unpacked 扩展仍需一次人工 Reload 才会加载新后台逻辑；在此之前不能声称 30 秒队列已产生真实 capture 样本。

ChatGPT 高智能协同已切换为 `@笔记本mcp20260902`。首轮 GPT-5.1、无回答和 `INVALID_REVIEW` 均按冻结规则作废；本截面当时只有 1 条 GPT-5.6 Sol + 新插件实读的运行/盈利漏斗 GO。后续两条合格独立复核已补足，3/3 共识否决原始 ticker→多 Dex 候选→事后涨幅方案，并把修订版收敛为 addressability-first 的 `kol-token-addressability-lag/v1`。该共识不支持提高 Agent 并发、扩大账号表、缩短冷却或降低证据门；下一步只观察 priority request→browser observation→Agent dispatch→独立 confirmation/exact binding→15/60/240 分钟 Paper 扣费结果。

首轮部署后的真实样本又暴露了一个更靠近主断点的生产/测试偏差。浏览器桥实际保存 `source=x:elonmusk`，并在 `raw_json.browser` 中记录平台、实体和原始页面；`recent_browser_observations()` 却只查询测试使用的 `source LIKE 'browser:%'`。因此 observation `5541` 已在 `19:14:36Z` 精确捕获 Elon 原帖，Token `8BjM…pump` 的 source link 也在 `19:18:27Z` 入库，但 `19:23:08Z` 仍被错误分类成 `high_impact_account_metadata_lead`，没有使用已存在正文。查询现已同时接受 legacy `browser:*` 与真实 `raw_json.browser` 对象，回归测试改用生产形态 `x:elonmusk` 并通过；完整 Python suite 再次 100% 通过，Runtime 受控切换为 PID 7192。修复后尚无新的自然 exact-post Token 样本，不能把旧样本回放成成功；继续等待真实前向触发。

紧接着的自然样本 `A6Ew…pump` 在 `19:31:17Z` 首次附带 Elon 精确链接，但 X Snowflake 证明原帖实际发布于 2019 年。旧路径仍以 `high_impact_account_metadata_lead` 调用一次 Luna、消耗 66,387 tokens 后才得到 `no_context`。当前新增无网络 Snowflake 时间解码：能可靠解码且早于现有 context lookback 的 X 帖子不会进入优先浏览器队列或名人 metadata Agent 准入；无法确认的链接保持原流程。Truth Social 同一帖子 `/@handle/posts/id` 与 `/@handle/id` 也已规范为一个队列项。4 项定向测试和 `git diff --check` 通过，Runtime/Web 再次受控加载；8787 当前旧 Elon 项已消失、Truth 重复项由 3 条降为 2 条。Paper、SQLite 正常，Live 继续锁定。Chrome 扩展仍需用户在扩展页执行一次 Reload，之后才能观察 priority request→browser observation 的首个自然前向闭环。

## 风险情报账号临时前向轮换（2026-09-02 03:55）

浏览器账号暴露的首批自然窗口显示：`@bubblemaps` 在 4 个完成窗口中捕获 3 条精确帖子、形成 3 个关联事件，其中 2 条当时仍为 decision-eligible；`@lookonchain` 同期 2 条精确帖子、0 个被接受事件。Bubblemaps 的 `$GWOOD` 风险帖首次看到时已经晚约 61 分钟，因此按既有 30 分钟因果门降为 identity；本地 Token 宇宙同时没有该 Robinhood Chain Token，所以没有进入 Candidate/Decision。这是“账号轮换延迟 + Token 未发现”的覆盖缺口，不是安全门拒绝。

为取得可比较的真实前向样本，当前仅做可撤销的观察位轮换：`@bubblemaps` 从 normal/priority 3 调为 critical/priority 4，`@lookonchain` 从 critical 调回 normal，四个 critical 总上限、Agent 数、Strategy、证据门、Paper 和 Live 锁均不变。该轮换不表示 Bubblemaps 已证明具有 alpha，也不把风险帖当作买入信号；后续按新鲜捕获率、被接受事件率、独立证据、Decision 和 15/60/240 分钟结果比较，样本不足时不永久固化。

## 账号轮换验收与下一实验门（2026-09-02 20:08）

后续真实暴露窗口已经取代上文“扩展仍需 Reload”的旧设备状态：`20260901T2000Z:x:@bubblemaps` 在 `20:06:02Z` 留下 priority 4 / critical / completed / accessible 心跳，证明现有扩展已同步并采用轮换配置；不需要再把人工 Reload 作为当前阻塞。该证据只证明配置传播和页面暴露，不证明帖子新鲜率、独立 confirmation、Decision 或收益已经改善。

当前候选的下一项实验是独立的 `kol-low-attention-dex-probe/v1`：只考察注册后、精确配置账号、priority≥4、feature/confirmation、来源新鲜且事件 attention 低于冻结阈值的观察；冻结当时最多两个查询词和 Dex 返回的完整候选集合，按 signal-available 时点随访 15/60/240 分钟。该层必须 append-only、`decision_eligible=0 / affects=none`，不得选择事后赢家，不写 Event→Token 决策关系、Decision、Position 或 Trade。它不能复用要求既有 Decision 的 `information-first-shadow`，也不能把 Dex 流动性最高池或同 ticker 搜索结果冒充 canonical Token。

这仍是设计候选，不是已部署功能。ChatGPT 高智能协同统一使用 `@笔记本mcp20260902`；本截面当时只有 1/3 条合格的最高强度独立复核，后续已补足为 3/3。复核后的结论仍是不修改生产 Strategy、Agent 并发、证据门或 Paper，而是先实现生产零污染、append-only 的 addressability-first shadow ledger。当前运行状态保持单 Paper Runtime、SQLite WAL、8787/8788 单监听和 Live 锁定。

## 当前 24 小时主漏斗复核（2026-09-02 04:14 CST）

活动 r6 在最近 24 小时记录 48,718 个新 Token、260,927 个快照、2,398 个 Observation 和 1,836 个 Event；Observation 角色为 feature 930、confirmation 5、identity 1,463。供给量足够大，不能再把交易稀疏简单归因于“没有发现新币”。同期有 11,666 个 eligible Context trigger，但只有 318 个 admitted（2.73%），312 个 Agent 终态中 `no_context=286`、`insufficient_reachable_sources=20`、`agent_error=5`、`cross_source_supported_context_only=1`；context-only 固定不可决策。

反向事件查询覆盖 2,706 个 Token，约 2,405 次为 zero-yield，identity-only found 283，真正 `reverse_news_matched=13`，约占被查 Token 的 0.48%。同期虽写入 reverse-news relation 949 条/236 Token 与 context relation 2 条/1 Token，但全部仍为 `decision_eligible=0 / affects=none`；可决策 relation Token 为 0。实体 Decision 为 WAIT 797、REJECT 344、CANDIDATE 1；主 Paper 仅形成 1 次 `$14.25` BUY。该唯一成交在决策前已有 Crypto Briefing 与 Coinbase Markets 两个来源，不能据单样本外推收益。

额度不是本窗口主断点：9 月 1 日 Token Context 为 280/384 calls、20,358,166/50,000,000 known tokens，token-budget block 和当日 call-limit block 均为 0。主要 admission 跳过是 `global_cooldown=2,119`、`no_eligible_trigger=1,280`，同时 reverse lookup 大量 zero-yield；这说明当前最值得优化的是触发质量、及时正文、独立 confirmation 与精确 Token 绑定的转化效率，而不是取消冷却、无限增加 Agent、放宽证据门或提高轮询数量。RSS、Mastodon 和 reverse-news 均有大量完成轮次但少量新增，进一步支持“新增与独立证据产出率”而非原始轮询量才是关键指标。

## 20:14Z 后自然增量与陈旧 Phantom 链接（2026-09-02 04:25 CST）

20:14Z 后主路径新增 5 个 Decision，全部为 `WAIT / no_matching_token`，没有新 CANDIDATE、主 Paper attempt 或 Trade；主 Paper 仍只有一个开放仓。隔离的 `onchain-paper-exploration/v1` 另有一笔 15 分钟 SELL，扣除 `$0.01` 网络费后该仓实现 `+$12.025430`，随后用 `$35 + $0.01` 新开一仓；探索账户累计已实现仍为 `-$23.583868`。两笔均为 400 bps Jupiter quote-only、`decision_eligible=0 / affects=none`，不能与主 Paper 合并或据单次盈利放宽门槛。

同一增量窗口首次发现 `@phantom` source link `120582`，但它不是已下发却漏采的 priority request。X status id 可确定性解码为 `2026-08-31T16:58:48.482Z`，本机直到 `2026-09-01T20:15:57.849Z` 才发现，已晚约 27 小时；Web 在构造 priority queue 前按 180 分钟 context lookback 正确过滤，当前 `/api/watchlist` 的 `priority_post_requests=[]`。该链接只停在 provider metadata hydration，`decision_eligible=0 / affects=none`，没有 Observation、Admission、Agent、Event 或 Decision。浏览器桥 X heartbeat 仍持续更新，且较早的 Phantom exposure 曾成功形成另一条 Observation→Event，因此本案应归为 stale metadata，而不是扩展、解析器或桥失联。当前无需补采旧帖或增加额外审计层，继续等待真正新鲜的精确 URL 自然进入队列。

## Context 触发产出与账号观察成熟度（2026-09-02 04:31 CST）

活动 24 小时 admission 分母为 2,789 个有 eligible trigger 的 attempt，其中 admitted 329（11.8%）。以 admitted 为结果分母：`no_context=289`、`insufficient_reachable_sources=20`、`agent_error=5`、`cross_source_supported_context_only=1`、verified/decision-eligible=0、尚未终结 14；已知 Agent 消耗 22,632,291 tokens。按严格有效产出=`context_only + verified`，只有 1 个，约 22.63M tokens/个；12 个 independent-domain 实例中 10 个只是单域 insufficient，不能升级成 verified。

分层显示：`high_impact_account_metadata_lead` admitted 57 次，消耗约 4.03M tokens，产生唯一一个 context-only；无 metadata 的 `onchain_momentum` admitted 173 次、约 11.50M tokens、0 有效；普通 `token_metadata_source_link` 94 次、约 6.96M tokens、0 有效。priority 2 共 61 次/约 4.10M tokens/1 有效，priority 1 共 267 次/约 18.46M/0 有效。该结果支持把有限 Agent 容量相对优先留给高影响 social-post lead，并验证现有 exposure-only 抑制是否真实阻止纯链上无 seed、普通 profile/site 和无本地精确正文的 X metadata 继续消耗 Agent；它不证明该唯一 context-only 有交易价值，也不能据此放宽 Decision 门。

浏览器注册后观察只有一个运行日：348 个 exposure，342 completed，157 个唯一精确帖，68 个新鲜 feature、0 confirmation；X 为 314 completed/157 hits/68 fresh feature，Bluesky 28/0/0，Truth 6 次均 login-required。所有具体账号都没有达到至少 20 completed、10 个独立日期等成熟门；精确来源冻结 Paper 平仓归因为 0。因此不能发布账号排名、宣布某账号具有 alpha，或仅凭 critical/normal 命中率差异永久调整 cadence。当前账号轮换只提供覆盖描述，真正评价仍需 confirmation、exact binding 和跨日期扣费 Paper 结果。

## 普通 X metadata Agent 成本泄漏修复（2026-09-02 04:38 CST）

exposure-only 开关首次生效后的账本证明：154 次纯链上无 seed 与 108 次命中 `exposure_only_unverified_provider_metadata_x` 的触发均为 0 dispatch/0 assessment；但另有 20 个普通 `token_metadata_source_link`、X/provider-metadata/social-post、无本地 observation 的触发仍进入 Agent，19 个已终结为 18 `no_context` + 1 insufficient，消耗 1,447,276 tokens。根因是准入条件错误地用 `link_kind != social_post` 排除了普通帖子，导致冻结规则只抑制 profile，而没有抑制最主要的未验证帖子。

最小修复移除该错误子条件：generic `token_metadata_source_link + platform=x + provider_metadata + observation_id为空` 现在统一 exposure-only；`high_impact_account_metadata_lead` 因 trigger kind 不同继续允许调查，本地精确 `high_impact_account_post` 与非 X website 也不受影响。旧测试中“generic X post 仍调用 Agent”的断言同步改为 exposure-only，避免与新规则并存。4 项定向测试通过。

Runtime 受控从 PID 19028 切换到单一 PID 22704。严格以 `2026-09-01T20:37:07.480Z` 为部署边界的首批 10 个自然 admission 中：1 个普通 generic X provider post 正确记录 `exposure_only_unverified_provider_metadata_x`，0 dispatch/assessment；1 个 high-impact metadata lead 仍正常 admitted/dispatch/assessment，结果为 `no_context`、108,533 tokens；exact high-impact post 暂无自然样本。8765/8787/8788 各一个监听，SQLite WAL 持续写入，Paper=true，Live=false/locked。该修复减少已证明的低产出 Agent 消耗，但不改变证据门、Strategy、Paper 或生产并发。

## 新鲜 priority 帖子与 Chrome 加载状态（2026-09-02 04:42 CST）

20:40Z 首次出现三条同时满足服务端门的真实 priority request：WhiteHouse `121757`（帖子发布 `20:33:48Z`）、OpenAI `121439`（`20:30:17Z`）和 Solana `121514`（`17:45:10Z`），API 连续下发且固定 `decision_eligible=false / affects=browser_observation_priority_only`。等待超过两个 30 秒轮换周期后，WhiteHouse 与 OpenAI 精确 status Observation 仍为 0；Solana 只有此前 `18:50:45Z` 从账号页捕获的旧 identity Observation，没有当前 priority 导航的新记录。三条均无 confirmation、exact Token relation 或 Decision。

Chrome 只读标签清单同时只显示扩展已有的 normal/critical 两个轮换标签，没有新 background priority 专用标签。这比账号主页 heartbeat 更直接地说明：当前 Chrome 实例尚未运行包含 priority-post alarm/tab 的最新 background 逻辑，或该逻辑未成功初始化；不能把 API 下发当成浏览器已访问。浏览器安全策略禁止 Codex 打开 `chrome://extensions`，也禁止通过 CDP/替代浏览器绕过，因此不能由当前自动化会话完成扩展 Reload。当前唯一必要设备步骤是用户在普通 Chrome 的扩展管理页对已解压的 memeTrader 扩展执行一次 Reload；之后验证第三个 priority 标签、exact status Observation 和 source-link lineage。不得用手工打开三个帖子代替该验收。

部署后首个 high-impact metadata case 也说明时效损失：Solana 帖子在 metadata 首次发现时仅 13 分钟，但第一次 admission 被 global cooldown 跳过，直到约 2 小时 38 分后 deferred retry 才进入 Agent，接近 180 分钟时限末端；Agent 最终 `no_context`、0 独立来源。当前先恢复精确 priority 正文入口并观察 generic X 抑制后的自然调度，不立即放宽全局冷却或新增 Agent。

## 优先原帖重载后的自然闭环验收（2026-09-02 12:18 CST）

用户在普通 Chrome 重载 unpacked 扩展后，Chrome 只读标签清单已出现独立的精确帖子标签，除 normal/critical 两个账号轮换标签外，当前还停留在 Solana 精确状态页 `2094999141881610259`。这关闭了“最新 background priority 逻辑未加载”的设备阻塞；不再要求重复 Reload。

该帖子形成了不可回填的自然链路：两个 PumpPortal Token source link `149464/149492` 分别于 `04:01:48Z/04:02:33Z` 首次出现；精确浏览器 Observation `6373` 于 `04:02:35Z` 入库，较最早 source link 约 47 秒，角色为新鲜 `feature`；随后创建 Event `4614`（attention 22），两个 Token 均以 `high_impact_account_post` 进入 Admission `8401/8412`。这证明 priority request → 精确正文 → Event → Token Context 的调度主链已真实运行，而不是仅有 API 下发或账号页心跳。

两个 Agent 调查仍分别终结为 `no_context`（41,088 与 74,278 tokens）：独立来源数为 0，其中一个项目网站 CA 与目标 Token 地址不一致；两者均没有 Decision、Position 或 Trade。该结果是正确的失败关闭，不能据此降低独立证据、精确 CA、canonical margin 或安全门。主断点因此从“优先正文入口未加载”收敛为“新鲜正文后的独立 confirmation 与精确 Token 绑定产出率仍低”。

ChatGPT 高智能协同本轮又启动两条独立、`极高` 推理会话，并均实际调用 `@笔记本mcp20260902` 读取统一 handoff 和本地代码：一条负责因果/统计污染与成熟门，一条负责交易经济性/最小工程路径。旧 GPT-5.1/`INVALID_REVIEW` 结果继续作废；在两条新结论完成并由 Codex 核验前，不实施 `kol-low-attention-dex-probe/v1`，也不改变生产 Strategy、Agent 并发、证据门、Paper 或 Live 锁。

两条新复核均以 GPT-5.6 Sol、`极高` 推理完成，并实际使用 `@笔记本mcp20260902` 只读核对代码、测试与 r6 SQLite；至此三路合格复核完成。共识是：原始“ticker/alias → Dex 候选 → 15/60/240 事后涨幅”按原样 `NO-GO`，因为 ticker/同名歧义、provider 排序、换池、下游筛选和成本会制造漂亮但不可交易的结果；修订为 addressability-first 后 `MODIFIED_GO`。下一实验固定为 `kol-token-addressability-lag/v1`，先记录 `T_signal / T_explicit_identifier / T_dex_pair_available / T_route_available / T_independent_confirmation`，主分母必须含 no-seed、zero、error、late、ambiguity 和 missing，探针数据不得写共享 Token/Snapshot 或任何生产决策状态。只有先证明信号时点能够及时得到唯一 CA、同 pair/surface 和真实路由，才启动成本后 15/60/240 随访；这直接针对当前“Token 供给很多但及时独立可决策绑定极低”的主断点。

## Addressability-first 第一阶段部署（2026-09-02 13:02 CST）

已实现并部署 `kol-token-addressability-lag/v1` 的第一阶段独立账本。Store 新增不可更新、不可删除的 registration、admission attempt、cohort 和 milestone 表；Runtime 只对注册边界之后、精确配置的重点账号、本地 live receive、feature/confirmation 且来源时间合格的浏览器 Observation 尝试入组。无 explicit CA 的合格 episode 仍以 `no_seed_at_signal` 留在主分母；有 CA 时只冻结信号时点已知的 exact Solana/EVM 地址，不使用 ticker、alias 或事后搜索。当前里程碑仅记录 signal、explicit identifier、已在本地发现的 Token 和同一快照中可证明的 Dex pair surface；所有记录固定 `decision_eligible=0 / affects=none`，没有共享 Token/Snapshot、Decision、Position 或 Trade 写入。

两个针对性测试通过：一项验证 exact CA、local Token、Dex pair、no-seed、幂等、不可变和生产零污染；一项验证精确重点账号浏览器 Observation 会自然创建 no-seed cohort。`git diff --check` 无内容错误，仅存在既有 Windows 换行提示。Runtime 由旧叶进程 PID `22704` 受控切换为新叶进程 PID `21328`，计划任务仍为 `Running / IgnoreNew`；8765、8787、8788 各一个监听，Web health 为 SQLite WAL 可读、近期数据库活动、Bridge reachable、Paper=true、Live=false/locked。

实验注册时间为 `2026-09-02T05:00:43.672750Z`，activation Observation 为 `6435`，部署截面最新 Observation 也为 `6435`，因此没有回填旧事件。此刻 admission/cohort/milestone 均为 0，是正确的前向起点，不是空结果失败。尚未实现 `T_route_available`、`T_independent_confirmation`、网络 zero/error/late/ambiguity 终态或 same-pair 成本后 15/60/240 随访；在这些阶段及自然样本成熟前，不改变 Strategy、Agent 并发、Paper 仓位或证据门。

## Frozen-definition v2 与 route/confirmation 部署（2026-09-02 13:27 CST）

ChatGPT active implementation reviewer 通过项目文件提出五项语义审查。Codex 逐项以当前代码和 live r6 核验：P0-A frozen registration 未成为运行权威、P0-B attention `35` 被错误纳入、P0-C `tokens.first_seen_at` 可能是 provider/caller 时间、P0-D 裸 EVM CA 跨链歧义均为真实缺陷；P0-E 的完整 estimand 仍未完成也属实。修正边界查询时 v1 仍为 0/0/0，但在受控部署前自然到达 Observation `6441`，旧 Runtime 创建了唯一 v1 cohort `1`：Elon priority 4、attention 22、`no_seed_at_signal`。该 cohort 与两个里程碑原样保留，不改写、不回填，也被 route registration 的 activation cohort `1` 排除。

新版本 `kol-token-addressability-lag/v2-frozen-definition` 注册于 `2026-09-02T05:21:07.545226Z`，activation Observation `6445`。Runtime 现在解析不可变 registration JSON 并 fail closed，cohort 保存 definition hash；attention 以冻结的严格 `<35` 执行；本地 Token 可见时间只来自满足 `observed_at <= ingested_at <= recorded_at` 的不可变 snapshot `recorded_at`；裸 `0x...` 若无唯一 contemporaneous chain hint，保留为 `ambiguous_evm_chain_at_signal` 且不得形成 unique route success。

`kol-token-addressability-route/v1` 同时注册，activation cohort `1`。每个 tick 最多处理一个独立 Jupiter quote-only work unit，先写 attempt 再请求；只发固定 35 USDC、4% slippage 的只读 quote，不请求 transaction。route 结果保留 same-surface exact/unmapped、no-route、protocol invalid、error、interrupted、no-seed、unsupported/ambiguous chain、pair missing 和 queue expiry。独立 confirmation 只认同一 Event 中 durable local availability、异于触发来源、feature/confirmation 且正文含 exact CA 的 Observation，并区分 timely、late 与 missing；同账号重复不算独立确认。所有表 append-only，固定 `decision_eligible=0 / affects=none`。

12 项定向测试与语法检查通过，覆盖 frozen definition/reopen/malformed fail-closed、34.999/35/>35 边界、durable clock、EVM ambiguity、exact confirmation 与同源排除、same-surface route、late/missing、Jupiter no-route/protocol/error、Web 安全聚合及共享 Token/Snapshot/Event relation/Decision/Position/Trade 零污染。受控部署后 Paper Runtime 叶进程 PID `7296`，8765/8787/8788 各一个监听；两个计划任务均 `IgnoreNew`，Web health 显示 Paper=true、SQLite ok、Bridge reachable、Live locked。v2 当前为 0 自然 cohort；系统继续前向等待，不用旧赢家或 v1 cohort填充。

完整目标仍未完成：no-seed episode 后续自然 exact identifier lineage、route/confirmation 的自然样本分布、same-pair/surface 成本后 15/60/240 随访和成熟门仍待前向推进。`/api/audit` 全量聚合在当前约 3.5GB 数据库上一次请求超过 15 秒并会阻塞单线程 Web；本轮没有借此扩大 UI 范围，已重启 Web 恢复健康，新增 addressability 聚合已由空库 Web 测试验证。该性能问题保留为 Web 后置优化，不改变收益主线。

## Immutable-attention v3 与 route v1 停用（2026-09-02 13:48 CST）

独立因果复核与本地代码/SQLite 交叉核验确认：v2 仍从可变 `events.attention` 读取触发值，因此不能作为严格前向定义。v2 在 0 admission、0 cohort、0 milestone 时明确标记为 `registered_abandoned_before_first_cohort`；旧 registration 与零分母原样保留，不删除、不修改、不回填。新基线 `kol-token-addressability-lag/v3-immutable-attention` 注册于 `2026-09-02T05:44:58.386225Z`，activation Observation `6499`，definition hash `f90c852666b1cf7d3d29df0b89474d0d346dad432e704c3ca746cf124917b3b4`。它只按 exact `(event_id, observation_id)` 连接 `event_attention_points`，冻结 attention point id、definition version、recorded time、coverage mode 与数值；missing/ineligible point fail closed，信号时钟取 observed/ingested/attention-recorded 的最大值。修改 Event 当前 attention 不再改变已冻结 cohort。

`kol-token-addressability-route/v1` 没有声明与 v3 兼容的 base version/hash，故被判定 `incompatible / registered_abandoned_before_first_attempt`；live r6 在停用边界仍为 0 attempt、0 result、0 confirmation。它的 registration 继续保留，但 Runtime 不再派发。Web 当前统计严格按 base/route version 过滤，并分别显示 v1 legacy、v2 abandoned、v3 current 与 route v1 incompatible，避免把旧 v1 cohort误报成 v3 自然样本。

定向验证实际结果：addressability 相关测试 `9 passed`，Web 空库与多版本隔离测试 `2 passed`，语法检查通过。受控部署后计划任务仍为单一 Paper Runtime 与单一 Web 服务；8765/8787/8788 均只绑定 loopback，SQLite/Web/Bridge 健康，Paper=true，Live=false/locked。当前 v3 为 `registered_waiting`，尚无自然 cohort；下一步是等待真实前向分母，并在交易经济性独立复核后另立兼容 v3 的 route 新版本，不因空结果放宽证据、风控或交易门。

## Compatible route v2 部署（2026-09-02 14:25 CST）

交易经济性独立复核确认 route v1 应在首个 attempt 前封存，并把 corrected route 的 P0 门限定为版本/hash、deadline、pair/queue/response attribution、surface、canonical exact CA/lineage、有界 refresh 与 Jupiter 公平调度。Codex 完成实现后，`kol-token-addressability-route/v2-compatible-deadline-edf` 于 `2026-09-02T06:23:40.203809Z` 严格前向注册，activation cohort `1`，definition hash `79ca058b7ae38bfccfbf260e19a5a5b315c3538ebb0127d59ff1b91d349d2c42`；其 compatible base version/hash 精确绑定 live `kol-token-addressability-lag/v3-immutable-attention` 与 `f90c852666b1cf7d3d29df0b89474d0d346dad432e704c3ca746cf124917b3b4`。route v1 registration 与 0/0/0 分母保持原样，Web 显示 `registered_abandoned_before_first_attempt`。

route v2 的 attempt 冻结 deadline 与 compatible base lineage；结果分开保存 pair、request、response timing，并明确 `dex_pair_late`、`queue_delay_expired`、`route_response_late` 和 clock invalid。surface 分为 `single_hop_exact`、`multi_hop_includes_frozen_pair` 与 `unmapped`；只有 canonical 32-byte Solana Pubkey、exact `solana:<CA>` Token、同 cohort milestone 与一致 request/result lineage 才可落账。refresh 使用 unresolved predicate 与每层硬上限，25-cohort 测试证明第 13/25 个不会被已完成头部永久饿死。KOL 不再拥有独立 provider periodic；universe/on-chain/KOL 共用每个 5 秒 epoch 最多 3 个后台请求，每个请求后释放 `_jupiter_quote_lock`，测试证明等待中的生产 route 可在两个后台请求之间取得锁。

定向测试 `11 passed`；完整 pytest 全绿，`compileall` 通过，online doctor 的 SQLite、Bridge、DexScreener、GeckoTerminal、Jupiter、GoPlus、Honeypot、RugCheck 和全部 required RSS 均通过且无 warning。受控部署后的 Runtime 叶进程为 PID `22776`；8765/8787/8788 各一条 loopback listener，Web health `ok=true / paper=true / live.locked=true`。部署截面 max Observation `6545`，base v3 和 route v2 仍均为 0 自然样本，正确状态是 `registered_waiting`；任何 route 结果仍固定 `decision_eligible=0 / affects=none`，不证明 sellability、净收益、fill、人物背书或安全。

## v3 零样本断点与浏览器采集恢复（2026-09-02 14:42 CST）

v3 激活边界 Observation `6499` 之后，活动库先新增 64 条 Observation，但全部为 `availability_proof=local_poll`，`local_receive=0`；与此同时 `browser_watch_account_exposures.last_heartbeat_at` 停在 `2026-09-02T05:16:54.615947Z`，早于 v3 注册时间 `05:44:58Z`。因此 v3 的 0 cohort 不是 attention、priority、Agent budget、Strategy 或 Paper 门槛造成，而是 Chrome 浏览器进程已退出：8765 Bridge 服务仍可响应 `/health`，导致控制台只能证明桥端口存活，不能证明扩展采集器正在运行。

复用已安装且启用的本机 Chrome 扩展启动 Chrome 后，原有 normal/critical 账号标签自动恢复轮换；登录态页面可正常读取，X 账号从 Elon/Uniswap 继续轮换到 CZ/Vitalik、Trump/Brian Armstrong。不可回填的数据库心跳自然推进到 `2026-09-02T06:42:26.866871Z`，新增 6 个 `20260902T0630Z` exposure，均为 accessible，证明扩展、watchlist 同步、轮换、bridge token 与入库链路已恢复。当前 `local_receive` 仍为 0，是因为轮到的页面没有满足现有 240 分钟正文窗口的新帖子；这属于尚无符合条件的自然前向输入，不应通过放宽 freshness、attention、evidence 或交易门制造样本。

本轮未修改代码、配置、策略或数据库历史记录；只恢复必要的 Chrome 常驻采集进程并记录真实断点。Paper Runtime、SQLite、8787/8788 与 Bridge 保持运行，Live 继续锁定。后续观察条件是 v3 注册后首次新鲜重点账号正文是否形成 `local_receive -> immutable attention -> cohort -> route/confirmation`，而不是仅看桥服务健康。

## Bridge 服务与浏览器采集健康拆分（2026-09-02 14:55 CST）

上一断点证明 `/health` 的旧语义会把“8765 Bridge 服务可达”误显示为“浏览器桥健康”，即使 Chrome 已退出且重点账号采集停止。Web 现在直接读取前向 `browser_platform_heartbeat:*` 本地状态，独立返回 `reachable` 与 `collector_status/collector_active/latest_collector_heartbeat_at`；采集心跳超过 `source_stale_minutes.browser`（当前 3 分钟）时整体浏览器卡片显示 `stale`，尚无心跳时显示 `not_observed`，只有服务可达且采集心跳新鲜才显示 `healthy`。

两项针对性测试覆盖“服务可达但尚无采集心跳”“服务可达且采集 active”“服务可达但采集 stale”；完整 pytest 全绿，`compileall` 通过，online doctor 的 SQLite、Bridge、DexScreener、GeckoTerminal、Jupiter、安全提供方和 required RSS 全部通过且无 warning。仅 Web 服务受控重启：8787 PID `200`、8788 PID `19580`；Paper Runtime/Bridge 保持 PID `22776`，Live 锁定。部署后 `/api/health` 返回 `bridge_service_reachable=true`、`collector_status=active`、最新采集心跳 `2026-09-02T06:53:26Z`、年龄约 0.89 分钟。当前 max Observation `6581`，v3 `local_receive/cohort` 与 route attempt/result 仍均为 0；这继续表示尚无满足窗口的自然重点账号新帖，不是系统再度失联。

## 首个 v3 候选输入暴露 repost 归因缺口（2026-09-02 15:00 CST）

恢复采集后出现首个真正新鲜的 priority 输入：X status `2095041695276765588` 的 Snowflake 时间为 `2026-09-02T06:50:38.803Z`，本机从 `06:54:29Z` 起发现 7 个不同 Solana Token 同时引用该 exact URL，Web 正确合并为一个 `@elonmusk` priority request，Chrome 专用标签也已自动导航到该精确页面。这证明 Token metadata→priority request→专用标签链路正常。

页面事实不是 Elon 原创帖：X 显示“Elon Musk reposted”，正文作者和原始 permalink 是 `@tunguz/status/2094973671970603195`，原帖时间早于 repost 约 4.5 小时。扩展 v0.6.5 以 article 原作者、原 permalink 和原帖 `<time>` 作为唯一事实，因此在用户配置的 240 分钟窗口外直接跳过，既没有 Observation，也丢失了“高影响账号刚刚 repost”这一独立动作。把原文直接标为 Elon 作者同样是错误的。

源码已升级为扩展 v0.6.6：只在 exact X status 页面属于已配置账号、但 rendered article permalink 属于另一作者时，保存 `actor=配置账号 / source_action=repost / content_author / content_url` 双重 lineage；信号时间用 actor status Snowflake，原文仍保留原作者。该 Observation 只是高影响账号注意力动作，不自动等于 endorsement、独立 confirmation、Token 身份、安全或交易资格。`node --check` 与 Bridge/Runtime 针对性测试全绿；新增的真实 content-script 行为测试用动态 Snowflake 构造“5 小时旧原帖 + 10 秒新 repost”，实际执行扩展脚本并验证 source/author/entity 为 actor、content_author/content_url 为原文、published_at 为 repost 时间且 priority=2，结果 `1 passed`。当前 Chrome 仍加载旧扩展实例，因此生产 Observation/v3 cohort 仍为 0；必须在普通 Chrome 的扩展管理页对 unpacked `E:\memeTrader\browser-extension` 执行一次 Reload 后，才能以前向数据验收，不能回填这次样本。

扩展版本可观测性随后补齐：v0.6.6 content heartbeat 发送 `chrome.runtime.getManifest().version`，background 与 Runtime 仅接受点分数字版本，Web `/api/health` 返回 `collector_extension_version`；畸形版本被清空且不会进入 SQLite。4 项针对性检查、完整 pytest、`compileall` 与 online doctor 全部通过，doctor 0 warning。Runtime 受控切换为 PID `6872`，Web 8787/8788 为 PID `25704/25868`；Paper、SQLite、Bridge 正常，Live 锁定。部署后浏览器心跳继续推进到 `2026-09-02T07:20:42Z`，但 `collector_extension_version=null`，这是当前 Chrome 仍运行旧 content script 的直接证据，而不是基于是否捕获帖子进行猜测。重载 v0.6.6 后首个新心跳应明确显示 `0.6.6`，否则验收失败。
