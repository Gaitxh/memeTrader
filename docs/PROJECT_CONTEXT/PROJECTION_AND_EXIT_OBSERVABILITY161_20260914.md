# 迭代161：准入后的真实去向与原生报价故障分类

## 范围与结论

2026-09-14 北京时间；证据截止以各工件 UTC 时钟为准。继承151–160，不重做策略注册，不改变资金期、参数、集中度上限或既有退出规则。两个 Terra/low 子 Agent 分别审查信号链路和退出复盘，根 Agent 实现原生报价修复、合并报告、交叉审查、测试与唯一生产部署。

1. **准入不是开仓。** PID45576 启动后 cohort15648 是一个 Robinhood Token、一个原池、一次机会，50 个臂级准入只产生两个独立 Paper 账户仓位；原池集中度保护有意限制风险，不是50个独立机会丢失。既有保护只增加进程计数，不写逐臂终局回执，造成链路可观测性缺口。历史漏记不能事后改写为已证实拒绝。
2. **原生曲线失败回包丢身份。** `bonding_curve_quotes` 请求异常分支少了成功分支中的 `pool_address`，`apply_curve_quote` 因而抛出 `native_exit_identity_amount`，掩盖 RPC 失败。已离线完整复现并修复该路径；不能断言历史同名五次错误全部来自此路径。
3. **组合退出部署不等于差异机制生效。** `alpha149_confirmed_recovery_decay_v1` 当前资金期42个自然终局、42个Token，16次本金未回收复审退出、14次波动率保护、10次流动性核销、1次时间退出、1次硬止损。持久化 SELL intent 中双确认衰减回本触发为0。没有证据据此宣称新机制改善收益，亦不能仅凭退出理由断言它必定被早期路径抢先触发。

## 实施、边界与测试

| 变更 | 实际处理 | 验证与不变项 |
|---|---|---|
| Store逐臂回执 | 新增 `chain_meme_trader_entry_gate_refusals` 空表、索引、不可变触发器；记录资金期/cohort/臂/决策/源成交/Token/原池/原因/尝试与落库时间 | 与仓位投影同一事务；无额外提交、无旧表重建、无历史回填；重复处理已入账臂不误记为拒绝；3项新回归覆盖幂等、原子回滚与不可变性 |
| 原生RPC失败 | 回包保留准确曲线池地址；校验身份后，零slot失败明确返回 `UNKNOWN_RPC`，不冒充成功但无新状态 | 429、ReadTimeout、无效RPC bundle三种故障完整测试；身份/数量错误仍拒绝；仓位、原始数量、现金流和native状态不变 |
| 逐策略投影复盘 | 新增 `scripts/projection_review161.py`，接入现有 `deep_cycle_report.py` 两小时/日报 | 有界2000cohort、同机会非独立臂分区；已入账/现金拒绝/集中度拒绝/未知分别报告；截止时间之后证据不使用；3项新测试 |
| 退出复盘 | `scripts/review_metrics151.py` 增加最新至多200个组合退出臂样本、分资金期计数、终局理由、真实SELL触发与单确认检查点 | 不把checkpoint当触发，不按字符串猜测确认；无样本与未知模式单列；对照比较仍待验证；3项新测试 |

根 Agent 的交叉审查修复了：重复投影误记已开仓臂、截断样本第201条泄漏进触发数、非结构化JSON确认误读和不经济的逐cohort仓位查询。新报告原查询在生产3秒只读预算被中断；改用既有 `(definition_version,token_id,...)` 索引并按Token复用后，168cohort读取 **78ms**。这只是报告查询效率，不是交易时延或收益改善。

实际测试：`test_native_rpc161.py` 4通过（含RPC失败不报成功心跳）；`test_pool_receipts161.py` 3通过；`test_projection_review161.py` 3通过；`test_exit_review161.py` 3通过。关联 `test_native_execution138.py + test_pump_native.py + test_pool_receipts161.py + test_projection_review161.py` 共71项通过；`test_exit_review161.py + test_post_exit151.py + test_forward_review151.py` 共20项通过。末次原生RPC/held/reference筛选回归7项通过。第一次原生测试因共用pytest临时目录的Windows文件占用而未执行；已改为项目内独立 `data/tmp/pytest-*-161-*` 目录，未删除其他Agent测试数据。

## 部署与前向核验

- 原Paper PID45576由根Agent在 `2026-09-13T23:11:52.403990Z` 停止，保留既有PowerShell监督器43000；没有重复启动第二个写入器。
- 新Paper **PID46272** 启动回执 `2026-09-13T23:12:04.070842Z`，`store.py/native_execution.py/collectors.py` 三个加载哈希全部与磁盘相符，新回执表已安装。Live=false、Paper、原资金期保留。
- `data/research/iteration161/integration161_20260913T231111Z.{json,md}`：完整当前/归档只读报告真实生成；两个新增诊断段已进入现有报告入口。报告代码由现有周期子进程按磁盘加载，不增加模型调用、行情HTTP批次或调参任务。
- `before_restart.json`、`after_restart.json`、`forward_observation.json` 位于同一工件目录。23:12:55Z心跳年龄0.382秒，主采集/退出循环推进；当前1个Solana仓位、原池缺口统计0，但最新有效报价约28秒，仍有 `QUOTE_USD_UNKNOWN,quote_liquidity_unavailable` 间歇失败，**不能称全部持仓始终新鲜**。
- 新回执部署后首次核查为0：表和代码上线已验证，后续自然触发样本尚需报告证明。未人为买入、伪造cap拒绝或把旧150条未明确去向准入补成拒绝。
- 两小时worker最近成功回执为22:57:49Z；日报最近04:13:05Z，尚未到下一次24小时期限。真实周期机制已存在，不用“安排了监督”替代新行为效果验证。
- 末次健康状态补丁已随 **PID8104 / 23:17:42.072973Z** 加载，四个模块含runtime哈希全部一致（`final_loaded.json`）。UNKNOWN_RPC不能报成功采样。前一进程自然产生trade40494：该SOL原生Paper仓位由原hard_stop退出，回款3.7490142U、实现−0.9657239U；非新增机制盈利证据。23:17后0持仓来自自然结束，不是删除仓位或行情缺失修复证明。

## 回滚

仅反向应用本阶段 source/test/report 补丁并自主重启Paper；保留追加回执表及所有数据，旧程序可忽略该表。不删除账户、持仓、交易、策略或证据。工作树已有其他UI、调度、配置、协作改动，本阶段只选择自己的Store schema/投影hunk，不把旧调度差异混入本提交；生产加载哈希与提交范围分别记录。

## 剩余与下一步

1. 新集中度回执自然触发及完整按臂去向覆盖验证；既有进程内集中度计数有双增问题，不能作独立样本统计，当前报告使用不可变回执。
2. 当前Solana持仓间歇缺流动性，以及WSOL参考/RPC超时仍需按负载实测分析；不把缺值当零，不增加付费数据依赖，不人为清空错误监督。
3. 组合退出差异机制未触发、母子同cohort成本后比较和新策略长期有效性待验证；不凭42笔负收益/0次机制触发盲目改旧策略。
4. 已有三轮工程工件持续推进；全目标、自然迁池自动恢复、跨时段稳定性与持续净盈利尚不能标为完成。
