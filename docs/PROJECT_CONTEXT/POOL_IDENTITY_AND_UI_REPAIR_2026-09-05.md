# 入场池绑定、账户展示与采样曲线修复

记录时间：2026-09-05。范围：当前独立 1000U Paper 账期；不重新初始化、不删除历史、不启用 Live。

## 已定位的问题

1. **系统缺陷：跨池估值和成交。** DexScreener 的同 token 返回可能包含多个池。原采集按最高流动性选池，退出虽然比较触发前后两帧，却没有核对入场池。当前账期成交 371492、cohort 8918 以另一池价格记入回款 408.0661967514557U、已实现收益 388.0661967514557U，不能视为有效策略盈利。
2. **资金路径污染。** 固定 1000U 下，错误回款可能资助后续本来无钱买入的机会。只减掉虚假收益不足以清理评估口径；必须在受影响策略内按原时间序列重放现金门，并保留、隔离受影响的后续记录。
3. **展示缺陷。** 累计曲线有后端与浏览器双重尾部截取；账户价值未明确展示余额加持仓；发现页缺少独立刷新与分链查询，compact 数据漏掉入场决定计数。旧最大回撤使用已实现闭仓序列，不是包含持仓估值的账户曲线。

## 最小修复

- 同一个 30-token HTTP 响应保留全部池；按持仓原入场池生成共享 mark，退出和估值只使用这一池，不增加按策略重复请求。缺少原池为未知或原池缺失，禁止替换成另一个流动性最高的池。
- 原池两次成功响应均缺失且持续超过 60 秒才核销；网络错误不当作结构性缺失。新鲜原池流动性小于 1U 按剩余仓位全损。
- 保留原始成交，通过已有 append-only correction 撤销无证据回款，并隔离资金门重放识别的污染后续样本；不补造反事实买卖。
- Web 当前价值从同一读事务的有效现金流和原池持仓价值计算，缺少有效估值不显示假 0。余额、持仓价值、总价值分别展示。
- 全账期曲线首次读取后按 snapshot ID 增量更新；每策略内部最多约 600 点、列表最多 32 点、详情最多 300 点，保留首末时间和分桶极值。最大回撤使用采样之前的有效账户快照计算，不由简化图线反推。污染区间保留为空白，不能连接成虚构收益。
- 发现页独立 5 秒缓存接口，支持 ALL / Solana / BSC / Robinhood；单链先筛选再取最近记录。漏斗是当前账期策略决定次数，不是去重 token 数或同一漏斗队列。多链采集轮次包含共享计数，不能称该单链独占计数。
- 账户总价值、余额、PNL、已实现/未实现、回撤、期望、盈利因子、尾部、完成数、胜率、开放仓、前向时长和编号可分别排序。

## 新增试验

独立 `broad_cost_coverage_scaleout_v1` 使用既有宽入口、4% 双侧成本、20U 单笔和 1000U 账户，在成本后收益达到预注册水平时分批兑现。它从实际部署 frontier 开始；旧 127 策略不改变。历史浮盈回吐现象只支持研究假设，不是该新策略的回测或自然业绩。详见 `BROAD_COST_COVERAGE_SCALEOUT_RESEARCH_2026-09-05.md`。

## 定向验证与上线状态

- 入场池绑定/正确池退出/未知池/批量返回、缺池计时、网络失败、低于 1U：6 项通过。
- 新策略幂等增量注册、旧定义不变、前向 frontier、资金与成本后分批：2 项通过。
- Web 账户回撤语义：1 项通过；JS 加载、选择器、账户单元格与发现刷新接线通过。
- Web 全账期650+快照、增量frontier、分链发现与漏斗2项通过；2个既有API/静态页面集成测试通过。固定/无限资金历史切换的污染重放最小回归1项通过。

## 09:00Z 部署与自然运行结果

代码 `15b4d73` 已推送并受控部署。迁移/纠正/账户快照耗时约5.09秒。原账期保持，原15,900笔交易的数量和最大ID383556均未改写；未做第二次初始化。部署截点实际识别4笔跨池SELL，涉及2个策略、3个策略仓位：371492、378100、378812、380189。追加UNRESOLVED纠正并隔离86个资金污染后续样本。早先1笔/57个的检查值只是较早截点，不能冒充最终数量。

新试验于09:00:51.814712Z加入，独立frontier833371，当前128策略。09:04:21截点新策略已有53 BUY、16 SELL、1 WRITEOFF，首BUY09:01:26晚于部署；这些是初步自然交易计数，不能证明盈利。已有127策略ID/规则/账期不变。Live锁定。

09:01 API核对：128策略，126个当前估值完整的账户满足“余额+持仓=总价值=1000+累计PNL”，最大浮点误差小于4e-13U；其余2个保留未知而非假0。分链发现接口实际返回4个筛选结果，单次约128–198ms；/api/live冷请求约241ms。曲线首时间仍为原账期07:37:09，不用新策略部署时间截断旧策略，但失效历史估值保留空白而非冒充可比较数据。

09:04短窗口：策略实际周期P95=1.097秒，持仓轮次P95=9.291秒，持仓批次获取P95=8.418秒，批次写入退出P95=0.0995秒。与部署前同类组件接近，尚无明显核心退化证据，不能据此宣称长周期稳定。所需原入场池数据年龄P95：Solana29.99秒/BSC13.61秒/Robinhood12.49秒；3个Solana token存在无成功原池估值。此项已换成原池口径，不与旧最高流动性池口径硬比。

进程实际工作集：Runtime约85MB、Web约116MB；发现页及曲线不触发外部行情采集。增量curve查询计划确认使用新增`(definition_version,id)`索引范围查找，不再扫描整个账期再排序。

上线检查另发现同仓多笔SELL correction在仓位展示中被最后一笔覆盖；现金流按逐笔合计正确。补充修复已将仓位修正按逐笔合计，任一UNRESOLVED保持未知；逐笔账本层不重复修正，partial SELL不再仅凭修正类型被误标为全部闭仓。最小回归`test_multiple_sell_corrections_accumulate_per_position_in_store_and_web`通过，验证两笔SELL的realized全部撤销、cash980、Store/Web仓位与账户一致、correction_count=2。

## 部署前真实性能基线

08:47:28Z，本账期持续运行未重置：策略循环实际周期 P50/P95 1.011/1.100 秒；当前持仓轮次 3.296/9.057 秒；HTTP 获取阶段耗时 5.266/8.167 秒；批次写入及退出 0.0296/0.0946 秒。跨链发现轮次 90 秒，处理耗时 P95 73.963 秒。Token 最高流动性诊断表中 Solana 数据年龄 P95 26.17 秒、BSC 8.89 秒、Robinhood 7.97 秒；这不是每个原入场池的年龄，修复后诊断改看所需入场池中的最旧值，不能直接据两个不同口径宣称提速。

数据库约 8.60GB，WAL 约 12.66MB，E 盘剩余约 60.23GB。主要延迟仍在外部获取及并发排队，不在约 0.1 秒的批次退出计算。页面可见 5 秒、隐藏 30 秒，不驱动外部采集。

## 回溯和边界

按 Git 提交定位代码修复；数据库 correction 与污染标记保留证据及原因，不直接改写源成交。新增能力如导致核心链路显著退化，应先局部减负或回退相关功能，不能回退原池身份约束去恢复虚假价格。持仓行情 HTTP 长尾尚未消除，不能宣称全量每币秒级或已经证明实盘可成交。
