# 248：16 仓有效容量与全历史 PNL 曲线

## 用户要求与会话理解

已完整读取 ChatGPT 会话 `Chat CMT_20260918-1` 的全部可用轮次。持续目标不是以策略数量或成交数量代替收益，而是把 `发现 → 新鲜原池数据 → 策略判断 → Paper 成交 → 退出与成本后结果` 中的失败逐项归因，并在不牺牲实时性、真实性和稳定性的前提下形成可区分的前向修订。现有与退役策略、用户样本与普通/失败对照、采集覆盖、执行和退出必须共同进入学习闭环；历史结果和失败不得删除或回填。

本阶段落实两个新的明确要求：普通 Paper 策略有效并发仓位由 8 提升到 16；PNL 曲线展示当前资金期自交易开始以来的完整可用历史，而非浏览器滑动尾窗。

## 实现

- `Store.CHAIN_MEME_TRADER_CONCURRENCY_CAP_FLOOR` 从 8 改为 16。有效定义层统一处理既有和未来策略；注册 JSON、旧持仓、旧交易和资金期不修改。每个受影响策略保留注册原值、2026-09-19 授权依据和重算后的有效行为哈希。
- 单笔仍为 20U，因此通常最多 16 × 20U = 320U 同时名义暴露/策略；现金、单币终身一次、原池、安全、后帧成交和其他策略门槛继续独立生效。提高容量不等于强制开仓。
- Web 后端原本已扫描完整合格账户快照、全区间抽样并用全量有效点计算回撤；问题在浏览器把历史裁成“固定首点 + 最近 599 点”。前端现在把 `full_period_sampled` 响应作为权威全区间样本直接替换，不再用客户端尾窗替代历史；总览明确显示“自交易开始以来、全区间抽样、不使用滑动窗”。

## 验证与部署

- Python：`tests/test_concurrency_cap_floor.py`、`tests/test_uniform_notional.py`、`tests/test_chain_web_curves.py` 共 16 项通过。
- Node：`chain_web_strategy_revision.test.js` 与 `chain_web_ui_stability.test.js` 通过；新增断言验证后续全历史响应仍保留最早交易期起点，且不存在 600 点尾窗裁剪。
- 当前数据库 `data/memetrader_forward.sqlite3` 的有效定义为 525 条策略，525/525 的有效 `max_concurrent_positions=16`；447 条注册值低于 16 被抬升，78 条原无上限字段被赋予统一有效上限。
- 受控重载后 Paper PID 9664，运行清单 525 条、`store.py` 哈希 `f73a7f...e1486` 与磁盘一致；Web PID 40368。资金期仍为 `chain-meme-trader/funding-20260906-v002-final-1000`，Paper-only，Live locked。
- `/health` 正常，`/api/performance` 正常；Web 根页面已返回新的全历史说明。重载后短窗尚无新决策、仓位或交易，因此本阶段没有新增盈利结论。

## 后续前向判断

继续观察长期停滞策略在 16 仓口径下的 `signal → admitted → next-frame BUY → terminal`，并将容量、上游无信号、退役依赖、数据缺口和现金不足分开。不得把更大容量当成策略质量改善；若额外仓位只扩大相关亏损，应按独立 Token、同源配对和成本后结果回退或暂停对应策略，而不是保留容量带来的表面成交量。
