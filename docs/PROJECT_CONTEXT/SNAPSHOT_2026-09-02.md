# memeTrader snapshot — 2026-09-02

- Chrome 扩展重载后，Web health 已确认采集器版本 `0.6.6`、collector active、Bridge reachable；repost lineage 修复已实际部署。该修复只保留动作主体、动作类型、原作者、原帖及 repost Snowflake 时间，不把 repost 当原创、背书、独立确认或交易证据。
- provider-post ambiguity Phase A 的三路高强度独立复核均为 `REVISE`，共识仅允许 local-only、append-only 前向 census。版本 `provider-post-ambiguity-shadow/v1` 从 exposure-link `451913` 后激活；首次检查的最小 admission link 为 `451914`，没有历史回填，episode/membership/checkpoint 均为0。
- 两项新 Phase A 测试与一项 Runtime 初始化测试通过。受控重启后 `memeTrader Paper Bot` 为 `Running / IgnoreNew`，8765 单 listener，SQLite WAL/Web healthy，Paper=true，Live=false/locked。没有提交或推送 Git。
- 内存检查发生在用户关闭 Chrome 之后，因此不能据此推断 Chrome 原始占用；当时系统总内存约 15.65 GB、可用 5.64 GB，memeTrader Python 约 0.1 GB。另有与 memeTrader 无关的 `devspace` Node 服务约占 3.0 GB。扩展源码升至 v0.6.7，将 critical/normal 账号轮换复用一个页面、priority post 使用第二个页面，页面数由3降为2；JS 语法检查通过。Chrome 下次启动时加载该版本，关闭期间 X 精确采集暂停。
- 后续最终事实修正：外部 Chrome/Edge 关闭后，扩展 0.6.6 心跳只在短暂残余窗口内保持新鲜，随后超过3分钟窗口并在最终检查时达到约11分钟；内置浏览器不能运行该 unpacked 扩展，因此不能替代外部 Chrome 常驻。两条新鲜 Elon 精确帖子分别约在发布后2–3分钟形成 `local_receive` Observation/Event，后来 provider 又提交同一 URL。Web priority request 原先仍返回这两个已捕获帖子，形成真实重复轮换；现已按精确 canonical path 排除已有 `local_receive + raw.browser` Observation。定向测试通过，Web 受控更新后 priority request 从2降为0；8787/8788 单 listener、SQLite/Paper正常、Live locked，8765直接健康检查可达。
- provider-post Phase A 已自然形成2个 episode、2个 membership、9个到期 checkpoint；激活点以前的同帖7 Token 分叉没有回填，新 membership 只来自激活后的链接，全部保持 `decision_eligible=0 / affects=none`。
- 两个自然 exact Elon-post Token 进一步证明信息链之后还有 hydration 调度阻隔：部署前已等待14–15分钟、attempts=0，队列共有859个 due item，前方分别436/491项。Store/Runtime 现仅对配置重点账号的 exact social-post Token提高 hydration 顺序，不改变普通项、Context trigger、Agent cooldown、证据门或交易门。两个定向测试通过；受控 Runtime 重启后两 Token 在首轮同时 hydrated 并记录 `high_impact_account_post` eligible transition。一个在 `08:57:18Z` 经 deferred retry admitted，Luna/low 消耗88,380 tokens，结果为 `no_context`；另一个仍由全局冷却控制并保留 deferred lineage。没有新增 Decision/Paper，Live保持锁定。
- 后续抽查最新 60 个 Token Context 终态发现另一条真实信息交接损失：直接 high-impact 调查可以携带 `observation_id/observed_text`，但 `global_cooldown_active` 后的 durable deferred retry 只恢复 source-link URL，丢失原始不可变浏览器正文；同时 Agent 提示仍要求再次访问 X，导致已在本机精确捕获的正文被描述为“无法访问”。现从原 trigger transition 恢复 exact Observation、正文及发布时间，并明确本地精确正文可证明“帖子当时说了什么”，但仍不能证明背书、独立确认、社区扩散或 Token 精确绑定。3 项定向测试通过；Paper Runtime 受控更新至 PID `6748`，8765 单 listener/health 正常，SQLite 可读，扩展 `0.6.7` X 心跳恢复，Paper 保持运行且 Live=false。
- 同一批终态进一步暴露出 source-level 重复调查：最新120次 assessment 中99次带 trigger URL，但只有47个不同 URL，首轮以后重复52次；涉及重复 URL 的调用共消耗约6.126M tokens。分开证据等级后，exact-browser 为10次/7个 URL/3次重复（约0.513M tokens），metadata-or-other 为89次/46个 URL/43次同等级重复（约5.220M tokens）；其余重复来自同 URL 的 metadata→exact 等级切换。主要问题不是日预算，而是重复的未核验同帖调用占用全局冷却槽，延迟其他独立帖子。不能直接复用整份 token assessment，因为不同 Token 的名称、CA 和绑定结论不同；下一步应把帖子级事实调查与 Token 级 exact binding 分开，并保留 metadata→本地精确正文的证据升级。
- 已先部署不丢样本的 source-fair 调度最小实现：每个证据等级为 trigger URL 生成 canonical source key，X/Twitter 同帖合并；exact-browser key 额外绑定本地正文 fingerprint，保证 metadata→exact 或正文 revision 是新的证据版本。同一 high-impact lane 内先排不同且近期未调查的帖子，再排已调查的不同帖子，最后才排同轮同帖 clone；所有 Token 仍保留，MAX=4、全局/单 Token 冷却、Agent 并发、证据门、Strategy/Paper/Live 均不变。4 项定向测试通过，Paper Runtime 受控更新为 PID `11252`，8765 单 listener/health 正常，SQLite 可读、Paper 模式、Live=false。该实现只改善独立 URL 覆盖，不缓存或复制 Token assessment。
- 活跃链范围已收敛为 `Solana + BSC`：运行配置、示例配置和 Runtime 默认值的 Gecko 网络与候选链均已同步；Base/Robinhood 停止新增发现和正式候选，但历史数据库不删除、不改写。配置与 Live 锁定定向测试通过；受控重启后 Runtime PID `21844`、8765 单 listener、Web health ok、SQLite WAL 可读、Paper=true、Live=false/locked。
- 收敛后的首个短前向窗口新增19个 Solana、7个 BSC Token，未新增其他链；形成3次 reverse-news lookup、8条 Event→Token relation，但尚无新 assessment、Decision 或主 Paper fill。进一步回看 `09:00Z–09:33Z` 的自然 Token Context：12次 assessment 全部 `no_context`，其中同一 Solana exact post 被6个 Token分别完整调查，合计404,124 tokens。现有 source-fair 只在有其他不同 source key 时改善排序，不能解决单一帖子 fanout。已创建 `CHATGPT_REVIEW_HANDOFF_SOURCE_FACT_TOKEN_BINDING_2026-09-02.md`，在三路高强度独立复核前不实施帖子事实复用；任何设计都必须保持 Token 绑定、Decision、安全和交易资格逐 Token 独立。
- 该前向窗口继续扩展至63个 Solana、26个 BSC Token，产生8个 `WAIT`、0个主 Paper fill。5次完成的 Context assessment 仍无一成为可决策证据：同一 Solana exact post 又被3个新 Token重复完整调查，额外消耗181,742 tokens且全部 `no_context`；一个不同的 Sam Altman exact post消耗119,690 tokens后为 `insufficient_reachable_sources`；一个普通 metadata trigger为 `no_context`。同时有116次 candidate evaluation 因 `match_score_below_minimum` 被过滤。这个自然窗口同时证明两个主要断点：帖子级重复调查占用 Agent 槽位，以及候选名称/叙事到精确 Token 关系的匹配强度不足；提高 Agent 频率本身不会解决任一断点。
- 普通 `token_metadata_source_link` 触发原先遗漏了其已验证格式的来源 URL，导致 `token_context_source_key` 为空、source-fair 无法对普通网站/社交来源做同源排序，assessment 也无法显示精确触发链接。现已只补回规范化 public URL，不改变 trigger priority、准入、角色或 decision eligibility；普通网站触发和 deferred metadata retry 两项定向测试通过。Paper Runtime 受控更新为 PID `6780`，Web/SQLite 正常，Live locked。
- Runtime 更新后的首个新 high-impact 调查仍由同一 Solana exact post 触发；admission `9098`、agent run `ee6849fcec0b446a9c58d0d43c8f44a3` 完成后 assessment `740` 再次为 `no_context`，单次101,104 tokens。该帖子在当前可观察窗口已累计10次不同 Token 的完整调查、约686,970 tokens，全部 `no_context`。这已足以证明 source-fair 排序不能替代 source-level fact reuse/cooldown，但直接跳过同源 Token 仍可能伤害 Token-specific binding recall，因此在三路独立复核前保持运行基线，不临时改写准入。
- source-fact/token-binding 的架构、因果统计和交易经济性三路最高强度 ChatGPT 独立复核均完成并给出 `REVISE`。实测双 Agent 槽同时忙约占 24 小时的 1.3%，队列 p95 约 0.001 秒；当前瓶颈不是 Agent 数量，而是同帖重复调查、触发分母和精确 Token 绑定。已部署 append-only `source-fact-single-flight/v1`，从 assessment `751` 后激活：同一 URL/证据等级/content revision 仅 owner 调用一次 Agent，后续 Token 复用不可变来源事实但独立计算 exact binding；不复制 safety/quote/Decision/Paper，错误和 no-context 保留有限刷新。3 项新定向测试、11 项既有 Context 测试及 Python 语法检查通过。重启后单一 Paper Runtime、8787/8788、8765、SQLite WAL 均健康，Paper=true、Live locked；新表已注册，尚未等待到首个自然 work unit。
- Portfolio 的链上探索 Shadow 已补充现金曲线与完整模拟指标：截至本轮 QA，现金约 `$722.8`、已实现 PNL 约 `-$172.1`、最大现金回撤约 `$279.2 / 27.9%`、3 个开放/43 个平仓、胜率约 `44.2%`、46 BUY/43 SELL、估计网络费约 `$0.8`。开放仓位缺少可靠 mark 时不伪造 equity，只显示零回收下界。该结果说明“新币基本不用考虑跌”不成立；无 SELL route/流动性消失必须作为锁仓或 write-off 留在分母。
- `source-fact-single-flight/v1` 已出现首个自然复用：同一不可变来源事实被 4 个 Token 独立绑定，只有 owner 调用 Agent，3 个 follower 的 assessment 记录 `tokens_used=0` 且不复制 run id；安全、Decision、报价与交易资格仍逐 Token 独立。这是节省重复调查的真实前向证据，但尚未证明提高有效证据或收益。
- 首批前向数据也暴露出负结果刷新过密与中断后永久饥饿：6 个 work unit 产生 9 个 `no_context` 结果，其中同一 exact Solana 帖子在旧 10 分钟窗口下被完整调查 4 次；一次 Runtime 重启中断的只读调查在租约过期后持续返回 `source_fact_uncertain_dispatch`。现将 `no_context` 复用独立设为从 Agent 完成时刻起 30 分钟，并在 reused binding 后写入正常的逐 Token 180 分钟冷却；中断 attempt 在租约过期并经过既有 10 分钟错误退避后可追加重试，不再永久卡住。4 项定向测试通过。真实前向验证中，旧 attempt `10` 之后于 `12:07:32Z` 自动创建 attempt `11` 并 admitted，`12:08:01Z` 完成 `no_context`；result `10` 的 `reusable_until=12:38:01Z`，精确等于完成后 30 分钟，且对应 Token cooldown 已写入。未回填历史、未放宽证据/安全/交易门。
- 已新增严格前向、独立分账的 `onchain-paper-exit-challenger/v1`。它只镜像注册后新产生的固定 Shadow BUY，不回填历史赢家；使用同一笔入场和 Jupiter 最小输出，DexScreener 每 15 秒只负责价格/流动性/活跃度标记，触发 hard stop、trailing、分批止盈、零活跃、流动性紧急或 240 分钟终止后，必须取得对应剩余数量的下一笔 Jupiter Token→USDC 最小输出才模拟成交。`no_route` 保持仓位待重试，240 分钟仍无路线才核销；不调用 Agent，不影响主 Paper、固定 Shadow、Strategy 或 Live。
- 动态退出对照已在真实 8787 Portfolio 显示现金、Dex 标记权益、已实现/未实现/总 PNL、最大回撤、开放/完成/核销、报价/no-route、逐仓动作及五条动态曲线。浏览器视觉 QA 与 API QA 通过；注册时间 `2026-09-02T11:35:26Z`，起始现金/权益 `$1000`，尚无注册后的自然成对新入场，因此没有伪造仓位或收益。空闲且账户状态完全不变时不再每 15 秒重复写相同曲线点，避免无研究价值的 SQLite 增长。
- 动态退出 Store/Runtime/Web 的定向测试、既有固定 Jupiter/Shadow 测试、Web API 测试、Python/JavaScript 语法检查均通过。部署后计划任务 `memeTrader Paper Bot` 运行、8765 单 listener、8787/8788/API/SQLite WAL 健康，Paper=true，Live=false/locked；未提交或推送 Git。
- 新币详情中的普通 X 帖子此前没有进入浏览器优先采集：部署前 15 分钟窗口共有 190 条 Token social link、48 个不同 X status，但命中配置 watch account 为 0，`/api/watchlist` 因而返回 0。现只对 15 分钟内新出现、Solana/BSC、平台已启用且 X Snowflake 在 180 分钟语境窗内的 Token `social_post` 增加 priority request；配置重点账号仍最高优先，显式停用账号仍排除，已捕获本地原文不重复轮换。首次部署后 priority request 从 0 变为 12，复用原有单 priority 页面，不增加浏览器标签或 Agent 数量。
- X `/status/<id>/photo|video/<n>` 已与基础 status URL 做 canonical 合并。新 `local_receive` 浏览器 Observation 会把完全匹配的 Token/source-link 写入既有 append-only `context_trigger_evaluation`，并把对应 Token 重新排入 metadata hydration；不在浏览器请求中运行 Agent，不回填旧 Observation。自然前向窗口从 8 个新 Observation 形成 32 条 Token handoff，证明 `Token detail → X priority → exact Observation → durable Token rehydration` 已运行。
- 随后定位到最后一个调度饥饿：精确浏览器原文与未核验普通 metadata 同属 priority 1，而普通 lane 原先只按最新市场快照选一条；持续涌入的未核验链接先占槽、随后又被 exposure-only 跳过。现普通 lane 先选 `browser_exact_entity_observation + observation_id`，再按既有 priority/快照新鲜度排序；不改变证据、冷却、预算、安全、Decision 或交易门。4 项针对性测试与 Python 语法检查通过，测试还确认市场快照早于原文时不会使用未来正文。
- 修复后首个自然样本在 `2026-09-02T12:41:56Z` admitted：Token `9ydZ…pump` 由 `@phosphenq` 精确原文触发，Luna/low 使用 38,141 tokens，于 `12:42:23Z` 返回结构化 `no_context`。当时 5 分钟成交量约 `$2.42`、1 buy/0 sell、momentum `17.74`，没有独立报道或公众人物关联，因此没有生成 Decision/Paper。这证明真实原文已能到达 Agent，同时低活动项目仍被可解释淘汰；增加并发不是该断点的解法。
- Chrome 控制通道实际确认 X 仍保持登录，只有4个现有采集/扩展标签；`12:49Z–12:56Z` 数据库持续收到新的 `local_receive` X 原文。旧 Web health 只看独立 heartbeat，Bridge 忙于同步处理 Observation 时健康探针可能超时，即使新原文正在入库也会误报 stale/error。健康判定现同时使用最新 browser Observation 与 heartbeat；健康探针繁忙但最近3分钟有真实采集活动时以 `recent_collector_activity` 标为 reachable/healthy。1项定向测试通过，Web 受控更新后 API 正确显示 collector active；不改变采集、策略或交易逻辑。
- 新自然窗口继续暴露 hydration 后静默丢候选：5个精确原文形成13个 Token handoff，但普通 lane 一轮只选择1个，未选 Token 已被标为 hydrated 且没有 Admission/重试。现新增有界 `browser_exact` lane：每轮最多4个、按 source-fair 优先不同帖子；超过上限的精确 Token 重新进入 hydration 队列，直到形成 Admission/deferred 终态。未核验 metadata 保持原单一低优先 lane并继续由 exposure-only 拦截；Agent并发2、全局/单 Token 冷却、source-fact single-flight、证据/安全/交易门均不变。4项定向测试和 Python 语法检查通过，单一 Paper Runtime 已更新，Live仍锁定。
- 有界 `browser_exact` lane 已获得自然前向证明：同一精确 Observation `7233` 关联的 owner Token 形成 admission `9490`、assessment `787`，Luna/low 使用 `62,851` tokens 后得到结构化 `no_context`；第二个 Token 形成 admission `9491`，以 `source_fact_reused` 复用同一不可变来源事实，独立生成 assessment `788` 且 `tokens_used=0`。超过本轮4个上限的精确 Token 已写回 `pending` 和新的 `next_attempt_at`，没有再被 hydration 静默吞掉。该帖子只有弱社区措辞，没有独立事实、精确交易催化或可验证公众人物关联，因此没有 Decision/Paper；这证明了交接与节流有效，不证明 alpha。
- Chrome 内存异常定位到单一 renderer（约 `3.68 GB`），只终止该异常进程后 Chrome 总工作集降至约 `1.54 GB`、系统可用内存由约 `3.59 GB` 回升至 `7.57 GB`，登录、扩展配置和浏览器主进程均保留，采集随后自然恢复。最终检查时 Chrome 18 个进程约 `3.54 GB`、最大单进程约 `1.33 GB`、系统可用约 `5.29 GB`；Web health 显示 SQLite `ok`、Paper=true、Live=false/locked、Bridge healthy、collector active，最新活动来自 heartbeat。外部 Chrome 仍是 X 精确采集所需运行载体，但不必保留无关标签；若再次出现单 renderer 异常增长，应只处理该进程而不是关闭整个采集器。
- 精确来源链开始到达正式候选层：Decision `2948` 为 `WAIT`，候选分 `57.9568`、match `67.4419`，两个同名 Solana Token 的 raw canonical margin 仅 `0.0061`。两者当时均约 `$2,744` 市值、流动性未知、5分钟零交易；第一名在决策前有 provider metadata 链接精确匹配 Event 内本地 Observation `7120`，第二名没有。旧解释只记录 `candidate_score_too_low`，掩盖了更重要的 same-name canonical ambiguity；现对低分且低 margin 的未来 Decision 同时记录 `candidate_score_too_low + canonical_token_ambiguous`，不改变评分、排序、安全或交易行为。定向测试通过，单一 Paper Runtime 更新后 8765 单 listener、SQLite/Bridge/Paper 正常、Live locked。
- 严格 as-of 的24小时诊断找到5个“多候选且至少一个 Token 在决策前已有 Event 精确 source-link”的 Decision。Event `4989` 有16个候选，其中5个 Token 同时链接同一 Solana 帖子、在同一秒创建且当时均无5分钟交易；这证明 exact source link 可以定义 identity-linked candidate set，但 fanout 时仍不能制造单一 winner。已建立 `CHATGPT_REVIEW_HANDOFF_EXACT_SOURCE_LINK_CANONICAL_IDENTITY_2026-09-02.md`；在三路独立复核和新 activation point 前不把该身份事实接入生产评分。
- Information-first Shadow 的固定时点学习出现系统性采样缺口：100个 trackable cohort 中，15/60/240分钟分别已有 `60/45/27` 个 observed 和 `38/46/53` 个 missing。missing 之后又重新出现有效行情的数量分别为 `17/18/5`，说明被动依赖其他发现器碰巧写快照造成 informative missingness。新 cohort `100` 的15分钟目标已到，但目标后尚无本地快照；同时直接只读 DexScreener 查询仍能返回其 Pump.fun pair 和价格，证明该 Token 当时可查询。没有为这个选中样本手工补快照。已建立 `CHATGPT_REVIEW_HANDOFF_INFORMATION_FIRST_ACTIVE_OUTCOME_SAMPLING_2026-09-02.md`，拟审查新的前向 activation、统一目标时刻取样、错误/missing 终态和 Dex mark/Jupiter 可执行报价分工；旧 v1 不回填、不重解释。
- Lead 对 active outcome sampling 的实质复核已同步：Phase 1 只允许 DexScreener 描述性 mark，15/60/240 分钟目标各按 `+0/+30/+120/+300s` 重试并在 `+300s` 冻结终态；Jupiter 必须留给以后另行预注册的可执行收益 cohort。现已部署 append-only `information-first-active-outcome-sampler/v1`，注册时间 `2026-09-02T14:46:49Z`、activation shadow cohort `104`，只接收严格大于该 id 的未来 cohort，不回填既有样本；所有行固定 `decision_eligible=0 / affects=none`，不写通用 `token_snapshots`，不接 Strategy/Paper/Live。两项新定向测试、Telegram/X 过滤回归及全套 pytest 均通过；doctor、SQLite WAL、Bridge、Web、Paper Runtime 与 Live lock 均正常。部署时最大 cohort 仍为 `104`，因此 targets/attempts/results/terminals 均为0，这是尚无 activation 后新 cohort 的正确空状态。
