# RESULT137 — actual conditional strategy delivery

ACK C2C-20260910-FULL-STRATEGY-IMPLEMENTATION-137. Same existing Codex writer; Lead read-only review, no topology change.

## Implemented and registered

Code42fcf7c integrates136 rather than duplicating its subsystem. On2026-09-10T01:00:48.437500Z/snapshot2574566, existing startup append registered:

| Addition | Arm | Contract |
|---|---|---|
|297|event_recovered_narrative_runner_v1|5U/max4; same frozen verified event signal as296; independent actual-net half-recovery/narrative behavior|
|298|organic_reawakening_recovered_runner_v1|5U/max4; same actual organic reawakening episode; shared recovered-runner behavior|
|299|synthetic_fast_harvest_v1|BSC1U/max1/absolute300s; conditional exact-pool provider simulation; no Agent/averaging/reentry|

All were API forward_enabled=true after loading. Existing293–296 and all older policy/funding/activation prefixes retain identical SHA256, checked in seven tables. Funding remains chain-meme-trader/funding-20260906-v002-final-1000; no reset, historical replay, refunds, or Live. New accounts are ordinary append-only independent Paper experiments, not a claim of profitability.

## A: reachable synthetic preflight and real post-buy evidence

The prior early-watch cycle could never obtain proof because it needed proof before reaching common safety. Descriptive SYNTHETIC_LPI_BUILDING_CANDIDATE now carries the same frozen displacement/signed-flow/concentration evidence without claiming sellability. A later valid original-pool frame may create one pending cohort. Existing common safety performs the existing bounded GoPlus/Honeypot work, using requested and returned token/chain/pool identity and original acquisition time. Actual BUY still requires the positive exact-pool simulation receipt, no hard/soft hazard, and a strictly later frame; missing/403/expired proof remains waiting. Provider-selected simulation amount is NOT exact1U executable quoting. No unsigned CapitalPulse114 restoration.

Post-buy distribution is no longer a read of one old pre-entry receipt. The existing idle tick can enroll the one open synthetic position into a minute-keyed, persistent-deduped post-open trade window. One of the existing eight queue slots is reserved for this work; held risk work precedes ordinary research within the SAME supplemental Gecko <=1/min/shared host budget. It does not expand watch capacity or total request allowance. Missing/truncated data remains UNKNOWN. New distribution receipts reach existing original-pool next-frame exit settlement; hard/floor/time safety remains. No post-buy receipt creates another entry signal. The production cohort table actually contains pair_address (PRAGMA verified); the new held query uses the existing position market index and cohort integer primary key, with no new index.

## B: independent recovered narrative behavior

The two new policies explicitly alias the real producer's frozen signal, origin clocks, token/pool and episode, with separate per-arm decision receipts. source_arm_ids alone is not the wiring. Tests confirm same source_entry_fill_id with their baseline. Existing minimum actual-net recovery is recomputed at the SELL frame; the raw sale must retain >=half current remaining quantity. Only actual settlement sets principal_recovered/high-water. Existing case-level NarrativeHold handles the new arms without extra per-arm Agent dispatch or quota changes. Only actual recovered principal plus healthy exact pool and persisted verified independent-source narrative permits extension from30m to60m; UNKNOWN takes the mechanical exit and trailing still overrides narrative. Baseline296 is unchanged.

## C/D: supported source boundary, not fabricated native execution

The bounded independent native code/evidence review found no new funded native subset whose proof has closed. Existing Pons current-state MODEL_QUOTE remains context-only where its older per-instance verified source/bytecode, fixed-block getters and verified stock conversion pass; it is not new natural execution proof. Known diagnostic source hash9e19ce…cd12684/curve0x25d370…eb41 is retained under data/research/native74, not replayed.

Pump exact-input still lacks an independent causal pre-transaction Curve/Global/FeeConfig fixture, SOL V2 buy, reserve-edge/refund/post-state and reverse-sell/PumpSwap handoff proof. Existing official commit9c82f61cb711b044a17f770ab8ce9f9bdf78f333 and IDL SHA256 b90bc471327f671449271d5d1d42354d1fae6f5a06502f5834459a3108138e49 are pinned in PUMP_EXACT_INPUT_PROOF_123.md; Rust builder serialization is not settlement math proof. Pons requires authenticated deployed standard-json/source/compiler immutable/deployer binding plus accessible fixed-block code/getters; existing36 UNKNOWN receipts/403 remain historical evidence. Four/LaunchLab supply listing identity/progress, not exact native buy/sell/USD lifecycle. No repeated403 probe or paid source was introduced. C is PARTIAL_SUPPORTED_SHADOW_ONLY / DATA_BLOCKED for native funding, with exact dependencies preserved.

Regime72 remains bounded outcome-free local supply metrics with UNKNOWN coverage and no frozen entry producer/opportunity contract; no arbitrary new regime gate is invented. The supported successor/rediscovery reawakening path is reused by119/298, rather than registering a duplicate migration account. Native successor execution still shares the native proof blockers. ACTIVE16/23 matched/as-of work remains nonblocking; old descriptions/results are not repeated. No blanket lifecycle pause or ultra-early threshold relaxation.

## E: local work reduction and tests

Missing synthetic proof is checked before resume materializes another token snapshot or attempts another account projection. A deterministic20-frame waiting fixture produces zero additional token_snapshots and zero BUY; the previous path reached snapshot insertion despite missing required proof. Ordinary safety behavior remains. This removes demonstrated redundant work; it is not a claimed network speedup.

Validation:91 closest classifier/cohort/Store/safety cases passed; narrative helper19 passed; three additional actual lifecycle tests passed after correcting fixtures to provider descending trade order, the actual2USD entry price, and pre-BUY narrative activation. Tests exercise no-cohort BUILDING candidate -> shared safety assessment -> later1U BUY -> actual post-buy page normalization/classification -> distribution SELL; same-fill baseline/derivative BUY -> actual partial SELL recovering >=5U while retaining half -> verified hold extension -> mechanical final SELL. Syntax/scoped diff checks passed. No forced natural trade or Agent call.

Runtime/API/ledger artifacts: data/research/delivery137/{before-live,before-performance,after-live,after-performance,immutable-before,immutable-after}. Short natural acceptance and canonical checkpoint follow below; source implementation, registration, loaded code and natural samples remain separate.

## Actual short forward acceptance / continuity

At01:05:29Z (~281s after activation), held_fetch p95=1.584495s versus pre-load1.919698s; held_apply_exit=.039232s versus.036207s; cohort observer=.665371s versus.678431s; flat selection=.673733s versus.588565s. Passive wait p95=2.078345s, drops0; Dex pool/connect errors0. Early startup81-sample held p95=2.82893s and8-sample flat p95=6.4912s were transient and are not hidden; the later120-sample window is within held guard. This establishes short forward health, not a causal speedup or long-run performance guarantee. The exact request ceiling and held priority are unchanged.

Post-activation bounded1500-row tail reaches before activation and contains8 classifier receipts, all UNKNOWN, no new safety-stage receipt or new three-arm position yet. No natural synthetic proof or narrative partial/extension claimed. Source/account readiness is real; market outcomes are still pending. See natural-counts.json; full counters elsewhere are cumulative, not this generation's denominator.

Earlier136 organic_early_flow_v1 cohort94707 is now independently read as WRITTEN_OFF at00:52:32.021999Z, realized-2U (before137 activation). A prior API closed_position_count=0 does not mean zero terminals: writeoffs are distinct. Signed flow/breadth and a negative cannot_sell_all flag did not guarantee liquidity retention. Preserve this loss and its original safety/flow evidence; no refund, exclusion from headline accounting, threshold change or alpha claim. Artifact earlier-organic-outcome.json.

Canonical checkpoint updated once to revision4/digest50ed80191574c959876a6158a5c40a4bd47477d32b8b0a6b694bfc64f30e7958. TASKS119 is implementation DONE rather than stale OPEN;137 records A/B/E delivered and native input blockers. CURRENT_TASK/HANDOFF projections match; attention points to137, no semantic ACK loop or changed pairing/session/memory. Local R2 readback remains historical, not revision4/new-browser verification. Task context explicitly labels old125 pause text superseded by126/133.

Current upstream primary-source reread: [official Pump public docs](https://github.com/pump-fun/pump-public-docs) and [PumpSwap state documentation](https://github.com/pump-fun/pump-public-docs/blob/main/docs/PUMP_SWAP_README.md). State/interface documentation does not supply the missing causal V2 exact-input settlement fixtures. No extrapolated native strategy was registered.

## Fixed-state evidence integration (supersedes formula blocker above)

ACK C2C-20260910-137-NATIVE-FIXEDSTATE-EVIDENCE. Reused Lead isolated probe without rerunning it or changing its files. The corrected real-Rent report PUMP_FIXED_STATE_BREAKTHROUGH_137.md provides slot445751903, IDL b90bc471327f671449271d5d1d42354d1fae6f5a06502f5834459a3108138e49 and Pump binary57cbdd6b3d02c58f35a288699d5f4f35acfcaca0e9f1440c07b62f9e8935a560. Five independently simulated exact-input buy/post-buy sell results match new versioned integer implementation. Budget76222005259 fails over real-token cap; no clamp/refund invented. Rent174933 discrepancy was simulator environment, not a trading charge.

Existing native_economic_frame now emits OBSERVED_SHADOW for supported SOL standard supply/nonmayhem/noncashback/current95+30bps single-tier state; other fee classes, complete/cap or unsupported quote remain UNKNOWN. This means verified formula subset, NOT a fresh live bytecode pin or funded execution permission. Coherent existing RPC/reference clocks and request budgets are unchanged; fees once per leg plus existing adverse Paper4%/4%, transaction rent/network excluded and explicitly UNKNOWN. Token controls and cash-budget/migration lifecycle remain independent gates. Legacy helper remains explicitly diagnostic, never relabeled exact.

Validation: tests/test_pump_native.py30 passed; scoped diff check passed. No production process/DB/funding/strategy mutation in this addendum. Source wired and tested; NOT YET LOADED, natural post-change counts unavailable. Canonical context revision5 records this boundary; no second writer/session or evidence replay.
