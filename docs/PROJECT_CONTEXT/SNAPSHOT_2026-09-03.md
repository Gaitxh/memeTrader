# memeTrader 前向快照 — 2026-09-03

状态：`ACTIVE / PAPER / LIVE LOCKED`
主线：`C2C-20260903-MEMETRADER-SYSTEM-RESEARCH-IMPL-001`

## 当前权威研究与实施顺序

- 系统研究：`CHATGPT_MEMETRADER_SYSTEM_RESEARCH_2026-09-03.md`
- Codex 交接：`CHATGPT_CODEX_IMPLEMENTATION_HANDOFF_MEMETRADER_SYSTEM_RESEARCH_2026-09-03.md`
- 顺序：P0-A active outcome deadline → P0-B immutable launch facts → P0-C market surface → P0-D liquidity survival → P0-E execution realism。
- 所有新研究特征保持 append-only、strict-forward、无历史回填、`decision_eligible=0 / affects=none`。

## P0-A 实施结果

- 核验时 r6：160,909 Tokens、5,974 Events、2,976 Decisions、8 Paper fills。
- active outcome：27 targets / 13 attempts / 13 results / 15 terminals；terminal 为 9 observed mark、2 late response、4 历史 scheduler miss。
- 修复：最多 4 个到期 target 同周期有界并发；每个 provider await 最多 15 秒且受 frozen deadline 进一步约束。
- 截止到达时 terminal 先冻结；迟到结果 append 为 `late_response`，不能覆盖 terminal。
- `scheduler_missed_deadline` 继续只表示截止前没有 request；已有 attempt 但没有及时 terminal 为 `terminal_missing`。
- 定向测试：3 passed，包括 provider 永久挂起、迟到结果不可覆盖 terminal、scheduler miss 与 attempt timeout 语义分离。
- 本阶段没有修改 Strategy gate、Paper 入场、Agent 并发或 Live。

## 下一断点

P0-B/C：在 Pump create 入库时冻结 immutable launch facts，并建立 surface/canonical/liquidity-control 研究描述；旧 r6 不回填进新实验分母。

## P0-B/C 实施结果

- `token_launch_facts/v1` 从 WebSocket 本地接收时刻冻结 PumpPortal create/migration；creator、signature、bonding curve、`pool_label`、初始买入/SOL、market cap 与虚拟储备不再受后续 hydration 覆盖。
- `token-market-surface/v2-bonding-curves` 只记录具有具体 `pairAddress` 的 DexScreener pair；Pump.fun/Meteora DBC 归为 bonding curve，PumpSwap 归为 CPMM，Pancake v2/v3 与 Raydium CLMM/CPMM 按可见标签保留。
- Token 级 migration 不再被用来证明任一具体 PumpSwap pair canonical；在真实 Pump AMM IDL/RPC pair-account 证明前，PumpSwap `canonical_status/liquidity_control` 保持 unknown。
- 两类表均 append-only、无旧 r6 回填、`decision_eligible=0 / affects=none`；生产自然记录已出现，未产生 Decision/Paper/Live 副作用。

## P0-D 实施与版本边界

- 两个现存事件循环热查询已按 live py-spy 根因最小修复：hydration 命中 token-first 部分索引；Jupiter baseline join 命中 attempts/results 组合索引，当前 r6 只读测量约 `5.79ms`（修复前约 `3.07s`）。
- `liquidity-survival-shadow/v1` 在 worker 未完成窗口形成 14 cohorts、0 attempts，冻结为 partial-deploy/abandoned；不补写、不回填。
- v2 因 due/finalizer 缺少版本谓词而混入旧 target，冻结为 contaminated/abandoned；既有行不删除、不改写、不用于后续 estimand。
- 当前正式前向版本为 `liquidity-survival-shadow/v3-version-isolated`，activation snapshot `623852`。只接收注册后首次达到 `$12,000` 的具体非 bonding-curve pair，固定 1/5/15/60 分钟目标；same-pair 流动性下降超过 90% 只标记 `liquidity_collapse_unclassified`，绝不冒充 LP withdrawal。
- v3 主动 worker 每轮最多 4 个 deterministic Dex 请求，15 秒且不越过 deadline；保留 no-pair、pair mismatch、timeout/error 与 scheduler miss，完全不接 Strategy/Paper/Live。
- 首个自然 v3 检查点：24 cohorts、34 attempts（28 observed / 6 timeout）、33 outcomes（28 survived / 1 liquidity collapse unclassified / 4 error），跨版本 outcome=`0`；当前没有 scheduler-missed outcome。

## 三个模拟账户、两种入场逻辑与估值真值

- `fair-comparison/2026-09-03-v1` 于 `2026-09-02T19:58:05.965625Z` 开始；综合策略与纯链上策略均以 `$1000` 为本轮起点。旧交易、旧账户和旧 Shadow 行完整保留，只从本轮曲线、费用与 PNL 展示中排除，没有删除或回填 r6。
- 纯链上固定周期与动态退出使用 `v2-fair-start` 新账本；它们属于同一入场策略的退出对照，不再显示成第三个主账户。
- 当前流动性为零、未知或没有可用容量时，DEX 页面价格不得生成账户未实现 PNL；只保留为明确标注的非可执行参考，且受可见流动性容量上限约束。只有时点有效、特定数量的 Jupiter 最小卖出输出才形成模拟成交与已实现 PNL。
- 某 Token 后来流动性归零，不会反向否定此前真实存在的可卖报价。后续研究应分别统计早期可执行退出收益、退出延迟、后续流动性死亡及 no-route 核销，禁止用未来归零状态从入场 cohort 中事后剔除样本。
- Portfolio API 热路径已从约 `16s` 降至约 `0.93s`；近期采集率只扫描有界尾部/有索引时间窗，不再每 15 秒全表扫描 4GB SQLite。
- 定向验证：5 passed；Python/JavaScript 语法检查通过。Runtime、8787/8788、SQLite 与浏览器桥运行，Live 仍锁定。
- 多链边界：当前发现层已包含 Solana、BSC、Base 和 Robinhood Chain，正式 candidate chain 仍为 Solana/BSC；Base 与 Robinhood Chain 暂保持 research-only。Robinhood Chain 不是 Robinhood App 的上架信号：它是 chain id `4663` 的 Arbitrum L2，以 ETH 支付 gas。后续 BSC/Base/Robinhood 必须分别接入时点有效的 amount-specific route、动态 gas/L1 data fee、venue fee、安全与卖出能力，不能复用 Jupiter/Solana 成本假设。

## 2026-09-03 追加核验：P0-E、Robinhood 与估值语义

- P0-D v3 当前只读截面已增长到 384 cohorts / 1,085 attempts / 1,200 outcomes，工程采样链已跨过最初检查点；主实现优先级转入 P0-E，而不是继续扩建同类 Shadow 基础设施。
- Solana `event-context-jupiter-route/v1` 已具备候选阶段固定金额 BUY→SELL 双向探针和严格时钟，但当前 r6 自然 probe 仍为 0；Runtime 对 route-backed candidate 保留 `route_backed_paper_execution_not_implemented`，避免把最大仓位容量探针冒充最终 policy-size fill。P0-E 仍未完成。
- 纯链上开放仓位在没有特定剩余数量卖出报价时，账户 equity/unrealized/total PNL 现在保持未知；DexScreener 价格只显示为受流动性容量限制的 `indicative` 参考。页面同时改正了准入说明，不再声称所有流动性、成交、买卖结构与安全字段都已成为硬门。两项定向测试通过。
- 最近 24 小时当前 SQLite 发现约 39,389 个 Solana、9,942 个 Robinhood、3,204 个 BSC、444 个 Base Token。Robinhood 官方 `/rhj/assets` 当前返回 194 个 chain-4663 Stock Token 合约；其中 103 个已与本机 Robinhood Token 表重叠，证明 Robinhood 必须先做 Meme 与 Stock Token/RWA 的官方地址级分类。当前仍不派 Agent、不写 Decision/Paper。

## 2026-09-03 追加实施：经济成交 v3 与策略 3

- 纯链上固定周期/动态退出已升级到 `v3-economic-execution`：Jupiter V2 `priceImpact`（百分点）与旧 `priceImpactPct`（小数比率）统一换算为 bps；非有限值拒绝。BUY 使用 minimum output，路由费/滑点包含在最低输出中，链费单列；SELL 只有扣除链费后净回收大于 0 才成交。
- 15/60 分钟出现结构有效但净回收不经济的卖出报价时，记录 `quoted_but_uneconomic` 并延期，不能制造负现金流 SELL；240 分钟仍无经济路线才核销。相关结果写入不可变 execution assessment，并在 Portfolio 显示拦截数与可执行估值覆盖。
- Portfolio 产品模型更新为三个顶层账户：策略 1 `信息 + Token`、策略 2 `纯链上固定周期基线`、策略 3 `同入场·买后叙事增强持有`。原动态止损/止盈 challenger 降为策略 2 的折叠研究详情，避免把退出规则误解为第三套选币逻辑。
- 策略 3 必须只复制激活后的策略 2 新 BUY，保存源 trade id 并精确匹配 Token、时间、金额、数量与成本；买后叙事只决定是否延长 runner，不能覆盖安全、流动性、硬止损、移动止盈和最长持有。旧赢家不得回填。
- 三个账户不可相加；策略 1/2 不是同入场单变量实验，只有策略 2/3 可用激活后的 paired cohort 比较。所有数值继续标为 Paper/模拟，不是 Mainnet 盈利。

## 2026-09-03 追加实施：P0-E 最终金额双向报价 challenger

- 新增 `event-route-execution-challenger/v1-entry-preflight`，只从注册后的新 route-backed Decision 开始，不回填旧 Decision/probe。
- 候选阶段 `$35` 最大仓位 Jupiter 双向 probe 继续只证明容量；Runtime 在最终现金、风险、日暴露与评分共同确定仓位后，把实际美元金额向下冻结为 USDC raw amount，再请求一组全新的 exact-size BUY→SELL preflight。
- BUY 的 minimum token output 是 SELL preflight 的精确输入；记录双腿 raw amount、请求/响应时钟、route-only 往返下界、费用完整性以及 no-route/error/protocol-invalid/interrupted。Probe output 不可缩放或复用为 fill。
- 当前版本是只读执行对照：`decision_eligible=0 / affects=none / no_fill_research_only`。旧主 Paper 账户、仓位、交易和曲线均不改变；`route_backed_paper_execution_not_implemented` WAIT 保持。
- Jupiter quote-only 返回的链费或同时点 SOL/USD 换算不完整时，明确记录 `cost_unknown`，不重复扣最低输出已包含的路由/池费/滑点，也不以固定 4%/125bps 冒充精确链费。
- 定向验证覆盖：最终 `$12.34` 必须请求 `12,340,000` USDC raw；SELL 输入必须等于 BUY minimum raw；研究报价不会创建主 Paper 仓位/交易或改变现金；Web API/UI 显示样本、终态与费用未知数，不把 challenger 叫成交。
- 发布验证：项目全套 `pytest -q` 与 `compileall` 通过；单一 Paper Runtime、8787/8788 Web、SQLite/WAL 与 8765 Bridge 恢复健康，Live 仍锁定。Portfolio 浏览器 QA 无 console error；执行真值条按五项同排，当前 activation decision `3049`，自然 attempt `0`，不虚构样本。

## 2026-09-03 追加前向结果：策略 3 配对与退出失败语义

- 策略 3 注册于 exploration BUY trade `123` 之后；首个核验截面为 4 个自然新 BUY / 4 个 exact pair，现金 `$859.96`。随后浏览器 QA 截面自然增长为 7 / 7，现金 `$754.93`，始终为 0 mismatch、0 backfill；每次均为相同 `$35 + $0.01` 模拟买入成本。开放仓位缺少 amount-specific 可执行卖出报价时，equity/unrealized/total PNL 保持未知。
- 叙事 runner 当前为 0，`narrative_evidence_status=not_mature_not_enabled`。现状只验证同入场账本，不验证热点、人物、社区或新闻能延长持有并改善扣费收益。
- 动态退出账本暴露出旧分类错误：Jupiter Swap V2 对无法找到报价返回 HTTP 400 `Failed to get quotes`，此前被记为 `error/JupiterQuoteError`。客户端已按官方响应映射为 `JupiterNoRouteError`；两项定向测试通过。
- 受控重启后，首个自然新结果 `onchain_paper_exit_challenger_quote_results.id=410` 已记录为 `no_route`、空 `error_type`，没有生成 SELL、现金或 PNL。历史 400 错误行完整保留，不追改。
- Paper Runtime 保持单任务运行；SQLite/WAL 正常，8787 Web 可读，Live=`false / locked`。本轮未提交或推送 Git。

## 2026-09-03 追加实施：策略 3 买后叙事前向观察

- 新增 `onchain-paper-narrative-context/v1-forward-only`。注册前沿冻结为当时策略 2 的真实 BUY trade 最大 id，而不是策略 3 已复制仓位的最大 id，避免“策略 2 已买入但策略 3 尚未复制”竞态造成隐性回填。
- 每个注册后 exact-paired 策略 3 仓位只选择满足 `opened_at <= snapshot.observed_at <= snapshot.ingested_at <= snapshot.recorded_at <= trigger.ingested_at <= trigger.recorded_at <= seed.recorded_at` 的具体快照；Seed、source BUY、cohort、Token、snapshot 与 trigger transition 必须全部一致。
- Seed 在 Agent admission 前已经写入但进程中断时，会用同一 immutable transition 恢复首次调度；已有任意 admission（包括 cooldown/budget skip）则不重复首调，后续由现有延期机制处理。
- `post_entry_narrative_position` 强制为 research-only：即使获得多来源、高置信、精确 CA 的结果，也只保存 Token Context assessment/audit，`decision_eligible=false / affects=context_display_only`，不返回 Observation 给 Runtime，不新增主 Event/Decision，不改变仓位、交易、现金或退出。
- 生产注册点为策略 2 BUY `133`；当前策略 3 的 8 个仓位均明确显示 `pre_registration_not_backfilled`，0 eligible、0 seed、0 Agent assessment，不回填旧样本。策略 3 runner 仍为 0，继续禁用。
- 定向验证共 7 passed（含正常 Token Context confirmation 不回归、旧样本排除、未 admission Seed 恢复、错误 lineage 拒绝、研究结果不进入主证据流、Web 组合语义）；JavaScript 语法检查通过。Paper Runtime、8787 Web、SQLite/WAL、8765 Bridge 正常，Live 仍锁定。本轮未提交或推送 Git。

## 2026-09-03 追加实施：EVM 指定金额双向路线研究与 Portfolio 收敛

- 新增 `onchain-only-shadow-evm-uniswap-v3-quote/v1-forward-only`，只覆盖 BSC、Base、Robinhood Chain 的注册后新 `onchain-only-shadow` cohort；生产注册时间 `2026-09-03T00:32:32.219916Z`，activation cohort `2112`。注册时已有 cohort 永不回填。
- 每条任务在同一固定区块上验证官方 Uniswap V3 pool，并枚举 direct 与不同 fee-tier 的两跳路径；固定精确 accounting amount，BUY 最低输出作为 SELL 输入，买卖两侧都计入 4% 不利滑点。网络/RPC 错误、无池、BUY 不可报价和 SELL 不可报价分别保存，不能互相伪装。
- 一次生产外的真实 BSC `$35` 验证在 block `119628438` 得到双向 0.3% 路线；双边 4% 最低输出后的稳定币回收比约 `0.9049294`。这只证明 pool math，未模拟 Router、Token transfer/tax/blacklist 或完整网络费，因此固定为 `economic_status=cost_unknown / decision_eligible=false / affects=none`，不是成交、收益或 PNL。
- Store 使用独立 immutable registration/attempt/result 三表；Runtime 每轮至多处理一个到期 EVM cohort，只写该研究表和 heartbeat，不调用 Paper apply，不写 Decision/Position/Trade/PNL。非 Solana 主 Paper 的 CANDIDATE 继续降为 WAIT，BSC/Base/Robinhood Paper 均禁用直到 route、gas/L1 fee、Token transfer 和卖出能力完整。
- 受控部署后 production EVM registration 为 1 条，attempt/result 为 0；max cohort 已自然增长到 `2115`，新增 cohort 均为 Solana，因此当前“0 个 EVM 结果”是无合格新样本，不是 worker 故障。主 Paper 保持 cash `$1000`、0 开放仓、8 条历史 trade；Live=`false`。
- Portfolio 首屏改为先显示三个清晰的策略 Tab；公平比较说明、策略 1 最终金额路线研究及 EVM route research 默认折叠。BSC/Base/Robinhood 即使未来 `quoted` 也显示 `PAPER DISABLED · RESEARCH ONLY`，不得进入任一现金、权益、仓位、交易或 PNL 曲线。
- 验证：新增/相关 6 项定向测试通过，原两项失败分别为 UTC 午夜测试不稳定和旧 BSC 可交易假设，修正后针对性通过；随后全套 pytest 第二轮 100% 通过，`compileall` 与 JavaScript 语法检查通过。本机 8765/8787/8788 各单 listener，8787/8788 health=true；公网地址为 `https://single-bunny-sensor-railway.trycloudflare.com`，凭据仍只保存在 E 盘。`doctor --online` 本轮长时间未退出后被终止，直接 health/SQLite/RPC 验证已完成，不能把它虚报为通过。
- Lead ChatGPT 的新增全量研究仍在既有高强度会话中运行；当前可见状态为搜索 7 个网站，尚无最终交接。Codex 没有等待它才推进已证实的 EVM 成本缺口，也没有把未完成意见写入策略。

## 2026-09-03 追加修复：策略 3 买后调查覆盖与主动结果采样

- `onchain-paper-narrative-context/v1-forward-only` 的真实前向结果为 11 个 seed：7 个 `coverage_gap`、4 个 `triggered`。抽查证明主要缺口不是 Token 缺失，而是首次 60 秒检查时尚无买后刷新快照便永久终结；其中多笔在数秒或数分钟后已经出现新快照。v1 行保持不可变，不改写、不回填。
- 新版本 `onchain-paper-narrative-context/v2-entry-snapshot-fallback` 于 `2026-09-03T01:19:26.926895Z` 从策略 2 BUY `155` 后注册。新 exact-paired 仓位优先使用当时已完整入库的买后刷新快照；若仍没有，则只允许使用该 cohort 真实入场前已经完整可用的精确 trigger snapshot 启动调查。`investigation_started_at` 仍必须晚于入场，因而没有未来数据或后来证据倒灌。
- 买后调查的延期准入只在对应策略 3 仓位仍为 `baseline/narrative_runner` 时保留，并以 exact trigger transition 去重；优先级低于精确高影响原帖和新鲜高热 Event，但高于批量 metadata。仓位关闭后不再消耗 Agent。该变化不增加最大 2 个 Agent、不绕过全局/单 Token 冷却、预算、结构校验或证据门。
- Portfolio 现在显示当前版本的 `post_entry_snapshot` 与 `entry_trigger_snapshot` 数量，并单列旧版不可变 seed 总数，避免新 activation 的 `0/0` 被误解为历史丢失。当前界面为 20 个 exact pair、0 mismatch；v2 为 0 eligible/0 seed，旧版 11 条仍可见；runner 仍为 0、禁用。
- `information-first-active-outcome-sampler/v1` 当前为 204 targets：144 observed、5 scheduler missed、2 late、2 terminal missing、50 pending；已终态缺失率为 9/153（5.88%），不是旧 broad dashboard 的约 53%。v2 从 shadow cohort `172` 后注册，把重试点由 `+0/+30/+120/+300s` 改为 `+0/+30/+120/+240s`，在 300 秒右闭截止前为最后请求保留 60 秒，并统一“恰好截止时仍可接受”的边界。
- 6 项集中测试通过；Python compileall 与 JavaScript 语法检查通过。受控重启后 8765/8787/8788 各单 listener，8787 health=true、SQLite 可读、mode=paper、Live=false。本轮未提交或推送 Git。

## 2026-09-03 追加实施：启动者历史前向 Shadow

- 新增 `creator-launch-risk-shadow/v1-local-history-lower-bound`，生产 activation launch fact=`10533`。仅为之后新到的 Solana Pump create 写不可变 cohort；旧 Token/旧 launch 不生成 cohort。
- 每个 cohort 只读取该 launch 入库前已存在的同地址 launch 与已终态结果，冻结 prior launch、24h launch、距上次 launch、240m raw outcome 与 60m liquidity-survival 计数。provider 报告的 `traderPublicKey` 明确不等于 RPC 已验证创建者、开发者经济实体或关联钱包簇。
- 首个自然截面：68 个合格新 create、68 个 cohort、0 个注册前 cohort；46 个具有本机先前 launch，27 个 prior launch≥10，最大 prior launch=194。当前结果尚未成熟，这些数字不构成风险结论或策略门。
- Token 详情 API 与中英 UI 已接入折叠面板，原始 payload/hash 不返回；4 项针对性测试通过，JavaScript 语法通过。浏览器实际展开检查成功。受控重启后 8765/8787/8788 单 listener、SQLite/WAL 与 Paper Runtime 正常，Live 仍锁定；未提交或推送 Git。

## 2026-09-03 追加修复：Strategy 3 延期调查假饥饿

- Strategy 3 v2 已自然产生 3 个 `post_entry_narrative_position` seed，但共享延期队列每 4 分钟只处理 1 项。真实断点不是普通 metadata 的静态分数高于 Strategy 3，而是已 `reused/source_fact_reused` 的 intent 未终止、以及处于 `token_cooldown_active` 的 Token 仍反复进入队首，浪费整个重试槽。
- `due_token_context_active_retries()` 现在把 exact-intent 的 `admitted` 与 `reused` 都视为完成；有效期尚未结束的 Token cooldown 按其真实的 Token-wide 语义抑制该 Token 的全部延期 intent。全局/错误冷却仍只按原有 exact lineage 匹配。没有改优先分数、Agent 并发、冷却时长、预算或任何交易门。
- 两项针对性回归测试通过：复用成功不再重复入队；不同 trigger 的 Token-wide cooldown 能阻止同一 Token 的 Strategy 3 intent 空转。
- 受控部署后的首轮自然 active retry 于 `2026-09-03T02:25:51.907991Z` 成功 `admitted` 一条真实 post-entry intent，并生成 `no_context` assessment；这证明调度槽恢复，不证明叙事有效或策略盈利。当前 v2 的 3 个开放 seed 仍待后续自然轮次依次处理，runner 与所有交易行为保持禁用。
- 同步审查问题已发送到既有 Lead ChatGPT 会话；当前只确认用户消息进入会话，尚未读取到 ChatGPT 回复，因此其意见没有进入本次工程决策。

## 2026-09-03 追加修复：当前版本调查隔离与退出监控公平轮换

- 复核发现 `2026-09-03T02:25:51.907991Z` 的成功 post-entry retry 实际属于已冻结的 context v1；当前 context v2 的 3 个 seed 已超过可重试时间 25–59 分钟，仍全部只有一次 cooldown skip。根因是 active retry 只检查关联策略 3 仓位是否开放，没有限定 seed 必须属于当前 context version。
- post-entry retry 现在必须同时匹配当前 `onchain-paper-narrative-context/v2-entry-snapshot-fallback` seed、exact trigger transition、source BUY 与仍开放的 paired position；旧 v1 行完整保留，但不再竞争当前唯一重试槽。
- 退出监控另有独立饥饿：每轮按最早 `opened_at` 取 3 个仓位，旧仓在 15 秒后再次到期并反复占满队首，导致新仓长期 0 mark。队列现改为“从未标记优先，其后按最旧 last mark”，频率、单轮上限和请求并发均未增加。
- 两项定向测试通过。受控重启后的自然证据：此前 0 mark 的 cohorts `2130/2137/2138/2139` 均获得首个前向 mark；current-v2 seed `12` 于 `2026-09-03T02:49:58.428011Z` 获得第二次 admission 并生成 assessment `1044/no_context`，而不是旧版本再次占槽。
- `no_context` 的当前结果为 0 个独立来源、社区状态 unknown、公众人物 not observed；它是有效负样本，不是买入/延长持有信号。Strategy 3 runner、主 Decision/Paper 和 Live 均未改变；另外两个 current-v2 seed 继续自然排队。
- 标记恢复后又发现 amount-specific 退出报价的单槽被同一失败仓重复占用：cohort `2105` 已 16 次，而多个同级待退出仓仍为 0。报价队列现同样先服务从未尝试的 pending mark，其后按最旧上次尝试轮换，退出动作优先级只作为同等等待时的次序，不再允许无限重试垄断。
- 再次受控部署后，原先 0 attempt 的 cohorts `2110/2112/2113/2117` 已依次得到首个真实 Jupiter 卖出尝试，终态包含 `no_route` 与 `quoted_but_uneconomic`；均未伪造成 SELL/PNL。三项集中测试通过。

## 2026-09-03 追加实施：20 USDC 公平成本版本

- 用户指定的新前向执行假设已冻结为：每次入场名义金额 `$20`、BUY 与 SELL 各 `4%` 不利滑点、每次经济成交固定扣除 `$0.40`。分批退出的每一个 SELL 都单独扣 `$0.40`；已有成交、旧仓位和旧版本结果不回写。
- 新链上版本为 `onchain-only-shadow/v2-20usdc`、`onchain-only-shadow-jupiter-quote/v2-20usdc-400bps`、`onchain-paper-exploration/v4-20usdc-flat040`、`onchain-paper-exit-challenger/v4-20usdc-flat040`、`onchain-paper-narrative-runner/v2-20usdc-flat040` 与 `onchain-paper-narrative-context/v3-20usdc-entry-snapshot`。注册时间从 `2026-09-03T03:33:04Z` 起，旧版本保持不可变。
- `fair-comparison/2026-09-03-20usdc-v2` 于 `2026-09-03T03:37:02Z` 启动；三种 Paper 策略均从 `$1000` 新前向起点开始。主策略旧累计现金 `$1001.7325`、11 笔历史成交作为 prior 保留，不进入新曲线。
- 在价格完全不变且不计 Token tax 的简化往返中，`$20` 加双边 4% 与双边固定 `$0.40` 约产生 `$2.3385` 净损失；标的价格约需上涨 `12.67%` 才达到盈亏平衡。该高成本假设会显著压低短线策略收益，是用户指定的保守实验条件，不应误称真实链费。
- SQLite 的版本触发器已由旧 Shadow 字面量改为核对注册定义中的 `source`，因此允许新的不可变版本链，同时仍拒绝跨版本、错时钟、错 Token、错金额或错报价 lineage。8 项相关定向测试通过。
- 主信息策略的未来成交已使用 20/4%/0.40 配置，但其策略定义仍未像链上策略一样拥有独立 machine-readable `definition_version` 外键；当前可追溯性来自 config、成交实际成本、fair epoch 与本快照。后续应只为新成交增加前向 policy version，不回填旧成交。
- 当前运行范围继续是 Solana-only。BSC/Base/Robinhood 的只读 EVM Quoter 研究表保留但不进入 Candidate、Paper 或 PNL。外部能力已变化：0x 当前支持 chain 56、8453、4663 的 Swap/Gasless；若未来恢复多链，优先以 amount-specific 0x executable quote 替换当前仅覆盖 Uniswap V3 pool math 的研究层，并分别补齐动态 gas/L1 data fee、allowance/nonce、transfer tax/blacklist、安全覆盖和稳定币会计。
- 受控部署后 `memeTrader Paper Bot`、8787/8788 Web、SQLite/WAL 与 Bridge 正常，mode=`paper`，Live=`false/locked/unavailable`；本节随后按用户明确要求随当前源码提交并推送。
