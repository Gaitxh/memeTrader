# memeTrader 前向快照 — 2026-09-03

## 2026-09-03 13:00Z — canonical base-token 身份边界修正

- Pump.fun 官方 IDL/README 复核确认 canonical `pool-authority` 必须按 Pool `base_mint` 派生；Program ID 有效，不需要替换。
- 新 surface 判定补充 `evaluated token == decoded base_mint`：quote 侧资产不能借用 base 侧的 canonical migration 身份。新增 quote-side 回归并与既有 canonical/label-only 测试共同通过。
- 单一 Paper Runtime 已受控重载；SQLite/Web/8765 正常，Live 继续锁定。下一项仍是 held-account subscription 与 confirmed-rug terminal/no-rearm，不扩展 UI、多链或 Agent。

## 2026-09-03 12:41Z — 单一 Solana 主线聚焦与 Route/Surface 真值分离

- 前向注册并部署 `strategy-focus/v1-solana-onchain-primary`：Agent `1366`、Jupiter result `3865`、S2 BUY `180`、S3 BUY `48` 为冻结前沿；主动信息 Agent、S1/S3 新入场及 EVM 研究暂停，便宜被动采集与既有仓退出继续。部署后 Agent max 仍为 `1366`。
- 新增不可变 `market-surface-safety/v1-canonical-pumpswap` 与 `execution-route-observation/v1-jupiter-order`。新入场必须分别取得 canonical PumpSwap 持仓面 PASS、BUY route PASS、按实际获得数量的 SELL route PASS，再继续原 v3 rug/价格冲击/费用门。
- 持仓面直接读取 pool owner/layout/PDA/creator/vault 与 mint 账户 program、mint/freeze authority、Token-2022 extensions；危险 PermanentDelegate/TransferHook/NonTransferable/Pausable fail closed。
- Pump 官方文档明确迁移后的 canonical pool 由协议拥有且 Pump 不撤出流动性，因此新 surface 以精确 migration PDA/creator/vault 证明为准，不再错误要求协议池必须 95% LP burn；旧 v3 语义不改写。
- 首批自然前向结果为 2 个 surface + 4 个方向 route observation：两个所选 PumpSwap 池均为非 canonical creator pool，且 Jupiter 路线多池或绕过所选池，均未买入；交界缺事实样本也明确 fail closed。当前尚不能声称新主策略具备收益证据。
- 多跳 route 的持仓面已进一步改为 BUY 最终 Token 相邻池，而非 DexScreener 展示池；该真实池必须通过 canonical RPC，SELL 必须重新经过它。主策略同时冻结并部署 migration age `<=600s`、queue `<=5s`、final preflight `<=10s`、best/stress recovery `>=0.90/0.85`、最多 5 仓、每日新敞口 `$100` 和 exit-alert 优先门；Jupiter 输出不重复扣 AMM fee/slippage。
- 定向测试与 Python compile 通过；单一 Paper Runtime、SQLite/WAL、8765/8787/8788 正常，Live 锁定。下一断点是核验与 DexScreener 所选池不同的实际 Jupiter execution pool，并加入冻结的 pool-age/cost/exposure 门；held-account subscription 与 confirmed-rug no-rearm 尚未完成。

## 2026-09-03 12:18Z — 精确剩余数量可执行权益层

- 前向注册 `onchain-paper-position-monitor/v1-exact-remaining-executable-equity`，activation source BUY trade `180`，不回填历史。
- 开放仓按当前真实剩余 raw quantity 请求 Jupiter，使用 `otherAmountThreshold` 并扣除冻结的 `$0.40` 模拟链费；退出报价优先，已有新鲜同数量退出结果可直接复用，不重复消耗 provider 请求。
- 旧 Jupiter 结果在出现更新 DEX mark 后失效；只有全部开放仓都有新鲜同数量真值时，通用 equity/unrealized/total PNL 才非空，否则只显示明确标注的 `indicative_*`。
- 三项定向测试与 Python compile 通过；Paper Runtime/Web 各受控重启一次，8765/8787/8788、SQLite WAL 正常，Live 锁定。
- 当前 6 个开放仓均属于注册前已进入 pending 的旧退出标记；截止本快照尚无注册后的自然报价样本，因此只确认工程路径和前向边界，不能声称实时覆盖率或盈利改善。
- 最新权威 P0 已转向单一 Solana canonical PumpSwap 主策略：下一步分离 Holding Surface Safety 与 Jupiter Execution Route Truth；暂停更多 venue、多链和高成本信息 Agent 扩展。

## 2026-09-03 12:01Z — 开放仓 PNL 真值边界修复

- 动态退出账户仍用 DexScreener mark 触发止损/止盈，但开放仓的通用 `equity_usd` / `unrealized_pnl_usd` / `total_pnl_usd` 不再承载非可执行 DEX 估值。
- DEX 容量估算只通过 `indicative_equity_usd` / `indicative_unrealized_pnl_usd` / `indicative_total_pnl_usd` 返回；UI 同时明确显示“可执行未实现 PNL”为空，直到全量剩余 raw amount 的持续 Jupiter 估值层接入。
- 这修复了正流动性但 Jupiter `no_route` 时仍可能展示看似可兑现浮盈的问题；零/未知流动性仍保持不可估值，不虚构 `$0` 或盈利。
- 两项定向测试和 Web JavaScript 语法检查通过；8787 Web 以新进程加载，当前 6 个开放仓返回 `indicative_only_open_positions_no_executable_quote`。Paper Runtime 未重复重启，Live 锁定。

## 2026-09-03 11:53Z — P0-A Raydium CPMM 官方 RPC 托管证据

- 新前向版本 `pretrade_rug_safety/v3-pumpswap-raydium-cpmm-rpc-custody` 已从 snapshot `747837` / Jupiter result `3859` 注册；v1/v2 历史保持不可变，v3 不回填。
- Raydium CPMM BUY 门现在核验准确 program owner、637-byte 官方 Pool layout/discriminator、全局 vault/LP authority、两个 vault PDA/mint/authority、LP mint PDA/authority、swap-enable 状态和当前可撤 LP 上限；第三方锁仓标签不再能单独形成 PASS。
- r6 前向样本池 `95HSSikkWYmGSfjqe8Fq7Y2tyitEbD7XcGyEv76RXWZg` 已通过主网官方 RPC 实测：owner 为 CPMM program，结构与 vault/LP authority 一致，当前 LP supply=0、burned=100%。
- 四项定向测试与 Python compile 通过；单一 Paper Runtime 受控重启后 SQLite WAL、8787/8788、Bridge 正常，Live 锁定。
- P0-A 尚未全部完成：Raydium AMM v4 的 OpenBook/未结 PnL 经济解码、CLMM position custody 以及其他 venue provenance 仍保持 `WAIT`；不能把本分段外推为全 Raydium 覆盖。

## 2026-09-03 11:43Z — P0-A PumpSwap 官方 RPC 托管证据

- 新前向版本 `pretrade_rug_safety/v2-pumpswap-rpc-custody` 已从 snapshot `745823` / Jupiter result `3857` 注册；v1 历史保持不可变，v2 不回填。
- 删除了“DEX 标签含 pump + mint 后缀为 pump 就视为 canonical”的错误捷径。新 BUY 门读取准确 pool account，并核验 PumpSwap program owner、官方 Pool discriminator/layout、pool PDA、base/quote mint、两个 vault 及其 authority、Pump canonical creator PDA 和可撤 LP 比例。
- 官方主网示例池实测为 canonical，当前可撤 LP 约 `0.021917%`；标签相同但缺少 RPC 证据的样本保持 `WAIT`，明确 PDA/vault/mint 不一致则 `REJECT`。
- 五项定向测试通过，Python compile 通过；Runtime/Web 单次受控重启后 Paper、SQLite WAL、浏览器采集桥正常，Live 锁定。
- P0-A 尚未完成：Raydium CPMM/AMM-v4/CLMM 解码和交易级创建/locker provenance 仍需补齐；不能据此宣称全部 venue 已覆盖。

## 2026-09-03 11:13Z — P0-B 死亡路线自适应退避首个分段

- 新增并部署 `onchain-paper-exit-quote-scheduler/v1-adaptive-dead-route-backoff`，不改写既有 exit-policy v4 或历史报价。
- 首次退出立即报价；失败后按 `15/30/60/120/300s` 退避，六次总尝试后停止占用 Jupiter。新的流动性与 5m 成交恢复可重新武装，最大持有期终止仍可抢占。
- 部署前 6 个未完成 `LIQUIDITY_EXIT` 标记累计 290 次尝试，其中 5 个已超过新上限；部署后这 5 个不再进入 Jupiter 队列，只剩 1 个尚未完成退避序列的任务。
- 定向测试 `3 passed`；单一 Paper Runtime、SQLite WAL、Web、Bridge 正常，Live 锁定。
- 这是 P0-B 的容量修复分段，不代表 RPC pool/vault 订阅、分阶段监控频率或 executable-equity 刷新已经完成。

## 2026-09-03 11:00Z — P0-A 买前撤池安全首个可执行分段

- `pretrade_rug_safety/v1` 已从 snapshot `741869` / Jupiter result `3850` 起严格前向注册。
- 新 S2 Paper BUY 扣款现在必须有不可变 PASS：包括 venue/pool identity、托管/锁定事实、Token 权限事实和按实际获得数量执行的即时 Jupiter SELL 预检。缺失、过期、未知或拒绝均不会扣款。
- S1 使用同一评估词汇并将仅因未知事实导致的结果保持为 WAIT；其 amount-specific BUY/SELL 仍留给 P0-C。
- 这是 P0-A 的部分完成：RPC/官方 program、vault 和 creation provenance 仍待补齐。Live 继续锁定，历史记录未改写。

状态：`ACTIVE / PAPER / LIVE LOCKED`
主线：`C2C-20260903-MEMETRADER-SYSTEM-RESEARCH-IMPL-001`

## 当前权威研究与实施顺序

- 系统研究：`CHATGPT_MEMETRADER_SYSTEM_RESEARCH_2026-09-03.md`
- Codex 交接：`CHATGPT_CODEX_IMPLEMENTATION_HANDOFF_MEMETRADER_SYSTEM_RESEARCH_2026-09-03.md`
- 顺序：P0-A active outcome deadline → P0-B immutable launch facts → P0-C market surface → P0-D liquidity survival → P0-E execution realism。
- 所有新研究特征保持 append-only、strict-forward、无历史回填、`decision_eligible=0 / affects=none`。

## P0-A 实施结果

- 核验时 r6：160,909 Tokens、5,974 Events、2,976 Decisions、8 Paper fills。
- active outcome：27 targets / 13 attempts / 13 results / 15 terminals；terminal 为 9 observed mark、2 late response、4 历史 scheduler miss。
- 修复：最多 4 个到期 target 同周期有界并发；每个 provider await 最多 15 秒且受 frozen deadline 进一步约束。
- 截止到达时 terminal 先冻结；迟到结果 append 为 `late_response`，不能覆盖 terminal。
- `scheduler_missed_deadline` 继续只表示截止前没有 request；已有 attempt 但没有及时 terminal 为 `terminal_missing`。
- 定向测试：3 passed，包括 provider 永久挂起、迟到结果不可覆盖 terminal、scheduler miss 与 attempt timeout 语义分离。
- 本阶段没有修改 Strategy gate、Paper 入场、Agent 并发或 Live。

## 下一断点

P0-B/C：在 Pump create 入库时冻结 immutable launch facts，并建立 surface/canonical/liquidity-control 研究描述；旧 r6 不回填进新实验分母。

## P0-B/C 实施结果

- `token_launch_facts/v1` 从 WebSocket 本地接收时刻冻结 PumpPortal create/migration；creator、signature、bonding curve、`pool_label`、初始买入/SOL、market cap 与虚拟储备不再受后续 hydration 覆盖。
- `token-market-surface/v2-bonding-curves` 只记录具有具体 `pairAddress` 的 DexScreener pair；Pump.fun/Meteora DBC 归为 bonding curve，PumpSwap 归为 CPMM，Pancake v2/v3 与 Raydium CLMM/CPMM 按可见标签保留。
- Token 级 migration 不再被用来证明任一具体 PumpSwap pair canonical；在真实 Pump AMM IDL/RPC pair-account 证明前，PumpSwap `canonical_status/liquidity_control` 保持 unknown。
- 两类表均 append-only、无旧 r6 回填、`decision_eligible=0 / affects=none`；生产自然记录已出现，未产生 Decision/Paper/Live 副作用。

## P0-D 实施与版本边界

- 两个现存事件循环热查询已按 live py-spy 根因最小修复：hydration 命中 token-first 部分索引；Jupiter baseline join 命中 attempts/results 组合索引，当前 r6 只读测量约 `5.79ms`（修复前约 `3.07s`）。
- `liquidity-survival-shadow/v1` 在 worker 未完成窗口形成 14 cohorts、0 attempts，冻结为 partial-deploy/abandoned；不补写、不回填。
- v2 因 due/finalizer 缺少版本谓词而混入旧 target，冻结为 contaminated/abandoned；既有行不删除、不改写、不用于后续 estimand。
- 当前正式前向版本为 `liquidity-survival-shadow/v3-version-isolated`，activation snapshot `623852`。只接收注册后首次达到 `$12,000` 的具体非 bonding-curve pair，固定 1/5/15/60 分钟目标；same-pair 流动性下降超过 90% 只标记 `liquidity_collapse_unclassified`，绝不冒充 LP withdrawal。
- v3 主动 worker 每轮最多 4 个 deterministic Dex 请求，15 秒且不越过 deadline；保留 no-pair、pair mismatch、timeout/error 与 scheduler miss，完全不接 Strategy/Paper/Live。
- 首个自然 v3 检查点：24 cohorts、34 attempts（28 observed / 6 timeout）、33 outcomes（28 survived / 1 liquidity collapse unclassified / 4 error），跨版本 outcome=`0`；当前没有 scheduler-missed outcome。

## 三个模拟账户、两种入场逻辑与估值真值

- `fair-comparison/2026-09-03-v1` 于 `2026-09-02T19:58:05.965625Z` 开始；综合策略与纯链上策略均以 `$1000` 为本轮起点。旧交易、旧账户和旧 Shadow 行完整保留，只从本轮曲线、费用与 PNL 展示中排除，没有删除或回填 r6。
- 纯链上固定周期与动态退出使用 `v2-fair-start` 新账本；它们属于同一入场策略的退出对照，不再显示成第三个主账户。
- 当前流动性为零、未知或没有可用容量时，DEX 页面价格不得生成账户未实现 PNL；只保留为明确标注的非可执行参考，且受可见流动性容量上限约束。只有时点有效、特定数量的 Jupiter 最小卖出输出才形成模拟成交与已实现 PNL。
- 某 Token 后来流动性归零，不会反向否定此前真实存在的可卖报价。后续研究应分别统计早期可执行退出收益、退出延迟、后续流动性死亡及 no-route 核销，禁止用未来归零状态从入场 cohort 中事后剔除样本。
- Portfolio API 热路径已从约 `16s` 降至约 `0.93s`；近期采集率只扫描有界尾部/有索引时间窗，不再每 15 秒全表扫描 4GB SQLite。
- 定向验证：5 passed；Python/JavaScript 语法检查通过。Runtime、8787/8788、SQLite 与浏览器桥运行，Live 仍锁定。
- 多链边界：当前发现层已包含 Solana、BSC、Base 和 Robinhood Chain，正式 candidate chain 仍为 Solana/BSC；Base 与 Robinhood Chain 暂保持 research-only。Robinhood Chain 不是 Robinhood App 的上架信号：它是 chain id `4663` 的 Arbitrum L2，以 ETH 支付 gas。后续 BSC/Base/Robinhood 必须分别接入时点有效的 amount-specific route、动态 gas/L1 data fee、venue fee、安全与卖出能力，不能复用 Jupiter/Solana 成本假设。

## 2026-09-03 追加核验：P0-E、Robinhood 与估值语义

- P0-D v3 当前只读截面已增长到 384 cohorts / 1,085 attempts / 1,200 outcomes，工程采样链已跨过最初检查点；主实现优先级转入 P0-E，而不是继续扩建同类 Shadow 基础设施。
- Solana `event-context-jupiter-route/v1` 已具备候选阶段固定金额 BUY→SELL 双向探针和严格时钟，但当前 r6 自然 probe 仍为 0；Runtime 对 route-backed candidate 保留 `route_backed_paper_execution_not_implemented`，避免把最大仓位容量探针冒充最终 policy-size fill。P0-E 仍未完成。
- 纯链上开放仓位在没有特定剩余数量卖出报价时，账户 equity/unrealized/total PNL 现在保持未知；DexScreener 价格只显示为受流动性容量限制的 `indicative` 参考。页面同时改正了准入说明，不再声称所有流动性、成交、买卖结构与安全字段都已成为硬门。两项定向测试通过。
- 最近 24 小时当前 SQLite 发现约 39,389 个 Solana、9,942 个 Robinhood、3,204 个 BSC、444 个 Base Token。Robinhood 官方 `/rhj/assets` 当前返回 194 个 chain-4663 Stock Token 合约；其中 103 个已与本机 Robinhood Token 表重叠，证明 Robinhood 必须先做 Meme 与 Stock Token/RWA 的官方地址级分类。当前仍不派 Agent、不写 Decision/Paper。

## 2026-09-03 追加实施：经济成交 v3 与策略 3

- 纯链上固定周期/动态退出已升级到 `v3-economic-execution`：Jupiter V2 `priceImpact`（百分点）与旧 `priceImpactPct`（小数比率）统一换算为 bps；非有限值拒绝。BUY 使用 minimum output，路由费/滑点包含在最低输出中，链费单列；SELL 只有扣除链费后净回收大于 0 才成交。
- 15/60 分钟出现结构有效但净回收不经济的卖出报价时，记录 `quoted_but_uneconomic` 并延期，不能制造负现金流 SELL；240 分钟仍无经济路线才核销。相关结果写入不可变 execution assessment，并在 Portfolio 显示拦截数与可执行估值覆盖。
- Portfolio 产品模型更新为三个顶层账户：策略 1 `信息 + Token`、策略 2 `纯链上固定周期基线`、策略 3 `同入场·买后叙事增强持有`。原动态止损/止盈 challenger 降为策略 2 的折叠研究详情，避免把退出规则误解为第三套选币逻辑。
- 策略 3 必须只复制激活后的策略 2 新 BUY，保存源 trade id 并精确匹配 Token、时间、金额、数量与成本；买后叙事只决定是否延长 runner，不能覆盖安全、流动性、硬止损、移动止盈和最长持有。旧赢家不得回填。
- 三个账户不可相加；策略 1/2 不是同入场单变量实验，只有策略 2/3 可用激活后的 paired cohort 比较。所有数值继续标为 Paper/模拟，不是 Mainnet 盈利。

## 2026-09-03 追加实施：P0-E 最终金额双向报价 challenger

- 新增 `event-route-execution-challenger/v1-entry-preflight`，只从注册后的新 route-backed Decision 开始，不回填旧 Decision/probe。
- 候选阶段 `$35` 最大仓位 Jupiter 双向 probe 继续只证明容量；Runtime 在最终现金、风险、日暴露与评分共同确定仓位后，把实际美元金额向下冻结为 USDC raw amount，再请求一组全新的 exact-size BUY→SELL preflight。
- BUY 的 minimum token output 是 SELL preflight 的精确输入；记录双腿 raw amount、请求/响应时钟、route-only 往返下界、费用完整性以及 no-route/error/protocol-invalid/interrupted。Probe output 不可缩放或复用为 fill。
- 当前版本是只读执行对照：`decision_eligible=0 / affects=none / no_fill_research_only`。旧主 Paper 账户、仓位、交易和曲线均不改变；`route_backed_paper_execution_not_implemented` WAIT 保持。
- Jupiter quote-only 返回的链费或同时点 SOL/USD 换算不完整时，明确记录 `cost_unknown`，不重复扣最低输出已包含的路由/池费/滑点，也不以固定 4%/125bps 冒充精确链费。
- 定向验证覆盖：最终 `$12.34` 必须请求 `12,340,000` USDC raw；SELL 输入必须等于 BUY minimum raw；研究报价不会创建主 Paper 仓位/交易或改变现金；Web API/UI 显示样本、终态与费用未知数，不把 challenger 叫成交。
- 发布验证：项目全套 `pytest -q` 与 `compileall` 通过；单一 Paper Runtime、8787/8788 Web、SQLite/WAL 与 8765 Bridge 恢复健康，Live 仍锁定。Portfolio 浏览器 QA 无 console error；执行真值条按五项同排，当前 activation decision `3049`，自然 attempt `0`，不虚构样本。

## 2026-09-03 追加前向结果：策略 3 配对与退出失败语义

- 策略 3 注册于 exploration BUY trade `123` 之后；首个核验截面为 4 个自然新 BUY / 4 个 exact pair，现金 `$859.96`。随后浏览器 QA 截面自然增长为 7 / 7，现金 `$754.93`，始终为 0 mismatch、0 backfill；每次均为相同 `$35 + $0.01` 模拟买入成本。开放仓位缺少 amount-specific 可执行卖出报价时，equity/unrealized/total PNL 保持未知。
- 叙事 runner 当前为 0，`narrative_evidence_status=not_mature_not_enabled`。现状只验证同入场账本，不验证热点、人物、社区或新闻能延长持有并改善扣费收益。
- 动态退出账本暴露出旧分类错误：Jupiter Swap V2 对无法找到报价返回 HTTP 400 `Failed to get quotes`，此前被记为 `error/JupiterQuoteError`。客户端已按官方响应映射为 `JupiterNoRouteError`；两项定向测试通过。
- 受控重启后，首个自然新结果 `onchain_paper_exit_challenger_quote_results.id=410` 已记录为 `no_route`、空 `error_type`，没有生成 SELL、现金或 PNL。历史 400 错误行完整保留，不追改。
- Paper Runtime 保持单任务运行；SQLite/WAL 正常，8787 Web 可读，Live=`false / locked`。本轮未提交或推送 Git。

## 2026-09-03 追加实施：策略 3 买后叙事前向观察

- 新增 `onchain-paper-narrative-context/v1-forward-only`。注册前沿冻结为当时策略 2 的真实 BUY trade 最大 id，而不是策略 3 已复制仓位的最大 id，避免“策略 2 已买入但策略 3 尚未复制”竞态造成隐性回填。
- 每个注册后 exact-paired 策略 3 仓位只选择满足 `opened_at <= snapshot.observed_at <= snapshot.ingested_at <= snapshot.recorded_at <= trigger.ingested_at <= trigger.recorded_at <= seed.recorded_at` 的具体快照；Seed、source BUY、cohort、Token、snapshot 与 trigger transition 必须全部一致。
- Seed 在 Agent admission 前已经写入但进程中断时，会用同一 immutable transition 恢复首次调度；已有任意 admission（包括 cooldown/budget skip）则不重复首调，后续由现有延期机制处理。
- `post_entry_narrative_position` 强制为 research-only：即使获得多来源、高置信、精确 CA 的结果，也只保存 Token Context assessment/audit，`decision_eligible=false / affects=context_display_only`，不返回 Observation 给 Runtime，不新增主 Event/Decision，不改变仓位、交易、现金或退出。
- 生产注册点为策略 2 BUY `133`；当前策略 3 的 8 个仓位均明确显示 `pre_registration_not_backfilled`，0 eligible、0 seed、0 Agent assessment，不回填旧样本。策略 3 runner 仍为 0，继续禁用。
- 定向验证共 7 passed（含正常 Token Context confirmation 不回归、旧样本排除、未 admission Seed 恢复、错误 lineage 拒绝、研究结果不进入主证据流、Web 组合语义）；JavaScript 语法检查通过。Paper Runtime、8787 Web、SQLite/WAL、8765 Bridge 正常，Live 仍锁定。本轮未提交或推送 Git。

## 2026-09-03 追加实施：EVM 指定金额双向路线研究与 Portfolio 收敛

- 新增 `onchain-only-shadow-evm-uniswap-v3-quote/v1-forward-only`，只覆盖 BSC、Base、Robinhood Chain 的注册后新 `onchain-only-shadow` cohort；生产注册时间 `2026-09-03T00:32:32.219916Z`，activation cohort `2112`。注册时已有 cohort 永不回填。
- 每条任务在同一固定区块上验证官方 Uniswap V3 pool，并枚举 direct 与不同 fee-tier 的两跳路径；固定精确 accounting amount，BUY 最低输出作为 SELL 输入，买卖两侧都计入 4% 不利滑点。网络/RPC 错误、无池、BUY 不可报价和 SELL 不可报价分别保存，不能互相伪装。
- 一次生产外的真实 BSC `$35` 验证在 block `119628438` 得到双向 0.3% 路线；双边 4% 最低输出后的稳定币回收比约 `0.9049294`。这只证明 pool math，未模拟 Router、Token transfer/tax/blacklist 或完整网络费，因此固定为 `economic_status=cost_unknown / decision_eligible=false / affects=none`，不是成交、收益或 PNL。
- Store 使用独立 immutable registration/attempt/result 三表；Runtime 每轮至多处理一个到期 EVM cohort，只写该研究表和 heartbeat，不调用 Paper apply，不写 Decision/Position/Trade/PNL。非 Solana 主 Paper 的 CANDIDATE 继续降为 WAIT，BSC/Base/Robinhood Paper 均禁用直到 route、gas/L1 fee、Token transfer 和卖出能力完整。
- 受控部署后 production EVM registration 为 1 条，attempt/result 为 0；max cohort 已自然增长到 `2115`，新增 cohort 均为 Solana，因此当前“0 个 EVM 结果”是无合格新样本，不是 worker 故障。主 Paper 保持 cash `$1000`、0 开放仓、8 条历史 trade；Live=`false`。
- Portfolio 首屏改为先显示三个清晰的策略 Tab；公平比较说明、策略 1 最终金额路线研究及 EVM route research 默认折叠。BSC/Base/Robinhood 即使未来 `quoted` 也显示 `PAPER DISABLED · RESEARCH ONLY`，不得进入任一现金、权益、仓位、交易或 PNL 曲线。
- 验证：新增/相关 6 项定向测试通过，原两项失败分别为 UTC 午夜测试不稳定和旧 BSC 可交易假设，修正后针对性通过；随后全套 pytest 第二轮 100% 通过，`compileall` 与 JavaScript 语法检查通过。本机 8765/8787/8788 各单 listener，8787/8788 health=true；公网地址为 `https://single-bunny-sensor-railway.trycloudflare.com`，凭据仍只保存在 E 盘。`doctor --online` 本轮长时间未退出后被终止，直接 health/SQLite/RPC 验证已完成，不能把它虚报为通过。
- Lead ChatGPT 的新增全量研究仍在既有高强度会话中运行；当前可见状态为搜索 7 个网站，尚无最终交接。Codex 没有等待它才推进已证实的 EVM 成本缺口，也没有把未完成意见写入策略。

## 2026-09-03 追加修复：策略 3 买后调查覆盖与主动结果采样

- `onchain-paper-narrative-context/v1-forward-only` 的真实前向结果为 11 个 seed：7 个 `coverage_gap`、4 个 `triggered`。抽查证明主要缺口不是 Token 缺失，而是首次 60 秒检查时尚无买后刷新快照便永久终结；其中多笔在数秒或数分钟后已经出现新快照。v1 行保持不可变，不改写、不回填。
- 新版本 `onchain-paper-narrative-context/v2-entry-snapshot-fallback` 于 `2026-09-03T01:19:26.926895Z` 从策略 2 BUY `155` 后注册。新 exact-paired 仓位优先使用当时已完整入库的买后刷新快照；若仍没有，则只允许使用该 cohort 真实入场前已经完整可用的精确 trigger snapshot 启动调查。`investigation_started_at` 仍必须晚于入场，因而没有未来数据或后来证据倒灌。
- 买后调查的延期准入只在对应策略 3 仓位仍为 `baseline/narrative_runner` 时保留，并以 exact trigger transition 去重；优先级低于精确高影响原帖和新鲜高热 Event，但高于批量 metadata。仓位关闭后不再消耗 Agent。该变化不增加最大 2 个 Agent、不绕过全局/单 Token 冷却、预算、结构校验或证据门。
- Portfolio 现在显示当前版本的 `post_entry_snapshot` 与 `entry_trigger_snapshot` 数量，并单列旧版不可变 seed 总数，避免新 activation 的 `0/0` 被误解为历史丢失。当前界面为 20 个 exact pair、0 mismatch；v2 为 0 eligible/0 seed，旧版 11 条仍可见；runner 仍为 0、禁用。
- `information-first-active-outcome-sampler/v1` 当前为 204 targets：144 observed、5 scheduler missed、2 late、2 terminal missing、50 pending；已终态缺失率为 9/153（5.88%），不是旧 broad dashboard 的约 53%。v2 从 shadow cohort `172` 后注册，把重试点由 `+0/+30/+120/+300s` 改为 `+0/+30/+120/+240s`，在 300 秒右闭截止前为最后请求保留 60 秒，并统一“恰好截止时仍可接受”的边界。
- 6 项集中测试通过；Python compileall 与 JavaScript 语法检查通过。受控重启后 8765/8787/8788 各单 listener，8787 health=true、SQLite 可读、mode=paper、Live=false。本轮未提交或推送 Git。

## 2026-09-03 追加实施：启动者历史前向 Shadow

- 新增 `creator-launch-risk-shadow/v1-local-history-lower-bound`，生产 activation launch fact=`10533`。仅为之后新到的 Solana Pump create 写不可变 cohort；旧 Token/旧 launch 不生成 cohort。
- 每个 cohort 只读取该 launch 入库前已存在的同地址 launch 与已终态结果，冻结 prior launch、24h launch、距上次 launch、240m raw outcome 与 60m liquidity-survival 计数。provider 报告的 `traderPublicKey` 明确不等于 RPC 已验证创建者、开发者经济实体或关联钱包簇。
- 首个自然截面：68 个合格新 create、68 个 cohort、0 个注册前 cohort；46 个具有本机先前 launch，27 个 prior launch≥10，最大 prior launch=194。当前结果尚未成熟，这些数字不构成风险结论或策略门。
- Token 详情 API 与中英 UI 已接入折叠面板，原始 payload/hash 不返回；4 项针对性测试通过，JavaScript 语法通过。浏览器实际展开检查成功。受控重启后 8765/8787/8788 单 listener、SQLite/WAL 与 Paper Runtime 正常，Live 仍锁定；未提交或推送 Git。

## 2026-09-03 追加修复：Strategy 3 延期调查假饥饿

- Strategy 3 v2 已自然产生 3 个 `post_entry_narrative_position` seed，但共享延期队列每 4 分钟只处理 1 项。真实断点不是普通 metadata 的静态分数高于 Strategy 3，而是已 `reused/source_fact_reused` 的 intent 未终止、以及处于 `token_cooldown_active` 的 Token 仍反复进入队首，浪费整个重试槽。
- `due_token_context_active_retries()` 现在把 exact-intent 的 `admitted` 与 `reused` 都视为完成；有效期尚未结束的 Token cooldown 按其真实的 Token-wide 语义抑制该 Token 的全部延期 intent。全局/错误冷却仍只按原有 exact lineage 匹配。没有改优先分数、Agent 并发、冷却时长、预算或任何交易门。
- 两项针对性回归测试通过：复用成功不再重复入队；不同 trigger 的 Token-wide cooldown 能阻止同一 Token 的 Strategy 3 intent 空转。
- 受控部署后的首轮自然 active retry 于 `2026-09-03T02:25:51.907991Z` 成功 `admitted` 一条真实 post-entry intent，并生成 `no_context` assessment；这证明调度槽恢复，不证明叙事有效或策略盈利。当前 v2 的 3 个开放 seed 仍待后续自然轮次依次处理，runner 与所有交易行为保持禁用。
- 同步审查问题已发送到既有 Lead ChatGPT 会话；当前只确认用户消息进入会话，尚未读取到 ChatGPT 回复，因此其意见没有进入本次工程决策。

## 2026-09-03 追加修复：当前版本调查隔离与退出监控公平轮换

- 复核发现 `2026-09-03T02:25:51.907991Z` 的成功 post-entry retry 实际属于已冻结的 context v1；当前 context v2 的 3 个 seed 已超过可重试时间 25–59 分钟，仍全部只有一次 cooldown skip。根因是 active retry 只检查关联策略 3 仓位是否开放，没有限定 seed 必须属于当前 context version。
- post-entry retry 现在必须同时匹配当前 `onchain-paper-narrative-context/v2-entry-snapshot-fallback` seed、exact trigger transition、source BUY 与仍开放的 paired position；旧 v1 行完整保留，但不再竞争当前唯一重试槽。
- 退出监控另有独立饥饿：每轮按最早 `opened_at` 取 3 个仓位，旧仓在 15 秒后再次到期并反复占满队首，导致新仓长期 0 mark。队列现改为“从未标记优先，其后按最旧 last mark”，频率、单轮上限和请求并发均未增加。
- 两项定向测试通过。受控重启后的自然证据：此前 0 mark 的 cohorts `2130/2137/2138/2139` 均获得首个前向 mark；current-v2 seed `12` 于 `2026-09-03T02:49:58.428011Z` 获得第二次 admission 并生成 assessment `1044/no_context`，而不是旧版本再次占槽。
- `no_context` 的当前结果为 0 个独立来源、社区状态 unknown、公众人物 not observed；它是有效负样本，不是买入/延长持有信号。Strategy 3 runner、主 Decision/Paper 和 Live 均未改变；另外两个 current-v2 seed 继续自然排队。
- 标记恢复后又发现 amount-specific 退出报价的单槽被同一失败仓重复占用：cohort `2105` 已 16 次，而多个同级待退出仓仍为 0。报价队列现同样先服务从未尝试的 pending mark，其后按最旧上次尝试轮换，退出动作优先级只作为同等等待时的次序，不再允许无限重试垄断。
- 再次受控部署后，原先 0 attempt 的 cohorts `2110/2112/2113/2117` 已依次得到首个真实 Jupiter 卖出尝试，终态包含 `no_route` 与 `quoted_but_uneconomic`；均未伪造成 SELL/PNL。三项集中测试通过。

## 2026-09-03 追加实施：20 USDC 公平成本版本

- 用户指定的新前向执行假设已冻结为：每次入场名义金额 `$20`、BUY 与 SELL 各 `4%` 不利滑点、每次经济成交固定扣除 `$0.40`。分批退出的每一个 SELL 都单独扣 `$0.40`；已有成交、旧仓位和旧版本结果不回写。
- 新链上版本为 `onchain-only-shadow/v2-20usdc`、`onchain-only-shadow-jupiter-quote/v2-20usdc-400bps`、`onchain-paper-exploration/v4-20usdc-flat040`、`onchain-paper-exit-challenger/v4-20usdc-flat040`、`onchain-paper-narrative-runner/v2-20usdc-flat040` 与 `onchain-paper-narrative-context/v3-20usdc-entry-snapshot`。注册时间从 `2026-09-03T03:33:04Z` 起，旧版本保持不可变。
- `fair-comparison/2026-09-03-20usdc-v2` 于 `2026-09-03T03:37:02Z` 启动；三种 Paper 策略均从 `$1000` 新前向起点开始。主策略旧累计现金 `$1001.7325`、11 笔历史成交作为 prior 保留，不进入新曲线。
- 在价格完全不变且不计 Token tax 的简化往返中，`$20` 加双边 4% 与双边固定 `$0.40` 约产生 `$2.3385` 净损失；标的价格约需上涨 `12.67%` 才达到盈亏平衡。该高成本假设会显著压低短线策略收益，是用户指定的保守实验条件，不应误称真实链费。
- SQLite 的版本触发器已由旧 Shadow 字面量改为核对注册定义中的 `source`，因此允许新的不可变版本链，同时仍拒绝跨版本、错时钟、错 Token、错金额或错报价 lineage。8 项相关定向测试通过。
- 主信息策略的未来成交已使用 20/4%/0.40 配置，但其策略定义仍未像链上策略一样拥有独立 machine-readable `definition_version` 外键；当前可追溯性来自 config、成交实际成本、fair epoch 与本快照。后续应只为新成交增加前向 policy version，不回填旧成交。
- 当前运行范围继续是 Solana-only。BSC/Base/Robinhood 的只读 EVM Quoter 研究表保留但不进入 Candidate、Paper 或 PNL。外部能力已变化：0x 当前支持 chain 56、8453、4663 的 Swap/Gasless；若未来恢复多链，优先以 amount-specific 0x executable quote 替换当前仅覆盖 Uniswap V3 pool math 的研究层，并分别补齐动态 gas/L1 data fee、allowance/nonce、transfer tax/blacklist、安全覆盖和稳定币会计。
- 受控部署后 `memeTrader Paper Bot`、8787/8788 Web、SQLite/WAL 与 Bridge 正常，mode=`paper`，Live=`false/locked/unavailable`；本节随后按用户明确要求随当前源码提交并推送。

## 2026-09-03 追加实施：0x 前向观察闭环与 C2C V3

- `onchain-only-shadow-evm-0x-price/v1-20usdc-forward-only` 已加入独立 append-only registration/attempt/result 表和 Runtime worker。它只观察 BSC/Robinhood 新 Shadow cohort，Robinhood 必须先通过官方 Stock Token/RWA exact-address 排除；所有结果固定 `decision_eligible=0 / affects=none`，不会进入 Decision、Trade、Paper、现金、权益或 PNL。
- activation 只在 `MEMETRADER_ZEROX_API_KEY` 存在时冻结；当前受控部署环境中该凭据不存在，所以 Web 正确显示 `not_registered/configured=false`，没有把注册前 cohort 回填、也没有生成假 0x 结果。凭据值从未写入数据库、API、日志或聊天。
- 0x `/price` 仍是 indicative observer，不是 firm/executable。BSC/Robinhood Paper 继续被 firm `/quote`、taker、allowance、buy/sell tax、transfer/sellability、动态 gas/L1-L2 成本与 fork/simulation 成熟门阻塞。
- 6 项相关测试通过；Python 编译与 `git diff --check` 通过。受控重启后 8787/8788 均监听，health 为 Paper、SQLite WAL 可读、single-instance lock 存在、数据库活动新鲜、Bridge healthy、Live locked。
- 正式 ChatGPT↔Codex 消息协议已统一为 `GXH_C2C_V3`，并复用原 `CHATGPT_CODEX_SYNC_STATE.json`、`CHATGPT_CODEX_SYNC.md`、`COMMON_SPACE/`，没有新建竞争账本。首条 V3 RESEARCH `C2C-20260903-084819-CODEX-EVM-S3-RESEARCH` 已获发送工具接受，但两次目标会话回读未出现 exact ID，故状态严格为 `DELIVERY_UNVERIFIED`，未宣称送达或重复发送。
- 当前下一策略断点仍是 Strategy-3 动态 information treatment 的预注册设计；在估计量、cohort、分母、动作时钟、no-future-data 边界和 promotion/stop gate 固定前，现有 fixed control 不变。

## 2026-09-03 追加实施：S1 精确身份检索与未变化 WAIT 复用

- `exact-source-link-identity-and-unchanged-wait/v1` 已进入生产。Event 的 `url/source_item_id` 只查询决策时已经记录的 Token exposure/source-link，形成有界、去重、按链过滤的 identity set；它不增加 match/quality 分数，不是独立证据，同帖多 Token 继续显示 fanout 并受 canonical 门约束。
- `no_matching_token` 的宽检索结果现在保存 accepted Observation、exact identity、direct/Agent binding、本地 overlap Token 与候选集合 fingerprint。输入未变化时在原 broad retrieval 后最多 300 秒复用 WAIT；新 Observation、identity membership 或本地 overlap Token 立即失效，300 秒有界检查点无条件重跑，因此不会形成永久负缓存。
- 针对性验证 `2 passed`，最终小改后聚焦验证 `1 passed`；此前 identity tranche 后全套 pytest 已 100% 完成无失败。受控部署后任务 `Running/IgnoreNew`、Paper、SQLite 新鲜、single-instance lock、Bridge healthy、Live locked。
- 首次生产查询尚无 `exact_source_link_identity/v1` 自然 ranking。当前只证明工程已激活，尚不能声称改善转化率、盈利或误绑定率；后续按前向 unique event 重复工作量、identity-set 非空率、ranked conversion、false canonical 与 safety reject 评价。
## 2026-09-03 09:42 UTC — P0-B Token-first WATCH observer active

- Activated `token-information-watch/v2-120s-exact-cross-source-observer` at funnel transition `715719`; no historical cohorts were backfilled.
- New eligible Solana on-chain triggers now create an append-only 120-second pre-entry WATCH and a one-shot `pre_entry_token_watch` investigation.
- The watch lane bypasses only the ordinary global Token Context cooldown; concurrency, quota/budget, token cooldown, output validation and Live lock remain unchanged.
- Frozen confirmation requires exact Token binding, cross-source support, verifier confidence `>=0.80`, and at least two independent origin domains. Explicit false/correction/retraction/satire/impersonation is separated from insufficient evidence.
- This deployment is observer-only (`buy_enabled=false`, `decision_eligible=0`, `affects=none`). Fresh final Jupiter re-quote, safety recheck and isolated S3 Paper BUY are the next engineering tranche.
- Targeted validation passed; scheduled Paper runtime restarted healthy; SQLite healthy; Live locked.

## 2026-09-03 10:00 UTC — Strategy 3 authority correction and UI/API exposure

- Product authority remains three accounts with two entry logics: Strategy 1 uses information plus Token evidence; Strategies 2 and 3 use the same token-only entry, while Strategy 3 studies post-entry information. The 120-second pre-entry WATCH remains an observer and is not a fourth strategy or a source of Paper buys.
- Web API now exposes the WATCH definition, activation point, forward cohort/state counts, and immutable `entry_enabled=false / decision_eligible=false / affects=none` flags under `research_observers`.
- Portfolio UI now labels Strategy 2 as token-only with fixed and dynamic exit arms, labels Strategy 3 as token entry followed by post-entry information research, and presents the pre-entry WATCH in a separate collapsed research panel.
- Targeted backend test passed; JavaScript syntax check passed. Live browser QA confirmed the corrected labels, observer separation, live runtime indicator, and no console errors. Web remained Paper-only and Live locked.

## 2026-09-03 10:07 UTC — S2 dispatch recovered; clean S3 control observed

- The earlier Jupiter-v2 zero-attempt snapshot is superseded. Current r6 has 10 natural `onchain-only-shadow-jupiter-quote/v2-20usdc-400bps` attempts: five valid baseline BUY quotes plus valid target SELL/no-route attempts. Request delays are approximately 2–13 seconds and all source cohorts are post-registration; no historical cohort was backfilled.
- The historical dispatch blocker was a stale loaded Runtime combined with a Runtime→Store signature mismatch that prevented the corrected checkout from starting. Current signatures agree and the focused shared-budget dispatch test passes.
- `onchain-paper-narrative-runner/v3-fixed-baseline-20usdc-flat040` now has five exact Strategy-2 paired BUYs and one paired SELL. The closed pair has identical time, gross amount, modeled fee and cash flow in S2 and S3. Older confounded runner versions remain immutable.
- The machine-readable portfolio contract now exposes each concrete arm's strategy family, signal/entry/sizing/exit/cost/execution versions, activation, policy role and promotion state. Targeted Web test passed; the deployed API reports Paper mode and Live locked.
- Lead's first WATCH/dynamic-exit natural sample was acknowledged. Two WATCH cohorts expired for insufficient confirmation, not capacity failure. One large dynamic-exit paired winner and one partial exit before liquidity death remain sparse forward evidence only; current thresholds are unchanged. Repeated failed liquidity-exit retries are preserved as a separate scheduler-policy candidate pending deadline/lock evidence.
## 2026-09-03 10:14 UTC — S1 exact-identity route bridge activated

- The already-active exact source-link identity retrieval now feeds the existing Jupiter two-way capacity probe as addressability only.
- Future route-probe rows use `event-context-jupiter-route/v2-exact-identity-addressable`; no v1 row was rewritten.
- Identity still provides no evidence/score/canonical/Safety/Paper privilege. Canonical ambiguity blocks before probing, and a valid route does not bypass `buy_flow_too_weak` or any other Safety reason.
- Focused validation: `4 passed`. The existing scheduled Paper Runtime was restarted once; Web/SQLite/Bridge are healthy and Live remains locked.
# 2026-09-03 10:37Z — Superseded S3 branch stopped before first evaluation

- A confirm-before-entry isolated Paper registration was briefly created from superseded handoff `005`, with activation frontier WATCH transition id 6.
- Later and more specific authority `C2C-20260903-103109-CHATGPT-STOP-ISOLATED-S3-RESTORE-P0` restored the product definition before any evaluation, BUY, trade or position occurred.
- Runtime registration/claim/finalization calls for that branch are disabled. Its empty schema and registration remain immutable research provenance; API marks it `superseded_research_only / execution_enabled=false`.
- Product Strategy 3 remains exact Strategy-2 entry followed by post-entry information research. WATCH remains observer-only and has no entry authority.
- Current priority is venue-aware Solana rug/custody safety, adaptive mechanical exit and Strategy-1 exact-size execution parity. Live remains locked.

## 2026-09-03 13:29Z — Exact held-account monitor and confirmed-rug terminal deployed

- `onchain-held-account-monitor/v1-exact-canonical-pumpswap` now subscribes only to the exact pool, base vault, quote vault, Token mint and LP mint belonging to forward-eligible canonical PumpSwap Paper positions. Each subscription set first records a confirmed RPC baseline, then processes `accountSubscribe` changes; no Agent participates in this exit path.
- Missing/identity-changed exact accounts, empty vaults, at least 90% vault depletion from the prior confirmed state, or LP supply reappearing create an immediate full-remaining `LIQUIDITY_EXIT` mark ahead of ordinary background exits.
- `onchain-paper-exit-quote-scheduler/v2-rug-terminal-no-rearm` confirms rug only when that exact ALERT is followed by a fresh Jupiter quote for the entire remaining raw quantity and the result is `no_route` or economically unrecoverable. Confirmation writes the position off once and appends immutable confirmed-terminal and dead-market-surface rows; the same mint/pool/current policy cannot retry, rearm or re-enter.
- A lone Jupiter `no_route`, DexScreener liquidity `0`, timeout, transport error or protocol error does not confirm rug. Ordinary failures continue to use the existing finite 15/30/60/120/300-second retry schedule.
- Focused Store, collector, Runtime, surface-gate and regression tests passed; Python compile passed. The scheduled Paper Runtime was stopped and restarted once and is running with the v2 scheduler and held-monitor registration. Live remains locked.
- The current r6 database has four older open challenger positions but zero exact monitor targets because none has a forward `market-surface-safety` PASS under the newly frozen lineage. They were not backfilled. The next truth checkpoint is the first naturally eligible post-registration position and its first exact account notification.

## 2026-09-03 17:36Z — v5自然尾部、单主升浪证据与v6 RiskKernel优先级

- 用户最新明确指定本项目始终使用`@笔记本量化MCP-官方隧道`，并提出“多数新Meme只有一波、最高点持续回撤后大概率结束”。该假设已作为可证伪active prior进入计划，不是直接写死为单一阈值。
- r6描述性检验：约4,084个Solana覆盖合格Token group中，首次30%运行中高点回撤后15/60/240分钟再创新高约8.33%/11.93%/13.82%；要求3分钟内第二观测仍低于阈值后，Pump地址子集60/240分钟约2.90%/6.82%。133个持续30%回撤事件仅12个后来创新高，0个在10分钟内发生，支持“先退出，真正复苏时注册REAWAKENING新cohort”，但当前四日窗口、稀疏采样与右删失不允许把30%视为最终生产参数。
- v5继续产生自然流量；17:36:39Z截面为22个底层cohort、175个BUY strategy-account fills、160个SELL fills、83开放/92关闭、0 writeoff。它仍是`ORDER_KERNEL_PILOT`：当前`/order`无taker，只是quote-simulated fill，不是buildable或confirmed成交。
- 唯一机会收益比账户复制收益更差：一个10个底层机会均关闭的截面，代表性20U路径合计约`-13.579866U`、3胜7负、中位约`-1.975839U`；Top1 `+37.913424U`，移除后约`-51.493290U`。同一时点复制策略账户已实现合计约`+225.812907U`，不能称系统利润。cohort2285一个底层赢家被12账户各记`+37.913424U`，必须标behavioral equivalence。
- cohort2286只进入宽Stage1且无held targets；全仓minimum recovery约20秒从`20.5434U`跌到`0.0062U`，最终`-19.9937U`。quote vault约1512 SOL降至1.10 SOL、base vault大增，属于卖压抽干而非LP removal。
- cohort2298虽有五类targets，quote vault仍在入场后约19.65/19.98/20.46秒降到baseline的38.83%/16.66%/5.37%，约24秒低于1%，base vault升至约7.25倍；风险状态因只认单步90%下降或双边耗尽而保持HEALTHY，最终等Dex liquidity<3000才以每账户约`-19.664298U`退出。单边quote reserve collapse应为RED，joint exact terminal+full-size不可卖才是DEAD。
- cohort2306每步不足10%的渐进reserve恶化未进入append-only risk events，约87秒每账户`-11.881309U`；mutable latest state无法支持1s/3s/10s/30s slope学习。未来版本需PositionRiskFrame或等价有界窗口账本。
- 13/13已观察v5 LP mint实际由Token-2022 program拥有，但目标硬编码legacy Token program，全部形成`account_program_owner_mismatch`固定误报；历史不改写，新monitor version按真实account owner冻结。
- `SolanaHeldAccountCollector.stream`在target fingerprint变化时整组断线重订阅；频繁新仓会造成O(N) churn和ENTRY_HOT gap。下一P0为所有仓位coverage、连续reserve/flow/recovery语义、增量/分片订阅和SELL抢占；Stage4买后信息衰减实验保留但不抢占该正确性主线。
- 权威研究与实施合同：`CHATGPT_SINGLE_WAVE_PEAK_EXIT_AND_MARKET_GRADE_PROFIT_PLAN_2026-09-04.md`；增量C2C：`C2C-20260903-173639-CHATGPT-V5-TAIL-RISK-UNIQUE-PNL-P0.md`。Live继续锁定。

## 2026-09-03 17:53Z — PumpSwap有效储备口径修正与本地风险报价P0

- 上一节2298的38.83%→0.44%是**真实WSOL vault相对raw baseline**，用于资金流；不是完整定价深度。官方Pool另含`virtual_quote_reserves`。该pool账户hash在观察窗口未变、virtual reserve为约17.584505288 SOL；effective reserve=`real+virtual`，正确比率约46.97%→27.75%→17.96%→13.82%→13.69%。结论仍是约20秒内严重卖压和约86.3%有效深度崩塌，但raw flow与effective depth必须永久分开。
- 当前官方Pump AMM IDL已保存到`data/tmp/pump_amm_official_20260904.json`，SHA256=`6b5c7ec4e5ef9742fa99dc57b0d75b1031b379bba02a7e1b3c5a4cad68d77e56`；官方NPM SDK为`@pump-fun/pump-swap-sdk 1.19.0`，导出`POOL_ACCOUNT_NEW_SIZE=300`。
- 18个抽样current mainnet exact pool account均为301 bytes；当前Python只解到211 bytes，遗漏`coin_creator/is_mayhem_mode/is_cashback_coin/virtual_quote_reserves`。18/18 virtual reserve非零，常见约17.584505288 SOL；2个cashback、0个mayhem。项目当前没有virtual reserve或Pump FeeConfig实现。
- 官方SDK`sellBaseInput`以effective reserve做constant-product计算，结合GlobalConfig/FeeConfig、market cap、canonical identity与creator/cashback动态费用，检查真实vault coverage后应用slippage。新版本必须按IDL/SDK hash冻结完整decoder、本地direct holding-surface risk quote和differential tests；它不替代Jupiter full-route execution truth。
- 27个v5 cohort延迟：source quote→next BUY quote-simulated fill p50/p95约8.607/16.249s，provider call p50/p95仅约0.526/2.819s；最终BUY minimum output有6/27相对source恶化>2%、2/27>5%、最差约13.95%。首个全仓估值仅18/27存在，3/27在10秒内，p50约208.79s、p95约669.54s。主要延迟在本机调度与风险覆盖。
- 新增C2C修正：`C2C-20260903-175300-CHATGPT-PUMPSWAP-EFFECTIVE-RESERVE-LATENCY-P0.md`。P0顺序为完整官方Pool decoder/FeeConfig→all-position local risk baseline与SELL抢占→continuous frames/incremental subscriptions→v6 3×4。历史row不改，Live锁定。

## 2026-09-03 19:12Z — “待可执行报价”根因修复与自然验收

- 该问题不是UI文案：修复前19个开放v5底层cohort中仅8个曾获得估值，11个从未发出估值请求。根因一是链上发现通道可占满每5秒3个Jupiter后台请求；根因二是估值选择器按仓位年龄排序，已报价旧cohort持续刷新而新cohort永久饥饿。
- 在不增加并发和总epoch上限的前提下，chain-only发现通道最多使用2个请求，保留BUY→精确反向预检；实际SELL继续最高优先；BUY与估值轮换；估值先处理从未报价者，再按最旧完成时间刷新。chain-only不再为冻结旧challenger消耗实时估值容量，发现轮询为15秒、当前v5策略循环为3秒，所有Jupiter请求仍受2.1秒host interval串行约束。
- 自然前向结果从8/19变为19/19都至少获得过一次指定剩余数量、400bps的有效Jupiter最低回收报价，修复了“新仓永久拿不到第一次估值”的饥饿。8790 `/api/state`曾自然达到12/12策略`complete_exact_jupiter`、汇总149/149；但精确报价会过期，keyless配额还与真实执行和发现共享，所以跨刷新窗口仍会短暂回落为部分仓缺少新鲜报价，不能声称永久全量实时完整。近期provider/protocol error=0，新增数值0估值=0。
- 精确持仓监听同时修复了另一真实停滞：开放仓目标超过Solana `getMultipleAccounts`单批100账户上限后，旧实现一次请求全部目标导致状态不再前进；现按最多100账户分批，120个当前目标已恢复更新。该修复恢复pool/vault/mint事实采集，不是UI遮挡。
- 缺失、陈旧、no-route与provider error仍为`UNKNOWN/null`，不会写成0；没有回填、重写历史、放宽入场/退出门或改变策略参数。Live保持锁定。本地PumpSwap官方公式仍是降低风险响应延迟的独立P0，不替代Jupiter执行权威。

## 2026-09-03 20:18Z — 报价节拍修正、本地池面预警与真实退出闭环

- Jupiter host limiter 已从“响应结束后再等2.1秒”修正为请求开始时间之间至少2.1秒；chain-only主循环由3秒缩至1秒，使keyless 0.5 RPS成为真实节拍上限而非额外叠加网络延迟。近期自然报价无429/provider error，但20余底层持仓仍会因0.5 RPS硬容量出现短时陈旧；该状态继续显示`UNKNOWN/null`，不以0或本地估算掩盖。
- `chain-meme-trader-local-surface-quote/v1-pumpswap-sdk-1.19.0` 已按同一confirmed `getMultipleAccounts`上下文持续生成exact-remaining direct PumpSwap raw minimum；它只作为canonical池面风险真值，不进入美元权益、PNL、Fill或策略排名。公共Solana RPC曾真实返回429/WS断连，运行时现对失败退避并在全部帧未知时明确写入不健康心跳，不再把去重后的错误行误计为成功。
- 新的严格前向风险合同`local-surface-critical-exit/v1`注册于local quote id `3211`之后。只有同cohort、同remaining amount、coherent slot、5秒内且状态为`LOCAL_SURFACE_CRITICAL`的新帧可创建全仓SELL OrderIntent；DEGRADED、NO_DIRECT_CAPACITY和全部UNKNOWN状态不能直接卖出，本地层也不能自行Fill或核销。
- 首个自然前向触发为cohort `2297`：20:17:56.540Z出现CRITICAL，同一底层机会仍开放的9个Stage账户生成SELL；20:17:58.091Z由一次新鲜Jupiter full-remaining报价完成9个独立账户Fill，每账户回收`0.572627U`。这证明预警→卖出意图→精确执行链真实运行，而非UI遮挡；历史Quote、PnL和已结算交易均未回写。
- 针对性测试通过；Paper Runtime受控重启，8790 API、SQLite、local-surface与chain-meme-trader心跳均正常，Live保持锁定。Runtime已支持通过仅存在于进程环境的`MEMETRADER_JUPITER_API_KEY`发送`x-api-key`并自动切换至1 RPS节拍，当前未配置且不在仓库保存Secret。若产品目标要求所有hot持仓始终在30秒内具备fresh aggregate executable PNL，仍需要至少1 RPS的Jupiter正式额度；这属于真实容量前置而非会计语义变更。

## 2026-09-03 20:35Z — Stage4 v2 报价饥饿修复与真实亏损闭环

- 严格配对的`chain-meme-trader/stage4-executable-equity-paired-v2`曾停在20:07:37Z、51个quote result。根因不是没有仓位，而是chain-only报价轮换只包含v5，导致该前向学习实验永久得不到新的指定剩余数量估值。
- 运行时现在把v5与Stage4 v2放入同一公平报价轮换；当二者cohort、Token、remaining raw amount和400bps完全相同时，一次Jupiter provider observation可投影为两个版本各自独立、不可变的attempt/result/frame。该共享不增加provider请求，也不把两个账本当成独立市场证据。
- 部署后Stage4 v2从51个quote result自然增长到111个、position-equity frame增长到110个，覆盖16个严格注册后cohort；针对性测试`3 passed`，无历史回填。
- 恢复后的新鲜Jupiter最低回收报价已让6个cohort的两条配对arm共同触发流动性退出并真实结算。代表性回收额为`0.005392U`、`0.039305U`、`0.158279U`、`0.464502U`、`0.542166U`和`17.825691U`，对应每20U仓位绝大多数接近全损；这修正了屏幕价格/缺失流动性可能制造的虚假未实现PNL。
- 两条arm当前各7个closed、9个open，累计已实现PNL均为`-108.209064U`。它们目前仍主要由共同流动性风险触发相同退出，尚未出现可归因于28%与15%可执行高水位回撤差异的自然分叉；因此不能宣称任何退出策略更优。
- `UNKNOWN/null`与真实经济0继续严格区分；页面缺少fresh exact quote时不得显示0、不得使用本地PumpSwap估算填补。Live保持锁定。
- 该语义已落实到存储和Web：新`no_route` valuation的`gross_usdc`写为`NULL`；若开放仓为0个fresh exact quote，则“已报价回收/PNL小计”也返回`NULL`并显示`UNKNOWN`，而非`$0.00`。只有至少一个开放仓具备新鲜报价时才展示明确标注的已报价子集；2项针对性测试通过，Paper与8790已受控重启。

## 2026-09-03 22:55Z — 单池容量误判修复、v9前向边界与PNL真值分层

- 自然样本证明旧v8把`LOCAL_NO_DIRECT_CAPACITY`错误提升成了全市场不可卖：cohort 251的Pump曲线真实quote reserve不足，但同期Jupiter对完整剩余数量仍给出约`17.268294U`最低回收。单一Pump曲线容量不足只是一条池面观察，不能证明聚合市场无路由、不能触发卖出、更不能核销20U。
- 该分支已从退出合同移除；只有新鲜`LOCAL_SURFACE_CRITICAL`仍可触发一次全仓Jupiter SELL验证。旧v8数据未删除或改写，已明确停止并由`chain-meme-trader/v9-entry3-exit4-local-observer-forward`在新激活边界后继续严格前向。
- v9自然截面：60个BUY intent全部filled、40个SELL intent全部filled；209个开放策略仓、31个关闭、0个writeoff。15个`LOCAL_SURFACE_CRITICAL_EXIT`已由Jupiter成交；大量`LOCAL_NO_DIRECT_CAPACITY`不再制造退出或核销。
- 8790现在分层显示并持久化：完整Jupiter可执行PNL、已报价子集、Pump本池估算、正流动性DEX参考PNL。精确缺失保持`UNKNOWN/null`；参考PNL使用虚线和覆盖数显示，未知仓位不补0，也不把参考价格冒充可执行成交。
- 当前真实硬限制仍可见而未被遮挡：代表性宽口径账户50个开放仓中，仅7个拥有新鲜Jupiter报价、1个拥有可用参考估值，所以完整总PNL仍为`UNKNOWN`，参考PNL小计约`47.04173U`。下一估值P0是从Jupiter route/pool事实补齐聚合市场面和提高exact-size估值覆盖，不是放宽UNKNOWN语义。
- 针对性测试与全量pytest均通过至`100%`；Runtime、SQLite及独立8790 Web持续运行，Live锁定，没有Git提交或推送。

## 2026-09-03 23:42Z — `no_route`虚假核销根因修复与v10干净前向时期

- 这次处理的是会计/执行真值，不是UI遮挡。生产复核发现v9会把普通Jupiter `no_route`在最大持仓期限或重试耗尽后直接记为`WRITEOFF`；停止前已产生32条`shared_max_hold_240m:...:UNKNOWN_NO_ROUTE:no_executable_sell_writeoff`污染记录。它们保留为不可篡改历史，但v9已停止，不再作为新策略表现。
- 新版本`chain-meme-trader/v10-entry3-exit4-route-surface-forward`于`2026-09-03T23:40:45.727649Z`从snapshot `804367`激活。全亏边界现为：精确held-account链上风险事实产生`RUG_EXIT`或已有confirmed terminal，随后对真实剩余raw数量的新鲜Jupiter SELL仍为`no_route`，才核销剩余成本。普通`no_route`、到期、重试耗尽、单池无容量、价格不动、provider错误均保持`UNKNOWN/null`，不记0也不记全亏。
- Jupiter BUY、SELL和valuation的`routePlan`已随不可变结果持久化。PumpSwap route leg只能作为surface candidate；必须由RPC核验pool owner/discriminator/mints/vaults/token programs后，才能对完整remaining raw生成本地exact单池估算。Pump bonding curve继续是分离的局部观察，不能代表聚合市场不可卖。
- 公共Solana官方端点连续出现429后，运行配置切换到公开RPC `https://solana-rpc.publicnode.com`；本地池面观察恢复。该RPC是当前运行依赖，不改变Jupiter聚合执行权威。
- 新时期首个自然截面已不再是“零交易”：6个broad-launch cohort完成Jupiter-backed BUY并投影为24个开放策略仓，随后继续自然增长；所有已完成BUY结果均保存route plan。同期一部分完整剩余数量SELL valuation可报价，另一些真实返回`no_route`，后者保持未定价且没有v10 `WRITEOFF`。
- 定向ChainMemeTrader/Jupiter/local-surface回归47项通过；随后完整pytest运行至`100%`、exit code 0。Runtime、SQLite及独立8790 Web已受控重启并加载v10；Live继续锁定。当前没有Git提交或推送。
- release online doctor除Jupiter单点外全部通过：DexScreener、GeckoTerminal、安全服务和9个RSS均可达，SQLite integrity=`ok`；doctor自身对keyless Jupiter的额外请求命中HTTP 429。常驻Runtime同一时段仍自然记录59个有效execution quote、107个有效valuation quote和94个真实`no_route`、0个provider error，说明服务在运行但keyless额度是明确容量限制，不得把它伪装成全量实时覆盖。
