# ERR-20260905-004：DexScreener 市场标记连接超时

状态：`FIXED`

## 1. 事件

- 错误编号：`7`
- 组件：`chain-meme-market-marks`
- 时间：`2026-09-04T19:56:08.959007Z`
- 类型：`ConnectTimeout`
- 严重程度：高

## 2. 影响判断

这是一次外部行情连接瞬时失败，并于 `2026-09-04T20:02:31.610464Z` 再次发生一次。它不是策略缺陷，也没有证据表明污染了 PNL、交易记录或持仓状态。Runtime 未因此停止，后续行情采集继续恢复。

## 3. 恢复证据

`source_health` 中 `chain-meme-market-marks` 已于 `2026-09-04T19:58:09.512587Z` 恢复成功；第二次错误后又于 `2026-09-04T20:08:16Z` 及后续 heartbeat 恢复成功，`last_error` 为空。由此确认两次事件均已自行恢复，无需代码改动。

## 4. 当前处理语义

- 使用有界请求超时和失败退避，随后由下一轮重新获取，避免一次连接失败阻塞主循环。
- 按链隔离行情采集故障，单链异常不扩散为全系统故障。
- 当前失败批次返回失败状态并等待下一轮重新获取，不用失败响应覆盖有效旧 mark 或制造 PNL。

## 5. 复发时的快速排查

1. 检查 `source_health` 的 `last_ok_at`、`last_error`、连续失败次数和恢复时间。
2. 确认是否仅 `chain-meme-market-marks` 单独失败，还是多个数据源/链同时失败。
3. 确认 Runtime 心跳、持仓状态、Decision/Trade 写入和 PNL 计算仍在推进。
4. 只有出现连续失败、行情长期不更新、Runtime 心跳停止，或 PNL/交易记录异常时，才升级为新的工程事件并暂停相关派生处理。

本次两次错误均未达到该升级条件。

## 6. 结论

本次事件属于已恢复的瞬时外部连接故障。没有代码修复项，不应作为策略表现、策略规则或 PNL 质量的证据。
