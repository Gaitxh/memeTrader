# 资源约束策略研究：实现与自然部署验收

日期：2026-09-07。代码提交 `fdb26b5` 已推送至现有分支。证据文件为 `RESOURCE_BOUND_DEPLOYMENT.json`；以下时刻除特别说明外均为UTC。工程接线通过不代表alpha或真实链上成交能力。

## 实现与定向检查

新增三个机制、六个候选/对照臂：池龄归一化活动加速、涨后降温端点守位、盈利支撑保持时钟豁免。机制及其反证见 `RESOURCE_BOUND_DESIGN.md`。复用现有观察、注册、后观察成交、会计和退出链路，只投影原始快照已有的五个可空字段，没有新增表、网络请求、采集频率/额度、后台任务或自动研究。

22个新纯函数用例、5个Store集成用例、33个相邻finalists/round2用例最终通过，共60个不同定向用例；`git diff --check`通过。先前缩进错误和费用测试夹具问题已修正，不把失败执行计为通过。没有跑全套测试、回测或参数搜索。

Store检查覆盖追加注册幂等、旧合同保留、SOL/BSC身份路径、候选独立拒绝、首次共同机会持久化、拒绝后不挑有利时点重入、共同后帧BUY、支撑豁免后条件失效SELL及净回款。纯函数检查覆盖缺失/零基线、池龄窗口、费用尺度、未来/重复/过时帧、第三点才确认支撑、换源和观察间隔清空支撑。

## 部署边界与账户保留

- 11:07:47.4538717Z启动部署；停止已核对的Paper进程树19656/20048/10988/16484，使用现有 `scripts/run_paper.ps1` 隐藏启动新supervisor24728。没有重启Web或操作8787/8788旧服务。
- 六臂实际激活：11:07:49.157685Z，北京时间19:07:49；统一snapshot frontier1527168、evaluation frontier723393。每臂有独立ID和行为hash。
- 资金期仍是 `chain-meme-trader/funding-20260906-v002-final-1000`。新臂各1000U初始资金、5U名义额、最多4仓；旧224账户没有初始化或补款，既有开放仓照常运行。
- 前后原注册、资金activation和97份旧追加合同的SHA摘要完全相同。资金/仓位余额允许由期间真实模拟成交变化，不将余额不动作为保持历史的定义。
- `/health`返回ok、running及原资金期；`/api/live`显示230策略，Paper开启、Live关闭；`/api/performance`有实际进度。

原边界摘要：注册 `b1e007a107ed5e4f4266a622dde33e176bc71f6b99e2f8e44f89206bca5e5faf`；资金activation `25585dd03571aaa590dbe494b70c17842e3e4b1583a0b5a992a9e6211538f857`；旧追加合同 `44e03e72a0dd503a496d14238772a72b076178272996b1579c7b77f75283d8e0`。

## 已实际观察到的自然进度

验收截止11:10:07.218776Z：本批10 BUY、5独立Token、10开放仓，无本批SELL。降温两臂各3仓（RH），盈利支撑两臂各2仓（SOL/BSC）；池龄两臂暂无BUY。没有强制制造成交，也没有由零成交推断机制无效。

共同入场例cohort59493：冻结的首次机会决策11:08:10.818260Z；实际fill对应snapshot1527311，observed_at=11:08:27.956014Z、ingested_at=11:08:28.218012Z，成交记录11:08:28.320776Z。观察晚于信号决策17.137754秒。使用冻结机会的decision_at，不能把成交时新建cohort的时间误当原始信号时间。

该后帧原始价0.00009014U，流动性31977.58U；买侧4%后执行价0.0000937456U，5U买入53335.8365619293枚，两臂共享fill59159。降温候选当时g55=2.773093、g5=−0.060387、成交额速率比0.576219，字段来自当时报告的回看聚合。它是端点条件成立，不证明盘中完整平台、独立钱包流入或买后延续。

自然BUY验证接线及实际成本计算。此截面尚不能验证新退出SELL、盈利支撑专属豁免的收益、池龄拒绝的选择效果、长期频率或任何正期望结论。

## 短窗性能与真实行情限制

对比11:06:20Z部署前与11:10:07Z验收截面，两侧主循环各120样本。负载和启动阶段不完全相同，不能据此归因提速。

|实际计时|部署前P95秒|部署后P95秒|
|---|---:|---:|
|主循环间隔|1.1438|1.2067|
|持仓行情循环间隔（120/114样本）|1.7043|1.6210|
|持仓获取耗时|1.1593|1.2782|
|持仓应用/退出耗时|0.0358|0.0382|
|单Token模式计算耗时|0.0991|0.0984|

短窗未见核心路径明显退化。pattern周期部署后仅9样本，不能把其P95较低宣称为性能改善。心跳和循环速度也不是行情新鲜度：验收时SOL持仓原池观察最老63.1841秒、6项failure；RH最老53.1672秒、1项coverage gap；BSC最老0.1943秒。现有免费源缺口仍在，不能称全系统实时或所有仓位可及时执行。

## 研究边界与未解决证据

指定Chat正文通过应用任务读取接口完整取得；请求的官方隧道两次Session terminated，不能声称隧道访问成功。本次没有发送Chat协作消息或采用未返回的Chat结论。

外部官方公告、执法及市场统计能约束假设，但没有得到完整、无幸存者偏差的外部赢家/死亡原池事前重放。项目最近固定1000个cohort在当前outcomes-v1没有0/15/60/240分钟记录；没有记录不等于无收益、死亡、待成熟或永久不能登记。完整未成交结果比较仍受限，未补写历史或扩张采集任务。

D–H保留具体设计，不标为已经实现。没有修改旧策略定义、回填获利者、删除失败、重置账户、恢复自动复盘或启用Live。最终交付为证据、八个机制方案和其中三个机制的独立自然前向实现；经济有效性需要后续自然样本判断。
