# Full-scope reread and execution correction, 2026-09-17

The user's two newest attachments are byte-identical. The full 12-section request and all 125 ordered sample-address lines were reread; `memory/USER_EXECUTION_REQUEST_20260916.md` has all 282 content lines in the same order but LF/final-newline bytes differ. Two independent read-only agents audited strategy and full-chain coverage. The authoritative task-to-acceptance map is `memory/ACTIVE_EXECUTION_SCOPE_20260917.md` with details in `memory/EXECUTION_TASK_LEDGER_20260916.md`. Stale 2026-09-14 and 2026-09-10 state headers are now labeled historical, not live instructions.

The deliverable is **not** a dashboard full of trades or a larger list of strategy IDs. It is a strictly as-of, costed, risk-aware Paper system that shares free discovery/data efficiently, evaluates plausible mechanisms, enters only on verifiable original-pool conditions, exits while executable, accounts for complete losses, and learns from independent natural token outcomes. No Live authorization, new funding reset or scheduled review exists.

The review identified five open groups: (1) a full-unit causal funnel for reduced trading; (2) reliable fresh exact-original-pool followup and safety-to-source-BUY conversion without starving held/SELL work; (3) source-visible and pool-created timelines for the 94 distinct retrospective research keys plus ordinary/failed controls; (4) every active/paused/retired rule version's behavior, overlap, frontier and reasoned keep/revise/retire decision; and (5) natural net, writeoff and winner-capture comparisons for implemented and new strategies. The prior 198-202 performance fixes and three deployed arms close only parts of groups 2 and 5. Engineering pass, deployment, natural trade and profitable alpha are separate states.

Fresh 17:05 UTC read-only DB data supersede the prior zero-position initial snapshot: `trajectory198_solana_confirmed_stop_v1` closed two independent-token source-paired positions at -2.655U and -5.134U. Both exits and net results matched its parent `trajectory187_solana_runner_v1`; `depth199_first_floor_cross_v1` and `activity200_old_pool_tempo_fast5_v1` still had zero positions. These two ties are a negative *differentiation* observation, not a profit estimate. An older `activity193` entry exited by hard stop in 38 seconds for -1.477U, before `activity200` activated, so it is not a paired short-hold result.

In equal 17-minute windows around the event-loop repair, pre/post V6 evaluations were 1,412/3,212 on 626/867 distinct tokens; cohort counts 4/40 on 3/15 tokens; source fills 1/12 on 1/7 tokens. Those windows have different incoming load, policy activation and market conditions, so this does not prove the query repair caused more trades. The later window still had 968 `entry_family_has_no_policy` evaluations across 249 tokens and 678 curve-stage liquidity-absent evaluations across 509 tokens. `entry_family_has_no_policy` spans `broad_launch`, `market_visible` and `flow_burst`; it must be separated by eligible cohort and token and checked against current active policy contracts, not interpreted as 968 missed profitable buys. The read-only strategy reviewer found no economically distinct, defensible new arm from these counts alone: existing families already cover many price/volume/flow variants, and reflexively reactivating failed broad arms would confound the study.

Next implementation action is the first proven break in the natural signal -> fresh exact original-pool frame -> safety -> source BUY -> per-arm position chain. Produce a bounded, cohort-keyed terminal decomposition with chain/source and reason, compare followup and held freshness under natural load, then repair a confirmed shared-path defect or deploy a truly distinct mechanism if evidence warrants it. Keep the same 1000U original-pool floor, 4%/4% adverse Paper slippage, append-only account history and as-of clocks. Review remains manually triggered. All unresolved acceptance rows stay open rather than being silently converted to success.
