# Pattern watch admission review80

REPLY_TO: C2C-20260909-PATTERN-WATCH-ADMISSION-80
Disposition: HISTORICAL_ADMISSION_UNIDENTIFIABLE; SHADOW_DESIGN_PREREGISTERED_NOT_DEPLOYED; TRENDING_PROBE_BLOCKED403.

## Proven mechanism, unavailable historical reconstruction
Current _remember_pattern_quotes does not compare activity/information value between floor-valid incumbents and new candidates. A full bucket rejects a valid new candidate unless an unusable incumbent can be replaced or early borrowing has spare chain capacity. This is a real selection limitation, not proof of an erroneous specific rejection. Legacy3/4/3, total10 perchain,15/15/20mTTL, held exemption and79 exact-pool fix remain authoritative.

Storage inspection: `chain-meme-pattern-watch` is one overwritten KV containing aggregate counts only. At07:13:03Z BSC5/4/1,RH3/4/3,SOL5/4/1:all10 nonheld. This current occupancy cannot be backdated to Today23 receipts. No runtime watch admission/victim history is persisted. Pattern evaluations prove sampled identities, but unobserved incumbents, response ordering, held-set changes, TTL admission times and process restarts are absent. Replaying normalized71 from an empty watch would be a hypothetical simulator with selected/floor-filtered input, not reconstruction of actual runtime admission. Do not report it as fact.

|Case|Historical full bucket?|Which incumbent won?|What is actually known from79|
|---|---|---|---|
|CRIMECAT|UNKNOWN|UNKNOWN|2 cohort rows,0 pattern; unknown-liquidity/old generic cash gates; sparse exact-pool path|
|JACOB|UNKNOWN|UNKNOWN|21 pattern observations exist; parent rate and reawakening sequence/gap gates|
|Quest|UNKNOWN|UNKNOWN|3 pattern observations; rejected then common-opportunity consumed|
|Bitcat|UNKNOWN|UNKNOWN|2 cohort rows,0 pattern; floor/unknown on27 frames|
|CLAWDHOOD|UNKNOWN|UNKNOWN|7 cohort rows,0 pattern; general policy/floor outcomes|

Thus the requested historical causal confirmation is not met. Do not replace live admission or invent would-admit counts. Today23, its22 reused provisional controls and later natural cases all have UNKNOWN counterfactual ranking admission until a paired prospective decision record exists. No Alpha or successful-recall claim.

## Frozen zero-request Shadow comparator proposal
This is an explicit design, NOT implemented or measured. It may be evaluated prospectively without adopting its admissions as live targets. It learns acquisition value, not winner prediction.

- Inputs: the same existing received quote batch at receipt time; exact chain/token/pool, known clocks, current held set, actual watch state immediately before admission, raw activity missingness, source/quote asset, and previously received causal observation times. No returns/PnL/future labels, address allowlist, external request or new data source.
- Control: record the actual legacy admission decision under c3044be including early borrowing and base reservation reclaim, not an approximate FIFO simulation.
- Challenger: independent ephemeral fixed-cap3/4/3+early spare borrowing state with identical15/15/20m expiry and held exemption. Never alter actual watch, actual requests or strategy eligibility. Record selection only; a shadow-selected identity without subsequent naturally received data is CENSORED, never treated as sampled.
- Pre-registered priority: protect a newly admitted candidate's first150s observation lease (bounded by existing expiry) so continual arrivals do not destroy60/120s continuity. Maintain one quiet-candidate reservation per bucket when a candidate with measured buys+sells<=3 exists; missing counts are UNKNOWN and do not qualify as quiet. This uses the existing broad-activity boundary solely for diversity, not trade eligibility.
- After protected leases/held exclusions: prefer identities missing a causal60/120 checkpoint over already complete identities; then underrepresented source+quote-asset combinations within that chain/bucket; then greater minimum(as-of buy count,as-of sell count), capped at200 to avoid unlimited turnover dominance; then older receipt time and canonical identity for deterministic ties. Quiet reservation outranks activity; no universal momentum cutoff. Rank only usable floor-valid/clocks-valid inputs; unknown diversity is a separate category, not proven diversity. These choices are hypotheses about information gain, not validated optimal weights.
- Per-event audit needed: receipt/source row identity, actual incumbents/admission/victim/reason, challenger choice/reason, both occupancy/expiry summaries, held membership, known checkpoint mask, ranking version. Bounded append/report batch outside critical held work; if budget exceeded mark DROPPED_AUDIT explicitly, not a fake complete denominator. Hard-cap buffered events and flush volume; no synchronous large JSON/database write in _remember_pattern_quotes.
- Acceptance before runtime adoption: all candidate receipts denominator, unique tokens not repeated frames; actual versus would-admit Today23 only where prospectively seen (no backfill), outcome-blind ordinary controls and new natural arrivals; quiet share, diversity, natural60/120 checkpoint coverage, churn, omitted audit events; unchanged actual watch/requests, held/SELL latency, passive drops. A selection-only Shadow cannot establish counterfactual additional quote availability, so no executable performance claim without a separately authorized intervention.

Because exact historical admission is not identifiable and no prospective Shadow is running, this request ends at the reported design/blocked measurement boundary. This does not silently claim implementation. No restart is needed for a design report.

## Trending feasibility and budget boundary
Official Gecko API reference lists /networks/{network}/trending_pools. Official CoinGecko equivalent documents duration5m/1h/6h/24h and up to20 pools/page; current trending mixes engagement/activity/security. None exposes historical point-in-time membership in the inspected documentation. A current trending result cannot prove pre-rally discovery of DUO/MUCHWOW.

Official Gecko FAQ currently states public30 calls/min, not10/min. This is a provider ceiling, not project available capacity. Existing new-pool/held fallback traffic and backoff share that budget; no deployment unless residual capacity and held-start priority are measured. Low-frequency candidate design would rotate one chain request per5min (0.2calls/min aggregate), only unused low-priority capacity and no429/backoff; no all-duration/page fanout. This is not enabled.

Two one-shot public probes,1h/page1/include base+quote:Robinhood07:14:17–19Z returned403 (1.90s);Solana07:14:25Z returned403 (.31s), separated6s. No response body/membership, no retry/auth workaround. Exact URLs/times/errors in data/research/admission80/trending_probe.json. Therefore DUO/MUCHWOW present-now=UNKNOWN, historical counterfactual=UNKNOWN, held/resource feasibility for deployment=NOT_PROVEN. No production collector added.

Sources checked2026-09-09:
- https://api.geckoterminal.com/docs/index.html
- https://apiguide.geckoterminal.com/faq
- https://docs.coingecko.com/reference/trending-pools-network

## Validation/change boundary
Read-only schema/current KV and code inspection; two bounded external probes; documentation diff check. No runtime code/strategy/config/SQLite mutation, no capacity/floor change, no reset/history rewrite/Live. Historical notes remain intact.79 fix remains deployed;77 champion comparison remains binding.
