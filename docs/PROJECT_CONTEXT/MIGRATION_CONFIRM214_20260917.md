# Migration second-frame Paper trial, 2026-09-17

## Observed problem and corrected funnel reading

The bounded read-only interval `[2026-09-16T18:12:00Z,18:42:00Z)` had 878 first-local discoveries, 4,967 V6 snapshot evaluations across 956 tokens, 3,987 fresh identity-bound >=1000U original-pool evaluations across 376 tokens, 39 cohorts across 15 tokens, 16 source BUY-filled cohorts across 11 tokens, and 29 projected position rows across 11 tokens. These are **different denominators**; none is a conversion rate from 878 discoveries. Of 4,967 evaluations, 1,813 ended `entry_family_has_no_policy` with a fresh pool, and 1,244/925 ended pattern/cohort observation. Of 23 cohorts without source fill, 16 had cash-only arm decisions, six had admitted decisions but no V6 fill or participant outcome, and one had no entry decision. The six have an unresolved terminal evidence gap; they are not proven safety refusals.

Independent code/account review corrected the first interpretation of the 1,813: R93 already registered market-visible, broad, flow and reawakening policies. `_chain_meme_trader_policy_active_for_snapshot` excludes those retired/paused for **natural losses** before family matching. In a nearby 30-minute window, 1,816 such rows were 1,037 market-visible, 663 broad-launch and 116 flow-burst, none reawakening. Existing `mv_*` arms show severe realized losses (for example `mv_tight_fast_exit_v1` -827.48U and `mv_quiet_depth_v1` -987.43U). Thus reviving an otherwise identical broad family arm to increase fills would bypass justified account governance, not repair an acquisition defect. The remaining six admitted/no-fill terminals need separate evidence, not a guessed cause.

## Distinct strategy hypothesis

`migration209_first_tradable_v1` naturally bought seven distinct tokens and all seven closed for **-41.2301U net realized** at the later readback. This is a small negative early-entry sample, not proof that delaying always helps. `migration214_pool_persistence_v1` is a separate forward Paper account testing whether an 8-90-second second quote from the **same** DexScreener Pumpswap pool, with price 0.85-1.50x trigger, liquidity at least 70% of trigger and the shared floor, at least two additional m5 buys and observed sells, improves net outcomes enough to offset delayed/missed winners. It receives only locally observed 209 signals, uses no added API, retains the parent 187 runner exit, ordinary 20U sizing, shared safety and a further next-observed fill. Pending receipts are restart-local, bounded to 1,024, and only future observed/ingested frames qualify. Absent or stale fields reject this experimental signal, not other strategies.

The comparison is **not** a same-entry paired exit experiment: 214 intentionally waits a frame and will select fewer opportunities. Manual review should count 209 triggers, 214 confirmations, both fills, rejected/missed 209 winners, token-clustered net distribution, losses and writeoffs. No address from the user's retrospective sample list is a whitelist or live feature.

## Implementation and initial forward state

`migration_confirm214.py` defines the policy and bounded tracker; `runtime.py` routes natural frames and `store.py` registers the new arm at its own frontier. Three new targeted tests cover as-of identity/pool/clock checks and next-quote Paper fill; three adjacent 209 tests also pass. Activation `2026-09-16T18:54:51.820915Z`, snapshot frontier 981803. Actual worker PID103840 manifest started 18:54:54Z with 507 policies and runtime/store/214 source hashes matching disk. Subsequent snapshot frontier 981985, 214 natural positions zero at that cutoff. `/health`, `/api/live`, `/api/performance` responded; Paper true, Live locked, same funding period, 20U ordinary buy, 4% adverse each side, 0U extra fee and 1000U original-pool floor. Runtime at 18:55:54Z showed zero original-pool gaps, but this alone is not a matched-load performance improvement. No history, account, old positions or 209 losses were reset or revised.

Status: **implemented, tested and loaded; no 214 natural trade or profitability evidence yet**. No timed review/deployment was enabled. Rollback of future 214 entries is the existing per-arm entry-pause control; historical 214/209 accounting and already-open positions must remain intact.
