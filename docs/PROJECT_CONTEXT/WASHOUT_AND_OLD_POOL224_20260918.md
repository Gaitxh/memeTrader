# Manual Paper continuation 224, 2026-09-18

## Baseline and observed failure

The active user scope remains the twelve-section request in `memory/USER_EXECUTION_REQUEST_20260916.md`, including its 125 ordered addresses, plus the 56 ordered later lines in `memory/USER_ADDITIONAL_SAMPLES_20260917.txt`. The previous committed source was `016f68a`; the working tree also contained unrelated uncommitted edits before this stage. The active database is `data/memetrader_forward.sqlite3`, definition `chain-meme-trader/funding-20260906-v002-final-1000`, Paper only. No funding period, fill, position, historic policy or Live permission was reset.

At the manual read, the latest added delayed-entry arm `alpha222_washout_half_reclaim_v1` had 50 terminal independent tokens and -351.2741U realized Paper PNL; eight fresh original-pool below-floor writeoffs accounted for -160U. During the read it settled one more loss. The exact pause receipt at `2026-09-18T01:30:19.955281Z` froze 51 terminal independent tokens, -356.5766U and eight writeoffs. Of the first 50 terminals, 30 pre-reclaim anchor failures lost -191.303U and nine trailing exits gained only 41.846U. The half-reclaim entry therefore did not eliminate false recoveries or withdrawal risk. This is a natural forward negative result, not a statement that every washout entry is impossible.

`trajectory220_breakout_anchor_v1` had 137 terminal independent tokens and -276.3926U, including 12 full pool writeoffs (-240U). Its 137 fully terminal exact-source-BUY pairs with `trajectory144_trend_runner_v1` were -276.39U versus -345.60U, a +69.21U difference for 220 (9 better, 4 worse, 124 tied). The structural exit may reduce loss on that shared cohort, but its absolute expectancy is unproven. No change to 220 was made here.

## Actions and deployment

Manual `scripts/converge_washout224.py --apply` paused **new 222 entries only** through the existing `chain-meme-account-convergence/v1` overlay. Its receipt is `washout224:convergence:2026-09-18T01:30:19.955281Z` and contains the previous control for deliberate rollback. The one already-open 222 position kept its original exit and accounting. The script refuses non-Paper/Live, fewer than 40 independent terminal tokens, net better than -150U, or fewer than five writeoffs; preview writes nothing. The pause does not refund or erase any loss.

At the 01:37Z readback there were zero 222 positions opened after the pause; its previously open position had naturally closed at 01:37:03Z by the original trailing exit for +0.3225U. All 52 222 positions were then terminal, with total realized -356.2540U. This later result does not retroactively change the 51-terminal evidence frozen in the pause receipt.

New `activity224_old_pool_flow_floor_fast_v1` is an append-only, same-entry Paper comparison to the existing old-pool activity193 arm. It shares the as-of original-pool signal, normal safety, later source BUY, 20U notional and configured costs. It changes only exit behavior: five-minute maximum hold; after one minute, a fresh original-pool five-minute buy share below 50% requests the existing next-observed SELL; a fresh original-pool liquidity value below 1600U requests an earlier exit before the unchanged global 1000U writeoff floor. Missing/stale marks do not satisfy either condition. Existing hard stop and trailing still apply. It uses no additional API request or new hot-path data query. The hypothesis is that short-hold old-pool opportunities may avoid flow/depth decay before a small gain reverses; it can also exit a durable trend too early or still lose the full 20U on abrupt pool removal.

The arm was registered at `2026-09-18T01:30:42.809338Z`, activation snapshot `1260478`, after the Paper worker was replaced under the existing supervisor (new worker PID 8000). The effective policy count became 510; snapshot frontier subsequently advanced to 1260561. `/health` was running, `/api/live` and `/api/performance` returned HTTP 200, the original funding period remained. No 224 position existed at the deployment check. The single `/api/live` response took 6.28 seconds; this is not a matched-load latency comparison or proof of regression/improvement.

At a subsequent short post-reload `/api/performance` read, the passive queue reported 123 enqueued/122 processed batches, one in flight, zero dropped and p95 wait 4.60 seconds; five held tokens (one BSC, four Solana) had no listed coverage gap and latest observed ages under four seconds. Intermittent held retrieval failures still appeared within the rolling window. These are **post-only**, load-unmatched operating checks, not a before/after efficiency result.

An approximately one-hour bounded ledger read (00:36-01:36Z, ID frontiers restricted to this interval) found 2,351 distinct first-local discovery chain-token keys (BSC 413, Robinhood 222, Solana 1,716). The snapshot table in the same interval held 1,992 distinct keys (BSC 442, Robinhood 272, Solana 1,278); these are **not the same discovery cohort**, so 1,992/2,351 is not a hydration pass rate. Evaluation reasons included 3,572 `entry_family_has_no_policy` rows/644 tokens, 2,680 `pattern_observation`/191, 1,667 `cohort_observation`/207, 1,428 `entry_pool_liquidity_absent_curve_stage`/903, 466 `invalid_exact_asof_market_snapshot`/423, 254 known-below-floor/165, and 118 unknown-pool/105. The same bounded trade read found 85 BUY rows/11 tokens and 91 SELL rows/13 tokens, so natural trading had **not stopped entirely**. Counts can repeat a token over frames, arms and reasons. The no-policy rows chiefly involved `market_visible` and `broad_launch` families whose entry policies were retired or paused after losses; they are a coverage tradeoff, not evidence that 3,572 profitable orders were blocked. This is a partial funnel with incompatible stage denominators, not the requested cohort-linked pass-rate/latency diagnosis. Its next step is ID-linked discovery-to-first-frame timing plus per-family eligible-arm state, without a hot-path query.

A plain PowerShell `/health` probe later returned proxy-generated HTTP 502. Direct localhost requests with `curl --noproxy '*'` returned `/health` 200/running, `/api/live` 200 and `/api/performance` 200, so that isolated 502 is not evidence of a backend outage. At this later read the 224 arm still had zero entry-decision rows and zero positions; no natural efficacy conclusion is available.

## Evidence, limits and next manual check

`tests/test_activity_flow224.py` and `tests/test_converge_washout224.py`: six focused tests passed after correcting the fixture to register the alpha149 parent. They cover independent append/idempotence, frozen entry alias, fresh-flow exit, fresh-pool-depth exit, stale-frame refusal, Paper-only bounded pause, control receipt and preserved synthetic ledger. Existing Store exit predicates were reused rather than reimplemented. The adjacent 200/222 checks passed in the same release slice (12 total). The next natural check must confirm actual 224 admissions, source BUY, position, fresh-flow/depth trigger, next-observed SELL and costed terminal outcomes against the same-source 193/200 controls. Code and deployment are complete; natural efficacy is **pending**.

The combined user sample audit remains 111 distinct research keys from 181 ordered lines: 80 current-period local canonical matches, 31 unresolved identities. Among those 80, 49 were evaluated without admission, five admitted without a position, 25 had Paper position records and one discovery lacked a snapshot. Existing matched ordinary/failed controls cover only 13 independent case tokens at a 30-minute endpoint. This stage did not repair all case/control, discovery-funnel or matched-load gaps. Those remain open in `memory/ACTIVE_EXECUTION_SCOPE_20260917.md`; no selected hot address became a trading allowlist and no later price was used as an earlier feature.

External compatibility check: the official DEX Screener [API reference](https://docs.dexscreener.com/api/reference) currently lists 300 requests/minute for token batches of up to 30 addresses, which gives a *theoretical* 9000 address slots/minute only when every batch is full. That is not discovery throughput, unique successfully hydrated tokens or guaranteed sustained service. The same documented pair fields include rolling buys/sells, liquidity, price/volume and pair creation time; they support the 224 proxy without adding a data source. Academic [momentum/liquidity research](https://arxiv.org/abs/1904.00890) concerns broader crypto portfolios, not this short-horizon microcap Paper system, so it was not imported as evidence of alpha or used to set a new global filter.
