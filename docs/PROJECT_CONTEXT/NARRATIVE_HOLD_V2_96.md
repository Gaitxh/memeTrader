# Active-period narrative hold V2

REPLY_TO: C2C-20260909-NARRATIVE-HOLD-MULTIAGENT-96

Code dc486ea. Replaces no old strategy or history. Old v5 postbuy route remains historical; new active-period observer begins at actual trade frontier, unique token/cohort rather than arm fanout. Append-only case/checkpoint/source/result/skip rows use existing pattern evidence; bounded KV holds scheduling cache and overlay readback. Interrupted dispatches become RUNTIME_INTERRUPTED and are not replayed.

New Paper arm narrative_hold_recovered_runner_v2: parent age-rate signal, 5U/max4, common safety and same next-frame fill semantics. Parent's actual soft max hold is 30 minutes. New arm reuses dynamic actual-net principal recovery; only after actual settlement has recovered debit, with positive fresh original-pool remaining runner, no known hard/soft behavioral hazard and persisted CONFIRMED_EXPANDING, soft hold may extend to 60 minutes. Hard stop, trailing, known floor/writeoff and entry safety remain authoritative. Parent/254/v2/v3 are unchanged. No fixed profit target, losing-position narrative override or chain/date rule.

Reuses AutonomousSearch readonly ephemeral worker and shared two-slot semaphore; one V2 case at a time. Scout token_context Luna/low and independent fact_verifier Terra/low. Profiles and actual model metadata are audited; unexpected Scout fallback cannot confirm expansion. New verifier admission callback rechecks core idle after waiting for shared capacity. No X paid credential/dependency. Metadata links are explicitly untrusted search hints.

Checkpoints 150/720/2700 seconds, bounded start margins90/180/900 seconds; later checkpoints require remaining open positions and prior constructive state or new local source evidence. Family selection favors age-rate/reawakening/clone/principal runners and healthy local marks. Four checkpoint dispatches/day maximum; 60k token reservation each within240k admission budget, actual reported tokens reconciled (missing usage conservatively30k per call), existing shared provider/agent quotas also apply. This is an admission/call budget, not a promise that an already-running model cannot exceed its estimate. Existing worker timeout/fallback bounds remain. Cases128, sources12, public source excerpts capped1500 chars each.

Freshly fetched public evidence is NOT backdated to checkpoint: stored publication/local availability and append-only source record become input only at later checkpoint. Exact contract must occur in retained source excerpt, and independent verifier must check exact binding on source, independent origin, contradictions/copies. Model interpretation remains fallible; no claim of guaranteed truth. Deterministic aggregator uses verified distinct origins, novelty, new origins versus previous checkpoint and multiple platforms, not raw mention volume. UNKNOWN, EMERGING, CONFIRMED_EXPANDING, STALE_OR_PROMOTION, CONTRADICTED are distinct. Corrections/promotion never extend hold. No historical replay.

During Agent execution, every5 seconds a bounded in-memory guard checks comparable held-fetch/apply p95 and passive drops; regression disables overlay and further research, while allowing already-running readonly worker to finish and persist evidence. It does not cancel a Python thread while falsely releasing its agent semaphore. No extra market request, target/watch/cadence changes.

Validation: 16 targeted narrative/principal tests plus 1 existing separate-verifier test PASS; diff check PASS. Same nonempty fill identity, unique case, restart interruption, source availability/no backdating, exact binding/independent growth, real settlement/freshness/floor, soft-only extension and continuing trailing exit, busy-core skip, latency rollback tested. Independent review challenged duration (resolved against actual30m parent and integration test), persistence (append-only rows verified), budget/as-of/admission; binding/source references, actual usage and in-flight guard strengthened.

Deployment/natural acceptance: pending readback below. No Alpha claim; first natural longitudinal evidence and economic effect remain prospective.

Loaded final78b49a6 via existing Paper launcher; activation remains11:37:01.280767Z, policy addition291. Seven old contract/funding table row sets exactly preserved; only additions290->291. Final-process short read11:39:15Z: /health running, Live locked, task3 cycles/0 failures, actual interval~15.08s; held fetch/apply p95~1.816s/.0776s, pattern~2.265s, passive drops0. Baseline held2.665s/apply.0935s/pattern6.866s had mature samples, so startup comparison is NOT a sustained speed claim. Old separate admission84 audit still has drops99 in this process; it is not a complete denominator and unrelated to passive queue.

Natural cutoff11:39:15Z: zero new arm positions and zero prospective postbuy cases; no Scout/Verifier invocation yet. INSUFFICIENT_NATURAL_EVIDENCE: model availability/longitudinal propagation and profitability are not claimed verified. Existing historical65 cases were not rerun. Evidence data/research/narrative96/acceptance.json,before_contracts.json and data/research/admission88/{pre_narrative96,final_narrative96}.json. Latest patch adds bounded1.5s idle admission wait (no frequency increase); one busy-core targeted test PASS after that patch. Next meaningful acceptance requires a natural new BUY cohort and its eligible checkpoint; no forced trade/replay.
