# 扩展地址全链路核对156

## 当前结论与范围

本阶段补齐目标文件后来追加的19个地址，不重复注册策略、不重置账户、不把案例加入买入白名单。统计截止2026-09-13 20:01:46 UTC（北京时间9月14日04:01），当前数据库、当前资金期。先前152报告仅覆盖最初15个地址，不能代表全部34个。

| 分组 | 用户地址 | 当前规范化链ID匹配 | 快照 / 基础评估 | 准入机会 / 策略准入次数 | 仓位 |
|---|---:|---:|---:|---:|---:|
| 原案例 | 15 | 14 | 558 / 558 | 7 / 68 | 0 |
| 补充案例 | 19 | 17 | 402 / 402 | 18 / 60 | 0 |
| 合计 | 34 | 31 | 960 / 960 | 25 / 128 | 0 |

完整查询34/34完成，无预算中断、查询错误、历史截断。31个匹配Token中，27个没有记录到策略准入，4个曾准入但没有源Paper成交、参与者入账或仓位。基础评估存在不等于所有策略完成判断；`pattern_observation` / `cohort_observation` 是观察路由标签，不是拒绝原因。25个cohort是唯一机会ID，不是25个独立Token或独立收益样本，128次策略扇出更不能作独立样本。

## 已准入但未入账的精确分解

| Token | 准入机会 / 臂 | 同cohort安全证据与后续结果 |
|---|---:|---|
| Robinhood `0xe27501d787d647cc82a5b4a7eafd5750386f1b77` | 7 / 68 | 7个机会均记录 `EXPIRED_SECURITY_OR_NEXT_FRAME`；同Token原池有4次 `CHECKED_UNKNOWN`，缺失税率、权限、可卖等字段。源成交/参与者结果/仓位均0。 |
| Robinhood `0x1cdb289befdfac8af945a288bcdccc382cb34d32` | 12 / 37 | 12个准入机会均记录同cohort过期；另有742次策略决定明确拒绝 `entry_cash_below_order_size`，不能与37次已准入混为同一种阻断。 |
| Robinhood `0xbea92c5bffd98972d7b83b8a7e7f6d0209751e18` | 5 / 17 | 5个准入机会均记录同cohort过期；另有341次现金不足策略拒绝。 |
| Solana `HHDuCwdk67cfJJKmohQ5ziw4JML9xFmB5u8r3kLXSAke` | 1 / 6 | cohort699记录 `CHECKED_REJECT` → `REJECT`，原因 `dangerous_transfer_fee_upgradable`；另有 `pool_custody_rpc_unavailable` 未知项。不是单纯数据未知或成交后入账丢失。 |

根因边界：前三个Token证据定位到安全等待/后续帧到期，不能仅凭这一终态区分每个机会的安全数据与下一合格行情各自贡献。安全缓存可由同Token/原池多个机会共享，不能要求每个cohort都有独立外部请求；脚本保留所有安全证据及精确cohort关联ID。现金不足是已记录的否决依据，本阶段未重算742/341次决定当时的现金流水，因此不将其认定为正确风控或工程错误，也不放开资金限制。

## 连续采集与未发现边界

新增案例中的三个具体缺口（只统计同一原池、不同观察时间、可用价格/流动性与15秒内入库的记录；不是路由可执行证明）：

- `HHDu…SAke`：75行中42个合格同池观测，最大相邻间隔7231秒；3行入库时已陈旧。
- `3Sv4…pbWQ`：54行中38个合格同池观测，最大相邻间隔4665秒；9行陈旧。
- `J53E…AXGc`：33行、两池合计31个合格观测，最大**单池**间隔3542秒；1行陈旧。不能将跨池迁移解释为全Token停更。

`0x6f3a…1e18`、`0x4b7d…ed88`、`H22…pump`、`Cby…pump`各只有一个合格观察，不构成持续跟踪证据。以上多为历史/修复前窗口，不能当作155已上线后的性能结果。

三个当前规范化ID未匹配地址均为合法40位EVM十六进制外形：`0x4b112e1ed0c0cb332d2b39e5dae3bba882f67777`、`0x0933f674bcfec78bd0f4720faf370fd9709feeee`、`0xa63e42d09bfca311e8091269ac45fdeba9b0eeee`。仅查询BSC/Robinhood规范化主键，未全扫旧混合大小写、其他链或已归档数据库；不宣称全历史未发现或上游无数据。

## 实施、测试、可回滚工件

- `scripts/audit_goal_cases156.py`：只读SQLite事务，默认30秒总查询预算、每类历史3000行上限，使用Token/资金期/快照/cohort索引；无网络调用。区分SOL大小写与EVM规范化；快照去重、单池间隔、无效/未来时间排除、采样范围与截断可见；按cohort关联决定→安全证据→源成交→参与者→仓位。普通Paper无源成交不冒称Live下单失败。
- `tests/test_goal_cases156.py`：5项通过，含内存SQLite关联、无成交不得虚构仓位、链/池大小写、时间/缺失值保护。
- 原始工件：`data/research/goal_cases156/cases_20260913T195533Z.{json,md}`；补充关联与校验：`cases_20260913T200146Z.{json,md}`。均保留、不覆盖152基线、不提交运行数据。
- 回滚仅移除该离线审计脚本/测试及本阶段文档增量；生产运行、策略、数据库历史从未修改，不需重启。

## 系统状态与下一步

03:59北京时间只读收据 `data/research/pool_resources154/status_health156.json`：Paper仍PID40164，心跳0.54秒、快照424336/交易40324均较前次推进；59仓/6币，其中28仓仍集中在一个原池不可估值。持仓请求进程累计442批/失败200批，滚动120批p95=11.664秒。这里分母都是请求批次，不是Token或订单；旧进程仍未加载155，不能宣称已恢复正常。

下一步优先级：先由获准的启动路径/操作者重载155并验证真实持仓请求延迟和错误率；保留原池缺字段风险。其次在同截止时间、可比资金期回放新增两Token的现金拒绝账本，并检查既有152代理策略的适用窗口，而非继续叠加相似参数臂或关闭安全规则。自然观察与持续盈利仍待验证；已有两小时只读复盘机制继续记录，不另外建立监控栈。
