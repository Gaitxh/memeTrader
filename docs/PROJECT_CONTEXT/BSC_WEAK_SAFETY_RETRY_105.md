# BSC weak security evidence105
REPLY_TO: C2C-20260909-BSC-WEAK-SAFETY-RETRY-105

Code355fa76. BSC tax-only and/or same-creator-negative reports are WEAK, allow=false. At least one explicit negative honeypot/sell-restriction/structural-control fact, or Honeypot.is isHoneypot=false, is needed for the existing UNKNOWN/PASS authorization boundary. Any hard reason still overrides it. This is not a safety guarantee or a profitability finding. Solana/RH authorization unchanged; cannot_sell_all compatibility is BSC-only for this stage.

Official GoPlus [response semantics](https://docs.gopluslabs.io/reference/response-details) distinguishes cannot_sell_all (restriction on selling the entire balance) from an unspecified cannot_sell flag; missing values are unknown. Tax0 does not prove absence of those restrictions. Explicit cannot_sell_all=1 is separately named hard evidence, not relabeled a confirmed honeypot.

After a first WEAK result the same pending cohort gets one retry due5s later, dispatched by the existing worker on its next eligible tick. It is not a new timer or promise of exact5s completion. Attempts are persisted before I/O; restart cannot repeat that retry. A second weak/empty/error result waits until original expiry, with no repeated45s refresh loop for that cohort. No signal expiry extension, provider semaphore/rate limit change or budget increase. Cache remains shared per token/pool; independently acquired later usable evidence can still be consumed on a strictly later original-pool market frame. Strong first checks retain the existing cache behavior and do not receive a fast retry.

WAIT_WEAK joins existing unfunded safety Shadow as a separate category, with existing15/60/240m horizons and bounded storage; never mixed with hard REJECT or WAIT_HAZARD. Source report availability and existing sanitized error type are included in assessments. Old security payload/error fields are cleared from the local copied snapshot before acquisition, never from stored history. Missing Honeypot report is not asserted to be a failed simulation or a safe result.

Validation:47 targeted safety/Shadow/cohort tests PASS, plus2 strong-first/no-fast-retry tests PASS. Includes weak->strong/weak/empty/hard,5s bound, restart persistence, at most one retry beyond cache TTL, unchanged stored snapshot, tax/creator-only wait, cannot_sell_all0/1, Solana/RH existing cases, next-observation safety/common ownership, Shadow capture. git diff --check PASS. No forced provider/Agent/trading call used.

Natural deployment frontier data/research/weak105/frontier.json:2026-09-09T19:02:07.118340Z, evidence_id256031. Supplied HODLSTER/HAMSTERUNI evidence is hypothesis-generating and contaminated fanout is handled separately by102; no historical reclassification or recovered-PnL assertion. Before snapshot data/research/admission88/pre_weak105.json: held_fetch p95=4.0668s/apply=.07780s/pattern=7.2177s; comparison must preserve actual sample/workload caveats.

Paper loaded~19:02:09Z. Post19:02:57Z held_fetch p95=3.3385s/apply=.04897s/pattern=4.6376s, unchanged funding/registration digest0a9ed6e938ef2f7201af23b7fa201ab9104982553d68b5baaf8ddc21ff1fd24a. Health running/Paper/Live locked. Short startup sample only, no sustained speed claim. Bounded new evidence ids256032–256089 contain zero safety cohorts (including zero BSC tokens): actual WEAK retry latency, BUY coverage and15/60/240m opportunity-loss outcomes remain INSUFFICIENT_NATURAL_EVIDENCE. Artifacts data/research/weak105/natural.json and admission88/post_weak105.json. No historical mutation/reset/new account or strategy.
