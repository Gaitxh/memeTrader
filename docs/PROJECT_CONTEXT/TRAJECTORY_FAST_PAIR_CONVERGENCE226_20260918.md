# Same-entry trajectory exit comparison and Paper convergence, 2026-09-18

The active Paper period has a strict exact-source-BUY, same-token, both-terminal comparison for `trajectory144_early_activity_fast_v1` against `trajectory144_trend_runner_v1`: 378 independent token pairs, fast -532.6225U, runner +275.2956U, runner-minus-fast +807.9181U. Runner beat fast on 25 tokens, lost on 23 and tied within 0.000001U on 330. This comparison holds entry and source BUY constant and includes the recorded Paper costs and writeoffs; it is more informative than comparing all-account totals from different opportunities. However, the largest one-token advantage was +373.16U (46% of the total difference), and even excluding it the gap is only an arithmetic +434.76U, not a new independent test. The runner had 42 full original-pool writeoffs in its whole current-period account versus fast's 31; it is **not** per-token risk dominant.

The 378 pairs split by chain: Solana 219 pairs, fast -172.69U versus runner +819.29U (runner +991.97U); BSC 131, fast -412.85U versus runner -590.84U (runner -177.99U); Robinhood 28, fast +52.91U versus runner +46.85U (runner -6.06U). Thus the profitable account-level result is a Solana right-tail effect, not a universal longer-hold rule. BSC fast is less bad but still negative; Robinhood is too small to promote a chain-specific clone. Neither this pair nor historical winners justify trading on a user-supplied address list or setting exits from a future peak.

`scripts/converge_fast226.py` pauses **new entries only** for the globally negative fast account when at least 100 independent exact-source terminal tokens have fast <=-150U and runner-fast >=300U. It does not alter the runner, existing positions, fills, funding, policy hash or Live settings. The manual Paper receipt `trajectory226:convergence:2026-09-18T02:02:46.695251Z` contains the previous lifecycle JSON for an intentional rollback. Two targeted tests cover the evidence threshold, preview, preservation of both ledgers, idempotence, runner continuity and Live refusal. Effective policy readback showed fast `DOMINATED_IN_PAIRED_TEST`/paused, runner still open to new entries, and zero fast BUY after the receipt at the first check. This is a loss-containment action based on observed paired behavior, not proof the retained runner will remain profitable.

The latest bounded shared-chain read separates two delays for 51 token identities with full local discovery and snapshot clocks: first discovery to first price-positive, liquidity-known snapshot p50 approximately 0s, p90 121.8s, p95 156.9s, max 1968.8s; first valid snapshot to first cohort p50 115s, p95 310.2s, with very long reactivation outliers. Two of the slowest discovery-to-cohort examples were old tokens already quoted for hours before reactivation, so that elapsed time cannot be labeled acquisition queue delay. A real Pump no-pair case attempted hydration within ~2.3s of discovery but did not obtain liquidity-known price until ~32.8 minutes later; another took 8.3 minutes after initial no-pair. Historical selector disposition (due time, reserved slot, capacity/reject reason) was not persisted, so a 90/210-second retry-selection bug is a hypothesis, not established. Do not change rate limits or pool truth gates from these cases. The next narrow engineering action is to record bounded no-pair selection reasons and distinguish first price-only from first liquidity-known metrics, then use natural observations to choose a scheduler correction.
