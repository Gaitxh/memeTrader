# Old-pool activity continuation Paper trial, 2026-09-17

## Why this is a separate strategy revision

The existing `activity193_old_pool_tempo_v1` acts on one fresh five-minute-vs-prior-55-minute activity frame. Its `activity200_old_pool_tempo_fast5_v1` contrast changes only the maximum hold; the observed 38-second early loss could not be addressed by a five-minute limit alone. A separate hypothesis is that an old-pool tempo burst which persists into a later independently acquired frame, without price or liquidity deterioration, is more selective. This is not proved by one loss or by the user's retrospective hot-token list.

New append-only arm `activity211_old_pool_confirmed_fast5_v1` freezes the first actual 193 trigger, then waits for a distinct 15–60-second later, same-provider, same-token/same-pool frame. The second frame must still meet the corrected 193 predicate (including per-side nested h1/m5 validity, a real sell and >=2000U pool depth), with price and liquidity no lower than at trigger. Both frames use ordered local clocks; confirmation quotes older than 30 seconds are refused. No missing values become zero. Safety, cash, current shared original-pool floor and a **third, next-observed** quote remain necessary for a Paper BUY. Position sizing is ordinary 20U with a six-position arm cap and the same five-minute exit, hard stop and trailing rules as 200. No API call or source priority was added.

`tests/test_activity_confirm211.py` and `tests/test_activity_tempo193.py`: eight tests passed. They cover same-frame/no-later-frame refusal, provider/pool change, price or liquidity deterioration, volume/predicate decay, stale/future clocks, expiry, append-only registration, inherited exits and distinct execution quote. This is engineering validation, not a profitability result.

Activation: `2026-09-16T18:26:26.177295Z` at snapshot 977498 in the existing `chain-meme-trader/funding-20260906-v002-final-1000` period. Paper PID108184 loaded matching hashes for `runtime.py`, `store.py`, and `activity_confirm211.py`; 504 total policies, Paper true, Live locked. At the immediate readback 211 had zero natural positions. Watch trigger count, qualifying second frame, safety/next-frame terminal, source fills, five-minute exits and net cost-inclusive outcomes against 200 on matched original 193 triggers. Missing confirmations must be counted as selection cost, not treated as zero-return trades.

## Independent current-system review

A bounded natural one-hour window `[2026-09-16T17:21:25Z,18:21:25Z)` found 2,241 first-local discovery tokens, 10,142 persisted snapshot events/1,895 tokens, 10,124 V6 evaluations/1,895 tokens, 40 cohorts/19 tokens, 15 source BUY fills/seven tokens and 34 strategy position events on those seven. The cohort terminal split was 15 source-filled, nine cash-only, seven explicit safety reject, six weak sellability and three safety-unknown/not-allowed; all 12 cash-veto arm rows belonged to one depleted account. These event/token/cohort/position denominators are intentionally separate.

The headline 53.9% first-local-to-later-snapshot rate is **not** an executable-opportunity miss rate. Source segmentation showed 1,370 PumpPortal create identities, only 56 with a later >=1000U liquidity quote proxy (4.1%); many curves may never acquire an executable original pool. Conversely Gecko new-pool first-local surfaces already carried an embedded quote, so absence of a *later* snapshot does not mean no initial market data. The 56 are price/liquidity proxies, not verified exact-pool sellable trades. The next engineering diagnosis must bind provider-exact pool and acquisition attempts before changing global filters or scheduler budgets.

Post-209 natural readback supersedes only the zero-position cutoff of the earlier stage209 report: four different Solana tokens opened 20U Paper positions between 18:21:59Z and 18:26:00Z, each with a recorded source-entry-fill ID; all four were open at 18:26Z. This is a real natural entry path, **not** evidence of positive net expectation or completed exit. Prior zeros remain correct for their earlier cutoff. Source: configured SQLite `chain_meme_trader_positions` by arm, not an artificial signal.

No scheduled review, live broker, funding reset, historical trade rewrite or winner allow-list was introduced. A later manual review must separate new-arm activation eras, include adverse 4%/4% Paper slippage, false positives, quote loss and full writeoffs, and decide keep/revise/pause from independent-token outcomes.
