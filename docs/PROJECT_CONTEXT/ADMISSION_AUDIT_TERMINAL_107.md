# Admission audit terminal cap107
REPLY_TO: C2C-20260909-ADMISSION-AUDIT-TERMINAL-107
Disposition: CODE_READY_TARGETED_PASS_NOT_LOADED

Verified code defect: _write sets bytes=MAX_BYTES and raises budget exhaustion, capture keeps incrementing drops, flush sends additional drop records, and the same failed append repeats. The old reported16:19 state is not the current process: fresh19:58:44Z KV shows generation20260909T195505379483Z, bytes8,471,282,receipts4154,written4103,drops0/errors0,pending0. Do not claim that current process is in the historical cap storm or rewrite its denominator to the old generation.

Small fix: terminal state once bytes reaches max, a successful write reaches exact max, or first OSError byte-budget exhaustion. Stop capture before incrementing counters or serializing candidates; clear/count pending once. Further flush returns without scheduling workers. Exhaustion worker records its one failure, then terminates; no repeating DROPPED_AUDIT writes/errors. Existing file is not modified on over-budget failure, deleted, rotated or replayed. Terminal status carries final counts/path/bytes, terminal_reason=BYTE_BUDGET_EXHAUSTED and discontinuous=true. Runtime publishes one final KV once worker is settled, then stops audit flush/KV work. No watch/membership/admission/provider/passive/held logic changed.

Startup reads the existing audit KV. Current terminal or legacy MAX_BYTES / byte-budget-exhausted status restores the same path/counters and stays terminal, no new file. Legacy pending count, if any, is explicitly counted lost rather than replayed. Uncapped restart still has preexisting lifecycle semantics (new generation); this patch does not claim to resume a continuous challenger state across restart.

6 targeted tests PASS. Fixtures verify unchanged file bytes, frozen counters/error count after repeated capture/flush, cap restoration without directory creation, legacy exhausted KV, actual watch/bucket equality before and after terminal state, ordinary overflow/I/O-error behavior and one-worker boundedness. git diff --check PASS. No broader tests necessary.

Not deployed in this stage: current process is uncapped; restarting it solely for this patch would cause the existing uncapped lifecycle to create a new audit file, contrary to this request's no-new-file scope. Keep the current process unchanged and load at the next coherent authorized lifecycle boundary; if current generation caps before then, preserve its actual terminal KV at that boundary. Do not reset KV, fabricate a cap, create a special audit rotation or call restart repeatedly. No runtime acceptance claimed. Before read: heldfetchp953.933s/apply.0814s/pattern9.411s; immutable funding/registration0a9ed6e938ef2f7201af23b7fa201ab9104982553d68b5baaf8ddc21ff1fd24a. Evidence data/research/admission88/pre_admission107.json. Runtime remains25d1059, Paper-only/Live locked as last loaded. No production file mutation in this stage.
