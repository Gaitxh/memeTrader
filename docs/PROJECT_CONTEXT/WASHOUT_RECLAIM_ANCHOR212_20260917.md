# Washout reclaim structural-exit Paper trial, 2026-09-17

## Observed reason and test

The active `alpha149_washout_reclaim_hold_v1` has a 120-minute maximum hold, a wide -45% hard stop with grace/confirmation, and a +45% activated trailing exit. These rules do not directly test whether a newly reclaimed price level subsequently fails. This trial does not claim that an anchor exit is profitable; it asks whether a confirmed loss of the price observed immediately before the entry uptick reduces avoidable losses without washing out later recoveries. The parent strategy and all existing positions retain their original exit contract.

`alpha212_washout_reclaim_anchor_v1` is an append-only, same-frozen-entry Paper contrast. Its source is the actual parent signal, never a retrospective winner address. At source signal creation it freezes the previous locally observed exact-pool price and its time into explicit decision evidence. After a normal next-observed Paper BUY, two distinct, increasing, fresh original-pool marks below that frozen price within 60 seconds trigger an intent; another later observed quote is required for the simulated SELL. A mark at/above the anchor clears the streak, as does a gap over 60 seconds. Missing, stale, out-of-order and wrong-pool marks cannot establish the signal. Existing liquidity/writeoff, hard stop, trailing, other exit rules and 120-minute limit remain. The current execution period projects ordinary 20U entries despite the inherited nominal policy field; no stake override was introduced. No extra API or scheduled action was added.

The new state is stored on the new position only. The cohort's explicit `reclaim_anchor212` evidence survives existing feature-vector compaction. The entry projection rejects this arm if the frozen anchor or local clock order is invalid; other admitted arms remain independent. Market action `RECLAIM_FAILURE_EXIT` participates in the existing price-plausibility and next-frame Paper fill checks.

Four new focused tests passed, along with six adjacent 198/211 regression tests (10 total): historical/future anchor rejection; repeated, wrong-pool, recovering and gap-separated marks; append-only registration and normal delayed BUY with frozen position state; a persisted first mark surviving restart, a second mark creating a pending exit and a third mark completing the exit. This is an engineering check, not an estimate of strategy edge.

Activation: `2026-09-16T18:37:01.344611Z` in the existing `chain-meme-trader/funding-20260906-v002-final-1000` period. Supervisor PID46168 restarted worker PID101276; runtime manifest timestamp `18:37:04.503994Z`, 505 policies including 212, with `runtime.py`, `store.py`, and `washout_reclaim212.py` source hashes matching disk. `/health`, `/api/live`, `/api/performance` returned 200; Paper true, Live locked and shared original-pool floor 1000U. The immediate 212 readback had zero natural positions. The current source/disk hash proves load, not any future result. Preserve this frontier when comparing 212 with the parent on independent tokens and the same source signal.

## Natural early-entry outcome and remaining work

At the same readback the earlier migration-first 209 arm had six distinct-token positions: five closed, one open. The five closed summed to **-16.2458U** realized after the configured Paper costs: +0.4211U, +3.3747U, -1.3014U, -16.5395U, -2.2007U. Four exited on the inherited price/activity/liquidity decay and one on a hard stop. The single large stop dominates this tiny cohort. The open position is not included in realized PnL. These five outcomes are natural observations, not evidence that the 209 idea should be globally copied or that it cannot work in another regime. User-selected hot tokens are not a whitelist or holdout proof.

Next manual review: compare 212 and its parent by identical source opportunities, distinct bought tokens, fresh-mark confirmation rate, missed recoveries, costed net exits and tail losses. Audit 209's large loss and the remaining open pool before modifying its rules. Continue the 111-case/control work, source-to-executable quote timing and all-strategy family disposition; none is completed by this additional arm. No automation or Live authority was enabled.
