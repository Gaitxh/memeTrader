# 第 28 轮：BSC 写销专项与深池额外臂 + 尘埃守卫扩展为双向 + 两份只读复盘收口

- 记录时间：2026-09-12T04:35Z（北京时间 12:35）
- 约束不变：`mode=paper`、`live.enabled=false`、现有策略规则零改动；policy additions **286**

## 1. 周期复盘（只读子代理）的决定性发现

| 发现 | 数据（24h，当前世代） |
|---|---|
| **写销全部来自 BSC** | 216 笔写销 100% 为 BSC：BSC 216/686 = **31.5% 写销率**、−$463（占净亏损 **74%**）；Solana **0/1,599**、Robinhood **0/184**；BSC 非写销账本为正 |
| **仅 ≥100k 深度档零写销且 PF>1** | ≥100k n=729、0 写销、PF 1.17、+$47；5–20k 写销率 25.1%、20–100k 9.6%（48h 与 alpha149 内部同向） |
| 净结果 | 24h −$626 / 2,402 笔平仓（PF 0.62） |
| 结构偏差 | alpha149 占入场 64%（PF 0.54、−$467），仅约 13 小时历史；2,469 笔仅覆盖 **159 个代币**（单币最高 96 笔/92 臂）→ 有效独立样本 ≈159 |
| 亏损集中 | 写销 −$698（216）+ 硬止损 −$681（594）= 毛亏损 **98.1%**；正贡献：到期 +$521、追踪 +$221 |

## 2. 洗出反事实（只读子代理）的结论

- 7 天硬止损代币级：回到退出价之上 80.9%，**超过往返摩擦 +8.34% 为 64.8%**，>+50% 35.1%；
- **回收率随持有时间单调下降**（<1 分钟 75.5% → 30 分钟以上 25.9%），说明最紧的止损确实打在入场噪声里；
- 但"统一延后 30–60 分钟"是**负中位数**（+60 分钟均值 +$1.62、**中位数 −$0.79**、仅 34.2% 为正、5 笔贡献 62.7%）→ 属彩票型尾部，不是平均优势；
- 结论：应做"容忍入场噪声 / 小额封顶 runner"，**不是**拉长持有。

## 3. 本轮动作

### 3.1 第 31 波：深池与"排除 BSC"两条额外臂（已注册部署）

| arm_id | 机制 | 条件 | U/持有 |
|---|---|---|---|
| `alpha149_deep_liq_band_v1` | `deep_liq_band`（新） | 池龄 30–180 分钟、深度 **≥100,000U**、FDV/深度 1–20、不下跌、深度不流失、买盘 ≥50% | 1 / 30 |
| `alpha149_deep_liq_band_nonbsc_v1` | `non_bsc_deep_band`（新） | 同条件 + **排除 BSC** | 1 / 30 |

实现以"包装既有 `mechanisms`"的方式加入新标志（异常回落 `False`），**既有臂判定逻辑一行未改**。

### 3.2 第 30 波：两对 A/B 的镜像臂（已注册部署）

把长持有与死池前兆两组 A/B 镜像到供给更大的 `survivable_steady` 入场（4 条新臂），
原因是原入场 `survivable_open_band` 供给稀薄（就绪 20–120/进程），40 分钟后每臂仅 2 笔持仓、0 平仓，
达到"各 ≥20 笔平仓"的判定门槛需要数小时。

### 3.3 缺陷修复：尘埃守卫扩展为**双向**

洗出复盘发现 **3.6%（913/25,408）的仓位含单次 >3 倍的标记跳变**（最大 1.3e7 倍，
某代币 15 秒内 0.000403 → 0.402），不设守卫会把 7 天延迟退出总额从 $2.57k 虚增到 **$21.7k**。

原守卫只挡向下尘埃报价；现扩展为对称判据：

- 向下：`价格 ≤ 中位价×10%` 且 `深度 ≤ 中位深度×25%` → 拦截；
- **向上（新增）：`价格 ≥ 中位价×10` 且 `深度 ≤ 中位深度×1.5`** → 拦截（无相应深度增长的飙升不得定价止盈）；
- 共用节流计数（`kv['mark-outlier-vetos']`）；流动性退出/写销路径不受影响；参照不足 3 条仍 fail-open。

## 4. 验证与部署

- 注册：284 → **286**（两条深池臂），`ledger_unchanged=true`；第 30 波 4 条此前已注册（280 → 284）。
- 测试：`tests/test_mark_outlier_guard.py` + `tests/test_whipsaw_guard.py` + `tests/test_dex_trajectory.py` 全通过（12 项）；双向守卫代码已通过语法与导入校验。
- 部署：运行进程已重载（PID 41028/41016），**双向守卫自此生效**。
- 新臂样本：第 30/31 波六条臂当前均为 0（刚上线）；机制就绪 `survivable_open_band` 98、`survivable_steady` 2、`deep_liq_band`/`non_bsc_deep_band` 0（该深度档在观测面稀薄，需继续观察）。

## 5. 监控与迭代计划

| 观察项 | 判定 |
|---|---|
| 深池臂可达性 | 若 `deep_liq_band` 长期 0 就绪 → 按"观测面分布不匹配"改用 20–100k 档新臂（不改原臂） |
| BSC 专项 | 证伪条件（子代理给出）：≥100 笔新 BSC 入场写销率 <10% 且非负盈亏 → 推翻 BSC 门假设 |
| 双向守卫 | 跟踪 `kv['mark-outlier-vetos']`、"−100% 平仓"与"异常止盈"笔数 |
| 容忍入场噪声 | 依洗出复盘结论，新增"入场后短窗口容忍 + 更紧执行层"的额外臂（与现有对照配对） |
| 同币拥挤 | 影子上限证据已足（被拦组 898 笔 −$333）→ **待用户授权**升级为真实风险层 |
