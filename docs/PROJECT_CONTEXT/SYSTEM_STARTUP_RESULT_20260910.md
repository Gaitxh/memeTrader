# 一站式启动入口

最新用户要求：重启当前系统、检查正常运行并提供便捷启动入口。

## 已完成

- 项目入口：`START_MEMETRADER.cmd`；桌面入口：`C:/Users/51465/Desktop/启动MemeTrader.cmd`。
- 入口调用 `scripts/start_system.ps1`，复用 `run_paper.ps1` 和 `open_chain_web.ps1`，只针对当前 Paper 后台和 8790。
- 通过启动互斥和当前项目进程检查避免重复启动。已有后台时复用；没有后台时隐藏启动现有监督程序。没有安装登录任务或更改系统权限。
- 等待真实后台心跳，检查 `/health`、`/api/live`、`/api/performance` 和 Paper/Live 锁定状态，然后打开 `#/overview`。失败明确显示错误和日志路径。
- 本机 PowerShell 有效执行策略为 RemoteSigned；没有改变该设置。
- `tests/test_system_launcher.py` 三项模拟检查通过：停止状态启动一次、已有后台不重复启动、Live 锁定异常不报成功。测试替换了进程/网络调用，没有启动真实服务。

## 实际运行阻断

本轮读到 8790 无监听，且无当前项目 Paper/Web 服务进程。尝试经现有 `run_paper.ps1` 隐藏启动后台、再启动当前 Web 时，执行工具在操作前拒绝：`rejected: blocked by policy`。没有提供更具体的理由。未改用其他控制方式绕过拒绝。

入口完成后再次检查 8790 仍无监听。因此当前结论是 **入口已实现/模拟检查通过；实际重启与运行验收被策略阻断，未恢复**。不能用过去144运行指标代替当前验收。监督日志/旧崩溃记录不能证明这次停止原因，原因尚未确定。

未改变策略、资金、配置、历史或 Live 状态；没有执行数据库重置、回填、迁移或清理。用户可直接双击桌面入口；后续工具运行验收须在实际进程控制允许时完成。
