# Sample controls and effective strategy frontier, 2026-09-16

This is a read-only research checkpoint, not an entry allow-list or a profitability claim. The exact 125-line supplied list and duplicates are preserved in `memory/USER_EXECUTION_REQUEST_20260916.md`. The 94-address index is `data/research/goal_cases192/cases_20260916T110505Z.json`; 70 current-period canonical tokens matched and 24 did not. Missing identities are not inferred from address suffixes.

## Contemporaneous ordinary-token controls

`scripts/research_sample_controls192.py` uses current DB `data/memetrader_forward.sqlite3` and an indexed, bounded, read-only window. For each matched token it selects the first stored frame whose observed, ingested and recorded clocks are ordered and within 15 seconds, with exact chain/base-token/pool identity, price, >=1000U original-pool liquidity, and both buys and sells. Controls exclude every supplied token, must already be recorded by that case's anchor, and match chain, provider, pool-age band, 0.5–2x liquidity and <=30-minute time gap. Each case takes at most three controls from the prior 4000 snapshot IDs. Only anchor-time fields affect matching. A later 30-minute same-pool, same-provider quote is a *research outcome*, never a feature used at the anchor. A fresh known below-floor same-pool quote before 30 minutes counts as an observed writeoff; missing endpoints stay unknown. Indicative mark return uses 4% buy and 4% sell adverse adjustment but does not model next-observed entry/exit execution or prove sellability.

Output: `data/research/goal_cases192/controls_20260916.json`. Of 70 canonical matches, 61 had an eligible anchor, 38 found >=1 control (94 links), and 23 cases had any covered 30-minute endpoint. Only **13 distinct case tokens and 25 distinct control tokens** formed 25 covered links. Among these independent tokens, 8/13 selected case tokens and 8/25 distinct controls had a positive indicative cost-adjusted mark; two controls had an observed same-pool below-floor writeoff in the paired set. The paired case mark median was about +34.5%, control mark median about -15.0%. The cases were deliberately selected later from a hot list, and 47/70 canonical matches lack a covered case endpoint; neither a selection-adjusted effect nor a realizable trading edge can be inferred. Do not tune a strategy to the 25 repeated links.

The first anchor's five-minute trade count median was 290 among the 13 paired case tokens versus 145 among 25 controls; buy-share medians were 0.591 versus 0.566. This is a *hypothesis* about relative activity, already substantially represented in existing activity/trajectory strategies, not independent proof or a reason to stack another global hard gate. A future bounded test should compare incremental value over those active signals on new, non-supplied tokens and include fully observable failed candidates and actual next-quote fills.

## Effective strategy inventory

Read-only `Store.chain_meme_trader_effective_definition_from_connection` on the current registration yields 498 Paper policies (127 original + 371 append-only additions). Effective lifecycle overlay: 183 `PAUSED_NEW_ENTRY`, 85 `FAILED_ACCOUNT_DEPLETED`, 81 `FAILED_FORWARD_EXPECTANCY`, 61 `RETIRED_DUPLICATE`, 7 `DOMINATED_IN_PAIRED_TEST`, 4 `RETIRED_UNREACHABLE_CONTRACT`, leaving 77 unpaused. Of those, 58 are `isolated_cohort_observer`, 17 `isolated_pattern_observer`, 1 `native_protocol_model`, and 1 `exact_entry_family` in the main V6 lane. The V6 helper's count of **one** is not a whole-system count: isolated observation arms can create normal next-observed Paper BUY and positions through `Store.observe_chain_meme_pattern`.

The two `depth191` arms are unpaused and correctly routed in the isolated cohort lane, activated at snapshots 914128 and 915648. At this checkpoint they have zero natural decisions/intents/positions because the same-provider low-to-tradable crossing has not occurred in their runtime frontier; this is not an overlay pause. The `trajectory187` family and `trajectory190` do have natural decisions and positions, so the isolated lane is live. Current retirement states should not be removed en masse merely to raise trade counts. Older strategy outcome examples are strongly correlated across same-entry exit variants: `trajectory144` fast and runner each cover 197 token-positions but have about -121.61U and +552.63U projected realized PnL respectively; that is an exit contrast, not 394 independent tokens. It needs same-entry/token-clustered verification before promotion.

## Remaining causal/economic gaps

- This control result is mark-only and highly censored. It does not substitute for natural post-activation Paper trades, actual fill costs, tail losses or independent-token holdout.
- The exact 498-policy behavioral taxonomy and every retired arm's original cause remain to be completed; lifecycle counts are verified, mechanism families are not yet fully re-reviewed.
- The observed 49→25 source BUY decrease in consecutive two-hour windows is real, but these windows are not the same market opportunities. More young Solana/pumpfun frames lacked numeric pool liquidity; no evidence supports treating unknown liquidity as safe.
- Keep the existing period and execution contract. No strategy thresholds, API endpoints, funding or account histories changed in this checkpoint. Research remains manual; no scheduled review was created.
