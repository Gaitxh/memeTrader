#66 frozen descriptive protocol
Use ONLY the21 clean terminal fast_max_hold pairs frozen in58, never add later outcomes. Decision cutoff=actual fast TIME_EXIT mark recorded_at; separately record delay from opened+900s. Entry pool from original entry snapshot. Use exact-pool snapshots with observed<=ingested<=recorded<=decision and <=15s record lag; fresh last frame <=30s or UNKNOWN. No later terminal highs/position peak fields as features. Quote-valued full quantity * price *.96 minus original stake (5U, prior buy4% already in quantity); verify no predecision partial sell. Compare positive(>0)/underwater(<=0); liquidity below entry versus >=entry; also retention vs sampled observed high; drawdown from sampled price high, price direction vs latest frame at least60s earlier; activity cooling if current5m volume below latest frame at least300s earlier (not wallet/net flow), missing stays UNKNOWN. Sampled extrema are lower bounds, not exact runtime running highs. Qualitative splits only, no threshold grid. Candidate support conjunction frozen: positive AND liquidity>=entry AND nonnegative60s direction AND noncooling5m volume with fresh same-source comparison. Evaluate incremental runner-fast, token/chain/date and top1/top3 removal; no registration absent robust independent support.
