# 九方向增量实践（2026-09-05）

用户已授权：先修显式入场池价值低于1美元仍BUY；保留原交易、现金、PNL，区分工程异常和自然亏损。随后逐一实现九方向与机制对照，持续观察实际结果，必要时用新ID调整。不是九项报告替代九项实现；不要求短窗口证明盈利。

## 当前进展（12:22Z；下文保留阶段历史）

**12:24Z部署事实**：7102626已推送。本轮受控重启在命令执行前被工具执行策略拒绝；未绕过，未停止原进程。8790于12:23:41Z仍健康、142策略、原账期；最后四臂尚未部署，不能宣称九方向自然输入全部验收。须本机正常重启路径恢复后继续。当前短窗口策略周期P50/P95=1.012/1.548秒；行情周期1.810/4.914秒；外部持仓获取1.270/4.866秒；按批写入退出.0358/.0835秒。与11:50量级近似，但负载/上游不同，不归因式宣称改善或长期SLA。

02e04c0已于11:54:18Z部署；D6/D7四臂于11:54:21Z新增，实际142策略、原账期起点07:37:08.842373Z保留。11:54检查心跳年龄0.425秒、Live锁定，49账户研究指标隔离而实际资金不变。当前补齐D1/D8代码，拟新增最后四臂至146；未把代码通过当作部署或自然盈利。

D1使用现有Solana RPC的[getSignaturesForAddress](https://solana.com/docs/rpc/http/getsignaturesforaddress)和[getTransaction](https://solana.com/docs/rpc/http/gettransaction)：首次只建立实时签名前沿，不回取入场历史；随后每15秒最多轮询两个已验证池，每池五秒预算，最多十个签名。完整窗口才计算已验证BUY指令签名地址广度、新进入地址及最大买方指令次数占比。按官方[PumpSwap IDL](https://github.com/pump-fun/pump-public-docs/blob/main/idl/pump_amm.json)核对program、pool、base/quote mint、vault、签名用户、outer/CPI指令。LP/未知指令、超限截断、RPC失败均明确为覆盖不完整；地址不等于独立人，次数占比不是资金占比。候选比较至少5地址/3新增/最大次数占比不高于50%，对照仅要求完整可用参与样本；不使用交易笔数冒充钱包数。

D8复用异步趋势Agent及事实核验：至少间隔60分钟、每天至多24次、单轮一事件/最多三来源，初始搜索最多二次（后续独立事实核验另受现有预算限制）。只能使用当时实际抓取的两独立原始来源正文中一致链/合约、30分钟内信息及确认事实；Agent摘要、营销列表、单一转载源不能成为证据。EVM无明确唯一链名不绑定。信息优先候选不附加价格动量门，对照增加当前买压/活跃确认。新信息最多补取两个目标行情，复用当前有限观察集，下一独立同池样本才成交。观察集已满仍可能没有覆盖，不虚报全市场覆盖。该Agent没有交易/钱包权限，不进入旧策略决策队列，不阻塞持仓退出。

最小验证已通过：D1指令身份、真实签名/完整窗口/截断边界；D8来源时间/独立来源/合约一致/推广拒绝与候选对照；有界scout的小时due检查；Solana/BSC下一帧同Fill、现金与旧策略隔离。部署前运行调度检查另拦下新任务错误并列到同一个create_task的问题，已分离成独立任务，回归通过。没有修改旧策略合同、历史收益或重复跑全套。

Lead反例处置：ACK_IMPLEMENTED（解释边界）。D6现有候选同时排除unwind与synthetic-support，是复合风险试验，不能将结果单独归因为某一因子；两个原始特征仍独立保存，不在同ID中改门控。D7证明的是migration事实之后的canonical兼容池，不将Token级事件吹成该笔事件创建该池的密码学证明。无需再加一层审批或修改已部署合同。

公共检索接入研究：现有免费[Bluesky AppView搜索](https://docs.bsky.app/docs/api/app-bsky-feed-search-posts)、[Mastodon标签流](https://docs.joinmastodon.org/methods/timelines/)、[RSS](https://www.rssboard.org/rss-specification)可提供时间戳正文；搜索排序不等于完整全量采样。[GDELT](https://blog.gdeltproject.org/gdelt-doc-2-0-api-debuts/)适合低频背景发现，不承担数秒退出。暂不增加收费逐笔源或新的常驻Agent平台。未来有稳定原始author ID才研究独立作者加速，付费推广需按[DexScreener官方字段](https://docs.dexscreener.com/api/reference)分层，不能将未观察到推广当作自然传播。

可检验的后续组合方向（不是已证alpha）：真实地址扩散×原始资金流、静默压缩×持续新参与、canonical迁移×深度留存、来源注意力×价格确认。先观察现有九组机制差异；不机械拼接或以微调参数扩充数量。

### 12:20Z 自然样本截点（部署前）

每格为BUY/SELL流水/终结仓/开放仓/已实现U，候选与对照是相关样本，不能叠加为独立系统利润。所有数据是当前资金账期、部署之后；不是测试造数。

| 方向 | 候选 | 对照 | 当前解释 |
|---|---|---|---|
| 静默复苏 | 0/0/0/0/0 | 6/7/6/0/-4.975 | 尚无满足完整静默后复苏的候选；对照说明数据在进入 |
| 持续突破 | 6/7/5/1/-15.170 | 6/8/6/0/-0.746 | 小样本不支持候选胜出，也不能调参追平 |
| 回撤收复 | 0/0/0/0/0 | 9/12/9/0/-4.200 | 尚无候选收复信号；保留等待与覆盖分母 |
| 条件残仓 | 35/25/30/5/-191.803 | 35/23/30/5/-177.825 | 首个约65分钟窗口候选较差，研究利润回吐和失池，而非直接宣布规则失败 |
| 恐慌收复 | 2/3/2/0/+0.540 | 3/4/3/0/-3.870 | 仅2/3终局，不能凭小额正收益称alpha |
| 支撑风险 | 2/0/0/2/0 | 2/0/0/2/0 | 真实输入已产生BUY；开放仓仅约10分钟 |
| 迁移确认 | 0/0/0/0/0 | 4/1/1/3/-1.538 | 候选缺更强迁移证据；对照是已验证池，不证明迁移关联 |
| 早期参与扩散 | 未部署 | 未部署 | 四臂中的两个，部署后才建立RPC前沿 |
| 信息优先注意力 | 未部署 | 未部署 | 四臂中的两个，部署后按既有Agent预算运行 |

池证据已从12:00的两帧推进至12:18的97帧/4池，36次解析中32次identity mismatch、4次成功。不能放松身份检查来追求交易数。物理Shadow帧的decision_eligible=false不意味着模式输入停用：D6只通过独立严格上下文读取已满足覆盖的原始特征；已有两笔自然BUY可核对。主源健康继续推进，WebSocket有一次ConnectionClosedError后仍在重连/采样；不宣称永不掉线。现有6池/每次2池/5秒预算是为了后台优先的明确容量，不为扩覆盖立即放大并发。

## P0：入场与核销底线一致

原因：market Paper入场只校验正价格，未检查显式池价值；退出却将同池价值<1的剩余仓核销。
修复：在公共入场分类前拒绝同一有效快照明确liquidity<1，写入拒绝原因及源池/流动性。NULL不等于0；恰好1不按此规则拒绝。不改变历史合同或合法入场阈值。
历史：从不可变entry_snapshot读取工程异常标记，账户/仓位标为不可直接用于策略优劣评价；不删交易，不退款，不重算反事实资金路径。描述性收支仍保留，不把剔除异常后的子集叫作真实账户收益。
截至10:25:28Z，当前账期366个策略仓位、52个共享cohort、49个策略命中显式入场dust，记录已实现合计-7320U。该合计含同币不同策略副本，不能作为独立系统损失或独立样本数。最后此类BUY为10:16:48.782648Z。
验证：6个入场边界用例（0、0.99、raw0.48、1、10000、NULL）；同价20U双侧4%算例、历史标签不改交易及资金；Store/Web最近相关回归通过。测试同时修正旧fixture只清通用行情而未清原入场池行情的问题。无生产历史写入。
部署状态：7cea431已推送，10:27:32Z已受控重启，原128策略与资金起点不变。部署后首个检查新dust BUY为0（未遭遇的新输入不算已验证自然拒绝）。部署前策略周期P95=1.106s，持仓批次写入退出P95=.098s；外部获取P95=8.499s，不能宣称每币秒级。

## P1第一批：五方向、十个独立候选/对照

方向2/3/4/5/9已有确定性机制和单一观察入口，下一独立同池采样成交。每臂新ID/真实注册边界、独立1000U；旧128不重置、不修改合同。普通入场不消费strategy-observer快照，避免扩大旧策略入场机会。候选及对照共享行情；每链最多10目标，早期3/成长4/成熟3，15分钟观察周期。行情批次被动复用；持仓目标绝不另外请求。非持仓每轮最多一个10币批次、三秒等待上限、低优先级可用时才请求。错过的观察是覆盖不足，不当作市场静默。

方向3检验连续突破对比末端冲高；方向4检验回撤后收复对比直接追涨；方向9检验恐慌中流动性保留；方向2要求真实10分钟静默序列；方向5同入场下+12%成本后收益处，对照全出，候选仅连续两帧买入比>=55%且流动性保留>=80%时留一半，否则全出。残仓+30%或15%高点回撤退出，通用硬止损/失池规则先行。这些阈值是预注册可检验假设，不是拟合收益承诺。

代码含九方向定义，但本批只注册上述十臂。方向1真实钱包、方向6精确Vault、方向7迁移RPC确认、方向8独立信息来源仍在接数据，不能宣称已完成。PumpPortal交易feed当前收费，未启用；正在复用免费RPC研究受限真实签名地址采样，样本覆盖须如实报告。尚无新十臂自然盈利结论。

部署阻断（10:58:38Z）：a2e6f15已提交推送；Solana/BSC同Fill/下一帧SELL/双侧4%/原池大小写集成、真实DEX空ingested_at、持仓复用及既有共享lane测试通过。受控重启命令被本机执行策略拒绝，未执行；未换shell/进程工具绕过。只读检查8790健康，仍128策略、experiment_*为0，旧进程及资金账期均保持。新十臂仅代码就绪，须由用户处理重启后才开始注册及自然观察。其余四方向没有部署，也尚未完成输入集成。

部署阻断已解除（11:15Z）：用户明确再次授权由Codex重启，正常PowerShell进程操作成功；11:14:44.957Z重启，十臂在11:14:49.385409—11:14:49.431773Z从snapshot835569开始注册。8790实际返回138策略/十个experiment臂，旧账期仍funding-20260905-fixed-1000。旧canonical-0006b989b189e0ac起点07:37:08.842373Z不变，现金19.078991290793738、开放仓5均与重启前一致；新臂初始现金1000。重启后短窗口策略周期P95=1.024秒、batch apply/exit P95=.105秒，后台新任务已调度；样本不足以宣称持续性能或盈利。没有重跑通过的测试、没有重复初始化。其余四方向仍未完成。

自然进展（11:16:45Z只读检查）：已写171份pattern观察，7份形成入场投影；新臂合计13BUY、2SELL。均为部署后当前账期记录，不是测试数据库。账户副本数不能作为独立Token样本数，这个短窗口仅证明执行链路已推进，不证明策略优劣。

## 实施队列（均保留，不能以完成P0代替本队列）

### 11:51Z 阶段：真实输入与研究统计隔离（代码验证通过，部署待核验）

方向6/7增加四个独立臂：原138不动，部署后预期142。方向6复用现有confirmed accountSubscribe，预入场最多6池、每15秒最多解析2池、5秒预算、先等待持仓工作让位。独立逻辑观察器共享物理订阅，不制造持仓；同slot两vault原始储备变化可作为入场风险特征，effective depth缺失仍保留UNKNOWN，不能把虚拟储备当现金。候选排除已观察到的unwind/synthetic support；regularity本身不叫SCAM，不触发用旧DEX价退出。

方向7对照为已RPC验证池，候选还需要真实部署后migration事件和两份事件后独立行情。验证index=0、Pump pool-authority creator与精确PDA，避免同mint的任意PumpSwap池被误称迁移池。官方依据：[PumpSwap Pool及迁移结构](https://github.com/pump-fun/pump-public-docs/blob/main/docs/PUMP_SWAP_README.md)。两组都使用下一独立同池行情成交、现有双侧4%和独立1000U，不移植Solana特征假装覆盖BSC/Robinhood。

已修实证统计缺陷：11:33Z只读审计发现49/138策略含入场dust工程异常，榜单前三亦不合格；虽然已有标记，PF、EV、胜率与尾部仍参与研究展示。现在研究指标置空/隔离，研究榜单排除不合格账户；保留真实cash、equity、realized/unrealized PNL、账户回撤及原交易。全策略账户表仍可按真实金额查看，不隐藏账户。榜单声明的expectancy排序与后端实现同步，0收益不再被`or -inf`当缺失。

经济核对（不是盈利结论）：panic候选cohort10701实际20U BUY、14.932493650392857U SELL，已实现-5.067506349607143U。入场.0005179×1.04=.000538616，20/执行价=37132.205504478 tokens；退出.0004189×.96，数量×退出价与账本一致。quiet对照cohort10681半卖2852.383451612 tokens回收13.6120302124U，分摊10U成本、实现3.6120302124U；不是把半卖全部回收额当利润。同价往返20U的双侧4%损失是1.538461538U，而非简单1.60U。

研究判断：cost-scaleout的500个原始终局中17个入场dust；全样本PF=.622242、EV=-1.827849U，去掉这17个后的诊断子集483个PF=.723987、EV=-1.188249U。该子集仍不等于未发生工程错误时的真实资金路径，不能退款或证明策略收益。条件runner约12仓/臂、恐慌反转2/3仓、突破2/2仓，窗口极短；当前不支持挑一个赢家改旧策略。可研究成本阈值以上真实兑现、保留流动性的反转、持续参与而非末帧冲高，均已转成对照试验，等待同期证据。

性能11:50Z：持仓外部获取P50/P95=1.175/4.005秒，按批写入退出=.0385/.0904秒；策略实际周期P50/P95=1.010/1.564秒，当前行情实际周期2.024/4.162秒（目标1秒）。Web5秒/隐藏30秒/币详情10秒独立刷新。外部获取仍是主要长尾，不是通过重复页面计算提速；新证据保持低优先级。DB9.13GB/WAL18.19MB，不做在线重型清理。

定向验证：D6风险候选/对照、D7真实时序/池身份/两帧、预入场复用订阅不制造仓位、旧resolver布局边界、Web研究隔离/排序共4个测试通过。方向1实际签名地址参与、方向8独立信息输入仍未完成；定义存在不算输入就绪。

1. 早期真实参与扩散：交易流代理与钱包广度分开；缺独立钱包数据不得换名冒充。候选对照检验扩散持续性。
2. 实际观察到的静默压缩→复苏：必须有部署后静默序列和后续确认；对照只看末端活跃。
3. 持续多窗口突破：对照单帧冲高；同池序列，成本后退出。
4. 冲击→回撤→收复：对照不等待回撤；未收复不能买。
5. 条件快速兑现/runner：同入场同规模对照，流动性/持续性决定保留残仓；硬风险不被叙事覆盖。
6. 机械交易/合成支撑风险：解释性状态+独立对照，不设SCAM通用门；Vault风险不能在旧DEX屏幕价伪造成交。
7. 曲线→AMM迁移确认：真实migration时间与新池身份，不能把首次DEX出现当已验证迁移。
8. 信息优先注意力/叙事：异步有界Agent和时点证据，价格持续性作独立对照；不得阻塞退出。
9. 流动性保留的恐慌反转：停止创新低后收复，留存对照；组合只采用明确可观察状态，不事后挑胜者。

实现边界：复用当前Registry/Fill/账户/退出路径；新增观察数据隔离旧128入场；共享有界采样，优先持仓和退出；新ID部署frontier后开始。每策略1000U、普通20U、双侧4%，无MAE/MFE、不重置、不回填、不覆盖、不启用Live。
每阶段：最小相关测试→提交推送→受控部署→自然样本核验；机制测试、人工场景和自然前向效果分开记录。既有两小时循环负责长期复盘，不重复创建自动化。
