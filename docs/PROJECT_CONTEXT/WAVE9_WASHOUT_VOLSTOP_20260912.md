# 2026-09-12 全面审查第 2 轮：洗出后再入场 + 波动率自适应止损（wave 9）

承接第 1 轮（修复两处可达性缺陷、量化“被正常振幅洗出”）。本轮把该量化结论直接转成新策略，并完成一次阶段性经济复盘。

## 1. 本轮依据的实测分布（600 个实时特征样本）

| 特征 | p10 | p50 | p90 |
|---|---|---|---|
| `volatility_30`（30 秒实现波动率） | 0.0 | **3.7%** | **18.9%** |
| `drawdown`（相对本段高点） | −28.3% | −0.08% | 0.0 |
| `return_30` | −11.2% | +0.59% | +11.0% |
| `frames_total`（本池累计帧） | 1 | **92** | 155 |
| `window_frames_30` | 12 | 14 | 18 |
| `plateau_fraction` | 0.86 | 0.92 | 0.99 |
| `pool_age_seconds` | 502 | 774 | 16,027 |

**关键推论**：固定 −20% 止损 ≈ 中位波动率的 5.4 倍，但对 p90 高波动池只有约 **1σ**——同一档止损对低波动池过宽、对高波动池过窄。这正是“被正常振幅洗出”的机制解释，也是本轮新增自适应止损的直接依据。

## 2. 新增策略（wave 9，5 条全新 ID，未改动任何现有策略）

| arm_id | 入场 | 退出 |
|---|---|---|
| `alpha149_washout_reclaim_v1` | 新机制 `washout_reclaim`：本段回撤≥12%、当前帧重新上行、深度≥前帧98%、买盘≥50%、深度≥2000U、本池≥4帧 | 原 −20%、15 分钟（快出对照） |
| `alpha149_washout_reclaim_hold_v1` | **完全同一入场信号** | −45%／宽限180s／连续2帧／深池买盘占优容忍；追踪+45%→25%；120 分钟 |
| `alpha149_vol_scaled_merged_v1` | `merged_multi_setup`（与 wave 8 合并臂同一入场） | **新退出 kind `alpha149_vol_scaled_stop`**：允许回撤 = 4×本池30秒实现波动率，下限 22%、上限 50%；−90% 仅灾难兜底；追踪+45%→25% |
| `alpha149_vol_scaled_goldendog_v1` | `sf_goldendog_deep_base` | 同自适应止损；追踪+60%→30%；240 分钟；1U |
| `alpha149_vol_scaled_exit_v1` | 同其它退出载体臂（搭载本池第一条 ALPHA149 信号） | 仅自适应止损 + 追踪，用于同机会对照 |

- 自适应止损参数为冻结实验定义：`VOL_STOP_MULTIPLE=4.0`、`FLOOR=0.22`、`CAP=0.50`；波动率未知时**不触发**（不把未知当 0）。
- 注册：append-only，`policy_additions` 221 → **226**，账本未变、资金周期与激活前沿不变；20:31:15Z 加载。
- 生效定义 353 条臂；5 条新臂合同完整（`trajectory_exit=alpha149_vol_scaled_stop`、hard_stop=−0.9 兜底等）。

## 3. 加载后自然证据

- 引擎非零机制 **4 → 23**（`righttail_lottery` 147 次 ready、`writeoff_structure_avoid` 134、`gap_repair_continuation` 76…）；出信号臂 30 条；wave 9 的自适应止损臂已出信号（merged 4、goldendog 2）。
- **wave 8 同信号 A/B 首次出现方向性差异（n=1，仅作机制验证）**：`survive_noise_wide_v1`（宽止损）以 **追踪退出 +0.2617U** 平仓；`survive_noise_confirm_v1`（同一入场、原 −20% 止损）以 **硬止损 −0.6353U** 平仓。
- 错误 #265/#266 自第 1 轮修复后**保持冻结**。
- 实时性无回归：`held_fetch` p50 0.76s / p95 3.14s；`held_apply_exit` p50 20.5ms / p95 27.3ms；`alpha149_features` p50 0.5ms；各周期 fail=0。
- 漏斗（最近 60 分钟）：发现 49,337 → 评估 46,994（95.3%）→ 准入决策 1,812 → 开仓 880 笔 / 137 个币。**“已发现代币未进入策略判断”已不是当前瓶颈**（缺口 5% 为无行情数据的代币）。

## 4. 阶段性经济复盘（最近 7 天，活跃周期，≥20 笔平仓的 221 条臂）

- **净盈利臂仅 5/221**。前三名：`resource_age_rate_candidate_v1` +634.96U（n=220，均值 +2.886U；退出结构 110 次最长持有 / 25 次追踪 / 81 次止损 / 仅 4 次写销）、`age_rate_horizon_fast_v1` +109.91U、`early_impulse_profit_lock_control_v1` +18.56U。
- 亏损端典型：账户耗尽型 −995～−999U（`staged_probe_5u_*` 1,135 笔、`common_funding_adjusted_breadth_5u_v1` 938 笔、多个 `canonical-*`），共同特征是**止损+写销笔数极高**。
- 退出族（7 天，41,085 笔平仓）：硬止损 12,697 笔 **−61,540U**；流动性写销 7,910 笔 **−102,354U**（事后仅 6% 回到入场价上方 → 真死亡盘）；追踪 +20,584U；最长持有 +7,115U；分批止盈 +13.2/+13.4U 每笔。
- **新增家族自身（alpha149，149 笔 / 85 笔已平仓，−85.07U）**：写销 28 笔 **−80.0U**、硬止损 30 笔 −20.2U、追踪 21 笔 **+14.4U**、最长持有 20 笔 +1.3U。→ 对本家族而言，**主要亏损来自“选到了会死的池子”（写销），而不是止损时机**；这与全库层面“止损是最大可控亏损”并不矛盾，指向两个不同杠杆：**入场存活率**与**退出容忍度**。

## 5. 验证

| 项目 | 结果 |
|---|---|
| `tests/test_alpha149.py`（新增：`washout_reclaim` 需同时满足洗出+回收、非回落/深度流失/帧数不足均不触发；自适应止损随波动率放大、上下限、未知波动率不触发；wave 9 合同与不污染既有臂） | 18 项 PASS |
| `tests/test_whipsaw_guard.py` | 2 项 PASS |
| 实盘：机制 23 种、信号臂 30 条、5 条新臂合同落地 | 通过 |
| 错误冻结、Live 锁定、资金周期/激活数/账本不变 | 通过 |

## 6. 诚实边界与下一轮方向

1. wave 8/9 的自然样本仍极小（各 1–4 笔），**不构成任何盈利结论**；同信号 A/B 是唯一能回答“放宽止损是否更优”的证据来源。
2. 事后回升统计基于快照路径的中位数，仍不排除极薄池的单点异常价。
3. 自适应止损的下限 22% 高于原固定 20%，因此**对低波动池它反而略宽**；这是刻意的（低波动池的 4σ 更小），需要前向数据检验。
4. 下一轮（3/256）方向：① 入场存活率族（写销规避/池质量分层与按风险定投注额的新臂）；② 退役/暂停臂的系统复盘与合并整合；③ 继续追踪 A/B 臂的自然差异。
