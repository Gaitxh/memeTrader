# Narrative local X hints — 96C

Reply to `C2C-20260909-NARRATIVE-LOCAL-X-96C`. Implementation `ef49ec4`, loaded prospectively at 2026-09-09 18:15:07Z.

The existing narrative checkpoint now reads local hints only when an existing checkpoint is due. It reads at most 32 token-source links and 16 token memberships, keeps at most 16 hints, and persists them separately from Scout sources. First local receipt must be after opening/previous checkpoint and no later than current cutoff; repeating an old metadata link does not create a new arrival. Episode availability conservatively includes episode and membership recording clocks. Unknown publication time remains unknown.

Hints carry URL/status/claimed handle, publication/local availability, provider/role/verification status and as-of episode fanout. Both single-token Reuters references and multi-token celebrity references remain UNTRUSTED_LEAD, never verified origin/binding. Fanout is ambiguity, not endorsement or positive diffusion. Hints may enable a later scheduled research checkpoint; they never enter the deterministic confirmation input directly. Scout/Verifier and existing fresh-source next-checkpoint rule remain unchanged. No new timer, network request, Agent budget, trading rule, watch capacity or funding change.

EXPLAIN before: token_source_links uses token_source_links_token_idx; membership token query scanned the table and sorted. Added only provider_post_ambiguity_memberships_token_idx(token_id,candidate_recorded_at DESC,id DESC). Production had 2,578 membership rows. After deployment EXPLAIN uses the new token index; episode primary-key lookup and existing episode index serve metadata/fanout. A bounded 16-token local read took 0.00412 seconds. No hot-loop full scan; bounded tails are hints, not a complete social denominator.

Validation: 13 existing narrative tests passed; two local-hint regressions passed after correcting a test's unnecessary equal-timestamp ordering assumption. Covers fanout, single news hint, future receipt exclusion, old-link refresh, later arrival, read-only SQL, index usage and no confirmation. Existing test verifies newly searched evidence cannot confirm until a later checkpoint. git diff --check passed.

Artifacts: data/research/narrative96c/readback.json; data/research/admission88/pre96c.json and post96c.json. Seven immutable registration/funding/activation tables retain digest 5abffced88f8b8e3228071a123dd8bb79f9d0effca00b1e9a85a5d17194c71a3. Health running, Paper=true, Live locked, one held position. Initial 46-second post-start window: passive drops 0; held-fetch p95 3.213s versus pre 2.544s, apply 47ms versus 55ms, pattern 7.472s versus 6.112s. Startup windows are not comparable long-run acceptance. No natural local-hint checkpoint yet at readback: implementation/deployment verified, natural checkpoint and sustained latency acceptance pending. No claim of profitability or confirmed social growth.

18:17:37Z follow-up (post96c_later.json): held-fetch p95 3.583s, apply 59ms, pattern 8.937s, audit drops zero and funding digest unchanged. These overall timings remain elevated, so runtime latency acceptance is NOT claimed. The added local query is checkpoint-only (not a 15-second scan), measured 4ms for 16 tokens; the snapshots do not establish that this change caused network/pattern latency. Keep this limitation visible rather than calling the deployment a performance improvement.
