# 第 9 轮：死池前兆退出臂（第 29 波）+ 前序证据链收口

- 记录时间：2026-09-12T04:10Z（北京时间 12:10）
- 约束不变：`mode=paper`、`live.enabled=false`、现有策略规则零改动；policy additions **280**

## 1. 诊断结论（数据支撑，来自第 7/8 轮）

| 结论 | 证据 |
|---|---|
| 写销是当前主要失血点 | 近 60 分钟：写销 **92 笔 −304.0U**；普通平仓 296 笔仅 −64.98U |
| 价格类全额亏损已被压到次要位置 | 硬止损 12 笔 −20.98U、追踪 4 笔 −8.0U（第 5 轮尘埃守卫上线后） |
| **放宽/延后止损无效** | 延迟 60 分钟退出估计仅 **+0.69$ / 21 笔**；硬止损"跌破成本 20% 后回本"仅 **4/241 = 1.7%**（全臂 7d 0/7） |
| 尘埃报价缺陷真实且已修 | 单条报价比池深度均值低 4 个数量级，把 +384% 的单子结算成 −100%；近 60 分钟仍有 4 条同类报价（1,952 条标记中） |
| 无本目标回归 | 错误 #267–#276 全为外部网络超时或 `database is locked`；冻结 #265/#266 未增长 |

## 2. 优化动作：第 29 波 2 条额外臂（append-only 278 → 280）

| arm_id | 入场 | U | 持有 | 合同 |
|---|---|---|---|---|
| `alpha149_deadpool_open_band_v1` | `survivable_open_band` | 1 | 60 分钟 | 去价格止损（−90% 仅灾难兜底），改用既有深度衰减退出：30 秒深度 ≤−15% 且换手 <0.1 且深度保留 ≤0.8 |
| `alpha149_deadpool_control_v1` | 逐字同一入场 | 1 | 60 分钟 | 系统默认 −20% / +30%→15%，不声明深度衰减退出 |

设计要点：同入场、同规模、同时限，唯一差别是"死池前兆退出"，因此可以直接回答
**"提前离开正在枯竭的池子能否救回写销损失"**；复用本族既有的 `alpha149_depth_decay`
种类，没有新增或改写共享引擎逻辑，也没有改动任何现有臂。

## 3. 验证

- 注册：`policy_additions` 278 → **280**，`ledger_unchanged=true`；
  两臂参数核对：处理臂 `notional=1.0 / max_hold=60 / hard_stop=-0.90 / trajectory_exit=alpha149_depth_decay`，
  对照臂 `notional=1.0 / max_hold=60 / hard_stop=-0.20 / 无 trajectory_exit`。
- 测试：`tests/test_alpha149.py` 与 `tests/test_alpha149_wave2425.py` 全通过（当前 116 条臂）。
- 部署：运行进程已重载；重载后 `survivable_open_band` 就绪 38 次、`open_band_scored` 22 次。

## 4. 监控与迭代计划

| 观察项 | 判定规则 | 下一步 |
|---|---|---|
| 死池前兆 A/B | 两臂各 ≥20 笔自然样本后比较写销率与总盈亏 | 处理臂写销率显著更低 → 推广到更多入场；无差异 → 说明前兆特征不够早 |
| 第 28 波长持有 A/B | 各 2 笔已配对 | 继续累积；先验预期收益小（反事实已证） |
| `thin_depth_tier` | 当前 0 就绪 | 下一轮仍为 0 则按"不可达"诊断，不调阈值 |
| 尘埃守卫 | `kv['mark-outlier-vetos']` 与"−100% 平仓"笔数 | 若阻止后仍有 −100%，追查第二条污染路径 |
| 诊断负载 | `database is locked` 频次 | 避免背靠背重扫描；必要时把重统计改到数据库副本 |

## 5. 长期闭环（已建成，逐轮运行）

1. **测量**：`scripts/supervise_metrics.py`（链路速率/通过率、代币级信号率、拥挤度、影子上限、9 条触发）；
2. **逐笔上下文**：`scripts/trade_context_ledger.py`（发现来源、入场机制与决策、峰值、退出、洗出标记）；
3. **复盘**：家族/持仓桶/链/退出原因汇总 + 子代理周期复盘与反事实；
4. **反哺**：每条结论都转成"同入场 + 不同合同"的**额外臂对**，再回到第 1 步测量。
