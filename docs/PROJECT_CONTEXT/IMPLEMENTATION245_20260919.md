# 三条主线续作245：真实增量选币瓶颈与在运行策略校准

## 范围和事实边界
最新用户要求仍以原十二节任务和完整样本为依据：现有/退役策略复核修订、新机制前向实现、发现采集与系统实时性三条线并进。Paper-only，不能以测试数量、报告数量或新增账户替代净收益，也不以无法证明盈利为由无限推迟合理试验。本文件只记本轮实际发生的工作，不把旧成果重算。

原143行及补充56项仍用既有原文和研究索引；本轮没有重新判链、重写异常地址、添加指定币白名单或重新做已完成的17键检索。未恢复的当时可见性不能用新资料补齐。

## A. 策略现状：524不是旧521
05:32UTC读取真实manifest：15968于04:57:16.643069Z启动，524条定义；已登记源码哈希无差异。实际新追加表显示：

| 策略 | 注册UTC | 激活快照 | 与父级的区别 |
|---|---|---:|---|
| revision242_retest_valid_at_fill_v1 | 04:33:25.876566 | 1714490 | 相同234回撤信号和退出；实际安全检查后成交前，再确认原折价、反弹和池深仍成立 |
| regime243_recent_trend_health_v1 | 04:44:26.804027 | 1717391 | 原144信号与退出不改；根据此前6小时、最多20个不同父级终局Token的净收益及核销比例，决定是否接纳 |
| revision244_robinhood_quiet_acceleration_v1 | 04:57:14.049542 | 1720583 | 从退役152对照提取Robinhood场景；相同信号、既有安全路径和退出，不重开父账户 |

243要求至少10个终局、合计净PnL>0、核销比例<=15%，这些是可否定的环境假设，不是已验证的盈利规律。244保留30分钟上限/-20%止损/30%-15%移动退出，不能称为高胜率快进快出。
这些注册早于本轮05:32恢复核验；本轮没有再次注册或冒称刚部署。证据：data/research/implementation243_20260919/resumed_state243.json。

242的遗留Store集成失败发生在注册所需历史定义JSON缺失阶段，并不是新交易判断失败。补齐原项目资源后，19项测试通过：新版过期折价只拒绝自己，旧臂仍可买；允许时共用同一后帧BUY；分批退出与父策略一致。没有生产回填交易。回执：data/research/implementation243_20260919/store242_243.xml。

本轮新的父/子策略自然盈亏提取被平台拦截，未执行，不能拿此前16笔或521条旧状态冒充现在的结果。归档中已有的正负收益仍各自保留时间和账本实例，不做整体收益简单相减的因果结论。

## B. 新的独立机制选择及阻碍
选择了“已见上涨→浅回撤横盘承接→后续新帧再突破”，目标是补230必须出现7%–20%回撤的覆盖空隙，而非再给所有策略加过滤。计划区分新池趋势持有与老池短持兑现，仅消耗已有行情和有界状态，不加采集器。模块写入被平台拦截，没有生成源码；未换名/换通道重写，也未注册。该方向目前只能标为设计决定，不能标为实现或运行。

## C. 发现采集：真正上线的周期性瓶颈修复
先测量再选择：243近期结果查询实测5.6ms，本轮不为此新增索引或缓存。现有flat老币目标选择任务的选择计算p50=0.4780s/p95=0.6232s；整任务p95=4.875s还包含网络等待，不能全部归咎于选币SQL。

`FlatSelector._advance`已知道上次及当前的整数主键前沿，却被查询规划器选成整期`definition_version`/pool索引搜索，加临时排序。真实固定150行增量：旧查询0.3739785s，新查询0.0026335s，逐行相同。新计划走INTEGER PRIMARY KEY(rowid>? AND rowid<?)，不再出现临时排序。

修复仅在增量查询的evaluation表声明NOT INDEXED；SQLite仍允许rowid访问。保留原版本过滤、两个主键前沿、排序、身份、JSON字段和元数据关联。不添加截断LIMIT、不丢记录、不新增索引/HTTP、不调低更新频率，其他表仍可使用索引。初始全量bootstrap未改。

SQLite官方依据：https://www.sqlite.org/lang_indexedby.html 与 https://www.sqlite.org/eqp.html 。此处是已证实误选索引后的窄修正，并非把规划限制当通用优化套路。

验证9项全部通过，包含已有跨版本交错、持仓开闭、换池等结果一致性；新增使用SQLite虚拟机指令步数验证扫描规模，不用容易波动的毫秒阈值判断测试通过。测试没有读写生产订单。

文件：src/memetrader/flat_selector.py；tests/test_flat_delta245.py。真实原文件逐字节备份flat_selector.before245.py，写入前检查无并发编辑。05:42:02UTC已应用测试过的源码，SHA256=21f9a1a4e583b701fb05ff9c85af641503b4ef445a032850c0400b55e8d055bb。

已核验15968→28732(本项目venv)→4296(本项目run_paper.ps1)进程关系，仅停止15968一次，由原监督器重载。配置Paper且Live=false、新文件哈希匹配后才停止；没有停Web或监督器，也没有改资金或策略控制。

05:44:30UTC回读新进程32668，启动05:43:30.875178Z，524条定义、原资金期，health=running。该旧manifest未收录flat_selector.py，不能伪称已有专用加载哈希；使用受验证源码、正常重载和启动后实际组件计时作为证据。

首分钟9个选择样本p50=0.05483s，p95=5.05389s，含一次昂贵启动初始化；不能选择性只报中位数或说全系统提速142倍。当时被动队列0丢弃，持仓报价最大年龄4.22s且有1个来源失败标记；这些是单次窗口，不是永久SLA或保证无问题。

证据目录data/research/implementation245_20260919/：flat_query_reproduction245.json、flat_tests245.xml、application245.json、deployment245.json、performance_before245.json、performance_after245.json。回退只能恢复本轮flat_selector.before245.py并走同样正常重载，不得用Git HEAD覆盖共享runtime/store。

## 最后实际运行读回（2026-09-19T05:49:51.402903Z）
- health=running。原524条策略定义、同一资金期的监督式重载已经验证；本轮仅更改选择器，不将242/243/244再次注册。
- 选择器73样本：p50=24.19ms，p95=107.28ms，p99=2426.99ms，0失败。启动峰值仍留在统计中，没有删除。前窗120样本p50=478.03ms、p95=623.17ms；负载和Token集合不同，仅作观察性比较。
- 含网络的flat完整任务27样本p50=0.938s、p95=2.6984s，0失败；不把SQL的约142倍比值用于全任务或全系统。
- 被动队列740入/739处理/深度1、0丢弃；该进程计数不可与重载前的累计数相减当吞吐增量。
- 读取时Robinhood一枚持仓报价年龄0.735s，Solana两枚0.072s，缺失/失败/覆盖缺口标记均为0。先前一分钟曾有一个来源失败，未抹去；这不是未来SLA。
- 更新历史文件已合并221条，旧条目保留；/api/updates实际返回本条ID。相应成功回执history_receipt245.json、final_readback245.json、final_performance245.json。

## 三条主线与原要求逐项交付状态
| 原任务组 | 本轮进展 | 不能混作完成的部分 |
|---|---|---|
| 旧/退役策略：06，结果验证09 | 校准已上线的242成交复核、243近期结果环境、244退役152分链修订；242真实Store19测试通过 | 19测试不等于新收益。最新父子盈亏提取被拦截；不能宣布全部策略重新审计完或已盈利 |
| 新机制：04/05/10 | 明确独立横盘承接突破的覆盖目的、现有字段和无新增采集预算边界 | 写入被拦截，未实现、未上线，不凑策略数量，不换通道重写 |
| 发现采集及性能：02/07/08 | 实测并修复原有老币增量选择扫描；9测试、正式应用、正常重载、运行计时齐全 | 不是新增数据源/提高配额，不保证每币固定刷新，也未穷尽全链路所有瓶颈 |
| 样本及事实：01/03 | 原始文件和既有129研究键索引继续使用，异常项不猜补 | 本轮没有重做全部样本/普通失败对照；历史缺失继续标记 |
| 记忆、历史、收尾：11/12 | 独立memory/主报告/原台账追记与Web更新历史可见，局部备份可回退 | 根入口此前权限问题不改ACL，Git提交以单独成功回执为准 |

本轮没有启用Live、改成本模型、创建定时研究或声称长期收益。一个具体性能缺陷闭合，不等于原十二节全部完成；策略产出不足的余项明确保留，系统改进不能替代它。
