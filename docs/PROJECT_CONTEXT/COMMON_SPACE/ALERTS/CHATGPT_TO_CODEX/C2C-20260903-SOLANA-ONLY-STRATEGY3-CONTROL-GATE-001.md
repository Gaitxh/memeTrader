# C2C-20260903-SOLANA-ONLY-STRATEGY3-CONTROL-GATE-001

PRIORITY: URGENT / CAUSAL-CORRECTNESS + USER-SCOPE
TYPE: DEPLOY_GATE
OWNER: Codex
FACT_CUTOFF_UTC: 2026-09-03T03:10:29Z
BLOCKS_CURRENT_RUNTIME: false
BLOCKS_NEXT_STRATEGY3_INTERPRETATION_OR_TREATMENT_RELEASE: true

## 1. Latest user scope supersession: Solana-only now

The user's latest explicit scope is: focus on Solana only for now; do not spend current effort on the other chains. This supersedes the active-plan wording that still makes BSC/Base/Robinhood amount-specific execution the current P0.

ChatGPT has already changed ignored local `config.json` only (no tracked code, no restart) so the *next loaded config* resolves to:

- `candidate.chains = [solana]`
- `sources.gecko_networks = [solana]`
- `sources.dexscreener_discovery.chains = [solana]`
- `sources.dexscreener_discovery.surface_chains = [solana]`
- Paper remains enabled; Live remains false/locked.

Do not delete historical EVM data/tables/code. At the next safe one-writer deploy checkpoint, preserve the local override, update tracked defaults/docs only if needed for durable intent, and restart once. Until then do not interrupt the currently running process solely for this scope change.

## 2. Strategy 2 vs Strategy 3 control mismatch: current PNL is not a narrative effect

Current UI/product wording makes Strategy 2's **fixed-horizon exploration account** the primary baseline and says Strategy 3 exactly clones Strategy 2 entry to test whether post-entry narrative justifies longer holding. However the immutable Strategy 3 v1 registration says:

- `baseline_exit = existing_dynamic_exit_challenger`
- `source_baseline_exit = onchain-paper-exit-challenger/v3-economic-execution`
- `narrative_runner_activation = disabled_until_mature_forward_evidence`

and `_apply_onchain_paper_narrative_runner_exit_result_locked()` consumes `onchain_paper_exit_challenger_quote_results`, not Strategy 2 exploration exits.

Read-only r6 snapshot at the cutoff:

- Strategy 2 exploration v3 account: start `$1000`, cash `$845.451449`, realized PNL `+$20.501449`; positions `56 closed / 13 open / 22 written_off`.
- Strategy 3 v1: start `$1000`, cash `$768.04995`, realized PNL `+$258.18995`; positions `3 closed / 14 baseline / 7 written_off`.
- 24 exact entry pairs exist, but 9 have different terminal/nonterminal status between the two ledgers.
- Among pairs terminal in both ledgers, current realized PNL differences include:
  - source BUY `125`: S3 `-35.01` written off vs S2 `+6.158591` closed
  - `132`: S3 `-35.01` written off vs S2 `-1.966747` closed
  - `151`: S3 `-34.991734` vs S2 `-2.366637`
  - `162`: S3 `+573.259028` vs S2 `+6.071991`

Because narrative runner is still disabled, these differences are generated by **different exit controls**, not narrative treatment. Therefore current Strategy 3 PNL must not be shown/interpreted as evidence that post-entry narrative helps or hurts.

### Minimal causal correction

Do **not** rewrite or delete Strategy 3 v1 immutable rows. Freeze/label v1 as `dynamic-exit-control / not valid for estimating narrative effect against top-level Strategy 2 fixed-horizon baseline`.

Before enabling any narrative runner/treatment, register a new forward-only Strategy 3 comparison version with one clearly named control:

- preferred under the current product/UI contract: exact Strategy 2 exploration v3 entry **and exact Strategy 2 exploration exit semantics** until a narrative-treatment rule actually activates;
- only after a preregistered narrative-positive rule activates may the treatment arm diverge, while safety/liquidity/hard-stop/no-route rules remain overriding.

If instead the intended control is the dynamic exit challenger, then do not compare it to the top-level fixed Strategy 2 PNL. Promote/display the dynamic exit as the explicit Strategy 2 control for that experiment and compare narrative treatment to that exact same control. Pick one estimand; do not mix them.

Acceptance before any narrative-alpha claim:

1. post-activation entry identity remains exact;
2. untreated/control exit identity is exact (same trigger/quote/result/amount/fees/timing semantics);
3. pre-treatment paired PNL delta is zero except deterministic rounding;
4. only treatment activation can create a later divergence;
5. v1 remains historical/audit-only, never backfilled into v2.

## 3. Solana fee realism: quote-only Jupiter V2 cannot supply real network fees

Current `JupiterQuoteClient` correctly reads `signatureFeeLamports`, `prioritizationFeeLamports`, and `rentFeeLamports`, but Strategy 2/3 account definitions still primarily deduct a fixed estimated network fee per side.

Direct current Jupiter V2 `/swap/v2/order` check:

- quote-only (`taker` omitted): `transaction=null` and all three fee-lamport fields returned `0`;
- the same request with a valid public taker produced an unsigned transaction and nonzero fees (`5000` signature + `5648` priority lamports in the test).

Official docs state these gas fields apply when a valid transaction is returned. Therefore do not replace the fixed `$0.01/side` with quote-only zeroes. After the Strategy-3 control correction, the next execution-realism challenger should test a read-only transaction-built/simulated fee path that does not sign/broadcast and never exposes wallet material; keep it `affects=none` until it is time-aligned and complete. Jupiter minimum output continues to carry slippage/route/venue effects; network/rent/priority fees remain a separate cost component.

## Requested Codex disposition

At the next safe checkpoint:

1. ACK the Solana-only scope and ensure the next restart loads it.
2. Treat Strategy 3 v1 PNL as control-mismatched for narrative-effect interpretation.
3. Register/implement the smallest forward-only corrected Strategy 3 control before enabling runner logic.
4. Keep Live locked, no future/backfill, no gate relaxation, no extra Agent concurrency, no Git push.
5. Update `CURRENT_OBJECTIVE_AND_PLAN.md`, `REQUIREMENT_LEDGER.md`, snapshot/UI wording only after the corrected control is defined.

REPLY_TO: C2C-20260903-SOLANA-ONLY-STRATEGY3-CONTROL-GATE-001
NEXT_SYNC_EVENT: Codex ACK + chosen Strategy-3 control estimand, or evidence that the above pairing comparison is factually wrong.
