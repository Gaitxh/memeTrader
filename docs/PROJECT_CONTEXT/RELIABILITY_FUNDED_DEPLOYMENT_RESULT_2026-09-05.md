# 2026-09-05 持仓响应、资金账期与界面实施结果

事实截止：2026-09-05 15:40:28（北京时间）。执行依据：EXECUTION_PLAN_RELIABILITY_AND_UI_2026-09-05.md，以及用户本次实施和一次统一初始化授权。

## 已实施的实际阶段

- `f23611e`：持仓请求并发槽不再被退避等待占用；成功批次立即写入行情并进行相关退出判断，包含旧账期持仓；显式有效零流动性进入既有小于1美元核销规则。
- 同阶段接入独立资金账期、有效资金收支及污染修正边界，避免同批现金重复使用。Paper仍按20U整笔，余额不足拒绝新入场，不缩量、不自动充值；已有仓继续退出。
- 增加有界运行计时：每组件最多120次、最多32组件，每10秒至多写一个诊断快照；无额外行情请求或后台线程。
- Web只重绘当前可见模块；可见5秒、隐藏30秒、Token详情10秒。新增05运行速度对照、08更新历史；指标包含真实周期、批次耗时、按链数据年龄/缺失/失败、存储余量。诊断只取当前相关数据源，不返回成千上万条历史逐币源记录。
- `8b3ed82`：启用一次独立1000U账期，并使账户/钱包/观察器跟随正确账期；旧钱包不展示新账期Paper PnL和仓位。两阶段都已推送当前Git分支并部署。

## 一次初始化及历史保护

- 一致性备份：`E:\memeTrader\data\backups\before-funded-period-20260905-0737.sqlite3`；8,347,525,120字节，完成于15:35:45。备份为本机数据库，不上传Git。
- 新账期：`chain-meme-trader/funding-20260905-fixed-1000`。
- 实际起点：2026-09-05T07:37:08.842373Z；快照前沿829428。
- 初始化即时核验：127账户、127唯一ID，每账户现金1000；新仓位0、新交易0；策略ID及行为合同哈希与原127个一致。
- 旧V22的153,643条仓位记录、298,340条交易记录在切换前后保留。该数字是原始记录数，不是独立经济样本数，更不等于无污染收益证据。
- 旧账期停止新买入，仍开放仓继续按原规则退出；旧卖出收入留在旧资金账期，不充值新账户。旧V21/V20持仓继续由carry通道服务。
- 本次许可已经使用；以后新增策略不得再统一重置。

## 自然运行核验

截至15:39左右，新账期产生2512笔BUY、201笔SELL和189笔剩余仓位核销；这是策略投影数，不能算成2512个不同币/独立样本。该批最早入场快照829429、最早观察时间07:37:42.473608Z；早于部署前沿或起点的记录均为0。

同窗口旧V22产生3071笔SELL/核销退出记录。最新新账期现金范围0至1000U，没有负现金；部分策略已用满当时可用现金，并非系统不能交易。已实现/未实现PnL及初始账户金额分开显示。

8790健康接口与Web均读取新账期，127个策略可见。新币事件继续进入，GeckoTerminal的Solana/BSC/Robinhood在07:39均有成功记录。Live总开关关闭，没有本轮真实签名或广播。实际打开了运行诊断与更新历史页面；旧浏览器标签需要刷新HTML才能显示新增导航。

## 性能：两组口径必须分开

### 初始化前，修复版在旧负载运行约14分钟

07:30:48Z观测：

| 项目 | P50 | P95 | 说明 |
| --- | ---: | ---: | --- |
| 当前持仓整轮实际周期 | 40.92秒 | 65.46秒 | 21轮，配置1秒并非覆盖1秒 |
| 批次写入和退出判断 | 0.034秒 | 0.103秒 | 最近120批，不含排队/HTTP |
| 策略与账户实际周期 | 1.01秒 | 25.85秒 | 仍有账户统计长尾 |
| Solana持仓数据年龄 | 44.46秒 | 98.26秒 | 1333个币，年龄不是成功间隔 |

修复刚启动时同一监控范围Solana数据年龄P50/P95约231/533秒；随后下降，但期间自然退出、请求失败与负载也发生变化。不能据此宣称严格受控基准或全量秒级。

### 初始化后短窗口，旧仓仍服务但当前账户历史较小

07:40:28Z观测：

| 项目 | P50 | P95 |
| --- | ---: | ---: |
| 策略与账户实际周期 | 1.011秒 | 1.045秒 |
| 当前账期持仓采集实际周期 | 1.271秒 | 6.910秒 |
| 旧账期采集实际周期 | 15.014秒 | 15.095秒 |
| 批次写入与退出判断 | 0.031秒 | 0.096秒 |
| Solana全部服务中持仓数据年龄 | 20.68秒 | 36.77秒 |

此时总监控去重币：Solana1335、BSC145、Robinhood69；该样本没有无成功价格或最近请求失败。Runtime RSS约82MB、Web约62MB；DB约8.37GB、WAL约6.98MB，E盘剩余约60.62GB（含本轮备份占用）。这些是短窗口测量，不是长期SLA；没有用新账期低负载测量替代旧负载结果。

## 测试与研究结论

运行/资金/行情定向集成21项；Live模拟6项；有界计时3项；Web2项；新账期与原规则相关6项；最终钱包跨账期与有界诊断各1项通过。各批有交叉，不能简单相加为独立覆盖数。JS语法与git diff检查通过。未跑无关全套，也未发送真实交易。

静默突破、强势回撤再启动、早期爆发和成熟延续的证据复盘见 `RESEARCH_REVIEW_2026-09-05_RELIABILITY.md`。本轮0个候选满足正式新增条件；缺少完整确认序列或排除延迟污染后的优势，故保留研究假设，不为数量发布策略。

## 真实未完成项和边界

1. 原负载存在数十秒采集长尾；不能承诺数千币每秒更新。账户统计随历史增长的成本仍需在后续真实负载下观察，不能因新账期短测良好就关闭问题。
2. Live仍依赖Paper成交镜像，独立资金下的信号与退出解耦未完成；实际成交、幂等恢复与逐链gas充分性未经主网小额验收。接口和模拟测试不等于可投产Live。
3. 免费注册RPC可作为后续补充，不是本轮Paper的阻塞；不要求用户现在发送任何密钥。若要正式Live，需更换聊天中已曝光的钱包密钥，并另行确认资金与真实交易。
4. 研究与两小时复盘是持续任务，不是本轮一次性交付的已证实盈利。MAE/MFE、手动策略选择、三库大重构均未新增。

## 回溯原则

代码可按上述提交追溯。不得为回退代码覆盖当前生产数据库、删除新自然样本或合并资金账期；旧代码若不认识新账期，应先修复兼容/调度而不是重新初始化。原始历史及污染证据保留，正式统计排除污染，不以异常原始利润评价策略。
