# Pregrad velocity60 frozen research specification

Before computing outcome associations: first causal two-frame PREGRAD state per token; increasing slot and second observed strictly after first recorded; neither curve_complete; same quote mint SOL/system or WSOL; recompute reserve delta /1e9 / elapsed seconds. Validate derived Pump curve PDA and source timestamps. Preserve missing seed/progress; do not replace missing by zero. Training dates Sep5–7 UTC by state availability; Sep8 holdout, Sep9 partial prospective descriptive check. Across versions deduplicate token, never reuse later state.

Velocity bins: nonpositive, then three positive quantile bands fitted ONLY on training positive values (1/3,2/3 cutpoints). Fixed reserve bands <10/10–30/30–60/>=60 SOL and frame-spacing <30/30–90/>=90s. No other threshold search. Migration is a separate outcome from token_launch_facts, after frozen state; report observed incidence at1h/6h with mature denominators and UNKNOWN absent coverage, not biological non-graduation certainty. Current watch is selected3-token/5min sampling, not all launches.

For observed migration: first later causal positive-price/liquidity>=1000 exact-token/pool market anchor after migration receipt, then strictly later observation after anchor recorded within120s for simulated entry. Fix this pool; no successor substitution. Endpoint first valid observation at/after15/60min within120s margin; missing or below-floor endpoints remain unavailable. Costed return=exit_price*.96/(entry_price*1.04)-1. MFE uses only subsequent valid exact-pool observations within horizon; sparse path is a lower-bound observed maximum, not executable high. No writeoff from absence. Explicit below-floor path evidence separate from simulated endpoint PnL.

Matched low-velocity controls: high positive tercile versus nonpositive/lowest positive band, same UTC state date, reserve band and age band(<60/60–180/>=180s); rank by as-of reserve/age/spacing distance, no replacement/outcome filtering. Select before examining postgrad returns; unmigrated/no-entry controls remain unavailable, never replaced with outcome survivors. Top1/top3 removal on available endpoint returns only, with survivor bias caveat. No strategy unless holdout economics and coverage satisfy requested robustness; otherwise reject without nearby-bin search.
