# RESULT136 — supported Paper strategy delivery

REPLY_TO: C2C-20260910-STRATEGY-DELIVERY-136. Sole existing Codex writer. This is delivery of four new funded hypotheses, not restored226/254 counted again, and not an Alpha claim.

## Implemented and registered

All four activated in `chain-meme-trader/funding-20260906-v002-final-1000` at **2026-09-10T00:26:54.368584Z**, snapshot frontier **2565133**. Existing original-pool ordinary Paper profile: configured4% buy/4% sell adverse slippage, zero extra fee,1000USD floor. All use persistent common cohort ownership, asynchronous common scope/scam safety and strictly later settlement. Old events predating each policy activation cannot enter the new arm. No fabricated chain authentication flags.

|Arm|Addition ID|Notional/max open|Signal producer and mechanism|Mechanical exit|
|---|---|---|---|---|
|organic_early_flow_v1|294|2U/2|Existing early watch -> rare shared low-priority signed-flow classifier; >=4 effective wallets and positive effective net/liquidity, after balanced both-side exclusion -> valid later original-pool frame -> cohort signal|15m max; -20% hard stop; +30% trailing activation/15% drawdown|
|organic_reawakening_flow_v1|295|5U/2|Existing actual reawakening cohort/event -> complete exact-pool organic breadth/net-buy evidence -> later market confirmation -> new independent flow-filtered cohort|30m max; same hard/trailing rules|
|clone_consensus_leader_v2|293|5U/4|Frozen bounded complete clone set -> unique same token/pool liquidity and m5-volume leader -> later original-pool confirmation. Signal no longer popped into Shadow|15m max; same hard/trailing rules|
|event_clone_narrative_reawakening_v1|296|5U/4|Persisted verified independent event/exact binding or already verified frozen clone set -> current original-pool confirmation; mature cases additionally require recorded rediscovery and existing reawakening confirmation -> common cohort|30m max; same hard/trailing rules; no narrative extension or partial claimed for this version|

The narrative-only verifier checkpoint now accepts the original persisted Scout source/model/evidence ID and strictly prior availability, rather than requiring a new Scout call. Root common entry owns safety and fills; Agents never do. MODE B's existing no-CA collector still lacks independent novelty producer: it remains WAIT, not assumed delivered merely because the conditional consumer exists. MODE A/C can consume future verified narrative results. No natural verified event is claimed yet.

## Explicit proof blockers

- Synthetic real BUILDING output (`state=UNKNOWN`, `phase=SYNTHETIC_LPI_BUILDING`) now reaches the selector when a genuine exact-pool fresh sell simulation is supplied. DISTRIBUTING remains hazard; old cycle state cannot authorize BUILDING. **Production has no authenticated same-pool sell-simulation producer; synthetic_fast_harvest_v1 is NOT_REGISTERED.** No false success/report-presence receipt. Its required1U/max1/absolute300s/no Agent/reentry/averaging contract is unchanged; full funded entry/distribution-exit lifecycle is not claimed deployed.
- Pump SOL native buy exact-input/reverse fixed-state proof and canonical handoff remain blocked127. Pons template/deployer provenance remains DATA_BLOCKED121. Four/LaunchLab native economics and regime/successor proposals are not delivered funded arms by this change. Ordinary DEX flow arms are not native-curve buys.
- Withdrawn114 unsigned Capital Pulse and90A15m not-covered exit remain withdrawn.

## Validation and load

Source commits527a302 and025ba90. Existing Paper supervisor loaded final source at **2026-09-10 08:28:11 +08:00** (Python24896, supervisor11684 at readback). Web remained running. APIs show all four additions forward-enabled. Seven existing registration/activation/policy/funding table prefixes match their pre-load SHA256 exactly. No reset or old-row rewrite; new policy/account additions are append-only.

Targeted checks:36 initial classifier/cohort/ownership tests passed;18 classifier tests after real classifier->worker->signal fixture passed;11 funded-entry/exit and event-source tests passed after final change. These overlap and are not represented as65 independent tests. All six changed modules compiled; scoped diff check passed. Regressions cover real BUILDING, truncated coverage, shared priority/cache, exact pool, post-frontier origin, safety WAIT, later authorization/fill, one cohort per key, four arms' actual mechanical close, and persisted Scout->frame->signal. Synthetic has selector tests, not a funded lifecycle proof.

Detailed registration, immutable hashes, API and prospective telemetry: `data/research/delivery136/`. Natural zero counts are not forced trades or profitability evidence. Final resource/natural readback is recorded below after the bounded observation window.

### Natural/resource readback at2026-09-10T00:30:19Z

All four new arms: **0 BUY,0 SELL,0 WRITEOFF**,0 admitted opportunities; accounts exist with1000U each under the unchanged existing account model, entry_paused absent and forward_enabled=true. No source result or forced fixture was inserted into production to manufacture a BUY. Existing226/254 and133 restorations remain untouched.

Final process had about128s of natural observation, not a long-run benchmark. Held-fetch p95 **1.904s** vs pre-load **1.668s** (+236.6ms,+14.2%,within both250ms/25% guard limits); failures0 in new process. Held apply p95 **43.6ms** vs62.9ms. Passive wait p95 **2.276s** vs2.286s, drops0, depth3; Dex PoolTimeout/connect errors0, generation1/retirements0. Cohort p95.687s vs.702s; pattern p955.355s vs6.174s (only8 new samples, not a speedup claim). Existing held retrieval also services carried/history surfaces; current open/held counts were0, so this is limited smoke acceptance, not stressed new-strategy exit acceptance. No claim all provider coverage is healthy.

Final user-visible system: running, Paper=true, Live locked by config; same funding period. Synthetic/native blockers above remain real, and no whole119/native completion is claimed. Current natural event/flow supply is sparse; common registered paths are now executable when their causal producers qualify.
