# Authoritative current checkpoint

```json
{
  "cycle_id": "round-120-59-60-restart-verified-and-gate_readout-shipped",
  "ROUND_85_THE_ENTRY_SIDE_IS_UNDECIDABLE_AT_N94_and_the_deficit_is_POSITIVE_EVENTS": {
    "the_question": "Round 84 established the book is 94 tokens with only 6 profitable, and the exit side is measured to exhaustion (rounds 82-84). So the question that decides profitability is whether the ENTRY side can separate those 6 from the other 88. Round 43 had already shown the +17,620U belongs to the entry filters.",
    "RESULT_NO_FEATURE_SEPARATES": "Seven entry-time features tested against the win/lose token labels with a 2,000-draw permutation test. Best is turnover_at_entry at p = 0.141; then liq_at_entry 0.253, mcap_at_entry 0.476, buy_ratio_at_entry 0.787, vol5m_at_entry 0.814, trades_at_entry 0.942, entry_trades_per_min 0.959. holders_at_entry had too few usable values. **ZERO features below 0.05 and ZERO below 0.10** - and with 7 features at 0.05 the expected count by chance is 0.35, so the result is not merely null but on the uninformative side. NOTE the direction of the two largest differences, recorded as observation and NOT as a finding: winning tokens had LOWER liquidity at entry (2.2e4 vs 8.39e4) and LOWER market cap (6.18e4 vs 3.84e6). Those are the biggest |diff| of seven and were selected post hoc, so they are not hypotheses yet.",
    "THE_REAL_CONSTRAINT_IS_POSITIVE_EVENTS_NOT_SAMPLE_SIZE": "Profitable tokens: 6 of 94 = 6.4%, 90% CI [3.3%, 11.9%], interval width 8.5 percentage points. A proportion needs roughly ten positive events before its interval behaves, and this has SIX. At the measured 6.4% share, reaching ten positives needs about **63 ADDITIONAL tokens**, taking the total from 94 to roughly 157. At the observed accumulation rate of **13.19 tokens/hour** (94 tokens over 7.13 h) that is about **4.8 hours**.",
    "POSITIONS_CANNOT_SUBSTITUTE": "4,660 positions over 94 tokens = 49.6 positions per token. Treating them as independent inflates the apparent sample 50x, and round 84 showed the positives are concentrated in the few winning tokens - so the extra positions are more copies of the same outcomes, not more evidence.",
    "I_MADE_AN_OBSERVED_POWER_ERROR_AND_KEPT_IT_VISIBLE": "My first version used the two-proportion sample-size formula with p0 taken from the observed 6/94. It returned 'tokens needed' BELOW the current sample (25 for +15pp, 7 for +30pp) and therefore NEGATIVE waiting times. The cause is the observed-power fallacy: p0 is estimated from six events, so p0(1-p0) and the effect size are both noise and the formula stops behaving. I recorded the error and its replacement in the script output rather than deleting it: THE BINDING QUANTITY IS THE NUMBER OF POSITIVE EVENTS, not any n derived from p0. A proportion whose numerator is 6 is unstable whatever the denominator.",
    "WHAT_THIS_DOES_NOT_ESTABLISH": "Low power can only refute, not confirm, so 'no significant feature' does NOT mean the features are useless - it means they cannot be resolved at this N. And any feature that HAD come out significant would still be a post-hoc pick among seven tried on the same 94 tokens, i.e. a hypothesis requiring forward evidence in the form of an EXTRA strategy module. Nothing here is a change.",
    "THE_PRIORITY_THIS_PRODUCES": "P0 - increase the number of independent tokens. It is the only route by which the entry side becomes decidable, and it is exactly what round 73 closed as the sole amplifier (about 4.7 dense windows per slot per hour), so it is a USER DECISION this session does not touch. P1 - keep round 82's entry-armed trailing-exit hypothesis in the forward queue (per-token +17.3U, median +2.1U, CI excludes zero) but note it cannot be decided until N grows. DO NOT - keep searching the same 94 tokens; this round's 0-of-7 significance is the evidence for that.",
    "STANDING_RULES_new": "68 - A proportion's stability is set by its POSITIVE EVENT count, not by its total. This book has 94 tokens with 6 profitable (6.4%, CI [3.3%, 11.9%]); the numerator is 6, so no denominator makes it stable, while the 4,660 positions are the same outcomes copied 49.6 times. Report the numerator before any proportion. 69 - Do not estimate a test's power from the same estimate the test is evaluating. I plugged the observed p0 = 6/94 into the sample-size formula and got a required n SMALLER than the sample and a negative waiting time - the observed-power fallacy. When p0(1-p0) and the effect size are both estimated from a handful of events, the formula's output is uninterpretable; use a quantity that needs no p0, here the positive-event count itself."
  },
  "ROUND_86_THE_CANDIDATE_CAP_IS_SELF_IMPOSED_but_a_QUOTE_IS_NOT_A_FRAME": {
    "the_question": "Round 85 collapsed the P0 to 'raise the number of independent tokens N', whose only amplifier is the observation slot budget (rounds 65/73) - a USER DECISION. Round 86 answers only its precondition: is the 30-slot candidate cap SELF-IMPOSED or PROVIDER-IMPOSED? Until that is known, the user's decision is not a real one.",
    "RESULT_THE_CAP_IS_OURS": "SELF-IMPOSED, on three independent readings. (1) CANDIDATE BUCKET OCCUPANCY is 8 of 9 buckets at cap: bsc {early 3/3, growth 4/4, mature 3/3}, robinhood {5/3, 2/4, 3/3}, solana {3/3, 4/4, 3/3} against base_caps {early 3, growth 4, mature 3} and CHAIN_CAP 10 => 3 chains x 10 = 30 candidate slots. Held positions do NOT consume these slots (runtime.py:7894 gates only `if key not in held`), so FULL means candidates are queueing behind the cap. (2) ZERO recorded rate-limit/429 errors across 48 tracked sources. (3) The freshest successful source contact is about 0 s old at the reference instant. `non_held_by_chain_bucket` is a direct in-memory read of the same counter the admission gate uses (runtime.py:8148-8150 against runtime.py:7894), so unlike the truncated lease panel it IS runtime state.",
    "BUT_THE_HEADLINE_I_ALMOST_WROTE_IS_REFUTED": "The quote path already reaches far more than 30 tokens: 280 tokens with NO position row received fresh marks in 15 minutes, median 14 marks each (about one per 64 s), i.e. 9.3x the 30 candidate slots. My first draft made that the headline ('the cap is the only thing discarding work already being done'). The same 280 tokens refute it: 81.3% have ZERO token_snapshots rows in the window, only 2.8% reach the >=3 frames the gate needs, and their cumulative snapshot count has median 1. **A QUOTE (mark) IS NOT A FRAME.** Marks are written by a different path (store.py:32569 writes only the marks table), so quote reach does not imply observation reach. The scarce resource remains lease residency, and the value of raising the cap is priced at round 65's yield (about 4.7 dense windows per slot per hour), NOT at 280 tokens.",
    "ATTRIBUTION_OF_THE_MARKS_no_guessing": "Three lanes write them, resolved by reading the call graph and then the runtime's own self-report: (1) the primary lane runtime.py:7697, targets from chain_meme_trader_market_mark_targets() = open positions + still-executable pending, measured at 12 targets (held_retrieval.target_supply.actual_open 12); (2) the flat-compression-breakout shadow lane runtime.py:7724, targets from FlatSelector.due_targets(limit=30), 34 distinct non-held tokens in 15 min and 1,960 rows in its own table since 2026-09-12T18:27:12Z (about 267/h); (3) complementary_market_data_once / _poll_gecko_network (runtime.py:2576 / 2794), component cadence 10.0 s (p50 10.03). The flat lane covers only 34 of the 280, so the remainder comes from the complementary lane's rotating universe.",
    "I_WAS_WRONG_FOUR_TIMES_AND_ALL_FOUR_STAY_VISIBLE": "(1) I printed 'live leases: 9' as a population; observation_leases145.dump_state(watch, now, *, limit: int = 30) and bounded_summary(..., limit: int = 30) cap the checkpoint at 30 rows, so the persisted row count (16 at the time) is a LOWER BOUND and even its tightness is unknowable from the panel. Fixed in scripts/lease_panel_readout.py: the warning is in the output and the module docstring, and the JSON field was renamed from `live_leases` to `lease_rows_persisted` + `count_is_lower_bound: true` + `panel_truncation_limit: 30`. (2) I estimated request headroom by dividing total chain_meme_trader_market_mark_history rows by the epoch span - a MIXED stream (held, candidate and just-closed marks all write there), so it is no stream's request rate. WITHDRAWN. (3) `never > held_in_window` compares SETS, so it tests superset-hood, not size; with two disjoint sets it is always False and sent 280-vs-17 down the 'MIXED' branch, contradicting the counts printed one line above. Fixed with len() on both sides AND by printing the branch driver. (4) I divided the cumulative counter components.flat_target_selection.items (13,225) by the BOUNDED deque length sample_count (120) to get 'about 110 targets per cycle'; runtime_timing.py:120 is `timing['items'] += int(items)` and sample_count is deque(maxlen), so the two are not divisible. Claim removed and replaced with the lane's own table row counts.",
    "WHAT_THIS_CHANGES_FOR_THE_USER": "The cap is raisable in principle, so 'raise N' is a REAL decision rather than one blocked by the data sources. It is still a USER DECISION and this session does not touch it. Two limits must travel with it: the gain is denominated in FRAMES per hour (about 4.7 dense windows per slot per hour, round 65), not in the 280 tokens the quote path already touches; and round 85 stands - only 6 of 94 tokens were profitable, so more tokens is more EVIDENCE, not more profit.",
    "TESTS": "scripts/lease_panel_readout.py updated and tests/test_lease_panel_readout.py taken from 6 to 9 tests, all passing: the truncation caveat is asserted to stay in the docstring, BASE_CAPS == {early 3, growth 4, mature 3} and CHAIN_CAP == 10 are pinned against the runtime constants, and a LIVE test asserts at least half the candidate buckets are at cap."
  },
  "ROUND_84_THE_BOOK_IS_94_TOKENS_NOT_4643_BETS_and_round_82_survives_clustering_with_a_tail_driven_gain": {
    "the_denominator_nobody_had_counted": "Rounds 82 and 83 both concluded the exit-side lever is coverage rather than sophistication. Before recommending anything on that basis, this round measured the structural property that sets the denominator: SETTLED POSITIONS 4,643; DISTINCT ARMS 188; **DISTINCT TOKENS 94**; positions per token 49.4; positions per cohort 5.6. So the book is not thousands of independent bets - it is 94 underlying outcomes recorded about 50 times each, because many arms open on the same token.",
    "THE_LOSS_IS_CONCENTRATED_AND_THE_HIT_RATE_IS_TINY": "Of 94 tokens, **88 (93.6%) are net negative and only 6 (6.4%) are net positive**. The worst 20 tokens carry 67.2% of the total loss. Tokens opened 8 or more times (67 of them) carry 4,611 of the 4,643 positions and **99.9% of the loss**, while the 25 tokens opened exactly once total -18.6U. Positions per token: p25 1, p50 59, p75 77, p90 94, max 126.",
    "ROUND_82_RE_MEASURED_WITH_TOKEN_CLUSTERING": "Round 82 reported +1,300U over 4,110 positions. The independent unit is 75 tokens. Per-token delta: p10 **-1.229**, p25 +0.011, **p50 +2.095**, p75 +22.352, p90 +64.370. Tokens IMPROVED 59 (78.7%), WORSENED 12 (16.0%), unchanged 4. **Mean per-token delta +17.333 U, token-clustered 90% CI [+12.226, +22.540], EXCLUDES ZERO.** So the DIRECTION SURVIVES clustering with statistical support, and this is now the interval that travels with the claim.",
    "BUT_THE_EFFECT_IS_TAIL_DRIVEN_and_that_must_be_quoted": "The mean is +17.3U while the MEDIAN is only +2.1U, i.e. more than 3x apart, so the gain is carried by a few tokens rather than being typical - round 31's concentration rule. The honest statement is: arming the trailing exit from entry improves about four fifths of tokens, by a median of +2.1U per token, with the average pulled up to +17.3U by a tail. The position-level figure of +0.316/position is this number multiplied by the duplication factor and is NOT additional evidence.",
    "THE_MORE_FUNDAMENTAL_OBSERVATION": "**Only 6 of 94 tokens are net profitable.** After ~4,700 opens, 188 arms and many research modules, the number of money-making underlying assets is in single digits. That is why every exit-side improvement can move the book from -1,338U toward -65U but CANNOT turn it positive: an exit rule changes how you LEAVE, not whether the assets you ENTERED make money. This is consistent with round 43 ('the +17,620U belongs to the entry filters') and supplies its sample basis. The size of the pool is set by the observation slot budget, which round 73 closed as the irreducible constraint and which is a USER DECISION.",
    "DELIVERED": "exit_rule_compare.py gains a cluster() helper (aggregate to token, then token-clustered bootstrap) and a new section 5 that prints the token count alongside the position count, the mean AND median per-token delta, the clustered interval, the improved/worsened split, and an explicit 'the effect IS tail-driven' note when the mean exceeds 3x the median. Section 1 now leads with the denominator. tests/test_exit_rule_compare.py grows to 13 with three new tests: cluster aggregates to tokens not positions (a fixture where the token-level and position-level means differ), the clustered interval must span zero when two tokens have opposite signs (so deduplication cannot manufacture precision), and empty input returns None. All 13 pass.",
    "STANDING_RULE_new": "67 - Count the INDEPENDENT UNITS before reporting any per-position effect size. 4,643 positions here cover 94 tokens (49.4 positions per token; p50 59, max 126) because 188 arms open on the same price series. 'Each position +0.316U' reads like 4,110 independent observations; it is 75, at +17.3U each with a median of only +2.1U. Same family as round 26 and rule 26, but it must be done BEFORE running the comparison rather than added as a caveat afterwards - I only thought to count this denominator after rounds 82 and 83."
  },
  "ROUND_83_ORDER_FLOW_EXITS_REJECTED_parameter_sign_flips_and_winners_gain_less": {
    "what_was_tested_and_why": "The user's mandate names 'signal-decay exit' and asks for derived features built from data already on hand. The mark history carries order-flow fields that NO exit rule reads: volume_5m_usd, buys_5m, sells_5m, populated on 33,995 of 33,995 marks (100%). Every deployed exit keys on PRICE (hard stop, trailing drawdown, take-profit tiers) or liquidity (the dust write-off). So three causal flow rules were tested against the round-82 price baseline: flow collapse (volume falls below f of its own running max), sell pressure (sells >= r x buys), and the combination.",
    "THE_HEADLINE_LOOKED_GOOD": "Flow collapse at vol <= 25% of running max scores +40.5 U-equiv against the price-drawdown baseline -57.3 and ACTUAL -1,338.0 on 4,025 positions. The flow rules are genuinely DIFFERENT signals, not a restatement of price: both fire on only 6.7% of positions, and where both fire the flow trigger precedes the price trigger by a median of 11 marks.",
    "BUT_IT_FAILS_ROBUSTNESS_so_IT_IS_REJECTED": "(1) MONOTONICITY FAILS: across volfrac 0.10/0.15/0.20/0.25/0.30/0.40/0.50/0.60/0.75 the sums are +11.3 / -9.2 / +66.2 / +40.5 / -3.0 / +62.8 / +23.8 / -8.7 / -87.3 - THE SIGN FLIPS REPEATEDLY ON A 0.05 STEP, and the swing across its own parameter (128U) EXCEEDS its advantage over the price rule (98U). That is the signature of noise, not a mechanism. (2) IT HELPS WINNERS LESS: on actual winners (n=1,144, actual +252.1) flow at 25% gives +44.9 while the simpler price rule gives +97.1 - and winners are exactly what the 'golden dog' problem is about. (3) ON THE RUNNERS (n=629 whose in-life max reached +50%) flow scores +426.9 against the price rule's +392.9, a 34U difference that does not justify a whole extra parameter family.",
    "AND_I_DID_NOT_SCAN_IT_FURTHER_ON_PURPOSE": "The price rule was tested at a single drawdown while the flow family was swept across three thresholds, so the flow family had a selection advantage; I deliberately did NOT run a fuller sweep to 'give it a fair chance', because the monotonicity check already shows further scanning only reaches more extreme cells of the same unstable family (0.75 gives -87.3, 0.20 gives +66.2). Scanning further inside a sign-flipping family increases selection risk rather than testing anything.",
    "THE_CONSTRUCTIVE_CONCLUSION_which_strengthens_round_82": "Both the price trailing drawdown (-57.3) and the flow collapse (+40.5) beat ACTUAL (-1,338.0) by an order of magnitude, while the difference BETWEEN them is a few tens of units and unstable for the flow side. So what deserves forward testing is WHETHER AN EFFECTIVE TRAILING EXIT APPLIES AT ALL, not WHICH SIGNAL DRIVES IT. This is a second independent axis pointing the same way as round 82: there, the activation threshold was worth ~7U while whether a trailing exit applies was worth ~1,200U; here, swapping to a more elaborate signal family is worth a few tens and does not hold up. THE PROBLEM IS COVERAGE, NOT SOPHISTICATION.",
    "A_CAVEAT_THAT_ALSO_MOTIVATED_THE_REJECTION": "volume_5m_usd / buys_5m / sells_5m are the PROVIDER's rolling 5-minute aggregates, not raw trade prints, so a 'collapse' may reflect the provider's own window mechanics rather than a change in the pool. Combined with the parameter instability, that is why this was not promoted to a forward test. Also: in-sample, single epoch, next-mark execution with the engine's 0.96 haircut and no extra slippage, on a repeat-heavy mark stream (round 79, p50 0.24 distinct prices per mark). NOTHING IS CHANGED and no module is created.",
    "STANDING_RULE_new": "66 - Before widening a parameter family's search, check that its SIGN is stable. Flow collapse gave +11 / -9 / +66 / +41 / -3 / +63 / +24 / -9 / -87 across its threshold, flipping sign repeatedly with a swing larger than its edge over the baseline rule. Continuing to scan there is not 'giving the idea a fair chance', it is adding selection risk. A real mechanism varies smoothly in neighbouring parameters; a sign flip is the signature of noise."
  },
  "ROUND_82_THE_UNMEASURED_COST_OF_ROUND_81_IS_MEASURED_and_it_is_FAVOURABLE_winners_improve_too": {
    "the_question_round_81_left": "Round 81 wrote, in its own words, that its test 'looked ONLY at positions that ultimately died' and that a more aggressive exit 'also acts on positions that would have recovered or run on to large gains, and that cost is completely unmeasured here' - which decides the sign of its +115U. This round measures it on the WHOLE settled book.",
    "THE_WHOLE_BOOK_RESULT": "3,878 settled positions with >=3 in-life marks (525 excluded, count reported per rule 64). ACTUAL sums to -1,255.7 U-equiv (mean -0.324/pos). Causal trailing drawdown ARMED FROM ENTRY: dd 10% -115.8 (+1,139.9 vs actual); dd 15% -112.5 (+1,143.2); **dd 25% -50.3 (+1,205.4)**; dd 30% -48.6 (+1,207.1); dd 50% -70.7 (+1,185.0). All seven drawdowns from 10% to 50% land between -48.6 and -124.8, i.e. all far better than actual - so the finding is parameter-insensitive.",
    "WINNERS_IMPROVE_TOO_which_refutes_the_feared_tradeoff": "Split by what the position ACTUALLY returned: WINNERS (realised > 0) n=1,102, actual +248.1, arm-from-entry dd25% +340.0, **delta +91.9**. LOSERS (realised <= 0) n=2,776, actual -1,503.8, arm-from-entry dd25% -390.3, delta +1,113.5. SO THE GAIN IS NOT BOUGHT BY CUTTING WINNERS - the winning group itself improves. That was the single most important thing round 81 could not check.",
    "I_FIRST_TESTED_A_BIAS_IN_MY_OWN_METHOD_and_it_was_refuted": "ACTUAL is scored at each position's LAST mark while an early-exiting rule is scored at its own exit mark, so if marks stop early for some positions the comparison is systematically biased toward early exit and the whole +1,200U could be truncation. MEASURED over all 3,880 cases: the last in-life mark sits p50 4 s, p75 7 s, p90 9 s before the close, and **ZERO cases exceed 60 s (0.0%)**. Re-running restricted to 'last mark within 15 s of close' (n=3,870) leaves the result unchanged: dd 25% -50.6, +1,203.7 vs actual. So the gain is NOT a truncation artifact.",
    "WHAT_ACTUALLY_HAPPENED_on_the_same_book_by_exit_path": "dex_pool_liquidity_below_configured_floor n=976 sum -947.2 (mean -0.970); market_mark_hard_stop n=1,024 sum -458.4 (-0.448); market_mark_trailing_exit n=427 sum +113.4 (+0.266); market_mark_max_hold n=1,056 +46.6; market_mark_take_profit_1 n=56 +12.9. TOTAL -1,255.7. The two loss sources are the hard stop and pool death (-1,405.6 combined) while all three positive paths total only +172.9, and the trailing exit is the only profitable path at scale.",
    "A_PRECISION_I_MUST_KEEP_activation_vs_application": "Do NOT read this as 'a lower activation would help'. On the same data, arming at +45% scores -48.0 and arming from entry -50.3, and per-arm (round 81) the arm's OWN contract scored +122.8 versus +114 to +120 for arm-from-entry - i.e. the activation choice is worth only about 7U, while whether a trailing exit applies at all is worth about 1,200U. THE REAL GAP IS NOT THE ACTIVATION LINE BUT THAT THESE POSITIONS HAD NO EFFECTIVE TRAILING EXIT (round 77: 78% of the write-off cohort never reached its arm's activation; round 76: the trailing exit captures only 34.9% of its median peak).",
    "NOT_A_REASON_TO_CHANGE_ANYTHING": "Upper bound by construction; the mark stream is repeat-heavy (round 79: p50 0.24 distinct prices per mark) so between-mark movement is invisible and both trigger and fill can differ; 4% adverse slippage is not modelled beyond the engine's 0.96 haircut; single epoch in-sample (parameter insensitivity weakens but does not remove round 26's selection risk); and it does not answer live executability. Round 43's 'take-profit has ~zero in-sample marginal value on the kept book' remains about the TAKE-PROFIT side and cannot be cross-cited here. NOTHING IS CHANGED. Per the user's rule any exit change must be an EXTRA strategy module with its own forward evidence. The round's contribution is to state the hypothesis concretely enough to test forward: a trailing-drawdown exit armed from entry at 25-30% would move this book from -1,256U to about -50U WITHOUT sacrificing winners (+91.9U on the winning group), and what remains unverified is live executability, cross-epoch stability, and interaction with the other exit rules.",
    "STANDING_RULE_new": "65 - Naming a bias is not handling it; measure it. I recognised immediately that scoring ACTUAL at the position's last mark while scoring an early-exiting rule at its own exit mark favours early exit - but only measurement showed it does not bind here (last mark p50 4 s from the close, zero cases over 60 s), and re-running on a restricted dense subset confirmed it. Say the bias out loud AND quantify it, with a restricted-set control."
  },
  "ROUND_81_THE_WINDOW_IS_NOT_THE_PROBLEM_493_healthy_seconds_and_45_marks_and_every_gain_was_seen": {
    "the_question_round_80_left": "Round 80 closed the write-off path (the pool really becomes dust, mechanism correct, evidence intact). That left the user's exit mandate open: was there a moment with BOTH a live pool and a realised gain, at which a rule could have sold?",
    "THE_WINDOW_IS_LARGE_measured_on_976_writeoff_positions": "Span over which liquidity was at or above the 1000 floor: p25 235 s, **p50 493 s**, p75 696 s, p90 934 s, max 7,167 s. Healthy span >= 300 s for **72.2%**. Known-liquidity marks inside that span: p25 22, **p50 45**, p75 64; >= 5 healthy marks for **99.3%**. So these pools stay tradeable for a median eight minutes with a median 45 observations.",
    "EVERY_GAIN_WAS_OBSERVED_WHILE_THE_POOL_WAS_ALIVE": "The peak reached WHILE HEALTHY is IDENTICAL to the peak over the whole life: p25 +0.0439 both, p50 +0.1647 both, p75 +0.2888 both. The reach rates match item by item: +5% 71.6% healthy vs 71.8% life; +10% 57.0% vs 57.0%; +25% 26.1% vs 26.1%. So the gains were never hidden behind a dead pool - they were seen, with liquidity, and not acted on.",
    "THE_CAUSAL_TEST_over_the_healthy_span_only_where_the_marks_are_informative": "973 usable cases. ACTUAL (dust write-off) sum -944.2 U-equiv, mean -0.970/pos. Trailing drawdown armed from the first healthy mark: dd 10% -> +106.5 (mean +0.109); dd 25% -> +119.9 (+0.123); dd 50% -> +120.3 (+0.124). Versus actual: about +1,050 to +1,064 U-equiv. Causality rules observed: running high uses only marks at or before t, execution at the NEXT mark, engine kernel 0.96*R-1, and a position whose pool dies before the rule fires is scored at the LAST HEALTHY MARK (deliberately the most favourable assumption, so this is an UPPER bound).",
    "THE_KEY_FINDING_ACTIVATION_NOT_DRAWDOWN": "**The drawdown parameter barely matters** - 10% through 50% all land between +106 and +120, mean +0.109 to +0.124. And simulating each arm's OWN contract (which arms only after the price first reaches 0.20-0.60) gives +122.8, essentially the same as arming from the first healthy mark (+114 to +120). SO THE LEVER IS WHETHER THE RULE IS ARMED AT ALL, NOT HOW DEEP THE DRAWDOWN IS - and round 77 already measured that 78% of these positions never reach their arm's activation level. This is robust rather than fitted: the insensitivity to the drawdown parameter removes most of the round-26 selection risk that a parameter sweep normally carries.",
    "THE_MATERIAL_CAVEAT_that_could_flip_the_sign": "THIS TEST LOOKED ONLY AT THE POSITIONS THAT ULTIMATELY DIED. A more aggressive exit rule ALSO acts on positions that would have RECOVERED or run on to large gains, and that cost is COMPLETELY UNMEASURED here. So +115U is the effect ON THIS COHORT, NOT on the book; whether it helps overall depends on how many eventual winners it sacrifices, which is unknown and could easily decide the sign. Also: upper bound by construction, 4% adverse slippage not modelled, single-epoch in-sample.",
    "NO_CHANGE_PROPOSED_but_the_design_space_IS_narrowed": "Nothing is changed and no module is created. What this round contributes is narrowing the user's five named exit dimensions to ONE live lever. Activation level: THE lever (0.20-0.60 leaves 78% permanently unarmed). Drawdown depth: insensitive, so no need to tune it and no selection risk in not doing so. Time stop: the pool is healthy a median 493 s, so a time stop would never fire on this cohort. Tiered take-profit: tiers exist on only 90 of 1,150 arms. Structural confirmation: liquidity IS the structural signal and it stays GOOD throughout the healthy span, so waiting for structural confirmation arrives at the death. Signal-decay exit: NOT measured this round, though 45 marks per position provide the material. THE FORM FOR TESTING IT remains the user's rule: an EXTRA strategy module whose carrier arms arm the trailing exit from entry instead of requiring +0.20-0.60, with its own forward evidence, answering the unmeasured question above.",
    "A_WRONG_SENTENCE_I_WROTE_AND_CORRECTED_in_the_same_round": "My first draft of r81b stated that the median healthy-pool span was 'of the order of ten seconds'. WRONG: I had taken round 81's other measurement - the 7-SECOND gap from the LAST healthy mark to the write-off - and treated it as the window length. Seven seconds is the final sliver, not the window; the window is 493 s. That correction flips the reading from 'no time to react' to 'ample time, no action'. I left both the wrong sentence and the correction visible in the script output because it is this project's characteristic failure mode.",
    "STANDING_RULE_new": "64 - A measurement of 'the last X' is NOT the duration of X. I measured 'the last healthy mark is 7 s before the write-off' and wrote 'the median healthy span is of the order of ten seconds', when the span is 493 s. 'The final moment' and 'the sustained period' are different quantities: the first describes how fast the end came, the second describes the time available to react. Treating one as the other turns 'ample time with no action' into 'no time to act'."
  },
  "ROUND_80_ROUND_79_IS_WRONG_THE_WRITEOFF_MECHANISM_IS_CORRECT_and_that_PATH_IS_CLOSED": {
    "THE_CORRECTION": "Round 79 concluded 'the evidence the write-off relied on never entered the mark history' and therefore 'write-offs are unauditable'. TOO STRONG AND WRONG. Reading the write-off's OWN source (store.py:34676-34704) shows it does NOT act on chain_meme_trader_market_mark_history: a mark raises the possibility, the engine then performs a POST-CONFIRMATION RE-QUOTE of the pool, accepts it only if fresh (line 34680 rejects anything older than 15 s) AND still below the floor (line 34693), and stores it as trigger_evidence['terminal_dust_pool'] before turning the mark into RUG_EXIT. Two tables answer different questions - the mark history is the routine heartbeat, the marks trigger_evidence is the decisive sample - and round 79 looked for the second's evidence in the first.",
    "MEASURED_1150_writeoffs": "Carrying terminal_dust_pool evidence: **1,150/1,150 = 100.0%** (post_confirmation is 0% because the successful path copies the same confirmation data into terminal_dust_pool at line 34694). CONFIRMED liquidity at the write-off: p10 $0.00, p25 $0.00, **p50 $0.08**, p75 $0.15, p90 $0.33, max $328.29 - **100.0% below the $1,000 floor**, and the MEDIAN is 8 CENTS. Confirmation freshness observed_at -> recorded_at: p10 0.000 s, **p50 0.002 s**, p90 0.189 s, with **ZERO exceeding the 15 s window**. So the mechanism settles on genuine dust pools with millisecond-fresh evidence, not on a marginal breach or a stale reading.",
    "AND_H1_IS_REFUTED_TOO": "Round 79's other hypothesis - that observation had already stopped and the write-off fired blind - is refuted by comparing the last in-life mark to the close PER EXIT PATH: hard_stop 4 s, max_hold 4 s, **write-off 3 s**, trailing 7 s, take_profit 4 s. THE WRITE-OFF PATH HAS THE TIGHTEST GAP OF ALL, and 88-99% of tokens have marks after the close, so observation continues. It was never blind.",
    "THE_IMPORTANT_CONSEQUENCE_this_closes_a_direction": "Round 77 found 78% of write-off positions never reached their own arm's trailing activation and I described that as a coverage gap implying 'a lower activation threshold' as a candidate. ROUND 80 CLOSES THAT. These positions were written off because the pool genuinely became dust (median 8 cents), NOT because the exit rule was blunt - the token died. A lower trailing activation would have to sell at some earlier moment, and there is now no evidence that such a moment existed: the confirmation is millisecond-fresh and the pool was already dust when confirmed. So the 1,150 write-off losses (~-0.974 stake each) are the real cost of tokens dying, NOT a repairable mechanism defect.",
    "WHAT_ROUND_79_STILL_GOT_RIGHT": "The mark history IS 76% repeats (distinct-prices/marks p50 0.24; 60.1% have their last 5 marks identical), so THAT table cannot support frame-by-frame exit-rule backtests - any such backtest reports 'never fires' as a property of the data. And 75.4% of write-offs do show a healthy last mark THERE - now explained rather than anomalous. Round 79's limitation on round 77's 'capturable window existed' inference also stands unchanged, because that inference rested on the frozen table.",
    "MY_METHOD_ERROR_this_round": "I had TWO tables available and read only one, then wrote 'not in this table' as 'the evidence does not exist'. Every step of round 79's reasoning was valid - the mark stream is frozen, 75.4% last marks are above the floor, write-offs happened - and the missing step was asking 'which table does the write-off actually read?'. I read dex_trajectory and observation_leases145 source that round but never the write-off's own, which is about 30 lines.",
    "DELIVERED": "scripts/writeoff_readout.py gains a section 4 that reads trigger_evidence_json and reports the confirmed dust-pool liquidity and the confirmation freshness, with the round-79/80 correction in its output. tests/test_writeoff_readout.py grows to 9 tests: the new live test asserts the evidence is present for >90% of write-offs, that >95% of confirmed liquidity is below the floor, that the MEDIAN is far below it (a cluster just under the floor would mean premature exits), and that under 5% of confirmations exceed the 15 s window. All 9 pass. ROUND120_79_RECORD.md carries a correction banner. No behaviour change.",
    "STANDING_RULE_new": "63 - When a table appears to be MISSING the evidence an event should have produced, first confirm which table that event actually reads. This project keeps a routine heartbeat table and a decisive-sample table for the same phenomenon (chain_meme_trader_market_mark_history versus chain_meme_trader_marks.trigger_evidence_json). Round 79 found no decisive evidence in the heartbeat table and recorded it as 'the evidence does not exist', concluding write-offs were unauditable - while the evidence was 100% present with a 2 ms median confirmation lag. Between 'not in this table' and 'not there' sits one grep."
  },
  "ROUND_79_THE_MARK_STREAM_FREEZES_SO_WRITEOFFS_ARE_UNAUDITABLE_and_round_77s_window_claim_is_undecidable": {
    "why_this_round_started": "The goal was a CAUSAL exit counterfactual on round 77's write-off cohort - 'a window existed' is not 'a rule could act in it', and the gap between those is the look-ahead trap this project has hit before (rounds 36/39/43). The counterfactual turned out to be UNRUNNABLE, and the reason is a property of the DATA, which is the finding.",
    "THE_ORIGINATING_EXAMPLE": "One write-off position: opened 19:13:17, entry_execution_price 3.12312e-05, highest_signal_price 3.672e-05; 17 marks between 19:20:24 and 19:23:43 ALL at price 3.672e-05 and liquidity 49999.99; distinct price values in the whole series: 1; write-off at 19:23:46 - so NO mark in the stream shows the collapse the write-off asserts.",
    "MEASURED_ACROSS_THE_COHORT_1019_usable_cases": "Whole-life price constant: 82 (8.0%). Whole-life liquidity constant: 82 (8.0%). **Last 5 marks all the same price: 612 (60.1%)**, same liquidity: 612 (60.1%). Distinct-prices/marks per position: p10 0.09, p25 0.16, **p50 0.24**, p75 0.29, p90 0.39 - i.e. about 76% of marks are repeats. Any in-life mark below the 1000 floor: 251 (24.6%). **LAST in-life mark still ABOVE the floor: 768 (75.4%).** So for roughly three quarters of the write-offs, THE EVIDENCE THE WRITE-OFF RELIED ON NEVER ENTERED THE MARK HISTORY - the engine acted on live data that was not persisted.",
    "WHAT_THIS_LIMITS_IN_ROUND_77": "Round 77's measurement 'at the moment the running high was recorded, liquidity was >= the floor in 966 of 966 cases' is CORRECTLY COMPUTED but its inference is not: if that mark sits in a frozen series, the measurement describes the pool at the START of monitoring, not at the moment of peak profit. What the record supports is 'the pool was healthy when monitoring began and no observation of a decline exists'. What it CANNOT support is 'a live window with both profit and a live pool existed'. That inference is WITHDRAWN as undecidable from this data. A supporting fact that does NOT change: the running high is the first in-life mark in only 11.2% of cases, so the high is a genuine maximum - the problem is that the series' remainder is repeats, making the high's mark often also the last mark.",
    "WHAT_SURVIVES_FROM_ROUND_77_and_it_is_the_important_part": "The core finding is unaffected because it never used the mark series: comparing the engine's own highest_signal_price_usd against each arm's OWN trailing_activate_return gives 253 (22.0%) reaching it and **897 (78.0%) never reaching it** - a COVERAGE gap, not a missed trigger. Likewise round 76's capture statistics rest on the engine's per-frame aggregate, not on the mark stream, so they stand.",
    "WHY_THE_CAUSAL_COUNTERFACTUAL_IS_UNRUNNABLE": "A trailing-drawdown rule reading only these marks can NEVER see a drawdown, because the median series is 76% repeats and the last 5 marks are unchanged in 60.1% of cases. So '0 triggers' is a DATA conclusion, not a market one. THIS MUST BE STATED AS TWO SEPARATE CAUSES because conflating them would read a data gap as 'the rule is useless': (a) I did have a real bug first - the running max was updated BEFORE the comparison, making price/peak - 1 always non-positive so every drawdown from 10% to 50% returned the identical number; (b) after fixing it the result was STILL zero, because the data is flat.",
    "THE_ACTIONABLE_GAP_accuracy_and_observability": "Write-offs are UNAUDITABLE on this data: about 75% have no observation of the decline, so 'was this write-off early or late' cannot be answered from what is retained, and any exit rule backtested on the mark stream will report 'never fires' as a property of the data rather than of the rule. Making write-offs reviewable would require the frame the write-off relied on to be persisted - that is an EXTRA record/module, and it is a user decision. NOTHING IS CHANGED this round.",
    "TWO_ERRORS_OF_MINE_RECORDED": "(1) The counterfactual bug above, whose signature - every parameter value returning the same number - is the SAME signature as rounds 74/75's parameter-blindness. Identical results across a parameter sweep should be read as a harness bug until proven otherwise. (2) ORDER OF WORK: after the bug I re-ran the full 1,019-case sweep before looking at a single raw series; printing one series immediately explained everything. One sample costs a second, two full sweeps cost an hour.",
    "DELIVERED": "data/research/diag_round120/r79_frozen.py (the freeze and evidence-absence measurements above; the counterfactual script r78_trailing_cf.py is retained as a RECORD OF A TEST THAT CANNOT BE RUN, with the reason documented rather than deleted). ROUND120_77_RECORD.md now carries a limitation banner at the top pointing here.",
    "STANDING_RULES_new": "61 - Separate 'not in the data' from 'not in the market'. A mark stream showing no drawdown may mean the market did not fall OR that the observation was not recorded; here 60.1% of write-off positions have their last 5 marks identical and 75.4% keep their last mark above the floor while being written off for liquidity. BEFORE backtesting any rule on a mark series, MEASURE ITS REPEAT RATE (here distinct-prices/marks has p50 0.24). 62 - Before running a sweep over N cases, print ONE real case. I ran 1,019 cases twice before reading a single series; that one series - 17 identical prices - explained the whole result immediately."
  },
  "ROUND_77_THE_BIGGEST_LOSS_PATH_IS_A_COVERAGE_GAP_NOT_A_MISSED_TRIGGER": {
    "what_was_measured": "Round 76 found the largest loss path - 1,150 positions ending in dex_pool_liquidity_below_configured_floor - had a MEDIAN running high of +0.171, i.e. they rose before dying. Round 77 dissected that cohort. 85.7% had a positive peak; median +0.1707; 60.4% reached +10%; 27.0% reached +25%. Life to write-off: p50 8.8 min, p75 12.2. In-life marks per position: p50 45 (p75 61, p90 84) - the engine was watching.",
    "THE_POOL_WAS_HEALTHY_AT_THE_HIGH": "The engine stores the running high's VALUE but not its TIME, so the high's time was recovered from the token's own mark series as the first mark within 1% of the recorded high - a LOWER bound, so the measured gap is an under-estimate. Result: of 966 traceable highs, **966 (100.0%) had pool liquidity at or above the 1000 USD floor at the moment of the high**, median 40,367 USD, minimum 9,168, maximum 153,048. ZERO were already dying. So the 'high coincided with the death' explanation is refuted and a genuine window with both a gain and a live pool existed. High to write-off: p10 0.5, p25 1.0, p50 3.1, p75 5.1, p90 5.6 minutes; 28.6% exceeded 5 minutes.",
    "THE_DECISIVE_PER_ARM_COMPARISON": "Each arm carries its own `trailing_activate_return`. Comparing each write-off position's peak against ITS OWN arm's level: **253 (22.0%) reached it; 897 (78.0%) NEVER did.** The activation levels across those arms run 0.20-0.50 (e.g. merged_multi_setup 0.45, survive_noise_wide 0.45, dense_flow_hold 0.50, hold_flow 0.50, deadpool_flow 0.30, moonbag_merged 0.30, dense_watch_breakout 0.25) while the cohort's median peaks are only 0.15-0.23. **SO FOR 78% OF THESE POSITIONS THE TRAILING EXIT COULD NOT FIRE BY CONSTRUCTION - this is a COVERAGE GAP, not a missed trigger.** The 253 that DID reach activation had median peak +0.502 (p90 +1.198, max +1.489) and were still written off. Of the 90 write-off positions on arms carrying take_profit tiers, 26 (28.9%) had a peak crossing the first tier.",
    "THE_MECHANISM_STATED": "1,150 positions written off on the liquidity floor -> 85.7% were once profitable (median +17.1%) -> at the high the pool was healthy in 966 of 966 traceable cases -> the engine held a median 45 marks -> the high-to-writeoff gap was a median 3.1 minutes -> but 78% never touched their own arm's trailing activation (0.20-0.50 versus peaks of 0.15-0.23) -> and the hard stops sit at -0.20 to -0.90, deeper than where these pools die. SO THE WRITE-OFF PATH HAS NO EXIT MECHANISM THAT CAN REACH THE +17%: the trailing line is too high, the hard stop too deep, and take-profit tiers cover only 90 positions. That is the origin of round 76's -436.6% capture.",
    "WHAT_THIS_DOES_NOT_ESTABLISH_and_no_change_is_proposed": "It does NOT show that changing any level would have captured the gain. The running high is only knowable after it is exceeded, and a real-time rule acts on the frame it sees; the pool's median 3.1-minute collapse also means any 'wait for confirmation' rule may be too slow. Round 43 already showed the take-profit's in-sample marginal value on the KEPT book was about zero once entry filters were applied. NOTHING IS CHANGED and no module is created.",
    "THE_GAP_THIS_IDENTIFIES_FOR_THE_USER_MANDATE": "The user's mandate names volatility-adaptive stops, tiered take-profit, signal-decay exit, time stops and structural-confirmation exit. This cohort is covered by NONE of them at its actual level: the trailing activation (0.20-0.50) barely intersects the peaks these pools reach (0.15-0.23); take-profit tiers exist on only 90 of 1,150; the hard stops (-0.20 to -0.90) are deeper than where the pools die; and the median 8.8-minute life makes a time stop likely too slow (an INFERENCE, not measured). The most testable blank is a LOWER activation threshold, and the mandated form for testing it is an ADDITIONAL strategy module with its own forward evidence - the same shape as exit_ladder150 (round 58). This round identifies the blank and the form only; it designs and deploys nothing.",
    "DELIVERED": "scripts/writeoff_readout.py - a read-only readout of the liquidity-floor cohort: the peak distribution, whether the pool was alive at the high (with the recovered-time lower-bound caveat in the docstring), the high-to-writeoff gap, and the per-arm activation comparison that separates a coverage gap from a missed trigger. tests/test_writeoff_readout.py with 8 tests: the round-69 kernel, percentile bounds, a total parser, the HIGH_TOLERANCE band (a looser band would inflate the gap and break the lower-bound claim), the cohort prefix and the deployed floor, plus two live assertions - that the cohort's peaks are mostly positive, and that the MAJORITY of it does NOT reach its own arm's activation, written so the finding fails loudly if it ever reverses. All 8 pass. No behaviour change.",
    "a_latent_bug_found_and_fixed_in_my_own_new_readout": "Both the readout script and its test first loaded the effective definition from an UNQUALIFIED `limit 1` on chain_meme_trader_registrations, which picks an arbitrary epoch whose definition_json carries no 'policies' key - so the contract came back empty and the per-arm section was silently vacuous. The test caught it; both now resolve the version from chain_meme_trader_positions first and then load THAT registration. Same class as round 71's wrong-version reads."
  },
  "ROUND_76_ROUND_75_IS_REVERSED_the_gated_30s_is_ALREADY_OPTIMAL_on_the_right_objective": {
    "THE_DIRECTIONAL_SELF_CORRECTION": "Round 75 optimised qualifying FRAME COUNT and concluded the gated 30 s sat on the low side of a peak, with 60 s better by 21%. THE OBJECTIVE WAS WRONG. store.py:27781-27785 runs inside the per-evaluation admission loop, so the gate is evaluated per TOKEN-FRAME and ONE qualifying frame unblocks that token at that frame. The right objective is how many DISTINCT TOKENS ever present a valid window, not how many qualifying frames exist.",
    "THE_REVERSAL_MEASURED": "Live, 18,371 series / 71,869 frames: 5 s -> 12,450 frames (0.59x), 99 tokens (0.43x). 15 s -> 13,996 (0.66x), 102 (0.44x). **30 s (gated) -> 21,170 (1.00x), 230 tokens (1.00x).** 60 s -> 25,766 frames (**1.22x**) but only 222 tokens (**0.97x**). 180 s -> 21,767 (1.03x), 82 (0.36x). 300 s -> 18,718 (0.88x), 66 (0.29x). BY FRAME COUNT 60 s WINS; BY TOKEN REACH 30 s WINS AND IS THE LIVE VALUE. The two objectives rank the sizes differently.",
    "WHY_THE_21pct_IS_WORTHLESS": "Depth split: series with 1 frame (11,700) and 2 frames (4,102) qualify ZERO frames at ANY window size, because the three-frame minimum is independent of window length. The +4,564 frames are 100% inside the 2,501 series that ALREADY have >=3 frames. On reach, 60 s reaches 221 tokens against 230 for 30 s: 54 tokens qualify only at 30 s, 45 only at 60 s, 176 both. MECHANISM: window(rows, seconds) needs a frame at or before t_end - seconds, so any series whose whole lifetime is shorter than `seconds` can NEVER qualify - and 66.0% of series live under 60 s. A wider window systematically discards short-lived tokens.",
    "THE_COUNTERINTUITIVE_THIRD_MECHANISM": "The gap rule rejects any consecutive gap > 30 s, so a REGULAR 30 s cadence lands exactly on the boundary (30 is not < 30) and the 30 s window qualifies NOTHING while the 60 s window tolerates it. Measured on one series: cadence 10 s -> 30 s window 58 versus 60 s window 55 (30 wins); cadence 20 s -> 29 versus 28 (30 wins); cadence 30 s -> **0 versus 19 (60 wins)**; cadence 45 s -> 0 versus 0. So 'is a wider window better' has NO single answer - it depends on the token's actual cadence, and the fleet is a mix (round 73 measured inter-frame gap p50 17.10 s, p75 76.93 s). That is why the aggregate slightly favours 60 s on frame count and 30 s on reach.",
    "CONCLUSION_NO_WINDOW_LENGTH_LEVER": "WINDOW LENGTH IS NOT AN AVAILABLE LEVER. The live 30 s is the peak of the reach curve. Round 75's '60 s is better' recommendation is WITHDRAWN, its record carries a correction banner, and consolidated section 10.19 plus the final attribution chain are corrected. This costs nothing real: the round-75 instrument and tests remain valid once the objective is reach.",
    "ANOTHER_FIXTURE_ERROR_OF_MINE_RECORDED": "My first test fixture for the disagreement used a long dense series plus a short series and asserted 60 s would have more qualifying frames. It failed at 8 versus 13, because a DENSE series also qualifies many frames at 30 s - I had assumed the direction. I only got the mechanism right after printing a lifetime-by-cadence table, which is the table now in the record. Same root cause as round 75: assuming the direction of an optimisation objective instead of deriving it.",
    "LEVERS_NOW_EXCLUDED_the_complete_list": "Cadence / scan frequency (round 73: 15 s to 5 s is worth +0.9%). Ingestion ceiling (round 73: 0.008% of frames). Lease length (round 65: yield and throughput cancel). Entry-time liquidity floor (round 42: zero). Hard-stop ladder (round 69: 68 vs 100 cohorts and slots are 93% idle). Entry filters too strict (round 72: the dominant gate's tokens convert at 1.03%, ABOVE the 0.46% base). WINDOW LENGTH THRESHOLD (round 76: the gated 30 s already maximises reach). WHAT REMAINS: the constraint is the ABSOLUTE NUMBER of frames - 86.4% of tokens get <=2 frames and every window length needs >=3 - and frame count is set by how many tokens can be held under sustained observation, i.e. the observation slot / lease residency budget, a USER DECISION this session does not touch.",
    "DELIVERED": "scripts/window_size_sweep.py gains reach(series, window_seconds) returning the set of distinct tokens that ever present a valid window, with the round-75/76 correction and its mechanism in the docstring; the output table now prints BOTH columns with their own multiples against the gated size and states explicitly which column to optimise; and when the two objectives disagree on the best size it prints that disagreement. tests/test_window_size_sweep.py now has 13 tests, three new: reach returns a set and never exceeds the qualifying frame count; a regular-30-s-cadence fixture pins that the two objectives can rank the sizes in OPPOSITE order; and a live test asserts the gated size is optimal for reach, written in the direction that fails loudly if the threshold ever does become a lever. All 13 pass. No behaviour change.",
    "STANDING_RULE_new": "60 - Before optimising a gate, establish the UNIT it is evaluated in and set the objective to that unit. store.py:27784 is evaluated per token-frame, so one qualifying frame unblocks that token at that frame and the objective is DISTINCT TOKENS REACHED. Round 75 optimised qualifying FRAME COUNT - easier to compute - and produced a recommendation pointing the OPPOSITE way. Frame count is the metric most likely to look like a result while measuring the wrong thing."
  },
  "ROUND_76_THE_GOLDEN_DOG_PROBLEM_QUANTIFIED_395_of_peaks_ended_negative": {
    "why_this_was_measured_now": "Every entry-side lever now has a measured refutation: cadence (73), ingestion ceiling (73), lease length (65), entry liquidity floor (42), stop width (69), entry filters too strict (72), and window-length threshold (76). What remains on the entry side is the observation slot budget, a USER DECISION. So the work moved to the user's explicit and still-open exit mandate - the 'golden dog you cannot hold' problem - which had never been measured systematically.",
    "METHOD_it_is_descriptive_not_a_counterfactual": "The engine records each position's own running high (highest_signal_price_usd) and entry_execution_price_usd, so capture is directly computable: peak_econ = 0.96*(high/entry_execution_price) - 1 (round 69's verified kernel); realised = realized_pnl_usd/stake_usd; capture = realised/peak_econ, meaningful only when peak > 0. This describes what the deployed exits ACTUALLY did to positions that EXIST. It is NOT a counterfactual on any rule.",
    "THE_HEADLINE_NUMBER": "Of 4,252 settled positions: among the 495 whose peak reached econ >= +50%, **193 (39.0%) ENDED NEGATIVE**, and the group turned a summed peak of +460.7 into a summed realised of -30.9. Capture by peak band: >=+10% n=1,776 capture -79.4% (43% positive); >=+25% n=1,012 capture -31.6% (52%); >=+50% n=495 capture -6.7% (61%); >=+100% n=176 capture -9.8% (53%). Peak distribution per position: p50 +0.0508, p75 +0.2421, p90 +0.5435, p95 +0.9734.",
    "WHICH_EXIT_PATH_CAPTURES": "By median: market_mark_trailing_exit n=368 median peak +0.733 -> median realised +0.210, capture 34.9%, 89% win rate - the current workhorse, giving back about 65% of the peak. market_mark_max_hold n=1,046 capture 44.2%, 51% win. **market_mark_take_profit_1 n=51 capture 98.6%, 100% win - the ONLY mechanism that holds, and it fired 51 times out of 4,252 settled positions (1.2%)**. market_mark_hard_stop n=1,271 capture -998.5%, 1% win. dex_pool_liquidity_below_configured_floor n=1,150 median peak +0.171 -> median realised -1.000, capture -436.6%, 1% win. Worst give-backs: +1.313 -> -1.000 (activity_floor150_t30_v1), +0.973 -> -1.000, +0.624 -> -1.000, all ending in the liquidity-floor write-off.",
    "THREE_STRUCTURAL_FACTS": "(1) The only mechanism that reliably holds a peak is the tiered take-profit, and it almost never fires - 1.2% of settled positions. (2) The trailing exit is the real workhorse and surrenders about two thirds of the peak it reaches. (3) The two largest loss sources are the hard stop and pool death, and the pool-death cohort is NOT a set of tokens that never moved - its median running high was +0.171.",
    "WHAT_THIS_DOES_NOT_ESTABLISH_and_why_no_change_is_proposed": "It does NOT show that a different exit rule would do better. The running high is only knowable AFTER it has been exceeded; a rule that sells at the peak would have to identify the peak in real time. Rounds 36/39/43's rules apply unchanged - no in-sample re-optimisation of a take-profit level on this same epoch. So NO exit parameter change is proposed and no module is created. The table answers 'how bad is the golden-dog problem and which paths cause it', not 'what should it be changed to'. Turning it into a verifiable improvement still requires the user's mandated form - an ADDITIONAL strategy module with its own forward evidence - which is exactly what exit_ladder150 (round 58) exists for; its widest pair still needs 100 cohorts."
  },
  "ROUND_75_THE_GATED_WINDOW_IS_30s_BUT_60s_QUALIFIES_21pct_MORE": {
    "the_counterfactual_redone_correctly": "Round 74 withdrew a broken version of this: my harness sliced each series to exactly MIN_FRAMES=3 before applying the rules, so the window length became irrelevant and 60/120/300 s all returned the identical 27,753 (39.90%). This round ports dex_trajectory.window (lines 49-55) line by line and evaluates all six sizes the engine already computes. The key property the withdrawn version destroyed: `part` runs from the cutoff row to the END of the series and is NOT capped at three frames, which is exactly what lets a longer window hold more frames.",
    "THE_RESULT_over_70,733_frames_evaluated_one_by_one": "5 s: 12,299 qualify (17.39%, 0.59x). 15 s: 13,831 (19.55%, 0.66x). **30 s (the live gate): 20,808 (29.42%, 1.00x).** **60 s: 25,240 (35.68%, 1.21x).** 180 s: 21,277 (30.08%, 1.02x). 300 s: 18,254 (25.81%, 0.88x). THE CURVE IS NOT MONOTONE - it peaks at 60 s and 300 s is WORSE than 30 s. So the live threshold is not 'too strict' in the sense of 'larger is better'; it sits on the LOW side of a peak.",
    "WHICH_RULE_BINDS_AND_WHY_60s_WINS": "Per-attempt failure shares by rule: (A) no full lookback, (B) span cap, (C) a gap over 30 s, (D) under 3 frames. At 5 s: B=52.6%. At 15 s: B=46.1%. At 30 s: B=38.1%, C=5.7%. At 60 s: B=25.6%, C=11.2%. At 180 s: A=35.1%, C=20.3%. At 300 s: A=40.4%, C=22.0%. At small windows the SPAN CAP kills it; at large windows the FULL-LOOKBACK and GAP rules take over. 30 s sits where the span cap is still large (38.1%) while gap failures have barely started (5.7%) - i.e. it rejects many frames that a modestly wider window would accept.",
    "CROSS_CHECK_AGAINST_TWO_INDEPENDENT_MEASUREMENTS": "(1) Round 73's simplified construction ('last 3 frames inside 30 s') gave 30.24%; this round's faithful contract gives 29.42% - two independent implementations agreeing within 0.8 percentage points over 70,733 frames. (2) Round 74's lease panel measured frame3 delay p50 = 49.67 s, and a 60 s window is exactly wide enough to hold a median third frame while a 30 s window is not. The three measurements are consistent.",
    "WHY_NOTHING_IS_CHANGED_and_this_is_a_hard_constraint": "store.py:27784 reads windows['30'] for every arm carrying requires_distinct_trajectory_frame=True. Changing which size it reads would directly alter the entry behaviour of those arms - the USER'S STANDING RULE forbids modifying any existing strategy and requires additions to be EXTRA strategy modules. So this is not deployed. Three technical questions also remain unanswered: (a) qualifying a window does NOT mean a policy would then admit; (b) a longer window measures a SLOWER phenomenon, and the window's outputs also feed velocity, acceleration and realized volatility (dex_trajectory.py:56-69), so the signals change meaning - 21% more qualifying frames is NOT 21% more correct signals; (c) profitability is entirely unaddressed and must not be inferred from a qualification rate. THE LEGITIMATE LANDING FORM, for the user to decide: an ADDITIONAL strategy module whose carrier arms gate on the 60 s window, running forward IN PARALLEL with the 30 s arms so the two thresholds accumulate comparable evidence.",
    "NO_NEW_DATA_NEEDED_which_is_what_makes_this_worth_reporting": "dex_trajectory.py:15 computes WINDOWS = (5, 15, 30, 60, 180, 300) already. The gate simply reads one of the six. So the lever costs no additional collection - it is a threshold choice among numbers the engine is already producing.",
    "DELIVERED": "scripts/window_size_sweep.py - a read-only sweep reporting qualifying frames per size, the multiple against the gated size, and the per-rule failure breakdown, with an explicit statement of what it does not show. tests/test_window_size_sweep.py with 10 tests, three of which exist for the actual mistakes made in rounds 74-75: test_the_window_parameter_changes_the_result fails if the construction ever makes the window length irrelevant again (the exact round-74 bug); test_part_is_not_capped_at_three_frames guards the property that lets a longer window hold more frames; and a live test asserts some already-computed size is at least as good as the gated one, so the round-75 finding reverses loudly if it ever stops holding. All 10 pass. No behaviour change.",
    "ALSO_RECORDED_a_test_fixture_error_of_mine": "My first test fixtures used short series, e.g. frames ending at t=30 with seconds=30. Those fail rule (A) full lookback - the series must START at least `seconds` before its last frame - so four tests failed on fixtures, not on the function under test. I printed (ok, why, part length, span, max gap) for each case to correct them rather than adjusting assertions.",
    "STANDING_RULE_new": "59 - When a curve has an interior optimum, check whether the live setting sits on the low or the high side before saying anything about direction. Qualification rose 17% -> 36% from 5 s to 60 s and FELL to 26% by 300 s, so the live 30 s is on the LOW side of a peak. Scanning only 5/15/30 would have suggested 'longer is better'; scanning only 30/300 would have suggested '30 s is right'. Six sizes were needed to see the peak, and the direction claim depends entirely on having both sides."
  },
  "ROUND_74_THE_SCHEDULER_WAS_RECORDING_ITS_OWN_VERDICT_ALL_ALONG": {
    "the_panel_that_was_always_there": "The runtime checkpoints its per-token observation state into the kv table under key `chain-meme-pattern-watch:leases145`. Each lease item carries frame_count, frame2_delay_seconds, frame3_delay_seconds, admitted_at, min_observe_until, next_due_at, last_useful_at, coverage_gap, phase, window_counted, window_expired and window_results. Companion keys: `chain-meme-pattern-watch` (watch bookkeeping) and `coverage145:status` (coverage accounting). Rounds 64-73 reconstructed observation behaviour from token_snapshots - what happened to be STORED - because this panel was not known to exist. The reconstruction could only reach 'too few frames'; the panel reaches the NEXT distinction.",
    "THE_DISTINCTION_THE_PANEL_ADDS_and_why_it_matters": "observation_leases145.expire_windows:158-164 labels each matured window: SOURCE_NO_UPDATE (line 163) means fewer than 3 frames existed by the deadline plus 30 s grace - the SOURCE did not answer, so no path repair helps. UNKNOWN_PATH_GAP (line 162) means the frames DID arrive but a requirement failed (a coverage gap, a last frame more than 30 s late, or frame_count < 3) - a continuity problem, not a source problem. The two have opposite remedies and round 73 could not tell them apart.",
    "THE_ARITHMETIC_THAT_CLOSES_THE_QUESTION": "dex_trajectory.window(rows, 30) requires THREE frames INSIDE 30 seconds (dex_trajectory.py:50-55, via len(part) >= 3 and the span limit), and store.py:27784 tests exactly `windows['30']` when it sets the dominant gate. The panel's own delay fields: frame2 p50 28.51 s, frame3 p50 49.67 s. A median third frame at 49.67 s cannot sit inside a 30 s window - short by about 1.7x. Independently recomputed from the stored cadence this round: only 30.24% of frames can present 3 frames inside 30 s, against the gate's 46.41% share of all per-arm verdicts (round 72). TWO INDEPENDENT PATHS AGREE.",
    "THE_PANEL_RATES_AND_THE_HONEST_SPLIT": "mature_window_counts, cumulative over the epoch, within-row shares only: 30 s window 1,886 attempts - OBSERVED 28.3%, SOURCE_NO_UPDATE 53.8%, UNKNOWN_PATH_GAP 18.0%; 120 s window 1,394 - 41.2% / 47.4% / 11.4%; 300 s window 812 - 39.2% / 29.9% / 30.9%. SOURCE_NO_UPDATE falls monotonically with window length (53.8% -> 47.4% -> 29.9%) and OBSERVED is worst at 30 s, which is the shape an update-cadence limit produces. BUT UNKNOWN_PATH_GAP is NOT monotone - highest at 300 s (30.9%) - so 'widen the window' is supported for the SOURCE_NO_UPDATE half and NOT established for the continuity half. Both halves are stated; neither is averaged away.",
    "MANDATORY_UNIT_CAVEAT": "mature_window_counts tallies distinct (token, window) PHASE TRANSITIONS, counted once each at maturity - these are NOT distinct tokens, and the panel covers only tokens admitted to the observer. COLUMN SUMS ARE NOT A TOKEN POPULATION. Only within-row shares are comparable. This caveat is asserted by a test so it cannot be dropped from the docstring.",
    "A_COUNTERFACTUAL_I_COMPUTED_AND_WITHDREW": "I computed 'what share of frames would qualify at a 60/120/300 s requirement' and got the IDENTICAL figure 27,753 (39.90%) for all three, which is impossible: widening a span limit cannot saturate at exactly the 60 s value. THE CAUSE WAS MY SCRIPT - slicing each series to exactly MIN_FRAMES=3 before applying the rules makes the window length irrelevant, since 3 points never exceed even the 30 s span limit. The figure is WITHDRAWN and NO counterfactual on window length is reported. Doing it properly needs the exact multi-frame construction of dex_trajectory.window at each size (end - part[0] may span the whole window, not 3 points).",
    "ANOTHER_MISREAD_I_MADE_THIS_ROUND": "My first probe assumed the top level of leases145 was a {token: item} mapping and reported 'lease items found: 0' on a panel that held real data. `leases` is a LIST. A test now asserts the shape explicitly, because the failure looked exactly like 'the panel is empty'.",
    "NO_PARAMETER_RECOMMENDED_and_why": "The window length is a design parameter, not a market fact, so widening it is legitimate to CONSIDER - but this round established only half the case (see THE_PANEL_RATES above), and I have no trustworthy counterfactual. NOTHING IS RECOMMENDED FOR CHANGE. If it is ever changed, the user's standing rule applies: it must be an ADDITIONAL strategy module, no existing strategy may be modified, and it needs its own forward evidence - this round does not answer whether a longer window would be profitable.",
    "DELIVERED": "scripts/lease_panel_readout.py - a read-only readout of the three kv panel keys: live lease frame counts, frame2/frame3 delays against the 30 s requirement, the cumulative mature-window outcome table with its unit caveat and the meaning of each verdict, plus the watch and coverage counters. tests/test_lease_panel_readout.py with 6 tests: percentile bounds, a total parser, the three key names, the LIST shape plus required item fields (guarding the exact mistake made this round), within-row normalisability of the window table, and the docstring's two-outcome distinction and unit caveat. All 6 pass. No behaviour change.",
    "STANDING_RULE_new": "57 - Before reconstructing a system's behaviour from its OUTPUTS, look for the state it writes about ITSELF. Rounds 64-73 inferred observation behaviour from token_snapshots; the kv lease panel had been recording frame_count, frame2/frame3 delays, window_results and a two-way failure classification the whole time. Reconstruction is not only slower, it is strictly less informative: round 73 could reach 'too few frames' but could not separate 'the source never answered' from 'the frames arrived and continuity failed', and those need opposite fixes. 58 - When a counterfactual returns the SAME number for every value of the parameter, suspect the harness before the data. Identical results at 60/120/300 s meant my construction had made the parameter irrelevant, not that the quantity saturates."
  },
  "ROUND_73_THE_DOMINANT_GATE_IS_FRAME_SUPPLY_NOT_CADENCE_two_hypotheses_refuted": {
    "the_gate_is_now_a_computable_condition_not_a_reason_string": "Round 72 measured await_distinct_dex_trajectory_frame at 46.41% of per-arm verdicts and terminal for 98.9% of the pairs that touch it. This round found the line that decides it (store.py:27781-27785) and the function it depends on (dex_trajectory.window, lines 49-55). It passes only when ALL FOUR hold: at least one row with t <= end-30s (a FULL 30 s lookback), span <= 30s + max(5, seconds/3), NO CONSECUTIVE GAP EXCEEDS 30 s, and at least THREE FRAMES. Two independent conditions can emit the reason string and the string does not distinguish them: (a) the trajectory frame's observed_at string differs from iso(snapshot.observed_at), or (b) windows['30'] is absent.",
    "HYPOTHESIS_1_REFUTED_a_string_format_defect": "I suspected (a) was a formatting bug: the comparison is a raw string equality between a value read out of SQLite and iso(datetime), which always emits 6 microsecond digits and a trailing Z (models.py:45-46). If any producer omitted microseconds, two strings denoting the SAME INSTANT would compare unequal and every such frame would be reported as awaiting a distinct trajectory frame - a defect that would consume nearly half the fleet's verdicts. MEASURED AND REFUTED: token_snapshots.observed_at / ingested_at / recorded_at and market_mark_history.observed_at are 100% in the exact 6-microsecond+Z format, and iso(parse_time(v)) == v EXACTLY in 20,000/20,000 rows for each. So the inequality is a REAL inequality - that frame genuinely is not the current one. The gate is meaningful.",
    "HYPOTHESIS_2_REFUTED_cadence_is_not_the_lever": "If frames arrived too slowly, compressing the poll cadence would help. Counterfactual: hold each series' OBSERVED lifetime and generate frames at a fixed cadence, then apply the window rules. Result over 6,431 multi-frame series: 5 s -> 6,164 window-capable; 10 s -> 6,140; 15 s -> 6,110; 20 s -> 6,090; 30 s -> 0; 60 s -> 0. Going from the observer's 15 s fast path to 5 s buys +0.9%. And 30 s/60 s cadences satisfy NOTHING, because the window forbids any consecutive gap above 30 s and a 30 s cadence cannot survive jitter. Cadence changes WHEN frames land, not HOW MANY arrive.",
    "HYPOTHESIS_3_REFUTED_frames_are_not_being_dropped": "I suspected the 30 s ingestion ceiling was discarding frames. MEASURED: observed_at -> recorded_at lag p10 0.015 s, p50 0.877 s, p90 7.399 s, p99 24.734 s, and only 7 frames (0.008%) exceed 30 s. Nothing material is dropped.",
    "THE_ACTUAL_CONSTRAINT_FRAME_SUPPLY": "63.7% of the 17,708 token series hold exactly ONE frame, 22.7% hold two, and only 13.6% ever reach the three the window needs (p50 = 1 frame). Applying the window's own rules at every frame: only 29.29% of frames could form the window, and only 224 of 17,708 series (1.26%) ever had a qualifying frame. Failure split per frame: span_exceeds_limit 38.1%, no_full_lookback 27.0%, gap_exceeds_30s 5.7%. Cross-checked against the engine's OWN re-evaluation counts: of the 11,266 single-frame tokens, 82.1% have exactly ONE evaluation row and none has more than five, while tokens with >=3 frames have a median of 8 evaluation rows. Every token with a snapshot has an evaluation row (17,673/17,673), so coverage does not leak. THE SYSTEM'S ACTUAL BEHAVIOUR IS: see a token once, evaluate it once, do not look again. A single-frame token therefore CANNOT pass a gate that requires three frames - not because the gate is too strict, but because there is no material for it.",
    "consequence_this_retires_a_whole_direction": "The user's standing requirement to raise scan frequency and reduce latency-driven opportunity loss does NOT apply here, and this round is the evidence: cadence is worth +0.9% at best, ingestion latency is sub-second at p50, and the decision path was already measured at p50 0.000 s (round 64). The binding quantity is the NUMBER of frames a token accumulates, which is set by how many tokens can be held under sustained observation - the observation slot / lease residency budget, a USER DECISION this session does not touch.",
    "DELIVERED": "scripts/frame_window_readout.py - a read-only readout that reports the frame-count distribution, per-token window feasibility with the exact failing rule, the inter-frame gap distribution, the ingestion lag (so 'frames are dropped' is confirmed or refuted rather than assumed), and the fixed-cadence counterfactual that separates cadence from supply. Plus tests/test_frame_window_readout.py with 8 tests pinning each of the four window rules independently, the derived constants against dex_trajectory.py, the counterfactual's monotonicity, and two live properties (most tokens have fewer than 3 frames; ingestion drops under 1%). All 8 pass. No behaviour change: the script opens the database mode=ro and writes nothing.",
    "a_bug_in_my_own_new_script_caught_before_commit": "The counterfactual's first version set its baseline from whichever cadence happened to be evaluated first (5 s) while the column was labelled 'vs 15s', so every row printed a 0.0% delta. Fixed to look the 15 s reference up explicitly, and a test now asserts a faster cadence can never produce FEWER window-capable series than a slower one over the same lifetime.",
    "P1_NEXT_accuracy_and_efficiency": "63.7% of tokens are observed once and can never satisfy the gate, yet each still consumes a discovery, a snapshot, an evaluation and one verdict per arm - the bulk of the 5.96M verdicts. If a single observation could establish that a token does not merit continued observation, that compute could be released; this is the most direct lever on the user's stated goal of reducing wasted computation and idling. NOTE: this round proposes the direction only and does NOT show that single-frame decisions are sound. Separately still open: wait_passive_cohort_opportunity (29.50%) and the gate both touch 38.7% of tokens and both convert at 1.03%, so they are probably the same candidates in series.",
    "STANDING_RULE_new": "55 - Refute the cheap wrong explanation before publishing the expensive right one. Three candidate causes for the dominant gate were separable by measurement alone: a string-format defect (refuted: 20,000/20,000 exact round-trips), a cadence shortfall (refuted: +0.9% from 15 s to 5 s), and frame starvation (confirmed: 63.7% of tokens hold one frame). Publishing the third without killing the first two would have been correct by luck, and the first two are exactly the kind of plausible defect this project has repeatedly found real. 56 - When a requirement asks for a frequency or latency improvement, first check whether the quantity that actually limits the outcome is a RATE at all. Here the ask was 'raise scan frequency and cut latency', but the limiter is a COUNT - frames per token - which no cadence can raise and which sub-second ingestion latency already satisfies."
  },
  "ROUND_72_THE_REAL_ADMISSION_GATE_READ_and_5.9M_verdicts_were_never_readable": {
    "the_unread_range_is_now_read": "Round 71 flagged store.py:28150-28366 as the unread admission range and named it the next action. It is NOT a threshold on the token; it is a per-arm CASCADE: store.py:28150-28213 dispatches by entry_filter['contract'] to one of N signal functions (capital_observation_signal / lifecycle_signal / impulse_signal / renewal_signal / runner_signal / archive_signal / research_signal / resource_entry_signal, with pattern_signal as fallback); :28214-28224 apply four blocks (pattern_already_enrolled_at_this_pool, entry_blocked[arm], round2_chase_original_opportunity_consumed, prior_closed_core_loss_same_pool); :28225 records outcomes[arm]=reason; :28226-28227 append to ready; :28228-28237 build admitted_arms; :28241-28269 re-check compatibility and can REMOVE an admitted arm; :28270-28280 drop a whole pair group if any member is missing. THIS EXPLAINS round 71's 'all 7,154 decisions admitted': entry_decisions only records arms that survived to the write, and most arms are gone before :28228.",
    "THE_DISTRIBUTION_WAS_ALREADY_PERSISTED_AND_NOBODY_HAD_READ_IT": "store.py:28225 writes outcomes and :28283 persists it as feature_json['outcomes'] - ONE REASON PER ARM PER CANDIDATE FRAME. The `reason` COLUMN of the same table holds only one summary string per row, which is why the real distribution was unreachable. Scale measured this round: 53,404 evaluation rows, 5,962,774 per-arm verdicts, 74 distinct reasons, 1,276,525 (token, arm) pairs. This had never been read before round 72.",
    "THE_DOMINANT_GATE_AND_THE_VERDICT_THAT_IT_IS_NOT_OVER_STRICT": "await_distinct_dex_trajectory_frame: 2,767,108 verdicts = 46.41% of all; touches 6,596 tokens = 38.7% of evaluated tokens (so it is SELECTIVE, not a universal first state); of the 1,121,875 (token,arm) pairs that touch it, **98.9% END on it** and only 2.6% ever record a different reason after first entering it; its tokens convert at 1.03% against a fleet base rate of 0.46%. CONCLUSION: it is not discarding good candidates (its rate is ABOVE base), it is where candidate flow TERMINATES. Mechanism is the round 64-65 chain: three DISTINCT trajectory frames need 60-90s of continuous observation while the provider yields new information only every ~30.1s, so most candidates never collect the third distinct frame and the pair simply stops advancing.",
    "DIRECT_ANSWER_TO_which_threshold_blocks_tradeable_candidates": "NO FILTER IS SYSTEMATICALLY DISCARDING TRADEABLE CANDIDATES AT SCALE. Ranked by volume blocked, the top gates convert at 1.03% / 1.03% / 1.10% / 1.44% against a 0.46% base - only 2-3x, and above base means they are not selecting for failure. The gates that LOOK most selective act on tiny populations and are not opportunity losses at all: strategy_token_lifetime_entry_consumed touches 57 tokens with 100% already positioned, and pattern_already_enrolled_at_this_pool touches 46 tokens with 100% already positioned - both are duplicate-entry GUARDS. What removes trade volume is a structural WAIT, not a strict threshold.",
    "TWO_AGGREGATION_ERRORS_IN_ONE_ROUND_both_rule_26": "(1) r72c printed PAIR-level counts under a 'token' column: await_distinct_dex_trajectory_frame showed 227,349 where the DISTINCT TOKEN count is 6,569, and strategy_token_lifetime_entry_consumed showed 514,895 where it is 57. (2) the first version of scripts/outcomes_readout.py printed an 'escape %' whose numerator was a PAIR count and denominator a VERDICT count, giving 40.6% for a gate whose true terminal share is 98.9%. Resolved in r72f: 'escape rate' is genuinely ambiguous because pairs oscillate (gate -> other -> gate), so the same data yields 67.5% (no later occurrence), 98.9% (every occurrence is last) and 2.6% (any different reason after the first). The load-bearing quantity is the FINAL-STATE share, which needs no such choice. The readout now prints verdicts / tokens / pairs side by side precisely so this class of error is visible next time.",
    "A_FAILED_TEST_RECORDED_SO_IT_IS_NOT_RETRIED": "r72b tried to grade gates by forward PRICE MOVE and failed for a structural reason: only 1,674 tokens have a mark series and 1,231 have >=2 marks, because the mark table is POSITION-DRIVEN; their running-high econ p10=p25=p50=p75 are ALL exactly -0.0400; and their implied token-level +10% touch rate is 7.05% against the engine's measured 41.08% for positions (round 69, n=3,892). That sample is not representative of refused candidates, so every 'DISCARDS MOVERS' verdict it printed is WITHDRAWN. Gate grading must use the engine's own records, as r72c/r72d/r72f do.",
    "DELIVERED": "scripts/outcomes_readout.py - a read-only repeatable readout of feature_json['outcomes'] (verdicts / tokens / pairs / rate / ends% / final% per reason, with the ambiguity and the no-price-data decision documented in its docstring), plus tests/test_outcomes_readout.py with 5 tests: three assert the arithmetic invariants that catch the two rule-26 errors above (verdicts vs tokens vs pairs as different units; ends% = pairs/pairs and demonstrably different from pairs/verdicts), one asserts the admission reason cohort_frozen_opportunity_ready is declared rather than inferred, one asserts the timestamp parser is total, and a live-database test asserts the gate both DOMINATES verdicts (>30%) and is TERMINAL (>80%) - so if the upstream observation constraint ever genuinely loosens, that test fails. All 5 pass. No behaviour change: the script reads only.",
    "P1_FOR_NEXT_ROUND": "wait_passive_cohort_opportunity (29.50% of verdicts) and await_distinct_dex_trajectory_frame (46.41%) both touch 38.7% of tokens and both convert at 1.03%, and the passive reason is terminal for 84.2% of ITS pairs. They are almost certainly acting on the same candidates - either in series (wait for frames, then wait for a cohort slot) or double-counting. If it is double-counting, the 29.50% overstates passive waiting. Resolving this is the accuracy item for the next round.",
    "STANDING_RULE_new": "53 - When a statistic's value depends on an arbitrary choice you have not made explicit, report the choice-free quantity instead. 'Escape rate' for a state a pair can re-enter yielded 67.5% / 98.9% / 2.6% on the same data depending on whether you ask about the first occurrence or all of them; the FINAL-STATE share (98.9%) answers the question without needing that choice. 54 - A readout's unit must be in its column header, and it must print every competing unit side by side. Both round-72 errors were unit mix-ups (pairs printed as tokens; pairs divided by verdicts) in a script whose whole purpose was to stop re-deriving a number wrongly."
  },
  "ROUND_71_THE_FULL_FUNNEL_MEASURED_and_I_walked_three_wrong_code_paths": {
    "THE_FUNNEL_the_user_asked_for_and_nobody_had_measured": "Whole epoch, distinct tokens: discovered 17,634 -> received a raw snapshot 16,583 (94.04%) -> reached an entry evaluation 16,583 (100% of snapshots) -> >=2 distinct frames 7,901 -> >=3 distinct frames ('qualified') 2,259 (12.8% of discovered) -> >=50 frames 106 -> **EVER opened a position 77 (0.44%)**. So DISCOVERY DOES NOT LEAK (94% reach evaluation); the cliff is qualified-candidate -> position: 2,259 -> 77. And observation depth is the dominant correlate: the 106 tokens with >=50 frames produced 55 of the 77 positions (51.89% conversion in that band) while every band from 1 to 49 frames sits at 0.00-0.88%.",
    "DEPTH_FIGURES_with_the_required_qualifier": "frames 1 -> 10,613 tokens / 12 positions (0.11%); 2 -> 3,743 / 7 (0.19%); 3 -> 733 / 1 (0.14%); 4-5 -> 332 / 0; 6-9 -> 615 / 0; 10-19 -> 245 / 0; 20-49 -> 227 / 2 (0.88%); 50+ -> 106 / 55 (51.89%). QUALIFIER THAT MUST TRAVEL WITH THIS TABLE: the engine records only an upward running high (highest_signal_price_usd) and has NO downside running-min column, so the ZERO hits in the 4-19 frame bands cannot be cross-validated against any engine-side quantity. Those zeros are recorded as a待查 item, NOT as a conclusion.",
    "ENTRY_DECISIONS_ARE_NOT_THE_GATE": "All 7,154 rows of chain_meme_trader_entry_decisions carry status='admitted' and reason='pattern_next_observation' - ZERO rejections of any kind. So the loss happens BEFORE the decision statement, not inside it. Any theory that blames an entry decision rule is refuted by this table.",
    "I_WALKED_THREE_WRONG_CODE_PATHS_IN_ONE_ROUND": "(1) I attributed everything to the historical_fidelity_mode block at store.py:29404-29895 and computed '330 policies -> 219 not paused -> 0 after excluding the 3 observer modes -> market_visible (49% of candidates) and flow_burst structurally unreachable -> 98.5% of policies cannot make entry decisions'. EVERY STEP OF THAT REASONING IS VALID BUT THE PREMISE IS FALSE: definition['historical_fidelity_mode'] is None for this epoch, so the block DOES NOT RUN. All of those numbers are WITHDRAWN. (2) I then attributed to the entry_gate five-gate loop at store.py:30677-30715 (two_way_route/economic_route/rug_safety/solana_focus/shadow_momentum); production contains ZERO *_pass and ZERO *_not_pass reasons, so that path does not run either. (3) Store.chain_meme_trader_policies() returns a DIFFERENT 12-policy set with an older schema (family/entry_gate, no entry_family/entry_filter/entry_match_mode) whereas the runtime effective definition has 330 policies - I reasoned from the wrong one for a while.",
    "I_ALSO_WITHDRAW_MY_OWN_earlier_claim_that_entry_filter_fields_are_unenforced": "I wrote that the 1,648 entry_filter keys (145 distinct names) are not read because Store.chain_meme_trader_entry_filter_matches (store.py:24304-24366) checks only 8 names. WRONG: the admission point really does read entry_filter - store.py:28270 groups by entry_filter['max_concurrent_positions'] and store.py:28295 filters on entry_filter['single_token_lifetime_entry']. Correct statement: that one FUNCTION has narrow coverage, but entry_filter is read elsewhere, so 'is field X enforced' must be confirmed at the admission point per field and CANNOT be inferred from one function's coverage.",
    "THE_CORRECT_ENTRY_POINT_FOUND": "This epoch's entry_decisions are written by store.py:28366-28404 (reason 'pattern_next_observation'). Two decisive properties: (a) line 28380 HARDCODES entry_family='broad_launch' on every cohort it creates, so the market_visible/flow_burst family assignment I had been analysing is NOT this path; (b) the decision has only three possible outcomes - admitted, ranker_concurrent_slot_limit (slots>=3), entry_cash_below_order_size - and production shows 100% admitted, so ADMISSION IS ALREADY COMPLETE BEFORE LINE 28366. The real gate is the construction of `ready` / `by_notional` / `active` at approximately store.py:28150-28366. READING THAT RANGE IS THE NEXT ROUND'S FIRST ACTION.",
    "LOSS_DECOMPOSITION_for_qualified_tokens_with_its_caveat": "Of the 2,259 qualified (>=3 frame) tokens: liquidity gate 1,242 (55.0%), no matching entry policy 901 (39.9%), invalid snapshot (asof) 58 (2.6%), converted 58 (2.6%). CAVEAT: the 'no matching entry policy' figure rests on the reason string no_active_matching_entry_policy, which is itself untrustworthy - it is emitted when `family_arms` is empty and it OVERWRITES a more specific earlier reason (store.py:29806 overwrites the :29715 value family_episode_already_enrolled_or_cooldown_active). So that 39.9% row needs re-attribution.",
    "THE_CONSTRAINT_MOVED_during_the_epoch": "Same cohort of qualified candidates, did it ever convert: hour 18 455->5 (1.10%), 19 447->4 (0.89%), 20 463->15 (3.24%), 21 382->13 (3.40%), 22 209->6 (2.87%), 23 208->14 (6.73%). cohorts per qualified candidate rose 0.043 (hour 19) -> 0.745 (hour 23). So the binding constraint is NOT constant - an early gate loosened. NOTE the honest bound: conversion is measured over the whole epoch so a late-qualified token has had less time; the last hour's rate is an UPPER bound.",
    "P0A_WITHDRAWN": "My proposed fix 'add dex_visible policies to serve market_visible / flow_burst' is WITHDRAWN - it rested on the false premise of wrong path (1). dex_visible does exist as a mode in code (store.py:23784-85) but its relevance to the live path is UNCONFIRMED, so no strategy may be added on that basis. SURVIVING recommendations: (P0-B, observability only) add a field recording the reason that no_active_matching_entry_policy overwrites, since that masking cost three hours of misattribution this round and changes no decision logic; (P1) map all 145 entry_filter field names to the code that actually reads them, producing enforced / unenforced / enforced-elsewhere lists; (P1) separate entry_pool_liquidity_absent_curve_stage (a curve-stage determination - all 5,398 of its tokens DO appear in token_market_surfaces and token_snapshots) from below_configured_floor (a correct refusal).",
    "OPEN_QUESTIONS_FOR_NEXT_ROUND": "(1) The 17,591 evaluation rows carrying no_active_matching_entry_policy can only be written by store.py:29806, which sits inside a branch that does not run for this epoch - so WHICH version/run wrote them? Until that is answered this reason distribution will keep misleading. (2) The admission filter chain at store.py:28150-28366 (the round's most important unread range). (3) The exact relationship between the 12 legacy policies, the 127 in registration.definition_json, and the 330 in the effective definition (policy_additions' 330 rows all have runtime_addition_id set and match the effective 330). (4) The 4-19 frame zero-hit bands, unverifiable from engine-side data.",
    "CORRECTIONS_TO_ROUND_70_KEPT": "Round 70 said '960 cohorts' - actually 690 produced positions (v6_cohorts has 977 rows). It said 'each cohort shared by ~4.06 arms' - the distribution is BIMODAL (42.5% of cohorts get exactly ONE arm, 2.2% get 41+; mean 5.65) so the mean misleads. It said '~160 cohorts/hour' - actually 116/hour (690 / 5.93h). UNAFFECTED: the position-slot utilisation conclusion (171 open / 330x8 = 2,640 = 6.48%). Denominator labelling required: policy_additions 330 is the REGISTERED set; the console's 457 is every policy the engine builds.",
    "STANDING_RULE_new": "51 - Before attributing production behaviour to a code path, PROVE THE PATH RAN. Three times in one round I read a block, reasoned correctly inside it, and published a conclusion - while definition['historical_fidelity_mode'] was None and the block never executed. The cheap proof is always available from data the path itself writes: if the branch emits reason strings, grep the production table for them. The historical_fidelity block emits no_active_matching_entry_policy / family_episode_already_enrolled_or_cooldown_active; the entry_gate loop emits *_pass / *_not_pass; production contains 7,154 rows of exactly one reason, pattern_next_observation, which belongs to a THIRD writer. Presence of a reason string in a table is not evidence about which branch wrote it when more than one version shares the table - check definition_version too. 52 - 'Field X is never read' requires enumerating the read sites, not measuring one function's coverage. I claimed 1,648 entry_filter keys were unenforced from a single function (store.py:24304) that checks 8 names, while store.py:28270/28295 read entry_filter at the admission point."
  },
  "ROUND_69_MY_EXCURSION_MEASUREMENT_WAS_ON_THE_WRONG_BASIS_AND_WRONG_JOIN": {
    "the_two_self_corrections": "Intent was the user's exit mandate: the ladder exit_ladder150 covers only the TAKE-PROFIT side, so I quantified whether a HARD-STOP ladder would reach a readout faster. Answer: NO (rejected, see below). But the round's real output is two corrections to my own rounds 68 and 69b. (A) ENTRY-SLIPPAGE CONSTANT TREATED AS SIGNAL: r69b divided marks by entry_SIGNAL_price and got '58.03% of positions reach -10%'. Measured: entry_execution_price_usd / entry_signal_price_usd is EXACTLY 1.0400 at p1 through p99, so every position starts 4% 'down' by construction and the median position's FIRST mark reads econ = -0.0400 with p25 = p50 = p75 all identical - the fingerprint of a CONSTANT. (B) WRONG JOIN: rounds 68c/69b/69d took MAX(chain_meme_trader_market_mark_history.price_usd) joined only on token_id with recorded_at >= opened_at, with NO pool restriction and NO closing upper bound. Measured against the engine's own highest_signal_price_usd column it was HIGHER in 1,471 of 3,892 positions, worst case x5.406 - a stale second pool or a post-close mark fabricates any multiple. Adding the pool and the closing bound: agreement 3,529/3,892 (90.7%), history higher only 53. This is rule 10 recurring (a window statistic must be identity-filtered before taking an extremum).",
    "THE_ENGINE_BASIS_READ_FROM_SOURCE_AND_INDEPENDENTLY_VERIFIED": "store.py:35686-35705 -> entry_price = position.entry_execution_price_usd OR entry_signal_price_usd; high = max(position.highest_signal_price_usd, mark_price); economic_return = current_economic_value/stake - 1. paper_execution.py:118 -> net = quantity * price * (1 - sell_slippage_bps/1e4) = 0.96. SO econ = 0.96*(high/entry_execution_price) - 1. INDEPENDENT CHECK against the engine's own recorded highest_economic_value_usd: stake-relative HEV reproduces 0.96*(high/entry)-1 within 2e-3 in 3,708/3,892 = 95.3%.",
    "CORRECTED_UPWARD_CDF_replacing_round_68c": "ENGINE column (n=3,892), execution basis: P(>=+5%)=0.4872, P(>=+10%)=**0.4108**, P(>=+15%)=0.3430, P(>=+20%)=0.2819, P(>=+25%)=**0.2117**, P(>=+30%)=0.1791, P(>=+35%)=0.1457. The pool+window-restricted history now agrees to within 0.4pp at every level (0.4094, 0.3399, 0.2801, 0.2078, 0.1769, 0.1457), which is what makes both trustworthy. ROUND 68c's 0.5702 and 0.3186 are WITHDRAWN - inflated by 16 and 10 percentage points. The structural-zero-information share for the ladder is therefore 58.9%, not the 42.05% I published.",
    "CORRECTED_LADDER_BANDWIDTH_THE_READOUT_GATE_IS_NOW_100_COHORTS": "cohorts needed for 20 divergences, ENGINE bound: +10-vs-+25 0.1991 -> **100** (was reported as 80); +15-vs-+25 0.1313 -> 152 (unchanged); +10-vs-+20 0.1290 -> 155 (was 100); +20-vs-+25 0.0701 -> 285 (was 391, so EASIER); +10-vs-+15 0.0678 -> 295 (was 166); +15-vs-+20 0.0612 -> **327** (was 252). READ +10-vs-+25 FIRST at a 100-cohort gate; the widest-to-narrowest spread is 3.3x, not 4.9x.",
    "HARD_STOP_LADDER_REJECTED_with_the_numbers": "Widest hard-stop pair -10% vs -60% needs 68 cohorts against the take-profit ladder's 100, i.e. only ~1/3 faster. THREE reasons to reject: (1) not enough to cover the cost of a new module + registration + tests + a controlled restart + another queued readout; (2) SLOTS ARE THE REAL CONSTRAINT, NOT STATISTICAL POWER - carriers are capped at 8 per arm (user-authorised, fleet-uniform), 32 slots total, measured throughput 24.3 cohorts/h, so 68 and 100 are BOTH multi-hour and the difference is swamped; (3) THE DOWNSIDE DISTRIBUTION IS SATURATED - past -5% the curve collapses and flattens (<=-20% 51.67%, <=-50% 38.39%, <=-60% 34.37%), so failing positions cannot be told apart, whereas the upside still separates ~20pp between +10% and +25%. A BETTER CANDIDATE, deliberately NOT built this round: a TIME-STOP ladder (info rate is high and orthogonal to stop width: 85.68% still open past 1 min, 64.13% past 5, 28.24% past 15, 15.53% past 30, 5.69% past 60).",
    "THE_DOWNSIDE_HAS_NO_ENGINE_CROSS_CHECK_and_that_is_a_standing_limit": "The engine records ONLY the upward running high (highest_signal_price_usd); it has NO downside running-min column. So every downside figure can only come from the mark history and there is NO engine-side quantity to validate it against - the two-basis agreement that protects the upside does not exist on the downside. All downside numbers must therefore carry the qualifier HISTORY BOUND, NOT ENGINE-VERIFIED. The one directional corroboration: of the 1,070 positions that actually exited via market_mark_hard_stop, 918 (85.8%) have a mark series whose low does reach -0.35, and the hard_stop group's running-low p50 is -0.711 against -0.089 for liquidity_floor - mechanically the right shapes, but systematic bias is not excluded.",
    "P0_STATE_MEASURED_THIS_ROUND": "exit_ladder150: 12 opened / 4 settled / 8 open per arm, 0 diverged in all six pairs (gate 100 now). exit150_full15_v1 vs bank15: 6 diverged (17 shared, 11 identical). exit150_full15_v1 vs full25: 17 diverged (32 shared, 15 identical).",
    "NOTHING_WAS_CHANGED": "No module was created, no strategy/threshold/contract/runtime behaviour modified, no service restarted, and no effect conclusion drawn (all three P0 pairs remain below the gate). Cross-arm stop-width differences in the fleet are NOT used as causal evidence because entry contracts differ.",
    "STANDING_RULES_new": "49 - Two readings of one quantity that differ by DOUBLE-DIGIT percentage points mean YOUR JOIN is wrong before it means the data is wrong. token_id plus a lower time bound is NOT a valid identity key for a window statistic: restrict the POOL and add the CLOSING upper bound. Here that alone moved the agreement with the engine's own column from 'history higher in 1,471 of 3,892 (worst x5.4)' to '3,529/3,892 exact, history higher in 53'. This is rule 10 recurring for the second time. 50 - Before computing any ratio-derived quantity, verify the BASIS STARTS AT ZERO. Dividing by entry_signal_price_usd when entry_execution_price_usd/entry_signal_price_usd is identically 1.0400 bakes a 4% constant into every row; the tell is p25 = p50 = p75 at exactly -0.0400. Reading the engine's own source gave the authoritative form econ = 0.96*(price/entry_execution_price) - 1, confirmed 3,708/3,892 against the engine's recorded highest_economic_value_usd. Computing a ratio on a quantity that contains a constant is the shared cause of rounds 41, 52 and 69."
  },
  "ROUND_68_I_FALSELY_DECLARED_AN_8_HOUR_OUTAGE_AND_CORRECTED_MY_OWN_WAIT_ESTIMATE": {
    "what_I_claimed_and_why_it_was_wrong": "Chasing round 67's P0 readout I read chain_meme_trader_positions.last_evaluated_at = 2026-09-13T00:09:22 while the local clock said 08:08:49, saw EVERY pipeline table's max clock stopped near 00:10, saw /api/live return generated_at 00:10:48, and concluded 'the runtime has been dead for 8 hours', preparing to restart the service. NOTHING WAS WRONG. The DB writes timestamps as UTC WITH A TRAILING Z (verified: all 171 clock columns carry 'Z'); I was comparing that against the LOCAL file mtime and Get-Date on a UTC+8 machine. 00:12Z IS 08:12+08:00. The data was being written at that instant.",
    "THE_before_after_double_read_that_settled_it": "Same read-only connection, 40 s apart (r68h_watch.py): positions 3,882 rows/00:11:14 -> 3,885/00:12:00; v6_entry_evaluations 77,427 -> 77,627; token_snapshots 77,430 -> 77,640; provider_post_ambiguity_admissions 214,723 -> 215,433; token_discovery_exposures 116,720 -> 117,109; file 2,484,748,288 -> 2,487,861,248 bytes (+3.1 MB/40 s). Live API: heartbeat_age_seconds 0.047, open_position_count 302. FULLY HEALTHY.",
    "the_damage_I_almost_did": "I was about to restart a HEALTHY production process. That would have interrupted the per-position valuation loop for ~300 open positions, dropped observation leases mid-burst, and disturbed the frontier of a 30-minute-old experiment - for no reason. The most serious risk this round came from my own reading, not from the system.",
    "ALSO_CORRECTED_an_assumption_I_had_asserted": "I had assumed the clock columns were NAIVE UTC with no zone marker. Measured: 0 naive, 171 with 'Z'. That assumption was simply false and is withdrawn.",
    "corrected_wait_estimate_for_the_ladder": "The same clock error made round 67 compute the ladder as 8.4 hours old with a 0.48 cohort/h settle rate. MEASURED: the 12 cohorts opened inside a 165-second ENTRY BURST (23:42:56 to 23:45:40), the experiment is 29.6 minutes old, throughput 12/0.493h = 24.3 cohorts/h; the 8 occupied slots then stay full because ENTRIES ARE BURSTY fleet-wide (191 minutes with >=1 open: per-minute p50 7, p90 56, max 128). 'Slots full, no new cohorts' is ENTRY SCARCITY, not a stalled experiment.",
    "MEASURED_DIVERGENCE_POWER_replaces_round_58s_assumption": "CORRECTED IN ROUND 69 - the numbers below were computed with MAX(mark_history.price_usd) joined only on token_id with no pool and no closing bound, which overstates the engine's own high in 1,471 of 3,892 positions (worst x5.4). See the ROUND_69 key for the authoritative rebuild. Round 58 estimated 'about 3.4 divergences/h' from an ASSUMED +10% reach rate that was never measured. Round 68 measured a running-max CDF but on the WRONG basis, reporting P(>=1.10)=0.5795 and P(>=1.25)=0.3286. AUTHORITATIVE (engine highest_signal_price_usd / entry_execution_price_usd, n=3,892): P(>=+10%)=0.4108, P(>=+15%)=0.3430, P(>=+20%)=0.2819, P(>=+25%)=0.2117, P(>=+30%)=0.1791, P(>=+35%)=0.1457. Cohorts needed for 20 divergences: +10-vs-+25 **100**, +15-vs-+25 152, +10-vs-+20 155, +20-vs-+25 285, +10-vs-+15 295, +15-vs-+20 327. READ THE WIDEST BAND FIRST: +10-vs-+25.",
    "structural_fact_42pct_of_cohorts_carry_ZERO_information": "CORRECTED IN ROUND 69: the share is 58.9%, not 42.05%, because P(>=+10%) is 0.4108 not 0.5795. THE MECHANISM IS UNCHANGED AND STILL HOLDS: because hard_stop_return (-0.35), trailing_activate_return (0.30), trailing_drawdown (0.35) and max_hold_minutes (90) are IDENTICAL in all four ladder arms, a cohort that never rises to +10% follows the SAME exit path in every arm, so it is structurally uninformative about the tier. Not a defect (a same-frontier single-factor design must behave this way) but it means more time or more slots CANNOT convert those cohorts into information; the only inputs are the reach rate and what happens after the peak.",
    "the_0_of_12_is_unlucky_NOT_defective_and_the_peak_is_flush_with_the_tier": "All 12 cohorts are carried by all four arms in lockstep (12 all-open/all-settled groups, 0 mixed-status), and the best running max across them is 1.1027 against a first tier at 1.10 - three thousandths below the trigger, i.e. the first divergence is imminent. Under the corrected P(band)=0.1991 (round 69) twelve cohorts expect 2.4 divergences, so 0 has probability about 7% - unlucky at n=12, not a defect. The 4 already-settled cohorts exited via market_mark_hard_stop (x3) and dex_pool_liquidity_below_configured_floor (x1), both IDENTICAL across the four arms, so they were forced to 0 divergence.",
    "P0_STATE_MEASURED_THIS_ROUND": "exit_ladder150: 12 opened / 4 settled / 8 open per arm, 0 diverged in all six pairs (gate is now **100** cohorts for the widest pair +10-vs-+25, corrected in round 69 from 80). exit150_full15_v1 vs bank15: 6 diverged (17 shared, 11 identical), was 4 - still <20. exit150_full15_v1 vs full25: 17 diverged (32 shared, 15 identical) - still <20, 3 short.",
    "NOTHING_WAS_CHANGED": "No strategy, threshold, contract or runtime behaviour was modified. The service was NOT restarted. The observation budget was NOT touched. All of this round's output is diagnosis and characterisation.",
    "STANDING_RULES_new": "47 - Before declaring an outage, write the SAME instant in BOTH clocks and verify which one each source uses. This DB stores UTC with 'Z' while file mtime, Get-Date and paper-supervisor.log (+08:00) are local, 8 hours apart here; reading one as the other makes a sub-second-heartbeat system look frozen for 8 hours. A stall claim requires a before/after DOUBLE read (same connection, tens of seconds apart, comparing ROW COUNTS - not just max timestamps, since an unchanged maximum can simply mean no new events in the window). 48 - For a same-frontier single-factor experiment, compute the DIVERGENCE BANDWIDTH before waiting on a readout: arms separate only when the cohort's running max falls between the two varied levels, so narrow-band pairs need far more cohorts. Order readouts by bandwidth, not by level. (GATE CORRECTED IN ROUND 69 TO 100 COHORTS for the widest take-profit pair.)"
  },
  "ROUND_67_FLEET_WIDE_PER_ARM_VERDICTS_the_cap_is_1.27pct_and_the_dominant_constraint_is_51pct": {
    "why": "Round 66 found ONE arm refused for `strategy_open_slot_limit` 1,609 times in 30 minutes. If that generalised, the uniform per-arm cap of 8 - a USER-AUTHORISED setting - would be a first-order constraint on position count, which is the user's core complaint. Its cost therefore had to be quantified rather than changed. It also put ROUND 28's claim under a spotlight: it concluded 'concurrency is NOT binding' from 'the OBSERVED MAX is 8', but the cap of 8 was authorised the SAME DAY, so an observed maximum of exactly 8 is the cap BINDING, not evidence of spare capacity.",
    "FLEET_WIDE_PER_ARM_VERDICT_DISTRIBUTION_4.9_MILLION_ENTRIES": "From 26,110 cohort evaluations. await_distinct_dex_trajectory_frame 2,500,621 (**50.98%**); wait_passive_cohort_opportunity 1,623,923 (33.11%); strategy_token_lifetime_entry_consumed 467,571 (9.53%); cohort_event_consumed_or_position_open 160,377 (3.27%); await_distinct_wide_frame 47,604 (0.97%); **strategy_open_slot_limit 40,017 (0.82%)**; pattern_already_enrolled_at_this_pool 23,486 (0.48%); cohort_frozen_opportunity_ready 18,598 (0.38%, these are ADMISSIONS); strategy_open_or_reserved_limit 13,620 (0.28%); strategy_token_open_or_reserved 8,105 (0.17%); runup_floor_exceeded 560; activity_floor_trades_not_met 466; activity_floor_volume_not_met 408. Concurrency-flavoured reasons total about 1.27%.",
    "THE_CAP_BINDS_ONLY_13_ARMS_AND_THEY_ARE_ALL_EXIT_CARRIERS": "exit150_widestop_v1 13,978 (66.3% of that arm's verdicts); exit150_bank15_v1 and bank25_v1 5,060 each (24.0%); alpha149_merged_multi_setup_v1 2,487 (9.5%); alpha149_survive_noise_wide_v1 2,329 (8.9%); exit150_full25_v1 1,767 (11.5%); the four exit_ladder150 arms 1,745 each (**76.2%**); exit150_full15_v1 1,155 (7.5%); alpha149_vol_scaled_merged_v1 1,047 (4.0%); alpha149_hold_flow_v1 154 (0.6%). Over the last 30 minutes the number of arms simultaneously at the cap was min 0, median 5, max 6 out of 182 arms with positions - at most 3.3%. MECHANISM: the exit-carrier arms CLONE THE SAME FROZEN SIGNAL, so they line up on the same cohorts and fill together, whereas the ~300 ordinary entry arms almost never reach the cap. This is also why round 66's experiment was cap-throttled: it belongs to the by-design-saturating class.",
    "ROUND_28_RIGHT_CONCLUSION_INSUFFICIENT_EVIDENCE": "The CONCLUSION (concurrency is not a fleet-wide constraint) SURVIVES - this round proves it at 1.27% over 4.9M verdicts. But the EVIDENCE it used ('the observed max is 8') does not support it, because 8 was the authorised cap: observing the ceiling is not evidence that no ceiling binds. The correct evidence is the distribution above (13 arms ever, at most 3.3% simultaneously). AND a qualification round 28 lacked: the cap IS a real constraint for the EXIT-CARRIER class (widestop has 66.3% of its verdicts blocked by it); it just does not move the fleet-level conclusion because that class is a small share of the fleet.",
    "THE_COMPLETE_MECHANISM_CHAIN_FOR_LOW_TRADE_VOLUME": "The user sees low position counts -> 51% of all per-arm verdicts are stuck on `await_distinct_dex_trajectory_frame` -> because three DISTINCT frames need 60-90 seconds of continuous observation (round 65) -> because the provider yields new information only about every 30.1 seconds, with just 28.8% of polls seeing a change -> so the only amplifier is how many leases can be held at once, i.e. the observation slot budget (a user decision, marginal product about 4.7 dense windows per slot per hour). The concurrency cap is explicitly EXCLUDED: 1.27% of verdicts, 13 arms, all of them exit carriers that saturate by design.",
    "STANDING_RULE_new": "46 - When an observed maximum happens to EQUAL a configured limit, it cannot be used as evidence that no limit binds. Round 28 inferred 'concurrency is not a constraint' from 'the observed max is 8' while 8 WAS the cap authorised that same day. To show a limit does not bind you must give the distribution - how many objects, and for how long, sit at it - not a single maximum. The conclusion happened to be right, which makes it luck rather than evidence."
  },
  "ROUND_66_MY_OWN_EXPERIMENT_IS_CAP_BLOCKED_NOT_SIGNAL_STARVED": {
    "the_symptom": "The P0 experiment exit_ladder150 (four same-frontier arms differing only in the first tier) sat frozen at 48 positions, 12 per arm, from 0.13h to 0.33h after its frontier. If it stops drawing new cohorts the dose-response curve can never reach the >=20-diverged gate, so the REASON mattered more than the tally.",
    "MY_OWN_PREVIOUS_MEASUREMENT_WAS_WRONG": "r66 swept concurrency from min(opened_at) to max(opened_at), which for these arms is a 3-MINUTE entry burst (23:42:56 to 23:45:40), so it never looked at the holding period and reported 'peak 5, cap 8, no cap pressure'. r66b then found exit_ladder150_t10_v1 refused for `strategy_open_slot_limit` 1,609 times out of 2,723 in the last 30 minutes - i.e. it IS at its cap. Both cannot be true, and the cause was the sampling interval. Same class as rounds 64/65.",
    "THE_REAL_MECHANISM_cap_blocked_not_signal_starved": "Re-swept over the CORRECT interval (frontier -> now): all four arms peak at 8 and sit AT the cap for 20 of 23 minutes (87%), with 8 open each right now. Refusal reasons for t10 over the last 30 minutes: strategy_open_slot_limit 1,609 (59%), cohort_event_consumed_or_position_open 341, await_distinct_dex_trajectory_frame 146, cohort_frozen_opportunity_ready 35 (admissions), strategy_token_lifetime_entry_consumed 16. SO THE EXPERIMENT ADVANCES AT THE RATE POSITIONS FREE A SLOT, NOT AT THE RATE SIGNALS FIRE.",
    "FOUR_WAY_SYMMETRY_IS_INTACT": "Despite the cap pressure the pairing structure is undamaged: 12 distinct cohorts touched, ALL 12 carried by all four arms (100%), 12 positions per arm exactly, and no difference between the arms' cohort sets. That is the necessary condition - if one arm filled first while another had room, that cohort would become a three-way unit and lose information about the tier.",
    "resolution_is_BIMODAL_and_that_paces_everything": "Resolved positions: 16, hold p25 0.7 min, p50 1.7 min, p75 1.8 min - very short. But only 4 of each arm's 12 cohorts have settled while 8 remain open past 20 minutes. So a cohort either resolves fast (a tier touch or pool death, ~2 min) or runs to the 90-minute max_hold before its slot frees. Cohort completion under saturation is therefore ~8 slots x (60/90) = ~5.3/hour, governed by the slow branch. Divergence additionally needs the four arms to resolve DIFFERENTLY: a pure pool death can write off identically across all four (round 57 saw 8 of 22 shared cohorts resolve identically), so only cohorts touching >=+10% diverge, and round 53 measured the +10% reach at about 65%. RE-PROJECTION: ~5.3 completions/hour x 65% ~ 3.4 divergences/hour, so 12 -> 20 needs roughly 2.5 hours, keeping the range at about 3-7 hours - unchanged from round 57's measured 2.9-7.7h, but now with the mechanism.",
    "NOTHING_TO_FIX_and_why_I_did_not": "The cap of 8 is a USER INSTRUCTION (CHAIN_MEME_TRADER_CONCURRENCY_CAP_FLOOR, basis 'user instruction: every strategy gets a cap of 8') applied UNIFORMLY to all 457 policies - and that uniformity is exactly what makes the comparison fair (round 62). Raising it for these four arms alone would break the single-factor property and would be changing risk control for experimental convenience. REUSABLE LESSON: size future experiments with the cap in mind - cap 8 plus bimodal holding means a four-arm dose-response advances about 5 cohorts per hour.",
    "STANDING_RULES_new": "44 - When your OWN experiment stalls, read its REFUSAL REASONS first; do not infer from counts. Here the single reason `strategy_open_slot_limit` at 59% identified cap-throttling immediately, where the frozen position count suggested (wrongly) a signal drought. 45 - A concurrency sweep's interval must cover the HOLDING period, not the ENTRY period. r66 swept [min(opened), max(opened)], which for these arms was 3 minutes, and reported 'no cap pressure' while sitting at the cap. Let the phenomenon being measured define the interval, not the two endpoints that happen to be in the data."
  },
  "ROUND_65_THE_PROVIDER_UPDATES_EVERY_30s_AND_THE_LEASE_LENGTH_LEVER_IS_FLAT": {
    "the_question": "Round 64 corrected the latency story to 'the first frame is fast (p50 9.08s), so the bottleneck is the DENSE frames a decision needs', and round 29 measured that 77.6% of observation episodes are a single frame. Round 47's window accounting pointed somewhere that is NOT the slot budget: in 54.3% of 30s windows the provider returned NO new numbers at all (SOURCE_NO_UPDATE). If the upstream cadence is slower than the observation cadence then no amount of extra slots yields three DISTINCT frames inside a 120s lease, and the fix would be lease DURATION - a configuration value - rather than budget.",
    "MEASURED_provider_information_cadence": "From already-collected observer frames: 6,627 (token, pool) series, 46,502 frames, 32,517 consecutive intervals. Only 9,369 (28.8%) of those intervals carried a CHANGE in (price, volume, liquidity). ALL intervals p10 2.48s p25 3.98s p50 14.99s p75 30.87s p90 88.25s; CHANGED intervals p10 4.81s p25 14.93s **p50 30.11s** p75 84.32s p90 124.32s. SO NEW INFORMATION ARRIVES EVERY ~30 SECONDS AND ONLY 28.8% OF POLLS SEE A CHANGE, which means three DISTINCT frames need roughly 60-90 seconds of continuous observation. That is the mechanistic answer to 'why do only 4.4% of tokens get a >=3-frame episode', and it also explains why `await_distinct_dex_trajectory_frame` is the LARGEST refusal reason for the floor arms (4,135 for t30): the condition is not an over-strict gate, it is intrinsically hard to satisfy.",
    "THE_LEASE_LEVER_IS_FLAT_measured_on_the_product_not_the_yield": "Simulated on the real series (1,422 with >=3 frames) under the adaptive cadence: yield of >=3 DISTINCT frames is 15.6% at the current EARLY_LEASE_SECONDS=120, 27.4% at 180s, 43.9% at the current PHASE_LEASE_SECONDS=300 and 47.6% at 600s - which LOOKS like a 2.8x win at 300s. It is not, because holding a slot 300s instead of 120s serves 2.5x fewer tokens per hour. THE PRODUCT (windows/hour = slots * 3600/lease * P(>=3 distinct)): 60s->99, 90s->153, **120s->141**, 150s->130, 180s->**165**, 240s->**167**, 300s->158, 420s->119, 600s->86, 900s->59. So the product is essentially FLAT from 90s to 300s, peaking around 180-240s at +17-19% over the current value, and going 120s -> 300s is worth only +12%. ROUND 29's 'slot turnover is closed as a lever' SURVIVES, now with a better mechanism: yield and throughput offset almost exactly.",
    "a_quantified_marginal_value_for_the_budget_decision": "At 30 candidate slots the system forms about **141 dense windows/hour, i.e. ~4.7 windows per slot per hour**. Since more slots means more concurrent leases, dense-window output should scale approximately LINEARLY, so roughly +30 slots ~ +141 dense windows/hour. This is the first marginal-product number the session has produced for the observation budget and it is directly usable in that decision. TWO UPPER BOUNDS, both making the figure conservative: the simulation assumes the observer always polls at its desired cadence with no contention, and it lets the observer use the whole lease even though `replaceable_early` can cut it short (series average 28.2 frames available, median 6, and the simulation uses them).",
    "relationship_to_other_conclusions": "Round 29 (slot turnover) SURVIVES with a clearer mechanism. Rounds 47/48 (slots saturated, 67% bucket-full) are unaffected - that is the ADMISSION side. Round 64 (first frame p50 9.08s, the tail is the queue) is consistent: the first frame is fast, the DENSE frames are hard. No configuration or behaviour was changed this round.",
    "STANDING_RULE_new": "42 - When evaluating a 'make it longer/deeper' parameter, measure THROUGHPUT (output per unit time), never success rate per attempt. Lengthening the lease from 120s to 300s raises the success rate from 15.6% to 43.9% (2.8x) while raising output per hour only from 141 to 158 (+12%), because the same capacity then serves fewer objects. Judging by success rate would have been wrong by an order of magnitude.",
    "CADENCE_IS_NOT_A_LEVER_EITHER": "Round 22's adaptive cadence backs off 15 -> 30 -> 60s when a frame is uninformative, to save ~3.4x wasted requests. Since the provider's informative interval is 30.11s, backing off to 60s could sample MORE SLOWLY THAN THE PROVIDER UPDATES and lengthen the time to three distinct frames. Tested: at the 120s lease the current policy forms 140 windows/h on 2,753 polls/h; capping the back-off at 30s gives 142 (+1%) on 2,808; capping at 20s gives 144 (+3%) on 2,972; polling always at 15s gives 145 (+3.4%) on 2,998 (+8.9% polls); 10/20/30 gives 144; 20/30 gives 141; and always 30s is clearly WORSE at 110 (-21%), which shows the 15s fast path has value. At the 300s lease the policies are indistinguishable (158 vs 158). SO THE BACK-OFF COSTS NO MEASURABLE WINDOW YIELD and faster polling cannot create information: only 28.8% of polls see a change, so WINDOW OUTPUT IS PROVIDER-GATED, NOT CADENCE-GATED. No change recommended.",
    "COVERAGE_INVESTIGATION_CLOSED_all_zero_budget_levers_measured": "Every candidate lever has now been measured and refused except the budget itself: more slots is the only effective amplifier (~4.7 windows per slot per hour, approximately linear, a USER DECISION); longer lease is flat (round 65b); faster cadence is +3.4% for +8.9% polls (round 65c); a better allocation rule is already at the feature frontier (round 50); higher request frequency follows from the cadence result. And the constraint now has a complete mechanism chain: THE PROVIDER GIVES NEW INFORMATION ABOUT EVERY 30 SECONDS -> three DISTINCT frames need 60-90s of continuous observation -> only 15.6%-43.9% of series manage that inside a 120-300s lease -> and lease yield and throughput offset almost exactly, so the product is flat -> THE ONLY AMPLIFIER IS HOW MANY LEASES CAN BE HELD AT ONCE, i.e. the slot budget.",
    "STANDING_RULE_new_43": "43 - Chase the mechanism to the layer that cannot be decomposed further. This round walked from '4.4% coverage' down to 'the provider updates every ~30 seconds', measuring and eliminating every intermediate layer (slot count, lease duration, cadence, allocation rule) on the way, and the only irreducible quantities left are the provider's update cadence and the number of concurrent leases. Until the intermediate layers are eliminated one by one, ANY of them can be mistaken for the lever."
  },
  "ROUND_64_THE_97s_DISCOVERY_DELAY_WAS_SELECTED_ON_THE_OUTCOME": {
    "the_number_I_have_cited_since_round_44": "Round 44 reported 'discovery -> first observation p50 96.94s while everything downstream is <=1.3s, so the delay is queueing for an observation slot', and I have repeated it in the consolidated report and the checkpoint. This round set out to CORRECT it for a clock mismatch (`observed_at` is the PROVIDER's timestamp - runtime.py:8687 copies the quote's observed_at/ingested_at onto the observation - while tokens.first_seen_at is LOCAL) and ended up finding that BOTH my new figure and round 44's original were wrong.",
    "the_hypotheses_that_failed_first": "(a) a silent drop at the 30s ingestion ceiling: REFUTED - 0 of 73,454 frames exceed 30.0s and only 7 (0.010%) exceed the observer's 30s guard, so there is nothing to fix there. (b) the clock mismatch itself: REFUTED as the cause - the two clocks agree to -0.05s at p50. (c) population composition: partly real (hourly p50 spans 0.42-42.39s and the cumulative p90 falls from 7,026s to 330s as the epoch progresses). (d) right-censoring at round 44's measurement time: this initially appeared to CONFIRM round 44 - recomputing its construction gave n=8,991, p50 96.87s, p90 1,767.94s against its n=8,994, 96.94s, 1,767.94s. (e) NULL observed_at rows: REFUTED - 0 of 73,454.",
    "THE_REAL_CAUSE_conditioning_on_the_outcome": "The `delay >= 0` filter is the culprit. Measured from the SAME ROW (the one with the earliest observed_at per token): delay-from-observed n=10,404 p50 95.262s; delay-from-recorded n=15,427 p50 9.084s; within-frame lag p50 0.064s. And 5,023 tokens (30.7%) have an earliest observation that PREDATES their first_seen_at, while 0 of them have a recorded_at that does. So with `observed_at` those 30.7% produce NEGATIVE delays and are dropped by `d >= 0`, and the survivors are exactly the tokens whose first observation came LATE - the median is selected on the very quantity being measured. A negative delay is not bad data: it means the provider's data timestamp is earlier than the moment we locally discovered the token, which is structural.",
    "CORRECTED_FIGURES": "'How long after learning of a token does the system have it under observation' must use two LOCAL clocks: tokens.first_seen_at -> min(recorded_at) gives **p50 9.08s, p90 329.20s, p99 8,330.16s, max 14,596s**. Round 44's p50 96.94s is that quantity after a filter that keeps only late-observed tokens. THE EMPHASIS CHANGES: the MEDIAN is fast (9s) and is NOT queue-limited; the problem is the TAIL - 42.5% exceed 30s, 31.0% exceed 120s, 26.6% exceed 300s and 4.3% exceed one hour. Round 44's DIRECTION (the delay sits before the first snapshot) holds for the tail but was wrong to state via the median.",
    "round_44s_construction_is_reproducible": "Recorded for fairness: on the observed_at clock, restricted to snapshots existing at round 44's measurement time, the construction reproduces n=8,991 p50 96.87s p90 1,767.94s against round 44's n=8,994 p50 96.94s p90 1,767.94s - a 0.1% match. So round 44's ARITHMETIC was right; what was wrong was treating a censored, outcome-selected sample as the population figure.",
    "a_clean_latency_result_from_the_same_pass": "Within-frame observed_at -> recorded_at (matched rows, n=73,474): p50 0.826s, p90 7.811s, p99 25.574s, 0% negative. And recorded_at -> decision: p50 0.000s, p90 0.672s, p99 3.903s. SO ONCE A FRAME IS STORED THE DECISION IS IMMEDIATE (p50 0 ms, p99 3.9s), and essentially all of the 1.2s in observed_at -> decision is collector-side time at the provider boundary, not pipeline processing.",
    "STANDING_RULES_new": "40 - Never filter a delay sample with `delay >= 0` without reporting how many were excluded and why they were negative. Two clocks have different origins, negativity is usually the real structure 'the upstream timestamp precedes our local origin', and dropping those rows selects on the quantity being measured - here it moved the median from 9s to 95s. Prefer same-origin clocks on both ends, and when crossing clocks, report the negative share alongside the median. 41 - When one quantity has two figures an order of magnitude apart, RUN BOTH CONSTRUCTIONS AND PRINT THEM SIDE BY SIDE rather than choosing. That is what exposed this: the n moved from 10,404 to 15,427, so the difference was the POPULATION, not the timestamp."
  },
  "ROUND_63_ROUND_57_PREMISE_RE_VERIFIED_AGAINST_THE_EFFECTIVE_DEFINITION": {
    "why_this_was_necessary": "Round 62 found I had three times treated the REGISTERED policy as the EFFECTIVE one. Round 57b's conclusion - that full15 and full25 differ in TWO behavioural fields, take_profit AND trailing_activate_return (0.30 vs 0.35) - was read from the registered JSON, and that conclusion is the ENTIRE reason exit_ladder150 exists (its four arms pin the trailing activation at 0.30 so only the tier varies). If the effective definition had normalised the trailing activation too, the deployed pair would have been single-factor all along and the shipped module would rest on a draft-value artifact.",
    "RESULT_ROUND_57b_STANDS": "Effective values, field by field: take_profit DIFFERS (0.15 vs 0.25) and trailing_activate_return DIFFERS (0.30 vs 0.35), while trailing_drawdown (0.35), hard_stop_return (-0.35), max_hold_minutes (90), notional_usd (20.0 both, normalised), the effective cap (8 both, normalised), trajectory_exit (None) and entry_match_mode (isolated_cohort_observer) are all IDENTICAL. Behavioural diff after removing identity keys: ['take_profit', 'trailing_activate_return']. So the confound is REAL IN THE EFFECTIVE CONTRACT and exit_ladder150's founding premise holds.",
    "MY_SHIPPED_MODULE_CONFIRMED_SINGLE_FACTOR_IN_THE_EFFECTIVE_DEFINITION": "The four ladder arms' effective behavioural diff is ['take_profit'] and nothing else; trailing_activate_return = {0.3} across all four, notional_usd = {20.0}, effective cap = {8}, tiers 0.10/0.15/0.20/0.25. Round 58b verified this from the registered JSON and this round from the effective definition - BOTH sources now agree.",
    "the_confound_is_systematic_to_the_25pct_variants": "bank15 vs bank25 also differ in take_profit AND trailing_activate_return, so BOTH level contrasts in the fleet are non-single-factor - a deliberate design choice rather than a typo, and precisely why the new module is needed. The two floor pairs are single-factor: t30 vs v5k and r15 vs r15a30 show NO behavioural difference beyond identity (their entry_filter difference is the floor declaration, i.e. the treatment itself).",
    "my_checker_tripped_on_an_identity_key_for_the_third_time": "The first run reported the four ladder arms as NOT single-factor with differing fields ['entry_filter', 'runtime_addition_id', 'take_profit'] - `runtime_addition_id` is registration identity, not behaviour, and it was missing from the probe's identity set. Adding it yields ['take_profit']. This is the same error as round 58b's missing `direction`, and I had written that lesson into the fixed code as a comment. ALSO recorded honestly: the probe's section E prints 'identical after stripping direction: False' for the two floor pairs, which is NOT a defect - their entry_filter difference IS the floor declaration - but the line's label assumes the exit-pair case and is imprecise.",
    "STANDING_RULES_new": "39 - Verify a PREMISE against a source of the SAME OR HIGHER authority than the one it came from. Round 57b concluded from the registered JSON and round 58b then verified it against the SAME registered JSON - two readings of one source is not a verification. Only after round 62 exposed the source problem did this round re-check against the EFFECTIVE definition, which is what made the confirmation independent. 32 (STRENGTHENED) - a single-factor checker's identity key set must be enumerated EXHAUSTIVELY each time, not extended as keys are noticed: missing `direction` and then `runtime_addition_id` each produced a false alarm on correct code."
  },
  "ROUND_62_READ_THE_EFFECTIVE_DEFINITION_NOT_THE_REGISTERED_JSON": {
    "THREE_WRONG_CONCLUSIONS_FROM_ONE_MISTAKE_IN_TWO_ROUNDS": "(1) r62: I saw exit150_widestop_v1 peak at 9 concurrent and the exit arms at 8, against a REGISTERED cap of 2 (entry_filter) / 4 (top level), and concluded 'the per-arm concurrency cap is NOT enforced' - from observed peaks, WITHOUT reading the enforcement path. (2) r62b: after reading CHAIN_MEME_TRADER_CONCURRENCY_CAP_FLOOR = 8 (store.py:301) and the enforcement site (store.py:27799), I compared peaks against the 'effective' cap taken from policy_additions.policy_json - which holds the REGISTERED value - and reported '89 arms exceed their effective cap'. Wrong again, in the same place. (3) Round 61, found retrospectively: I declared per-arm notional_usd a DEAD FIELD never read by the runtime. Error 2 is what forced me to read the effective definition, and that immediately showed the field IS read - by a uniform-notional revision.",
    "THE_CORRECT_SOURCE_OF_TRUTH": "Store._chain_meme_trader_effective_definition(version, definition_json) rebuilds the definition at load and applies RECORDED, AUTHORISED revisions. For this epoch it carries two: `concurrency_cap_floor` {floor 8, raised_policies 240, filled_uncapped_policies 78, basis 'user instruction: every strategy gets a cap of 8'} and `uniform_notional_usd` {notional_usd 20.0, changed_policies 280, basis 'user instruction: one uniform 20U per trade so strategies can be compared fairly'}. EFFECTIVE distributions over 457 policies: max_concurrent_positions = {8: 457} (445 arms carry a concurrency_cap_revision); notional_usd = {20.0: 311, None: 146} with None resolving to definition default policy_notional_usd = 20.0. Per arm: exit150_full15_v1 1.0->20.0 and 2->8; exit150_widestop_v1 1.0->20.0 and 2->8; exit_ladder150_t10_v1 1.0->20.0 and 2->8; activity_floor150_t30_v1 2.0->20.0 and 2->8; organic_early_flow_v1 2.0->20.0 and 2->8.",
    "CORRECTED_CONCLUSIONS": "(a) The concurrency cap IS enforced, uniformly at an effective 8: enforcement at store.py:27799 on `occupied[arm] = COUNT(*) of that arm's status='open' positions`, and it demonstrably FIRED - 13,679 evaluations carry the reason string 'strategy_open_slot_limit' in feature_json. Peaks vs the EFFECTIVE cap across 182 comparable arms: 12 sit EXACTLY at 8 (the cap binding), 169 below, and only ONE exceeds it (exit150_widestop_v1 peak 9, +1), consistent with the check running per observation so several in-flight observations each see room before any commits. (b) 20U per trade is a deliberate uniform normalisation, not a dead field. So the correct per-arm risk picture is: 8 x 20U = 160U maximum concurrent exposure per arm, i.e. 16% of its 1,000U starting cash - a coherent, intentional, authorised design rather than a defect. (c) The comments I added to exits150.py and exit_ladder150.py in round 61 asserted the wrong mechanism and were REWRITTEN. (d) UNCHANGED: round 61's self-audit result stands - every comparison I reported is same-notional (20U) and needs no correction.",
    "STANDING_RULES_new": "37 - `policy_additions.policy_json` is a DRAFT; the authority is the effective definition rebuilt at load. Before reasoning about ANY limit, size, threshold or contract field, call Store._chain_meme_trader_effective_definition(...) and read the effective value. This session read the draft and got it wrong three times - r61's notional, r62's 'not enforced', r62b's '89 arms over cap' - TWO OF THEM AFTER I had already written the rule 'verify before reasoning'. 38 - The conclusion 'this limit is not enforced' can only come from the enforcement PATH's code, never from an observed peak. A high peak is equally consistent with 'no limit' and with 'a higher limit': this round's peaks of 9 and 8 were caused by the effective cap being 8 rather than the registered 2. Read the enforcement site, then the effective value, and only THEN compare peaks."
  },
  "ROUND_61_checked_whether_I_compare_20x_different_position_sizes": {
    "the_self_audit": "exits150.py declares notional_usd=1.0 while the epoch's policy_notional_usd is 20.0. If arms traded 20x different sizes, 'U/pos' would not be comparable across them - and round 57's contrasts plus the checkpoint's live tally put exit-arm and floor-arm U/pos side by side. Same class as round 52's formula defect and round 27's `closed`-means error, so it was checked before being trusted.",
    "VERDICT_NO_reporting_defect": "Every position in the epoch books stake 20.0U - 3,389 positions over 180 arms - so all reported comparisons ARE same-notional: round 57's full15/full25, bank15/bank25, t30/v5k and r15/r15a30, and the checkpoint's exit+floor tally. A cheap falsification: had it gone the other way, four round-57 contrasts and the tally table would all have needed redoing.",
    "BUT_per_arm_notional_usd_IS_A_DEAD_FIELD": "Declared vs booked: exit150_full15_v1 1.0 -> [20.0]; exit_ladder150_t15_v1 1.0 -> [20.0]; organic_early_flow_v1 2.0 -> [20.0]; organic_short_observed_flow_v1 5.0 -> [20.0]. ALL IGNORED. The stake is the GLOBAL config.json `paper.max_position_usd = 20`, passed as `paper_stake_usd` on the shared entry paths (runtime.py:1292/1304/1323/1331 -> store.py:20606/20930); NO path reads a policy's `notional_usd`. So the per-arm sizes declared in exits150.py, exit_ladder150.py, cohort_experiments.py (2/5), market_microstructure.py (1/2/5) and mode_learning144.py (2) are all decorative. The ONE exception is the native protocol lane: pump_native_absorption_fast_v1 books ~4.7085-4.7096U over 17 positions, each once, derived from its own native model rather than from config.",
    "impact_bounded": "UNAFFECTED: rounds 40/43's stack (it used each position's real stake_usd, so those U figures are right); round 28's '4,400U deployed' (computed from real positions: 220 x 20); round 57's four contrasts and the tally (all 20U); the ladder dose-response's single-factor property (all four arms at 20U); any cash-flow/PnL reconciliation. AFFECTED: the RISK READING - exits150.py's '1U x 4 slots' wording (5 descriptions + docstring + snapshot()) and exit_ladder150.py's description imply 4U per arm where each position actually risks 20U, a 20x understatement; and a field that LOOKS like a live risk limit is empty.",
    "action_comments_only": "A comment was added at each module's _COMMON stating the measured evidence, the real source (global paper.max_position_usd) and that the descriptions' '1U' wording must be disregarded. The registered description TEXT was deliberately NOT edited: it is contract text for already-registered arms, and the round-57 precedent (leaving exit150_full25_v1's inaccurate description alone) applies - the benefit of better wording is smaller than the risk of contract-identity churn. Comments are in no contract and have zero behavioural effect. Verified inert: git diff --stat is 20 insertions / 0 deletions, 26 tests pass, and OVERRIDES' notional_usd and all four tier values are unchanged.",
    "scale_free_arm_ranking_as_a_byproduct": "With sizes uniform, U/pos and %-of-stake differ by a constant, and the percentage view ranks the arms: runup_floor150_r15a30_v1 -8.20%, runup_floor150_r15_v1 -10.71%, exit150_full15_v1 -10.75%, the four ladder arms -17.32%, exit150_bank25_v1 -34.63%, control alpha149_merged_multi_setup_fast_v1 -38.09%, carrier control alpha149_vol_scaled_exit_v1 -42.79%. The three best are exactly this session's strongest-evidence mechanisms and the two worst are the controls - BUT these are UNPAIRED per-side means over different cohort sets (the controls have run longer on more cohorts), so this is directionally consistent context and NOT a verdict. Rounds 34/35's rules are unchanged.",
    "STANDING_RULE_new": "36 - A configuration field that is declared but never read by the runtime is MORE dangerous than a missing one, because it reads like a live risk limit. Before reasoning about risk from any declared limit, verify it is actually enforced - by comparing the declared value against what is actually booked (here: declared 1.0/2.0/5.0, booked 20.0 on every position).",
    "CORRECTED_IN_ROUND_62": "The sub-claim 'BUT_per_arm_notional_usd_IS_A_DEAD_FIELD' is WRONG and is withdrawn. The field is NOT unread: the EFFECTIVE definition rewrites it. `definition['uniform_notional_usd']` records a user instruction 'one uniform 20U per trade so strategies can be compared fairly' and changed 280 policies to 20.0, and each arm carries `notional_revision {registered: 1.0 (or 2.0), effective: 20.0}`. So 20U per trade is a DELIBERATE, authorised uniform normalisation, not a dead field. Likewise the REGISTERED max_concurrent_positions of 2 is raised to an effective 8 by `definition['concurrency_cap_floor']` (CHAIN_MEME_TRADER_CONCURRENCY_CAP_FLOOR = 8, store.py:301), which raised 240 policies and filled 78 that had no cap. THE UNCHANGED PART: the round-61 self-audit conclusion stands - every comparison I reported IS same-notional (20U) and needs no correction. The stale part remains that the descriptions' '1U x 4 slots' wording does not match the effective contract. The comments I added to exits150.py and exit_ladder150.py in round 61 asserted the wrong mechanism and were REWRITTEN in round 62."
  },
  "ROUND_59_RESTART_VERIFIED_CLEAN_and_my_sixth_sampling_artifact": {
    "restart_verification": "Round 58's controlled restart verified beyond 'it came back': all five live streams fresh (snapshots 0s, evaluations 0s, marks 1s, opened 1s, closed 1s); 5-minute throughput 1,232 snapshots / 1,234 evaluations / 418 marks / 167 opened / 124 closed; per-generation telemetry rebuilt (funnel counting again, skip_bucket_full already 4); candidate slots 29 of 30 immediately re-saturated; runtime-crash.log unchanged at 22,904 bytes.",
    "NO_duplicate_entries": "The decisive hygiene check: (arm, cohort) pairs with more than one position = 0, and arms-per-cohort was 5.55 before the restart against 5.52 after. If losing in-memory state had let arms re-enter consumed opportunities, arms/cohort would have RISEN. It did not, so the restart did not corrupt the forward record.",
    "MY_SIXTH_SAMPLING_ARTIFACT": "I measured 167 positions opened in the 5 minutes after the restart against 13 per 10 minutes before it - about 13x - and was about to report a post-restart entry burst. The PER-MINUTE histogram refuted it: entries are highly spiky and the pre-restart window already contained 71/minute and 61/minute, while the post-restart minutes (18, 17, 82) sit entirely inside that distribution. The mean comparison (14.4/min over 64 minutes vs 37.2/min over FOUR) was a window-length artifact that happened to land on a spike. Decomposition confirms it: of the 149 post-restart positions, 109 came from the 68 pre-existing arms and only 40 from the four new ones (10 each, perfectly symmetric).",
    "two_useful_measurements_from_the_same_pass": "(a) The new arms' accumulation ceiling is DISTINCT TOKENS, not cohorts: `single_token_lifetime_entry: true` gives each arm at most one entry per token, and the measurement shows exactly 12 positions / 12 tokens / 12 cohorts with max/token = 1. (b) Exit carriers capture only part of the fleet's cohorts: the fleet touched 32 cohorts since the ladder frontier and each ladder arm captured 12 (38%), because a carrier clones only the first frozen signal that fires on a pool.",
    "P0_readability_projection": "The one same-frontier pair with history, full15 vs full25, is now at 27 shared settled / **16 DIVERGED** (round 57 measured 14), accumulating at **6.8 diverged cohorts/hour** (first divergence 21:22:54 to last 23:44:40, span 2.36h). Projecting the ladder arms: about 2.9 hours to the >=20-diverged gate. STATED RANGE: the ladder arms capture only 38% of fleet cohorts, so a capture-scaled rate of 2.6/hour implies about 7.7 hours; and the early rate may be below the mature one because divergence also requires a position to reach a tier or die. THE ORDER OF MAGNITUDE IS HOURS, NOT DAYS - that is the operational value of round 57's same-frontier finding.",
    "STANDING_RULE_new": "34 - For a spiky process, do not compare rates over short windows; compare DISTRIBUTIONS. This round's mean comparison differed 2.6x while the underlying per-minute distributions overlapped completely."
  },
  "ROUND_60_gate_readout_SHIPPED": {
    "what": "scripts/gate_readout.py answers, per rejection REASON, whether the refusal turned out to be right - by looking FORWARD 60 minutes on each candidate's ALREADY-COLLECTED frames at the +15% economic level the deployed take-profit targets. It adds NO acquisition requests. It exists because the evaluation log records why the system refused 60,000+ times and is never revisited, so this question had to be re-answered from scratch with a fresh ad-hoc query every time.",
    "three_design_choices_that_matter_more_than_the_arithmetic": "(1) OBSERVER BOOKKEEPING IS NOT A DECISION - cohort_observation and pattern_observation record that a frame entered an observation sequence, not a strategy refusal; they are classed separately and excluded from the verdict, because lumping them in makes ~61% of the log read as strategy refusals. (2) CENSORING IS PART OF THE TABLE - a rejected candidate only has an outcome if the observation policy kept watching its token, so 'no later frame' is UNKNOWN and never 'did not move', and a reason whose censoring is too high is reported UNDETERMINABLE rather than clean. (3) THE KERNEL MUST NOT BE RE-DERIVED - the script inlines 0.96*R - 1 with the round-52 evidence (the engine's own recorded values match it exactly 1,265/1,300 while the intuitive form matches 0/1,300).",
    "its_current_output": "31,490 unique (token, minute) evaluations, 17,474 with an outcome (55.5%), base +15% touch rate 0.1748. Largest decision gate no_active_matching_entry_policy n=13,844 cens 40.2% touch 0.1632 CI [0.1505,0.1782] - covering baseline. entry_pool_liquidity_below_configured_floor n=1,645 cens 68.3% touch 0.1056, CI top BELOW baseline, i.e. filtering correctly. entry_pool_liquidity_absent_curve_stage 96% censored -> UNDETERMINABLE. VERDICT PRINTED BY THE TOOL: no decision reason refuses a population that touches +15% more often than baseline, so no threshold, logic or risk rule is systematically discarding movers (6 decision reasons examined).",
    "tests": "tests/test_gate_readout.py, 4 tests. The decisive one guards the round-52 defect directly: it pulls pre_trigger.economic_return from the live marks - the value the ENGINE computed - and requires the corrected kernel's exact-match rate to exceed 90%. Another asserts the corrected and previously-used kernels stay measurably different, and its comment says that if they ever stop differing the test protects nothing and should be DELETED rather than left to mislead a maintainer into restoring a wrong formula.",
    "STANDING_RULE_new": "35 - When adding a test that guards one specific regression, state in the test WHEN IT SHOULD BE DELETED. Otherwise a later legitimate change makes it fail in a way that points maintainers back toward the defect."
  },
  "ROUND_43_THE_HEADLINE_NEEDS_RECLAIMING_THE_TP_ADDS_EXACTLY_NOTHING": {
    "what": "Round 40's stack projection (+17,369U), the epoch's headline, used BOTH anchors round 41 proved wrong: positions.entry_snapshot_id (a later frame) and entry_execution_price_usd (carries the 4% BUY_SLIP), and its run-up window was not pool-filtered. Re-priced with r40's method verbatim so the difference is attributable to the anchor alone.",
    "THE_STACK_SURVIVES": "r40 anchors: 614 kept, -1,445.4U, +18,413.5U. PRODUCTION anchors: 801 kept, -2,243.8U (-2.80U/pos), delta +17,615.0U. Actual book 2,522 positions -19,858.9U (-7.87U/pos). Token-clustered 90% CI [+12,469, +22,990] EXCLUDES ZERO; drop best 1/2/3/5 = +16,096/+14,822/+13,622/+11,306U; top token only 8.6%. The corrected anchor keeps 187 MORE positions because r40's 4pp slippage inflation made its run-up filter STRICTER than the real one. STILL A LOSS -2,243.8U; an 89% loss reduction, not a profit, every level in-sample. RARE: a headline that survived an anchor correction.",
    "BUT_THE_COMPONENT_TABLE_WAS_MISSING_ITS_KEY_ROW": "With the row 'entry filters, NO TP': actual -19,858.9U; TP only +10,439.7U; activity floor only +12,638.5U; run-up<=15% only +16,991.3U; entry filters (floor+run-up) NO TP +17,620.0U over 801 positions; FULL STACK +17,615.0U. THE STACK IS WORSE THAN THE ENTRY FILTERS ALONE BY 5.0U. TP incremental = -5.0U, 90% CI [-573, +693], does NOT exclude zero.",
    "THE_MECHANISM_pure_substitution_visible_in_the_2x2": "EXCLUDED by run-up>15% and TP never reaches: n=840, 0U gain. EXCLUDED by run-up and TP DOES reach +15%: n=629, actual -7,951.8U -> +2,518.9U, TP gain +10,470.7U. KEPT by run-up and TP never reaches: n=882, 0U. KEPT by run-up and TP reaches +15%: n=171, +798.8U -> +767.7U, TP gain -31.0U. So ALL of the TP's standalone +10,439.7U comes from positions the run-up cap excludes anyway; on the kept book the TP LOSES 31U because capping at +15% surrenders the runner tail r38 identified as the profit carrier. Direct overlap: of the 563 positions the TP improves, 512 (90.9%) are excluded by the run-up cap first, worth +10,720.7U.",
    "WHICH_filter_makes_it_redundant": "floor only: no TP +12,638.5U -> with TP +15,583.6U, TP adds +2,945.1U. run-up only: no TP +16,991.3U -> with TP +16,960.2U, TP adds -31.1U. SO IT IS THE RUN-UP CAP, not entry filtering in general, that makes the take-profit redundant.",
    "why_mechanically": "A pool that already ran up before entry IS the pool that later dies and that the TP rescues. Both mechanisms target the same positions.",
    "consequences": "(1) The +17,620U belongs to the ENTRY FILTERS; attributing any of it to exit-side take-profit is a misattribution. (2) Round 36/39's 'the exit side is where the profit is' is about REALISED attribution (forced -20,332U vs elective +1,348U) and is NOT contradicted - but the COUNTERFACTUAL marginal leverage of exit work is ~zero once entry filters are in place. Future citations must say WHICH of the two they mean. (3) Round 25's ordering (token 56.9% > entry timing > exit contract 14.6%) now has direct deployed-configuration support. (4) Deploying exit150_full15_v1 REMAINS CORRECT - it is deployed ALONE sharing the control's entry contract with NO entry filters, so its standalone value is the +10,439.7U row and its forward reading is still meaningful; it is redundant only IN COMBINATION.",
    "WHERE_THE_RESIDUAL_SITS_next_target": "In the kept 801-position book: 882 positions pass BOTH entry filters and never reach +15% (-3,666.4U), against 171 that do (+798.8U). By chain: solana 633 -1,688.8U, robinhood 106 -342.3U, bsc 62 -212.8U. The surviving book holds only 18 tokens, 13 of them net negative, worst token solana:Dw4hxvotjTZ -879.8U (52 positions). CONCENTRATION IS NO LONGER THE PROBLEM - round 26's 'low market cap' temptation (BSC = 70.8% of the loss, a fat cut point) no longer exists; the residual is spread across 18 tokens with no big cut available. The residual is 'the selected tokens simply do not work', and round 26 proved no new token-level rule is validatable on 44 tokens. SO: the deployed stack has exhausted what this epoch's evidence can support; the residual needs MORE INDEPENDENT TOKENS, i.e. the dense-observation budget (round 29, user-decided). Not touched."
  },
  "ROUND_58_NEW_MODULE_SHIPPED_AND_DEPLOYED_exit_ladder150": {
    "the_observed_failure_this_changes": "Round 120-57 measured the session's first near-readable same-frontier paired forward result - full15 vs full25, 22 shared / 14 diverged, within-cohort difference -7.124U/pos, CI [-12.942,-1.661] - and then found the pair is NOT single-factor: BOTH +25% arms also raise trailing_activate_return from the 0.30 base to 0.35 (exits150.py:158/195). So that figure cannot be attributed to the take-profit level and THE FLEET HAS NO CLEAN ESTIMATE of the level's effect. That is exactly what the user's rule 'compare parameter-only variants' requires and it did not exist.",
    "mechanism_basis_for_the_levels_not_a_replay_fit": "In all 14 of those diverged cohorts the position's own peak economic return never reached +25% - the five dominant ones peaked at -7.7%, 15.5%, 16.5%, 17.1%, 18.4%. So full25's +35% trailing activation was UNREACHABLE and cannot be their cause, while full15's +15% tier was reachable in four of the five. The tier is the operative difference, which is both the module's justification and the basis for its level choices.",
    "WHAT_WAS_BUILT": "src/memetrader/exit_ladder150.py - four exit-carrier arms identical in EVERY field except the first tier's `return`: exit_ladder150_t10_v1 / t15 / t20 / t25, all 100% capture, hard stop -0.35, **trailing_activate_return pinned at 0.30**, trailing_drawdown 0.35, max_hold 90, notional 1.0, max_concurrent 4, trajectory_exit None. Pinning the trailing activation is the entire point: it is the field that confounded the deployed pair. All four register at ONE frontier because round 57 measured same-frontier pairs accumulating diverged cohorts about an order of magnitude faster than cross-frontier ones (14 and 17 vs 4 and 3).",
    "NOT_a_level_search": "The declared purpose is to measure the SHAPE of the curve (monotone / flat / peaked), not to promote a level. All four levels have equal footing. Round 53's in-sample monotone ordering (+10% +15,501.9U, +15% +14,190.2U, +20% +11,738.9U, +30% +7,069.8U) is the HYPOTHESIS these arms test; picking a winner from that same-epoch replay would be exactly the round-26 selection trap. No existing arm was modified, retuned or replaced - this is pure addition.",
    "wiring": "alpha149.py (OVERRIDES.update + EXIT_ARMS.update, deliberately NOT added to SPECS - exit carriers must not get their own entry gate), store.py register_chain_meme_exit_ladder_experiments() cloned VERBATIM from register_chain_meme_exit150_experiments (same live template alpha149_vol_scaled_exit_v1, same append_chain_meme_trader_policy API so the frontier and behaviour hash are stamped by the existing API), runtime.py startup call next to EXIT150's.",
    "DEPLOYED_AND_VERIFIED": "Controlled restart via the supervisor's own mechanism (killed only the `memetrader run` tree; the 8790 console and unrelated services untouched). 4/4 arms registered; ONE shared frontier 2026-09-12T23:42:11; each arm one tier at its own level; trailing_activate_return 0.30 on all four (PINNED); every other contract field identical; 4 DISTINCT behaviour hashes none shared with any pre-existing arm; entry_filter bodies identical once the identity key `direction` is stripped; all nine pre-existing arms intact and still accumulating; runtime-crash.log UNCHANGED at 22,904 bytes (clean restart, not a crash); supervisor logged run_seconds=6023.5 exit=-1 (my force-kill, the expected value); and 20 ladder positions opened within the first minute because the exit-carrier design clones the first frozen entry signal. tests/test_exit_ladder150.py: 9 tests, and 45 pass together with the sibling module tests.",
    "t15_duplicates_full15_ON_PURPOSE_and_it_is_declared": "exit_ladder150_t15_v1 is behaviourally identical to the deployed exit150_full15_v1 (verified: they differ only in the entry_filter `direction` identity string). This is deliberate so the curve has four internally consistent points under one contract and one frontier, and it is written into the module's snapshot() dishonesty_guards along with the statement that the (full15, t15) difference is a FRONTIER effect and must not be read as a contract effect.",
    "MY_CHECKER_WAS_THE_THING_THAT_WAS_WRONG": "r58's single-factor check reported `entry_filter` as a differing behavioural field and printed NOT single-factor. The CAUSE WAS MY CHECKER not stripping the nested identity key `direction` - r58b section A strips it and confirms the four entry_filter bodies are IDENTICAL (1 distinct). The conclusion is unchanged (only take_profit is a behavioural difference) but it is recorded because a too-coarse checker and a genuinely non-single-factor contract produce the SAME wrong alarm, and I nearly edited correct code because of it.",
    "STANDING_RULES_new": "32 - A single-factor assertion's checker must strip IDENTITY keys before comparing behavioural fields. `direction`, `name`, `canonical_id` vary per arm but are not behaviour; a checker that treats identity as behaviour reports errors on correct code - which happened this round. 33 - A registration-type change must CLONE the sibling module's existing registration function verbatim rather than reimplement it. Cloning inherited the live schema, the template arm and the append API's frontier/hash stamping, and the pinned trailing_activate_return was confirmed landed by measurement. Reimplementing a 'looks the same' registration path is where this class of change goes wrong."
  },
  "ROUND_57_THE_SAME_FRONTIER_PAIRS_the_readout_never_tests": {
    "the_gap_in_my_own_tooling": "scripts/experiment_readout.py pairs each arm against ITS OWN control (full15 vs bank15). But arms registered at the SAME frontier share EVERY cohort, so pairs that differ in one contract field AND joined together are the fleet's cleanest paired tests - and the tool never tests them. Measured this round: frontier 32175 full15/full25, frontier 15928 bank15/bank25, 36360 t30/v5k, 38633 r15/r15a30.",
    "THE_MAIN_RESULT_full15_vs_full25": "22 shared settled cohorts, **14 DIVERGED** (8 identical). Within-cohort difference (full25 - full15) = **-7.124U/pos**, token-clustered 90% CI **[-12.942, -1.661] EXCLUDES ZERO**. 7 cohorts favour each arm. Unpaired per-side means (context only): full15 -2.30U/pos vs full25 -6.86U/pos.",
    "MY_READING_WAS_TOO_STRONG_AND_I_CAUGHT_IT": "Field-by-field comparison of both registered contracts found TWO behavioural differences, not one: take_profit 0.15 vs 0.25 AND **trailing_activate_return 0.30 vs 0.35** (exits150.py:158/195 shows BOTH +25% arms raise the trailing activation). So -7.124U/pos cannot be cleanly attributed to the take-profit level. (Separately verified: store.py:24269-24286 shows behavior_contract_hash DOES cover take_profit and trailing_activate_return, so the contract fingerprint is faithful - no defect there.)",
    "THE_MECHANISM_RESCUES_THE_ATTRIBUTION": "Decomposing the 14 diverged cohorts: 5 are 'full15 banked the tier, full25 never did and the pool died' = **-117.9U**; 8 are 'both banked, different level' = +20.1U; 1 is 'full15 banked, full25 exited otherwise' = -1.9U. And the per-cohort PEAK economic return (correct kernel, each position's own mark series) shows that in ALL 14 diverged cohorts the peak never reached +25%, let alone +35% - the five dominant ones peaked at -7.7%, 15.5%, 16.5%, 17.1%, 18.4%. THEREFORE full25's +35% trailing activation was UNREACHABLE in those positions and CANNOT be their cause, while full15's +15% tier WAS reachable in 4 of the 5. The bulk of the effect is attributable to the take-profit LEVEL on mechanism, not on contract text. HONEST EXCEPTION: cohort 580 exited full15 via take_profit_1 yet my replay's peak is -7.7%, so my mark series for that token disagrees with what the engine saw - 1 of 5 unexplained, recorded as such.",
    "BUT_THE_RESULT_IS_FRAGILE_and_below_the_gate": "drop the 1/2/3 most pro-full15 cohorts -> -5.792 / -4.301 / -2.563U/pos; **drop the 5 -> +2.022U/pos, SIGN FLIPS**; the favouring count is 7:7. 5 cohorts carry the entire effect and 14 < the 20-diverged gate. CORRECT STATEMENT: a strong mechanistic signal that the +15% tier catches pool deaths the +25% tier misses, NOT a verdict.",
    "CROSS_CHECK_bank15_vs_bank25_gives_the_SAME_mechanism": "30 shared, **17 diverged**, within-cohort difference -1.815U/pos, CI [-4.474, +0.619] INCLUDES ZERO - and ALL 17 divergences are the same mechanism (9 'both wrote off, different amount already banked' + 8 'neither wrote off'). This is the strongest support for the mechanism: WHEN THE TIER DECIDES WHETHER YOU EXIT AT ALL (full capture) THE LEVEL MATTERS A LOT (-7.12U/pos); WHEN YOU EXIT ANYWAY (50% partial) IT BARELY MATTERS (-1.82U/pos, CI includes zero).",
    "FORWARD_EVIDENCE_FOR_THE_USERS_EXIT_FOCUS": "'+15% full capture beats +25% full capture' now has FORWARD support, with the mechanism being that the higher tier misses the window in which pool deaths can be banked. This independently corroborates rounds 39/53's replay-based ceiling analysis from the forward book itself.",
    "the_floor_pairs_are_behaviourally_IDENTICAL": "t30 vs v5k: 11 shared, **0 diverged**. r15 vs r15a30: 10 shared, **0 diverged**. Every shared cohort resolves identically including the close reason. NOT redundancy - their rejected sets differ (t30 rejected 38 cohorts, v5k 45; r15 45, r15a30 59) - but on the KEPT book the two floor variants give identical results in this sample, so the user's 'compare parameter-only variants and keep the better' has NO basis to choose yet: the behaviour has not diverged.",
    "THE_PAIRING_CLOCK_IS_ADVANCING_and_same-frontier_pairs_are_an_order_of_magnitude_faster": "full15 vs bank15: shared 3 -> 8, diverged 2 -> 4. full25 vs bank25: shared 3 -> 9, diverged 1 -> 3. Same-frontier pairs sit at 14 and 17 diverged. QUANTITATIVE LESSON for future design: two variants of one hypothesis MUST be registered at the SAME frontier.",
    "documentation_defect_recorded_not_fixed": "full25's `description` (and `name`, and exits150.py's design note) say its 'only difference from full15 is the take-profit level' - the code contradicts that (trailing activation also differs). NOT FIXED: the description is registered into policy_additions.policy_json for a LIVE arm, and editing a running arm's registered text is a change to an existing strategy whose benefit (more accurate wording) is far smaller than the risk (contract identity churn). The correction lives in the record so the experiment is not misread.",
    "STANDING_RULES_new": "30 - 'Differs by only one parameter' must be verified FIELD BY FIELD, never inferred from naming or a description. My own design note, the arm names and the registered description all said one field; the contracts differed in two. 31 - When an effect is concentrated in a few units, 'drop the best few' is not a robustness check, it REMOVES the treatment effect itself. The honest move is to report the point estimate, the concentration (sign flips on dropping 5) AND the mechanism decomposition together, so the reader sees 'real but very thin' rather than being told 'robust' or 'not robust'."
  },
  "ROUND_56_THE_SATURATION_CRUX_RESOLVED_FROM_CODE_and_the_finding_gets_STRONGER": {
    "the_concern": "The adversarial reviewer of round 48 was asked to attack the crux: if chain_total/occupied are computed from a DIFFERENT set than the one the cap is enforced against, the saturation reading could be an accounting artifact. Two apparent contradictions in the same record also needed explaining: held_count vs chain_total looked inconsistent (solana 10/10, bsc 1/10, robinhood 3/10), and coverage145:status.membership reported bsc total 11 and early 5, both ABOVE CHAIN_CAP=10 and the early base cap of 3.",
    "ANSWER_FROM_THE_CODE_runtime.py:7865-7897": "base_caps = {early:3, growth:4, mature:3}; slot=(chain,bucket); `if key not in held:` then `if occupied.get(slot,0) >= base_caps[bucket]` -> continue else increment occupied and chain_used; and for borrowed early overflow `if chain_used.get(chain,0) < 10` -> increment the same two counters. SO `occupied` and `chain_used` ARE LITERALLY the counters the caps are tested against at lines 7883 and 7894 - the occupancy the funnel reports IS the enforced counter. NOT an accounting artifact.",
    "BOTH_CONTRADICTIONS_EXPLAINED": "(1) `if key not in held` means a HELD lease never increments those counters, so the ten candidate slots per chain sit ON TOP of held positions - the comment at 7890-7891 says exactly that ('Held watches never consume these ten candidate slots'). held_count therefore counts a different set from chain_total, which is why they look inconsistent. (2) `membership` is built by account_windows() over the WHOLE watch including held leases, while the caps are enforced only over the non-held candidate set - so membership exceeding CHAIN_CAP is EXPECTED, not a violation.",
    "RECONCILIATION": "held defined in code (runtime.py:8780 'held=OPEN_POSITION only'), so derived from the positions table rather than sampling the funnel's 32-entry deque: bsc 3, robinhood 3, solana 10 = 16 held tokens. Candidate slots = 30 (three chains at {early:3,growth:4,mature:3}). 30 + 16 = 46 against telemetry `watched` = 45 - off by 1, which is EXPECTED because a token that both holds a position and occupies a candidate slot is counted twice in that sum (noted as an upper bound in the probe). So 45 = 30 candidate + 15 held. The derived per-chain held counts (3/3/10) match the held_count values in the occupancy snapshots exactly, confirming held_count IS the open-position token count per chain.",
    "THE_FINDING_IS_STRONGER_ON_A_LARGER_SAMPLE": "Round 48: 17 occupancy snapshots, occupied==base_caps in 86%. This round: 19 snapshots, occupied==base_caps in **100%**, chain_total>=10 in **100%**. skip_bucket_full/admission_attempt = 92/139 = **66%** (round 47 measured 73/110 = 67%), with basic_valid 104 and only 9 admissions (6 base + 3 mover_reserved), skip_invalid_input 37, skip_chain_full 6. So the same reading reproduces across a longer process generation and a larger sample.",
    "stopped_the_reviewer": "The subagent had been running across roughly six of my rounds - far longer than its bounded task warranted, and its task included repeatedly polling /api/live, so it was probably stuck in polling. Its crux question is answered, so it was INTERRUPTED rather than left running. Per the guidance, a subagent's question being answered is the signal to stop it, and leaving it alive would both waste context and let 'still waiting on the review' become a reason not to make progress.",
    "STANDING_RULES_new": "28 - When a doubt's answer is in the CODE, read the code rather than adding samples. The doubt here was 'is the reported counter the enforced counter?' - only runtime.py:7883/7894 could answer it; more telemetry snapshots can corroborate but cannot prove it, and collecting more would have produced a more convincing WRONG answer. 29 - Stop a subagent the moment its question is independently answered; letting it run wastes context and turns 'waiting for review' into an excuse not to progress."
  },
  "ROUND_55_DISCOVERY_FUNNEL_PER_SURFACE_AND_THE_NARRATIVE_FEATURE_DIRECTION_IS_REJECTED": {
    "the_gap_this_closes": "Round 47 tried to measure per-surface dropping from /api/live and had to ABANDON it - that block carries a 14-second window and hydration showed exposed > returned, so the fields mean different things per surface. The right place was always the PERSISTED tables, which I had not found: token_discovery_rounds 14,851 rows (provider, surface, returned_count, exposed_token_count, new_token_count, snapshot_count, no_pair_count, status, error_type), token_discovery_exposures 102,628 rows (token_id, role, first_local_discovery, new_token, snapshot_count, observed_at), token_discovery_exposure_source_links 189,079. Window 2026-09-12T18:26:53Z .. 23:32:43Z (~5h).",
    "funnel_measured": "returned 223,680 -> exposed 102,423 (45.8%) -> snapshots 24,206 (23.6% of exposures) -> new tokens 11,788 (11.5%). Per surface: profile_updates 4,698 rounds / 112,652 returned / 31,473 exposed / 0 snapshots; hydration 7,115 / 12,618 / 22,737 / 11,946; geckoterminal new_pools 615 / 12,260 / 11,934 / 12,260; token_profiles 425 / 19,096 / 7,239 / 0; boosts_latest 266 / 20,712 / 6,500 / 0; pumpportal create 308 / 6,190 / 6,055 / 0; boosts_top 201 / 15,402 / 4,740 / 0; community_takeovers 230 / 14,883 / 4,588 / 0; ads 204 / 3,570 / 3,570 / 0.",
    "THE_45.8%_IS_DEDUPLICATION_NOT_LOSS": "Proved by dividing exposures by DISTINCT tokens: promotion 14,810 exposures over only **165** tokens (89.8 exposures/token); identity 46,691 over 5,358 (8.7); hydration 1.6; new_pool 1.8; create 1.1; migration 1.0. So profile_updates' 112,652 returned -> 31,473 exposed is the SAME tokens re-reported, not candidates discarded. DISCOVERY IS NOT SYSTEMATICALLY DROPPING ANYTHING. Reliability: only 4 error types across 14,851 rounds - ProcessRestart 89, ConnectTimeout 13, ReadTimeout 1, ReadError 1 = 104 = 0.70%.",
    "dense_observation_rate_by_role_overall_19.8%": "migration 32.83% (65/198) = 1.66x; promotion 21.82% (36/165); market_followup 20.84%; new_pool 11.98%; hydration 4.16%; identity 4.16%; create 1.88%. migration (the pumpportal graduation lane) is a STRUCTURAL event signal and the standout - noted as a watch item, not actionable at 112 tokens.",
    "THE_OBJECTIVE_EXPLICITLY_ASKED_FOR_THIS_FEATURE_DIRECTION_AND_IT_FAILED_ITS_OWN_GATE": "The task asks specifically for derived/approximate features standing in for narrative/hotspot/news, built from available data. The plan's P1 proposed using promotion/identity surfaces as SLOT-ALLOCATION features only - legal because the frozen contract forbids identity/promotion as ENTRY EVIDENCE, not as an allocation signal, and because a discovery ROLE is known at discovery time, BEFORE the first market observation, so it is point-in-time legal (unlike the round-50 feature that counted frames received after the decision). Tested with round 50's instrument: base touch 0.0812; deployed mover union rule 0.1950 (2.40x) - the benchmark. Roles: market_followup 0.1376, migration 0.1429, new_pool 0.1054 - ALL BELOW the union rule; promotion 0.0714 (0.88x base, i.e. paid promotion / community takeovers / profile updates do NOT predict moves in this epoch); identity 0.0326; create 0.0223. PAIRED test at the same cut size 877: migration + union top-up +0.0011, CI [-0.0079, +0.0011] DOES NOT EXCLUDE ZERO, and migration covers only 112 tokens (1.9%) so it cannot fill the queue; every other role is strictly worse. VERDICT: NOT DEPLOYED.",
    "this_is_the_sixth_free_lunch_refutation": "Rounds 42 (entry-time liquidity floor), 46 (risk gates over-strict), 48 (budget fragmented), 50 (slot allocation improvable), 54 (a decision reason blocking movers), 55 (narrative/promotion features) - six independent hypotheses that more trades or better selection were available for free, all falsified by measurement. The iteration plan's section 2.2 is updated from 'proposed' to 'tested and rejected'.",
    "STANDING_RULES_new": "26 - A merged count must be divided by the DISTINCT ENTITY count before being read as a ratio. promotion's 14,810 exposures cover 165 tokens; reporting exposed/returned = 45.8% as a drop rate would have misread DEDUPLICATION as LOSS - the same trap round 47 refused to walk into, this time hidden inside the persisted table. 27 - A direction the task explicitly names must still pass the acceptance gate it was given. Using narrative/promotion features to allocate slots was named in the brief and I proposed it with a gate; it failed (CI includes zero), so the answer is 'not deployed' rather than 'kept because it was requested'."
  },
  "ROUND_54_THE_DEFINITIVE_PER_REASON_GATE_QUALITY_TABLE": {
    "what_this_answers": "The objective's core question - 'which stage blocks tradeable candidates through over-strict thresholds, logic or risk rules?' - answered PER REJECTION REASON. Method: for every evaluation, look FORWARD 60 minutes on that token's already-collected frames and ask whether it reached the +15% economic level the deployed take-profit targets. No new observation requests. A reason whose refused population touches ABOVE baseline is discarding movers. Round 51 did this with the WRONG econ helper (round 52), so its levels were understated; this re-runs it with econ = 0.96R - 1.",
    "THE_LOOK_AHEAD_BOUNDARY_stated": "Round 50 caught a feature that leaked the future. Rule 21 governs FEATURES. Evaluating a GATE must use hindsight - the distinction is WHO may use the future, not WHETHER it may be used. Stated explicitly so the two are not confused.",
    "base_rates_corrected": "29,919 deduplicated (token, minute) evaluations; 16,565 (55.4%) have an outcome. Base +15% econ touch rate **0.1734** (round 51 reported 0.1508 with the wrong kernel); base 2x rate 0.0456. 44.6% of evaluations have NO later frame - structural, because follow-up frames are decided by the observation policy and refused candidates rarely keep being watched. 'No later frame' is UNKNOWN, never 'did not move'.",
    "THE_TABLE": "no_active_matching_entry_policy n=13,183 cens 39.9% touch 0.1598 CI [0.1468,0.1744] 2x 0.0543; pattern_observation n=6,355 cens 6.1% touch 0.1418 CI [0.1041,0.1860] 2x 0.0173; entry_pool_liquidity_absent_curve_stage n=4,684 cens 96.0% touch 0.1658 CI [0.1230,0.2083] 2x 0.1016; cohort_observation n=3,543 cens 49.6% touch 0.3619 CI [0.3155,0.4093] 2x 0.0840; entry_pool_liquidity_below_configured_floor n=1,568 cens 67.9% touch 0.1071 CI [0.0750,0.1447] 2x 0.0694; entry_pool_liquidity_unknown n=543 cens 62.8% touch 0.1485 CI [0.0844,0.2216] 2x 0.0941.",
    "THE_ANSWER_NONE": "Excluding observer bookkeeping (cohort_observation / pattern_observation, which are NOT decisions), NO decision reason's 90% CI lower bound exceeds baseline. The LARGEST decision gate, no_active_matching_entry_policy (22.97% of all evaluations), touches at 0.1598 with CI [0.1468,0.1744] - covering baseline, with a point estimate slightly BELOW it: the gate is NEUTRAL, neither discarding movers nor selecting them. Both liquidity gates sit below baseline (0.1071 / 0.1485) and below_configured_floor's CI TOP (0.1447) falls UNDER baseline, so that gate is filtering CORRECTLY. entry_pool_liquidity_absent_curve_stage is 96% censored - UNDETERMINABLE, not clean. This is the FIFTH independent confirmation of the same conclusion (rounds 42, 46, 48, 50, 54).",
    "WHERE_THE_REAL_DIFFERENCE_IS_the_observation_lane_not_any_gate": "admitted n=14 touch 0.3571 (2.06x) - TOO SMALL TO CONCLUDE, stated as such; rejected overall 16,551 touch 0.1733 (1.00x); non-bookkeeping decisions 8,813 touch 0.1567 (0.90x, CI [0.1441,0.1706]); cohort_observation (the DENSE observation lane) 3,543 touch 0.3619 (2.09x, CI [0.3155,0.4093]). So the dense-observation lane touches at 2.09x base while the decision population as a whole is slightly below it. This is round 29's coverage finding reappearing as a property of the observation LANE, not of any filter: the opportunity lives in the dense lane, the gates are neutral, and getting tokens INTO the dense lane is what matters - which rounds 47/48/50 already showed is capacity-bound and near its allocation frontier.",
    "STANDING_RULE_new": "25 - 'Cannot be measured' must be reported separately from 'no problem'. entry_pool_liquidity_absent_curve_stage is 96% censored (touch rate rests on ~190 observations) while no_active_matching_entry_policy is 39.9% censored; placing them in one table without the censoring column makes them look equally reliable. THE CENSORING RATE IS PART OF THE TABLE, not a footnote."
  },
  "ROUND_53_ROUND_39s_CEILING_RE_RUN_WITH_THE_CORRECT_KERNEL_AND_IT_IS_STRONGER": {
    "why": "Round 39's ceiling (+15% take-profit converts 484 forced exits for +10,303.6U, token-clustered CI excludes zero, survives dropping the best five tokens) is the session's strongest positive result and the justification for the deployed exit150_full15_v1 arm. Round 52 proved the econ helper it shares with r40/r43/r51 was WRONG and ~5.1% stricter, which can only UNDER-fire a take-profit - so round 39's numbers were understatements. Re-run with r39_ceiling_test.py's method verbatim except the kernel, both reported side by side.",
    "peak_econ_by_actual_close_mechanism_CORRECT_kernel": "pool death n=870 p25 +7.4% p50 **+18.2%** p75 +28.9% PnL -17,007.3U; hard stop n=689 p25 -7.7% p50 -1.5% p75 +10.6% PnL -6,535.2U; elective n=1,193 p25 -5.1% p50 +4.4% p75 +23.4% PnL +1,398.9U. The WRONG kernel gave pool death p50 +12.5%. SO MORE THAN HALF OF ALL POOL DEATHS WERE UP 18%+ BEFORE DYING (rounds 38/39 recorded +15.2%) - 'killed by the rules, not by the market' is now sharper.",
    "conversion_test_corrected": "+10% -> 794 converted (594 pool death + 200 hard stop), +15,501.9U. +15% -> **662 converted (537 pool death + 125 hard stop), +14,190.2U**. +20% -> 510, +11,738.9U. +30% -> 280, +7,069.8U. Round 39 recorded 484 converted / +10,303.6U at +15%. DO NOT move the deployed level to +10% because that row is higher - that is exactly the round-26 selection trap on the same epoch. The deployed +15% stays.",
    "THE_CEILING_corrected_with_BOTH_denominators": "Round 36's claim was '58.5% OF ALL EXITS are forced', so the comparable quantity is forced-and-never-reached over ALL settled positions. CORRECT kernel: forced 1,559, never reached +15% 897, that is 57.5% of forced but **32.6% of all 2,752** => TOUCHABLE 67.4%. Wrong kernel: 1,051 never reached = 38.2% of all => 61.8% touchable. Round 39 reported 859/2,393 = 35.9% untouchable => 64.1% touchable on a smaller population. SO ON A CONSISTENT DENOMINATOR THE CORRECTION RAISES THE TOUCHABLE SHARE FROM 64.1% TO 67.4%.",
    "robustness_corrected": "gain +14,190.2U, token-clustered 90% CI [+8,987, +19,637] EXCLUDES ZERO; 24 of 48 tokens positive; drop best 1/2/3/5 = +12,747/+11,439/+10,188/+7,762U; top token only 10.2% of the gain. Versus round 39's +10,303.6U CI [+5,697,+15,068] drop-best-5 +10,973U: the point estimate is 38% HIGHER and concentration has NOT worsened.",
    "what_this_means": "Round 39's direction and conclusion hold AND are strengthened. The adversarial review found a defect that made my PESSIMISTIC number too pessimistic and my POSITIVE result too conservative. Round 36's '58.5% is a hard ceiling' is further weakened: only 32.6% of settled positions are forced AND never reached +15%; 67.4% are touchable. The deployed exit150_full15_v1 rests on a stronger basis than recorded (+14,190U not +10,304U) - and its forward reading is still blocked on the pairing clock, unchanged.",
    "MY_OWN_DENOMINATOR_ERROR_CAUGHT": "My first version of this probe printed 'touchable share 42.5%' by dividing forced-and-never-reached by FORCED only (897/1,559 = 57.5%). Round 36/39's comparable figure divides by ALL positions. Same data, two denominators, 25 percentage points apart - it would have looked like a correction while actually being a change of yardstick.",
    "STANDING_RULE_new": "24 - When reporting a share, ALWAYS state the denominator. This round produced 57.5% and 32.6% from the same 897 positions; the difference was the yardstick, not the finding."
  },
  "ROUND_52_CORRECTION_my_economic_return_formula_was_WRONG_in_rounds_39_40_43_51": {
    "how_this_round_arose": "The objective asks for multi-Agent parallel work with cross-validation. Round 52 put an ADVERSARIAL reviewer on the session's most consequential claim (round 43's substitution result). It returned QUALIFIED and alleged a material formula defect. I did NOT take that on trust - a subagent's claim is advice, not evidence - and went to the primary source myself. THE ALLEGATION IS CONFIRMED.",
    "THE_DEFECT": "I used econ = (1-BUY)/(1+SELL) * (px/entry_execution_price_usd) - 1 - 2*60/1e4. Two errors: (a) entry_execution_price_usd ALREADY contains the 4% buy slippage (measured mean ep/sig = 1.0398), so dividing by (1+SELL) charges it TWICE; (b) the deployed settings carry NO additional per-fill fee (kv chain-paper-execution:activation:20260912182645 -> additional_fee_usd_each_fill 0.0), so the 1.2% term is a fee that does not exist. The deployed kernel is econ = 0.96*R - 1 (store.py:25359-25363 errata, \"stake_usd*remaining_raw/initial_raw*current_price/entry_execution_price_usd*0.96\").",
    "VERIFIED_FIRST_HAND_not_taken_on_trust": "chain_meme_trader_marks.trigger_evidence_json.pre_trigger.economic_return stores the value the ENGINE computed at each decision. Joining each of 1,300 marks to its position for the entry price and scoring both formulas against the recorded value: 0.96*R - 1 matches EXACTLY 1,265/1,300 (and 86/107 real market_mark_take_profit marks); my formula matches 0/1,300. Mean absolute error 0.00997 vs 0.04896. My formula is ~5.1% STRICTER on the price ratio at every target (+15% required R>=1.2588 instead of R>=1.1979), so it can only UNDER-fire the take-profit - confirmed: on the kept set the correct kernel fires 207 times, mine 180.",
    "CORRECTED_BY_MY_OWN_RERUN_not_by_the_reviewers_number": "r52d_corrected.py re-runs round 43 with the correct kernel, method otherwise identical. Stack: -2,461.7U on 894 kept, delta +19,663.7U, 90% CI [+14,398,+25,140] (vs the record's +17,615U). TP incremental on top of the entry filters: +476.6U, 90% CI [-345,+1,390] - versus the recorded -5.0U. The reviewer's independent paths gave +91.0U (mark replay) and -62.1U (engine record booked at the level); all three correct-kernel figures agree that the 90% CI INCLUDES ZERO, which matters more than the point estimate.",
    "THE_OVER_GENERALISATION_IS_WITHDRAWN": "The sensitivity grid - which the reviewer raised and I reproduced with my own method - falsifies my round-43 generalisation: cap<=10% +160.8U CI [-472,+844]; cap<=15% (deployed) +476.6U CI [-340,+1,469]; cap<=20% +728.2U CI [-194,+1,751]; NO CAP +3,581.4U CI [+1,004,+6,882] EXCLUDES ZERO. So the zero is a property of the DEPLOYED <=15% RUN-UP CAP, not of 'entry filters' in general. Correct statement: the take-profit is redundant UNDER THE DEPLOYED <=15% CAP, because that cap already excludes the pools that ran up before entry and later die. The SUBSTITUTION MECHANISM ITSELF STANDS unchanged, because 90.9% of the take-profit's beneficiaries being excluded by the cap is a COUNT and does not depend on the economic formula.",
    "IMPACT_ON_OTHER_RECORDS_all_in_one_direction": "Every affected figure is an UNDERSTATEMENT of take-profit value, so no conclusion flips from holding to not holding - only the numbers change: round 43 (TP incremental and stack delta - corrected in this round; the record carries a correction banner); round 40 (stack projection's TP component understated -> read +17,369U as a LOWER BOUND); round 39 (the +15% TP ceiling +10,303.6U understated -> also a lower bound, so the ceiling argument gets STRONGER); round 51 (per-reason +15% touch levels understated, but the COMPARISON BETWEEN REASONS is unaffected because the same formula was applied to all of them); round 50 (used ratio >= 2.0, not econ - unaffected).",
    "REUSABLE_FIX": "data/research/diag_round120/econ_kernel.py now holds the correct kernel with the evidence for it and an explicit `legacy_wrong_economic_return` kept only so a diff against old recorded numbers is explainable. The file says DO NOT re-derive this, import it.",
    "STANDING_RULES_new": "22 - A replay of economic return / slippage / fees must be REVERSE-ENGINEERED FROM THE PRODUCTION KERNEL and validated against values the engine itself recorded, not reassembled from the intuition 'buy slippage + sell slippage + fees'. That entry_execution_price_usd already contains the buy slippage alone was enough to make a plausible formula 5% wrong and move a point estimate by two orders of magnitude (-5.0U -> +476.6U). VALIDATION CRITERION: exact equality against pre_trigger.economic_return, not 'close'. 23 - Adversarial review must run in an INDEPENDENT CONTEXT and must return INDEPENDENTLY CHECKABLE PRIMARY EVIDENCE, not just a conclusion. This round's reviewer earned its result by offering 'score both formulas against trigger_evidence_json and require exact equality' - a test I could rerun myself. A review that offers only a verdict is not adopted."
  },
  "ROUND_50_THE_LAST_BUDGET_FREE_LEVER_DOES_NOT_EXIST": {
    "why": "Rounds 47/48 proved the ~30-slot observation budget is saturated (all three chains at CHAIN_CAP simultaneously; 67% of admission attempts refused bucket-full). The budget is a user decision I must not touch, so the only remaining degree of freedom is WHICH candidates consume the slots. mover_watchlist.py's own basis says its union rule 'catches about a third of the tokens that double, so this is a density improvement on the watched set, NOT a detector.' A slot spent on a token that never moves is stolen from one that would. This tested whether a better ranking buys coverage for free.",
    "MY_FIRST_ATTEMPT_WAS_UNDER_POWERED_AND_I_DISCARDED_IT": "r50 simulated an hourly queue (top-C per hour) and reported buy_pressure x trades at 0.0694 vs the current rule at 0.0139. That was NOISE: only 6 hourly buckets existed, so precision@24 rested on 6x24 = 144 picks against a 3.6% base rate - every figure quantised to multiples of 1/24 and rule differences were 1-2 tokens. The threshold rule was also mis-simulated, because ranking by a boolean leaves all flagged tokens tied and sorted() then picks arbitrarily. Replaced with a pooled, capacity-matched design.",
    "THE_BENCHMARK_MEASURED_PROPERLY_n=5,748": "Base rate of doubling within 60 min of the first observation: 3.62% (208 tokens). The current mover union rule flags 851 = 14.8% of arrivals - almost exactly the capacity fraction - with precision 8.23% (lift 2.27x), so 91.8% of watched tokens do NOT double. It catches 70 of 209 doublers = 33.5% recall; the oracle with the same 851 slots would catch 209, so the headroom is 139 tokens. NOTE the module's own recorded basis said base 10-25% / precision 16-36%; my levels differ (3.62%/8.23%) but the lift is the same order (1.35-1.95x vs 2.27x). Differences come from how the observed population and 'doubling' are defined; all rule comparisons below are on the SAME population and outcome, so the comparison is fair and the LEVEL is not an unbiased conversion rate.",
    "I_CAUGHT_A_LOOK_AHEAD_FEATURE_DRESSED_AS_THE_WINNER": "r50b's top scorer was `frames already seen` at precision 19.74% (lift 5.46x), net +98 doublers. REJECTED. `frames` counts every frame the token ever received, i.e. a quantity determined AFTER the decision. Measured distribution (r50d): frames observed BEFORE the first observer frame are 0 for 3,905 tokens (67.92%), 1 for 1,680 (29.22%), 2+ for 0.86% - so 97.15% sit at 0 or 1 and the pre-decision version is NEARLY DEGENERATE (max 13). Total frames span 1..1,810, which is exactly why the look-ahead version looked strong. Density and doubling ARE correlated (round 29's 18x), but density is partly an EFFECT of the move, not a cause available before it.",
    "STANDING_RULE_new": "21 - Before trusting any ranking/filter feature, recompute it restricted to BEFORE the decision moment. If the restricted distribution is nearly degenerate (this round: 97.15% at 0/1) while the unrestricted version performs well, the performance comes from the future, not the feature. Test for degeneracy BEFORE quoting a lift.",
    "THE_ONE_REAL_CANDIDATE_FAILS_THE_PAIRED_TEST": "With the look-ahead feature removed, exactly one available feature beats the current rule on the point estimate: liq x buy_pressure 9.40% (2.60x) CI [0.0776,0.1116] vs the union rule 8.23% (2.27x) CI [0.0682,0.0987]. The CIs overlap, and comparing two overlapping CIs is not evidence, so a PAIRED bootstrap was run (same population, same cut size 851, 3,000 token resamples): +10 doublers (80 vs 70), paired difference +0.0118, 90% CI [+0.0000, +0.0218] - DOES NOT EXCLUDE ZERO (the lower bound touches it). Every other available feature is WORSE than the deployed rule at the same cut size: buy_pressure 4.94%, trades 5.41%, turnover 4.47%, liquidity 4.35%, random 3.29%.",
    "CONCLUSION": "There is NO budget-free lever that raises coverage. The budget is saturated (round 48) AND the allocation rule is already near the frontier of what available decision-time features can achieve (round 50). This is the THIRD refutation of a free-lunch hypothesis this session, alongside round 42 (entry-time liquidity floor: 0 of 818) and round 46 (risk gates over-strict: the monotonicity guards fire 0 times in 38,110 frames). The final answer to the objective's core question is now stated harder: nothing is blocking on thresholds, logic or risk rules; it blocks on CAPACITY, the capacity is FULL, and the allocation rule is already near-optimal for the features that exist. Raising coverage requires MORE OBSERVATION BUDGET - the user-decided item (declined round 79, partially approved 83/88). NOT touched."
  },
  "ROUND_50_ITERATION_PLAN_AND_ARCHITECTURE_PUBLISHED": {
    "file": "docs/PROJECT_CONTEXT/ITERATION_PLAN_AND_ARCHITECTURE_20260913.md",
    "what": "The objective asks for layered-architecture, derived-feature, dynamic-risk and observability proposals plus a multi-round plan. Each proposal names the observed failure it changes, and each refusal says why from measurement.",
    "PROPOSED": "(P1) use narrative/promotion signals (boosts_latest, community_takeovers, profile_updates - all already collected) as SLOT-ALLOCATION features only. The frozen contract forbids identity/promotion as ENTRY evidence but does not forbid using them to choose WHICH pool to observe, and the slot is the scarce resource. Acceptance gate is the same instrument as round 50: capacity-matched doubling rate vs the union rule under a PAIRED bootstrap, CI must EXCLUDE zero or it is not deployed. (P1) a rejection-outcome ledger: 61,684 of 61,862 evaluations were rejected and NEVER revisited, so the system records why it refused but never whether refusing was right - the fix records one outcome row per rejected (token, decision time) at a fixed later horizon, reusing already-collected frames so it adds NO observation requests and does not touch the budget. (P2) a per-token concentration cap as an ADDITIONAL arm, whose acceptance metric is VARIANCE not return - it cannot turn a losing quadrant profitable and must not be sold as if it could.",
    "REFUSED_with_reason": "Layered light-prefilter + full-feature architecture: NOT built - the system already HAS that layer (the 24-slot mover prefilter), round 50 shows it is near the feature frontier, and CPU is not the bottleneck (observed->decision p50 1.20s), so saved computation cannot become positions. Raising request frequency: NOT done - the slot count is the constraint (round 48 saturated) and round 29 measured the new-information rate already flat at 16.8-19.2% after 300s, which is what round 22's adaptive cadence exists for. Dynamic position sizing: NOT done - every quadrant loses in-sample so sizing up a losing book loses more, and Kish effective n ~= 30 means any sizing rule bets on ~30 observations."
  },
  "ROUND_49_CODE_CHANGE_console_rejection_labels_corrected": {
    "the_observed_failures_this_changes": "(1) reasonText()'s fallback had `value.includes('entry') -> '策略入场条件成立'` and `entry_snapshot_too_old` contains 'entry', so a REJECTION for staleness rendered as 'entry conditions MET' - the console displayed the opposite of what happened. (2) The four highest-frequency evaluation reasons were all absent from the lookup map (measured over 61,862 evaluations: cohort_observation 34.4%, pattern_observation 26.9%, entry_pool_liquidity_absent_curve_stage 7.1%, invalid_exact_asof_market_snapshot 4.7%), so they rendered as RAW ENGLISH IDENTIFIERS - and entry_pool_liquidity_absent_curve_stage matched `includes('liquidity')` and rendered as 'pool liquidity abnormal' when it is a bonding-curve pool with no measurable liquidity, i.e. a data-availability limit, not a fault. Two of the four are not rejections at all but the observer's own bookkeeping, so the console made 61% of the log read as strategy refusals.",
    "the_change": "src/memetrader/chain_web_static/app.js only (presentation layer). Added an `observed` table ahead of the existing `known` map, naming each reason for WHAT IT MEASURES (rule 16): cohort_observation/pattern_observation -> '观察者记账…（非策略拒绝）'; entry_pool_liquidity_absent_curve_stage -> '发射曲线阶段，原池流动性尚不可测'; invalid_exact_asof_market_snapshot -> '行情快照未通过时点有效性校验（多为该池暂无美元报价）'; entry_pool_liquidity_below_configured_floor -> '原池流动性低于已配置下限，禁止买入'; entry_snapshot_too_old -> '行情快照过旧，超过入场新鲜度上限'. Also added MY OWN four experiment arms' entry-floor reasons to `known` (they previously fell through the `not_met` branch to a raw identifier): activity_floor_trades_not_met, activity_floor_volume_not_met, runup_floor_exceeded, runup_floor_window_unknown. NO STORED reason string changed, so historical comparability and any string-matching code are unaffected.",
    "two_explicit_dont_lie_constraints_encoded_in_the_test": "the invalid_exact_asof label must NOT contain 过旧/陈旧/过期 (it is an identity/validity check - that exact confusion cost round 45), and the curve-stage label must NOT contain 异常 (it is a data limit, not a pool fault).",
    "verification": "NEW tests/chain_web_reason_text.test.js (11 assertions) including that entry_snapshot_too_old must NOT equal '策略入场条件成立'. That assertion FAILS on the old code (the old fallback returned exactly that string), so the test is meaningful rather than tautological. All three pre-existing console tests still pass (chain_web_strategy_revision, chain_web_ui_stability, web_detail_request_state). PRODUCTION VERIFIED: the served /app.js is BYTE-IDENTICAL to the working-tree file (173,724 bytes, sha256 c3b64db4aa72355b) and the ASCII markers the fix adds (const observed=, entry_pool_liquidity_below_configured_floor, runup_floor_window_unknown) are present in the served bytes.",
    "operational_fact_established": "chain_web.py:4459-4464 serves assets straight from static_dir, _send_asset caches nothing, and the response is Cache-Control: no-cache. SO THIS CONSOLE'S UI ASSETS ARE READ FROM THE WORKING TREE - a page refresh picks up an edit, there is no build step. (A stale nested snapshot exists at .venv/src/memetrader/src/memetrader/chain_web_static/app.js, 157,938 bytes, but the console does NOT serve it - excluded by hash.)",
    "the_fifth_measurement_artifact_of_the_session": "I fetched /app.js and matched '观察者记账' -> False, while the byte count matched the working-tree file exactly. I nearly wrote 'the change is not live, a build is needed'. THE CAUSE WAS MY OWN MEASUREMENT: mimetypes reports text/javascript with no charset, so PowerShell decoded the UTF-8 bytes as Latin-1 and every Chinese character was mangled (the console shows ۲��߼��� style mojibake), making any substring match fail. A byte-for-byte comparison plus ASCII-only markers reversed the conclusion immediately.",
    "STANDING_RULE_new": "19 - Before substring-matching text returned over HTTP, confirm the decoding; compare BYTES or ASCII-only markers instead. An identical byte count with a failed match is almost always a decoding problem, not a deployment problem. (Fifth instance this session of an artifact masquerading as a finding: write-off rate, `closed` means, invalid-asof, exposed>returned, and this.)"
  },
  "ROUND_48_COVERAGE_QUESTION_CLOSED_THE_BUDGET_IS_SATURATED_NOT_FRAGMENTED": {
    "the_two_possibilities_had_opposite_remedies": "Round 47 measured from live counters: 110 admission attempts, 74 (67%) refused as skip_bucket_full, of which bucket_full_chain_full 73 and bucket_full_chain_spare 12. observation_leases145 sets BASE_CAPS={early:3, growth:4, mature:3} (sum 10) and CHAIN_CAP=10; three chains x 10 = 30 = round 29's '~30 slots'. (a) SATURATED - every chain at its cap, total 30 genuinely full, only a budget increase helps (user-decided). (b) FRAGMENTED - one chain at 10 while others idle, so rebalancing the SAME slots raises density at zero extra request budget. RediscoveryFunnel.quote() records `occupancy` with each admission_attempt, so production had already recorded which.",
    "MEASURED_ANSWER_SATURATED": "17 examples carry the occupancy snapshot (keys: chain, chain_total, held_count, occupied, base_caps, target_bucket). `occupied` equals BASE_CAPS exactly - {early:3, growth:4, mature:3} = 10 = CHAIN_CAP - at nearly every sample, ON ALL THREE CHAINS SIMULTANEOUSLY (solana held 10, bsc held 1, robinhood held 3 - the held_count is the pattern watch's own leases, not the accounting). `non_held_by_chain_bucket` is {3,4,3} for all three chains too. Independently confirmed by coverage145:status.membership at 23:17:30Z: bsc {early:5,growth:4,mature:2} total 11, robinhood {3,4,3} total 10, solana {3,4,3} total 10. Fragmentation is the MINORITY: bucket_full_chain_spare 12 of 85 = 14%, against chain_full 73 of 85 = 86%.",
    "the_conclusion": "The observation budget is NOT being fragmented by the per-chain cap - it is genuinely FULL. Raising dense-observation coverage has exactly one route, a budget increase, and that is the user-decided item (declined round 79, partially approved rounds 83/88). BUDGET NOT TOUCHED.",
    "the_constraint_argument_is_now_closed_with_a_MECHANISM": "Four independent measurements, the last one mechanistic: (29) only 4.4% of observed tokens ever get a 3-frame episode and dense converts 18x; (44) discovery->first snapshot p50 97s while everything downstream is <=1.3s, and 0 tokens have an observer frame first; (47) production counters - 67% of admission attempts refused for BUCKET FULL while 82 of 110 episodes had a fully usable frame; (48) the occupancy snapshot at refusal - all three chains at CHAIN_CAP=10 and all three buckets at base_caps, 100% of samples. So the final answer to 'do over-strict thresholds/logic/risk rules systematically block tradeable candidates' remains NO, now stated harder: nothing is blocking on thresholds; it blocks on CAPACITY, and capacity is the 30 user-approved slots.",
    "recorded_not_acted_on": "chain-meme-pattern-watch this generation (1.25h): watched 44, mover_watching 22/24, sampled 21, projected 0, borrows 46, replacements 141, reservation_reclaims 41, pool_migrations 0, and other_pool_quote_skips_since_start 5,633 - the largest single UNEXPLAINED counter seen this round. I deliberately did NOT interpret it: explaining a metric before confirming its semantics is the error this session has made four times (rules 1/12/16). Left as an open item; read the code before concluding.",
    "STANDING_RULE_new": "18 - Before concluding 'not enough capacity', measure the OCCUPANCY AT THE MOMENT OF REFUSAL. Otherwise 'genuinely full' and 'fragmented by a cap' are indistinguishable, and their remedies differ by an order of magnitude in cost. This round's occupancy came from production's own refusal record; if that field did not exist, the correct move is to ADD IT FIRST rather than guess."
  },
  "ROUND_47_CONSTRAINT_NOW_MEASURED_NOT_INFERRED_and_the_reservation_FIX_IS_VERIFIED": {
    "why_this_round": "Round 44 INFERRED that the p50 97s discovery delay is queueing for an observation slot. rediscovery_funnel.py exists to answer exactly that, counting admission_attempt and the REASON for each failure plus basic_valid. It is in-memory per process generation, so it must be read from the live KV landing point, not the DB.",
    "the_bound_gate_measured_live": "kv `rediscovery-funnel94` at 23:16:41Z, scope=prospective_process_generation: episode 112, admission_attempt 109, **skip_bucket_full 73 (67%)**, bucket_full_chain_full 72 / chain_spare 12, skip_chain_full 6, skip_invalid_input 29, **basic_valid 82**, admit_base 4, admit_mover_reserved 3, hydration_hydrated 109, snapshot 109, cohort_observation 78, pattern_observation 10. STATED LIMIT: the funnel counts one per episode per stage/reason, so 82 vs (4+3) is NOT a same-denominator conversion - the claim '95% of valid frames are dropped' would be WRONG. What stands: the dominant refusal is BUCKET FULL (73/109 = 67%), not invalid data (29) and not the absence of a usable frame (82 episodes had one). So the constraint is BUCKET OCCUPANCY, not post-acquisition dropping - round 29's ~30 slots, round 44's 'the whole delay is before the first snapshot', and this 67% are three independent measurements of one constraint. Still the user-decided budget item; NOT touched.",
    "THE_RESERVATION_FIX_IS_VERIFIED_IN_PRODUCTION": "mover_watchlist.py records its own measured failure: 'Round 101 measured a 0-of-24 overlap between the flagged set and the leases the observer actually holds ... so the reservation almost never fired (about 6 admissions/hour against a design of 32)'. reserved_candidates() was written to fix it and IS wired (runtime.py:7746-7747, tests/test_mover_reserved_wiring.py). But wired+unit-tested != fires in production (rounds 31/33), so the live counters were read from kv `chain-meme-pattern-watch` at 23:16:43Z over the 1.25h generation starting 22:01:47Z: mover_watching **22 of 24**; mover_reserved_injections (REACH) **261**; mover_reserved_admissions (OUTCOME) **69**; 27 injected / 39 admitted token ids; overlap(injected, flagged) 6 of 27 (22%). RATE: **55.3 admissions/hour against the failed baseline of 6 and a design of 32 - 9.2x the failure, 1.7x the design**; flagged-and-held went from 0-of-24 to 22%. This is exactly the effectiveness/coverage axis: the module's own basis is that at 12 slots the added acquisition was only +5.7% against an approved +13-14% while 59 of 67 ready pools stayed sparse and converted at 0.0% against 62.5% for dense ones. The reservation steers the ALREADY-APPROVED 24 slots toward mover-flagged candidates, raising density at ZERO extra budget. A real reachability defect was suppressing the use of an approved budget; it is fixed and now verified in production.",
    "a_trap_I_avoided": "The /api/live discovery block's returned_count vs exposed_token_count LOOKS like a discovery drop rate. It is not usable: the block carries only a 14-second window and `hydration` shows exposed > returned (160%), so the two fields mean different things per surface. Nothing was concluded from it.",
    "two_bookkeeping_items_not_trading_defects": "(1) pattern-admission-shadow: status DROPPED_AUDIT, audit_discontinuous true, receipts 47,474 / written 47,347 / dropped_audit 98, bytes ~104.8 MB PER PROCESS GENERATION. Marked affects='none', decision_eligible=0, so it does not affect trading - recorded only, not fixed (it changes no observed trading failure). (2) cohort-flow:v1 this generation: startup_30m BUY 68/REJECT 8, steady BUY 64/REJECT 10, TERMINAL_SELL 52+46, TERMINAL_WRITEOFF 16+15; activity_floor150_t30_v1 BUY 5/writeoff 1, v5k BUY 6/writeoff 1.",
    "STANDING_RULE_new": "17 - Wired plus unit-tested does NOT mean it fires in production. A mechanism must have a PRODUCTION COUNTER proving it happened; this round could only judge the fix because the runtime writes reach-vs-outcome counters (mover_reserved_injections vs mover_reserved_admissions). Stronger than rule 6: rule 6 says verify after shipping, rule 17 says BUILD THE COUNTER FIRST."
  },
  "ROUND_45_46_RISK_STAGE_FULLY_DECOMPOSED_over_strict_hypothesis_REFUTED": {
    "the_contradiction_I_had_to_resolve": "The objective requires the RISK stage reported with rejection reasons. My own records disagreed by 160x about it: the consolidated funnel said `invalid-asof` 4.78% of 57,290 evaluations; round 28's checkpoint said `entry_snapshot_too_old` 0.03% (limit 90s).",
    "BOTH_ARE_CORRECT_they_measure_different_checks": "0.03% is STALENESS and reproduces independently: snapshot age >30s at decision = 19 of 61,862 = 0.031%; >90s = 12 = 0.019% (the 12 `entry_snapshot_too_old` rows). 4.69% is `invalid_exact_asof_market_snapshot`, an IDENTITY/VALIDITY check with nothing to do with age. The funnel label `invalid-asof` was too terse and I created the confusion myself. FOURTH time this session two figures looked contradictory but were only under-labelled (write-off rate, `closed` means, status='written_off', now invalid-asof).",
    "EVERY_reason_verbatim_over_61,862_evaluations": "cohort_observation rejected 21,263 (34.37%); pattern_observation rejected 16,614 (26.86%); no_active_matching_entry_policy 14,210 (22.97%); entry_pool_liquidity_absent_curve_stage 4,388 (7.09%); invalid_exact_asof_market_snapshot 2,899 (4.69%); entry_pool_liquidity_below_configured_floor 1,747 (2.82%); entry_pool_liquidity_unknown 551 (0.89%); cohort_observation ADMITTED 174 (0.28%); entry_snapshot_too_old 12 (0.02%); pattern_observation admitted 4. Rejected 61,684 (99.71%), admitted 178 (0.29%).",
    "RISK_FLAVOURED_TOTAL_15.51%_fully_attributed": "entry_pool_liquidity_absent_curve_stage 4,401 (7.11%); invalid_exact_asof_market_snapshot 2,901 (4.69%); entry_pool_liquidity_below_configured_floor 1,747 (2.82%); entry_pool_liquidity_unknown 551 (0.89%); entry_snapshot_too_old 12 (0.02%). ALL ON FRESH EVIDENCE: mean snapshot age at decision 0.28-5.86s per reason, max 30s; only the 12 too-old ones average 164s. ZERO of the 178 admitted evaluations rests on a snapshot older than 30s.",
    "reinforces_round_42": "`entry_pool_liquidity_below_configured_floor` ALREADY fires on the ENTRY side and rejected 1,747 evaluations - it simply never fired for the positions that later died, because their entry-time liquidity was fine. Consistent with round 42's 0-of-818.",
    "THE_OVER_STRICT_HYPOTHESIS_REFUTED_A_THIRD_TIME": "I suspected the two MONOTONICITY guards (h1_trades>=m5_trades, h1_volume>=m5_volume at store.py:29452-29459) were discarding valid candidates on a provider refresh-lag artifact. THEY FIRE 0 TIMES IN 38,110 OBSERVER FRAMES. The actual cause: `price <= 0` because `priceUsd` is ABSENT, 2,871 (98.97%), plus pairCreatedAt missing/zero 30 (1.03%). Example solana:FsA28Svt...pump dex=pumpfun priceUsd=None priceNative=0.0000003415 txns m5={0,0} h1={4,2}. THE REJECTIONS ARE CORRECT - a pool with no USD price cannot be priced and a pool with no creation time has no age.",
    "the_largest_addressable_block_is_a_DATA_limit_not_a_filter_defect": "99.0% of invalid_exact_asof_market_snapshot and 100% of entry_pool_liquidity_absent_curve_stage are BONDING-CURVE pools (DexScreener gives no priceUsd): pumpfun 913+3,942, fourmeme 1,932+330, meteoradbc 13+128. Together ~11.8% of evaluations - the largest single addressable block after the observation-slot queue. The system ALREADY has a lane for it: pump_native_absorption_fast_v1 via later_observed_protocol_model_paper, now 14 positions over 14 DISTINCT TOKENS at exactly 1.00 positions/token, realised -14.05U (-1.00U/pos), exits 11 hard_stop (-15.59U) / 2 max_hold (-1.10U) / 1 trailing (+2.64U). Of the 6,948 distinct curve-pool base addresses seen in rejections, only 11 tokens have EVER been entered by any arm, at -13.65U. So the lane converts 0.20% of the block and has no edge at n=14. NOT building a derived-price lane: curve pools also have no measurable liquidity, so the configured 1000U pool floor cannot be satisfied and position pricing, the slippage model and the write-off logic would all lose their basis. NO CODE CHANGED.",
    "STANDING_RULE_new": "16 - When two figures in the record look contradictory, first check whether they are merely UNDER-LABELLED names for different quantities (four occurrences this session). Name a metric for WHAT IT MEASURES, not for the code section it lives in."
  },
  "ROUND_44_LATENCY_DECOMPOSED_THE_REAL_DEFECT_IS_A_QUEUE_NOT_CODE": {
    "re-measured_with_round_20s_exact_definition": "r44 first used min(token_snapshots.recorded_at) as discovery and got p50 122.6s against round 20's 8.05s - a 15x gap that was a DEFINITION difference, not a regression. Round 20 uses tokens.first_seen_at -> min(token_snapshots.observed_at) (r20_latency.py:30-40). Re-run verbatim: n=8,994, p50 96.94s, p90 1,767.94s, p99 8,549.44s, max 14,596s.",
    "THE_DECISIVE_MEASUREMENT": "Of the 8,994 observed tokens, ZERO have an observer frame as their first snapshot - all 8,994 got market data first. And every downstream stage is fast: observed->ingested p50 0.08s, ingested->recorded p50 0.02s, observed->recorded p50 0.77s (p90 8.44s), observed->decision p50 1.20s (p90 8.93s), observation->position opened p50 0.10s (p90 2.57s, max 8.01s). SO END-TO-END FOR A TOKEN THAT IS OBSERVED IS p50 ~1.30 SECONDS. The entire 97-second median sits BEFORE the first snapshot.",
    "conclusion": "The 'one real latency defect' (round 2's discovery->first-observation tail) is NOT a code defect - it is QUEUEING FOR AN OBSERVATION SLOT. TIMELINESS AND COVERAGE ARE THE SAME CONSTRAINT. Also: tokens has 14,205 rows but only 8,994 (63.3%) ever got a single snapshot - 36.7% of discovered tokens are never observed at all. No code fix addresses this; it is the round-29 user-decided observation budget (declined round 79, partially approved 83/88). Budget NOT touched.",
    "stability_re-checked_clean": "paper-supervisor.log unchanged at 483 runs / 34,342 bytes (mtime 70 min ago); runtime-crash.log unchanged at 22,904 bytes since round 33 - ZERO new writes, so the crash-loop mode is historical and the round-32/33 refused-start logging is working. Live path fresh: latest snapshot age 3s, evaluation 2s, mark-history 1s; 2,310 snapshots / 2,311 evaluations / 930 marks per 10 min, 13 opened, 73 closed."
  },
  "ROUND_44_CONSOLIDATED_REPORT_UPDATED_TO_V3": {
    "what": "docs/PROJECT_CONTEXT/FULLCHAIN_DIAGNOSIS_CONSOLIDATED_20260913.md was v2 and covered only rounds 120-1..120-13; rounds 14-44 were only in per-round records. Added section 10 (the v3 delta) and edited sections 0 and 4.",
    "section_4_corrected": "Section 4's 'the exit side is where the profit is' is a REALISED-ATTRIBUTION claim and stands - but it is an outcome-conditional statistic, not a counterfactual. Section 10.3 quantifies the difference: once entry filters are in place the take-profit's incremental value is ZERO. Both statements are true and future citations must say which one they mean.",
    "headline_re-attributed": "The stack's +17,620U belongs to the ENTRY FILTERS. Attributing any of it to exit-side take-profit is a misattribution.",
    "methodology_rules_now_15": "rules 7-15 added (outcome-conditional != counterfactual; an interval disagreeing with its own point estimate is buggy; audit the anchor the mechanism uses; filter windows by identity key before taking extremes; verify a group-by key's canonicalisation; decompose a derived discriminator; prefer production's recorded verdict; SQL col or 0 is a boolean; a component table needs the component-removed row).",
    "priorities_revised": "P0 experiment clock (report DIVERGED cohorts first); P0 full15 unreadable at 2 diverged; P1 the 882-position residual with NO fat cut point left; P1 dense-observation coverage now supported by TWO independent lines (4.4% get a 3-frame episode AND the p50 97s discovery delay) - user-decided budget."
  },
  "ROUND_42_ENTRY_TIME_LIQUIDITY_FLOOR_LEAD_CLOSED_AT_ZERO": {
    "the_lead": "All 818 pool-death write-offs carry dex_pool_liquidity_below_configured_floor_writeoff - the pool's FRESH liquidity fell under the already-configured shared floor (1000 USD default). The system applies that floor only at EXIT. If the pool were ALREADY under it at the entry decision, the -20U was determined before the position opened. And unlike round 26's market-cap cut this is not a cut point searched over this data - it is the system's OWN pre-existing configured constant on a point-in-time field.",
    "REFUTED": "818 of 818 pool-death write-offs had a decision snapshot (0 missing) and ZERO of them were below the floor: 90.6% were >=10,000U, 9.4% in 3,000-10,000U, none below 3,000. Per token (21 tokens produced all 818) the LOWEST decision-time liquidity was 7,967U, p25 20,147U, p50 41,167U. And the whole entry universe sits far above the floor: min 1,840U, p01 5,953U, p10 10,862U, p50 41,969U; below 2x the floor only 7 positions (0.3%), below 5x only 19 (0.7%). THE OBSERVATION UNIVERSE NEVER PRESENTS A SUB-FLOOR DECISION SNAPSHOT - the floor is a POST-entry event, not a pre-entry screen. Lead CLOSED, no code changed. (Not searching a higher liquidity cut point - that is the round-26 selection trap.)",
    "independent_corroboration_of_the_activity_floor_on_the_corrected_anchor": "Quadrant on cohort.source_snapshot_id: liquidity>=floor & trades>=30 n=1,623 -7,746.6U (-4.77U/pos) write-off 10.5%; liquidity>=floor & trades<30 n=1,136 -12,650.8U (-11.14U/pos) write-off 57.0%; liquidity<floor row EMPTY by construction. A 7.4pp write-off gap and 6.37U/pos, on a different anchor, field and method than round 19 - consistent with r40's 'activity floor only +11,811U'.",
    "MY_SQL_BUG_this_round": "r42's quadrant wrote `((ds.buys_5m or 0)+(ds.sells_5m or 0))`. IN SQL, OR IS A LOGICAL OPERATOR: `190 or 0` evaluates to 1, so every position's trade count came out as 1 or 2 and all 2,759 landed in the trades<30 bin - the table printed one row. COALESCE is the SQL form. In PYTHON `or` returns the operand itself, which is why the identical expression in round 41's probes was correct and the bug did not surface there.",
    "STANDING_RULE_new": "In SQL, `col or 0` is a boolean, not a null-coalesce - use COALESCE. Python's `or` has different semantics, so porting an expression between the two silently changes its meaning."
  },
  "ROUND_43_STANDING_RULE_new": {
    "15": "A component-attribution table MUST include the row with the evaluated component REMOVED. Without 'entry filters, no TP' the stack's +17,615U looked like a sum of its parts; that one row is what exposed the take-profit's incremental value as -5U."
  },
  "ROUND_41_DEPLOYED_ARMS_ARE_CONTRACT_COMPLIANT_ZERO_VIOLATIONS": {
    "what": "Round 33's discipline - verify a shipped mechanism in production - applied to all six arms shipped in rounds 17-20. Each makes a falsifiable promise; this checked every live position of each against it.",
    "result_zero_violations": "exit150_full15_v1 15/15 tier-1 exits closed via market_mark_take_profit_1 with remaining_quantity_tokens=0 (FULL capture confirmed, not assumed); exit150_full25_v1 10/10; activity_floor150_t30_v1 13/13 entries pass buys_5m+sells_5m>=30; activity_floor150_v5k_v1 12/12 pass volume_5m_usd>=5000; runup_floor150_r15_v1 15/15 pass run-up<=15% (max observed 13.29%); runup_floor150_r15a30_v1 10/10. Checkability is TOTAL for the exit arms: 24 = 15 tier-fired + 9 tier-never-fired (contract not applicable, NOT 'unknown').",
    "CROSS_CHECKED_AGAINST_PRODUCTION_NOT_MY_RECOMPUTATION": "store.py:28059-28063 writes each arm's verdict into feature_json.$.outcomes.<arm>, so the guard's own decision is queryable. The floors BITE: activity_floor_trades_not_met 203, activity_floor_volume_not_met 279, runup_floor_exceeded 172 (r15) and 172+activity_floor_trades_not_met 83 (r15a30) = 909 rejections. The 2026-09-12T22:12:13.697988Z decision records ALL FOUR floor arms as cohort_frozen_opportunity_ready (admitted), matching the four positions produced.",
    "live_tally_23:07Z": "exit150_full15_v1 n=24 -0.78U/pos; exit150_bank15_v1 n=30 -5.02U/pos; exit150_bank25_v1 n=30 -5.62U/pos; exit150_full25_v1 n=24 -4.30U/pos; exit150_widestop_v1 n=18 -3.56U/pos; activity_floor150_t30_v1 n=13 -4.07U/pos; activity_floor150_v5k_v1 n=12 -4.41U/pos; runup_floor150_r15_v1 n=15 -2.46U/pos; runup_floor150_r15a30_v1 n=10 -1.85U/pos. System healthy: 2,773 positions, 154 open, 60,602 snapshots, latest position 23:06:56Z."
  },
  "ROUND_41_MY_FIRST_AUDIT_REPORTED_FOUR_VIOLATIONS_THAT_WERE_BOTH_MINE": {
    "defect_A_wrong_anchor": "r41 read positions.entry_snapshot_id and entry_execution_price_usd. The contract reads the DECISION snapshot (store.py:27955-27957 uses the frame being decided on = chain_meme_trader_v6_cohorts.source_snapshot_id). entry_execution_price_usd carries the 4% BUY_SLIP, so my run-up was inflated by ~4 percentage points - at a 15% cap, any true run-up above ~10.6% falsely flagged. Twelve of the fifteen r15 positions showed runup_ex% of exactly 4.00%, i.e. pure slippage.",
    "defect_B_missing_the_pool_filter": "store.py:27662-27673 keeps only the DECISION'S OWN pool (canonical_token_address(chain, pair.pairAddress)). My SQL filtered token + cohort_observer + time only. solana:L2mVBBTJZ... was observed at ONE instant (2026-09-12T22:09:20.465748Z) in TWO pools priced 1.0886e-05 (id 46377, pool 4U8iA9GS...) and 3.7230e-03 (id 46378, pool 3i2C1MQ5...) - 342x apart. Without the pool filter history[0] took the stale pool and produced +35431%; production took 3.7230e-03 and produced +3.8944% -> ADMIT, exactly as its own recorded verdict says.",
    "THE_POOL_FILTER_IS_LOAD_BEARING_not_decorative": "Remove it and the run-up cap refuses almost every entry as 'already run up'. Multi-pool tokens are 271 of 5,416 observed (5.0%); cross-pool average-price disagreement >=100x on 14 tokens. And grouping by the RAW address string FABRICATES pools: 219 token-pool rows are one EVM address differing only in EIP-55 casing, which split one pool into two and invented 1.78e6x 'cross-pool disagreement' until canonical_token_address was applied."
  },
  "ROUND_41_PRICE_INTEGRITY_HYPOTHESIS_RAISED_AND_REFUTED": {
    "the_hypothesis": "Worst 20-minute windows spanned up to 319,016x on ONE pool, and low-end prices recurred at ~1.8e-06 across UNRELATED Solana tokens - a real price does not cluster like that. Hypothesis: some frames put priceNative (quote-denominated) into price_usd, understating the USD price by the quote asset's USD value. Discriminator: price_usd/priceNative must equal the quote token's own USD price.",
    "REFUTED_BY_CONDITIONING_ON_THE_QUOTE_TOKEN": "37,141 observer frames: VOLATILE quote (SOL/WBNB), ratio should be the quote's USD price - median 726.2 and only 11 frames (0.04%) below 2. STABLE quote (USDT/USDC/USDG), ratio~1 is CORRECT - 3,952 of 3,960 (99.80%). So 6,466 of the 6,477 frames my unconditioned test flagged were NORMAL stablecoin-quoted pools. bsc:0x0c886587... (161 write-offs at entry price 0.0002384) is quoted in USDT, so priceNative==priceUsd is right; its -3,220U is the known POOL-DEATH channel (r36/r39), not a price defect. SECOND_TIME_THIS_SESSION a derived metric had to be decomposed against its underlying quantity before being believed.",
    "residual_is_zero_impact": "15 frames (0.04% of 37,241; all geckoterminal; prices down to 1e-21) are internally inconsistent on a volatile-quote pool - the signature of a DRAINED pool's residual price. Traced to money: 0 used as a position entry snapshot, 0 as a cohort decision snapshot, 0 referenced by chain_meme_trader_marks.snapshot_id, 0 prices present in chain_meme_trader_market_mark_history. MONITORING ITEM with falsifier = any such frame becoming a decision or mark snapshot. NO CODE CHANGED."
  },
  "ROUND_41_STANDING_RULES_new": {
    "9": "To audit a deployed mechanism, take the anchor THAT MECHANISM uses - not the latest frame and not the fill frame. Slippage sits between decision and fill, and it is large enough to turn compliance into an apparent violation.",
    "10": "Window statistics must be filtered by identity key (pool/pair) BEFORE taking extremes; one stale second pool fabricates an arbitrary multiple.",
    "11": "Verify a group-by key's CANONICALISATION before trusting the grouping: EIP-55 casing alone split one pool into two.",
    "12": "Decompose a derived discriminator against the underlying quantity before citing it: ratio~1 is CORRECT for stablecoin-quoted pools, so the unconditioned version reported 99.9% false positives.",
    "13": "When production records its own verdict, use that instead of a recomputation; recomputation explains a divergence, it does not convict."
  },
  "ROUND_40_DEPLOYED_STACK_PROJECTION": {
    "what": "Rounds 17-39 measured each deployed mechanism separately. Round 20 priced a combination on 1,710 positions (-1,540U); the population has since grown 2.4x. This re-prices the stack once on the current population with a proper interval.",
    "the_projection": "2,397 positions with a usable mark series. Levels as deployed: TP 100% at +15% econ; activity floor buys_5m+sells_5m >= 30; run-up cap <=15% over the 20 min before entry. actual -18,803.3U (-7.84U/pos); +TP only -8,499.6U (+10,303.6); activity floor only -6,992.0U (+11,811.3); floor+TP -4,187.0U (+14,616.3); FULL STACK (floor+run-up+TP) 603 positions -1,434.1U (-2.38U/pos) = +17,369.2U. stack-actual reconciles exactly.",
    "robustness": "token-clustered 90% bootstrap CI [+12,074, +22,695]U EXCLUDES ZERO. Drop best 1 token +15,850U; best 2 +14,577U; BEST 3 +13,332U; BEST 5 +10,973U. A 92.4% reduction in the epoch loss that survives removing the five best tokens entirely.",
    "HONEST_HEADLINE": "Even the full deployed stack STILL LOSES on this epoch: -1,434.1U. It is a 92% reduction, NOT a profit. And every level (+15%, trades>=30, run-up<=15%) was chosen on THIS SAME epoch, so the combination compounds in-sample selection. It projects the DEPLOYED DESIGN; it is not a result.",
    "encouraging_stability_sign": "Round 20 priced essentially the same combination at -1,540U on 1,710 positions; this round prices it at -1,434U on 2,397. The projection barely moved across a 2.4x population increase - what a stable relationship looks like rather than a fitted artifact. But both come from the same epoch and are not independent.",
    "BUG_I_CAUGHT_IN_MY_OWN_BOOTSTRAP": "The first run reported a stack delta of +590U with CI [-394,+1,858] excluding zero = False, which CONTRADICTED the table's +17,365U in the same output. A CI and point estimate disagreeing by 30x is a bug, not a nuance. CAUSE: I computed the position-wise delta as sim-pnl with sim=pnl for filtered-OUT positions, i.e. counting 0 for a position the stack does not trade. But NOT TRADING A LOSING POSITION MEANS AVOIDING THAT LOSS, so the correct delta there is -pnl. The error undercounted the gain by exactly the avoided losses, which is most of it. Fixed; the corrected figure reconciles to the decimal. Recorded because a plausible-looking CI silently disagreeing with its own headline is exactly this session's recurring failure mode - and here the disagreement was the ONLY signal.",
    "STANDING_RULE_new": "When a point estimate and its own interval disagree by an order of magnitude, the interval is buggy - reconcile them before believing either."
  },
  "ROUND_40_full15_REACHED_PER_SIDE_THRESHOLD_BUT_IS_NOT_READABLE": {
    "the_numbers": "exit150_full15_v1 settled=20 (+0.07U/pos, win 80.0%, write-off 5.0%); control exit150_bank15_v1 settled=23 (-6.08U/pos, 26.1%, 47.8%).",
    "BUT": "The per-side gate is satisfied (20 >= 20) and round 35's rule applies: THE USABLE BASIS IS DIVERGED COHORTS, OF WHICH THERE ARE 2. So the correct statement is 'the unpaired contrast is +6.15U/pos in favour of full15; the paired basis is 2 diverged cohorts' - NOT A VERDICT. Exactly the situation rounds 34-35 were built to catch, and it was caught."
  },
  "ROUND_39_ROUND_36s_CEILING_IS_REFUTED": {
    "the_claim_tested": "Round 36 recorded as a STANDING CONCLUSION that '58.5% of exits are forced by the pool dying or a one-interval gap, and NO stop width, ladder level, trailing rule or time limit can act on those'. Round 38 had shown the pool-death population peaked at a MEDIAN +15.2% econ - and a take-profit at +15% closes the position BEFORE the pool dies. So at least some of the 'forced' share should be touchable.",
    "IT_IS_REFUTED": "Peak econ by the mechanism that ACTUALLY closed the position (2,393 replayed): POOL DEATH n=760 peak p25 +4.4% p50 +15.2% p75 +22.7%; HARD STOP n=583 peak p25 -12.4% p50 -7.9% p75 +2.7%; elective n=1050 peak p50 -0.7%. 52% OF THE POOL-DEATH POSITIONS REACHED +15% BEFORE DYING - they were not killed by the market, they were killed by the rules sitting below where the position had already been.",
    "the_conversion_test": "+10% -> 607 converted, +12,044.6U. +15% -> 484 converted (393 pool deaths + 91 hard stops), +10,303.6U. +20% -> 311, +6,886.8U. +30% -> 168, +4,194.7U. AT +15%, decomposed: pool death 393 converted, actual -7,499.0U -> +1,489.3U = +8,988.3U; hard stop 91, -1,248.3U -> +313.9U = +1,562.2U; elective 304, +1,689.8U -> +1,442.9U = -246.9U. So +8,988U of the gain comes from POOL DEATHS - precisely the population round 36 called untouchable. Cost on the elective side is only -247U.",
    "THE_CORRECT_CEILING": "Genuinely untouchable = forced AND never reached the target: pool deaths never up 367 of 760 + hard stops never up 492 of 583 = 859 of 2,393 = 35.9%. SO THE TOUCHABLE SHARE IS ~64.1%, NOT 41.5%. Round 36's number was a property of where the current rules sit, not of the market.",
    "THE_PRECISE_ERROR": "Round 36 read the REALIZED close reason as evidence about what rules could do. The realized mechanism is CONDITIONAL ON THE RULES IN PLACE - change the rule and the mechanism changes. Same class of mistake as the earlier gate refinements, in a new place: AN OUTCOME-CONDITIONAL STATISTIC USED AS A COUNTERFACTUAL.",
    "ROBUSTNESS_much_stronger_than_round_17": "Round 17's version gave +4,178U on 906 positions with 83% of the gain in 3 of 24 tokens, and dropping the best three left only +715U. On the current population (2,393 positions, 43 tokens the same replay gives: gain +10,303.6U; TOKEN-CLUSTERED 90% BOOTSTRAP CI [+5,697, +15,068]U EXCLUDES ZERO; 20 of 43 tokens gain; top-1 token 14.0% of the gain; DROP BEST 1 +8,861U / BEST 2 +7,618U / BEST 3 +6,416U; 556 positions gain vs 232 lose; top-10% of positions = 55.6% of the gain. THIS IS THE STRONGEST POSITIVE RESULT OF THE SESSION.",
    "honest_limits": "Replay, in-sample on this epoch; assumes execution at the first mark at or above the target (the system's own hard-stop semantics - internally consistent but not forward). DO NOT RE-OPTIMISE THE LEVEL on this sample: the +10% row (+12,044.6U) beats +15%, but choosing the target from the same data is the round-26 selection trap. The deployed arms use +15%/+25%, chosen in round 17 on a different sample - hold the level fixed. Gain is concentrated (top 10% of positions = 55.6%). It is a GROSS figure.",
    "what_it_means_for_deployed_work": "exit150_full15_v1 is ALREADY DEPLOYED and targets exactly this (sells 100% at +15%). Its observed behaviour is consistent - 5.3% write-off vs bank15's 47.8% - because it converts pool deaths into take-profits before the pool dies. Its forward verdict is still BLOCKED ON THE PAIRING CLOCK (19 settled, 2 diverged cohorts); this replay strengthens the case for the DESIGN without substituting for the forward test."
  },
  "ROUND_38_ENDGENEITY_HYPOTHESIS_TESTED_AND_REFUTED": {
    "the_hypothesis": "Round 36 concluded 'the exits work' from the forced/elective split (forced -20,332U, elective +1,348U). But that split is ENDOGENOUS - a position exits electively only if it SURVIVED long enough for a TP/trailing/time rule to fire. So the elective profit could be SELECTION (survivors) rather than exit skill, and the claim would be too strong.",
    "IT_WAS_WRONG": "FORCED positions peaked at median +7.3% econ; the ELECTIVE population at -0.8%. THE FORCED POPULATION PEAKED HIGHER. The typical elective-exit position never went meaningfully positive. So the elective side's +1,344U is NOT a selection effect on peak quality - the hypothesis is REFUTED and round 36's directional claim survives this challenge.",
    "value_capture_by_mechanism": "FORCED pool died n=760 peak +15.2% exit +1.2% give-back 0.0% total 5,261.5U PnL -14,839.0U. FORCED hard stop n=562 peak -7.9% exit -25.8% give-back 16.8% total 3,410.6U PnL -4,902.4U. elective time exit n=663 peak -2.5% exit -3.2% PnL +585.8U. ELECTIVE TRAILING n=171 peak +88.5% exit +55.7% give-back 14.8% total 800.0U PnL +951.8U. elective signal-decay n=145 PnL -243.8U. elective take-profit n=24 PnL +110.5U.",
    "THE_ELECTIVE_SIDE_IS_THIN_AND_TAIL_DRIVEN": "The entire elective PnL (+1,344U) rests on 171 trailing exits (+952U); time exits add +586U, decay subtracts -244U. CONCENTRATION WITHIN MECHANISMS: for elective time exit the TOP 10% OF POSITIONS PRODUCE 508.6U OF THE 585.8U = 87% of the profit from a tenth of the population; for trailing 298.5U of 951.8U (31%). Median peak on the elective side is -0.8%, so most positions go nowhere and a small runner tail carries everything - consistent with r17 (only 5 of 906 reached +100%) and r25.",
    "THE_GIVE_BACK_IS_NOT_RECOVERABLE_HEADROOM": "Trailing exits hand back a median 14.8 percentage points (800U total), which LOOKS like the 'golden dog' leak. IT IS NOT RECOVERABLE: capturing the peak requires knowing the peak, a future function. Round 17 already measured that a full-capture take-profit at +15% earns LESS on the runner tail than trailing does while earning far more on the round-trippers. The 800U is the PRICE of the trailing rule, not a defect in it. The forced hard stops' 16.8pp median give-back (3,410U) is the r17/r35 gap-through and is likewise not recoverable.",
    "CORRECTED_STATEMENT_of_r36": "58.5% of exits are forced by mechanisms no exit contract can influence, and that is where the entire loss sits. The remaining 41.5% is net positive - and it is positive DESPITE having WORSE peak economics than the forced population, so it is NOT a survivor-selection artifact. But it is thin (top 10% of time-exits carry 87% of their profit) and its largest apparent leak (800U of trailing give-back) is the unavoidable price of not knowing the peak, not headroom. The ceiling conclusion is unchanged; what changes is that 'the exits work' is now a TESTED claim rather than an inference from an endogenous split."
  },
  "ROUND_36_THE_EXIT_SIDE_IS_WHERE_THE_PROFIT_IS": {
    "the_measurement": "2,463 settled positions classified by close_reason. FORCED pool died (write-off) 764 = 31.0%, -14,919.0U. FORCED hard stop 678 = 27.5%, -5,413.2U. FORCED TOTAL 1,442 = 58.5%, -20,332.2U. Elective time exit 644 = 26.1%, +611.1U. Elective trailing 167 = 6.8%, +930.8U. Elective signal-decay 145 = 5.9%, -243.8U. Elective take-profit 24 = 1.0%, +110.5U. Elective runner review + custom kinds 41 = 1.7%, -61.4U. ELECTIVE TOTAL 1,021 = 41.5%, +1,347.8U. Reconciles: -20,332.2 forced + 1,347.8 elective = -18,984.4U, the epoch's loss.",
    "FINDING_1_the_ceiling": "Only 41.5% of settled positions exit by a mechanism an exit contract actually decides. The other 58.5% are FORCED by the pool dying or by a one-interval gap crossing every configured level simultaneously. No stop width, ladder level, trailing rule or time limit can act on those. EXIT-SIDE RESEARCH HAS A HARD CEILING OF ROUGHLY THE ELECTIVE SHARE - and round 35 showed that even inside it most cohorts resolve identically because the two arms' rules coincide.",
    "FINDING_2_the_sign_change": "forced exits -20,332.2U vs elective exits +1,347.8U. THE ENTIRE LOSS SITS ON POSITIONS WHOSE EXIT WAS FORCED. WHERE THE EXIT CONTRACT IS ACTUALLY IN CONTROL, THE SYSTEM IS NET PROFITABLE. So the exit logic is NOT the problem and NOT a promising target - THE EXITS WORK. The positions that die are the problem, and a position dies because of WHICH TOKEN was entered, not how it was exited. That is an ENTRY problem.",
    "fourth_independent_measurement_pointing_the_same_way": "r17 replay: no stop level helps, the collapse is an 11s gap-through. r25 variance decomposition: token 56.9%, arm only 14.6%. r29: 4.4% of observed tokens get dense observation and those convert at 18x. r36: forced exits -20,332U vs elective +1,348U. COMBINED: the exit side is where the profit is; the loss is entirely an entry-selection problem, and entry selection is capped by dense-observation coverage (4.4%), which is a user-decided budget item.",
    "supporting_detail_the_hard_stop_gaps_a_third_of_the_time": "Of the 678 forced hard stops, realised/stake: p10 -0.911, p50 -0.263, p90 -0.197. A -0.20-economic stop implies about -0.183 at the level. 219 of 678 (32%) realised worse than -0.35/stake - CLEARLY GAPPED THROUGH - with p10 near -0.91 (essentially total loss). Round 17's gap-through quantified on the whole epoch rather than a sample, and why widening the stop changed nothing (r35: 0 of 10 cohorts diverged).",
    "STANDING_CONCLUSION_new": "Exit-side work faces a 41.5% ceiling and the elective share is ALREADY NET POSITIVE. Do NOT propose further exit variants without saying what in the forced 58.5% they could possibly change - normally the answer is nothing.",
    "RECONCILED_independently_in_SQL": "forced -20,332.2U (1,442) and elective +1,346.7U (1,025) with forced+elective==total exactly. The finding is not sensitive to the classification (the only difference is four 'other' custom-kind closes classified on slightly different keys).",
    "THE_CEILING_IS_MOVING_favourably": "whole epoch 2,467 positions: forced share 58.5%, forced -20,332.2U, elective +1,346.7U. LAST 2 HOURS, 1,847 positions: forced share 53.4%, forced -13,850.1U, elective +1,438.9U. So the elective share is rising (41.5% -> 46.6%), consistent with the EXIT150 arms accumulating elective take-profit and trailing exits, and THE RECENT WINDOW'S ELECTIVE PnL ALONE (+1,438.9U) EXCEEDS THE ENTIRE EPOCH'S (+1,346.7U). CAVEAT: the recent window is a SUBSET of the epoch - these are not independent samples. Worth watching, not banked."
  },
  "ROUND_35_FIRST_PAIRED_COMPARISON_IS_UNINFORMATIVE": {
    "finding_a_properly_paired_pair": "full15/full25 share only 3 cohorts with bank15/bank25 (different frontiers 32175 vs 15928), but bank15/bank25/widestop ALL joined at 15928 so they DO share cohorts. Running at per-side threshold 10 exposed the session's first properly paired comparison: widestop vs bank15, 10 shared settled cohorts across 10 tokens, within-cohort difference -0.138U, 90% token-clustered CI [-0.414, +0.000], does NOT exclude zero - against an UNPAIRED arm-level gap of 2.27U/pos.",
    "BUT_10_SHARED_IS_REALLY_1_THEN_0": "Breaking the 10 pairs down by whether the two arms' CLOSE REASONS differ: NINE have a difference of exactly 0.00 and ONE differs (-1.38U). Seven are dex_pool_liquidity_below_configured_floor (a write-off, forced by the pool dying regardless of any stop) and two are market_mark_hard_stop where BOTH arms realise the SAME PnL under a -0.35 and a -0.55 stop (solana:3wEecdaDc -19.99 both; solana:Dw4hxvotjTZ -18.19 both).",
    "THE_MECHANISM_is_round_17s_gap_through_from_a_new_angle": "A collapsing pool gaps past BOTH stop levels inside one mark interval, so the stop width never gets to matter. The single differing cohort has the SAME close reason on both sides (market_mark_trailing_exit), i.e. a small mark variation rather than a different rule firing. So the effective basis for the stop-width question is ZERO informative cohorts.",
    "THE_GATE_WAS_STILL_COUNTING_THE_WRONG_THING": "Round 34 fixed 'count each side's total' -> 'count shared cohorts'. This round shows SHARED IS STILL NOT ENOUGH: cohorts that resolve identically carry no information about the contract being varied. The correct basis is the cohorts where the two arms DIVERGE. The readout now reports and gates on that: full15 vs bank15 = 3 shared of which 2 diverged; full25 vs bank25 = 3 shared of which 1 diverged; WIDESTOP VS BANK15 = 10 SHARED OF WHICH 0 DIVERGED. Default --min-paired 20 now means 20 DIVERGED cohorts.",
    "WHAT_THIS_CORRECTS": "'WIDESTOP IS WORSE' IS NOT SUPPORTED. Carried out of rounds 16, 17 and 23 on unpaired arm-level means (-8.35 vs -6.08U/pos) and round 17's price replay. The paired evidence says the stop width has NEVER CHANGED AN OUTCOME in 10 shared cohorts. The 2.27U/pos gap is COHORT MIX, not the contract. The wide-stop hypothesis is therefore not merely unsupported - it is STRUCTURALLY UNTESTABLE in this market, because the terminal move is a gap-through that crosses both levels at once. That is a stronger and more useful statement than 'we need more data'.",
    "also_noted": "full25 moved 17 -> 18 settled and -5.45 -> -4.98U/pos in one interval, again showing how little a ~18-position mean should be trusted."
  },
  "ROUND_35_PATTERN_THREE_ROUNDS_OF_GATE_REFINEMENT": {
    "table": "r27: write-off rate decomposed into full-loss vs partial -> the stated mechanism was wrong. r34: per-side count -> SHARED cohorts -> the headline contrast is unpaired. r35: shared -> DIVERGED cohorts -> stop width is untestable and a standing claim is withdrawn.",
    "lesson": "In each case the DIRECTION survived and the STRENGTH OF CLAIM did not.",
    "STANDING_RULE_new": "Before reading a paired experiment, COUNT THE UNITS WHERE THE THING BEING VARIED ACTUALLY CHANGED THE OUTCOME. Shared units are not usable units."
  },
  "ROUND_34_THE_EXIT150_HEADLINE_CONTRAST_IS_UNPAIRED": {
    "what_happened": "exit150_full15_v1 reached 19 settled, one short of the 20-per-side threshold. To exercise the analysis path before it produced a 'verdict', the readout was run with --min-settled 19. It produced a within-cohort difference of +5.866U with a token-clustered CI [+1.793,+9.939] that 'excludes zero' - RESTING ON THREE COHORTS, where dropping two tokens leaves a single observation with a difference of exactly 0.000U. Reported without that context it would have been meaningless.",
    "the_gate_was_counting_the_wrong_thing": "experiment_readout.py gated on EACH SIDE'S OWN settled count, but a paired test needs SHARED cohorts. Two exit carriers registered at DIFFERENT FRONTIERS can each clear 20 settled while sharing only a handful. That is exactly the case: exit150_full15_v1/full25_v1 joined at frontier 32175, while bank15/bank25/widestop joined at 15928. THEY SHARE ONLY 3 SETTLED COHORTS ACROSS 3 TOKENS.",
    "fixed": "the readout now takes --min-paired (default 20) and REFUSES the paired verdict when the shared basis is too thin, additionally stating the per-position figures are UNPAIRED. Verified both ways: refuses at 3 cohorts, default run unchanged for the other experiments.",
    "consequence_for_the_headline_I_have_carried_since_r17": "The contrast 'full15 -0.00U/pos / 78.9% win / 5.3% write-off versus bank15 -6.08U/pos / 26.1% / 47.8%' is UNPAIRED. The two arms traded DIFFERENT COHORTS ON DIFFERENT TOKENS, so the difference is between two different opportunity sets, not between two exit contracts applied to the same opportunities. It remains SUGGESTIVE and directionally consistent with round 17's replay - and it is NOT the controlled comparison the exit-carrier design is meant to provide. The matched-control property holds only for cohorts a pair BOTH joined, which for a frontier-differing pair accumulates slowly.",
    "what_the_next_verdict_must_say": "When full15 clears 20 settled, the correct statement is 'the paired basis is still N cohorts; the unpaired contrast is X' - NOT a verdict. The paired test needs ~20 SHARED cohorts, a different and slower clock.",
    "instability_reminder": "full15's per-position figure moved +0.41U -> -0.00U on the addition of a SINGLE settled position. That is the appropriate confidence to have in a 19-position mean."
  },
  "ROUND_33_REFUSED_START_FIX_VERIFIED_END_TO_END": {
    "why_verify_instead_of_explore": "Round 32 shipped a cli.py change but the running trader predates it, so nothing had exercised the new code. Every diagnostic line is now closed by measurement, so the highest-value action was to confirm the one change I shipped actually works.",
    "the_verification_done_safely_in_production": "The single-instance lock means a second trader correctly refuses to start, so attempting one is a ZERO-RISK way to exercise the exact fixed path - the second process never gets past the lock and cannot write to the database. Lock file present (data/memetrader.lock, 1 byte). Crash log 22,020 -> 22,904 bytes. Second instance exited 3 with 'another memeTrader process is already running'.",
    "the_fix_WORKS": "The new log entry contains the FULL FRAME CHAIN including the chained cause: runtime.py:810 msvcrt.locking -> PermissionError [Errno 13] Permission denied -> runtime.py:817 raise RuntimeError -> cli.py:123 with SingleInstance(lock_path) -> RuntimeError. A bare message would NOT have been diagnosable; the chain is what identifies the mechanism.",
    "THE_447_RUN_MODE_IS_NOW_EXPLAINED_NOT_JUST_COUNTED": "msvcrt.locking(handle, msvcrt.LK_NBLCK, 1) raises PermissionError [Errno 13] when another process holds the byte range, and SingleInstance.__enter__ converts that into the RuntimeError. So the 447 short exit=3 runs across the two bursts were THE LOCK BEING HELD at startup - a stale holder or a genuine overlapping start - and each restart failed within ~0.5s exactly as observed. Round 31 could not see this because the path logged nothing; round 32 made it visible; round 33 confirmed the visibility works and read the mechanism off it.",
    "residual_limit": "WHY the lock was held in those two windows cannot be recovered retroactively. Windows byte-range locks are released when the holding handle closes, so a merely-killed process should not hold it - pointing at either an overlapping start or a process alive but not the trader. The next occurrence will be directly diagnosable from the log.",
    "deployment_note": "The verification exercised the CURRENT cli.py via a directly-invoked instance. The live trader started 06:01 and still carries the pre-r32 cli.py; the change takes effect on its next restart. No restart was performed - it is not needed and would clear the running process's in-memory observation state for no benefit."
  },
  "ROUND_32_PROVIDER_RELIABILITY_NOT_THE_BOTTLENECK": {
    "measured": "mark history (19,590 rows) 99.7% VISIBLE, 0.3% MISSING:NO_VISIBLE_POOL_OR_PRICE; live per-pool marks 99.2% VISIBLE; consecutive_misses ZERO for 1,397 of 1,398 pools; last_success_at IS NULL for 0 of 1,398; staleness p50/p90/p99 all 0s (max 4,350s, one pool); dexscreener 99.7% ok, geckoterminal 100%.",
    "only_anomalies": "failures concentrate in a SINGLE pool (68 consecutive misses); plus 9 pools with DATA_REJECTED:QUOTE_USD_UNKNOWN,quote_liquidity_unavailable and 1 with TimeoutError.",
    "conclusion": "Source reliability is NOT part of the coverage bottleneck. Combined with round 29 (95.6% of observed tokens never get a 3rd frame), the frames are not LOST TO FAILURES - they are never REQUESTED. The observation gap is a budget/admission issue, not a data-quality one. The remedy is unchanged."
  },
  "ROUND_32_CORRECTION_round_31s_crash_attribution_was_WRONG": {
    "the_mechanism": "cli._run_runtime has TWO failure paths with DIFFERENT exit codes: 'except RuntimeError' (a REFUSAL TO START, most often the single-instance lock) returns 3; 'except Exception' (a real crash, e.g. httpx.InvalidURL) returns 1.",
    "the_measurement_short_runs_under_5s_by_exit_code": "00:00 -> exit1=5, exit3=377. 04:00 -> exit1=0, exit3=64. TOTALS: exit3=447 vs exit1=10.",
    "the_correction": "The DOMINANT startup-failure mode is exit=3 - the single-instance lock refusing to start - at 447 runs vs 10 real crashes. runtime.py:817 raises RuntimeError('another memeTrader process is already running') and that path wrote NOTHING to any log. ROUND 31 ATTRIBUTED THE 382-RUN AND 64-RUN BURSTS TO THE httpx CRASH. THAT WAS WRONG. The error was mistaking a coincident timestamp for causal attribution: runtime-crash.log's last write was 00:00:27, inside the 00:00 burst window, but that burst contained BOTH 5 httpx crashes (which wrote the log) and 377 lock refusals (which could not). The exit code had already separated the two modes and I did not use it.",
    "what_round_31_got_right": "the httpx.InvalidURL: Invalid port ':1]' defect is real, reproduced, isolated to the bracketed [::1] in NO_PROXY, and ALREADY FIXED by trust_env=False (commit 2064db9). It is simply a MINOR mode - 10-20 events, not 446."
  },
  "ROUND_32_FIX_SHIPPED": {
    "what": "The RuntimeError path now records its traceback to the same runtime-crash.log via a new shared helper _record_runtime_crash(root, detail). Exit code (3), stderr line and all other behaviour unchanged. The helper never raises - logging must not become the reason a process fails to report its own failure.",
    "why_it_is_justified": "It names a concrete OBSERVED failure: 447 unlogged startup failures, the dominant mode by 45x. Previously a supervisor restart loop showed an exit code and nothing else.",
    "tests": "tests/test_cli_crash_reporting.py, 6 tests: a refused start records its FRAME CHAIN (a bare message is not diagnosable), the log appends rather than truncates, and a logging failure is swallowed."
  },
  "ROUND_31_STABILITY_THE_LARGEST_HISTORICAL_DEFECT_EXPLAINED": {
    "why": "Stability is one of the five dimensions the objective names and it had not been audited this session.",
    "the_log": "data/logs/paper-supervisor.log holds 483 run records: exit=3 on 455, exit=1 on 15 (my own taskkill restarts), exit=-1 on 13. 457 of 483 runs lasted UNDER 5 SECONDS - startup failures restarted every 5s. Bursts at ~00:00 (382 runs) and ~04:48 (64 runs). Recent runs are healthy and long (463s -> 1249s); longest on record 2672s. So it is a HISTORICAL recurring crash-loop mode, not a current one.",
    "ROOT_CAUSE": "runtime-crash.log (last written 00:00:27, matching the 382-run burst) has 10 identical tracebacks ending in httpx.InvalidURL: Invalid port: ':1]' via httpx/_client.py:1412 -> collectors.py:1263 (_new_client). httpx builds one URLPattern per NO_PROXY entry when constructing a client, and ANY construction failure kills the process at startup.",
    "REPRODUCED_AND_ISOLATED": "The live environment has NO_PROXY='openreview.net,...,localhost,127.0.0.1,::1,[::1]'. httpx.AsyncClient() with it RAISES InvalidURL: Invalid port: ':1]' - reproduced exactly. Per-entry isolation: NO_PROXY='[::1]' ALONE raises; every other entry alone is fine and the unbracketed ::1 is fine. SOLE CULPRIT: the bracketed IPv6 loopback [::1], which httpx reads as host '[' with port ':1]'.",
    "IT_IS_ALREADY_FIXED_no_new_code_shipped": "trust_env=True (httpx default) RAISES; trust_env=False is OK. EVERY httpx client construction in the current code sets trust_env=False (three in collectors.py, one via _client_options, one in cli.py), verified in the committed version at HEAD. trust_env=False arrived in commit 2064db9 'port the runtime to a partially filtered host: explicit egress + sanitized launcher' - exactly the change that removes host-proxy inheritance.",
    "I_wrote_a_fix_then_DELETED_it": "I wrote src/memetrader/egress_env.py (an in-process NO_PROXY normaliser unbracketing bare IPv6 entries), verified it against 8 cases including [::1]:8080 and None, and confirmed it preserved bypass intent - THEN DELETED IT. The current code is immune, so the repair would have changed nothing, and the project rules are explicit that a defensive mechanism must name the observed failure it changes. NO SOURCE FILE WAS MODIFIED this round.",
    "two_of_my_own_probes_were_wrong_first": "1) the entry-isolation probe used httpx.URLPattern, not exported at top level, so EVERY entry looked unparseable and the 'drop the bad ones' test silently cleared the whole NO_PROXY list instead of isolating one entry. 2) a regex scan for clients 'lacking trust_env' captured only the first line of multi-line calls, reporting two false positives plus one on my own scratch file. The per-entry AsyncClient() test - the behaviour that actually matters - settled it.",
    "watch_item": "the crash loops are ENVIRONMENT-DEPENDENT: a malformed NO_PROXY in whatever launches the process. If startup failures recur, check that variable first. The 04:48 burst is not fully explained (the crash log was last written at 00:00), low priority now the current code is immune."
  },
  "ROUND_30_DENSE_EPISODE_FREE_LEAD_CLOSED": {
    "the_lead": "Round 29 flagged the 11 entered tokens with no >=3-frame episode as the most promising remaining lead, BECAUSE widening it would need no observation budget.",
    "IT_DOES_NOT_SURVIVE_CONTACT": "All 11 are the native_protocol_model lane - 100%. 11 tokens, all solana:...pump, exactly 1 POSITION EACH, realised -10.8U over 11 positions (-0.98U/pos). Closes: 9 hard_stop (-12.83U), 1 trailing (+2.64U), 1 max_hold (-0.40U). By contrast the 37 dense-episode tokens are isolated_cohort_observer 2,485 (98.7%) + isolated_pattern_observer 33.",
    "why": "Their token-level observation is near-zero (0-4 snapshots each, 0-1 observer frames), so these tokens are not 'undensed' - they are TRADED ON AN ENTIRELY DIFFERENT EVIDENCE BASIS. Not a coverage route widenable within the DEX-pool observation architecture; a separate protocol lane with one position per token and no demonstrated edge.",
    "round_29_caveat_resolved": "Dense observation is the route for 37 of 48 entered tokens (77%); the other 23% arrive through a lane that is neither free to widen nor profitable on the evidence so far."
  },
  "ROUND_30_ANALYTICAL_HAZARD_native_lane_is_a_join_dead_zone": {
    "the_observation": "All 11 native-lane positions carry entry_snapshot_id = 0 (NULL or dangling) with entry_reason = later_observed_protocol_model_paper. The cohort lane by contrast is CLEAN: 2,485 positions, 0 NULL, 0 dangling.",
    "NOT_a_defect": "later_observed_protocol_model_paper names next-observed / trigger-anchored execution explicitly, which is the documented and frozen semantic. The position is valued on the NEXT observation rather than an as-of entry snapshot. Do not report this as missing evidence.",
    "but_IT_IS_an_analytical_dead_zone": "These positions CANNOT be joined to entry-time market features at all. Any query joining positions to token_snapshots on entry_snapshot_id SILENTLY DROPS THEM - a 0-row result that looks like 'no data' rather than 'different lane'. This is the same trap that bit an earlier round. STANDING NOTE: for any entry-feature query, STATE WHICH LANE IT COVERS."
  },
  "ROUND_30_DIAGNOSIS_IS_COMPLETE": {
    "every_link_measured_and_every_candidate_eliminated": "discovery healthy; collection healthy (94.5% observed); feature/trajectory healthy (66.8% form ready 30s windows); processing speed NOT binding (p50 0.18s observed->position); freshness rules NOT binding (0.03% of evaluations); signal generation NOT binding; arm fleet not redundant (311 contracts) and not prunable (r25); token-level exclusion unvalidatable at n~40 (r26); capital/concurrency/pending intents NOT binding (1.35% utilised, r28); slot turnover not a lever (r29); the dense-episode-free lane not widenable and no edge (r30).",
    "THE_ANSWER_TO_THE_OBJECTIVES_CORE_QUESTION": "No threshold, logic rule, risk rule or processing limit is systematically blocking tradeable candidates. Dense observation converts at 7.02% (37 of 527) against 0.39% overall - an 18x difference - so the system is not failing to convert what it can see, it is failing to GIVE CANDIDATES ENOUGH OBSERVATION TO BE JUDGED. THE BINDING CONSTRAINT IS DENSE-OBSERVATION COVERAGE: 4.4% of observed tokens (527 of 11,991).",
    "what_remains_honestly": "The binding constraint is NOT mine to change - it is a user-decided budget item (increase declined round 79; partial increases approved rounds 83 and 88) and the completed evidence base is now the deliverable on that point. Six forward experiments are in flight and NONE is readable yet; exit150_full15_v1 is closest at 17 of 20 settled and positive per position. Their verdicts need WALL-CLOCK TIME, not more analysis. Everything else in my authority was either shipped or closed by measurement."
  },
  "ROUND_29_SLOT_TURNOVER_CLOSED_AS_A_LEVER": {
    "the_candidate": "replaceable_early evicts only bucket=='early' leases, so growth/mature leases squat their full PHASE_LEASE_SECONDS=300; with ~30 slots the ceiling is ~30 x 3600/300 = 360 dense pools/hour. The premise was that a 300s slot is mostly idle after the interesting frames arrive.",
    "THE_PREMISE_IS_FALSE_measured": "7,964 reconstructed observation episodes (observer snapshots, same token, gaps <=60s): only 863 (10.8%) reach >=3 frames; 6,184 (77.6%) are a SINGLE frame; median duration 0.0s. Time to the 3rd frame among those that reach it: p25 29.2s, P50 31.6s, p75 52.0s, p90 65.1s - a window forms by ~32s median and ~65s at p90, comfortably INSIDE the 120s early lease. So lease duration is NOT what prevents window formation.",
    "late_frames_are_not_dead": "New-information rate (price AND volume AND liquidity all changed) by elapsed time: 0-30s 11.1%, 30-60s 26.1%, 60-120s 24.0%, 120-300s 17.4%, 300-600s 16.8%, 600s+ 19.2%. After the first 30s the rate is FLAT at ~17-26% across every band and 2,359 new-information frames arrive after 300s. EARLIER RELEASE IS NOT FREE.",
    "bonus": "the 0-30s band is the POOREST at 11.1% - the round-21 over-polling signal, already addressed by round 22's adaptive cadence.",
    "verdict": "Slot turnover is CLOSED as a lever - the third no-budget candidate eliminated by measurement, after capital/concurrency (r28) and arm-fleet + token-exclusion (r25-26)."
  },
  "ROUND_29_BOTTLENECK_STATED_PRECISELY": {
    "the_numbers": "11,991 tokens observed; only 527 (4.4%) ever receive a >=3-frame episode - the minimum needed to be judged at all. Conversion: ALL observed 47/11,991 = 0.39%; DENSELY observed 37/527 = 7.02%. DENSE OBSERVATION CONVERTS AT 18x THE OVERALL RATE.",
    "the_statement": "The system is NOT failing to convert candidates it can see - it is failing to GIVE CANDIDATES ENOUGH OBSERVATION TO BE JUDGED. 95.6% of observed tokens never get a third frame, and 77.6% of observation episodes are a single sample that can never form a window.",
    "caveat_bounding_the_claim": "11 of the 48 entered tokens NEVER had a >=3-frame episode, so dense observation is the dominant route (77% of entries) but not the only one. A different lane reaches entries without it.",
    "ANSWER_TO_THE_OBJECTIVES_CORE_QUESTION": "The objective asks which link systematically blocks tradeable candidates through over-strict thresholds, logic or risk rules. The measured answer is NONE OF THEM. Over 28 rounds every threshold, risk rule, capital limit, concurrency cap, throttle, processing limit, arm-fleet property and token-selection rule has been tested and eliminated. The single binding constraint is DENSE-OBSERVATION COVERAGE - 4.4% of observed tokens. It remains a USER-DECIDED budget item (increase declined round 79; partial increases approved rounds 83 and 88) and was NOT changed."
  },
  "ROUND_28_CAPITAL_CONCURRENCY_HYPOTHESIS_CLOSED": {
    "the_last_unexamined_hypothesis": "That the account is out of usable capital, or concurrency / per-arm risk limits are saturated, so a fired signal cannot become a position. IT IS NOT - measured.",
    "measurements": "capital_eligibility = per_strategy_account_independent; starting_cash_usd_each_arm = 1000.0; 325 registered arms -> nominal capacity 325,000U; deployed in open positions 4,400.00U (220 positions) = 1.35% UTILISED; idle 320,600U; an arm at 1000U / 20U could hold 50 positions concurrently and the OBSERVED MAX is 8; max_open_positions = 0 (unlimited).",
    "no_capital_refusal_exists": "The entry-evaluation distribution has exactly 8 distinct reasons and NONE is capital-, cash-, limit- or concurrency-flavoured.",
    "global_throttle_is_empty": "max_pending_buy_intents = 8 is a GLOBAL cap that could have throttled a burst of buyers - there is no evidence it ever has: chain_meme_trader_order_intents 0 rows, execution_attempts 0, execution_results 0, quote_attempts 0, and marks with status=PENDING 0.",
    "useful_side_number": "468 fills across 37 DISTINCT TOKENS spawning 2,506 positions = 5.36 positions per fill. The cohort replication of round 25 seen from the execution side, confirming token breadth is set upstream at the signal/observation layer, not at execution. Trades: BUY 2,506 / SELL 1,600 / WRITEOFF 764."
  },
  "ROUND_28_CONSOLIDATED_BOTTLENECK_STATEMENT": {
    "every_link_measured": "discovery healthy (10,089 tokens); collection healthy (94.5% observed); feature/trajectory healthy (66.8% of accepted observations form a ready 30s window); processing speed NOT binding (observed snapshot -> position p50 0.18s); freshness rules NOT binding (entry_snapshot_too_old is 0.03% of evaluations, limit 90s); signal generation NOT binding; arm fleet not redundant and not prunable (r25); capital NOT binding (1.35% utilised); concurrency NOT binding (8 of 50); pending-intent throttle NOT binding (all queues empty).",
    "THE_SINGLE_BINDING_CONSTRAINT": "How many pools can be observed densely at once (~30 slots). Everything downstream - token breadth (46 tokens), effective sample size (~30), and therefore the ability to learn token selection at all - follows from that one number. It is a USER-DECIDED budget item (increase declined round 79; partial increases approved rounds 83 and 88) so it must NOT be changed silently. This round's contribution is that every alternative explanation is now eliminated by MEASUREMENT rather than assumed away."
  },
  "ROUND_27_METRIC_CORRECTION_writeoff_rate_is_not_a_loss_rate": {
    "the_claim_I_audited": "Since round 17 I have repeated 'exit150_full15_v1 has a 5.9% write-off rate against exit150_bank15_v1's 50.0%'. bank15 sells 50% at +15% and rides the rest, so a position can BANK PROFIT and still be marked written_off when the remainder's pool dies - which would conflate 'the pool died' with 'the pool died AFTER banking'.",
    "audit_result_safe_in_aggregate": "Across all 764 written-off positions: full loss (|pnl| >= 98% of stake) 97.4%; partial loss (banked something) 1.6%; positive PnL but marked written_off 1.0%. So in AGGREGATE written_off really does mean 'lost essentially everything' and the metric is safe for the fleet.",
    "but_UNSAFE_for_partial_exit_arms": "exit150_full15_v1: 1 wo (1 full-loss, 0 partial), realised +0.26U/pos. exit150_bank15_v1: 11 wo = 3 FULL-LOSS (-60.00U) + 8 PARTIAL (-28.87U, mean -3.61U), realised -6.01U/pos. exit150_widestop_v1: 7 wo with only 1 full-loss. exit150_full25_v1: 6 wo ALL full-loss.",
    "stated_correctly": "full15 write-off 5.9% of which full-loss 5.9% / partial 0.0%; bank15 write-off 50.0% of which FULL-LOSS only 13.6% and PARTIAL 36.4%.",
    "conclusion_UNCHANGED_mechanism_SHARPER": "Realised PnL per position already accounts for whatever was banked, so the money comparison is unaffected: full15 +0.26 vs bank15 -6.01 = +6.27U/pos, direction of the round-17 replay unchanged. What changes is the stated MECHANISM and it gets MORE favourable to the ladder design: bank15's ladder DOES bank (8 of its 11 write-offs were partial, averaging -3.61U rather than -20U) - IT JUST BANKS TOO LITTLE TO MATTER. full15 converts those same cohorts into real closures by selling 100% at the first tier.",
    "second_trap_in_the_same_table": "bank15's CLOSED (non-write-off) positions average -3.95U/pos while full15's average +1.53U/pos. That is NOT a performance difference either: the closed SETS are different by construction. bank15 does not close at +15% (it sells half and rides), so its closed set is dominated by stop/time exits while full15's is dominated by its take-profit. Comparing 'closed mean' across arms with different exit paths is MEANINGLESS. Only realised PnL per position is comparable.",
    "corrections_owed": "The mis-framed contrast appears in the records for rounds 17, 23, 25 and 26 and in this checkpoint. The CONCLUSION in each is unaffected; the stated MECHANISM is. This entry carries the corrected form.",
    "pattern_third_self_correction_in_three_rounds": "round 25 an outcome-identity merge list confounded by write-offs; round 26 a vacuously-true leave-one-out check; round 27 this. COMMON CAUSE: metric semantics ASSUMED rather than checked. Discipline that would have caught all three: before using a derived metric as evidence, DECOMPOSE IT AGAINST THE UNDERLYING MONEY at least once."
  },
  "ROUND_26_TOKEN_SELECTION_CANNOT_BE_LEARNED_FROM_46_TOKENS": {
    "where_this_went": "Round 25 put 56.9% of PnL variance on TOKEN identity (vs 14.6% on arm), so token selection is the highest-leverage area. Epoch: 46 tokens (36 with a usable first-entry snapshot), 7 winners, 39 losers, total -18,753.1U.",
    "winner_vs_loser_at_first_entry": "mcap winners med 889,300 vs losers 28,540 (31.2x); trades 593 vs 59 (10.0x); vol 30,460 vs 3,599 (8.5x); liq 77,920 vs 43,100 (1.8x); bp 70.8 vs 82.7; age_min 4.06 vs 3.90. CHAIN: solana 6 winners/18 losers (-4,927.9U); bsc 1 winner/18 losers (-13,279.9U = 70.8% of the total loss); robinhood 0/3.",
    "no_rule_keeps_all_winners": "No single token-level exclusion keeps all 7 winners except the trivial 'trades >= 0' (no exclusion at all).",
    "the_tempting_candidate": "first-entry mcap <= $28,300 covered 14 tokens containing ZERO winners while 6 of 7 winners sat above it: mcap >= 2.83e4 gives -6,707.2U versus -18,743.9U actual = +12,036.6U of loss avoided. At face value the largest single lever found this session.",
    "PERMUTATION_TEST_KILLS_IT": "Shuffle the token outcomes 4,000 times and for each shuffle search every possible cut for the best achievable exclusion: permutations where the BEST cut did at least as well as the real one = 4000/4000 = 100.0%. EVERY shuffle produced a cut as good or better. With only 36 tokens and free choice of cut point, a gain that size is GUARANTEED BY CHANCE. 'Zero winners below $28,300' is a property of the search, not of the market.",
    "the_trap_to_remember": "The POSITION-level version looked convincing and was also misleading: token-clustered 90% CI [+2.295,+10.623] EXCLUDES ZERO, kept 15.2% write-off vs dropped 65.7%. But that interval is CONDITIONAL on a cut point chosen by searching the same data, so it does not carry the significance it appears to. CLUSTERING FIXES THE DEPENDENCE PROBLEM, NOT THE SELECTION PROBLEM.",
    "supporting_checks_consistent_with_artifact": "not a pure chain effect (within BSC low-mcap 0 winners/-10,556.8U vs high 1 winner/-2,723.2U; within Solana 0 vs 5 winners) which is exactly why it looked convincing; not the activity floor restated - r(mcap, trades) across tokens is only +0.145.",
    "SECOND_ERROR_OF_MINE_CAUGHT": "My leave-one-token-out check printed 'removals that still leave ZERO winners: 36 of 14' and I initially read it as strong evidence. It is TRIVIALLY TRUE - removing a loser from a set that already contains zero winners obviously leaves zero winners. The test was vacuous as written and the count exceeded the set size because I iterated over all tokens rather than the low-mcap set. Do not trust this check in this form.",
    "CONCLUSION_and_line_closed": "Token-level exclusion rules CANNOT be validated from this epoch. Any cut searched over 36-46 token outcomes will look good and there is no out-of-sample route with this few tokens. This is the SECOND line closed in two rounds: (a) prune/merge the arm fleet - CLOSED (311 distinct contracts; differences unexercised not duplicated); (b) token-level exclusion filters - CLOSED (unvalidatable at n=36-46). The binding constraint is the NUMBER OF INDEPENDENT TOKENS (effective n ~30). Learning token selection requires MORE TOKENS, not more analysis of the same 46."
  },
  "ROUND_25_EFFECTIVE_N_IS_30_NOT_2256": {
    "variance_decomposition": "token identity eta^2 = 56.9% (46 tokens); cohort (token x snapshot) = 77.9% (405); ARM identity only 14.6% (174 arms). Positions per token: median 57, max 118.",
    "Kish_effective_n": "2,245 settled positions over 46 tokens with median 57 positions/token gives Kish effective n = 30.0. A per-position statistic quoted at n=2,245 is really about 30 INDEPENDENT observations. Every interval computed this session was already token-clustered so those stand - this quantifies WHY they are wide and sets a hard ceiling on what this epoch can teach.",
    "PRIORITY_ORDERING_derived": "token selection (56.9%) > entry timing (cohort adds ~21pp to 77.9%) > exit contract (14.6%). Consistent with everything since round 17: exit side had almost no headroom, entry floors moved the write-off rate, and run-up (an entry-timing feature) was the strongest marker after activity. DO NOT quote per-position statistics at n>30 for this epoch without clustering."
  },
  "ROUND_25_82pct_OF_POSITIONS_ARE_REPLICATION": {
    "numbers": "2,256 settled positions but only 406 DISTINCT entry decisions (cohorts) = 5.56 positions/cohort (median 2, max 50). Positions beyond the first on their cohort: 1,850 = 82.0%. Within-cohort PnL spread: MEDIAN 0.00U (p90 22.57, max 42.85) - for most multi-arm cohorts every arm realised the SAME result.",
    "fragmentation": "174 arms hold settled positions, median 9 each; 125 of 174 (72%) are below the 20-settled readability threshold, holding 871 positions between them. The fleet is simultaneously over-replicated in outcomes and under-powered per arm."
  },
  "ROUND_25_MERGE_HYPOTHESIS_TESTED_AND_REJECTED": {
    "naive_reading": "'82% of positions are duplicates - merge them.' This does NOT hold.",
    "confound_I_introduced_and_caught": "Pairing arms on identical (PnL, close_reason) produced a '50-arm provably identical group' that included the round-19 floor arms, which is implausible. CAUSE: a WRITE-OFF forces PnL = -stake and an identical reason, so any two arms written off on the same cohorts look trivially identical, and union-find chained those spurious pairs into one giant component. Excluding write-offs fixed it.",
    "second_check_outcome_identity_is_weak": "Of the 861 shared cohort-outcomes used to declare 114 pairs identical: market_mark_hard_stop 48.7%, max_hold 30.9%, trailing 13.1%, decay 6.7%, vol_scaled_stop 0.6%. Only 30.9% is the time exit and 0 of 114 pairs rest ONLY on max-hold, so the pairs are not merely untriggered. BUT 48.7% is the COMMON hard stop, which arms share by design - two arms agreeing there shows the shared rule bound first, not that they are the same strategy. Only ~20% of the evidence involves a rule that differs between arms. I DID NOT ACT ON IT.",
    "SAFE_TEST_contract_identity": "325 registered arms -> 311 DISTINCT contracts. Only 8 contracts shared by >1 arm, covering 22 arms holding just 28 positions (1.2%); 5 of the 8 duplicate groups hold 0 positions. THE ARM FLEET IS NOT MADE OF DUPLICATES AND THERE IS ESSENTIALLY NOTHING TO MERGE."
  },
  "ROUND_25_WHAT_THE_TWO_RESULTS_MEAN_TOGETHER": "The arms are CONTRACTUALLY DISTINCT (311 contracts) but BEHAVIOURALLY INDISTINGUISHABLE IN OUTCOME (arm eta^2 14.6%, median within-cohort spread 0.00U). The reason is NOT duplication: ~80% of exits are resolved by rules the arms SHARE (common hard stop 48.7% + time exit 30.9%), so the fleet's contract diversity is largely UNEXERCISED. Consistent with round 17 (hard stop dominant, 289 of 885 closes, gap-through fires stops early). IMPLICATION: arm proliferation is not the defect, and pruning would be both unsafe (nothing is a true duplicate) and ineffective (the differences are untested, not absent). The binding common rules are what suppress expression of the fleet's diversity.",
  "ROUND_24_DERIVED_FEATURE_SCREEN": {
    "what": "Nine point-in-time derived features built from the 20-minute pre-entry window plus the entry snapshot, screened against the WRITE-OFF label (2,369 positions / 37 tokens, base rate 32.2%) with tertile separation + token-clustered bootstrap + drop-the-best-token. This is the objective's 'derived/approximate feature' direction. Round 18 had screened the WRONG label (+15% touch); write-off is the one rounds 19/20 actually move.",
    "survivors": [
      "liq_trend +42.2pp CI [+0.249,+0.582]",
      "vol_trend +33.9pp CI [+0.139,+0.520]",
      "buy_pressure +42.7pp CI [+0.258,+0.589]",
      "mcap_liq -43.2pp CI [-0.626,-0.234] (the only MONOTONIC one: 76.2 -> 18.3 -> 2.4)"
    ],
    "rejected": [
      "turnover",
      "price_vol_pct",
      "frames_in_window",
      "holders and liq_per_holder (not populated, 0 usable)"
    ],
    "CHAIN_CONFOUND_applied_to_all": "This device's write-offs are ~100% BSC, so a feature that merely separates chains looks powerful. Within-chain: liq_trend +42.2 -> +16.8pp (BSC) / +14.0pp (SOL) so ~60% was chain mix; vol_trend +34.1 -> -0.4pp on BSC (DEAD) but +17.2pp on SOL; buy_pressure +42.4 -> +53.9pp on BSC (STRONGER) / +3.2pp SOL; mcap_liq -43.2 -> -25.7pp BSC / -18.3pp SOL but non-monotonic on both (BSC 74.9/88.4/26.8)."
  },
  "ROUND_24_BUY_PRESSURE_strongest_marker_but_REDUNDANT": {
    "feature": "buy_pressure = buys_5m/(buys_5m+sells_5m)*100 on the entry snapshot (rolling 5-minute figure, so point-in-time by construction).",
    "strength_on_bsc": "1,038 positions, 19 tokens, 63.3% write-off. Token-clustered bootstrap WITHIN BSC +0.539, 90% CI [+0.337,+0.708] EXCLUDES ZERO. Leave-one-token-out +0.487..+0.613, EVERY removal still positive. Quintiles show a SHARP THRESHOLD not a gradient: q1 30.4% wo (-6.59U/pos), q2 26.1% (-6.00), q3 82.6% (-16.78), q4 83.6% (-16.04), q5 93.3% (-18.40). Threshold at bp ~83%. A cap would cut the BSC book from -13,264.6U to -1,772.2U at bp<=75 (86.6% reduction).",
    "WHY_IT_WAS_NOT_SHIPPED": "It is largely ALREADY CAPTURED by the round-19 activity floor. r(buy_pressure, trades) = -0.631, and 'bp high & trades>=30' has n=3. The activity floor (trades>=30) already keeps only 232 of 1,038 BSC positions, of which 229 are bp-low (21.8% wo) and 3 are bp-high. So a buy_pressure cap on the surviving book would remove just 3 positions - negligible, and not worth a new correlated arm on a book with no per-token concentration limit.",
    "THE_REAL_VALUE": "It independently CORROBORATES the activity floor's mechanism from the other direction: the BSC death zone is ONE-SIDED BUYING WITH FEW TRADES - a pool being pumped with almost no two-sided flow. Two independent features converging on the same population is evidence the round-19 floor targets the right thing, not a new lever.",
    "residual_left_open": "Even after the activity floor, bp-low BSC positions still show 21.8% write-off. That residue is the next honest target; buy_pressure does not address it and nothing screened this round does."
  },
  "ROUND_23_READOUT_TOOL": {
    "commit": "682926c (pushed)",
    "tool": "scripts/experiment_readout.py (new, tracked). One command answers 'is any experiment readable yet, and what does it say' - previously done with ad-hoc SQL each round.",
    "DESIGN_POINT_that_matters": "paired_arm_ab.py pairs two EXIT carriers within a cohort, which is right because they share the entry opportunity. That design is WRONG for the ENTRY floors: a floor arm enters a SUBSET of the control's cohorts, so within-cohort pairing silently drops exactly the cohorts the floor rejected - the ones the hypothesis is about. Pairing would have hidden the effect it was meant to measure. So the readout applies a different design per kind: exit -> paired within-cohort diff + drop-the-top-token stress; floor -> SET-DIFFERENCE using the cohorts the arm refused BY ITS OWN FLOOR (from recorded outcomes), then measuring the control's own settled positions on those cohorts. It prints NOT READY and no verdict below the settled threshold (default 20 per side)."
  },
  "ROUND_23_INTERIM_READINGS_all_sub_threshold_NOT_verdicts": {
    "exit150_full15_v1_vs_bank15_plus15": "full15 settled=16, -0.00U/pos, win 81.2%, write-off 6.2% | bank15 settled=22, -6.01U/pos, win 27.3%, write-off 50.0%  <- THE STANDOUT, matches round 17's prediction that full capture at +15% beats leaving 50% riding",
    "exit150_full25_v1_vs_bank25_plus25": "full25 settled=14, -6.89U/pos, win 50.0%, write-off 42.9% | bank25 settled=22, -7.01, win 36.4%, write-off 50.0%  <- almost NO separation, which round 17's replay did NOT predict; the capture-fraction effect may be concentrated at the +15% level rather than being general. WATCH THIS.",
    "exit150_widestop_v1_vs_bank15": "widestop settled=10, -8.35U/pos, write-off 70.0% | bank15 22, -6.01, 50.0%  <- WORSE, consistent with rounds 120-16/120-17 having twice weakened the wide-stop argument",
    "activity_floor150_t30_v1": "settled=10, -4.72U/pos, win 20.0%, write-off 10.0% | control 26, -9.71, 19.2%, 42.3%",
    "activity_floor150_v5k_v1": "settled=8, -5.71U/pos, win 25.0%, write-off 12.5% | control 42.3%",
    "runup_floor150_r15_v1": "settled=9, -3.87U/pos, win 22.2%, write-off 22.2% | control 42.3%",
    "runup_floor150_r15a30_v1": "settled=7, -1.90U/pos, win 28.6%, write-off 14.3% | control 42.3%",
    "observation": "EVERY floor shows a much lower write-off rate than the control (10.0-22.2% vs 42.3%) - the specific mechanism each was built for. But every number here is sub-threshold and NOT a verdict."
  },
  "ROUND_23_COUNTERFACTUAL_verified_two_ways_but_rests_on_n3": {
    "claim": "For activity_floor150_t30_v1 the floor rejected 19 cohorts and the control itself traded 3 of them at -20.00U/pos with 100% write-off.",
    "verification": "method A (recorded outcomes = floor reason): 19 cohorts, 3 settled, -20.00U/pos, 100% wo. method B (independent: cohorts the control traded but the arm never did): 22 cohorts, 22 settled, -10.84U/pos, 45% wo. Both agree the avoided set is deeply negative.",
    "why_they_differ": "A isolates rejections attributable to the FLOOR; B also includes cohorts the arm missed for unrelated reasons (concurrency, single_token_lifetime_entry, or the arm not existing yet at that frontier). B is an upper bound on scope, not the floor's effect.",
    "HONEST_CAVEAT": "A's avoided set is only 3 positions. The floor rejected 19 cohorts but only 3 were opportunities the control would actually have taken - the control has its own gates. The -20.00U/pos figure is SUGGESTIVE, NOT ESTABLISHED, and the readout correctly refuses a verdict for it."
  },
  "ROUND_22_LANDED_ADAPTIVE_CADENCE": {
    "commit": "7b40eb5 (pushed)",
    "what": "observation_leases145.record_frame gains an optional `content` param; cadence_seconds backs the next due time off on an information-free sample: 0-2 unchanged -> 15s, 3-5 -> 30s, >=6 -> 60s CAP, immediate reset on any change.",
    "safety_constraints_implemented_and_tested": [
      "back off ONLY when price AND volume AND liquidity are ALL unchanged (a volume-only change is 3.2% of samples and resets the cadence)",
      "capped at 60s with immediate reset, so worst-case detection delay for a new move is one interval",
      "content=None reproduces the previous FIXED cadence exactly, so it is reversible per caller"
    ],
    "durability_and_observability": "unchanged_run persists through dump_state/restore_state so a restart does not silently reset every backoff; last_content is deliberately NOT persisted (run restarts at 0 = conservative). bounded_summary now reports unchanged_run, cadence_seconds, content_known.",
    "verified_live": "cadence values live [15,30,60]; 24 slots at 15s, 1 at 30s, 5 at 60s; content_known 30/30. Requests/min 187.2 -> 183.3 (essentially flat). Implied request cost across the 30 slots 120 -> 103-105/min = 12.5-14.2% FEWER REQUESTS for the same slots.",
    "coverage_quality_did_NOT_regress": "input_ready:window30 share 68.291% -> 70.190% -> 70.993% (improved slightly). trajectory accepted 47,226 -> 53,336; ready 32,251 -> 37,865. signal second_wave 16 -> 18.",
    "tests": "8 new tests in tests/test_observation_cadence.py, plus the two DEDICATED observer regression suites (test_observation_leases145.py, test_observation_leases145_runtime.py) and the related suites all pass."
  },
  "ROUND_22_CORRECTION_my_3_42x_estimate_was_TOO_HIGH": {
    "claim": "Round 21 projected matching cadence to the source update rate would cover ~3.42x more distinct pools. The REALISED effect is ~12-14% fewer requests, not 3.42x.",
    "why_it_was_a_measurement_population_error": "(1) the 70.8% information-free rate was measured over tokens with >=20 snapshots - the DENSE-pool history, which is dominated by pools that sat in a slot a long time precisely BECAUSE they were quiet or dead. (2) volume_5m_usd is a ROLLING 5-MINUTE SUM, so it changes on almost any trade, so under the strictly-safe criterion the backoff only engages for pools with essentially no trading. (3) the live slots show median unchanged_run of 0-1, i.e. most occupied pools ARE updating.",
    "correct_statement": "70.8% of samples in the dense history are information-free, but the LIVE SLOT POPULATION at any instant is mostly active, so only a minority of slots can back off.",
    "coverage_gain_UNPROVEN": "the distinct-pool-per-bucket series is strongly TRENDING (543 -> 939 -> 796 -> 612 -> 587 across the pre-change window), so a naive before/after comparison is confounded. With one partial post-change bucket there is NOT enough data to claim a coverage gain either way.",
    "narrower_criterion_declined": "backing off when PRICE alone is unchanged would engage far more often, but it would also back off across volume-only changes (3.2% of samples, real information). Not worth taking without its own evidence; the conservative criterion stands."
  },
  "ROUND_21_OBSERVATION_BUDGET_IS_3_4x_UNDERUTILISED": {
    "finding": "observation_leases145.record_frame sets next_due_at = observed_at + TARGET_SECONDS (15) regardless of whether the source has anything new. Measured over 13,651 consecutive same-token snapshot pairs (237 tokens with >=20 snapshots): price AND volume both changed 25.7%, volume-only 3.2%, price-only 0.3%, and NOTHING changed (price+volume+liquidity all identical) 70.8%. So 70.8% of polled requests return no new information. Matching cadence to the source's real update rate would cover ~3.42x MORE DISTINCT POOLS for the SAME request budget - no budget increase needed.",
    "worst_where_it_matters": "TRADED tokens have a HIGHER repeat rate than untraded: traded median 82.2% (p25 77.3, p75 85.0, 64.5% of tokens >80%) vs never-traded median 66.7%. 30 of 31 traded tokens exceed 50% repeats; 20 of 31 exceed 80%. Worst case bsc:0x64a6c4c1 took 1,085 snapshots at 89.7% repeats and produced 67 positions. 21 dense pools recorded ZERO information change across 668 polls and NONE of the 21 ever produced a position.",
    "run_lengths": "consecutive no-information runs: p50 3, p90 10, p99 31, max 55. Runs of >=5 waste 7,834 polls.",
    "NOT_an_accuracy_defect_CONTAINMENT_VERIFIED": "record_frame rejects a frame only on observed_at <= prior (a TIMESTAMP test), and a live frame shows observed_at == ingested_at == recorded_at exactly, so observed_at is our receipt time and repeats DO advance the clock. In the live trajectory144:state, 60 pools have frames>=3 (all 60 formed window_30) and 26 of those (43.3%) have <=1 distinct price across their ENTIRE frame history; across the 60, 1,737 frames contain only 378 distinct prices (78.2% repeats). BUT containment holds: the 26 static pools emitted 0 signals and produced 0 positions and carry phase=None, while the 34 active pools emitted 14 signals and hold 1,085 positions. The phase/flag machinery never engages on zero-variation pools. So there is NO accuracy defect to fix and no reason to touch the trajectory engine's frame handling.",
    "self_correction": "I first computed '81% of pools have a single price' and caught that it was contaminated by the 108 one-frame pools, for which the statement is trivially true. Corrected figure is 26 of 60 eligible pools. The first number overstated the defect by ~2x.",
    "window_formation_is_healthy": "trajectory144:state counts show 29,657 of 44,380 accepted observations (66.8%) produced input_ready:window30; 14,723 input_unknown:window30. Window formation is NOT the leak.",
    "hypotheses_killed_this_round": "slot waste (all 30 slots occupied, 11 held by open positions, only 4 of 30 = 13% yield <=1 frame then SOURCE_NO_UPDATE); dense-pool waste (40 of 68 dense pools never produced a position but account for only 2,876 snapshots = 7.3% of observation)."
  },
  "ROUND_21_ADAPTIVE_CADENCE_DESIGN_not_implemented": {
    "why_deferred": "the observation supply is the HIGHEST-BLAST-RADIUS surface in the system - it feeds every arm at once, and six forward experiments (exit150_*, activity_floor150_*, runup_floor150_*) are accumulating evidence a cadence regression would quietly corrupt. Design is specified and containment evidence is in hand; it should land as its own change with its own verification.",
    "constraints": [
      "back off ONLY when price AND volume AND liquidity are ALL unchanged (volume-only changes are 3.2% of samples and are real information)",
      "modest capped backoff 15s -> 30s -> 60s cap, with IMMEDIATE reset on any change, so worst-case detection delay for a new move is one backoff interval",
      "must not thin the ACTIVE pools - the trajectory window needs >=3 frames within 30s, so an over-aggressive backoff would destroy it. Verify, do not assume."
    ],
    "success_metric": "distinct pools covered per hour at unchanged request volume, with NO regression in input_ready:window30 share (currently 66.8%)."
  },
  "ROUND_20_DIAGNOSIS_latency_and_funnel": {
    "funnel": "10,089 discovered -> 9,531 observed (94.5%) -> ONLY 37 EVER ENTERED (0.4%). 37,774 evaluations -> 1,710 positions (4.53%). Settled 1,318 at -11,493.58U (-8.72U/pos). The bottleneck is observation capacity between observation and judgement, NOT a threshold defect.",
    "gate_distribution": "cohort_observation 34.38% + pattern_observation 22.77% = 57.15% is observer bookkeeping that can NEVER admit; no_active_matching_entry_policy 25.85%; entry_pool_liquidity_absent_curve_stage 7.66%; invalid_exact_asof_market_snapshot 5.17%; entry_pool_liquidity_below_configured_floor 3.04%; entry_pool_liquidity_unknown 1.10%; entry_snapshot_too_old 0.03%. So the gate distribution does NOT show an over-strict filter.",
    "latency": "observed->ingested p50 0.03s; observed->recorded p50 0.63s; observed->evaluated p50 1.21s (p90 11.33s); entry snapshot observed->opened p50 0.18s / p90 2.57s. THE PROCESSING PATH IS NOT THE BOTTLENECK.",
    "real_speed_defect": "discovery->first-observation TAIL: p50 8.05s but p90 1,500.95s (25 min) and p99 8,517s (2.4h). Same observation-capacity limit expressed as latency.",
    "self_correction": "I first read entry_execution/entry_signal - 1 as a 'latency premium' at 99.5% of positions. It is EXACTLY 4.0000% at every percentile (p10=p50=p99=max) - the frozen BUY_SLIP=0.04 cost model, not latency. Do not re-report it as an execution-quality defect.",
    "delay_confound_stated": "Entering LATER does better (after 1h: -1.31U/pos, 0.0% write-off) BUT that group is conditioned on surviving an hour, so it is NOT evidence that faster entry would help. Recorded so it is not misread as a speed recommendation."
  },
  "ROUND_20_SHIPPED_RUNUP_FLOOR150": {
    "commit": "bc4dabc (pushed)",
    "arms": [
      "runup_floor150_r15_v1 (run-up <= 15%)",
      "runup_floor150_r15a30_v1 (run-up <= 15% AND trades >= 30)"
    ],
    "module": "src/memetrader/runup_floor150.py (new)",
    "marker": "runup = current price / earliest observed price in the 20 minutes before the decision - 1. The 20-minute window is deliberate: the acceptance loop's own `history` (store.py:27614, LIMIT 80) holds exactly that window, so the cap costs ZERO extra queries. Verified live: ZERO runup_floor_window_unknown rejections.",
    "measured_basis": "write-off rate jumps from 0.6% at a 15% cap to 6.0% at 20% and 24.8% uncapped (1,712 positions). Token-clustered significant BOTH ways: write-off +0.322pp CI [+0.082,+0.565]; PnL -4.851 U/pos CI [-9.491,-0.055]. Not the survival confound (r=-0.116 with entry delay).",
    "second_pass_correction": "The money effect ONLY clears the bar as a low-vs-high contrast. A cap contrast gives +3.96 U/pos CI [-0.59,+8.42] because it dilutes by putting the ambiguous middle in the dropped group. I nearly recorded the finding as failing; the tertile contrast is significant.",
    "complementarity_with_activity_floor": "quadrants: runup ok & trades>=30 -> 0.3% write-off at -3.36U/pos; runup high & trades<30 -> 64.1% write-off at -12.48U/pos. Combined book -11,434U -> -1,860U with win rate 13.9% -> 23.6%.",
    "honest_limits": "It is TOKEN SELECTION, not entry timing (within tokens: median diff exactly 0.000, positive in only 6 of 15; acts on 12 of 37 tokens). Level chosen in-sample. The kept book STILL LOSES -2.88U/pos. Forward experiment only, NOT a validated fix.",
    "verified_live": "325 additions; both arms at OWN frontier 38633; control unchanged at 0; arms trading (6 positions each); floor BITES: 6x runup_floor_exceeded each alongside 6x accepted.",
    "discipline": "screen reuses the round-19 hook, returns None for every other arm, sits after signal validation, distinct auditable rejection reasons (runup_floor_exceeded / runup_floor_window_unknown / activity_floor_trades_not_met), missing evidence never admits. No existing strategy, stop, hold or exit contract changed. 120 tests pass."
  },
  "ROUND_19_LANDED_ACTIVITY_FLOOR150": {
    "commit": "024b0d6 (pushed)",
    "arms": [
      "activity_floor150_t30_v1 (buys_5m+sells_5m >= 30)",
      "activity_floor150_v5k_v1 (volume_5m_usd >= 5000)"
    ],
    "module": "src/memetrader/activity_floor150.py (new)",
    "why_entry_arms": "unlike EXIT150 these are ENTRY arms and DO go into alpha149.SPECS - they must, so the shared acceptance loop can screen them. Confirmed by round 18: alpha149.py:2283 iterates SPECS generically and an arm fires when flags.get(kind) is set.",
    "one_factor_design": "reuse the control's kind ('merged_multi_setup') so they fire on the same mechanism flags and get the same frozen signal, and clone the control's exit contract verbatim (stop -0.2, trail .30/.15, hold 15, tp [], 2U, isolated_cohort_observer). Control = alpha149_merged_multi_setup_fast_v1, chosen because merged_multi_setup is the highest-volume generic mechanism (6 arms x 123 emits/374 cohorts) and round 16 already characterised it. Verified on the BUILT policy: every exit-contract field equal, and on all shared keys only identity/label fields differ.",
    "enforcement": "opt-in by arm id in the shared cohort-acceptance loop (store.py), placed AFTER signal validation so a recorded rejection means exactly 'a valid signal existed but the pool was too quiet' rather than 'this arm had no signal'. reject_reason returns None for every arm that is not ours - asserted against the whole 158-arm registry.",
    "auditability": "the floor is declared on each arm's own entry_filter.activity_floor (visible in the registered policy body) AND the rejection reason is distinct: activity_floor_trades_not_met / activity_floor_volume_not_met.",
    "missing_evidence": "never admits. Coverage is complete (0 nulls in 35,349 snapshots) so this is a safety net.",
    "verified_live": "321 -> 323 additions; both at OWN frontier 36360; control frontier unchanged (append-only respected). Arms TRADE: t30 5 positions, v5k 2 positions within ~3 min. Floor BITES: in cohort feature_json -> outcomes, t30 shows 1x activity_floor_trades_not_met + 7x ready; v5k shows 5x activity_floor_volume_not_met + 4x ready; the control shows ZERO floor reasons. v5k rejects more than t30, matching the measured pass rates (47.2% vs 59.3%).",
    "correction_to_my_own_expectation": "I first expected the reason in chain_meme_trader_v6_entry_evaluations.reason, but store.py:28327 persists observation_reason ('cohort_observation'/'pattern_observation') there, NOT the per-arm reason. The per-arm reason lives in the cohort outcomes map. Verified empirically rather than assumed.",
    "tests": "108 pass (tests/test_activity_floor150.py is new, 11 cases)."
  },
  "workspace": "H:\\OpenTrader\\memeTrader_2",
  "runtime": {
    "db": "data/memetrader_forward.sqlite3",
    "epoch": "chain-meme-trader/funding-20260906-v002-final-1000",
    "supervisor": "scheduled task 'memeTrader Paper Bot' -> scripts/run_paper.ps1 (auto-restarts the trader 5s after it exits)",
    "web": "http://127.0.0.1:8790 (chain-web, separate process)",
    "restarted": "2026-09-12T21:19:08Z (round 120-17; registers exit150_full15_v1 + exit150_full25_v1 at frontier 32175)",
    "process_note": "each service shows TWO python processes - the .venv parent and a system-python RE-EXEC'd child. This is by design, NOT a duplicate-writer hazard."
  },
  "ROUND_18_FALSIFIED_entry_features_cannot_predict_the_15pct_touch": {
    "status": "FALSIFIED before building. Do NOT build a '+15%-touch predictor' / 'quiet pool' entry filter.",
    "why": "Position level looked strong (mcap ratio 0.18, sells 0.00, both 'separated'), but every check kills it: (1) positions are projected from a cohort=(token,snapshot) x many arms, so they SHARE one feature vector and per-token medians are literally identical (bsc:0x1853979987 mcap 27,377/27,377, trades 11/11) - the within-token test had no variance by construction; (2) at the correct unit (167 cohorts, 7.3/token) the within-token rank correlation is 0.000 for EVERY feature (liq and mcap: 10 tokens negative, 0 positive); (3) the direction INVERTS when the 5 luckiest tokens are removed (trades 0.12 -> 1.22, sells 0.09 -> 2.04, mcap ratio -> 1.00); (4) token-clustered bootstrap CI [-0.127, +0.297] spans zero; (5) economically backwards - quiet pools -12.57U/pos vs busy -4.25U/pos."
  },
  "ROUND_18_FOUND_one_filterable_population_is_69pct_of_the_loss": {
    "headline": "An activity floor on the entry snapshot's OWN fields collapses the death rate. Write-off rate 34.8% -> 6.0% at trades>=30 (vol5>=10000 -> 8.4%). Token-clustered bootstrap on the per-position gain: +9.73 U/pos, 95% CI [+3.93, +14.17], EXCLUDES ZERO.",
    "chain_stratified_mechanism": {
      "bsc_lt30_trades": "404 positions, 78.6% write-off, -15.39 U/pos  <- -6,218U = 69.2% of the entire -8,990U epoch loss",
      "bsc_ge30_trades": "132 positions, 7.6% write-off, -4.75 U/pos",
      "solana_lt30_trades": "55 positions, 0.0% write-off, -2.67 U/pos",
      "solana_ge30_trades": "352 positions, 6.8% write-off, -4.43 U/pos",
      "baselines": "bsc 547 pos / 61.4% wo / -12.82 U/pos; solana 407 / 5.9% / -4.19; robinhood 93 / 0.0% / -2.97"
    },
    "near_miss_correction": "It is tempting to read 'the floor hurts Solana because kept -4.43 is worse than dropped -2.67'. That is the WRONG comparison. Removing a set that loses -2.67U/pos IMPROVES the book by +147U. The floor helps BOTH chains: +6,394U on BSC, +148U on Solana.",
    "units": "the gate's own quantity is `trades = last['buys'] + last['sells']` and `last['volume']` - the SAME 5m fields measured here."
  },
  "ROUND_18_COMBINED_levers": {
    "actual": "-8,990.05U over 1,041 positions (-8.64 U/pos)",
    "tp_only": "-3,743.41U",
    "floor_only_trades30": "-2,462.54U over 575",
    "floor_plus_tp": "-2,162.84U over 575",
    "best_vol5_50k_plus_tp": "-506.00U over 275 (-1.84 U/pos)",
    "verdict": "The two levers PARTIALLY OVERLAP rather than add (5,247 + 6,528 != 6,827) because the floor removes positions the take-profit would have rescued. Best case is a 94.4% loss reduction but token-clustered bootstrap gives 95% CI [-1,429, +141] and P(profitable)=12.9%. IT STILL LOSES, and the residue is concentrated (1 of 7 traded tokens is -250U)."
  },
  "ROUND_18_ROOT_CAUSE_why_the_existing_defence_never_bites": "The system ALREADY has 296 numeric entry gates, consumed at revision_evidence_extensions.py:279, with min_trades 4-12 and min_volume 300-1200. The transition is between 10->20 trades and 1,000->3,000 volume, so THE EXISTING FLOORS ARE CALIBRATED EXACTLY WHERE THEY DO NOTHING (trades>=10 is worth +552U of a possible +6,528U). Structurally: those floors live on evidence_extension_l0 arms that hold 1-2 positions each, while the 1,409 positions actually held come from 154 isolated_cohort_observer arms whose ENTIRE entry_filter is {direction, max_concurrent_positions: 2, single_token_lifetime_entry: True} - no activity gate at all.",
  "ROUND_18_HOOK_LOCATED_not_landed": "store.py:27832-27850 is the cohort acceptance loop; each qualifying policy commits at accepted_cohort_signals[arm]=signal (27850). A new arm can be screened there by its own id (existing arms -> allow immediately), precedent being store.py:27951 which dispatches an arm to its own module via entry_filter['failed_impulse_cooling']. NOT LANDED because a new cohort arm only trades if the cohort evaluator emits a signal for it: cohort_signals is an INPUT parameter (store.py:27508) produced upstream, cohort_experiment_policies() enumerates its arms explicitly, and the alpha149 cohort arms emit via alpha149.signals_for from SPECS. So the additive path is: append a new arm to alpha149.SPECS (the established wave pattern) + the one-line screen + its own register_* frontier. NEEDS VERIFICATION that the new arm reaches cohort_signals before it is trusted.",
  "ROUND_18_FUNNEL_REASONS": "evaluations this epoch: cohort_observation 11,667 | no_active_matching_entry_policy 9,166 | pattern_observation 7,555 | entry_pool_liquidity_absent_curve_stage 2,714 | invalid_exact_asof_market_snapshot 1,807 | entry_pool_liquidity_below_configured_floor 1,055 | entry_pool_liquidity_unknown 396 | entry_snapshot_too_old 12.",
  "ROUND_18_JOIN_KEYS_measured": "shadow_cohort_id -> v6_cohorts.id 1098/1098; source_entry_fill_id -> v6_entry_fills.id 1091/1098; entry_snapshot_id -> token_snapshots.id 1091/1098; entry_fill_id -> v6_entry_fills.id 0/1098 (silently empty - the trap that bit a previous round). Rich per-arm feature vectors are NO LONGER usable historically because round 120-9 compacted them to 3 fields.",
  "ROUND_17_EXIT_SIDE_IS_CLOSED": {
    "status": "Four independent falsifications on real forward marks (906 closed positions, 24 tokens, actual -7,962.84U). No exit rule, level or cadence change can recover the loss. ALL remaining effort belongs on the ENTRY side.",
    "1_no_tighter_stop_helps": "Enforcing -0.15/-0.20/-0.25/-0.30/-0.40/-0.50/-0.65 econ on EVERY position gives -358/-149/-89/-113/-149/-152/-142 U versus doing nothing. Tightening also stops the positions that recover.",
    "2_stop_fires_correctly": "mark[-2] median econ -12.4% -> mark[-1] -36.0%, a median 23.6% single-step drop. Inter-mark gap 10.9s (config TARGET_SECONDS=15) and the last mark is 1-3s before close, so we were neither starved nor blind. The overshoot is a gap-through INSIDE one interval - there is no delay to remove.",
    "3_no_liquidity_warning": "0s median warning at every liquidity threshold 1k..12k. NOTE: I suspected a 50,000 liquidity clamp and FALSIFIED it as my own aggregation artifact (11,750 marks, 2,969 distinct values, max 67.5M; liquidity+volume co-move 1,507 times vs liquidity-alone 29; 0 pools show price-moving-but-liquidity-frozen). Do not re-investigate.",
    "4_lease_contention_not_the_cause": "observation_leases145.replaceable_early admits only bucket=='early' leases (growth/mature are not evictable - a real asymmetry) BUT marks already beat the 15s target, so it is not this round's bottleneck."
  },
  "ROUND_17_PROFIT_CAPTURE_FINDING": {
    "headline": "The user's problem '金狗拿不住' is BACKWARDS. Only 5 of 906 positions ever reached +100% econ, but 241 touched +15% and 186 of those finished NEGATIVE (only 55 positive). Their realised total was -3,059.50U; banking the whole position at +15% would have returned +723.00U. The failure is NEVER TAKING PROFIT, not failing to hold.",
    "priced_on_the_same_replay": {
      "full15_100pct_at_15": "+4,178.36U",
      "full25_100pct_at_25": "+2,796.84U",
      "full10_100pct_at_10": "+4,107.39U",
      "bank15_50_40_50_DEPLOYED": "+2,446.19U - captures only 58.5% of the effect",
      "bank25_50_40_50_DEPLOYED": "+1,498.52U"
    },
    "the_50pct_left_riding_is_what_round_trips": "full15 is worth +1,732U more than the deployed bank15. The LEVEL is not knife-edge (+10% ~ +15%; +20% already gives back 1,160U); the CAPTURE FRACTION is the factor that matters.",
    "honest_limits": "In-sample on one epoch; survives spike rejection (run=3 gives +4,272U) and survives dropping the best three tokens (+715U); token-clustered bootstrap 95% CI [+969U, +8,307U]. BUT only 7 of 24 tokens improve, 16 are flat at exactly 0.00U (never reach the level), and 3 BSC tokens contribute 83%. Forward experiment only - never promote on the motivating window."
  },
  "ROUND_17_CHANGE": {
    "commit": "137bdfa (pushed)",
    "module": "src/memetrader/exits150.py v1 -> v2",
    "added_arms": [
      "exit150_full15_v1 (+15%, 100% capture)",
      "exit150_full25_v1 (+25%, 100% capture)"
    ],
    "design": "Completes a 2x2 against the deployed partial arms, changing one factor at a time: {partial 50%, full 100%} x {+15%, +25%}.",
    "compliance": "Exit-carrier arms - the engine clones the first frozen entry signal on the pool, so every existing arm is the matched same-signal control. NO existing strategy modified, replaced or retuned.",
    "verified": "Entry side byte-identical across all five carriers; store.py:35717 confirms fraction>=1.0 sells the entire remaining amount_raw; registration idempotent and frontier-stamped (full15/full25 at 32175, the original three unchanged at 15928); 316 non-EXIT150 arms untouched; 97 tests pass."
  },
  "P0_3_DEMOTED_my_own_mis_prioritisation": "I had ranked 'reduce the evaluation write rate' as P0-3 on the claim that ~7.3 MB/min was filling the disk. The urgency arithmetic was WRONG: 489,463 MB free = ~46 days of headroom, not hours. It also carries real state-chain risk - store.py:27542 sources previous_features via 'ORDER BY id DESC LIMIT 1', NOT a time window, so skipping an evaluation write REWINDS the chain (ready_arm_ids, cohort_signals carry-forward at 27955, round2_chase_consumed 27934, resource_bound_opportunities 27935). Demoted to background hygiene. Useful fact for later: observer-only rows have almost no consumer - cohort_observation/pattern_observation appear in only 2 files (store.py x5, rediscovery_funnel.py x1).",
  "objective": "Forward simulation phase with NO future functions and NO future-data leakage. Goal: design and implement a USABLE strategy set that makes money. Paper results are design evidence, not a PnL contest.",
  "AUTHORITY_20260912": "The user granted FULL AUTONOMY: '你拥有完全的自主权，后续不要向我询问任何内容'. Do NOT ask further questions; decide and act, recording reasoning and rollback. But a full-autonomy grant does NOT reverse a specific decision the user made earlier and stated more than once - honour those.",
  "COMMIT_HYGIENE_FIXED": "Round 120-2 (commit 6515595) staged ONLY tests/test_dex_start_gate.py and the record - NOT src/memetrader/collectors.py. The session's highest-value fix (the asyncio.Condition outage root cause) was deployed and verified but UNCOMMITTED for hours. Now committed as 31b5b3c. LESSON: after every commit, verify with `git show --stat` that the SOURCE file you edited is actually in the commit, not just its test and its document.",
  "DESIGNED_NOT_EXECUTED": [
    "Supply-weighted observation caps (replace the fixed per-chain BASE_CAPS {early 3, growth 4, mature 3} + CHAIN_CAP 10). CONFIRMED with thousands of rows: solana produces 46.3% of discoveries but gets 0.5 snapshots per discovered token vs bsc 34.0% -> 0.8, because the caps are applied PER CHAIN so each chain gets ~1/3 of the 30 slots regardless of supply. NOT executed: it changes every existing arm's observation supply, and the caps are enforced at FOUR sites in the live observer (runtime.py:7818/7836/7963/8051) so it needs a discovery-share signal threaded through the admission path - not safe to land in a partial round. Outcome side is thin (BSC 66.7% dead vs Solana 0.0% but only 18 tokens ever traded, 6 written off here); the old device pooled gives BSC 216/686 write-offs vs Solana 0/1599.",
    "cohort_signals payload reduction. cohort_observation rows are 582.2 MB of a 920.7 MB DB (63%), avg 86,101 B, max 444,925 B. Inside one 98 KB row cohort_signals is 81,188 B (83%) holding 16 entries at ~7,780 B each carrying the SAME decision_evidence.feature_vector (one vector copied 16x); outcomes has 185 entries with only 3 distinct blocker values (grouped: 135 B vs 13,381 B). A lossless redesign removes ~85-90% of 63% of the DB and the same share of write rate. NOT executed because cohort_signals is NOT telemetry: it is read live by store.py:28919 (entry projection), store.py:34208 (exit path), store.py:27887/27942/28046 (cohort episode continuity), cohort_enrollment.py:67, narrative_hold.py:258, mode_learning145.py:234. Needs a reader migration and its own round. DB growth measured at ~7 MB per 10 min = ~1 GB/day, so this is an active operational concern."
  ],
  "COVERAGE_BOTTLENECK_IS_LOCATED": "The token-level signal rate of 0.38% comes from WATCH CAPACITY - how many pools can be observed densely - NOT from a continuity, freshness or mechanism-threshold defect. The system observes the pools it selects extremely well (gap p50 1.4-2.7s, 96-99% of gaps under 30s, and 4 of 6 busiest pools PASS the 30s-window test with the two failures marginal) and everything else about once. 30 observation-lease slots (3 chains x {early 3, growth 4, mature 3}, TARGET_SECONDS=15) plus a 24-slot mover registry, currently at capacity (watched=34, mover_watching=24). Mechanisms fire amply: deep_pool_flow 370 ready, nonbsc_flow 250, mid_band_flow 227, righttail_lottery 538.",
  "DO_NOT_BUILD": [
    "A cadence-aware engine on a larger MAX_GAP_SECONDS - it would unlock nothing, and widening only the gap cannot help because the window's 40-SECOND SPAN is what binds. Changing the span changes what return_fraction / acceleration / volatility MEAN, so it would need NEW mechanism kinds calibrated on the new features, not new arms on the old ones.",
    "An observation-depth-budget reallocation - already 54.2% of mover slots are in the good bands (20k-100k: 6, >=100k: 7) and 1k-5k holds only 12.5%.",
    "A mark-supply fix - 1706 mark_history rows per 15 min here versus the old session's 46; 494 of 518 positions received more than one in-window mark.",
    "Loosening the engine's 30s freshness rule - only 1 of 6137 snapshots exceeds 30s and the engine refusal ratio is 12.2%."
  ],
  "next_action": "ROUND 86 BANNER (read first; everything after it is round-68-era detail kept for history). ROUND 86 SEPARATED SELF-IMPOSED FROM PROVIDER-IMPOSED ON THE OBSERVATION BUDGET: the 30-slot candidate cap is OURS - 8 of 9 candidate buckets are at cap, 48 tracked sources record ZERO rate-limit/429 errors, and the freshest successful source contact is about 0 s old. So 'raise N' is a REAL, available decision, not one blocked by the providers. BUT THE GAIN IS DENOMINATED IN FRAMES, NOT TOKENS: the quote path already touches 280 never-held tokens per 15 minutes (median 14 marks each, 9.3x the cap) while 81.3% of those same tokens have ZERO snapshots in the window and only 2.8% reach the >=3 frames the gate needs - a QUOTE IS NOT A FRAME (rule 43). Price any budget increase at round 65's measured yield (about 4.7 dense windows per slot per hour), never at 280 tokens. Round 85 still bounds the value: 6 profitable of 94 tokens, so more tokens is more EVIDENCE, not more profit. STILL A USER DECISION - this session does not touch the budget and has changed no strategy, threshold or runtime behaviour. P0 (unchanged) exit_ladder150 dose-response: four arms REGISTERED AND DEPLOYED 2026-09-12T23:42:11 at ONE shared frontier, identical in every field except the first tier (10/15/20/25%), with trailing_activate_return PINNED at 0.30 - the field that confounded the deployed full15/full25 pair. Read them with SAME-FRONTIER pairing, NOT cross-frontier: round 57 measured same-frontier pairs accumulating DIVERGED cohorts about an order of magnitude faster (14 and 17 vs 4 and 3). Read with scripts/experiment_readout.py once enough DIVERGED cohorts exist between any two of the four (the cohort gate depends on the pair's divergence bandwidth - see the READ-ORDER RULE below); report the DIVERGED count first, never the unpaired per-side means. The declared purpose is the SHAPE of the curve, not promoting a level - picking a winner from round 53's same-epoch replay is the round-26 selection trap. P0 exit150_full15_v1 vs bank15: paired basis advanced 4 -> **6 DIVERGED** cohorts (17 shared, 11 identical) as of round 68. Still below the 20-diverged gate. Do NOT report the unpaired +6.15U/pos contrast as a verdict. P0 full15 vs full25 (same frontier): 32 shared / **17 DIVERGED**, within-cohort difference about -7U/pos, CI [-12.942,-1.661] - a STRONG MECHANISM SIGNAL that the +15% tier catches pool deaths the +25% tier misses (in all 14 diverged cohorts the peak econ never reached +25%), but FRAGILE: dropping the 5 largest cohorts flips the sign, and the pair is confounded. NOT a verdict. P0-1 KEEP THE EXPERIMENTS RUNNING - the pairing clock is the binding constraint and nothing analytical accelerates it. ROUND 68/69 READ-ORDER RULE: read the WIDEST divergence band first, and the gate is a COHORT count. A pair separates only when a cohort's running max falls BETWEEN its two varied levels. AUTHORITATIVE gate (round 69 rebuild, engine basis n=3,892): +10-vs-+25 needs **100** cohorts, +15-vs-+25 152, +10-vs-+20 155, +20-vs-+25 285, +10-vs-+15 295, +15-vs-+20 327. Reach rates P(>=+10%)=0.4108, P(>=+25%)=0.2117. The ladder is 12 opened / 4 settled / 8 open with 0 diverged and best peak 1.1027 against a 1.10 first tier. P1 the residual after the activity floor: bp-low BSC positions still show 21.8% write-off - effective sample ~19 BSC tokens, so any rule needs the round-26 permutation treatment. STACK (rounds 40/43, in-sample, CORRECTED kernel): stack delta +19,663.7U, CI [+14,398,+25,140], book still -2,461.7U on 894 kept - a reduction that STILL LOSES, and every level was chosen on this same epoch. The +17,620U belongs to the ENTRY FILTERS, not to exit-side take-profit. STANDING (round 53, corrected kernel): 67.4% of settled positions are TOUCHABLE (forced AND never reached +15% = 32.6% of all); a +15% TP converts 662 forced exits for +14,190.2U, CI [+8,987,+19,637]; pool-death peak econ p50 +18.2%. COVERAGE: the ~30 candidate observation slots are SATURATED (occupied == base_caps in 19/19 snapshots; 66% of admission attempts refused bucket-full) and the slot-allocation rule is already near the feature frontier - a USER-DECIDED budget item, NOT touched. DO NOT RE-OPEN (each closed by measurement): r42 entry-time liquidity floor, r46 risk gates over-strict, r48 budget fragmented, r50 slot allocation improvable, r54 a decision reason blocking movers, r55 narrative/promotion features, r25 arm pruning, r26 token exclusion, r28 capital, r29 slot turnover, r30 dense-free lane, r31 httpx NO_PROXY, r32 provider reliability, r35 stop width, r36/r39 the exit ceiling. EFFECTIVE-vs-REGISTERED (rounds 62/63, cost three wrong conclusions): policy_additions.policy_json is a DRAFT. The contract that RUNS is the effective definition built at load - call Store._chain_meme_trader_effective_definition(version, definition_json) BEFORE reasoning about any limit, size, threshold or contract field. This epoch's effective revisions: uniform_notional_usd -> 20.0 for every policy (280 changed; basis 'one uniform 20U per trade so strategies can be compared fairly') and concurrency_cap_floor -> 8 for every policy (240 raised, 78 previously uncapped), so the correct per-arm risk picture is 8 x 20U = 160U maximum concurrent exposure = 16% of an arm's 1000U starting cash. The two '1U x 4 slots' descriptions in exits150.py and exit_ladder150.py are stale relative to that. Round 63 re-verified against the effective definition that round 57's premise HOLDS (full15/full25 differ in take_profit AND trailing_activate_return) and that the four ladder arms are single-factor (only take_profit).",
  "MEASUREMENT_DISCIPLINE": "Four hypotheses have been killed by measurement across rounds 120-5/120-6 and ONE WAS A BUG IN MY OWN PROBE ('0 of 18 pools can form a window' - the query filtered provider LIKE 'dexscreener%', excluding the strategy-observer mirror rows). None produced a code change; all would have looked like reasonable fixes. ALWAYS take a second independent measure before publishing a '0 samples / stalled / defect' claim or changing code. Also: never LIKE wildcards on arm_id; use julianday() not datetime('now'); dedupe snapshots by (token_id, observed_at); separate windowed from all-time totals.",
  "authoritative_current_numbers_20260912T2027Z": {
    "source": "scripts/supervise_metrics.py --hours 2 AND scripts/trade_context_ledger.py --minutes 180",
    "discovery_per_hour": 3523.7,
    "evaluations_sampled": 17427,
    "admitted": 592,
    "positions": "450 total, 398 settled, 177 write-offs, WIN RATE 8.5%, total -4091.68U, PF 0.03",
    "positions_per_token": 12.75,
    "TOKEN_SIGNAL_RATE": "0.38% (10 of 2641 evaluated tokens reached any arm) - the user's core complaint, precisely located, long-standing (old session baseline 0.57%)",
    "WASHED_OUT": "0 (0.0%) - no position ran >=+50% then closed at a loss, so 'cannot hold a winner' is NOT supported by this device's evidence; the money is lost on never selecting a winner plus friction",
    "only_positive_exit_types": "market_mark_trailing_exit 9 closes +43.06U; alpha149_plateau_stall 5 closes +9.24U",
    "hold_bucket_worst": "5-15m: 215 settled, 7.0% win, -3232.86U (almost all of it the 177 write-offs)",
    "hard_stop_24h": "114 closes, -502.44U, mean -4.407U",
    "unresolved_error_cases": 15
  },
  "cross_session_scripts_that_ALREADY_EXIST": [
    "scripts/supervise_metrics.py - funnel/coverage/economics/capacity/stability + 9 review triggers, writes data/reports/supervise/",
    "scripts/trade_context_ledger.py - per-trade context: discovery source, entry mechanism, MFE, exit reason, hold minutes, washed_out flag",
    "scripts/paired_arm_ab.py - within-cohort paired A/B, token-clustered bootstrap CI, MIN_SETTLED_PER_SIDE=20, prints NOT READY below that",
    "scripts/register_alpha149.py - idempotent append-only arm registration"
  ],
  "confirmed_facts": [
    "Round 120-3 committed and pushed as c493589 (120-2 was 6515595, 120 was 74ea78e) on branch work/2026-09-04-c2c-115000-additive-strategy.",
    "EXIT150 IS LIVE: three exit-carrier arms registered at a TRUE forward frontier (activation_snapshot_id 15928, not the legacy 0), 316 -> 319 policy additions, present in the running manifest (446 policies), and already holding positions.",
    "The carrier contract already existed: alpha149's signal loop emits entries from SPECS only and then clones the first firing entry arm's signal into every EXIT_ARMS entry. So each new arm gets the SAME opportunity as whichever entry arm fired first and only its exit differs - every existing arm is the matched same-signal control. No existing strategy was modified.",
    "THE MONEY DEFECT: per-position peak economic return was p50 +22.7% / p90 +34.8% / max +55.0% (+20% reached by 51.0%, +30% by 32.0%, +60% by 0.0%) while the configured first take-profit tier is +80%. next_tp_index and principal_recovered were 0 on ALL 350 positions - the ladder fired zero times and no profit was ever banked (give-back 4508.94 USD).",
    "The stop sat inside the noise: hard_stop_return -0.20 = a -13.3% price move vs the pools' own p90 30-second move of 9.49%; 44 of 92 hard stops fired within one minute; hard_stop_liquidity_veto_usd was set on 0 of 92 arms.",
    "The three arms vary exactly one thing each: bank15 vs bank25 isolates where to take the first profit; bank15 vs widestop isolates stop width. Each leaves a ~15% moonbag under the trailing stop.",
    "Existing arms provably untouched: alpha149_vol_scaled_exit_v1 d7b2084ffe293867, alpha149_merged_multi_setup_v1 3029187564e9c2f1, alpha149_confirmed_stop_steady_v2 20460e412abb69a2, all take_profit=[] and the same stops as before.",
    "OUTAGE ROOT CAUSE (round 120-2, fixed): collectors.py wrapped condition.wait() in asyncio.wait_for; the timeout cancels the inner wait(), leaving the asyncio.Condition lock released, after which notify_all() raises and the Condition is permanently unusable. Both per-host start gates now pace outside the condition.",
    "THE CONCENTRATION CAP WORKS: fan-out per cohort fell from 35-42 arms to 0-4 and the worst burst from 40 positions in one second to 4; a live pool sits at exactly 8 arms (the cap). Distinct tokens rose 11 -> 13 -> 15 as arms spread to other instruments.",
    "LOOK-AHEAD AUDIT CLEAN (round 120-2): 0 of 14,040 evaluations consume a snapshot observed/ingested/recorded after the decision; 0 of 4,468 rows have a feature timestamped after their own decision; all four ordering invariants hold. The 12 initial flags were a false positive - the running high is seeded with the entry price at open.",
    "The 169 write-offs are 4 real data reads on 4 BSC pools that are genuinely dead (independently re-verified live). The write-off arithmetic books exactly -20.00 and is correct; do not re-open this.",
    "Cash exhaustion is REFUTED: 443/443 arms can open a 20 USDC position (min arm cash 880). shared_available_cash_usd=0.0 is min() of an empty dict.",
    "489718 arm-instances block at await_distinct_dex_trajectory_frame; only 321 of 954 accepted frames (33.6%) yield a usable 30s window because the real cadence is 60-120s.",
    "tests/test_cohort_store.py fails, but it ALREADY fails at pristine HEAD (verified in a separate git worktree of 74ea78e): runtime.py:7763 assumes get_kv never returns None, and a kv value of JSON null breaks it. Pre-existing, not ours."
  ],
  "implemented_this_round": [
    "src/memetrader/exits150.py - three exit-carrier arms with reachable staged ladders (bank15 / bank25 / widestop), paper_only, notional 1.0.",
    "alpha149.py wave 42: additive import merging exits150 into OVERRIDES/EXIT_ARMS; deliberately NOT into SPECS.",
    "store.py register_chain_meme_exit150_experiments() cloning this epoch's own registered exit carrier as the schema template; called from runtime.py's registration block.",
    "tests/test_exit150.py (11 tests); 85 tests pass overall. test_alpha149's blanket 'every EXIT_ARMS entry has trajectory_exit' assertion replaced by the stronger real invariant: every exit arm declares exactly ONE exit mechanism."
  ],
  "open_risks": [
    "src/memetrader/store.py, runtime.py, collectors.py and alpha149.py each carry this round's changes AND pre-existing uncommitted changes from earlier rounds; only new files and records were staged. Staging those four is still pending.",
    "EXIT150's ladder CHOICE is in-sample on one 87-minute window. It must be judged only on data observed after frontier 15928 and must not be promoted on the strength of the window that motivated it.",
    "max_concurrent_positions registered as 2 (inherited from the live carrier template), not the 4 declared in the module. The append-only ledger cannot be updated and 2 is the more conservative bound; recorded so it is not mistaken for a bug.",
    "token_discovery_quote_attempts and source_poll_attempts are 0 rows, so the DexScreener failure mode is inference from a traceback, not proof.",
    "pretrade_rug_safety_registrations covers Solana only and produced 0 assessments.",
    "5 pump_native_absorption_fast_v1 positions bypass the cohort/decision/fill contract (entry_snapshot_id NULL, stake ~4.71) while landing in the same positions table."
  ],
  "rules": [
    "Live stays locked; Paper only; no reset or re-initialization; new strategies join only at their own deployment frontier.",
    "Do not modify or replace existing strategies; improvements arrive as NEW additional strategy modules.",
    "Never loosen strict timing, identity, protocol-validity, account-truth or dead-surface rules to create trades.",
    "Every optimization must be gradual and reversible, and must not break stability, realtime behaviour or accuracy.",
    "RULE 40 (round 86): separate a SELF-IMPOSED cap from a PROVIDER-IMPOSED one, and read the running system's state ABOUT ITSELF. 8 of 9 buckets at cap + 0 rate-limit records across 48 sources + freshest success about 0 s old => the 30-slot cap is ours. Use an in-memory counter read (non_held_by_chain_bucket) - never a truncated panel and never a mixed-stream rate.",
    "RULE 41 (round 86): a list a checkpointer writes is a LOWER BOUND by default. dump_state(..., limit: int = 30) / bounded_summary(..., limit: int = 30) mean the persisted lease count is <= 30 and its tightness is unknowable from the panel - I first read 16 rows as a population. Same family: a CUMULATIVE counter (items += ...) must not be divided by a BOUNDED deque length (sample_count).",
    "RULE 42 (round 86): `>` on a set is SUPERSET-hood, not a size comparison - it is always False for two disjoint sets. `never > held_in_window` sent 280-vs-17 down the wrong branch while the correct counts were printed one line above. Use len() on both sides AND print the counts the branch is deciding on.",
    "RULE 43 (round 86): a QUOTE (mark) is not a FRAME. 280 never-held tokens each received about 14 fresh marks in 15 minutes, yet 81.3% had 0 snapshots in the window, only 2.8% reached >=3 frames and their cumulative snapshot count has median 1. Marks and snapshots are written by different paths, so quote reach never implies observation reach."
  ],
  "ROUND_58_NEXT_ACTION_REWRITTEN": {
    "why": "The next_action field still said the paired basis was 2 diverged cohorts and that the stack projects +17,369U - both stale after round 43 (corrected kernel), round 53 (corrected ceiling), round 57 (same-frontier pairing measured 4 and 14 diverged) and round 58 (the ladder arms deployed). It is the first field a future round reads, so it was rewritten to carry the current P0s, the corrected figures and the full do-not-reopen list."
  }
}
```

Only this revision defines current work. Historical rounds are audit evidence, not an active queue.
