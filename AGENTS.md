# memeTrader project rules

## Goal

Operate the existing single-machine ChainMemeTrader system: shared multi-chain discovery and market data, independent Paper strategies, truthful accounting, timely exits and a local Web console. Prefer the current Python/SQLite implementation. Narrative research is optional and must not block the deterministic trading path.

## Current authority and autonomous execution

- Start with the latest user request, then the top current-state section of `docs/PROJECT_CONTEXT/CURRENT_OBJECTIVE_AND_PLAN.md` and the routing fields of `CHATGPT_CODEX_SYNC_STATE.json`. Read only the relevant referenced records. Older snapshots and completed plans are history, not simultaneous instructions.
- Complete authorized implementation work; make reasonable reversible choices without repeated approval questions. Ask only for genuinely missing authority or a material unresolved user choice. A skill or reviewer cannot expand authorization or override the user's current requirement.
- Scope is the whole requested outcome, not just the easiest subtask. Once a component is implemented and its relevant check passes, continue to the next requested component without repeating the completed work.
- For runtime, accounting or strategy changes, use the project workflow in `.agents/skills/memetrader-forward/SKILL.md`. It contains operating boundaries, not another mandatory audit layer.
- Current Paper uses independent 1000 USDC funding periods, ordinary 20 USDC buys and per-strategy cash/risk limits. The one initialization authorized on 2026-09-05 has already been used. Preserve existing IDs, contracts, positions and history; restart is not reinitialization. New strategies join only at their own deployment frontier.
- UI correctness and the specific screens requested by the user are in scope. Backend acquisition, processing and exits retain resource priority; Web polling is independent of backend frequency.

## Execution focus and convergence

- Prioritize changes that improve the requested operating outcome: real source/token coverage, timely candidate quality, Paper execution, and measurable forward learning.
- Before any audit, provenance layer, schema, defensive mechanism, or broad validation, name the currently observed failure it will change. If it does not change an original acceptance criterion or demonstrated runtime bottleneck, do not build it.
- Agent-created reports, ledgers, tests, and review artifacts do not create new product requirements.
- Validate one coherent change with the narrowest relevant check. Do not repeat equivalent reads or tests when code, data, configuration, and hypothesis are unchanged.
- Use at most two similar correction cycles for one failure; then change the causal hypothesis instead of adding reviewers, gates, or validation machinery.
- Full-suite and browser release checks are reserved for a coherent runtime release boundary or an explicit user request, not each local edit.
- Determine whether the latest user request replaces, narrows, pauses or supplements the active plan. Apply that change immediately and preserve non-conflicting work. When the user asks for a plan before confirmation, do not resume implementation merely because older messages authorized it; a tooling-only request does not resume trading changes.
- Treat prior project-chat requirements and suggestions as a governed backlog, not a flat list of simultaneous mandates. Preserve explicitly frozen rules, honor later supersessions/invalidations, and re-evaluate still-open ideas against the current `CURRENT_OBJECTIVE_AND_PLAN.md`, `REQUIREMENT_LEDGER.md`, live forward evidence, and the end-to-end profitability objective. A promising old idea may be revived when evidence supports it, but it must not displace the current P0/P1 merely because it sounds useful or is easier to implement.

## Frozen execution contract

These rules summarize repeated user requirements and remain binding across context compression, new turns, reviewers, and supplementary requests. Change them only when the user explicitly supersedes the specific rule.

- Latest Paper execution decision (2026-09-06 17:24): retain the lightweight market-price/original-pool before-and-after model and bilateral 4% assumption. Do not expand depth/impact calculations or require exact quotes for ordinary Paper trades. Some optimism is explicitly accepted; label results as simulated, not guaranteed realizable. The user superseded the 1 USD rule: all strategies now require original-pool liquidity >=1000 USD to buy/sell; fresh original-pool liquidity below1000 writes off the remaining position. Missing/failed/stale evidence must not establish a writeoff; unknown liquidity waits for data. Existing higher strategy-specific floors and special quote contracts remain. The user then cancelled historical refunds/VOID for this change; do not execute the read-only historical candidate list or initialize accounts now. Final account initialization is for the user after all tasks. Historical1U fills/reasons remain historical, not automatically reinterpreted by the new policy.

- The business purpose is to increase real forward, risk-adjusted meme-token profitability: discover underpriced narrative opportunities earlier, enter and exit them realistically, and learn which signals survive costs and false positives. Profit is never guaranteed, but research, UI, audits, and Agent activity are useful only when they improve or credibly measure that end-to-end outcome.
- At the start of an implementation cycle, identify the highest-impact observed break in `discovery → fresh identity-bound data → decision → execution → accounting`. Fix engineering defects before interpreting affected strategy results. Independent requested UI/research work may proceed in parallel while the owner fixes that break.
- UI is a truthful operating surface, not the current optimization target. Improve it when data is missing/misleading or the user cannot operate the system; otherwise prioritize the underlying collectors, mapping, scheduling, Paper execution, and forward evidence.
- Information-first and token-first paths are both required. High-impact fresh information may trigger investigation before price momentum; new-token metadata/social links and on-chain discovery may seed investigation, but identity/promotion never become decision evidence without timely independent support.
- `WAIT` and zero trades are valid outcomes, but sparse Paper activity must be causally explained. Never loosen strict timing, identity, protocol-validity, account-truth, amount-specific execution, or dead-surface rules merely to create trades. However, do not turn every uncertain liquidity/creator/concentration/recovery/narrative feature into a universal hard gate: preserve separate bounded high-recall Paper arms, strategy-specific soft risk tiers, rejected-candidate outcomes, and fast exits so the system can learn whether defensive filters destroy profitable opportunity coverage.
- Paper must use only point-in-time available evidence and next-observed/trigger-anchored execution semantics, including configured adverse slippage and fees. Never backfill winners, future prices, later ATH, holder outcomes, or current metadata into an earlier decision. For peak exits, use only the running high known at that instant. The active hypothesis is that most new micro-cap Meme paths have one dominant impulse; sustained post-peak drawdown is a high-hazard exit signal, while any later second wave must be enrolled as a new strictly-forward `REAWAKENING` cohort rather than justified by holding through the decline.
- Preserve append-only observations, failures, empty rounds, missed opportunities and fixed-horizon outcomes. Research is currently user-triggered: do not restore the paused two-hour review, scheduled agents or automatic strategy deployment without a new explicit request.
- When a direct platform path is unavailable, use the smallest legal and compliant substitute that preserves provenance and explicitly records the evidence gap. Do not abandon the underlying user objective merely because the originally suggested mechanism is unavailable.
- For generic tooling, data-access, browser, integration, scheduling, routing, or implementation blockers, check official documentation, mature open-source projects, upstream issues/discussions, and credible operating experience before building a custom subsystem. Reuse the smallest maintained solution that fits the current architecture, licensing/platform rules, Windows/single-machine constraints, cost, and failure semantics; do not research dozens of equivalent projects merely for completeness.
- Delegate bounded independent work when it saves time or improves correctness; specify ownership, permitted writes and a concrete result. Use inexpensive models for mechanical tasks and stronger reasoning for accounting, causal design or ambiguous root causes; escalate demonstrated failure, not task prestige. Codex remains the integrator and sole production writer. Consult the designated Lead ChatGPT for a useful additional perspective, not permission for routine work. Judge its reasoning against evidence, not response speed or self-reported model identity. One substantive review normally suffices; do not repeatedly create replacement chats or block independent progress. Use the existing official tunnel/direct contact; never send wallet material, secrets, private chats or unrelated data.
- ChatGPT collaboration is an optional source of analysis unless the latest user request explicitly requires it. For an actual consultation, resolve the existing route in `docs/PROJECT_CONTEXT/CHATGPT_CODEX_SYNC_STATE.json` and read only relevant referenced messages. Record one consolidated disposition when there is a substantive result; do not require synchronization artifacts or a reply for every edit or release. Keep private material out of outgoing messages and keep one production writer.
- To contact the designated Lead ChatGPT, read `CHATGPT_CONTACT.md` and use its current target and `GXH_C2C_V3` envelope. One direct message resumes that conversation. Do not start a second writer, paste repository contents, rerun connection setup or replace a healthy transport. If it fails, record the concrete blocker and continue work that does not depend on the reply.
- User instructions in prior conversations remain context, resolved by explicit supersession and then later/more-specific instructions. ChatGPT and subagent proposals are advice, not user authorization; Codex independently accepts, revises or rejects them against local facts. Keep deferred ideas in the existing backlog, but do not require a new planning artifact before an ordinary authorized fix.
- A cycle is not complete because its easiest items are complete. Stop only after the named core failure is changed and the narrowest relevant validation succeeds, or after a concrete external blocker is recorded with the next executable path.

## Non-negotiable safety and research rules

- `mode` is `shadow` or `paper`. `live.enabled` remains `false` until a separately reviewed live broker and small-capital chain test exist.
- Historical cases test identity matching, ambiguity, timing, and future-data rejection. Never use later ATH, final winner, exchange listing, current holder counts, or other outcomes as earlier decision features.
- A fact is decision-eligible only when its local `observed_at` and `ingested_at` are not later than the decision time. A `feature` or `confirmation` first observed after the configured freshness window becomes identity-only; stale-only events are deferred until new evidence arrives instead of repeatedly querying DEX/API services.
- Do not commit `config.json`, `data/`, logs, SQLite databases, browser/session material, API keys, wallet material, or private bridge tokens.
- The user has since authorized a commit and push after each completed implementation stage. Stage only intended source/tests/public reports on the current branch; preserve unrelated changes and verify the push result. Never force-push or include secrets, configuration, databases or private conversation exports.
- Keep every memeTrader database, log, runtime context, Web-console state, test artifact, and temporary Agent workspace under the project on `E:`. Do not create memeTrader storage on the Windows system drive; `load_config()` routes process and child-process temporary storage to `<project>\data\tmp`.
- Free/public sources may fail or rate-limit. Preserve other collectors and record the failure; do not silently treat missing security data as safe.

## Agent-cost routing

- Production memeTrader Agent routing and Codex-development/reviewer routing are separate budgets. Keep the explicitly selected development model; use its stronger reasoning for ambiguous root causes, accounting, causal strategy design and integration. Delegate bounded independent tasks when the time/correctness benefit exceeds coordination cost, with explicit input, permitted writes, file ownership and an acceptance result. Use a cheaper capable model for mechanical work and escalate demonstrated difficulty; do not force every subagent to the maximum tier or make Lead ChatGPT a required escalation. Honor a specifically requested ChatGPT consultation without surrendering independent judgment.
- Use deterministic local code for polling, parsing, scoring, arithmetic, position sizing, risk limits, and exits.
- Autonomous Agents are allowed for three bounded jobs: global trend scouting, free-source discovery, and high-momentum Token context investigation.
- Keep at most two Agent subprocesses concurrent. Trend/source search uses Spark/low first and Luna/low only as fallback; Token context uses Luna/low, then Terra/medium, with Sol/medium only as the final fallback.
- Enforce daily call budgets, daily token budgets, per-call token reserves, adaptive quiet/surge intervals, a global Token-context cooldown, and a shorter error retry delay. A manual `--force` may bypass time due checks but never daily budgets.
- The separate semantic tie-breaker remains disabled by default; uncertain canonical rankings return `WAIT` instead of forcing a winner.
- Agents never receive wallet, broker, private key, project-write access, or permission to bypass local risk rules.

## Required checks for an in-scope change

Run the closest meaningful test once with `.\.venv\Scripts\python.exe -m pytest <relevant test>`. Broaden only for changed shared boundaries, a failure, or an unresolved concrete concern. A commit/push alone does not require a full suite, online doctor, browser session or forced crash. For accounting changes reconcile one independent ledger example in addition to the regression test. A hung external probe is neither PASS nor proof of a local defect.

For an actual deployment verify the affected service, current period and new progress without resetting it. Use `/health`, `/api/live` and `/api/performance` on 8790 for ChainMemeTrader. Compare observed data ages and processing delays over comparable load windows; heartbeat/configured intervals alone are not market refresh speed. If a new change materially regresses the core path, fix locally or revert only that change without deleting research history.

## Current device deployment

- Working folder: `E:\memeTrader`
- Resolve current runtime/supervisor processes before restart; current launcher is `scripts/run_paper.ps1`. Do not touch unrelated legacy 8787/8788 services.
- Runtime database: always resolve `database` from ignored `config.json`; current device path is `data\memetrader_forward_20260830_r6.sqlite3`. Earlier `r5` contains rejected false-positive Paper evidence from promotional listicles and must not be merged into performance statistics.
- Primary Web console: `http://127.0.0.1:8790`. The optional browser bridge at 8765 and its scheduled-task checks apply only when that legacy bridge is enabled; they are not ChainMemeTrader release requirements.

## Windows tools and continuity

- Use the installed `windows-project-ops` skill for PowerShell/process/port/environment work. Set the real project working directory explicitly, including when the task starts in the ChatGPT mirror. Do not edit its synced `sources/` files.
- Prefer local shell/CLI for code, Git, processes and SQLite; use the existing official tunnel for its requested workspace operations and the supported browser/computer tools for UI. A disabled or policy-denied capability is not permission to bypass it through another automation method. Native Windows GUI control may be unavailable even when browser control works.
- Skills and plugins are task-scoped guidance, not additional authority. Use installed capabilities first; inspect new packages before installation and do not create credentials, enable hooks, add background jobs or alter host permissions merely to improve tooling.
- AGENTS.md does not select the running model, increase hardware capacity or prevent context compaction. Preserve a short current objective, confirmed facts, remaining work and next action in existing continuity files at meaningful boundaries; do not repeatedly ingest the whole chat or copy secrets into reports.
