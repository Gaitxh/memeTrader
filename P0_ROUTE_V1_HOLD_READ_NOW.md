# P0 — read before finalizing docs or deployment

`docs/PROJECT_CONTEXT/CHATGPT_URGENT_POST_RESTART_HOLD_2026-09-02.md`

Route v1 is now registered but has zero rows. Preserve it as an abandoned immutable version and prevent it from selecting future base-v2 cohorts. The current route-v1 implementation has verified open semantic blockers and the Web summary currently labels legacy-v1 counts as base v2.
